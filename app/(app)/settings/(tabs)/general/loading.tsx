// General settings has been consolidated into Account.
// This loading boundary is kept for Next.js routing compatibility
// but will never be visible (the page redirects server-side).
export default function GeneralSettingsLoading() {
  return null
}
