---
milestone: v4.16
milestone_name: Admin Inbox Consolidation
phases: [154, 155]
audited: 2026-07-06
status: passed
auditor: gsd-integration-checker
---

# v4.16 Admin Inbox Consolidation — Milestone Integration Audit

**Verdict: PASSED.** Both phases wire together correctly into one coherent, working Inbox surface. All 4 requirements (INBOX-01..04) are fully closed end-to-end across phase boundaries, not just individually. One non-blocking finding: a stale, unrelated git worktree left in the repo directory containing pre-milestone code (flagged below, not a phase-154/155 regression).

## Scope

This audit checks the SEAM between Phase 154 (route consolidation + settings) and Phase 155 (master-detail viewer), verifying the combined system, not re-litigating each phase's own already-passed VERIFICATION.md.

---

## Cross-Phase Wiring Summary

**Connected:** 9/9 expected cross-phase connections verified
**Orphaned:** 0 exports/components created but unused
**Missing:** 0 expected connections not found

| # | Connection | Status | Evidence |
|---|-----------|--------|----------|
| 1 | admin-nav.tsx single "Inbox" item to /admin/inbox | WIRED | components/admin/admin-nav.tsx:25, one Inbox-icon entry, MessageCircle import removed, no leftover WhatsApp/WA Templates entries |
| 2 | Old /admin/whatsapp redirects to /admin/inbox | WIRED | app/admin/whatsapp/page.tsx is a 5-line redirect stub |
| 3 | Old /admin/whatsapp-templates redirects to /admin/inbox/settings | WIRED | app/admin/whatsapp-templates/page.tsx is a 5-line redirect stub |
| 4 | Phase 154's /admin/inbox/page.tsx (conversations-only split) wires into Phase 155's two-pane AdminWhatsAppClient | WIRED | page.tsx passes conversations, initialConversationId, filtersSlot, paginationSlot props; client consumes all 4 and renders the two-pane layout, no old table/Sheet markup remains anywhere in the file |
| 5 | Phase 154's Settings gear affordance (Settings2 link in /admin/inbox header) links to Phase 155's untouched /admin/inbox/settings | WIRED | app/admin/inbox/page.tsx lines 75-80 link to /admin/inbox/settings; Phase 155 made zero changes to the settings route (git diff --stat across all 4 phase-155 commits shows zero diff for app/admin/inbox/settings/) |
| 6 | /admin/inbox/settings Accounts tab renders AdminWhatsAppAccounts (relocated component) | WIRED | app/admin/inbox/settings/page.tsx imports from ./admin-whatsapp-accounts (co-located, relocated verbatim in 154-02), renders with configs/senders/companyId props |
| 7 | /admin/inbox/settings Templates tab renders WhatsAppTemplatesPanel (left in place, imported) | WIRED | app/admin/inbox/settings/page.tsx imports from components/admin/whatsapp-templates-panel (unmoved, per plan's explicit discretion choice), renders with templates prop from listTemplates() |
| 8 | lib/actions/admin-whatsapp-accounts.ts mutations revalidate the page that actually shows the mutated data | WIRED | All 6 revalidatePath call sites (lines 157, 211, 329, 365, 447, 513) target /admin/inbox/settings, correctly retargeted from the old /admin/whatsapp |
| 9 | Phase 155's thread pane calls loadAdminConversationThread and renders MessageBubble (both Phase-154-relocated, Phase-155-reused unchanged) | WIRED | admin-whatsapp-client.tsx imports and calls loadAdminConversationThread(selectedId, row?.company_id); MessageBubble imported from components/whatsapp/message-bubble.tsx (untouched by either phase) |

## API / Data-Layer Coverage

**Consumed:** all data-layer functions used by the milestone have live callers
**Orphaned:** none found

| Function | Consumer | Status |
|---|---|---|
| listAdminWhatsAppConversations | app/admin/inbox/page.tsx | CONSUMED |
| parseAdminWhatsAppFilters | app/admin/inbox/page.tsx AND app/admin/inbox/settings/page.tsx (companyId filter passthrough to Accounts tab) | CONSUMED (both phases' pages) |
| loadAdminConversationThread | app/admin/inbox/admin-whatsapp-client.tsx (Phase 155's rewritten call site, URL-selection-driven) | CONSUMED |
| listTemplates | app/admin/inbox/settings/page.tsx | CONSUMED |
| saveWhatsAppAccount / saveWhatsAppSender / setWhatsAppSenderStatus / removeWhatsAppSender (6 revalidatePath sites) | AdminWhatsAppAccounts (settings page) | CONSUMED, revalidation retargeted correctly |

No API/data-layer function was found exported-but-uncalled, and no page fetches data that never renders.

## Auth Protection

Both new/relocated admin pages gate on requireAdmin() before any service-role read:
- app/admin/inbox/page.tsx line 31: await requireAdmin() is the first line of the function body
- app/admin/inbox/settings/page.tsx line 24: await requireAdmin() first line, explicitly commented as "the real access control" since the service-role client below bypasses RLS

Redirect stubs (app/admin/whatsapp/page.tsx, app/admin/whatsapp-templates/page.tsx) correctly omit their own requireAdmin() call, the redirect target re-checks it, so there is no gap: an unauthenticated visitor hitting the old URL still lands on an auth-gated page, not an unprotected one.

**Protected:** 2/2 sensitive pages verified
**Unprotected:** 0

## E2E Flow Traces

### Flow 1: Nav to Inbox to select conversation to thread renders inline (no Sheet)

1. components/admin/admin-nav.tsx renders one "Inbox" nav item to /admin/inbox, CONFIRMED
2. /admin/inbox (Phase 154's conversations-only page) renders Phase 155's two-pane AdminWhatsAppClient, CONFIRMED, no accounts tab / tab-switcher chrome, no table/Sheet markup (grep-confirmed zero matches)
3. Clicking a row calls selectConversation(row), which calls router.replace with scroll:false to update ?conversation=<id>, CONFIRMED, shallow update, page param preserved
4. useEffect on selectedId calls loadAdminConversationThread(selectedId, row?.company_id), sets thread state, renders MessageBubble per message in the right pane on the SAME page, CONFIRMED, no modal/dialog role rendered
5. Footer note "Read-only. Shows up to the last 30 days of messages." renders, CONFIRMED, copy preserved verbatim from the pre-refactor Sheet

**Status: COMPLETE.** Old table+Sheet UI does not exist anywhere in the live tree; this is a genuine replacement, not an addition alongside the old UI.

### Flow 2: Direct link / refresh SSR-selects thread

1. app/admin/inbox/page.tsx line 35 computes initialConversationId from sp.conversation server-side
2. Passed as a prop to AdminWhatsAppClient
3. Client's selectedId derivation reads the URL param first, falling back to initialConversationId, so first client render already has the id (no flash of empty state before the URL param is read)
4. Thread-loading effect fires on mount with that id

**Status: COMPLETE.**

### Flow 3: Empty state (no conversation param)

admin-whatsapp-client.tsx (lines 148-155): when nothing is selected, renders EmptyState (icon MessageSquare, title "Select a conversation") instead of a blank pane. **Status: COMPLETE.**

### Flow 4: Mobile collapse

List pane is hidden on md+ breakpoints when a conversation is selected, shown otherwise; thread pane is flex when selected, hidden on mobile otherwise; Back button (mobile-only, ChevronLeft plus text, 44px touch target) calls clearSelection() which strips the conversation param. **Status: COMPLETE.**

### Flow 5: Gear/Settings affordance to Inbox Settings to both tabs work to Back to Inbox

1. /admin/inbox header renders a Settings2-icon link to /admin/inbox/settings, CONFIRMED
2. Settings page is requireAdmin-gated, fetches configs/senders/templates via Promise.all, renders shadcn Tabs (accounts | templates), SSR-seeded from the tab query param, CONFIRMED
3. Accounts tab renders AdminWhatsAppAccounts (byte-relocated component, same props), CONFIRMED
4. Templates tab renders WhatsAppTemplatesPanel (left in its original components/admin/ location, just imported), CONFIRMED
5. "Back to Inbox" link returns to /admin/inbox, CONFIRMED (app/admin/inbox/settings/page.tsx lines 50-52)

**Status: COMPLETE.**

### Flow 6: Old bookmarked URLs still resolve

/admin/whatsapp redirects to /admin/inbox; /admin/whatsapp-templates redirects to /admin/inbox/settings. Both are the ONLY files remaining at the old paths (no leftover data-fetching logic, no dead components sitting alongside the stubs). **Status: COMPLETE.**

### Flow 7: Integrations WhatsApp credentials, unchanged, reachable independently

app/admin/integrations/whatsapp-config-form.tsx and whatsapp-system-prompt-form.tsx were last modified 2026-06-06 and 2026-06-09 respectively, both predate this milestone's phases (2026-07-05) by weeks. Zero commits from Phase 154 or 155 touch anything under app/admin/integrations/. **Status: COMPLETE, correctly out of blast radius.**

---

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| INBOX-01 | admin-nav.tsx (154) to /admin/inbox route to renders Phase 155's two-pane viewer; old routes redirect to stubs; all hardcoded path literals (filters' router.replace, revalidatePath) retargeted | WIRED | none |
| INBOX-02 | /admin/inbox/page.tsx (154's SSR shell, computing initialConversationId/filters/pagination) to AdminWhatsAppClient (155's two-pane component) to loadAdminConversationThread (154-relocated, unchanged) plus MessageBubble (untouched) | WIRED | none |
| INBOX-03 | /admin/inbox header Settings link (154) to /admin/inbox/settings tabbed page (154) to AdminWhatsAppAccounts plus WhatsAppTemplatesPanel (both reused unchanged) to Back link returns to /admin/inbox (Phase 155's new page, confirming the settings-viewer round-trip survives the mid-milestone UI swap) | WIRED | none |
| INBOX-04 | Data layer (lib/queries/admin-whatsapp.ts, lib/actions/admin-whatsapp star files) plus DB tables (whatsapp_ prefixed) plus app/admin/integrations/whatsapp star files, all provably untouched (git-log/mtime plus git diff --stat across all 11 milestone commits) to all affected test files (unit plus e2e) retargeted and green | WIRED | none |

**Requirements with no cross-phase wiring:** none. All 4 requirements span or depend on both phases' artifacts (INBOX-01/03/04 were Phase 154's own delivery, but INBOX-02's Phase-155 viewer sits inside the exact page/route/nav entry Phase 154 built, and INBOX-03's settings round-trip link target changed between phases, this is precisely the seam this audit exists to check, and it holds).

---

## Test Suite Verification (re-run independently during this audit, not trusted from SUMMARY alone)

    npx vitest run tests/unit/admin/whatsapp-filters.test.ts tests/unit/whatsapp/admin-authority-contract.test.ts tests/unit/settings/tenant-whatsapp-surface.test.ts tests/unit/admin/whatsapp-account-actions.test.ts tests/unit/admin/whatsapp-templates.test.ts
    Result: 5 files / 67 tests passed

- tests/unit/settings/tenant-whatsapp-surface.test.ts line 183 confirmed asserting app/admin/inbox/page.tsx (not the old path), re-read directly, not trusted from SUMMARY text.
- tests/e2e/admin-whatsapp.spec.ts confirmed 13 occurrences of app/admin/inbox, 0 occurrences of app/admin/whatsapp as a route/path string. Confirmed the static-contract assertion at line 231 reads the exact post-refactor call site loadAdminConversationThread(selectedId, row?.company_id), matching Phase 155's real call site, not a stale placeholder.
- Confirmed creds-gating is real (a describe-level test.skip when admin email/password env vars are absent, plus defensive per-test skips when zero seed rows exist) — not silently-omitted tests.

## Pre-Existing Non-Blocking Issues (confirmed accurate, not re-flagged as milestone-blocking)

1. lib/actions/admin-whatsapp.ts JSDoc false-positive: the e2e static-contract test "loadAdminConversationThread contains no update/insert/delete calls" regex-matches the literal word "revalidatePath" inside a doc comment stating the function performs no such call. Confirmed via direct read of the file: the match is inside a comment describing what the function does NOT do, not an actual call. File untouched by both phases (last real edit predates Phase 154 by 26 days, per git blame). Accurately described in both phases' deferred-items.md, no correction needed.
2. 6 pre-existing unrelated full-suite failures (2 blog-rls.test.ts Supabase mock chaining failures, 1 landing-page.test.tsx AuthDialog portal timing failure, 4 Windows parallel-import timeout flakes) — confirmed via grep that none of these files reference admin-whatsapp or admin/inbox, and confirmed the flaky 4 pass in isolated re-runs. Accurately described.
3. 4 new live-nav e2e tests, creds-gated: TEST_ADMIN_EMAIL/TEST_ADMIN_PASSWORD are unavailable in this environment; tests report "skipped" with a clear reason, not silently omitted or falsely passing. Confirmed the skip-gate exists in the source. Accurately described as pending CI/staging execution, not a phase gap.

None of the above block the milestone, they are correctly scoped out of both phases and correctly documented.

## Orphaned / Leftover Artifacts

Within the live working tree (main branch): none found. Specifically checked for:
- A duplicate admin-whatsapp-accounts.tsx left behind at the old app/admin/whatsapp/ location, confirmed absent.
- Old nav entries / the old MessageCircle icon import, confirmed absent.
- Any remaining /admin/whatsapp or /admin/whatsapp-templates route-string literal outside the two redirect stubs, confirmed zero via a repo-wide grep on main (excluding the stub files themselves and one harmless import-path substring match inside the string "whatsapp-templates-panel").
- Leftover table/Sheet markup in the Inbox viewer from Phase 154's "preserve verbatim" table+Sheet UI that Phase 155 was supposed to replace, confirmed fully removed (zero table/Sheet matches in admin-whatsapp-client.tsx).

One finding outside the source tree, flagged for hygiene (not a phase-154/155 regression):

A registered git worktree at .claude/worktrees/hungry-matsumoto-7c0805 (branch feat/hungry-matsumoto-7c0805, last commit 2026-07-05 14:18, a debug session investigating the blog-rls/AuthDialog test failures referenced above) still contains the pre-milestone code: the old two-item nav (WhatsApp/WA Templates with the MessageCircle icon), the old /admin/whatsapp table+filters+accounts page, and the old e2e spec targeting /admin/whatsapp. This is expected: the worktree branched off main before Phase 154 started and was never rebased, merged, or removed. It is not part of the deployed build (worktrees are a local git mechanism, not included in the app's build output) and does not affect production, but it is a stale artifact sitting in the repo directory that will keep surfacing as false-positive "old route reference" hits in any future repo-wide grep. Recommend removing this worktree (and deleting the now-obsolete branch, since its debug findings appear to already be folded into deferred-items.md) as a housekeeping follow-up, not a milestone blocker.

---

## Success Criteria Checklist

- [x] .planning/v4.16-MILESTONE-AUDIT.md created with a clear status (passed)
- [x] Cross-phase wiring explicitly checked (9/9 connections traced through source, not re-stated from SUMMARYs)
- [x] All 4 requirements (INBOX-01..04) confirmed fully closed end-to-end
- [x] Orphaned/leftover files flagged, none in the live tree; one stale out-of-tree git worktree flagged for hygiene

## Final Status

PASSED. Phase 154 and Phase 155 combine into a single, working, read-only Inbox: one nav item, a genuine two-pane master-detail viewer (not the old table+Sheet, fully removed), a working Settings sub-page with both tabs reusing unchanged components, correct redirects for both old bookmarked URLs, and an untouched Integrations credentials surface plus data layer. No integration gaps found. The milestone is ready to ship/archive.
