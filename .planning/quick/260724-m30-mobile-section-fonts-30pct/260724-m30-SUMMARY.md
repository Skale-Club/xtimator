---
phase: quick-260724-m30
status: complete
date: 2026-07-24
harvests: SEED-047
files_modified:
  - components/landing/how-it-works-section.tsx
  - components/landing/features-section.tsx
  - components/landing/final-cta-section.tsx
---

# Summary: +30% mobile text/titles on content sections (harvest SEED-047)

Scaled the substantive text + titles of the landing content sections ~1.3× on
**mobile only** (base, `<640`), pinning `≥640` to the original via `sm:`.

## Changes (base → mobile value; sm: pins tablet/desktop unchanged)
- **how-it-works**: section h2 `clamp(24…)`→`clamp(31…)` +`sm:clamp(24…)`;
  intro p `text-sm`→`text-[18px]`; step h3 `0.9rem`→`1.17rem`; step p
  `0.8rem`→`1.04rem`.
- **features**: section h2 same clamp treatment; intro p `text-sm`→`text-[18px]`;
  card title `text-base`→`text-[21px] sm:text-base`; card desc
  `text-sm`→`text-[18px] sm:text-sm`.
- **final-cta**: h2 base `text-[24px]`→`text-[31px]` (sm/lg clamps already pin);
  p `text-sm`→`text-[18px]` (sm:text-lg pins).

## Recommendation calls (SEED-047 said "every section")
- **HERO skipped** — already large and heavily tuned (quick-260724-t2r 2-row /
  natural-wrap). +30% would regress it / risk overflow. Easy to add if wanted.
- **Trust-bar + eyebrows + feature pill skipped** — compact stat row / micro
  uppercase labels; +30% risks layout breakage or looks odd.

## Verification (computed style, live landing)
- 390px: section h2 = 31px (was 24), card title = 21px (was 16), card desc =
  18px (was 14) → all ~+30% ✓
- 800px: features h2 = 32px, card title = 16px → `sm:` pins hold, tablet
  unchanged ✓

Note: mobile text is intentionally slightly larger than tablet at the 640
boundary (the direct consequence of "mobile only, keep sm/lg") — only visible
when resizing a desktop browser across 640, not on real devices.

## Notes
- Local commit only, not pushed.
