---
phase: quick-260723-img7
plan: 01
type: execute
wave: 1
depends_on: [quick-260723-a7c]
autonomous: true
requirements: []
files_modified:
  - lib/image/webp.ts
  - components/ui/slider.tsx
  - components/admin/image-position-editor.tsx
  - lib/platform-config.ts
  - lib/schemas/admin.ts
  - app/admin/landing/actions.ts
  - app/admin/landing/landing-editor.tsx
  - components/landing/hero-section.tsx
  - components/landing/features-section.tsx
  - components/landing/how-it-works-section.tsx
  - components/landing/landing-page.tsx
  - app/admin/branding/actions.ts
  - app/admin/seo/actions.ts
  - app/(app)/settings/(tabs)/account/page.tsx
  - components/settings/profile-section.tsx
  - lib/actions/settings.ts
  - lib/actions/admin-company.ts
  - lib/actions/company.ts
  - components/onboarding/onboarding-survey.tsx
  - lib/actions/client.ts
  - components/clients/client-sheet.tsx
  - lib/actions/price-book.ts
  - lib/queries/price-book.ts
  - components/price-book/price-book-item-dialog.tsx
  - components/price-book/price-book-list.tsx
  - lib/actions/photo.ts
  - lib/queries/photo.ts
  - components/workspace/photos/photo-drop-zone.tsx
  - components/workspace/photos/photo-card.tsx
  - components/capture/capture-recorder.tsx
  - supabase/migrations/20260723000001_image_position_metadata.sql

must_haves:
  truths:
    - "A shared ImagePositionEditor (zoom slider + drag-to-reposition, {scale,x,y}) and convertImageToWebp() helper exist and are reused across every wired surface"
    - "Every image upload that produces a NEW stored asset converts to WebP server-side, EXCEPT og:image (social-crawler compatibility risk) and favicon (rel=icon format expectations)"
    - "The zoom/drag editor is applied only where cropping actually happens visually (hero, feature cards, step images, avatar, price-book item photo) — not on small object-contain logos or the OG meta image, which have no visible crop surface"
    - "Job-site project photos keep their client-side canvas compression (upload speed on cellular is unaffected) and get a server-side WebP re-encode after upload; AI photo analysis always reads the full original image, never a cropped/zoomed view"
    - "New position-metadata JSONB columns are additive (ADD COLUMN IF NOT EXISTS) and NULL-safe everywhere — no visual change for any existing row until an admin explicitly sets a position"
  artifacts:
    - path: "lib/image/webp.ts"
      provides: "convertImageToWebp(file, quality?) — single sharp-based conversion helper used by every upload action"
      contains: "convertImageToWebp"
    - path: "components/admin/image-position-editor.tsx"
      provides: "Reusable zoom+drag editor, cover/contain fit modes, {scale,x,y} output"
      contains: "ImagePositionEditor"
    - path: "supabase/migrations/20260723000001_image_position_metadata.sql"
      provides: "Nullable position JSONB columns on companies/clients/company_price_book/photos/platform_branding — NOT applied to remote, manual apply per repo convention"
      contains: "logo_position"
  key_links:
    - from: "ImagePositionEditor onChange"
      to: "server action FormData / typed param"
      via: "JSON {scale,x,y}, validated server-side by imagePositionSchema"
      pattern: "imagePositionSchema.safeParse"
---

<objective>
Started as a user request to add a zoom-in/zoom-out slider on the admin landing-page image uploaders. Expanded (per explicit user follow-ups, confirmed via AskUserQuestion at two checkpoints) into: (1) a drag-to-reposition control alongside the zoom, and (2) automatic WebP conversion, both applied to EVERY image-upload surface in the app, not just landing content — including job-site photos captured during the estimate walkthrough.
</objective>

<context>
- Built on top of quick-260723-a7c (How It Works animation restore + admin toggle), which restored components/landing/how-it-works-section.tsx to a state this plan then further modified.
- An Explore-agent audit at the start of this task inventoried every image-upload path in the app (~15 surfaces) and which already converted to WebP (only step/feature landing images did) vs. uploaded raw.
- Design decisions made mid-execution, each with a stated reason (see SUMMARY.md for the full list): og:image and favicon excluded from WebP; logos excluded from the position editor (object-contain, no crop surface, mostly rendered at 24-32px); job-site photos keep client-side compression and get NO drag/zoom editing UI in this pass (data model + display-application only).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foundation — WebP helper, Slider primitive, ImagePositionEditor, migration</name>
  <files>lib/image/webp.ts, components/ui/slider.tsx, components/admin/image-position-editor.tsx, supabase/migrations/20260723000001_image_position_metadata.sql</files>
  <action>Shared sharp-based convertImageToWebp(); shadcn Slider via the unified radix-ui package; ImagePositionEditor with cover/contain fit modes and drag+zoom; additive migration for position JSONB columns on tables that only stored a bare URL string.</action>
  <done>Foundation typechecks clean in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Landing content (hero, step, feature images)</name>
  <files>lib/platform-config.ts, lib/schemas/admin.ts, app/admin/landing/actions.ts, app/admin/landing/landing-editor.tsx, components/landing/hero-section.tsx, components/landing/features-section.tsx, components/landing/how-it-works-section.tsx, components/landing/landing-page.tsx</files>
  <action>Hero image converts to WebP (previously raw). heroImagePosition / howItWorksSteps[].imagePosition / features[].imagePosition added to the existing landing_content JSONB (no migration). Admin scale applies via a wrapping div so it composes with — rather than overrides — hero's fragile cross-breakpoint Tailwind transforms and features' hover-scale effect. Card 2's hardcoded 1.15/-10% quirk becomes the fallback default, overridable per-step.</action>
  <done>tsc clean; 11/12 landing tests (1 unrelated flaky AuthDialog timing test); live SSR HTML verified clean.</done>
</task>

<task type="auto">
  <name>Task 3: Branding, SEO, settings, company/onboarding/client logos, price-book photo</name>
  <files>app/admin/branding/actions.ts, app/admin/seo/actions.ts, app/(app)/settings/(tabs)/account/page.tsx, components/settings/profile-section.tsx, lib/actions/settings.ts, lib/actions/admin-company.ts, lib/actions/company.ts, components/onboarding/onboarding-survey.tsx, lib/actions/client.ts, components/clients/client-sheet.tsx, lib/actions/price-book.ts, lib/queries/price-book.ts, components/price-book/price-book-item-dialog.tsx, components/price-book/price-book-list.tsx</files>
  <action>
WebP conversion added to: platform logo, company logo (settings), admin-quick-create company logo, avatar, price-book item photo. Explicitly NOT converted: og:image (LinkedIn/Facebook WebP compatibility risk — verified via web search), favicon (rel=icon format expectations).
Position editor added to: avatar (circle, object-cover — genuine crop case), price-book item photo. Explicitly NOT added to: platform/company/client logos (object-contain, no crop, rendered at 24-32px), og:image (never rendered as an <img> in-app, meta-tag only).
Two upload paths previously bypassed server actions entirely (client uploaded directly to Supabase Storage, so sharp/WebP was unreachable): onboarding logo and per-client logo. New server actions: uploadOnboardingLogoAction, uploadClientLogoAction. Fixed a pre-existing bug found while rewriting the onboarding path — it stored the bare storage PATH as companies.logo_url instead of the resolved public URL.
  </action>
  <done>tsc clean; price-book unit tests 30/30 (dialog + list).</done>
</task>

<task type="auto">
  <name>Task 4: Job-site project photos</name>
  <files>lib/actions/photo.ts, lib/queries/photo.ts, components/workspace/photos/photo-drop-zone.tsx, components/workspace/photos/photo-card.tsx, components/capture/capture-recorder.tsx</files>
  <action>
New uploadProjectPhoto server action: accepts the client's ALREADY-canvas-compressed blob (compressImage() unchanged — resize to 2000px, JPEG, kept specifically because it keeps job-site cellular uploads small/fast), re-encodes to WebP server-side, uploads, creates the DB row. Both call sites (photo-drop-zone.tsx, capture-recorder.tsx) now hand off to this action instead of uploading directly to storage from the browser. capture-recorder.tsx needed a replacement retry wrapper (uploadProjectPhotoWithRetry) since the existing uploadWithRetry is typed against StorageProvider.upload directly.
Verified WebP is safe for the AI vision pipeline: analyze-photos.ts's getMimeType() already explicitly maps .webp -> image/webp; Gemini/OpenRouter vision models accept WebP natively.
photos.position (display-only) wired into the type/query and applied in photo-card.tsx's grid thumbnail, but NO drag/zoom editing UI was built for job-site photos — a deliberate scope call (capture is a fast, many-photos-per-visit flow; no natural low-friction spot for a crop-editing step without new UI design), not an oversight. Position never affects the stored bytes — AI analysis always reads the full original.
  </action>
  <done>tsc clean; 15/15 relevant unit tests (create-photo-guards, photo-thumbnail-cap, analyze-photos-job).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors after every task
- Landing, price-book, and photo-related unit test suites pass (see per-task done criteria)
- Live dev server SSR HTML confirmed the landing changes render without error
</verification>

<success_criteria>
Every image-upload surface in the app that has a genuine crop use case gets the zoom/drag position editor; every upload that produces a new stored raster asset (except og:image and favicon) converts to WebP server-side; job-site photo capture speed and the AI analysis pipeline are both unaffected. Commits local only, not pushed.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-img7-image-position-webp-everywhere/260723-img7-SUMMARY.md`
</output>
