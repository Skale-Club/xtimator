---
phase: 260705-8u0-estimate-design-template-system-registry
plan: 04
subsystem: ui
tags: [react-hook-form, zod, tailwind-v4, shadcn-radio-group, supabase]

# Dependency graph
requires:
  - phase: 260705-8u0-01
    provides: "lib/estimate/templates/registry.ts — ESTIMATE_TEMPLATES, isEstimateTemplateId, DEFAULT_ESTIMATE_TEMPLATE_ID, EstimateTemplateId"
provides:
  - "CompanySettings.estimate_template_style: string (TS-only until migration lands)"
  - "updateCompanySettings persists estimate_template_style, validated via isEstimateTemplateId guard"
  - "CompanyInfoForm renders a 2-option radio-card picker (Classic/Modern) in Settings > Company tab"
affects: [estimate-design-template-system-pdf-wiring, estimate-design-template-system-share-wiring]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Radio-card picker using has-[[data-state=checked]]:border-primary (Tailwind v4 arbitrary variant, already proven in button.tsx/alert.tsx)"
    - "react-hook-form FormField wired through existing onSubmit FormData build (not a standalone auto-save control like ThemeToggleRadioGroup)"

key-files:
  created: []
  modified:
    - lib/queries/company.ts
    - lib/actions/settings.ts
    - components/settings/company-info-form.tsx

key-decisions:
  - "Radio-card options render tpl.label/tpl.description through t() for i18n consistency with the rest of the form, even though the plan's interface snippet showed raw strings"
  - "Picker placed as a full-width lg:col-span-2 row immediately after Currency in the Main fields grid, per plan's suggested layout"

patterns-established:
  - "Registry-driven settings pickers: map ESTIMATE_TEMPLATES directly to radio-cards rather than hardcoding options, so future template additions require zero changes here"

requirements-completed: [TEMPLATE-SETTINGS]

# Metrics
duration: 12min
completed: 2026-07-05
---

# Phase 260705-8u0 Plan 04: Settings Estimate Template Picker Summary

**Company tab gains a registry-driven Classic/Modern radio-card picker for `estimate_template_style`, wired end-to-end through `updateCompanySettings` with defensive validation, while onboarding stays completely untouched.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-05T10:33:00Z
- **Completed:** 2026-07-05T10:45:25Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- `CompanySettings` interface now declares `estimate_template_style: string`, matching the live-in-types-only column (migration pending user's morning authorization)
- `updateCompanySettings` extracts, validates (`isEstimateTemplateId` guard, falling back to `DEFAULT_ESTIMATE_TEMPLATE_ID`), and persists `estimate_template_style` on every Company tab save
- `CompanyInfoForm` renders a 2-option radio-card picker (Classic/Modern) sourced directly from `ESTIMATE_TEMPLATES`, mirroring `ThemeToggleRadioGroup`'s visual structure but wired through the existing form/schema/onSubmit flow instead of self-persisting on change

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend CompanySettings type and updateCompanySettings action** - `f856c59f` (feat)
2. **Task 2: Add the radio-card template picker to CompanyInfoForm** - `b02c97f` (feat)

**Plan metadata:** (pending — this commit)

## Files Created/Modified
- `lib/queries/company.ts` - `CompanySettings` interface gains `estimate_template_style: string`
- `lib/actions/settings.ts` - `updateCompanySettings` reads/validates/persists `estimate_template_style`; imports `isEstimateTemplateId`/`DEFAULT_ESTIMATE_TEMPLATE_ID` from the registry
- `components/settings/company-info-form.tsx` - `companyInfoSchema` gains `estimateTemplateStyle: z.enum(['classic','modern'])`; `defaultValues`, `onSubmit`'s FormData build, and a new `FormField` (radio-card grid) all wired; imports `ESTIMATE_TEMPLATES`/`DEFAULT_ESTIMATE_TEMPLATE_ID` from the registry and `RadioGroup`/`RadioGroupItem` from `@/components/ui/radio-group`

## Decisions Made
- Ran `t(tpl.label)` / `t(tpl.description)` on the registry's option copy so the picker participates in the existing i18n pipeline like every other label in this form (the plan's interface snippet used raw strings; this is a minor faithful-to-pattern adjustment, not a deviation requiring a rule).
- Confirmed via `git log`/`Grep` that Tailwind's `has-[...]` arbitrary variant is already used in `components/ui/button.tsx` and `components/ui/alert.tsx`, so no fallback conditional-className was needed — used the plan's `has-[[data-state=checked]]:border-primary` selector as-is.

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched the plan's action blocks precisely; no Rule 1-4 triggers encountered.

## Issues Encountered

None. `npx tsc --noEmit -p tsconfig.ci.json` passed cleanly after each task. `npx vitest run tests/unit tests/eval` showed 1 failing test (`tests/unit/company-action.test.ts` > "INSERT branch: writes NO trial clock...", a 5000ms timeout) when run in the full parallel suite; re-running that file in isolation passed 11/11 in 1.87s, confirming it is the pre-existing parallel-only flake in the `company` test file named as a known non-regression in the task constraints — not caused by this plan's changes (this plan never touches `lib/actions/company.ts`).

## Scope Verification

- `git diff --stat` across both task commits confirms `lib/actions/company.ts` (onboarding) is absent from this plan's changeset — new companies continue to get `'classic'` silently via the DB column default, zero onboarding-flow change.
- `Grep` for `estimate_template_style|Estimate Design` across the repo returns only: registry/query/action/type/migration files (data layer, expected), `components/settings/company-info-form.tsx` (the picker itself), and `.planning/` docs. No tour, banner, or badge component references this feature anywhere.
- No new untracked files were produced by this plan's edits (pre-existing untracked files from wave 1/parallel work — `types/database.types.ts`, `supabase/migrations/20260705000001_estimate_design_template.sql`, `lib/queries/share.ts`, `tests/unit/pdf/estimate-pdf-modern-totals.test.tsx` — were left untouched and unstaged, out of this plan's scope).

## User Setup Required

None - no external service configuration required. Note: the `estimate_template_style` column write from this plan's code is a no-op against the live production DB until the user applies the pending migration (`supabase/migrations/20260705000001_estimate_design_template.sql`) tomorrow morning — this is expected per the plan's scope note, not an issue.

## Next Phase Readiness
- The Settings picker is fully wired and ready to persist real data the moment the migration lands.
- Next waves (PDF renderer wiring, share-page wiring) can now read `company.estimate_template_style` from `CompanySettings`/`getEstimateByShareToken` (already typed) to select the correct template component via the registry's `Record<EstimateTemplateId, Component>` map.

---
*Phase: 260705-8u0-estimate-design-template-system-registry*
*Completed: 2026-07-05*

## Self-Check: PASSED

All files confirmed present:
- lib/queries/company.ts
- lib/actions/settings.ts
- components/settings/company-info-form.tsx
- .planning/quick/260705-8u0-estimate-design-template-system-registry/260705-8u0-04-SUMMARY.md

All commit hashes confirmed present in git log:
- f856c59f
- b02c97f
