---
phase: 260705-8u0-estimate-design-template-system-registry
plan: 02
subsystem: pdf
tags: [react-pdf, pdf-templates, estimate, i18n, wcag-contrast]

# Dependency graph
requires:
  - phase: 260705-8u0-01
    provides: "EstimateTemplateId union, isEstimateTemplateId guard, DEFAULT_ESTIMATE_TEMPLATE_ID in lib/estimate/templates/registry.ts"
provides:
  - "components/pdf/estimate-pdf-modern.tsx — EstimatePDFModern editorial PDF template, same EstimatePDFProps/data logic as Classic"
  - "app/api/estimates/[id]/pdf/route.ts — registry-keyed PDF_TEMPLATE_COMPONENTS lookup selecting Classic vs Modern"
  - "lib/queries/estimate.ts — getEstimateWithContext now selects estimate_template_style"
  - "tests/unit/pdf/estimate-pdf-modern-totals.test.tsx — structural totals-order test for Modern"
affects: [estimate-design-template-system-registry-03, estimate-design-template-system-registry-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Registry-keyed Record<EstimateTemplateId, Component> lookup replaces if/else template selection in server routes"
    - "PDF template variants share one EstimatePDFProps contract; only StyleSheet/JSX diverges, all data/helpers/order identical"

key-files:
  created:
    - components/pdf/estimate-pdf-modern.tsx
    - tests/unit/pdf/estimate-pdf-modern-totals.test.tsx
  modified:
    - lib/queries/estimate.ts
    - app/api/estimates/[id]/pdf/route.ts

key-decisions:
  - "Kept brandOnFill (readableTextColor) computed but visually unused in Modern — preserves interface/helper parity with Classic per plan's hard constraint, at the cost of one harmless eslint no-unused-vars warning (not an error, does not fail CI)"
  - "Deferred pre-existing 'element as any' eslint warning in route.ts as out-of-scope (predates this plan's edits, confirmed via git diff HEAD before changes)"

patterns-established:
  - "Modern PDF template: Times-Roman/Times-Bold built-in fonts, thin hairline rules instead of brand-color fills, large standalone hero grand-total, increased whitespace — genuinely different visual identity while trusting the exact same persisted totals/deposit values as Classic"

requirements-completed: [TEMPLATE-PDF]

# Metrics
duration: 12min
completed: 2026-07-05
---

# Phase 260705-8u0 Plan 02: Modern PDF Template Summary

**Cloned the Classic estimate PDF into a serif "Modern" editorial template (Times-Roman/Times-Bold, thin rule dividers, hero grand-total) and wired the PDF route to pick Classic vs Modern via the registry-keyed `PDF_TEMPLATE_COMPONENTS` map — zero data/logic drift from Classic, proven by a mirrored totals-order test.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-05T06:37:00Z (approx, first tool call)
- **Completed:** 2026-07-05T06:49:35Z
- **Tasks:** 3 completed
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments
- `EstimatePDFModern` renders identical totals/labels/order to `EstimatePDF` (Classic) — proven by a dedicated structural test mirroring the Classic totals test exactly, importing `EstimatePDFModern` instead
- PDF route now resolves Classic vs Modern via `Record<EstimateTemplateId, typeof EstimatePDF>`, defaulting any unrecognized/legacy `estimate_template_style` value to `'classic'` — existing companies get byte-identical Classic output
- `getEstimateWithContext`'s company select now includes `estimate_template_style`, closing the data path from DB column (types-only, not yet live) to route selection

## Task Commits

Each task was committed atomically:

1. **Task 1: Add estimate_template_style to the PDF route's company query** - `cef2a386` (feat)
2. **Task 2: Create the Modern PDF template component (TDD)**
   - RED: `5730b91a` (test) — failing test confirmed against non-existent component
   - GREEN: `d2cb6517` (feat) — `EstimatePDFModern` implemented, all 3 totals tests pass
3. **Task 3: Wire the PDF route to select Classic vs Modern via the registry** - `9f4d5398` (see note below)

**Deferred-items log:** `6632b4c4` (chore: document pre-existing out-of-scope lint warning)

_Note: TDD tasks may have multiple commits (test → feat → refactor) — Task 2 followed test → feat, no refactor needed._

### Note on Task 3's commit hash

The working tree for this quick-phase is shared (in-place, no worktree isolation) with a concurrently-running wave-4 executor (plan `260705-8u0-04`, Settings template picker). Task 3's `app/api/estimates/[id]/pdf/route.ts` edit was staged and ready, but before I could commit it standalone, the parallel wave-4 process ran its own commit (`9f4d5398`, message: "docs(260705-8u0-04): complete Settings estimate template picker plan") which — due to the shared working tree — swept up my already-written, uncommitted route.ts diff as part of its tree snapshot.

I verified via `git show 9f4d5398 -- "app/api/estimates/[id]/pdf/route.ts"` that the diff captured is byte-for-byte exactly Task 3's intended change (registry import, `PDF_TEMPLATE_COMPONENTS` map, defensive `templateId` resolution, `createElement(PDFComponent, ...)`) — nothing from wave 4 or any other source was mixed in, and nothing of mine was lost or corrupted. Per git safety protocol, destructive history rewrites to "fix" the attribution were not performed. The code is correctly committed and verified working; only the commit message attribution is mislabeled.

## Files Created/Modified
- `components/pdf/estimate-pdf-modern.tsx` - New Modern editorial PDF template component (`EstimatePDFModern`, default export), consumes `EstimatePDFProps` unchanged
- `tests/unit/pdf/estimate-pdf-modern-totals.test.tsx` - New structural test proving Modern's totals block matches Classic's values/order exactly
- `lib/queries/estimate.ts` - One-line addition of `estimate_template_style` to `getEstimateWithContext`'s companies select
- `app/api/estimates/[id]/pdf/route.ts` - Registry-keyed `PDF_TEMPLATE_COMPONENTS` lookup; resolves `templateId` defensively via `isEstimateTemplateId`/`DEFAULT_ESTIMATE_TEMPLATE_ID`; renders via resolved `PDFComponent` instead of hardcoded `EstimatePDF`

## Decisions Made
- Preserved `brandOnFill` (`readableTextColor(brandColor)`) as a computed-but-JSX-unused value in `EstimatePDFModern`, per the plan's explicit "keep unchanged" list of computed values — Modern's design has no fill backgrounds needing a contrast-derived foreground color, so the value is intentionally inert. This produces one harmless `@typescript-eslint/no-unused-vars` warning (0 errors), which does not fail `tsc` or the test suite.
- Left the pre-existing `renderToBuffer(element as any)` eslint warning in `route.ts` untouched — confirmed via `git diff HEAD` that this cast predates my Task 3 edit and is unrelated to the registry-keyed selection change (scope boundary: only fix issues directly caused by this plan's changes).

## Deviations from Plan

None — plan executed exactly as written across all 3 tasks. The only deviation is administrative (commit attribution noted above), not a code or logic deviation.

## Issues Encountered

**Shared working tree race with a concurrent wave-4 executor.** This quick-phase's plans are being executed in-place (no git worktree isolation, per known Windows MAX_PATH constraint) alongside another active plan (260705-8u0-04, Settings template picker), which independently touches the same `route.ts` file area of the repo (via `.planning/STATE.md`/SUMMARY commits). Mid-Task-3, I observed `.planning/STATE.md` and an unrelated `260705-8u0-04-SUMMARY.md` unexpectedly appear staged alongside my edit; I unstaged those non-task files immediately (`git reset --`) and re-staged only my own file. Moments later the parallel process's own "docs: complete" commit captured my already-written route.ts diff before I could commit it standalone. Verified content integrity via `git show` — the diff is exactly and only my Task 3 change, no corruption, no missing pieces, no cross-contamination from wave 4's own edits. No further action was needed; Classic behavior is unaffected and Modern is fully wired.

## User Setup Required

None - no external service configuration required. Note: the live Supabase migration (`supabase/migrations/20260705000001_estimate_design_template.sql`) has NOT been applied to the database yet, per this plan's explicit scope note — that remains a separate, user-authorized step outside this plan's boundary. `types/database.types.ts` is already patched so TypeScript compiles clean against the eventual schema.

## Next Phase Readiness

- Registry-keyed PDF template selection is live and verified: Classic (`estimate-pdf-totals.test.tsx`, 3 tests) and Modern (`estimate-pdf-modern-totals.test.tsx`, 3 tests) both pass, `npx tsc --noEmit -p tsconfig.ci.json` is clean.
- Plan 03 (share page wiring) and plan 04 (Settings template picker) both build on this same registry and were observed executing concurrently — their commits (`ae2205db`, `6cba27fc`, `9f4d5398`) are visible in the shared log but out of this plan's scope to verify.
- Blocker/concern for a future integration pass: once the Supabase migration is applied live, re-verify the full round trip (Settings picker → DB column → PDF route → rendered PDF) end-to-end, since this plan only proved the PDF route half in isolation with the DB column absent from the live schema.

---
*Phase: 260705-8u0-estimate-design-template-system-registry*
*Completed: 2026-07-05*

## Self-Check: PASSED

- FOUND: components/pdf/estimate-pdf-modern.tsx
- FOUND: tests/unit/pdf/estimate-pdf-modern-totals.test.tsx
- FOUND: .planning/quick/260705-8u0-estimate-design-template-system-registry/260705-8u0-02-SUMMARY.md
- FOUND: .planning/quick/260705-8u0-estimate-design-template-system-registry/deferred-items.md
- FOUND commit: cef2a386 (Task 1)
- FOUND commit: 5730b91a (Task 2 RED)
- FOUND commit: d2cb6517 (Task 2 GREEN)
- FOUND commit: 9f4d5398 (Task 3, mislabeled attribution — see note above)
- FOUND commit: 6632b4c4 (deferred-items log)
