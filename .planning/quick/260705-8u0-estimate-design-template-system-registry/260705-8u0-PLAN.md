---
phase: 260705-8u0-estimate-design-template-system-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/estimate/templates/registry.ts
autonomous: true
requirements: [TEMPLATE-REGISTRY]
must_haves:
  truths:
    - "A single source of truth lists the available estimate design templates (classic, modern) with id/label/description"
    - "Consumers can validate an unknown/legacy string safely falls back to 'classic'"
  artifacts:
    - path: "lib/estimate/templates/registry.ts"
      provides: "ESTIMATE_TEMPLATES array, EstimateTemplateId type, isEstimateTemplateId guard, DEFAULT_ESTIMATE_TEMPLATE_ID constant"
  key_links: []
---

<objective>
Create the estimate design template registry — the single shared source of truth for template ids, used by the PDF route, the share page, and the Settings picker to select the correct component via a `Record<EstimateTemplateId, Component>` map (never inline if/else).

Purpose: Establishes the "meio de campo" scaffolding so adding a future 3rd template is one registry entry + one map entry + one new component file — nothing else touched.
Output: `lib/estimate/templates/registry.ts` with `ESTIMATE_TEMPLATES`, `EstimateTemplateId`, `isEstimateTemplateId`, `DEFAULT_ESTIMATE_TEMPLATE_ID`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<!--
This is a greenfield file — no existing registry pattern to mirror in this codebase.
Downstream plans (02, 03, 04) import EstimateTemplateId / isEstimateTemplateId / DEFAULT_ESTIMATE_TEMPLATE_ID
from this exact path: lib/estimate/templates/registry.ts
-->

<interfaces>
<!-- Contract that plans 02, 03, 04 depend on. Implement EXACTLY this shape. -->

```typescript
// lib/estimate/templates/registry.ts

export interface EstimateTemplateDefinition {
  id: 'classic' | 'modern'
  label: string
  description: string
}

export const ESTIMATE_TEMPLATES: ReadonlyArray<EstimateTemplateDefinition>

export type EstimateTemplateId = EstimateTemplateDefinition['id']

export function isEstimateTemplateId(value: unknown): value is EstimateTemplateId

export const DEFAULT_ESTIMATE_TEMPLATE_ID: EstimateTemplateId // = 'classic'
```

Matches the DB constraint already in the repo (supabase/migrations/20260705000001_estimate_design_template.sql):
`check (estimate_template_style in ('classic', 'modern'))`, `default 'classic'`.

Note: the live migration has NOT been applied yet (blocked by a permission system tonight,
requires the user's morning authorization). `types/database.types.ts` has already been manually
patched to include `estimate_template_style: string` in the companies Row/Insert/Update blocks
to match the migration file, so TypeScript compiles clean against the eventual schema. Do NOT
attempt to apply the migration — that is explicitly out of scope for this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the estimate template registry</name>
  <files>lib/estimate/templates/registry.ts</files>
  <action>
Create `lib/estimate/templates/registry.ts` as a pure, side-effect-free module (no React, no I/O) exporting:

1. `EstimateTemplateDefinition` interface: `{ id: 'classic' | 'modern', label: string, description: string }`
2. `ESTIMATE_TEMPLATES: ReadonlyArray<EstimateTemplateDefinition>` — exactly two entries:
   - `{ id: 'classic', label: 'Classic', description: 'Bold corporate letterhead with brand-colored section headers and a boxed totals table.' }`
   - `{ id: 'modern', label: 'Modern', description: 'Quiet editorial style with serif typography, thin rule dividers, and a large standalone total.' }`
3. `EstimateTemplateId` type derived from `ESTIMATE_TEMPLATES[number]['id']`
4. `isEstimateTemplateId(value: unknown): value is EstimateTemplateId` — a type guard that checks `typeof value === 'string'` and the value matches one of `ESTIMATE_TEMPLATES`' ids. Never throws.
5. `DEFAULT_ESTIMATE_TEMPLATE_ID: EstimateTemplateId = 'classic'` constant.

This mirrors the existing `isSupportedCurrencyCode` / `isSupportedLanguage` guard pattern already used in the codebase (lib/money/currency.ts, lib/i18n/resolve-estimate-language.ts) — a `.includes()` check against a readonly ids array, not a switch statement.

Add a short header comment explaining: this registry is the "meio de campo" — the PDF route and share page look up their template COMPONENT via a `Record<EstimateTemplateId, Component>` map keyed off these ids, never via inline if/else, so a future 3rd template is one registry entry + one map entry + one new component file.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json</automated>
  </verify>
  <done>lib/estimate/templates/registry.ts exists, exports compile clean, isEstimateTemplateId('modern') === true, isEstimateTemplateId('bogus') === false, isEstimateTemplateId(undefined) === false.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit -p tsconfig.ci.json` passes with the new file in place. No other files reference it yet (that happens in plans 02-04) — this plan is purely additive scaffolding.
</verification>

<success_criteria>
- lib/estimate/templates/registry.ts exists with the exact exports listed above
- Type-checks clean
- No existing file imports or behavior is touched
</success_criteria>

<output>
After completion, create `.planning/quick/260705-8u0-estimate-design-template-system-registry/260705-8u0-01-SUMMARY.md`
</output>
