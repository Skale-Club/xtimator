---
phase: 260705-8u0-estimate-design-template-system-registry
plan: 01
subsystem: templates
tags: [typescript, estimate, pdf, registry, type-guard]

# Dependency graph
requires: []
provides:
  - lib/estimate/templates/registry.ts exporting ESTIMATE_TEMPLATES, EstimateTemplateId, isEstimateTemplateId, DEFAULT_ESTIMATE_TEMPLATE_ID
affects: [260705-8u0-02, 260705-8u0-03, 260705-8u0-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Template registry pattern: readonly array of {id,label,description} + derived union type + .includes()-based type guard (mirrors lib/money/currency.ts isSupportedCurrencyCode and lib/i18n/resolve-estimate-language.ts isSupportedLanguage)"

key-files:
  created: [lib/estimate/templates/registry.ts]
  modified: []

key-decisions:
  - "EstimateTemplateId derived via (typeof ESTIMATE_TEMPLATES)[number]['id'] rather than a separate literal union, so the array stays the single source of truth"
  - "isEstimateTemplateId never throws — returns false for any non-matching type (undefined, wrong string, etc.), safe for validating legacy/unknown DB values"

patterns-established:
  - "Consumers (PDF route, share page, Settings picker) select the correct template component via a Record<EstimateTemplateId, Component> map keyed off registry ids — never inline if/else. Adding a 3rd template later is one registry entry + one map entry + one new component file."

requirements-completed: [TEMPLATE-REGISTRY]

# Metrics
duration: 5min
completed: 2026-07-05
---

# Phase 260705-8u0 Plan 01: Estimate Template Registry Summary

**Greenfield `lib/estimate/templates/registry.ts` establishing classic/modern as the single source of truth for estimate design template ids, mirroring the existing currency/language type-guard pattern.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-07-05T10:33:00Z
- **Completed:** 2026-07-05T10:37:40Z
- **Tasks:** 1
- **Files modified:** 1 (created)

## Accomplishments
- Created `lib/estimate/templates/registry.ts` with `ESTIMATE_TEMPLATES` (classic, modern), `EstimateTemplateId`, `isEstimateTemplateId`, `DEFAULT_ESTIMATE_TEMPLATE_ID`
- Verified exact interface contract required by downstream plans 02/03/04
- Verified `isEstimateTemplateId` behavior at runtime: `'modern'` → true, `'bogus'` → false, `undefined` → false

## Task Commits

Each task was committed atomically:

1. **Task 1: Create the estimate template registry** - `d2f9a737` (feat)

**Plan metadata:** pending (this commit, docs: complete plan)

## Files Created/Modified
- `lib/estimate/templates/registry.ts` - New pure module: `EstimateTemplateDefinition` interface, `ESTIMATE_TEMPLATES` readonly array (classic/modern with id/label/description), `EstimateTemplateId` type, `isEstimateTemplateId` type guard, `DEFAULT_ESTIMATE_TEMPLATE_ID` constant ('classic')

## Decisions Made
- Followed plan exactly as specified — mirrored the `isSupportedCurrencyCode` / `isSupportedLanguage` guard pattern (readonly array + `.includes()` check), not a switch statement
- Derived `EstimateTemplateId` from `(typeof ESTIMATE_TEMPLATES)[number]['id']` to keep the array as the sole source of truth (no duplicated literal union)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. This is a pure, side-effect-free TypeScript module (no React, no I/O), so no browser preview was applicable — verification was via `tsc --noEmit` type-check plus a scratch runtime check of the guard function, both of which passed and were removed from the working tree before committing.

## User Setup Required

None - no external service configuration required. Note: the underlying DB migration (`supabase/migrations/20260705000001_estimate_design_template.sql`) is a pre-existing unstaged file from earlier work tonight, not part of this task, and remains unapplied per explicit plan scope (out of scope, blocked pending the user's morning authorization).

## Next Phase Readiness
- `lib/estimate/templates/registry.ts` is in place with the exact exports plans 02, 03, and 04 depend on (`ESTIMATE_TEMPLATES`, `EstimateTemplateId`, `isEstimateTemplateId`, `DEFAULT_ESTIMATE_TEMPLATE_ID`)
- No existing file imports or behavior was touched — purely additive scaffolding
- Downstream plans can now build the `Record<EstimateTemplateId, Component>` maps for the PDF route, share page, and Settings picker

---
*Phase: 260705-8u0-estimate-design-template-system-registry*
*Completed: 2026-07-05*

## Self-Check: PASSED

- FOUND: lib/estimate/templates/registry.ts
- FOUND: d2f9a737
