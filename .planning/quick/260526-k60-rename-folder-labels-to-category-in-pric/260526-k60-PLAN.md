---
phase: quick-260526-k60
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/price-book/price-book-list.tsx
  - lib/i18n/translations.ts
autonomous: true
requirements:
  - QUICK-k60-01
must_haves:
  truths:
    - "User sees 'Category' (or PT/ES equivalent) wherever the Price Book UI previously said 'Folder' in the 7 listed string literals"
    - "All 7 modified strings are rendered through t() from useTranslation() so they translate when the user switches to PT or ES"
    - "Switching language to PT shows 'Nova Categoria', 'Excluir Categoria', etc. for the modified strings"
    - "Switching language to ES shows 'Nueva Categoría', 'Eliminar Categoría', etc. for the modified strings"
    - "No other files, components, variable names, function names, or unrelated strings are changed"
  artifacts:
    - path: "components/price-book/price-book-list.tsx"
      provides: "useTranslation imported + t() wrapped around 7 user-facing strings; internal identifiers (folderId, newFolderDialogOpen, handleCreateFolder, etc.) untouched"
    - path: "lib/i18n/translations.ts"
      provides: "7 new entries each in pt and es blocks (6 Category-related + 'Deleting...' already exists for both — verify and only add if missing)"
  key_links:
    - from: "components/price-book/price-book-list.tsx"
      to: "lib/i18n/use-translation.ts"
      via: "import { useTranslation } from '@/lib/i18n/use-translation'; const { t } = useTranslation()"
      pattern: "useTranslation\\(\\)"
    - from: "components/price-book/price-book-list.tsx"
      to: "lib/i18n/translations.ts"
      via: "t() lookup at render → staticDict[language][text] hit"
      pattern: "t\\('(New Category|Delete Category|Category name\\.\\.\\.|Category deleted|This will delete the category)"
---

<objective>
Rename user-facing "Folder" labels to "Category" in `components/price-book/price-book-list.tsx` and wire them through the existing i18n system (`useTranslation` + `staticDict`) so they follow the active language (EN/PT/ES).

Purpose: Align Price Book copy with the rest of the product (which already uses "Category"), and respect the user's selected language.

Output: 2 modified files — component swaps 7 string literals + adds useTranslation hook; translations dictionary gains 6 PT entries + 6 ES entries for the new Category-related strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@components/price-book/price-book-list.tsx
@lib/i18n/use-translation.ts
@lib/i18n/translations.ts

<interfaces>
<!-- Extracted from lib/i18n/use-translation.ts -->

```ts
export function useTranslation(): {
  t: (text: string) => string
  language: 'en' | 'pt' | 'es'
}
```

Usage convention (from existing components):
```tsx
'use client'
import { useTranslation } from '@/lib/i18n/use-translation'

function MyComponent() {
  const { t } = useTranslation()
  return <Button>{t('Save')}</Button>
}
```

EN fast-path: when language === 'en', t(text) returns text unchanged with zero overhead. So wrapping with t() is safe even before adding PT/ES entries.

Static dictionary shape (from lib/i18n/translations.ts):
```ts
export const staticDict: Record<'pt' | 'es', Record<string, string>>
```

Existing relevant entries to NOT duplicate:
- 'Deleting...' exists in both pt ('Excluindo...') and es ('Eliminando...') blocks — do not re-add
- 'Category' exists in both pt ('Categoria') and es ('Categoría') blocks — do not modify the existing entry, only add the new compound entries below it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap 7 Folder→Category strings with t() in price-book-list.tsx</name>
  <files>components/price-book/price-book-list.tsx</files>
  <action>
    Make the following exact changes in `components/price-book/price-book-list.tsx`. DO NOT touch anything else — no variable renames, no function renames, no unrelated copy changes, no whitespace reformatting beyond what these edits require.

    1) Add import (alphabetical-friendly — place after the existing `@/components/...` and `@/lib/...` imports, e.g. immediately after the `formatMoney` import on line 66):
       ```ts
       import { useTranslation } from '@/lib/i18n/use-translation'
       ```

    2) Inside the `PriceBookList` function component, immediately after `const router = useRouter()` (line 80), add:
       ```ts
       const { t } = useTranslation()
       ```

    3) Line ~229 (inside `handleConfirmDeleteFolder`):
       - BEFORE: `toast.success('Folder deleted')`
       - AFTER:  `toast.success(t('Category deleted'))`

    4) Line ~276 (New Folder button label, inside the header `<Button variant="outline" onClick={() => setNewFolderDialogOpen(true)}>`):
       - BEFORE: `New Folder`
       - AFTER:  `{t('New Category')}`

    5) Line ~569 (Delete Folder confirmation `AlertDialogTitle`):
       - BEFORE: `<AlertDialogTitle>Delete Folder</AlertDialogTitle>`
       - AFTER:  `<AlertDialogTitle>{t('Delete Category')}</AlertDialogTitle>`

    6) Line ~571 (Delete Folder confirmation `AlertDialogDescription` body):
       - BEFORE: `This will delete the folder. Items in this folder must be moved or deleted first.`
       - AFTER:  `{t('This will delete the category. Items in this category must be moved or deleted first.')}`

    7) Line ~581 (`AlertDialogAction` content — pending+non-pending ternary inside the Delete Folder dialog):
       - BEFORE: `{isPending ? 'Deleting...' : 'Delete Folder'}`
       - AFTER:  `{isPending ? t('Deleting...') : t('Delete Category')}`

    8) Line ~591 (`DialogTitle` inside the New Folder dialog):
       - BEFORE: `<DialogTitle>New Folder</DialogTitle>`
       - AFTER:  `<DialogTitle>{t('New Category')}</DialogTitle>`

    9) Line ~594 (`Input` placeholder inside the New Folder dialog):
       - BEFORE: `placeholder="Folder name..."`
       - AFTER:  `placeholder={t('Category name...')}`

    CRITICAL — do NOT change:
    - The `Delete Folder Confirmation` JSX comment on line ~565 (it's a comment, not user-facing copy; leaving it avoids scope creep on naming)
    - The `New Folder Dialog` JSX comment on line ~587 (same reason)
    - Variable names: `folderId`, `folderName`, `folders`, `newFolderName`, `newFolderDialogOpen`, `setNewFolderDialogOpen`, `deletingFolderId`, `setDeletingFolderId`, `deleteFolderDialogOpen`, `setDeleteFolderDialogOpen`, `collapsedFolders`, `setCollapsedFolders`, `renamingFolderId`, `renamingFolderValue`, `adjustFolder`, `handleAdjustFolder`, `handleCreateFolder`, `handleConfirmDeleteFolder`, `handleAdjustClose`, `handleRenameConfirm`, `folderSections`, `groupedByFolder`, `folderItems`, `isVirtual`, etc.
    - Function names: `createFolder`, `updateFolder`, `deleteFolder` (imported from `@/lib/actions/price-book`)
    - Type names: `PriceBookFolder`
    - The Delete Item AlertDialog (lines ~542-563) — its title/description/button are not in scope and must remain untouched
    - The empty-state copy on lines 242-244 ("No price book items yet", description) — not in scope
    - `data-testid` attribute on line 395 (`adjust-btn-folder-${...}`) — purely internal id, not user-visible
    - The internal JSX comment on line 403: `Items inside folder` — comment, not user-facing
    - The `Uncategorized` virtual section name on line 142 — out of scope (this rename is strictly the 7 listed strings)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String -Pattern "price-book-list" | Measure-Object -Line | ForEach-Object { if ($_.Lines -eq 0) { Write-Host "OK: no tsc errors in price-book-list.tsx" } else { Write-Host "FAIL: tsc errors"; exit 1 } }</automated>
  </verify>
  <done>
    - File compiles (`npx tsc --noEmit` shows no new errors for `components/price-book/price-book-list.tsx`)
    - `useTranslation` is imported from `@/lib/i18n/use-translation`
    - `const { t } = useTranslation()` is called inside `PriceBookList`
    - Exactly 7 t() call-sites added at the lines listed above (Grep: `t\\('(Category deleted|New Category|Delete Category|This will delete the category|Deleting\\.\\.\\.|Category name)` returns ≥7 matches in this file)
    - Zero occurrences of the literal strings `'Folder deleted'`, `>New Folder<`, `>Delete Folder<` (the AlertDialog title), `placeholder="Folder name..."`, or the old `Delete Folder` button label outside comments
    - No other variables, functions, or strings have been modified
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Category translation entries to lib/i18n/translations.ts (pt + es)</name>
  <files>lib/i18n/translations.ts</files>
  <action>
    Add the new Category-related entries to both the `pt` and `es` blocks of `staticDict`. Do NOT touch any existing entries; do NOT add `Deleting...` (already present in both blocks at line ~113 for pt and line ~294 for es); do NOT modify the existing `Category` entry (line ~138 pt, line ~319 es — keep it as 'Categoria' / 'Categoría').

    1) In the `pt` block, insert these 5 entries immediately after the existing `'Category': 'Categoria',` line (around line 138). Add a leading comment for clarity:
       ```ts
       // Price Book — folders/categories (quick-k60)
       'New Category': 'Nova Categoria',
       'Delete Category': 'Excluir Categoria',
       'Category name...': 'Nome da categoria...',
       'Category deleted': 'Categoria excluída',
       'This will delete the category. Items in this category must be moved or deleted first.':
         'Isso excluirá a categoria. Os itens desta categoria precisam ser movidos ou excluídos antes.',
       ```

    2) In the `es` block, insert these 5 entries immediately after the existing `'Category': 'Categoría',` line (around line 319). Add a leading comment for clarity:
       ```ts
       // Price Book — folders/categories (quick-k60)
       'New Category': 'Nueva Categoría',
       'Delete Category': 'Eliminar Categoría',
       'Category name...': 'Nombre de la categoría...',
       'Category deleted': 'Categoría eliminada',
       'This will delete the category. Items in this category must be moved or deleted first.':
         'Esto eliminará la categoría. Los elementos de esta categoría deben moverse o eliminarse antes.',
       ```

    SCOPE GUARDS:
    - Do NOT add any other Folder-related entries elsewhere in the file
    - Do NOT add 'Deleting...' — it already exists in both blocks
    - Do NOT modify the existing 'Category' / 'Categoria' / 'Categoría' entry (used by the Notifications feature — Phase 77)
    - Do NOT reformat unrelated entries or change quote style
    - Keep the existing trailing commas and surrounding structure intact
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String -Pattern "translations" | Measure-Object -Line | ForEach-Object { if ($_.Lines -eq 0) { Write-Host "OK: no tsc errors in translations.ts" } else { Write-Host "FAIL: tsc errors in translations"; exit 1 } }</automated>
  </verify>
  <done>
    - File compiles (`npx tsc --noEmit` shows no new errors for `lib/i18n/translations.ts`)
    - Grep for `'New Category': 'Nova Categoria'` returns exactly 1 match in `lib/i18n/translations.ts`
    - Grep for `'New Category': 'Nueva Categoría'` returns exactly 1 match in `lib/i18n/translations.ts`
    - Grep for `'Category deleted': 'Categoria excluída'` returns exactly 1 match
    - Grep for `'Category deleted': 'Categoría eliminada'` returns exactly 1 match
    - Grep for `'This will delete the category` returns exactly 2 matches (one pt, one es)
    - Existing `'Category': 'Categoria',` and `'Category': 'Categoría',` lines remain unchanged
    - `'Deleting...'` entries remain unchanged (not duplicated)
  </done>
</task>

</tasks>

<verification>
End-to-end smoke (manual UAT optional, automated checks are the gate):

1. `npx tsc --noEmit` — passes (no new errors anywhere)
2. `npm run lint -- components/price-book/price-book-list.tsx lib/i18n/translations.ts` — passes (or returns only pre-existing warnings)
3. Grep audit for residual literal "Folder" in user-visible strings in the modified component:
   - `rg "Folder deleted|>New Folder<|>Delete Folder<|placeholder=\"Folder name" components/price-book/price-book-list.tsx` → 0 matches
4. Grep audit confirming t() wraps for the 7 strings (already covered in task `done` lists).
</verification>

<success_criteria>
- 2 files modified (component + translations dict), no other files touched
- All 7 user-facing strings now render through `t()` (EN unchanged; PT/ES served from `staticDict` static dictionary, no API fallback needed)
- No internal identifiers renamed (folderId, newFolderName, handleCreateFolder, etc. all preserved)
- No new TypeScript errors introduced
- Plan executable in a single auto wave (no checkpoints, ~30% context budget)
</success_criteria>

<output>
After completion, create `.planning/quick/260526-k60-rename-folder-labels-to-category-in-pric/260526-k60-SUMMARY.md` summarizing:
- 7 string sites swapped (with line numbers)
- 5 new translation keys × 2 languages added (Deleting... reused, Category reused)
- No internal variable/function renames performed
</output>
