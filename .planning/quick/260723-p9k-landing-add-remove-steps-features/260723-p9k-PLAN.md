---
phase: quick-260723-p9k
plan: 01
type: execute
wave: 1
depends_on: [quick-260723-img7]
autonomous: true
requirements: []
files_modified:
  - lib/schemas/admin.ts
  - app/admin/landing/actions.ts
  - app/admin/landing/landing-editor.tsx
  - components/landing/how-it-works-section.tsx
  - components/landing/features-section.tsx

must_haves:
  truths:
    - "A + button next to the numbered selector on both the How It Works and Features admin tabs adds a new item (up to a max), and a Remove button on the active item's detail panel removes it (down to a min)"
    - "How It Works allows 3-6 steps (was hard-locked to exactly 3); steps beyond index 2 render halo-only since the waveform/typing-dots/camera animations are hardcoded to indices 0/1/2 by design (each matches specific step copy)"
    - "Features already allowed 1-6; the ticker's seamless-loop width, previously hardcoded for exactly 4 items, is now computed from the live item count so 5-6 features don't visibly jump/glitch on mobile"
  artifacts:
    - path: "lib/schemas/admin.ts"
      provides: "howItWorksSteps relaxed from .length(3) to .min(3).max(6)"
      contains: "min(3).max(6)"
    - path: "app/admin/landing/landing-editor.tsx"
      provides: "handleAddStep/handleRemoveStep/handleAddFeature/handleRemoveFeature, keeping the parallel image-state arrays (stepImages/currentStepUrls, featureImages/currentFeatureUrls) in sync with the dynamic field-array length"
      contains: "handleAddStep"
  key_links:
    - from: "+ button (handleAddStep/handleAddFeature)"
      to: "useFieldArray append + parallel image-state array push"
      via: "stepsArray.append / featuresArray.append, mirrored into stepImages/currentStepUrls or featureImages/currentFeatureUrls"
      pattern: "stepsArray.append({ eyebrow: '', ... })"
---

<objective>
User request: "i need a plus button beside the numbers on admin to add more of them" (How It Works + Features numbered selector tabs, screenshotted). Features already supported 1-6 items in the schema; How It Works was hard-locked to exactly 3, each with a specific matching card animation. Clarified via AskUserQuestion: allow How It Works to grow too, with halo-only backgrounds for any step beyond the first 3 (no animation exists for a 4th+ step).
</objective>

<context>
- app/admin/landing/landing-editor.tsx already used react-hook-form's useFieldArray for both arrays (stepsArray/featuresArray) — append/remove were already available, just not exposed in the UI.
- how-it-works-section.tsx's showWave/showCursor/showPhotos are already gated on `i === 0/1/2` specifically — steps beyond index 2 ALREADY fall through to no animation with zero code change; only the schema's `.length(3)` cap and one hardcoded `for (let i = 0; i < 3; i++)` loop in actions.ts's step-image-upload branch needed relaxing.
- Both landing sections have a mobile Ticker component with a hardcoded halfWidth (used to compute the seamless-loop scroll distance) sized for the ORIGINAL fixed item count (3 for steps, 4 for features) — needed to become item-count-driven or a 5th/6th feature (already schema-legal) or 4th+ step (newly legal) would visibly glitch the ticker loop.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax the How It Works step count + fix the hardcoded server loop</name>
  <files>lib/schemas/admin.ts, app/admin/landing/actions.ts</files>
  <action>howItWorksSteps: .length(3) -> .min(3).max(6). actions.ts's step-image-upload loop: `for (let i = 0; i < 3; i++)` -> `for (let i = 0; i < (parsed.data.howItWorksSteps?.length ?? 0); i++)` (mirrors the features loop, which was already dynamic).</action>
  <done>Schema accepts 3-6 steps; server-side image handling covers every step, not just the first 3.</done>
</task>

<task type="auto">
  <name>Task 2: Fix both tickers' hardcoded halfWidth</name>
  <files>components/landing/how-it-works-section.tsx, components/landing/features-section.tsx</files>
  <action>how-it-works: `halfWidth={888}` -> `halfWidth={steps.length * 296}`. features: `halfWidth={1216}` -> `halfWidth={features.length * 304}`.</action>
  <done>Ticker seamless-loop math scales with the actual item count.</done>
</task>

<task type="auto">
  <name>Task 3: Add/remove UI in the admin editor</name>
  <files>app/admin/landing/landing-editor.tsx</files>
  <action>
handleAddStep/handleRemoveStep/handleAddFeature/handleRemoveFeature: call the existing useFieldArray append/remove, mirror the same insert/filter into the parallel stepImages/currentStepUrls or featureImages/currentFeatureUrls state arrays (these track upload state per-index outside react-hook-form and would desync otherwise), bump the uploader remount key, and set the active index to the new/adjacent item.
Dashed "+" button after the numbered selector buttons in both tabs, disabled at MAX_STEPS=6 / MAX_FEATURES=6. Destructive "Remove" button in each item's detail panel header, hidden when at MIN_STEPS=3 / MIN_FEATURES=1 (matches the schema floor).
  </action>
  <done>Admin can add up to 6 / remove down to the schema minimum on both tabs; image upload state stays correctly indexed through adds/removes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- landing tests 12/12
- Live dev server: /admin/landing correctly redirects unauthenticated (307, no server error — confirms the route compiles); public `/` SSR HTML unchanged/clean (animations, step/feature titles all present)
</verification>

<success_criteria>
Admins can grow How It Works to up to 6 steps (extras halo-only) and Features to up to 6 cards from the numbered-selector UI, and remove items back down to each section's minimum, without breaking the mobile ticker's seamless loop or any existing content.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-p9k-landing-add-remove-steps-features/260723-p9k-SUMMARY.md`
</output>
