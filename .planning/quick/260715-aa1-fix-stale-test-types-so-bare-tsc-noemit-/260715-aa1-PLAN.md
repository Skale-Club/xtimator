---
phase: quick-260715-aa1
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [TSCLEAN-01]
files_modified:
  - tests/unit/whatsapp/handler.test.ts
  - tests/unit/whatsapp/handler-inngest-dispatch.test.ts
  - tests/unit/whatsapp/handler-intent-routing.test.ts
  - tests/unit/billing/calibration.test.ts
  - tests/unit/ai/refine-shared-prompt.test.ts
  - tests/unit/estimate/observability.test.ts
  - tests/unit/components/landing-page.test.tsx
  - tests/unit/billing/seat-billing.test.ts
  - tests/unit/chat/route.test.ts
  - tests/unit/estimate/step-runner.test.ts
  - tests/unit/estimate/mobile-line-item.test.tsx
  - tests/unit/inngest/generate-estimate-job.test.ts
  - tests/unit/observability/env-check.test.ts
  - lib/estimate/compute-totals.ts
  - tsconfig.ci.json

must_haves:
  truths:
    - "Bare `npx tsc --noEmit` reports 0 errors (down from 25)"
    - "The CI-scoped typecheck stays clean (0 errors)"
    - "The full unit+eval suite still passes with 3442 passing, 0 failures"
    - "No test was skipped, disabled, or weakened to reach 0 errors"
    - "computeEstimateTotals output is byte-identical for every item that supplies unit_price"
  artifacts:
    - path: "lib/estimate/compute-totals.ts"
      provides: "ComputeTotalsItem with unit_price optional + ?? 0 arithmetic guard"
      contains: "unit_price?: number"
    - path: "tests/unit/estimate/markup-totals.test.ts"
      provides: "MARK-01 goldens, unchanged, now typechecking"
  key_links:
    - from: "tests/unit/whatsapp/*.test.ts"
      to: "getEntitlementsForTier (async)"
      via: "mockResolvedValue + complete Entitlements fixture"
      pattern: "mockResolvedValue"
    - from: "lib/estimate/compute-totals.ts:106-110"
      to: "effectiveUnitPrice"
      via: "?? 0 fallback on the non-derive branch"
      pattern: "item\\.unit_price \\?\\? 0"
---

<objective>
Make bare `npx tsc --noEmit` clean (25 errors → 0), all confined to `tests/`.

Purpose: `tsconfig.ci.json` is scoped to app/lib/components/hooks and EXCLUDES `tests/`, so
this drift is invisible to the CI gate and has been rotting. The vitest suites PASS at runtime
(3442 passing) — these are typecheck-only errors. The goal is to make the types HONEST, not to
make errors disappear.

Output: 3 atomic commits — test-fixture/regex fixes, vitest-4 mock-typing fixes, and one
user-approved source type fix.
</objective>

<context>
@.planning/quick/260715-08i-reorganize-admin-billing-page-platform-a/deferred-items.md

NOTE: the `whatsappEnabled` diagnosis in that deferred-items.md is **WRONG**. `whatsappEnabled`
DOES still exist on `Entitlements` (lib/entitlements.ts:44). See Task 1 for the real cause.

Toolchain: TypeScript 5.9.3, vitest 4.1.4, tsconfig `target: ES2017`.
Baseline verified at planning time: exactly 25 errors, all under `tests/`.
</context>

<interfaces>
<!-- Verified against source at planning time. Use these directly — no exploration needed. -->

lib/entitlements.ts:22 — the FULL required shape (every field is required):
```typescript
export type Entitlements = {
  maxEstimatesPerMonth: number | null
  maxEstimatesPerDay: number | null
  maxPriceResearchPerMonth: number | null
  monthlyCreditGrant: number
  maxPhotosPerEstimate: number
  maxAudioMinutesPerEstimate: number
  whatsappEnabled: boolean   // <- STILL EXISTS. deferred-items.md is wrong.
  pdfEnabled: boolean
  priceBookEnabled: boolean
  customDomainEnabled: boolean
  chatEnabled: boolean       // <- MISSING from all 5 whatsapp fixtures
}
```

lib/entitlements-server.ts:27 — `getEntitlementsForTier` is **async** ⇒ `Promise<Entitlements>`.

lib/ratelimit.ts:68 — `retryAfter` IS real; the fixture is correct, the mock's inferred type is not:
```typescript
export interface RateLimitResult {
  allowed: boolean
  count: number
  max: number
  retryAfter?: number
}
```

lib/estimate/compute-totals.ts:27 — `unit_price: number` is REQUIRED, but the implementation at
:105 guards `typeof item.unit_price === 'number' && item.unit_price > 0` — a check only meaningful
if the field can be absent. The TYPE is wrong; the test is right (Cluster 8).

lib/billing/billing-config.ts:41 — `TierBilling` requires 8 fields:
`monthlyCreditGrant`, `subscriptionPriceCents`, `subscriptionPriceAnnualCents`, `includedSeats`,
`stripePriceIdMonth`, `stripePriceIdYear`, `entitlements`, `featureBullets`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix drifted test fixtures, a missing import, and ES2017-illegal regex flags</name>
  <files>
    tests/unit/whatsapp/handler.test.ts,
    tests/unit/whatsapp/handler-inngest-dispatch.test.ts,
    tests/unit/whatsapp/handler-intent-routing.test.ts,
    tests/unit/billing/calibration.test.ts,
    tests/unit/ai/refine-shared-prompt.test.ts,
    tests/unit/estimate/observability.test.ts,
    tests/unit/components/landing-page.test.tsx
  </files>
  <action>
**Cluster 1 — whatsapp handler tests (5 errors).** ROOT CAUSE (verified, NOT what
deferred-items.md says): `getEntitlementsForTier` is `async`, so the mock's expected type is
`Promise<Entitlements>`. Against a Promise, EVERY object key is excess, and TS2353 just names the
first one positionally (`whatsappEnabled`) — hence the bogus "field no longer exists" reading.

Change `mockGetEntitlements.mockReturnValue({...})` → `mockResolvedValue({...})` at these 5 sites:
  - tests/unit/whatsapp/handler-inngest-dispatch.test.ts:132 and :231
  - tests/unit/whatsapp/handler-intent-routing.test.ts:130
  - tests/unit/whatsapp/handler.test.ts:142 and :289

Do NOT touch the many unrelated `vi.fn().mockReturnValue({...})` Supabase query-builder chains in
these same files — those are correct (sync builders). ONLY the 5 `mockGetEntitlements` sites.

Once switched, the literal is checked against `Entitlements` properly and each of the 5 fixtures
will surface a MISSING `chatEnabled: boolean` (verified: none of the 5 have it). Add it, matching
each fixture's apparent tier shape (no assertion in these whatsapp tests reads `chatEnabled`, so
this is runtime-inert; match the shape for honesty):
  - handler.test.ts:142 (priceBookEnabled: true → pro-shaped) → `chatEnabled: true`
  - handler.test.ts:289 (priceBookEnabled: false → free-shaped) → `chatEnabled: false`
  - handler-inngest-dispatch.test.ts:132 (priceBookEnabled: true) → `chatEnabled: true`
  - handler-inngest-dispatch.test.ts:231 (priceBookEnabled: false) → `chatEnabled: false`
  - handler-intent-routing.test.ts:130 (priceBookEnabled: true) → `chatEnabled: true`
Re-run tsc and add any OTHER field the compiler reports missing against the full `Entitlements`.

**Cluster 2 — tests/unit/billing/calibration.test.ts (2 errors, :151 and :172).** Fixtures build
partial `TierBilling` objects `{ monthlyCreditGrant, subscriptionPriceCents }`; the type now needs
6 more fields. Spread the corresponding default tier and override ONLY the two fields each test
cares about — this preserves intent exactly (both tests only exercise grant-vs-price):
  - Fixture 1 (~lines 145-148): the `tiers` object replaces all three tiers with partials. Give
    each its own spread: `free: { ...DEFAULT_BILLING_CONFIG.tiers.free, monthlyCreditGrant: 5000,
    subscriptionPriceCents: 0 }`, and likewise for `pro` (1000/2900) and `business` (3000/9900).
    This test asserts `res.tiers.free.skipped === true` — the free override MUST keep
    `subscriptionPriceCents: 0` so the zero-price skip still fires.
  - Fixture 2 (~lines 167-169): already has `...DEFAULT_BILLING_CONFIG.tiers`; add the per-tier
    spread to `pro` (1000/2900) and `business` (3000/9900). Keep the hand-computed ratio comments
    and the `toBeCloseTo(0.0766, 3)` assertions untouched.

**Cluster 4 — regex `s` (dotAll) flag needs ES2018 (4 errors).** Do NOT change tsconfig `target`
(raising it repo-wide would affect production emit — out of scope). Fix test-locally:
  - tests/unit/ai/refine-shared-prompt.test.ts:49 —
    `/buildSystemPrompt\([^)]*mode:\s*['"]refine['"]/s`. NOTE: this pattern contains NO `.` at all,
    so the `s` flag is already a no-op. Just DROP the `/s` flag. Pure no-op, no rewrite needed.
  - tests/unit/estimate/observability.test.ts:38, :58, :66 — these DO use `.`. Replace each `.`
    (the dot-star wildcards only — not literal dots) with `[\s\S]` and drop the `/s` flag:
      :38 `/tags.*web|metadata.*web|'web'.*estimate-engine|estimate-engine.*web/s`
          → `/tags[\s\S]*web|metadata[\s\S]*web|'web'[\s\S]*estimate-engine|estimate-engine[\s\S]*web/`
      :58 and :66 `/langfuseSessionId.*transcript|transcript.*langfuseSessionId/s`
          → `/langfuseSessionId[\s\S]*transcript|transcript[\s\S]*langfuseSessionId/`
    :58/:66 are `.not.toMatch` assertions — a BROADER pattern is the safe direction there, and
    `[\s\S]*` is exactly equivalent to `.*` under `/s`, so the assertions are unchanged.

**Cluster 5 — tests/unit/components/landing-page.test.tsx:134.** `afterEach` is used but not
imported. Add it to the existing vitest import on line 1:
`import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "whatsapp/handler|billing/calibration|refine-shared-prompt|estimate/observability|components/landing-page" | wc -l</automated>
    Expect 0. Then confirm no regression in these suites:
    <automated>npx vitest run tests/unit/whatsapp tests/unit/billing/calibration.test.ts tests/unit/ai/refine-shared-prompt.test.ts tests/unit/estimate/observability.test.ts tests/unit/components/landing-page.test.tsx</automated>
  </verify>
  <done>
    12 of the 25 errors gone (5 whatsapp + 2 calibration + 4 regex + 1 afterEach). All named
    suites still green with an unchanged passing count. No assertion text altered.
    Commit: `test(quick-260715-aa1): fix drifted fixtures and ES2017-illegal regex flags`
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix vitest-4 mock typing drift and a non-optional `delete`</name>
  <files>
    tests/unit/billing/seat-billing.test.ts,
    tests/unit/chat/route.test.ts,
    tests/unit/estimate/step-runner.test.ts,
    tests/unit/estimate/mobile-line-item.test.tsx,
    tests/unit/inngest/generate-estimate-job.test.ts,
    tests/unit/observability/env-check.test.ts
  </files>
  <action>
**Cluster 3a — tests/unit/billing/seat-billing.test.ts:92** (TS2556). `syncSeatItemSpy =
vi.fn(async () => {})` takes 0 args but is invoked via `(...args: unknown[]) =>
syncSeatItemSpy(...args)`. Declare it to accept the rest args:
`const syncSeatItemSpy = vi.fn(async (..._args: unknown[]) => {})`.
This should also make `.mock.calls[0]` widen from `[]` to `unknown[]`, legalising the 3-tuple casts
at :223 and :291 (TS2352). Re-run tsc to confirm; if either cast still errors, fix that cast
narrowly — do NOT reach for `as any`.

**Cluster 7 — tests/unit/chat/route.test.ts (2 errors).** BOTH are one root cause. The mock
`const rateLimitMock = vi.fn(async () => ({ allowed: true, count: 1, max: 20 }))` has its return
type INFERRED from the default impl, so it (a) accepts 0 args while being spread-called at :67, and
(b) has no `retryAfter`, breaking `mockResolvedValueOnce({..., retryAfter: 30 })` at :158.
`retryAfter` IS real (`RateLimitResult.retryAfter?: number`, lib/ratelimit.ts:75) and the test
asserts `res.headers.get('Retry-After')` is `'30'` — so do NOT drop it from the fixture; that
would break the test at runtime. Instead type the mock against the real contract:
```ts
import type { RateLimitResult } from '@/lib/ratelimit'
const rateLimitMock = vi.fn(
  async (..._args: unknown[]): Promise<RateLimitResult> => ({ allowed: true, count: 1, max: 20 })
)
```
A type-only import is erased at runtime, so it does NOT interfere with the `vi.mock('@/lib/ratelimit', ...)`
factory. This single change fixes both :67 and :158.

**Cluster 3b — tests/unit/estimate/step-runner.test.ts:50.** `StepRunner.run` is generic
(`<T>(name, fn: () => Promise<T>) => Promise<T>`); a `vi.fn` can't carry the generic, so its return
collapses to `Promise<unknown>`. This test only asserts `typeof graph.invoke === 'function'` — it
makes NO assertion on the spy — so drop the spy and type the object directly:
`const runner: StepRunner = { run: (_n, fn) => fn() }` (import the `StepRunner` type if not already).

**Cluster 3c — tests/unit/estimate/mobile-line-item.test.tsx:143, :149, :153.** vitest 4 changed
`Mock<TArgs, TReturn>` to the single-type-arg `Mock<TFunc>`. Rewrite in the function-type form:
```ts
const onUpdate = vi.fn<
  (
    field: 'description' | 'quantity' | 'unit' | 'unit_price' | 'discount' | 'taxable',
    value: string | number | boolean | null
  ) => void
>()
const onRemove = vi.fn<() => void>()
```
CRITICAL — preserve this test's intent: the comment above line 143 states the hand-written union
exists SO THAT `tsc --noEmit` fails if the `ItemCardMobile` prop signature drifts. Do NOT "simplify"
this by deriving the type from the component (e.g. `ComponentProps<typeof ItemCardMobile>['onUpdate']`)
— that would auto-follow drift and silently destroy the very check this test exists to provide.
Keep the union written out by hand. Fixing :143 and :149 should clear the :153 assignment error.

**Cluster 3d — tests/unit/inngest/generate-estimate-job.test.ts:209.** `ReturnType<typeof vi.fn>`
is `Mock<Procedure | Constructable>`, which isn't callable (TS2348). Replace the cast with
`vi.mocked(buildEstimateGraph)`, which preserves the real signature and IS callable:
```ts
const mockGraph = vi.mocked(buildEstimateGraph)
const graphInstance = mockGraph.mock?.results?.[0]?.value ?? mockGraph()
```
Keep the surrounding comment and the call-count contract assertions exactly as they are.

**Cluster 6 — tests/unit/observability/env-check.test.ts:72, :82** (TS2790). VERIFIED: the
validator uses a falsy check (`!process.env[name]`, lib/observability/env-check.ts:39/41), so `''`
WOULD read as missing — but `Reflect.deleteProperty` preserves the exact `delete` semantics with no
ambiguity and typechecks cleanly. Use it:
  - :72 `delete process.env.NEXT_PUBLIC_SUPABASE_URL`
        → `Reflect.deleteProperty(process.env, 'NEXT_PUBLIC_SUPABASE_URL')`
  - :82 `delete process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
        → `Reflect.deleteProperty(process.env, 'NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY')`
Only :72 and :82 error (the adjacent `NEXT_PUBLIC_SUPABASE_ANON_KEY` delete is already optional in
the env type). Convert ONLY the erroring lines; leave the rest untouched. These tests assert
`validateProductionEnv()` THROWS when the var is MISSING — `Reflect.deleteProperty` keeps that
semantics identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "seat-billing|chat/route|step-runner|mobile-line-item|generate-estimate-job|env-check" | wc -l</automated>
    Expect 0. Then confirm no regression:
    <automated>npx vitest run tests/unit/billing/seat-billing.test.ts tests/unit/chat tests/unit/estimate/step-runner.test.ts tests/unit/estimate/mobile-line-item.test.tsx tests/unit/inngest/generate-estimate-job.test.ts tests/unit/observability/env-check.test.ts</automated>
  </verify>
  <done>
    A further 12 errors gone (3 seat-billing + 2 chat + 1 step-runner + 3 mobile-line-item +
    1 generate-estimate-job + 2 env-check). Only the markup-totals error remains (1 of 25).
    All named suites green, unchanged passing count. No `as any` / `@ts-expect-error` / `@ts-ignore`
    introduced anywhere.
    Commit: `test(quick-260715-aa1): fix vitest-4 mock typing and non-optional delete`
  </done>
</task>

<task type="auto">
  <name>Task 3: Make ComputeTotalsItem.unit_price optional (approved source fix) and refresh the stale CI-config comment</name>
  <files>lib/estimate/compute-totals.ts, tsconfig.ci.json</files>
  <action>
**Cluster 8 — GENUINE SOURCE BUG. The user has explicitly approved THIS ONE source change**,
overriding the general no-source-change rule. It is the only production-source edit authorized in
this plan.

`tests/unit/estimate/markup-totals.test.ts:14` passes `{ quantity: 2, cost: 80, markup_pct: 25 }`
with NO `unit_price` — which is MARK-01's entire point (the server derives unit_price from
cost × (1 + markup_pct/100) when the AI gave no explicit price). But `ComputeTotalsItem` declares
`unit_price: number` as REQUIRED, while the implementation itself guards with
`typeof item.unit_price === 'number' && item.unit_price > 0` (:105) — a check that is only
meaningful if the field can be absent. The TYPE is wrong; the test is right. Do NOT edit the test.

1. lib/estimate/compute-totals.ts:29 — make it optional:
   `unit_price?: number` (keep it above the `discount?` line; leave every other field alone).

2. lib/estimate/compute-totals.ts:106-110 — `effectiveUnitPrice` must never become `undefined`.
   Add a `?? 0` fallback on the non-derive branch ONLY:
   ```ts
   const effectiveUnitPrice =
     hasMarkup && !hasExplicitPrice
       ? Math.round((item.cost as number) * (1 + (item.markup_pct as number) / 100) * 100) / 100
       : item.unit_price ?? 0
   ```
   This MUST be a strict runtime no-op: when `unit_price` IS supplied the branch returns it
   unchanged, so `lineGross` (:110) and every downstream golden stay byte-identical. The `?? 0` only
   fires in the previously-untypeable "no unit_price AND no markup" case, which already produced
   `NaN` — `0` is strictly better and no existing test exercises it.
   Do NOT touch the `hasExplicitPrice` guard (:105), the `Math.round(x * 100) / 100` expressions,
   or the `{ ...item, unit_price: effectiveUnitPrice, total: lineNet }` return (:115). The
   byte-identity discipline documented at the top of the file is load-bearing.

3. tsconfig.ci.json — the header comment claims "A full-repo `tsc --noEmit` is ALREADY red: 9
   pre-existing errors, ALL in tests/**" and justifies the scoping on that basis. Once this task
   lands that is false (it is 25 today, 0 after). Update ONLY the comment text to state that the
   full-repo typecheck is now clean and that this config remains the CI gate because it scopes to
   shipped production source. Do NOT change `include`, `exclude`, `extends`, or any other key —
   the scope stays exactly as-is and this edit changes zero compiler behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Expect 0 errors — this is the whole point of the task.
    <automated>npx vitest run tests/unit/estimate</automated>
    Every estimate golden must stay green — this proves the `?? 0` change is a runtime no-op.
  </verify>
  <done>
    Bare `npx tsc --noEmit` exits 0 with 0 errors. `ComputeTotalsItem.unit_price` is optional,
    the arithmetic is guarded, and every MARK-01 / compute-totals golden still passes unchanged.
    Commit: `fix(estimate): make ComputeTotalsItem.unit_price optional to match MARK-01 semantics`
  </done>
</task>

</tasks>

<verification>
Run all three gates from the repo root, in order. ALL THREE must pass.

1. Bare typecheck — the objective:
   `npx tsc --noEmit`
   → MUST report 0 errors (baseline was exactly 25).

2. CI-scoped typecheck — must not regress:
   `npx tsc --noEmit -p tsconfig.ci.json`
   → MUST stay clean (0 errors).

3. Full suite — must stay green:
   `npx vitest run tests/unit tests/eval`
   → MUST be 3442 passing, 0 failures.
   A DROP in the passing count is a FAILURE, not a pass — it would mean a test was silently
   skipped or disabled rather than fixed. Compare the number, not just the exit code.
</verification>

<success_criteria>
- [ ] `npx tsc --noEmit` → 0 errors (was 25)
- [ ] `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors (unchanged)
- [ ] `npx vitest run tests/unit tests/eval` → 3442 passing, 0 failures (count NOT lower)
- [ ] Exactly one production source file changed: `lib/estimate/compute-totals.ts` (user-approved)
- [ ] `tsconfig.json` `target` unchanged (still ES2017)
- [ ] `tsconfig.ci.json` `include`/`exclude` unchanged (comment text only)
- [ ] Zero `as any`, `@ts-expect-error`, or `@ts-ignore` introduced
- [ ] Zero tests skipped, deleted, or `.skip`/`.todo`'d
- [ ] Every test's assertion intent preserved (notably: the hand-written union in
      mobile-line-item.test.tsx, `retryAfter: 30` in chat/route.test.ts, `skipped === true` in
      calibration.test.ts, and the missing-env THROW semantics in env-check.test.ts)
- [ ] 3 atomic commits
</success_criteria>

<output>
On completion, note in the quick task record:
- Final error count for each of the 3 gates
- That deferred-items.md's `whatsappEnabled` diagnosis was wrong (real cause: async
  `getEntitlementsForTier` ⇒ `Promise<Entitlements>` ⇒ every key reads as excess)
- That Cluster 8 was a genuine source-type bug, fixed with user approval
- Any error left unfixed and why (per the constraint: if a fix isn't obvious, leave it and report)
</output>
