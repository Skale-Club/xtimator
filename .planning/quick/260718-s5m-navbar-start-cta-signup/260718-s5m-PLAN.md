---
phase: quick-260718-s5m
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - components/landing/top-nav-auth.tsx
  - components/landing/top-nav.tsx
  - components/landing/landing-page.tsx

must_haves:
  truths:
    - "The landing navbar shows a Start button to the RIGHT of Login (logged-out state only), styled with the same blue gradient primary variant as the hero Start button"
    - "Navbar Start opens the auth dialog in signup mode; Login keeps opening login mode; hero Start keeps opening signup mode (already wired via onOpenAuth('signup'))"
    - "Navbar Start label comes from the same DB-driven content.ctaLabel as the hero (falls back to 'Start' when not provided)"
    - "Logged-in state unchanged (avatar dropdown only); home-cacheability source pins unaffected (no useSearchParams, client auth enhancement intact)"
  artifacts:
    - path: "components/landing/top-nav-auth.tsx"
      provides: "Primary-variant Start button + openSignup(), wrapped with Login in a flex row"
      contains: "variant=\"primary\""
    - path: "components/landing/landing-page.tsx"
      provides: "ctaLabel threaded into TopNav"
      contains: "ctaLabel={content.ctaLabel}"
  key_links:
    - from: "TopNavAuth Start button"
      to: "AuthDialog signup mode"
      via: "openSignup() → onOpenAuth('signup') when provided (LandingPage.openAuth), else local dialog state with mode 'signup'"
      pattern: "onOpenAuth\\('signup'\\)"
    - from: "LandingPage content.ctaLabel"
      to: "TopNavAuth ctaLabel prop"
      via: "TopNav pass-through prop"
      pattern: "ctaLabel"
---

<objective>
Per user request: navbar gains a Start button to the right of Login, in "the same blue square" (gradient-brand primary) as the hero CTA; Start opens sign-up, Login opens sign-in. Hero Start already opens signup (onOpenAuth('signup') → AuthDialog initialMode) — no hero change needed. Note: tests/e2e/auth-modal.spec.ts already expects a nav Start CTA ("per landing-nav.tsx", a since-removed component) — this restores that intended behavior.
</objective>

<context>
- components/landing/top-nav-auth.tsx — logged-out branch renders only the Login text button; fragment sits as 2nd child of TopNav's justify-between row, so Login+Start must be wrapped in one flex div to stay grouped right
- components/landing/top-nav.tsx — pass-through props
- components/landing/landing-page.tsx — owns content; add ctaLabel={content.ctaLabel} (source pinned by tests/unit/seo/home-cacheability.test.ts — additions don't touch pinned patterns)
- Button variant="primary" = gradient-brand shimmer (hero Start look); size="sm" (36px) fits the 64px navbar
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navbar Start CTA opening signup</name>
  <files>components/landing/top-nav-auth.tsx, components/landing/top-nav.tsx, components/landing/landing-page.tsx</files>
  <action>
1. top-nav-auth.tsx: add `ctaLabel?: string` prop (default 'Start'); import Button; add openSignup() mirroring openLogin() with 'signup'; wrap Login + new `<Button variant="primary" size="sm" className="px-4">` in `<div className="flex items-center gap-4">` (Start AFTER Login).
2. top-nav.tsx: add optional ctaLabel prop, pass to TopNavAuth.
3. landing-page.tsx: pass ctaLabel={content.ctaLabel} to TopNav.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/components/landing-page.test.tsx tests/unit/seo/home-cacheability.test.ts</automated>
    <manual>Dev-server DOM: navbar shows Login then Start (primary variant) right of it; hero Start unchanged</manual>
  </verify>
  <done>Navbar Start (blue primary, right of Login) opens signup; label DB-driven; typecheck + tests green (modulo the pre-existing ?auth=login flake documented in quick-260718-q2v).</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code only, then planning artifacts) — DO NOT PUSH</name>
  <files>components/landing/top-nav-auth.tsx, components/landing/top-nav.tsx, components/landing/landing-page.tsx</files>
  <action>
- Stage exactly the three files; commit on `dev`: `feat(quick-260718-s5m): add navbar Start CTA opening signup right of Login`
- Then SUMMARY.md + STATE.md row; commit: `docs(quick-260718-s5m): add quick-task plan/summary and STATE row`
- Do NOT push.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1 # code commit touches exactly 3 files</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- landing-page + home-cacheability tests pass (except documented pre-existing flake)
- DOM: nav order = logo … Login, Start; Start has data-variant="primary"
</verification>

<success_criteria>
Navbar shows a blue-gradient Start button right of Login that opens the signup dialog; Login opens sign-in; hero unchanged; commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-s5m-navbar-start-cta-signup/260718-s5m-SUMMARY.md`
</output>
