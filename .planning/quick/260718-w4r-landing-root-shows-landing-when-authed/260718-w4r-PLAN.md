---
phase: quick-260718-w4r
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - proxy.ts
  - tests/unit/middleware.test.ts
  - tests/e2e/landing-page.spec.ts

must_haves:
  truths:
    - "An authenticated GET / renders the marketing landing page — the proxy no longer 307s it to /dashboard"
    - "Anonymous protected-route gating is unchanged: no-claims requests to /dashboard etc. still redirect to /?auth=login"
    - "The proxy still runs getClaims() on / so the Supabase session cookie refresh keeps happening for logged-in visitors"
  artifacts:
    - path: "proxy.ts"
      provides: "middleware without the authed-'/'→'/dashboard' redirect"
      contains: "Protect private routes"
    - path: "tests/unit/middleware.test.ts"
      provides: "Landing-root rules updated: authed GET / stays on landing"
      contains: "does NOT redirect"
    - path: "tests/e2e/landing-page.spec.ts"
      provides: "e2e asserting authed GET / stays on /"
      contains: "authenticated root stays on landing"
  key_links:
    - from: "proxy.ts proxy()"
      to: "app/page.tsx"
      via: "authed / now falls through to the landing route"
      pattern: "isProtectedRoute"
---

<objective>
Typing localhost:9633 (root /) while logged in should show the marketing landing
page, not bounce to /dashboard. User report: "if i only type this, it should go
to website landing page not dashboard."
</objective>

<context>
- proxy.ts lines ~145-155 hold the only authed-'/'→'/dashboard' redirect; the
  perf comment at ~line 107 references it and needs updating.
- Nothing else depends on that redirect: password sign-in (lib/actions/auth.ts)
  and the OAuth callback (app/(auth)/callback/route.ts) redirect to /dashboard
  or /onboarding explicitly on their own.
- The landing TopNav (components/landing/top-nav-auth.tsx) resolves the session
  client-side and already shows the avatar dropdown with a Dashboard link for
  logged-in users, so the landing page is fully functional when authed.
- tests/unit/middleware.test.ts "Landing root (/) routing rules" replicates the
  old redirect logic inline; tests/e2e/landing-page.spec.ts "authenticated root
  redirect" asserts the old 307. Both must flip to the new behavior.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove authed-root redirect + update tests</name>
  <files>proxy.ts, tests/unit/middleware.test.ts, tests/e2e/landing-page.spec.ts</files>
  <action>
1. proxy.ts: delete the `if (claims && pathname === '/')` redirect block; keep
   getClaims() running on / (session cookie refresh + protected gating below).
   Rewrite the short-circuit comment at ~line 107 to stop referencing the
   removed redirect.
2. tests/unit/middleware.test.ts: in "Landing root (/) routing rules", replace
   the "authenticated GET / triggers redirect to /dashboard" test (and the
   companion "/dashboard does NOT trigger the landing-root redirect" test) with
   an assertion that / is public and never anonymous-redirected — authed or not,
   GET / falls through to the landing page.
3. tests/e2e/landing-page.spec.ts: rename "authenticated root redirect" →
   "authenticated root stays on landing"; after login, goto('/') and expect the
   URL to stay '/' and the landing shell to render.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/middleware.test.ts</automated>
  </verify>
  <done>Authed / renders the landing page; anonymous gating untouched; scoped typecheck + middleware unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code+tests, then planning artifacts) — DO NOT PUSH</name>
  <files>proxy.ts, tests/unit/middleware.test.ts, tests/e2e/landing-page.spec.ts</files>
  <action>
- Stage exactly the three files; commit on `dev`:
  `fix(quick-260718-w4r): authed / renders landing page — drop proxy redirect to /dashboard`
- Create SUMMARY.md, add STATE.md Quick Tasks row, commit:
  `docs(quick-260718-w4r): add quick-task plan/summary and STATE row`
- Do NOT push. Do NOT stage the unrelated dirty files (estimate-document/editor,
  document-alignment snapshot, document-page-view test, 260718-p3v planning dir).
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed; unrelated dirty files untouched.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- `npx vitest run tests/unit/middleware.test.ts` → all pass
- Manual: logged-in browser GET http://localhost:9633/ → 200 landing page (no 307)
</verification>

<success_criteria>
A logged-in user typing localhost:9633 lands on the marketing page (with the
avatar/Dashboard menu in the nav); anonymous users visiting protected routes are
still bounced to /?auth=login.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-w4r-landing-root-shows-landing-when-authed/260718-w4r-SUMMARY.md`
</output>
