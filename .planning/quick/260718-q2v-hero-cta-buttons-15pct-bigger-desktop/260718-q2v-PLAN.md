---
phase: quick-260718-q2v
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - components/landing/hero-section.tsx

must_haves:
  truths:
    - "On desktop (≥1280px), the hero Start and See Demo buttons are ~15% bigger in every dimension: height 40px→46px, font 14px→16px, Start min-width 160px→184px, See Demo min-width 144px→166px, horizontal padding 16px→18px, Start arrow icon 16px→18px"
    - "Mobile (<640px) and tablet (640-1279px, where the globals.css iPad media queries force button height/font with !important) are unchanged — all new classes gate on xl:"
  artifacts:
    - path: "components/landing/hero-section.tsx"
      provides: "xl:-gated 15% size bump on both hero CTAs"
      contains: "xl:h-[46px]"
  key_links:
    - from: "hero CTA buttons"
      to: "desktop 15% scale"
      via: "xl: utility overrides on top of Button size=default (h-10 px-4 text-sm) and the See Demo explicit h-10 text-sm sm:px-4 sm:min-w-36"
      pattern: "xl:h-\\[46px\\]"
---

<objective>
Per user request (desktop screenshot): the hero "Start" and "See Demo" buttons need to be 15% bigger. Desktop-only, consistent with the prior quick-260718-n7d hero changes (all gated on xl: ≥1280px; iPad media queries own 640-1279px and already force their own button sizes with !important).
</objective>

<context>
- components/landing/hero-section.tsx — CTA row (lines ~83-104): Start uses Button size=default (cva: h-10 px-4 py-2 text-sm) + className min-w-40 + ArrowRight size-4; See Demo overrides inline: w-fit px-6 ... sm:min-w-36 h-10 text-sm sm:px-4
- components/ui/button.tsx — size default = h-10 px-4 py-2, base text-sm; untouched (landing-only change, so other app buttons keep their size)
- 15% math: h 40→46, text 14→16.1≈16 (text-base), min-w 160→184 / 144→165.6≈166, px 16→18.4≈18, icon 16→18.4≈18
</context>

<tasks>

<task type="auto">
  <name>Task 1: xl: 15% scale on Start and See Demo</name>
  <files>components/landing/hero-section.tsx</files>
  <action>
1. Start Button className: `min-w-40` → `min-w-40 xl:h-[46px] xl:min-w-[184px] xl:px-[18px] xl:text-base`; ArrowRight: add `xl:size-[18px]`.
2. See Demo Button className: append `xl:h-[46px] xl:min-w-[166px] xl:px-[18px] xl:text-base`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/components/landing-page.test.tsx</automated>
    <manual>Dev-server DOM measurement at ≥1280px: both buttons 46px tall, 16px font, min-widths 184/166; below 1280px unchanged</manual>
  </verify>
  <done>Both CTAs render 15% bigger at xl; sub-1280px identical to before; typecheck + landing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code only, then planning artifacts) — DO NOT PUSH</name>
  <files>components/landing/hero-section.tsx</files>
  <action>
- Stage exactly components/landing/hero-section.tsx (unrelated dirty price-book/trash files in tree — do not stage); commit on `dev`: `fix(quick-260718-q2v): hero CTA buttons 15% bigger on desktop`
- Then SUMMARY.md + STATE.md Quick Tasks row; commit: `docs(quick-260718-q2v): add quick-task plan/summary and STATE row`
- Do NOT push.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1 # code commit touches exactly 1 file</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- `npx vitest run tests/unit/components/landing-page.test.tsx` → all pass
- DOM measurement at 1390px: Start/See Demo 46px tall, computed font 16px; at <1280px: 40px/14px as before
</verification>

<success_criteria>
Hero CTAs are 15% bigger on desktop only; mobile/tablet untouched; commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-q2v-hero-cta-buttons-15pct-bigger-desktop/260718-q2v-SUMMARY.md`
</output>
