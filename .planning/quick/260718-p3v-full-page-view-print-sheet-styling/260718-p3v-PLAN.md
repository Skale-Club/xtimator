---
phase: quick-260718-p3v
plan: 01
type: execute
wave: 1
depends_on: [quick-260718-m2q]
autonomous: true
requirements: []
files_modified:
  - components/workspace/estimate/estimate-document.tsx
  - components/workspace/estimate/estimate-editor.tsx
  - tests/unit/estimate/document-page-view.test.tsx

must_haves:
  truths:
    - "In 'Full page' mode the estimate document renders as a print-preview sheet: square corners, hairline light border, paper drop shadow, US-Letter min-height (1056px at 96dpi to pair with the existing 816px width)"
    - "In 'Full width' mode (default) the document renders byte-identical to today: rounded-3xl border-4 shadow-lg with the #3f3f46 border"
    - "The sheet styling is driven by a new optional `pageView` prop on EstimateDocument (default false — all other call sites unaffected)"
    - "estimate-editor passes pageView={viewMode === 'page'} so the persisted localStorage preference drives both width AND sheet styling"
  artifacts:
    - path: "components/workspace/estimate/estimate-document.tsx"
      provides: "pageView prop + conditional root container styling (sheet vs card)"
      contains: "pageView"
    - path: "components/workspace/estimate/estimate-editor.tsx"
      provides: "pageView wired from viewMode state"
      contains: "pageView={viewMode === 'page'}"
    - path: "tests/unit/estimate/document-page-view.test.tsx"
      provides: "Root-container styling assertions for both modes"
      contains: "min-h-[1056px]"
  key_links:
    - from: "estimate-editor.tsx viewMode state"
      to: "EstimateDocument root container"
      via: "pageView boolean prop"
      pattern: "pageView"
---

<objective>
Make the "Full page" view mode actually look like a print preview. Quick-260718-m2q added the toggle but only constrained width to 816px — the document keeps its rounded-3xl border-4 app-card chrome, so it doesn't read as a printed page. User report: "the full page needs to show the page like you see it when you want to print."
</objective>

<context>
- EstimateDocument root (components/workspace/estimate/estimate-document.tsx ~line 1656) is a hardcoded `rounded-3xl border-4 shadow-lg overflow-hidden` div with inline borderColor '#3f3f46' and a forced light "paper" token scheme.
- estimate-editor.tsx already owns viewMode ('width' | 'page', localStorage-persisted) and wraps the document in `mx-auto w-full max-w-[816px]` when 'page'.
- 816px = 8.5in US Letter @96dpi; 1056px = 11in — min-height gives the sheet letter proportions even for short estimates, exactly like a print preview page.
- Read-only versions honor the persisted preference through the same editor path, so they get the sheet look too (consistent with m2q's decision).
- tests/unit/estimate/document-bill-to.test.tsx shows the mock setup needed to render EstimateDocument in unit tests.
</context>

<tasks>

<task type="auto">
  <name>Task 1: pageView prop + sheet styling + wiring + tests</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/workspace/estimate/estimate-editor.tsx, tests/unit/estimate/document-page-view.test.tsx</files>
  <action>
1. estimate-document.tsx:
   - Add optional `pageView?: boolean` to EstimateDocumentProps (doc comment referencing quick-260718-p3v), default false in the destructure.
   - Root div becomes conditional:
     - pageView: `min-h-[1056px] border shadow-2xl overflow-hidden` (square corners) with light hairline borderColor '#d4d4d8'.
     - default: unchanged `rounded-3xl border-4 shadow-lg overflow-hidden` with borderColor '#3f3f46'.
2. estimate-editor.tsx: pass `pageView={viewMode === 'page'}` to EstimateDocument.
3. New test file tests/unit/estimate/document-page-view.test.tsx: render EstimateDocument (mode="view", minimal fixture data, client null) with and without pageView; assert default root has rounded-3xl + border-4 and no min-h; pageView root has min-h-[1056px] + shadow-2xl and no rounded-3xl/border-4.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/estimate/document-page-view.test.tsx tests/unit/estimate/ tests/unit/components/estimate-floating-actions.test.tsx</automated>
  </verify>
  <done>Page mode renders as a letter-proportioned sheet; width mode unchanged; scoped typecheck + estimate unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code+tests, then planning artifacts) — DO NOT PUSH</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/workspace/estimate/estimate-editor.tsx, tests/unit/estimate/document-page-view.test.tsx</files>
  <action>
- Stage exactly the three files; commit on `dev`: `fix(quick-260718-p3v): Full page mode renders document as print-preview letter sheet`
- Create SUMMARY.md, add STATE.md Quick Tasks row, commit: `docs(quick-260718-p3v): add quick-task plan/summary and STATE row`
- Do NOT push. Do NOT stage the unrelated dirty files (app/globals.css, components/landing/hero-section.tsx, other .planning dirs).
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed; unrelated dirty files untouched.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- `npx vitest run tests/unit/estimate/` + floating-actions tests → all pass
- Width mode render byte-identical to today (pageView false → identical classes/styles)
</verification>

<success_criteria>
Toggling "Full page" shows the estimate as a centered, square-cornered, letter-proportioned white sheet with a paper shadow — visually matching the printed/PDF page — while "Full width" stays exactly as before.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-p3v-full-page-view-print-sheet-styling/260718-p3v-SUMMARY.md`
</output>
