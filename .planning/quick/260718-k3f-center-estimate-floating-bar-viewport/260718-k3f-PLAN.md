---
phase: quick-260718-k3f
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - components/workspace/estimate/estimate-floating-actions.tsx
  - components/workspace/project-workspace.tsx
  - app/(app)/projects/[id]/page.tsx

must_haves:
  truths:
    - "On desktop (md+), the floating action pill is horizontally centered relative to the FULL viewport width — including the sidebar — not the content column"
    - "On desktop, the pill sits lower: bottom-2 (8px) instead of bottom-6 (24px)"
    - "Centering is immune to sidebar collapse (w-[213px] ↔ w-16) because positioning is viewport-based (fixed), not content-flow-based"
    - "Mobile behavior is unchanged: sticky bottom-3 inside the content column (sidebar is hidden on mobile, so content center == viewport center there)"
    - "The last content of the estimate can scroll clear of the pill on desktop (clearance spacer covers the fixed pill's height + offset)"
    - "The streaming skeleton's pill mirror uses the same positioning so nothing jumps on hydration"
  artifacts:
    - path: "components/workspace/estimate/estimate-floating-actions.tsx"
      provides: "Pill wrapper: sticky on mobile, fixed+viewport-centered on md+"
      contains: "md:fixed md:inset-x-0 md:bottom-2"
    - path: "components/workspace/project-workspace.tsx"
      provides: "Desktop bottom-clearance spacer sized to clear the fixed pill"
      contains: "md:h-16"
    - path: "app/(app)/projects/[id]/page.tsx"
      provides: "Skeleton pill mirror with matching fixed positioning"
      contains: "fixed inset-x-0 bottom-2"
  key_links:
    - from: "EstimateFloatingActions Pill (md:fixed)"
      to: "viewport"
      via: "position:fixed + inset-x-0 + flex justify-center — containing block is the viewport, so center includes the sidebar width"
      pattern: "md:fixed md:inset-x-0"
    - from: "project-workspace.tsx clearance spacer"
      to: "fixed pill overlay"
      via: "md:h-16 spacer at the end of the content column keeps the final content scrollable above the ~46px pill + 8px offset"
      pattern: "md:h-16"
---

<objective>
Center the estimate page's floating action pill (Settings / Link Client / Refine with AI / Photos / Send) on the FULL screen width and move it lower, per user request (screenshot annotation: pill should sit near the bottom edge, centered on the viewport center line — which includes the sidebar width).

Current state: the Pill wrapper in estimate-floating-actions.tsx is `sticky bottom-3 md:bottom-6 z-40 flex justify-center` INSIDE the content column (`<main>` is the scroll container, sidebar `w-[213px]`/`w-16` sits to its left). `justify-center` therefore centers on the content column, which puts the pill ~106px right of true viewport center, and `md:bottom-6` keeps it 24px up.
</objective>

<context>
- components/workspace/estimate/estimate-floating-actions.tsx — the Pill wrapper (only consumer: estimate-editor.tsx)
- components/workspace/project-workspace.tsx — bottom clearance spacer (`md:h-6`) paired with negative margin that cancels main's padding
- app/(app)/projects/[id]/page.tsx — ProjectWorkspaceSkeleton contains a pill mirror (`sticky bottom-6 hidden md:flex`)
- No transform/filter/backdrop ancestors between the pill and the viewport (verified: ProjectPageShell is a bare context provider; workspace divs are plain flex), so `position: fixed` resolves against the viewport as required.
- Known tradeoff (accepted): at narrow desktop widths (~768-900px) a true-viewport-centered pill can visually overlap the sidebar's bottom edge; z-40 keeps it above. This is inherent to "center on full screen width" and matches the user's explicit request.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reposition the pill, resize the clearance spacer, sync the skeleton</name>
  <files>components/workspace/estimate/estimate-floating-actions.tsx, components/workspace/project-workspace.tsx, app/(app)/projects/[id]/page.tsx</files>
  <action>
1. estimate-floating-actions.tsx — Pill wrapper className:
   `sticky bottom-3 md:bottom-6 ...` → `sticky bottom-3 md:fixed md:inset-x-0 md:bottom-2 ...`
   (keep z-40, flex justify-center, px-4 md:px-0, pointer-events-none, safe-area padding). Update the Pill comment: desktop is fixed to the viewport so centering includes the sidebar; mobile stays sticky in the content column.
2. project-workspace.tsx — clearance spacer: `md:h-6` → `md:h-16` so the estimate's last content (totals) can scroll above the now-always-overlaying fixed pill (~46px tall + 8px bottom offset). Extend the comment to note the desktop spacer now also provides scroll clearance under the fixed pill.
3. app/(app)/projects/[id]/page.tsx — skeleton pill mirror: `sticky bottom-6` → `fixed inset-x-0 bottom-2` (keep `z-40 hidden md:flex justify-center pointer-events-none`) so the skeleton pill renders in the same place as the hydrated one.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/components/estimate-floating-actions.test.tsx</automated>
  </verify>
  <done>Pill fixed+viewport-centered+bottom-2 on md+, sticky bottom-3 on mobile; spacer md:h-16; skeleton matches; scoped typecheck green; floating-actions unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code only, then planning artifacts) — DO NOT PUSH</name>
  <files>components/workspace/estimate/estimate-floating-actions.tsx, components/workspace/project-workspace.tsx, app/(app)/projects/[id]/page.tsx</files>
  <action>
- Stage exactly the three code files; commit on `dev`: `fix(quick-260718-k3f): center estimate floating bar on full viewport and lower it`
- Then create SUMMARY.md, update STATE.md Quick Tasks Completed table, and commit planning artifacts: `docs(quick-260718-k3f): add quick-task plan/summary and STATE row`
- Do NOT push — leave commits local for user review.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1 | grep -c "" # code commit touches exactly 3 files</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- `npx vitest run tests/unit/components/estimate-floating-actions.test.tsx` → all pass
- Visual: pill centered on viewport center line (including sidebar), sitting 8px above the bottom edge on desktop; mobile unchanged
</verification>

<success_criteria>
The floating action pill on the estimate page is centered on the full screen width (sidebar included, immune to sidebar collapse) and sits near the bottom edge on desktop; mobile is untouched; last content scrolls clear of the pill; skeleton matches; commits are local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-k3f-center-estimate-floating-bar-viewport/260718-k3f-SUMMARY.md`
</output>
