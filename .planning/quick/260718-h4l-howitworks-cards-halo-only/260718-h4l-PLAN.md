---
phase: quick-260718-h4l
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - components/landing/how-it-works-section.tsx
  - app/globals.css

must_haves:
  truths:
    - "The three How-it-works cards no longer render any blue line art (waveform bars, camera outline/lens/flash, speech bubble outline/dots)"
    - "Each card image slot keeps a soft primary-colored halo glow behind the person image"
    - "Card layout, hover states, step titles/descriptions, and grid/ticker behavior unchanged"
  artifacts:
    - path: "components/landing/how-it-works-section.tsx"
      provides: "Single HaloBackground radial glow replacing SoundWaveBackground/CameraBackground/SpeechBubbleBackground"
      contains: "HaloBackground"
    - path: "app/globals.css"
      provides: "Orphaned keyframes removed (bar-pulse, cam-flash-unit, bubble-dot-1..6)"
  key_links:
    - from: "StepCard image slot"
      to: "HaloBackground"
      via: "unconditional render behind the z-10 Image"
      pattern: "<HaloBackground />"
---

<objective>
Per user request (with screenshot of the Record audio card): remove the blue lines from the how-it-works cards — keep only the halo. All three decorative SVG backgrounds (animated waveform bars, camera outline, speech bubble + typing dots) go away; each card keeps a soft primary radial halo behind its image.
</objective>

<context>
- components/landing/how-it-works-section.tsx — WAVEFORM_BARS + SoundWaveBackground (card 1), CameraBackground (card 3, already has a radial glow), SpeechBubbleBackground (card 2); StepCard takes showWave/showPhotos/showCursor flags from both the desktop grid and the phone Ticker
- app/globals.css:5-45 — keyframes bar-pulse, cam-flash-unit, bubble-dot-1..6 used only by those components
- tests/unit/components/landing-page.test.tsx only asserts the 3 step titles; e2e only checks the "Record audio" heading — no test pins the SVG art
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SVG line art with a single halo glow</name>
  <files>components/landing/how-it-works-section.tsx, app/globals.css</files>
  <action>
1. how-it-works-section.tsx: delete WAVEFORM_BARS, SoundWaveBackground, CameraBackground, SpeechBubbleBackground; add HaloBackground (static primary radial-gradient div, aria-hidden, pointer-events-none); StepCard drops showWave/showPhotos/showCursor props and always renders HaloBackground under the image; update both call sites (grid + ticker).
2. globals.css: remove the now-unused bar-pulse, cam-flash-unit, and bubble-dot-1..6 keyframes.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/components/landing-page.test.tsx</automated>
    <manual>Dev-server: all three cards show only the halo glow behind the image — no blue bars/outlines/dots</manual>
  </verify>
  <done>Cards render halo-only backgrounds; typecheck + landing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code only, then planning artifacts) — DO NOT PUSH</name>
  <files>components/landing/how-it-works-section.tsx, app/globals.css</files>
  <action>
- Stage exactly the two files; commit on `dev`: `fix(quick-260718-h4l): how-it-works cards keep halo only, drop blue SVG line art`
- Then SUMMARY.md + STATE.md row; commit: `docs(quick-260718-h4l): add quick-task plan/summary and STATE row`
- Do NOT push.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1 # code commit touches exactly 2 files</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- landing-page unit tests pass
- Browser: no blue waveform/camera/bubble art on any of the 3 cards; halo visible behind each image
</verification>

<success_criteria>
All three how-it-works cards show only a primary-colored halo glow behind their images; no blue line graphics remain; commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-h4l-howitworks-cards-halo-only/260718-h4l-SUMMARY.md`
</output>
