---
phase: quick-260525-lt5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/projects/project-row-actions.tsx
autonomous: true
requirements:
  - UI-CONFIRM-SOFT-DELETE
tags:
  - ui
  - destructive-actions
  - confirmation-dialog

must_haves:
  truths:
    - "Clicking 'Delete' on an active project opens a confirmation dialog (no immediate soft delete)"
    - "Clicking 'Delete' on an archived project opens a confirmation dialog (no immediate soft delete)"
    - "Confirming the dialog calls softDeleteProjectAction and shows the existing 'moved to Trash' toast"
    - "Cancelling the dialog closes it without calling softDeleteProjectAction"
    - "The existing 'Delete permanently' (hard delete) confirmation continues to work exactly as before, with its own separate dialog state"
  artifacts:
    - path: "components/projects/project-row-actions.tsx"
      provides: "Soft-delete confirmation AlertDialog wired to both active and archived Delete menu items"
      contains: "softConfirmOpen"
  key_links:
    - from: "DropdownMenuItem (status=active, label=Delete)"
      to: "soft-delete AlertDialog"
      via: "setSoftConfirmOpen(true) on click (replaces direct run(softDeleteProjectAction))"
      pattern: "setSoftConfirmOpen\\(true\\)"
    - from: "DropdownMenuItem (status=archived, label=Delete)"
      to: "soft-delete AlertDialog"
      via: "setSoftConfirmOpen(true) on click (replaces direct run(softDeleteProjectAction))"
      pattern: "setSoftConfirmOpen\\(true\\)"
    - from: "soft-delete AlertDialogAction (Move to Trash)"
      to: "softDeleteProjectAction"
      via: "run(() => softDeleteProjectAction(projectId), ...) inside onClick, then setSoftConfirmOpen(false)"
      pattern: "softDeleteProjectAction\\(projectId\\)"
---

<objective>
Wrap the two existing "Delete" menu items (status=active and status=archived) in `components/projects/project-row-actions.tsx` so they open a confirmation AlertDialog instead of immediately calling `softDeleteProjectAction`. Confirming the dialog performs the soft delete; cancelling does nothing. The existing "Delete permanently" hard-delete confirmation must remain functionally unchanged.

Purpose: Match the established two-stage trash pattern (soft delete → 30-day trash → hard delete) by requiring an explicit confirm step before moving a project to Trash. Today the soft delete is one click with no undo prompt, which is a footgun next to non-destructive items like Archive.

Output: A single edited file with one additional `useState` for the soft-delete dialog and one additional `<AlertDialog>` block reusing the already-imported AlertDialog primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@components/projects/project-row-actions.tsx

<interfaces>
<!-- Extracted from components/projects/project-row-actions.tsx — the executor should use these directly. -->
<!-- All AlertDialog primitives are ALREADY imported at the top of the file (lines 14-23). DO NOT add new imports. -->

Already imported and in use in this file:
```typescript
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
} from '@/components/ui/alert-dialog'
import { softDeleteProjectAction, hardDeleteProjectAction, ... } from '@/lib/actions/project'
```

Existing local helper to reuse verbatim:
```typescript
function run(
  action: () => Promise<{ data?: unknown; error?: string }>,
  successMsg: string
): void
// Wraps a server action in startTransition, shows error or success toast, calls router.refresh() on success.
```

Existing hard-delete confirmation block (lines 145-170) — DO NOT touch:
```tsx
<AlertDialog open={confirmOpen} onOpenChange={setConfirmOpen}>
  ...
  <AlertDialogTitle>Permanently delete project?</AlertDialogTitle>
  ...
  <AlertDialogAction onClick={() => { run(() => hardDeleteProjectAction(projectId), ...); setConfirmOpen(false) }}>
    {isPending ? 'Deleting...' : 'Delete permanently'}
  </AlertDialogAction>
</AlertDialog>
```

Existing soft-delete trigger sites that MUST change to open the new dialog:
- Lines 82-93: `status === 'active'` Delete `<DropdownMenuItem>` — `onClick` currently calls `run(() => softDeleteProjectAction(projectId), ...)` directly.
- Lines 109-120: `status === 'archived'` Delete `<DropdownMenuItem>` — same pattern.

The `status === 'trash'` "Delete permanently" menu item (line 133-139) calls `setConfirmOpen(true)` and is the reference pattern. It MUST remain unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap soft-delete in AlertDialog confirmation (mirror hard-delete pattern)</name>
  <files>components/projects/project-row-actions.tsx</files>
  <action>
Modify `components/projects/project-row-actions.tsx` to gate soft delete behind an AlertDialog confirmation. Concrete edits, in order:

1. **Add a second `useState` for the soft-delete dialog** next to the existing `confirmOpen` state (line 42):
   ```tsx
   const [confirmOpen, setConfirmOpen] = useState(false)           // existing — leave alone
   const [softConfirmOpen, setSoftConfirmOpen] = useState(false)   // NEW
   ```

2. **Change the `status === 'active'` Delete `<DropdownMenuItem>` `onClick`** (lines 82-93) from calling `run(...)` directly to opening the new dialog. Mirror the exact shape used by the `status === 'trash'` "Delete permanently" item (line 133-139):
   ```tsx
   <DropdownMenuItem
     className="text-destructive"
     disabled={isPending}
     onClick={() => setSoftConfirmOpen(true)}
   >
     Delete
   </DropdownMenuItem>
   ```

3. **Apply the same change to the `status === 'archived'` Delete `<DropdownMenuItem>`** (lines 109-120). Both `active` and `archived` Delete items must end up identical and both open `softConfirmOpen`.

4. **Add a new `<AlertDialog>` block** immediately AFTER the closing `</AlertDialog>` of the existing hard-delete dialog (after line 170), still inside the outer `<>...</>` fragment. Use the existing hard-delete dialog (lines 145-170) as the structural template — same `AlertDialogHeader` / `AlertDialogFooter` / `AlertDialogCancel` / `AlertDialogAction` shape — but with the following content differences:

   - State: `open={softConfirmOpen} onOpenChange={setSoftConfirmOpen}`
   - `AlertDialogTitle`: `Move project to Trash?`
   - `AlertDialogDescription`: `` `"${projectName}" will be moved to Trash. You can restore it from the Trash view within 30 days before it is permanently deleted.` ``
   - `AlertDialogAction` className: keep the same destructive styling as the hard-delete action (`"bg-destructive text-destructive-foreground hover:bg-destructive/90"`) — soft delete is still destructive intent and visual parity matters for muscle memory.
   - `AlertDialogAction` label: `{isPending ? 'Moving...' : 'Move to Trash'}`
   - `AlertDialogAction` onClick body:
     ```tsx
     onClick={() => {
       run(
         () => softDeleteProjectAction(projectId),
         `"${projectName}" moved to Trash`
       )
       setSoftConfirmOpen(false)
     }}
     ```
     Note: the success message string is identical to what the menu items used to pass — preserve the existing toast wording exactly so behavior is unchanged from the user's perspective once they confirm.

5. **Do NOT modify:**
   - The hard-delete `<AlertDialog>` block (lines 145-170) — its state (`confirmOpen` / `setConfirmOpen`), title, description, action, label, and onClick all stay byte-for-byte the same.
   - The `status === 'trash'` Delete permanently `<DropdownMenuItem>` (lines 123-141) — still calls `setConfirmOpen(true)`.
   - Imports — `AlertDialog*` primitives and `softDeleteProjectAction` are already imported.
   - The `run()` helper, `Props` interface, `useTransition`, `useRouter`, or the `DropdownMenu` structure.
   - The `Archive` / `Unarchive` / `Restore` menu items — those are non-destructive and stay one-click.

6. **`e.stopPropagation()` is already handled** at the `DropdownMenuTrigger` button (line 66) and `DropdownMenuContent` (line 71) level — do not add it again on the new dialog or menu items.

7. **Conventions used elsewhere in this file (preserve them):**
   - Single-quote strings for JSX attributes and string literals; template literals only when interpolating.
   - `disabled={isPending}` on both `AlertDialogCancel` and `AlertDialogAction` in the new dialog (matches lines 154 + 156 pattern).
   - No new className utilities beyond what is already in the hard-delete dialog.

After the edit, the diff should be: one added `useState` line, two `onClick` handlers replaced (active + archived Delete), and one new `<AlertDialog>` block appended after the existing one. No other lines should change.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "npx tsc --noEmit -p tsconfig.json" 2>&amp;1 | Select-String -Pattern 'project-row-actions' -SimpleMatch; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
    Manual sanity (Claude self-check, no human needed):
    1. `grep -n "softConfirmOpen" components/projects/project-row-actions.tsx` returns exactly 4 matches: 1 useState declaration, 2 setSoftConfirmOpen(true) in onClick handlers, 1 open={softConfirmOpen} on the new AlertDialog (plus 1 setSoftConfirmOpen(false) inside the action onClick = 5 total references; the `open={softConfirmOpen} onOpenChange={setSoftConfirmOpen}` line counts as 2). Accept any count >= 4.
    2. `grep -n "confirmOpen" components/projects/project-row-actions.tsx` shows the original `confirmOpen` (not softConfirmOpen) is still referenced by the hard-delete dialog and the trash-row menu item — confirming it was not renamed.
    3. `grep -n "Permanently delete project?" components/projects/project-row-actions.tsx` still returns exactly 1 hit (hard-delete dialog title untouched).
    4. `grep -n "Move project to Trash?" components/projects/project-row-actions.tsx` returns exactly 1 hit (new soft-delete dialog title).
    5. No DropdownMenuItem with label "Delete" (active or archived branch) calls `run(() => softDeleteProjectAction` directly anymore — both should call `setSoftConfirmOpen(true)`. The only remaining `softDeleteProjectAction(projectId)` reference should be inside the new AlertDialogAction onClick.
  </verify>
  <done>
- `components/projects/project-row-actions.tsx` compiles with no new TypeScript errors (`tsc --noEmit` clean for this file).
- Clicking Delete on an active project opens an AlertDialog titled "Move project to Trash?" — clicking Cancel closes it with no side effects; clicking "Move to Trash" calls `softDeleteProjectAction` and shows the `"<name>" moved to Trash` toast.
- Clicking Delete on an archived project opens the same dialog and behaves identically.
- Clicking Delete permanently on a trashed project still opens the original "Permanently delete project?" dialog and calls `hardDeleteProjectAction` exactly as before (unchanged).
- No other files modified.
- No new imports added.
  </done>
</task>

</tasks>

<verification>
After execution:

1. **Type check:** `npx tsc --noEmit` produces no new errors attributable to `components/projects/project-row-actions.tsx`.
2. **Grep audits** (run from project root):
   - `grep -n "setSoftConfirmOpen(true)" components/projects/project-row-actions.tsx` → 2 hits (active + archived Delete onClick handlers).
   - `grep -n "softDeleteProjectAction(projectId)" components/projects/project-row-actions.tsx` → 1 hit (inside the new AlertDialogAction onClick only).
   - `grep -n "hardDeleteProjectAction(projectId)" components/projects/project-row-actions.tsx` → 1 hit (unchanged, inside the existing hard-delete AlertDialogAction).
   - `grep -n "Move project to Trash?" components/projects/project-row-actions.tsx` → 1 hit.
   - `grep -n "Permanently delete project?" components/projects/project-row-actions.tsx` → 1 hit (unchanged).
3. **Visual smoke (optional, by user):** On `/projects`, exercise the 3 dropdown branches:
   - Active row → Delete → dialog appears → Cancel = nothing; Confirm = row moves to Trash with toast.
   - Archived row → Delete → same behavior.
   - Trash row → Delete permanently → original dialog appears → Confirm = row removed with "permanently deleted" toast.
</verification>

<success_criteria>
- Single file modified: `components/projects/project-row-actions.tsx`.
- Two soft-delete trigger sites now open a confirmation dialog instead of acting immediately.
- One new AlertDialog block exists in the JSX, structurally mirroring the existing hard-delete dialog.
- One new `useState` (`softConfirmOpen`) added.
- Zero changes to: imports, hard-delete dialog, hard-delete trigger, archive/unarchive/restore items, `Props`, `run()` helper.
- TypeScript compiles cleanly for the modified file.
</success_criteria>

<output>
After completion, create `.planning/quick/260525-lt5-add-alertdialog-confirmation-before-soft/260525-lt5-SUMMARY.md` summarizing:
- What changed (state hook added, two onClicks rewired, one AlertDialog block added).
- Why it ships (parity with established two-stage trash pattern; removes the no-undo footgun next to non-destructive Archive).
- Hard-delete confirmation explicitly verified as unchanged.
- Any micro-decisions (e.g., chose to keep destructive red styling on the soft-delete confirm button for visual parity, or chose neutral styling — record which and why).
</output>
