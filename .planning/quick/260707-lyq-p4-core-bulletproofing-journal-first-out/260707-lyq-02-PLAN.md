---
phase: quick-260707-lyq
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/estimate/poll-outcome.ts
  - components/capture/capture-recorder.tsx
  - hooks/use-job-status.ts
  - tests/unit/estimate/poll-outcome.test.ts
autonomous: true
requirements: [QUICK-lyq-04]
must_haves:
  truths:
    - "The capture popup detects outcomes journal-first (getAttemptOutcome): completed only for surviving estimates (no ghost navigation), needs_details drives the vague toast, failed surfaces the REAL server reason within ~3s instead of a 6-minute timeout"
    - "Retry genuinely re-runs: new dispatchNonce + fresh requestId per retry click; attemptId stays (lineage); no re-upload"
    - "Stage progression comes from the journal (transcribe succeeded → generating), not from a separate recordings query"
    - "NOT_FOUND_GRACE_MS = 60s (production showed a 30s Inngest run-creation lag at 19:45 UTC)"
  artifacts:
    - path: "lib/estimate/poll-outcome.ts"
      provides: "Journal-first poll with estimates-table fallback"
    - path: "components/capture/capture-recorder.tsx"
      provides: "attemptId-driven outcome handling + real Retry + real failure messages"
  key_links:
    - from: "pollEstimateOutcome"
      to: "getAttemptOutcome"
      via: "journal-first tick"
      pattern: "getAttemptOutcome\\("
    - from: "onRetry"
      to: "dispatchNonce + fresh requestId"
      via: "retry refs reset"
      pattern: "dispatchNonce"
---

<objective>
P4 Wave 2 (client): consume Wave 1's journal. The popup's outcome loop becomes journal-first —
eliminating the ghost-estimate navigation (production attempt 8a0c13e8), surfacing real failure
reasons in seconds, and making Retry actually retry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260707-lyq-p4-core-bulletproofing-journal-first-out/260707-lyq-01-SUMMARY.md (Wave 1 exact signatures)
@lib/actions/attempt-outcome.ts (Wave 1 output — AttemptOutcome union)
@lib/estimate/poll-outcome.ts
@components/capture/capture-recorder.tsx (race helper raceEstimateOutcomeAgainstJob; outcome handler handleEstimateOutcome; retry handlers; attempt refs)
@hooks/use-job-status.ts (NOT_FOUND_GRACE_MS)
@lib/actions/recording.ts (startRecordingPipeline signature incl. dispatchNonce)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Journal-first pollEstimateOutcome</name>
  <files>lib/estimate/poll-outcome.ts</files>
  <action>
    Extend pollEstimateOutcome opts with `attemptId?: string` and `onStageProgress?: (lastStep: string | null) => void`.
    Tick behavior when attemptId present: call getAttemptOutcome(attemptId) (import from
    '@/lib/actions/attempt-outcome') FIRST each tick:
    - completed → return { state: 'completed', estimateId }
    - needs_details → return NEW outcome variant { state: 'awaiting_details' } (reuse existing variant — map needs_details onto it so capture-recorder's existing handling works)
    - failed → return NEW variant { state: 'failed'; step: string; reason: string } (extend EstimateOutcome union)
    - pending → fire onStageProgress(lastStep) and fall through to the EXISTING estimates/projects fallback checks (keep them: they cover journal-read failures and double as belt-and-braces), except the awaiting_details fallback check must be SKIPPED when attemptId is present (the journal owns vague detection now — this kills the stale-status false positive).
    - unauthorized → treat as pending (do not fail the UI on a scoping edge).
    Keep: abort semantics, interval, timeout → { state: 'timeout' }. evaluateOutcomeTick stays exported/unchanged for the fallback path (adjust only its call sites if needed).
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && grep -n "getAttemptOutcome" lib/estimate/poll-outcome.ts && npx tsc --noEmit && npx eslint lib/estimate/poll-outcome.ts</automated></verify>
  <done>Journal-first tick with fallback; failed variant added; stale-status check disabled under attemptId.</done>
</task>

<task type="auto">
  <name>Task 2: capture-recorder — real Retry, real reasons, journal stages</name>
  <files>components/capture/capture-recorder.tsx, hooks/use-job-status.ts</files>
  <action>
    2a. hooks/use-job-status.ts: NOT_FOUND_GRACE_MS 20_000 → 60_000; update the comment with
        today's evidence (19:45 UTC: run created 30s after event acceptance).
    2b. Wire attemptId into every pollEstimateOutcome call (all three paths) +
        onStageProgress: lastStep 'transcribe'/'analyze' with status succeeded → setStage('generating');
        remove the recordingId/onTranscriptReady mechanism if now redundant (check Wave 1 summary
        for what onStageProgress receives — adapt: the callback gets lastStep; map save_recording→'transcribing', transcribe→'generating', analyze→'generating').
    2c. Handle the new 'failed' outcome: failAt(mapStepToStageKey(step), reason) where
        mapStepToStageKey: save_recording→'saving', transcribe→'transcribing', analyze→'analyzing',
        generate_estimate→'generating' (reuse/invert the existing stepMap in failAt). The reason
        shown is the server's real error_message. Do NOT double-report: the journal already has
        this failure — suppress the reportClientPipelineFailure call inside failAt when the
        failure ORIGIN is the journal (add an optional param to failAt, e.g. failAt(s, msg, {skipReport: true})).
    2d. REMOVE the raceEstimateOutcomeAgainstJob helper and its pollJob racing entirely — the
        journal surfaces real failures in ~1 tick, making the job-poll race redundant (pollJob
        remains in use by OTHER components; do not touch them). Photos path: the analyze jobId
        poll used for stage progression is also replaced by onStageProgress.
    2e. Real Retry: add `dispatchNonceRef = useRef(0)`. In BOTH failure-card onRetry handlers:
        `dispatchNonceRef.current += 1; requestIdRef.current = null; ensureAttempt(); ...` then
        re-run the path (runPipeline for audio — audioBlob retained; handleGenerate for
        text/photos if that is how retry currently routes — inspect and keep the existing retry
        wiring, only injecting the nonce+fresh-requestId reset). Pass
        `dispatchNonce: dispatchNonceRef.current` to startRecordingPipeline (audio). Text/photos:
        fresh requestId alone already yields new event ids (estimate-/analyze- ids derive from
        requestId) — verify and document in comments. attemptIdRef is NOT reset (lineage), and
        previousEstimateIdRef is NOT re-read on retry.
    2f. Update stale comments (the REC-03 comment block claiming requestId is reused on Retry —
        rewrite: attemptId is stable lineage; requestId/nonce are re-minted per retry so a Retry
        is a genuine re-run, 260707-lyq).
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && grep -n "dispatchNonceRef" components/capture/capture-recorder.tsx && ! grep -q "raceEstimateOutcomeAgainstJob" components/capture/capture-recorder.tsx && grep -n "60_000\|60000" hooks/use-job-status.ts && npx tsc --noEmit && npx eslint components/capture/capture-recorder.tsx hooks/use-job-status.ts</automated></verify>
  <done>Journal-driven outcomes/stages; race removed; Retry re-runs with nonce+fresh requestId; grace 60s; eslint baseline preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Tests + full suite</name>
  <files>tests/unit/estimate/poll-outcome.test.ts</files>
  <action>
    Extend poll-outcome tests (mock getAttemptOutcome module): journal completed → completed;
    needs_details → awaiting_details; failed → failed with reason; pending → falls through to
    estimates fallback; stale projects.status='awaiting_details' with attemptId present does NOT
    produce awaiting_details (the production stale-status case). Update any existing tests broken
    by the signature change. Then run the FULL `npx vitest run` and report counts vs the known
    baseline (3148 pass / 1 pre-existing landing-page flake).
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && npx vitest run tests/unit/estimate/ tests/unit/hooks/ && npx tsc --noEmit</automated></verify>
  <done>Journal-first behavior covered incl. the stale-status regression; full suite at baseline.</done>
</task>

</tasks>

<verification>
tsc clean; eslint baselines; full vitest at baseline; grep gates from each task pass.
</verification>

<success_criteria>
Re-running today's vague-input scenario shows the vague toast (no ghost navigation); a hard
generation failure shows its real reason within seconds; Retry visibly re-runs the pipeline.
</success_criteria>

<output>
After completion, create `.planning/quick/260707-lyq-p4-core-bulletproofing-journal-first-out/260707-lyq-02-SUMMARY.md`
</output>
