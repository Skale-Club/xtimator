---
phase: quick-260718-d2f
plan: 01
type: execute
wave: 1
depends_on: [quick-260718-t7d]
autonomous: true
requirements: []
files_modified:
  - lib/actions/price-book.ts
  - components/price-book/price-book-list.tsx
  - tests/unit/price-book/price-book-list.test.tsx

must_haves:
  truths:
    - "Clicking the category-header trash icon WITH items selected opens the existing bulk-delete dialog and moves the selected items to Trash (same flow as the floating bar)"
    - "Clicking it with NOTHING selected opens the Delete Category dialog, which now deletes the category AND moves all its items to Trash (no more 'move items first' refusal)"
    - "Items trashed via category deletion stay restorable — the folder FK is ON DELETE SET NULL, so restored items land in Uncategorized"
    - "Single-item delete, floating bulk bar, Adjust %, rename, and Uncategorized (no trash button) unchanged"
  artifacts:
    - path: "lib/actions/price-book.ts"
      provides: "deleteFolderWithItems server action: soft-delete active items in folder, then delete the folder row"
      contains: "deleteFolderWithItems"
    - path: "components/price-book/price-book-list.tsx"
      provides: "Dual-function trash button + count-aware Delete Category dialog copy"
      contains: "delete-folder-btn-"
  key_links:
    - from: "category trash button"
      to: "bulk dialog OR folder dialog"
      via: "selected.size > 0 branch"
      pattern: "selected\\.size > 0"
    - from: "handleConfirmDeleteFolder"
      to: "deleteFolderWithItems"
      via: "server action call"
      pattern: "deleteFolderWithItems\\("
---

<objective>
Per user request (screenshot of a category header row): the trash icon should have two functions — delete whatever is selected, or, if nothing is selected, delete the category with all items in it. Today it only tries deleteFolder, which refuses when the category has items.
</objective>

<context>
- components/price-book/price-book-list.tsx — trash button (only on non-virtual folders) currently opens the Delete Category dialog → deleteFolder, guarded "Remove all items ... first". Bulk selection (t7d) already exists: Set<string> + floating bar → bulkDeleteDialog → trashPriceBookItems.
- lib/actions/price-book.ts — trash family from t7d (trashPriceBookItems etc., assertWritable + company scoping).
- supabase/migrations/20260518000003_price_book_folders.sql — folder_id FK is ON DELETE SET NULL: deleting the folder auto-un-categorizes remaining (trashed) rows; nothing blocks, nothing is hard-deleted.
- t() falls back to a live-translate API for unlisted strings (t7d added none) — no translations.ts change needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: deleteFolderWithItems action + dual-function trash button + tests</name>
  <files>lib/actions/price-book.ts, components/price-book/price-book-list.tsx, tests/unit/price-book/price-book-list.test.tsx</files>
  <action>
1. actions: add `deleteFolderWithItems(folderId)` — assertWritable + getAuthContext; UPDATE company_price_book SET deleted_at=now() WHERE company_id, folder_id, deleted_at IS NULL; then DELETE price_book_folders row (company-scoped). revalidatePath /price-book + /trash. Keep deleteFolder exported (unused by UI now).
2. component: import deleteFolderWithItems; trash button gets `data-testid` and branches — selection non-empty → setBulkDeleteDialogOpen(true); empty → existing folder-dialog path. New memo deletingFolderItemCount from UNFILTERED items. Dialog copy: count>0 → "delete the category and move its N item(s) to Trash…", 0 → "delete the empty category". Confirm calls deleteFolderWithItems; toast reflects items moved.
3. tests: mock deleteFolderWithItems; new describe — (a) selection → bulk dialog + trashPriceBookItems called, folder action NOT called; (b) no selection → category dialog shows count; (c) confirm calls deleteFolderWithItems('folder-labor'), trash NOT called.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/price-book/</automated>
    <manual>Category trash with checked items trashes them; with none checked deletes category + items to Trash; items restorable from /trash as Uncategorized</manual>
  </verify>
  <done>Dual behavior implemented and tested; typecheck + price-book suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code only, then planning artifacts) — DO NOT PUSH</name>
  <files>lib/actions/price-book.ts, components/price-book/price-book-list.tsx, tests/unit/price-book/price-book-list.test.tsx</files>
  <action>
- Stage exactly the three files; commit on `dev`: `feat(quick-260718-d2f): category trash button trashes selection, or deletes category with items`
- Then SUMMARY.md + STATE.md row; commit: `docs(quick-260718-d2f): add quick-task plan/summary and STATE row`
- Do NOT push.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1 # code commit touches exactly 3 files</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- price-book unit suites green including 3 new tests
- Manual: both trash behaviors + restore from /trash
</verification>

<success_criteria>
Category trash icon: selection → bulk trash of selected items; no selection → category deleted with all its items moved to Trash (restorable). Commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-d2f-price-book-trash-dual-function/260718-d2f-SUMMARY.md`
</output>
