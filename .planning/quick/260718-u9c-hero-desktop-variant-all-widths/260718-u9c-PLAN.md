---
phase: quick-260718-u9c
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - app/globals.css
  - components/landing/hero-section.tsx

must_haves:
  truths:
    - "On EVERY desktop viewport ≥1024px, the hero shows the same rendering: 2-row title, 2-row 18px subheadline, 46px/16px CTAs side-by-side (previously 18px text + 46px CTAs only kicked in at ≥1280px)"
    - "Real iPads keep their tuned rendering: the iPad hero media queries additionally require (pointer: coarse) — landscape was already scoped by quick-260718-h9x; this task scopes the two portrait blocks the same way — and their !important font/height overrides still win on coarse devices"
    - "Mobile (<640px) unchanged"
  artifacts:
    - path: "components/landing/hero-section.tsx"
      provides: "subheadline lg:text-[18px]; Start/See Demo/icon size bumps gated lg: instead of xl:"
      contains: "lg:h-[46px]"
    - path: "app/globals.css"
      provides: "(pointer: coarse) on the two portrait iPad hero blocks"
      contains: "orientation: portrait) and (pointer: coarse)"
  key_links:
    - from: "lg: gates on p font + CTA sizes"
      to: "uniform desktop hero from 1024px up"
      via: "same convention quick-260718-h9x used for the 2-row layout gates; tablets ≥1024 are protected by their media queries' specificity/!important"
      pattern: "lg:text-\\[18px\\]"
---

<objective>
User: "all desktop views need to be 2 rows for title or description like we talked about. also make sure buttons from hero remain the same size across all desktop layouts."

The concurrent quick-260718-h9x task (uncommitted, same files) already moved the 2-row title/subheadline/CTA-row gates xl → lg and pointer-scoped the iPad LANDSCAPE query. Remaining gaps, owned by this task: subheadline still 16px (lg:text-base) and CTAs still 40px below 1280px; portrait iPad blocks still catch portrait-shaped desktop windows.
</objective>

<context>
- Follows h9x's convention (plain lg: gates; desktop = ≥1024) instead of a pointer-aware custom variant, so the file keeps ONE gating mechanism. A @custom-variant desktop draft was written and removed during this task.
- Tablets ≥1024 (iPad Pro 12.9 portrait, Air/Pro-11 landscape): heights/fonts stay forced by their media queries' !important; the only leak is lg:min-w-[184px]/[166px] (~+20px wider CTAs) — accepted, rows have room.
- p font restructure: `lg:text-base xl:text-[18px]` → `lg:text-[18px]` (single value across desktop; coarse tablets get 20px from `.hero-left p` media rules by specificity).
- Coordination: h9x's hunks are uncommitted in the same two files; if it has not committed by the end of this task, the code commit here carries both sets, explicitly annotated.
</context>

<tasks>

<task type="auto">
  <name>Task 1: lg: gates for subheadline size + CTA sizes; portrait pointer scoping</name>
  <files>app/globals.css, components/landing/hero-section.tsx</files>
  <action>
1. globals.css: add `and (pointer: coarse)` to the 640-1023 and 1024-1279 portrait iPad hero blocks.
2. hero-section.tsx: p `sm:text-[14px] lg:text-base xl:text-[18px]` → `sm:text-[14px] lg:text-[18px]`; Start `xl:h-[46px] xl:min-w-[184px] xl:px-[18px] xl:text-base` → lg:*; ArrowRight `xl:size-[18px]` → lg:; See Demo `xl:h-[46px] xl:min-w-[166px] xl:px-[18px] xl:text-base` → lg:*. Refresh comments.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/components/landing-page.test.tsx tests/unit/seo/home-cacheability.test.ts</automated>
    <manual>Dev-server DOM at 1206×800 AND 1390×800: identical hero metrics — h1 2 lines, p 2 lines @18px, CTAs 184×46 / 166×46 @16px side-by-side</manual>
  </verify>
  <done>Hero renders identically on every desktop width ≥1024px; tablets/mobile untouched; typecheck + tests green (modulo documented pre-existing ?auth=login flake).</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code, then planning artifacts) — DO NOT PUSH</name>
  <files>app/globals.css, components/landing/hero-section.tsx</files>
  <action>
- Re-check `git log` for a quick-260718-h9x commit. If absent, stage the two files and commit with a message noting the commit folds in h9x's uncommitted co-resident hunks; if present, commit only this task's remaining diff.
- Then SUMMARY.md + STATE.md row; commit docs. Do NOT push.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1 # code commit touches exactly 2 files</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- tsc clean; landing 4/5 (pre-existing flake) + cacheability 4/4
- DOM at 1206px and 1390px: identical hero metrics (measured: h1 2 lines @54.3/56px, p 2 lines @18px, Start 184×46@16, See Demo 166×46@16, shared row top)
</verification>

<success_criteria>
Every desktop width ≥1024px renders the same hero (2-row title, 2-row 18px description, 46px CTAs in a row); tablets and mobile unchanged; commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-u9c-hero-desktop-variant-all-widths/260718-u9c-SUMMARY.md`
</output>
