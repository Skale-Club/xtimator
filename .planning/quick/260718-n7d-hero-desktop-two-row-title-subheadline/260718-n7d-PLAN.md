---
phase: quick-260718-n7d
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - components/landing/hero-section.tsx

must_haves:
  truths:
    - "On desktop (≥1280px), the hero h1 renders as exactly 2 rows: 'Professional estimates' / 'in seconds.'"
    - "On desktop (≥1280px), the hero subheadline renders as 2 rows (break before 'and branded') instead of 3, at 18px (10% under the previous 20px)"
    - "Mobile (<640px) and tablet (640-1279px, governed by the iPad media queries in globals.css) rendering is unchanged: h1 keeps its existing break pattern, subheadline keeps its 3-row md+ breaks and existing sizes"
    - "Headline/subheadline text content is DB-driven and unchanged — only <br> visibility and a nowrap span change; unit tests asserting textContent still pass"
  artifacts:
    - path: "components/landing/hero-section.tsx"
      provides: "Desktop 2-row h1 + 2-row 18px subheadline; sub-desktop behavior preserved"
      contains: "xl:whitespace-nowrap"
  key_links:
    - from: "h1 first <br>"
      to: "desktop 2-row layout"
      via: "xl:hidden on the word-1 break + xl:whitespace-nowrap span around words 1-2; the existing 'hidden sm:block' break before 'in seconds.' supplies the single desktop break"
      pattern: "xl:hidden"
    - from: "subheadline breaks"
      to: "desktop 2-row layout"
      via: "existing tablet breaks gain xl:hidden; a new 'hidden xl:block' break before 'and branded' splits desktop into 2 balanced rows"
      pattern: "hidden xl:block"
---

<objective>
Desktop-only (≥1280px, where the iPad media queries in globals.css no longer apply) hero typography changes per user request with screenshots:

1. H1 in 2 rows: "Professional estimates" / "in seconds." (currently 3 rows: break hardcoded after word 1, plus sm+ break after word 2).
2. Subheadline 10% smaller (xl 20px → 18px) and in 2 rows instead of 3 (currently hard breaks before "pricing," and before "you leave" at md+).
</objective>

<context>
- components/landing/hero-section.tsx — h1 word-split breaks (lines 49-56) and subheadline IIFE breaks (lines 58-81)
- app/globals.css iPad media queries own 640-1279px (`.hero-h1` / `.hero-left p` overrides); true desktop = xl (≥1280px), matching the user's ~1390px screenshots — so all changes gate on `xl:`
- At xl the h1 is 56px in a 580px box; "Professional estimates" measures right at that limit, so words 1-2 get an `xl:whitespace-nowrap` span (a few px of invisible overflow on the dark background beats a surprise 3-row wrap)
- Subheadline desktop split: break before "and branded" → "Record a site walkthrough, add photos, pricing," / "and branded estimate before you leave the driveway." (balanced ~47/52 chars). Fallback when the DB text lacks "and branded": natural wrap at xl (tablet/mobile breaks unaffected)
- tests/unit/components/landing-page.test.tsx asserts heading textContent only — unaffected
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop 2-row h1 and 2-row 18px subheadline</name>
  <files>components/landing/hero-section.tsx</files>
  <action>
1. H1: wrap word[0] + first break + word[1] in `<span className="xl:whitespace-nowrap">`; change the first `<br />` to `<br className="xl:hidden" />`. Keep the `hidden sm:block` break before the remainder. Update the stale "3 lines on desktop" comment.
2. Subheadline size: `xl:text-[20px]` → `xl:text-[18px]`; refresh the ratio comment.
3. Subheadline breaks: add `xl:hidden` to both existing `block sm:hidden md:block` breaks; insert `<br className="hidden xl:block" />` before "and branded" (computed via indexOf, guarded to fall back to natural xl wrap when absent or out of order).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/components/landing-page.test.tsx</automated>
    <manual>Browser preview at ≥1280px: h1 shows 2 rows, subheadline shows 2 rows at 18px; tablet/mobile widths unchanged</manual>
  </verify>
  <done>Desktop hero shows the 2-row title and 2-row 18px subheadline; sub-1280px rendering identical to before; typecheck + landing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code only, then planning artifacts) — DO NOT PUSH</name>
  <files>components/landing/hero-section.tsx</files>
  <action>
- Stage exactly components/landing/hero-section.tsx (working tree has unrelated dirty files from another task — do not stage them); commit on `dev`: `fix(quick-260718-n7d): hero desktop 2-row title and smaller 2-row subheadline`
- Then create SUMMARY.md, add STATE.md Quick Tasks Completed row, commit planning artifacts: `docs(quick-260718-n7d): add quick-task plan/summary and STATE row`
- Do NOT push.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1 # code commit touches exactly 1 file</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- `npx vitest run tests/unit/components/landing-page.test.tsx` → all pass
- Visual at ~1390px: title 2 rows, subheadline 2 rows @18px; at tablet/mobile widths nothing moved
</verification>

<success_criteria>
Desktop hero title reads "Professional estimates" / "in seconds." on two rows; subheadline is 18px on two balanced rows; mobile/tablet untouched; commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-n7d-hero-desktop-two-row-title-subheadline/260718-n7d-SUMMARY.md`
</output>
