---
phase: quick-260723-a7c
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - components/landing/how-it-works-section.tsx
  - app/globals.css
  - lib/platform-config.ts
  - lib/schemas/admin.ts
  - app/admin/landing/landing-editor.tsx
  - components/landing/landing-page.tsx

must_haves:
  truths:
    - "The three How-it-works cards render their animated backgrounds again (waveform bars, speech-bubble typing dots, camera outline/flash) ON TOP of the existing halo glow"
    - "A super-admin can turn those card background animations on/off from /admin/landing → How It Works tab; the setting persists in platform_branding.landing_content and takes effect on the public page immediately"
    - "When the toggle is OFF, cards show halo-only (the current state); the halo is always shown regardless"
    - "Reduced-motion users never see motion — the animation components already gate on prefers-reduced-motion internally"
    - "No DB migration: howItWorksAnimations is a boolean field inside the existing landing_content JSON column, defaulting to true for rows saved before it existed"
  artifacts:
    - path: "components/landing/how-it-works-section.tsx"
      provides: "Restored SoundWaveBackground/SpeechBubbleBackground/CameraBackground + WAVEFORM_BARS; HowItWorksSection gains animationsEnabled prop that gates the show* flags"
      contains: "animationsEnabled"
    - path: "app/globals.css"
      provides: "Re-added keyframes bar-pulse, cam-flash-unit, bubble-dot-1..6"
      contains: "@keyframes bar-pulse"
    - path: "lib/platform-config.ts"
      provides: "howItWorksAnimations added to LandingContent type + DEFAULT_LANDING_CONTENT (true) + backfill (?? true) in getBranding"
      contains: "howItWorksAnimations"
    - path: "lib/schemas/admin.ts"
      provides: "landingContentSchema gains howItWorksAnimations: z.boolean().optional().default(true)"
      contains: "howItWorksAnimations"
    - path: "app/admin/landing/landing-editor.tsx"
      provides: "Switch toggle bound to howItWorksAnimations in the How It Works tab"
      contains: "howItWorksAnimations"
    - path: "components/landing/landing-page.tsx"
      provides: "Passes animationsEnabled={content.howItWorksAnimations} into HowItWorksSection"
      contains: "animationsEnabled"
  key_links:
    - from: "admin toggle (howItWorksAnimations)"
      to: "HowItWorksSection animationsEnabled"
      via: "landing_content JSON → getBranding → landing-page.tsx prop"
      pattern: "animationsEnabled={content.howItWorksAnimations}"
---

<objective>
Two-part quick task. (1) Restore the animated card backgrounds on the How It Works landing section that were dropped on 2026-07-18 in commit 78cfbe3c ("keep halo only, drop blue SVG line art") — the audio waveform bars, the speech-bubble typing dots, and the camera outline/flash — while keeping the current halo glow behind each image. (2) Add a super-admin toggle at /admin/landing that enables/disables those card background animations, persisted in the existing landing_content JSON (no migration).
</objective>

<context>
- The removed component code lives verbatim at 78cfbe3c^:components/landing/how-it-works-section.tsx (SoundWaveBackground card 1 / SpeechBubbleBackground card 2 / CameraBackground card 3, plus the WAVEFORM_BARS precompute and StepCard showWave/showCursor/showPhotos props).
- The removed keyframes live in the 78cfbe3c diff of app/globals.css: bar-pulse, cam-flash-unit, bubble-dot-1..6 (they sat between `@source not "../.planning";` and the `/* Photo icon swap */` comment).
- Config path: LandingContent (lib/platform-config.ts) is stored in the platform_branding.landing_content JSON column; getBranding reads it with per-field backfill; landingContentSchema (lib/schemas/admin.ts) validates; saveLandingContent persists `...parsed.data`; getLandingContent feeds the public LandingPage. So a new boolean flows end-to-end with NO migration and NO action change.
- Switch UI already exists (components/ui/switch.tsx) and is used with FormField elsewhere.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore keyframes</name>
  <files>app/globals.css</files>
  <action>Re-insert bar-pulse, cam-flash-unit, bubble-dot-1..6 keyframes right after `@source not "../.planning";`.</action>
  <verify><automated>grep -c "@keyframes bar-pulse" app/globals.css</automated></verify>
  <done>Keyframes present.</done>
</task>

<task type="auto">
  <name>Task 2: Restore animation components + gate by animationsEnabled</name>
  <files>components/landing/how-it-works-section.tsx</files>
  <action>Re-add WAVEFORM_BARS + SoundWaveBackground + CameraBackground + SpeechBubbleBackground and StepCard show* props. Keep HaloBackground rendered unconditionally under the animations. Add animationsEnabled?: boolean = true to HowItWorksSection; gate each show* flag with animationsEnabled at both call sites (grid + ticker).</action>
  <verify><automated>npx tsc --noEmit -p tsconfig.ci.json</automated></verify>
  <done>Cards layer animation over halo when enabled; halo-only when disabled.</done>
</task>

<task type="auto">
  <name>Task 3: Config field</name>
  <files>lib/platform-config.ts, lib/schemas/admin.ts</files>
  <action>Add howItWorksAnimations boolean to LandingContent type, DEFAULT_LANDING_CONTENT (true), and getBranding backfill (?? true). Add howItWorksAnimations: z.boolean().optional().default(true) to landingContentSchema.</action>
  <verify><automated>npx tsc --noEmit -p tsconfig.ci.json</automated></verify>
  <done>Field flows type→schema→persistence.</done>
</task>

<task type="auto">
  <name>Task 4: Admin toggle + wire consumer</name>
  <files>app/admin/landing/landing-editor.tsx, components/landing/landing-page.tsx</files>
  <action>Add a Switch FormField bound to howItWorksAnimations in the How It Works tab. Pass animationsEnabled={content.howItWorksAnimations} from LandingPage into HowItWorksSection.</action>
  <verify><automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/components/landing-page.test.tsx</automated></verify>
  <done>Toggle drives the public animations.</done>
</task>

<task type="auto">
  <name>Task 5: Commit (code, then docs) — DO NOT PUSH</name>
  <action>Commit the 6 code files: `feat(quick-260723-a7c): restore how-it-works card animations + admin on/off toggle`. Then PLAN/SUMMARY + STATE row: `docs(quick-260723-a7c): add quick-task plan/summary and STATE row`. Do NOT push.</action>
  <done>Two local commits; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- landing-page unit tests pass
- Browser: animations visible when toggle ON, halo-only when OFF; admin save round-trips the toggle
</verification>

<success_criteria>
Animated backgrounds are back on the How It Works cards over the halo, and a super-admin can toggle them on/off from /admin/landing with immediate effect. Commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-a7c-howitworks-anim-restore-admin-toggle/260723-a7c-SUMMARY.md`
</output>
