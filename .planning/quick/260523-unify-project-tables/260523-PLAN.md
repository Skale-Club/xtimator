# Quick Task 260523: Unify project tables

## Goal

Standardize project listing UI so the `/projects` page uses the shared `DataTable`/table primitives instead of a separate `ul/li` design, and reuse the same project table structure from the dashboard.

## Tasks

1. Create a reusable project table adapter on top of `components/ui/data-table.tsx`.
2. Move project status badge styling to a neutral projects component and preserve the existing dashboard import.
3. Replace the custom `/projects` list with the shared table while preserving page filters, archive/trash actions, and empty states.
4. Verify TypeScript/tests for the touched components.
