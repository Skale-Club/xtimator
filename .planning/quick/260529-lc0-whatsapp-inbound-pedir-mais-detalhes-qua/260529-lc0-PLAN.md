---
phase: quick-260529-lc0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260529000002_whatsapp_sessions_awaiting_details.sql
  - lib/whatsapp/ask-details.ts
  - lib/inngest/functions/whatsapp-process.ts
  - lib/whatsapp/handler.ts
  - tests/unit/whatsapp/ask-details.test.ts
  - tests/unit/inngest/whatsapp-process-job.test.ts
  - tests/unit/whatsapp/handler.test.ts
autonomous: true
requirements: [WA-DETAILS-01]
must_haves:
  truths:
    - "Vague inbound message → bot asks for more details (localized pt/en/es), NOT a $0 estimate + send/cancel prompt"
    - "The $0 estimate generated from the vague message is deleted and the project reverts to draft / total 0"
    - "An awaiting_details session is created for the sender pointing at the same draft project"
    - "The sender's next message complements the SAME project (no new project) and regenerates"
    - "Re-evaluation of vagueness runs again after each regeneration (loop until priceable)"
  artifacts:
    - path: "supabase/migrations/20260529000002_whatsapp_sessions_awaiting_details.sql"
      provides: "CHECK constraint accepting 'awaiting_details' state"
      contains: "awaiting_details"
    - path: "lib/whatsapp/ask-details.ts"
      provides: "isVagueEstimate + buildAskDetailsMessage (localized) + revertVagueEstimate helpers"
      exports: ["isVagueEstimate", "buildAskDetailsMessage", "revertVagueEstimate"]
    - path: "lib/inngest/functions/whatsapp-process.ts"
      provides: "vagueness branch in whatsAppProcessJob"
      contains: "awaiting_details"
    - path: "lib/whatsapp/handler.ts"
      provides: "awaiting_details routing that re-dispatches with existing projectId"
      contains: "awaiting_details"
  key_links:
    - from: "lib/inngest/functions/whatsapp-process.ts"
      to: "lib/whatsapp/ask-details.ts"
      via: "isVagueEstimate / buildAskDetailsMessage / revertVagueEstimate after generate-estimate"
      pattern: "isVagueEstimate|buildAskDetailsMessage|revertVagueEstimate"
    - from: "lib/whatsapp/handler.ts"
      to: "lib/inngest/events.ts"
      via: "re-dispatch EVENT_WHATSAPP_PROCESS with existing draft_project_id for awaiting_details"
      pattern: "awaiting_details"
---

<objective>
No fluxo inbound do WhatsApp, quando a mensagem do cliente é vaga demais para
precificar (estimate `total == 0` OU sem nenhum line item), o bot deve RESPONDER
PEDINDO MAIS DETALHES (localizado pt/en/es) e abrir sessão `awaiting_details` que
faz a próxima mensagem complementar o MESMO projeto e regenerar — em vez de gerar
e enviar um estimate de $0 e abrir `awaiting_confirm`.

Purpose: Validado em produção 2026-05-29 — mensagem vaga gerou estimate $0,00 +
awaiting_confirm. Comportamento incorreto: o owner recebe um "estimate" inútil.

Output:
- Migration que adiciona `'awaiting_details'` ao CHECK de `whatsapp_sessions.state`.
- Helpers `lib/whatsapp/ask-details.ts` (detecção, mensagem localizada, reversão).
- Branch de "vago" no `whatsAppProcessJob` (Inngest).
- Roteamento de `awaiting_details` no handler (re-despacha o mesmo projectId).
- Testes unitários cobrindo detecção/reversão/roteamento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260529-lc0-whatsapp-inbound-pedir-mais-detalhes-qua/260529-lc0-CONTEXT.md

# Arquivos-chave (já lidos pelo planner — embutidos abaixo os contratos relevantes)
@lib/inngest/functions/whatsapp-process.ts
@lib/whatsapp/handler.ts
@lib/whatsapp/confirm.ts
@lib/services/generate-estimate.ts
@lib/inngest/events.ts

<interfaces>
<!-- Contratos que o executor usa — NÃO explorar o codebase, usar daqui. -->

GenerateEstimateResult (lib/services/generate-estimate.ts):
```typescript
export type EstimateLanguage = 'en' | 'pt' | 'es' // de lib/i18n/resolve-estimate-language
export type GenerateEstimateResult = {
  estimateId: string
  version: number
  clientSuggestion: ClientSuggestion | null
  language: EstimateLanguage
}
```
generateEstimateForProject seta projeto status='estimate_ready' + total=grandTotal,
cria estimate (is_current=true). NÃO ALTERAR este serviço.

WhatsAppProcessPayload (lib/inngest/events.ts):
```typescript
export const EVENT_WHATSAPP_PROCESS = 'whatsapp/process.requested' as const
export type WhatsAppProcessPayload = {
  companyId: string; projectId: string; ownerPhone: string;
  messages: unknown[]; batchKey: string
}
```

whatsapp_sessions schema (migration 20260510000002_phase40_whatsapp.sql):
- state TEXT NOT NULL DEFAULT 'awaiting_input'
  CHECK (state IN ('awaiting_input','awaiting_confirm','awaiting_edit'))
  → constraint inline auto-nomeado: whatsapp_sessions_state_check
- colunas: id, company_id, phone_number, state, draft_project_id,
  draft_estimate_id (REFERENCES estimates ON DELETE SET NULL), expires_at, created_at
- RLS deny-all → SOMENTE service client (requireServiceClient).

Padrão de delete-em-cascata de estimate (confirm.ts handleRegenerate, linha ~217):
```typescript
await supabase.from('estimates').delete().eq('id', session.draft_estimate_id)
// cascade remove estimate_sections + estimate_items
```

formatMoney(amount, currencyCode) de '@/lib/money/currency' (já importado no job).
sendWhatsAppMessage(phone, { type:'text', text:{ body } }) de '@/lib/whatsapp/client'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — aceitar 'awaiting_details' no CHECK de whatsapp_sessions.state</name>
  <files>supabase/migrations/20260529000002_whatsapp_sessions_awaiting_details.sql</files>
  <action>
Criar migration idempotente que substitui o CHECK constraint inline de
`whatsapp_sessions.state` (auto-nomeado `whatsapp_sessions_state_check`) para
incluir `'awaiting_details'`. Padrão idempotente DO $do$ igual a
20260529000001_whatsapp_company_id_unique.sql:

```sql
-- Quick task 260529-lc0: allow whatsapp_sessions.state = 'awaiting_details'
-- so the WhatsApp bot can ask the owner for more details when an inbound
-- message is too vague to price (estimate total == 0 OR no line items),
-- instead of generating/sending a $0 estimate.
-- The inline CHECK created in 20260510000002_phase40_whatsapp.sql is named
-- whatsapp_sessions_state_check by Postgres. Drop + re-add with the new value.
-- Idempotent: guarded by constraint existence / no-op if already includes value.

DO $do$
BEGIN
  -- Drop the existing CHECK (inline auto-named) if present
  IF EXISTS (
    SELECT 1 FROM pg_constraint
    WHERE conname = 'whatsapp_sessions_state_check'
      AND conrelid = 'public.whatsapp_sessions'::regclass
  ) THEN
    ALTER TABLE public.whatsapp_sessions
      DROP CONSTRAINT whatsapp_sessions_state_check;
  END IF;

  -- Re-add with 'awaiting_details' included
  ALTER TABLE public.whatsapp_sessions
    ADD CONSTRAINT whatsapp_sessions_state_check
    CHECK (state IN ('awaiting_input','awaiting_confirm','awaiting_edit','awaiting_details'));
END $do$;
```

NÃO embutir segredos. Aplicar via MCP Supabase Xtimator `apply_migration` (o
executor tem acesso); se indisponível, o orquestrador aplica. Em paralelo deixar
o arquivo no diretório de migrations para paridade com `bunx supabase db push`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/20260529000002_whatsapp_sessions_awaiting_details.sql','utf8'); if(!s.includes('awaiting_details')||!s.includes('whatsapp_sessions_state_check')) process.exit(1)"</automated>
  </verify>
  <done>Migration existe, é idempotente (DO $do$ + DROP IF EXISTS + ADD), inclui 'awaiting_details', e foi aplicada no projeto Supabase Xtimator (insert de teste com state='awaiting_details' não viola o CHECK).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helpers ask-details.ts (detecção + mensagem localizada + reversão) — TDD</name>
  <files>lib/whatsapp/ask-details.ts, tests/unit/whatsapp/ask-details.test.ts</files>
  <behavior>
    - isVagueEstimate(estimate): true quando total == 0 (null/0) OU quando não há
      nenhum line item em nenhuma seção. Recebe um shape mínimo:
      { total: number|null; sections: Array<{ items?: Array<unknown>|null }>|null }.
      false quando total > 0 E existe ao menos 1 item.
    - buildAskDetailsMessage(language): retorna string pedindo detalhes com exemplos
      (tipo de serviço, área m²/cômodos, materiais, prazo), localizada:
      language 'pt' → texto em português; 'es' → espanhol; 'en' (default/qualquer
      outro) → inglês. Cada idioma menciona os 4 exemplos.
    - revertVagueEstimate(supabase, projectId, estimateId): apaga o estimate
      (cascade remove sections/items) via .from('estimates').delete().eq('id',estimateId),
      e reverte o projeto: .from('projects').update({ status:'draft', total:0 }).eq('id',projectId).
      Não lança se estimateId for null/undefined (só reverte o projeto).
  </behavior>
  <action>
Escrever PRIMEIRO os testes em tests/unit/whatsapp/ask-details.test.ts (RED),
depois implementar lib/whatsapp/ask-details.ts (GREEN).

Tipos:
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { EstimateLanguage } from '@/lib/i18n/resolve-estimate-language'

export type VagueCheckEstimate = {
  total: number | null
  sections: Array<{ items?: Array<unknown> | null }> | null
}
export function isVagueEstimate(e: VagueCheckEstimate | null): boolean
export function buildAskDetailsMessage(language: EstimateLanguage): string
export async function revertVagueEstimate(
  supabase: SupabaseClient,
  projectId: string,
  estimateId: string | null
): Promise<void>
```

isVagueEstimate: `const totalNum = e?.total ?? 0; const hasItems = (e?.sections ?? []).some(s => (s?.items?.length ?? 0) > 0); return totalNum <= 0 || !hasItems`.

buildAskDetailsMessage: objeto MESSAGES por idioma (pt/en/es). Exemplo PT:
"Preciso de mais alguns detalhes para preparar o orçamento. Pode me dizer: o tipo
de serviço, a área (m² ou cômodos), os materiais e o prazo desejado?" — EN e ES
equivalentes, sempre cobrindo os 4 exemplos. `return MESSAGES[language] ?? MESSAGES.en`.

revertVagueEstimate: se estimateId, `await supabase.from('estimates').delete().eq('id', estimateId)`
(mesmo padrão de confirm.ts handleRegenerate — cascade limpa sections/items). Depois
`await supabase.from('projects').update({ status: 'draft', total: 0 }).eq('id', projectId)`.

Testes (vitest, com `import { describe, it, expect, vi } from 'vitest'` explícito —
tsc exige imports explícitos neste projeto):
- isVagueEstimate true para total=0; true para total=null; true para sections com
  items vazios; true para sections=null; false para total=100 com 1 item.
- buildAskDetailsMessage('pt') contém marcadores PT; ('es') ES; ('en') EN;
  idioma desconhecido cai em EN. Cada um menciona os 4 exemplos (asserts por
  palavra-chave do idioma).
- revertVagueEstimate: mock supabase com from() retornando { delete: ()=>({eq}) }
  e { update: ()=>({eq}) }. Verificar delete em 'estimates' eq estimateId E
  update em 'projects' com { status:'draft', total:0 }. Com estimateId=null, NÃO
  chama delete em estimates mas ainda reverte projeto.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/whatsapp/ask-details.test.ts</automated>
  </verify>
  <done>Todos os testes de ask-details passam; helpers exportados com as assinaturas acima; mensagens cobrem pt/en/es com os 4 exemplos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Branch "vago" no whatsAppProcessJob — pedir detalhes em vez de awaiting_confirm</name>
  <files>lib/inngest/functions/whatsapp-process.ts, tests/unit/inngest/whatsapp-process-job.test.ts</files>
  <behavior>
    - Após o step 'generate-estimate', o job avalia vagueness lendo o estimate
      recém-criado (total + sections/items).
    - Se VAGO (isVagueEstimate true):
      * apaga o estimate $0 e reverte o projeto (revertVagueEstimate)
      * cria whatsapp_sessions com state='awaiting_details', draft_project_id=projectId,
        draft_estimate_id=null, expires_at = mesmo TTL (SESSION_TTL_MINUTES)
      * envia buildAskDetailsMessage(result.language) ao ownerPhone
      * NÃO executa o step 'confirm-and-session' (não abre awaiting_confirm)
    - Se NÃO vago: comportamento atual inalterado (confirm-and-session + resumo).
    - Source-level contract (teste lê o arquivo): existe um step.run para a
      avaliação/ramo de detalhes e o arquivo referencia 'awaiting_details',
      isVagueEstimate, buildAskDetailsMessage e revertVagueEstimate.
  </behavior>
  <action>
Editar lib/inngest/functions/whatsapp-process.ts. Importar do novo helper:
`import { isVagueEstimate, buildAskDetailsMessage, revertVagueEstimate } from '@/lib/whatsapp/ask-details'`.

Após `const result = await step.run('generate-estimate', ...)`, adicionar um
step que decide o ramo. Substituir o atual `await step.run('confirm-and-session', ...)`
incondicional por:

```typescript
// Re-evaluate "vagueness" — vague inbound text often yields a $0 estimate
// with no line items. In that case ask the owner for more details instead of
// sending a useless $0 confirmation. (Quick task 260529-lc0)
const isVague = await step.run('evaluate-vagueness', async () => {
  const supabase = requireServiceClient()
  const { data: est } = await supabase
    .from('estimates')
    .select('total, sections:estimate_sections(items:estimate_items(id))')
    .eq('id', result.estimateId)
    .single()
  return isVagueEstimate(
    est as { total: number | null; sections: Array<{ items?: Array<unknown> | null }> | null } | null
  )
})

if (isVague) {
  await step.run('ask-details', async () => {
    const supabase = requireServiceClient()
    // Remove the $0 estimate and revert the project to draft so the next
    // inbound message regenerates cleanly against the same project.
    await revertVagueEstimate(supabase, projectId, result.estimateId)
    const expiresAt = new Date(
      Date.now() + SESSION_TTL_MINUTES * 60 * 1000
    ).toISOString()
    await supabase.from('whatsapp_sessions').insert({
      company_id: companyId,
      phone_number: ownerPhone,
      state: 'awaiting_details',
      draft_project_id: projectId,
      draft_estimate_id: null,
      expires_at: expiresAt,
    })
    await sendWhatsAppMessage(ownerPhone, {
      type: 'text',
      text: { body: buildAskDetailsMessage(result.language) },
    })
  })
  return result
}

// Not vague → existing confirm-and-session flow (unchanged)
await step.run('confirm-and-session', async () => { ...existente... })
```

Manter o corpo de 'confirm-and-session' exatamente como hoje. Garantir que os
tipos compilam (result.language é EstimateLanguage).

ATUALIZAR tests/unit/inngest/whatsapp-process-job.test.ts: adicionar asserts
source-level (o teste existente lê o arquivo com readFileSync). Acrescentar:
- src referencia 'awaiting_details'
- src referencia isVagueEstimate( e buildAskDetailsMessage( e revertVagueEstimate(
- existe step.run('evaluate-vagueness'... e step.run('ask-details'...
Manter os asserts existentes (id, idempotency, >=3 step.run, generate-estimate).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/inngest/whatsapp-process-job.test.ts</automated>
  </verify>
  <done>Job ramifica em vago: apaga estimate $0, reverte projeto p/ draft, cria sessão awaiting_details, envia mensagem localizada e NÃO abre awaiting_confirm; caminho não-vago inalterado; tsc limpo; testes do job passam.</done>
</task>

<task type="auto">
  <name>Task 4: Handler roteia awaiting_details para o MESMO projeto (re-despacha sem criar projeto novo)</name>
  <files>lib/whatsapp/handler.ts, tests/unit/whatsapp/handler.test.ts</files>
  <action>
Editar lib/whatsapp/handler.ts. Hoje `processInboundWithDebounce` e
`processInboundMessage` só ramificam quando `existingSession?.state === 'awaiting_confirm'`.
Adicionar ramo para `awaiting_details` que NÃO passa pelo debounce/criação de
projeto: re-despacha EVENT_WHATSAPP_PROCESS com o `draft_project_id` existente
para acumular contexto e regenerar (o job re-avalia vagueness em loop).

Criar uma função interna `dispatchToExistingProject(messages, session, companyId, ownerPhone, supabase)`:
```typescript
async function dispatchToExistingProject(
  messages: WhatsAppMessage[],
  session: { draft_project_id: string | null },
  companyId: string,
  ownerPhone: string,
): Promise<void> {
  if (!messages.length || !session.draft_project_id) return
  const lastMessageId = messages[messages.length - 1].id
  const batchKey = `wa-batch-${lastMessageId}`
  const { inngest } = await import('@/lib/inngest/client')
  const { EVENT_WHATSAPP_PROCESS } = await import('@/lib/inngest/events')
  await inngest.send({
    name: EVENT_WHATSAPP_PROCESS,
    id: batchKey,
    data: {
      companyId,
      projectId: session.draft_project_id, // SAME project — accumulate context
      ownerPhone,
      messages,
      batchKey,
    },
  })
}
```

Em AMBOS `processInboundWithDebounce` e `processInboundMessage`, logo após o
ramo `awaiting_confirm`, adicionar:
```typescript
if (existingSession?.state === 'awaiting_details') {
  // The owner is supplying more detail for the SAME draft project. Re-dispatch
  // to the same projectId so the worker accumulates context and regenerates.
  // (Idempotent via wamid batchKey; no new project, no debounce.)
  await sendTypingIndicator(message.id).catch(() => undefined)
  return dispatchToExistingProject(
    [message],
    existingSession as { draft_project_id: string | null },
    companyId,
    ownerPhone,
  )
}
```
Notas:
- `existingSession` já seleciona `draft_project_id` (linha select existente).
- Manter idempotência por wamid (batchKey = wa-batch-{lastMessageId}).
- NÃO criar projeto novo, NÃO chamar processInboundMessages (que cria projeto).
- O entitlement gate não é re-checado aqui de propósito: a sessão só existe se o
  primeiro processamento (que checou entitlement) passou; e o owner continua a
  mesma conversa. (Registrar isso como nota no SUMMARY.)

ATUALIZAR tests/unit/whatsapp/handler.test.ts: adicionar describe 'awaiting_details
session routing':
- Dado existingSession { state:'awaiting_details', draft_project_id:'p-1' },
  processInboundMessage(TEXT_MESSAGE,...) deve:
  * chamar mockInngestSend uma vez
  * event.data.projectId === 'p-1' (MESMO projeto, não cria novo)
  * NÃO chamar projectInsertSpy (sem criação de projeto)
  * NÃO chamar processConfirmationReply
  Ajustar makeSupabaseMock se necessário para devolver draft_project_id na sessão
  (o select já o inclui; garantir que o mock retorna o campo).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/whatsapp/handler.test.ts</automated>
  </verify>
  <done>Mensagem em sessão awaiting_details re-despacha EVENT_WHATSAPP_PROCESS com o draft_project_id existente, sem criar projeto novo nem passar pelo debounce; idempotência por wamid preservada; testes do handler passam; tsc limpo.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/whatsapp/ask-details.test.ts tests/unit/inngest/whatsapp-process-job.test.ts tests/unit/whatsapp/handler.test.ts` — todos passam.
- `npx tsc --noEmit` — sem erros nos arquivos tocados.
- Migration aplicada: insert manual com state='awaiting_details' não viola CHECK.
- Não há novos segredos no diff (CLAUDE.md secret handling).
</verification>

<success_criteria>
- Mensagem vaga (total==0 OU sem itens) → bot pede detalhes (pt/en/es), apaga o
  estimate $0, reverte projeto para draft/total 0, cria sessão awaiting_details,
  e NÃO abre awaiting_confirm.
- Próxima mensagem do mesmo remetente complementa o MESMO projeto e regenera
  (loop até ficar precificável).
- generateEstimateForProject NÃO foi alterado (compartilhado com UI/MCP).
- Idempotência por wamid/batchKey preservada em todos os caminhos.
</success_criteria>

<output>
Após completar, criar `.planning/quick/260529-lc0-whatsapp-inbound-pedir-mais-detalhes-qua/260529-lc0-SUMMARY.md`.
Registrar no SUMMARY: (a) que o entitlement gate não é re-checado no ramo
awaiting_details (decisão); (b) loop de pedir-detalhes sem limite rígido por ora
(consideração do CONTEXT.md decisão 3 / Claude's Discretion).
</output>
