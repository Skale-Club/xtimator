---
phase: quick-260718-t7d
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - supabase/migrations/20260718000001_price_book_soft_delete.sql
  - types/database.types.ts
  - lib/queries/price-book.ts
  - lib/actions/price-book.ts
  - lib/agent-tools/query-company-data.ts
  - components/price-book/price-book-list.tsx
  - components/app-shell/nav-items.ts
  - app/(app)/trash/page.tsx
  - components/trash/trash-list.tsx
  - tests/unit/price-book/price-book-list.test.tsx
  - tests/unit/trash/trash-list.test.tsx

must_haves:
  truths:
    - "Each price book category header has a select-all checkbox (red-box position, left of the pencil/trash/Adjust cluster) that selects/deselects all VISIBLE items in that category"
    - "Each item row (desktop table + mobile card) has a checkbox; a bulk action bar appears when anything is selected showing the count and a Delete action"
    - "Deleting (single or bulk) is a SOFT delete — sets deleted_at, item disappears from the price book, toast says it moved to Trash"
    - "A 'Trash' entry exists in the left sidebar (between Price Book and Settings), hidden in demo, in the mobile More overflow"
    - "/trash lists recently deleted items (name, category, price, deleted date) with per-item Restore and Delete forever, plus an Empty Trash button that permanently deletes everything (confirm dialog, cannot be undone)"
    - "Soft-deleted items are excluded from: the price book page, estimate-editor autocomplete (getPriceBookItems), the chat agent's listServices/findServiceByName, and CSV-import dedup checks (so a trashed name can be re-imported)"
    - "Permanent-delete actions only touch rows already in trash (deleted_at not null) and are company-scoped"
  artifacts:
    - path: "supabase/migrations/20260718000001_price_book_soft_delete.sql"
      provides: "deleted_at timestamptz column + partial index on active rows"
      contains: "deleted_at"
    - path: "lib/actions/price-book.ts"
      provides: "soft deletePriceBookItem + trashPriceBookItems/restorePriceBookItems/destroyPriceBookItems/emptyPriceBookTrash"
      contains: "emptyPriceBookTrash"
    - path: "app/(app)/trash/page.tsx"
      provides: "Trash route (auth + company gate, lists deleted items)"
    - path: "components/trash/trash-list.tsx"
      provides: "Trash UI: restore / delete forever / empty trash"
  key_links:
    - from: "price-book-list.tsx selection Set"
      to: "trashPriceBookItems server action"
      via: "bulk action bar Delete → confirm dialog → startTransition"
      pattern: "trashPriceBookItems"
    - from: "nav-items.ts Trash entry"
      to: "app/(app)/trash/page.tsx"
      via: "href /trash (demoHidden, overflow)"
      pattern: "'/trash'"
    - from: "getPriceBookItems / listServices / findServiceByName / import dedup"
      to: "active rows only"
      via: ".is('deleted_at', null)"
      pattern: "deleted_at"
---

<objective>
User request (screenshot: red box on the Price Book category header): (1) select-all option per category to bulk-delete items at once; (2) a Trash entry in the left sidebar to view recently deleted items and permanently delete them ("delete all forever"). Requires soft delete on company_price_book.
</objective>

<context>
- Table: company_price_book (no deleted_at today). Supabase project prmqgcrnpuvpzruyzvuv (MCP server f2b95485 matches .env.local URL). Migration is additive + applied via MCP apply_migration; file also committed to supabase/migrations/.
- deletePriceBookItem currently hard-deletes; single-item delete converts to soft (reversible via Trash).
- Read sites to filter: lib/queries/price-book.ts getPriceBookItems; lib/agent-tools/query-company-data.ts listServices+findServiceByName; lib/actions/price-book.ts import dedup (~line 330) + import-wizard commit dedup (~line 540). Explicit-id paths (undo import, bulk adjust) untouched. seedIndustryPriceBook's own count is unfiltered — trashing everything does NOT re-trigger the seed (correct).
- deleteFolder guard counts ALL items including trashed (unchanged) — a category with only trashed items refuses deletion until trash is emptied; FK would block anyway.
- Selection semantics: header checkbox targets the VISIBLE (search-filtered) items of that category; indeterminate when partially selected.
- Nav: NAV_ITEMS in components/app-shell/nav-items.ts; add Trash (Trash2 icon) between Price Book and Settings, demoHidden + overflow. BottomNav consumes NAV_ITEMS automatically.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration (file + apply) and types</name>
  <files>supabase/migrations/20260718000001_price_book_soft_delete.sql, types/database.types.ts</files>
  <action>
- Migration: `alter table company_price_book add column if not exists deleted_at timestamptz;` + partial index `idx_company_price_book_active on company_price_book (company_id) where deleted_at is null;`. Write file AND apply via MCP f2b95485 apply_migration (name: price_book_soft_delete).
- types/database.types.ts: add `deleted_at: string | null` to company_price_book Row, `deleted_at?: string | null` to Insert and Update.
  </action>
  <verify><automated>SQL applied without error; typecheck in Task 4 covers types</automated></verify>
  <done>Column + index exist remotely; migration committed; types updated.</done>
</task>

<task type="auto">
  <name>Task 2: Queries, actions, agent tools</name>
  <files>lib/queries/price-book.ts, lib/actions/price-book.ts, lib/agent-tools/query-company-data.ts</files>
  <action>
- getPriceBookItems: `.is('deleted_at', null)`. New `DeletedPriceBookItem` (PriceBookItem + deleted_at: string) and `getDeletedPriceBookItems` (`.not('deleted_at','is',null)`, order deleted_at desc).
- Actions: deletePriceBookItem → update `{deleted_at: now}` (keep signature/return). New company-scoped actions with assertWritable: `trashPriceBookItems(ids)`, `restorePriceBookItems(ids)`, `destroyPriceBookItems(ids)` (hard delete, additionally `.not('deleted_at','is',null)`), `emptyPriceBookTrash()` (hard delete all trashed for company). All revalidate /price-book and /trash.
- Filter `.is('deleted_at', null)` on the two import dedup queries and on listServices/findServiceByName.
  </action>
  <verify><automated>covered by Task 4 gates</automated></verify>
  <done>Soft delete is the only delete path from the price book UI; trash queries/actions exist; active-row readers filtered.</done>
</task>

<task type="auto">
  <name>Task 3: UI — bulk select on price book, sidebar Trash entry, /trash page</name>
  <files>components/price-book/price-book-list.tsx, components/app-shell/nav-items.ts, app/(app)/trash/page.tsx, components/trash/trash-list.tsx</files>
  <action>
- price-book-list.tsx: selection Set state; per-row Checkbox (new leading table column on desktop, checkbox on mobile cards); category-header select-all Checkbox in the ml-auto cluster before the pencil (red-box position), checked/indeterminate per visible folder items; bulk bar (sticky bottom, shown when selected.size>0): "N selected", Clear, destructive Delete → AlertDialog → trashPriceBookItems → toast "Moved N item(s) to Trash", clear + refresh. Single-delete dialog copy updated to "Move to Trash" semantics.
- nav-items.ts: `{ label: 'Trash', href: '/trash', icon: Trash2, demoHidden: true, overflow: true }` before Settings.
- app/(app)/trash/page.tsx: mirror price-book page auth/company pattern; getDeletedPriceBookItems; render TrashList in glass Card.
- components/trash/trash-list.tsx: list (image, name, category, price, deleted date), per-row Restore + Delete forever (confirm), header Empty Trash (confirm: permanent, cannot be undone), empty state, useTransition + toasts + router.refresh().
  </action>
  <verify><automated>covered by Task 4 gates</automated></verify>
  <done>All three UI surfaces work per must_haves.</done>
</task>

<task type="auto">
  <name>Task 4: Tests + gates + commits — DO NOT PUSH</name>
  <files>tests/unit/price-book/price-book-list.test.tsx, tests/unit/trash/trash-list.test.tsx</files>
  <action>
- Extend price-book-list tests: select-all checkbox selects visible folder items and shows the bulk bar count; bulk Delete confirm calls trashPriceBookItems with the selected ids.
- New trash-list tests: renders deleted items; Restore calls restorePriceBookItems; Empty Trash confirm calls emptyPriceBookTrash.
- Gates: `npx tsc --noEmit -p tsconfig.ci.json` clean; `npx vitest run tests/unit/price-book tests/unit/trash` green.
- Commit code (`feat(quick-260718-t7d): ...`), then SUMMARY + STATE row (`docs(quick-260718-t7d): ...`). DO NOT PUSH.
  </action>
  <verify><automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/price-book tests/unit/trash</automated></verify>
  <done>Gates green; two local commits on dev.</done>
</task>

</tasks>

<verification>
- Scoped typecheck clean; price-book + trash unit tests green
- Migration applied on remote (column + partial index present)
- Manual flow (blocked on auth for the agent; user-verifiable): select all in a category → Delete → items land in /trash → Restore brings one back → Empty Trash removes the rest permanently
</verification>

<success_criteria>
Bulk select-and-delete works from the category header; deleted items are recoverable in a sidebar Trash page until permanently deleted (individually or all at once); no reader of active price book data ever sees trashed rows; commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-t7d-price-book-bulk-select-trash/260718-t7d-SUMMARY.md`
</output>
