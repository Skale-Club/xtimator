---
phase: quick-260705-2gp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/openrouter-client.ts
  - lib/ai/with-fallback.ts
  - lib/services/generate-estimate.ts
  - lib/inngest/functions/transcribe-audio.ts
  - lib/admin/integrations-providers.ts
  - lib/billing/byok.ts
  - lib/estimate/price-research/adapters/openrouter-web.ts
  - lib/estimate/price-research/adapters/anthropic-web.ts
  - lib/estimate/price-research/cache.ts
  - tests/unit/ai/transcribe-fallback.test.ts
  - tests/unit/ai/with-fallback.test.ts
  - tests/unit/ai/empty-output-guards.test.ts
  - tests/unit/services/generate-estimate.test.ts
  - tests/unit/billing/byok.test.ts
  - tests/unit/estimate/price-research-openrouter-web.test.ts
  - tests/unit/estimate/price-research-anthropic-web.test.ts
  - tests/unit/estimate/price-research-cache.test.ts
autonomous: true
requirements: [D1, D2, D3, D4, D5, D6, D7, D8, D9]

must_haves:
  truths:
    - "Silent/garbage audio makes the transcription job FAIL with a clear user-facing message instead of saving an empty transcript, recording success, and charging credits"
    - "A photo analysis that returns no text fails loudly instead of silently feeding an estimate with zero photo context"
    - "A project whose only inputs are UNANALYZED photos (no ai_description) is rejected by the generation precondition instead of producing a context-free estimate"
    - "Super-admin can see/rotate the Gemini fallback key and configure the OpenAI Whisper-fallback key in the admin AI integrations panel (OpenRouter stays primary)"
    - "When both AI providers fail, the thrown ProvidersUnavailableError exposes the fallback's error as fallbackCause while .cause remains the primary error"
    - "When a research model returns parseable results but ZERO citations are indexed, a console.warn makes the silent 100%-evidence-gate-rejection mode visible"
    - "An Inngest retry of the transcription function never inserts a duplicate ai_cost_events row"
    - "A BYOK key decrypt failure is captured in Sentry (tagged byok.keyResolution + company_id), not just console"
    - "A failed price_research_cache upsert emits a console.warn instead of vanishing"
  artifacts:
    - path: "lib/ai/openrouter-client.ts"
      provides: "Empty-output guards on transcription + vision"
      contains: "Transcription produced no text"
    - path: "lib/ai/with-fallback.ts"
      provides: "fallbackCause on ProvidersUnavailableError"
      contains: "fallbackCause"
    - path: "lib/services/generate-estimate.ts"
      provides: "hasPhotos counts only analyzed photos"
      contains: "photos.some"
    - path: "lib/admin/integrations-providers.ts"
      provides: "gemini + openai rows restored in the 'ai' category"
      contains: "'gemini'"
    - path: "lib/inngest/functions/transcribe-audio.ts"
      provides: "retry-idempotent cost capture"
      contains: "record-ai-cost"
    - path: "lib/billing/byok.ts"
      provides: "Sentry capture on BYOK key-resolution failure"
      contains: "byok.keyResolution"
    - path: "lib/estimate/price-research/adapters/openrouter-web.ts"
      provides: "flat+nested annotation tolerance and zero-citations warn"
      contains: "0 citations indexed"
    - path: "lib/estimate/price-research/adapters/anthropic-web.ts"
      provides: "zero-citations warn mirror"
      contains: "0 citations indexed"
    - path: "lib/estimate/price-research/cache.ts"
      provides: "cache-write failure observability"
      contains: "cache write failed"
  key_links:
    - from: "lib/ai/openrouter-client.ts"
      to: "lib/inngest/functions/transcribe-audio.ts onFailure (ai_job.failed notification)"
      via: "thrown Error propagating out of step.run('whisper-transcribe')"
      pattern: "Transcription produced no text"
    - from: "lib/admin/integrations-providers.ts CATEGORIES 'ai'.providers"
      to: "admin /admin/integrations/ai cards + platform_integrations rows"
      via: "loadCategoryInitials iterating category.providers (generic — no other UI change needed)"
      pattern: "id: 'openai' as IntegrationProvider"
    - from: "lib/inngest/functions/transcribe-audio.ts"
      to: "ai_cost_events single-row-per-run invariant"
      via: "step.run('record-ai-cost', ...) memoization across Inngest retries"
      pattern: "step\\.run\\('record-ai-cost'"
    - from: "lib/estimate/price-research/adapters/*.ts"
      to: "operator log stream (degraded-provider detection)"
      via: "console.warn on results>0 && citations==0"
      pattern: "\\[price-research\\]"
---

<objective>
Fail-proof the generation flow: validate empty AI outputs (transcription, vision), tighten the generation input precondition, restore fallback-key configurability in the admin panel, harden research citation parsing with visibility into the zero-citations failure mode, and close four observability/idempotency gaps (fallbackCause, BYOK Sentry capture, duplicate cost events, silent cache-write failures).

Purpose: A 4-subsystem adversarial audit (verified claim-by-claim against code + production DB) found the pipeline records SUCCESS on empty AI outputs, generates estimates from zero usable context, has a DEAD OpenAI transcription fallback in production (key row missing and unmanageable in the panel), and a measured 97% price-research evidence-gate rejection with zero telemetry. Every fix here is additive/defensive.

Output: 9 defects (D1-D9) closed across lib/ai, the generation pipeline, admin config, and price-research — with unit tests locking each new behavior and all existing contracts preserved (the only intentional contract changes are the empty-output throws, which no existing test asserts against).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@lib/ai/openrouter-client.ts
@lib/ai/with-fallback.ts
@lib/services/generate-estimate.ts (lines 127-201)
@lib/admin/integrations-providers.ts
@lib/billing/byok.ts
@lib/inngest/functions/transcribe-audio.ts (lines 130-250)
@lib/estimate/price-research/adapters/openrouter-web.ts
@lib/estimate/price-research/adapters/anthropic-web.ts
@lib/estimate/price-research/cache.ts
@lib/observability/capture.ts (never-throw Sentry pattern to mirror)
@tests/unit/ai/with-fallback.test.ts
@tests/unit/ai/transcribe-fallback.test.ts
@tests/unit/services/generate-estimate.test.ts
@tests/unit/estimate/price-research-openrouter-web.test.ts
@tests/unit/estimate/price-research-cache.test.ts
@tests/unit/billing/byok.test.ts

<interfaces>
<!-- Verified against the codebase 2026-07-05. Executor should use these directly. -->

From lib/ai/with-fallback.ts (current — D8 modifies this):
```typescript
export class ProvidersUnavailableError extends Error {
  readonly providerUnavailable = true as const
  constructor(causeOrMessage?: unknown, maybeCause?: unknown) { ... }
}
export async function callWithFallback<T>(args: {
  op: string
  primary: () => Promise<T>
  fallback: () => Promise<T>
}): Promise<FallbackOutcome<T>>
// Both-fail path today (lines 102-106): `catch { throw new ProvidersUnavailableError(primaryErr) }`
// TEST CONTRACT (with-fallback.test.ts): .cause === PRIMARY error, providerUnavailable === true.
```

From lib/platform-config.ts:40-45 (NO change needed — both ids already valid):
```typescript
export type IntegrationProvider =
  | 'resend' | 'anthropic' | 'openai' | 'gemini' | 'openrouter' | ...
```

From lib/admin/integrations-providers.ts (row shape to mirror exactly):
```typescript
{
  id: 'openrouter' as IntegrationProvider,
  title: 'OpenRouter',
  description: 'Single API key, hundreds of models. ...',
}
```

From lib/estimate/price-research/adapters/openrouter-web.ts:48-51 (D4 extends this type):
```typescript
type UrlCitation = {
  type?: string
  url_citation?: { url?: string; title?: string; content?: string }
}
```

From lib/inngest/functions/transcribe-audio.ts:202-218 (D7 target — exact current code):
```typescript
const minutes = (ident.durationSeconds ?? 0) / 60
const whisperCost = computeWhisperCostUsd(ident.durationSeconds)
void recordAICost({
  attemptId, operationType: 'audio_minutes', provider: 'openrouter',
  model: sttModel, realCostUsd: whisperCost,
  companyId: ident.companyId, projectId: ident.projectId,
  units: minutes > 0 ? minutes : null,
})
```

VERIFIED TEST FACTS (do not re-derive):
- generate-estimate.test.ts:291-301 "accepts photos-only input (no transcript)" — its mock photo ALREADY has `ai_description: 'Kitchen with damaged cabinets'` → D3 keeps it green as-is.
- transcribe-fallback.test.ts has exactly 3 cases (primary success / fallback serves / both-fail throws) — none asserts empty-transcript acceptance → D1's throw breaks nothing.
- price-research-cache.test.ts buildServiceMock's upsert already resolves `{ data: null, error: null }` → D9's `const { error } = await ...upsert(...)` destructure is safe for all existing cases.
- tests/unit/inngest/transcribe-audio-job.test.ts asserts via readFileSync + regex on step.run('whisper-transcribe') / ('save-transcript') — D7's ADDED step.run is invisible to it.
- No test locks the CATEGORIES catalog shape → D5 is safe.
- byok.test.ts does NOT mock @sentry/nextjs today — it MUST gain `vi.mock('@sentry/nextjs', () => ({ captureException: vi.fn() }))` when byok.ts starts importing Sentry (Task 2).
- quality-signal.test.ts (lines 10-16) is the in-repo precedent for mocking @sentry/nextjs per-file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Empty-output guards in the AI client + fallbackCause on ProvidersUnavailableError (D1, D2, D8)</name>
  <files>lib/ai/openrouter-client.ts, lib/ai/with-fallback.ts, tests/unit/ai/transcribe-fallback.test.ts, tests/unit/ai/with-fallback.test.ts, tests/unit/ai/empty-output-guards.test.ts</files>
  <behavior>
    - Test (transcribe-fallback.test.ts, NEW case): OpenRouter STT responds ok with `{ text: '' }` → `transcribeAudioOR` rejects with message containing 'Transcription produced no text'. The 3 existing cases stay byte-identical and green.
    - Test (empty-output-guards.test.ts, NEW file): `analyzePhotoOR` with fetch mocked to an ok response whose `choices[0].message.content` is `''` → rejects with 'Photo analysis produced no description'. Second case: non-empty content passes through unchanged (returns the exact string). Mock '@/lib/platform-config' (getIntegrationKey → 'or-key-placeholder'), '@/lib/observability/langfuse' (generation/flushAsync no-ops, mirror transcribe-fallback.test.ts lines 16-21), '@/lib/billing/record-ai-cost' (recordAICost: vi.fn(async () => {})), and '@/lib/ai/providers/gemini' (defensive no-op — never reached since an empty primary is a wrapper "success").
    - Test (with-fallback.test.ts, EXTEND both-fail coverage): the caught ProvidersUnavailableError exposes `fallbackCause` instanceof Error with message 'FALLBACK_ERR' while `.cause` REMAINS the primary error ('PRIMARY_ERR') and `providerUnavailable === true`. Existing 4 cases unchanged.
  </behavior>
  <action>
    D1 — lib/ai/openrouter-client.ts, transcribeAudioOR (after the callWithFallback destructure, currently lines 153-158): add
    `if (result.trim().length === 0) throw new Error('Transcription produced no text — no speech detected in the audio')`
    before `return result`. Placement is deliberately AFTER the wrapper (per the audit): an empty-but-HTTP-ok primary response is a wrapper "success", so the fallback is not consulted for it — genuinely silent audio returns '' from both providers anyway, and the throw propagates out of step.run('whisper-transcribe') so the Inngest onFailure (ai_job.failed notification, already wired) and the WhatsApp per-item dropped-note catch both surface a CLEAR message instead of a silent empty save + credit charge.

    D2 — same file, analyzePhotoOR (after its callWithFallback destructure, currently lines 283-291): add
    `if (result.trim().length === 0) throw new Error('Photo analysis produced no description')`
    before `return result`. Same rationale/error-surfacing path.

    D8 — lib/ai/with-fallback.ts: add a public optional field `fallbackCause?: unknown` to ProvidersUnavailableError (declare on the class; do NOT change the existing constructor signatures — the dual-shape constructor is a pinned test contract). In callWithFallback's both-fail path, change `catch {` to `catch (fallbackErr) {`, construct `const err = new ProvidersUnavailableError(primaryErr)`, assign `err.fallbackCause = fallbackErr`, throw `err`. Message stays 'All providers unavailable', `.cause` stays the PRIMARY error, `providerUnavailable` brand untouched. Update the class/function doc comments to mention fallbackCause (an operator can now see WHY the fallback also failed, e.g. 'Gemini API key not configured').

    Do NOT touch translateTextsOR, traceTranscription, recordAICost calls, or any other export.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai</automated>
  </verify>
  <done>Empty transcript and empty photo analysis both throw with the exact audit-specified messages; ProvidersUnavailableError carries fallbackCause without disturbing the .cause/providerUnavailable contract; all files under tests/unit/ai pass including the 3 pre-existing transcribe-fallback cases and 4 pre-existing with-fallback cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generation precondition, admin fallback keys, BYOK Sentry capture, cost-event idempotency (D3, D5, D6, D7)</name>
  <files>lib/services/generate-estimate.ts, lib/admin/integrations-providers.ts, lib/billing/byok.ts, lib/inngest/functions/transcribe-audio.ts, tests/unit/services/generate-estimate.test.ts, tests/unit/billing/byok.test.ts</files>
  <behavior>
    - Test (generate-estimate.test.ts, NEW case): photos WITHOUT ai_description (e.g. `[{ id: 'photo-1', ai_description: null }]`), no transcripts, no prompts → rejects with 'At least one audio transcript, photo, or prompt is required'. The existing "accepts photos-only input (no transcript)" case stays green UNCHANGED (its mock already carries ai_description — verified).
    - Test (byok.test.ts, NEW case): row `{ byok_enabled: true, byok_openrouter_key: 'not-valid-json' }` → getByokOpenRouterKey returns null (existing fail-open contract) AND the mocked Sentry captureException was called once with `tags: { background: 'byok.keyResolution', company_id: <the id passed> }`. Requires adding `vi.mock('@sentry/nextjs', () => ({ captureException: vi.fn() }))` at the top of byok.test.ts (precedent: quality-signal.test.ts lines 10-16). All existing byok cases unchanged.
  </behavior>
  <action>
    D3 — lib/services/generate-estimate.ts line 135: replace
    `const hasPhotos = photos.length > 0`
    with
    `const hasPhotos = photos.some((p) => p.ai_description && p.ai_description.trim().length > 0)`
    so the precondition matches what the prompt builder actually consumes (lines 175-177 filter to ai_description). Keep the error message string byte-identical (existing test asserts it). This is a mock-realism-aligned tightening, not a contract change — the service always read ai_description for the prompt.

    D5 — lib/admin/integrations-providers.ts: in the 'ai' category's `providers` array, AFTER the existing openrouter row, re-add two rows mirroring its exact shape (id/title/description, `as IntegrationProvider` cast):
    1. `{ id: 'gemini' as IntegrationProvider, title: 'Google Gemini', description: 'Fallback engine for estimate generation and photo analysis when OpenRouter fails. OpenRouter remains the primary engine for all AI tasks.' }`
    2. `{ id: 'openai' as IntegrationProvider, title: 'OpenAI', description: 'Whisper speech-to-text fallback — used only when OpenRouter transcription fails. OpenRouter remains the primary transcription engine.' }`
    Both ids are already in the IntegrationProvider union (platform-config.ts:40-45) and loadCategoryInitials handles any provider list generically — NO other UI changes. Context: prod DB has a gemini key that is invisible/unrotatable in the panel, and the openai row is MISSING entirely, making the OpenAI transcription fallback DEAD (openrouter-client.ts:129-130 throws 'OpenAI transcription key not configured'). Restoring the rows lets the operator save the key — the code path is already wired.

    D6 — lib/billing/byok.ts: add `import * as Sentry from '@sentry/nextjs'`. In getByokOpenRouterKey's catch block, KEEP the existing console.warn and add, wrapped in its own inner try/catch (never-throw discipline, mirror lib/observability/capture.ts):
    `Sentry.captureException(err, { tags: { background: 'byok.keyResolution', company_id: companyId } })`
    OBSERVABILITY ONLY — the fail-open-to-platform-key return and the billing-policy question (debiting byok_enabled companies served by the platform key) are explicitly DEFERRED; do not change any return value or debit behavior.

    D7 — lib/inngest/functions/transcribe-audio.ts (~line 209): replace the bare `void recordAICost({ ... })` with
    `await step.run('record-ai-cost', async () => { await recordAICost({ ...same payload, byte-identical fields... }) })`
    Keep `minutes` and `whisperCost` computed OUTSIDE the step (lines 202-208 unchanged) so the SAME whisperCost value threads into both 'record-ai-cost' and the adjacent 'record-credit-debit' step (mirror its shape exactly). This makes the cost insert memoized across Inngest retries — a save-transcript retry can no longer double-insert an ai_cost_events row. Preserve the surrounding comments' intent (update the "void so a cost-write never breaks transcription" wording: recordAICost is never-throw, so awaiting inside a step is still safe).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/services/generate-estimate.test.ts tests/unit/billing/byok.test.ts tests/unit/inngest/transcribe-audio-job.test.ts</automated>
  </verify>
  <done>Unanalyzed-photos-only input is rejected while the analyzed-photos-only case still passes; gemini + openai cards render from CATEGORIES (source contains both ids in the 'ai' category); BYOK decrypt failure calls Sentry.captureException with the byok.keyResolution tag and still returns null; recordAICost in transcribe-audio sits inside step.run('record-ai-cost') with whisperCost computed outside; all three targeted test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Research citation parsing tolerance + zero-citations telemetry + cache-write observability (D4, D9)</name>
  <files>lib/estimate/price-research/adapters/openrouter-web.ts, lib/estimate/price-research/adapters/anthropic-web.ts, lib/estimate/price-research/cache.ts, tests/unit/estimate/price-research-openrouter-web.test.ts, tests/unit/estimate/price-research-anthropic-web.test.ts, tests/unit/estimate/price-research-cache.test.ts</files>
  <behavior>
    - Test (price-research-openrouter-web.test.ts, NEW case): a FLAT-shape annotation `{ type: 'url_citation', url: 'https://example.com/drywall-prices', content: 'Average drywall repair runs about $150 in Austin, TX.' }` (no nested url_citation object) is indexed → result has source_url + snippet populated and `isUsableCandidate(out[0]) === true`.
    - Test (price-research-openrouter-web.test.ts, NEW case): model returns 1 parseable result but ZERO annotations → a `console.warn` spy (vi.spyOn(console, 'warn')) receives a message containing '[price-research]' and '0 citations indexed', AND the result still comes back evidence-gated (source_url null, snippet null, isUsableCandidate false). The evidence gate itself is UNTOUCHED.
    - Test (price-research-anthropic-web.test.ts, NEW case): mirror of the zero-citations warn — parseable results, no citations in any block → console.warn fires with '[price-research]' + '0 citations indexed', results stay evidence-gated. Follow that file's existing mock conventions for the Anthropic SDK response.
    - Test (price-research-cache.test.ts, NEW case): upsert resolves `{ data: null, error: { message: 'boom' } }` → `put()` RESOLVES (never throws) and console.warn was called with a message containing 'cache write failed'. All 8 existing cases green (their upsert mock already returns `{ data: null, error: null }`).
    - All 12 existing openrouter-web cases stay green — including 'nulls out source_url/snippet when the model cites a url NOT in the annotations' (that case now also triggers the warn, which is harmless to its assertions).
  </behavior>
  <action>
    D4a — openrouter-web.ts: extend the UrlCitation type (lines 48-51) with optional FLAT fields: `url?: string` and `content?: string` alongside the existing nested `url_citation` object. Rewrite the citation-indexing loop (currently lines 171-177) to be shape-tolerant with type discrimination:
    - skip an annotation ONLY when `a?.type` is present AND `!== 'url_citation'` (an absent type stays tolerated, matching today's behavior);
    - resolve the url as `a.url_citation?.url` falling back to the flat `a.url`;
    - resolve the snippet as `a.url_citation?.content` falling back to the flat `a.content`, defaulting to `''`;
    - keep the existing `typeof url === 'string' && url.trim().length > 0` guard and `citationByUrl.set(url, snippet)`.

    D4b — openrouter-web.ts: immediately after the successful `priceResearchPayloadSchema.safeParse` (after line 199's `if (!parsed.success) ...`), add:
    `if (parsed.data.results.length > 0 && citationByUrl.size === 0) console.warn(`[price-research] openrouter-web: ${parsed.data.results.length} results but 0 citations indexed — annotations shape mismatch or missing; all results will fail the evidence gate`, { resultCount: parsed.data.results.length, annotationCount: (message?.annotations ?? []).length })`
    This is the telemetry for the measured 97% evidence-gate rejection (100-estimate batch): the model answered, the citations vanished, every source_url was nulled, isUsableCandidate rejected 100% — silently.

    D4c — anthropic-web.ts: mirror the same warn after ITS successful safeParse (after line 139's `if (!parsed.success) ...`):
    `if (parsed.data.results.length > 0 && citationByUrl.size === 0) console.warn(`[price-research] anthropic-web: ${parsed.data.results.length} results but 0 citations indexed — citation blocks missing; all results will fail the evidence gate`, { resultCount: parsed.data.results.length })`

    HARD CONSTRAINT for D4: do NOT touch isUsableCandidate, the cited/null-out re-association logic in either adapter, missFor, or the schema. The evidence gate stays byte-identical — this task only widens what counts as a real citation ANNOTATION and makes the zero-citations state loud.

    D9 — cache.ts put(): change the unchecked upsert to
    `const { error } = await svc.from('price_research_cache').upsert({ ...unchanged payload... }, { onConflict: 'company_id,normalized_name,region,currency_code' })`
    then `if (error) console.warn('[price-research] cache write failed', error.message)`. Never throw — put() keeps resolving void.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/price-research-openrouter-web.test.ts tests/unit/estimate/price-research-anthropic-web.test.ts tests/unit/estimate/price-research-cache.test.ts</automated>
  </verify>
  <done>Flat-shape url_citation annotations are indexed (isUsableCandidate true with a matching citation); both adapters warn loudly on results-without-citations while still returning evidence-gated misses; cache-write failures surface via console.warn without throwing; all pre-existing cases in the three test files pass unchanged.</done>
</task>

</tasks>

<verification>
Full gate (run after all three tasks):
```
npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit tests/eval
```
Known pre-existing parallel-only flakes (billing / company / team-invite / mcp-route files): if one fails in the full run, re-run that file in isolation — a pass in isolation means NOT a regression from this plan.

Contract-change audit (the ONLY intentional behavior changes — everything else must be byte-compatible):
1. transcribeAudioOR now THROWS on an empty final transcript (previously returned '') — no existing test asserted empty acceptance (verified: transcribe-fallback.test.ts has exactly 3 cases).
2. analyzePhotoOR now THROWS on an empty final analysis — no existing test covers this path directly (all 12 test files referencing analyzePhotoOR mock it at the module boundary).
3. generateEstimateForProject now rejects when the ONLY inputs are photos without ai_description — the existing photos-only test's mock already carries ai_description, so it stays green with zero edits.
4. ProvidersUnavailableError gains an optional fallbackCause — purely additive; .cause and providerUnavailable contracts pinned by existing tests remain intact.
</verification>

<success_criteria>
- All 9 audit defects (D1-D9) closed exactly as specified in the verified evidence — no scope creep, no evidence-gate weakening, no DB migrations, no secrets in code.
- `npx tsc --noEmit -p tsconfig.ci.json` clean.
- `npx vitest run tests/unit tests/eval` green (modulo the documented parallel-only flakes, confirmed green in isolation).
- New tests exist and pass for: empty-transcript throw, empty-vision throw, fallbackCause exposure, unanalyzed-photos rejection, BYOK Sentry capture, flat-shape citation indexing, zero-citations warn (both adapters), cache-write-failure warn.
- isUsableCandidate and both adapters' cited/null-out re-association logic byte-identical.
- Deferred (explicitly OUT of scope): BYOK billing policy (debit when platform key serves a byok_enabled company); any empty-primary-triggers-fallback redesign of callWithFallback.
</success_criteria>

<output>
After completion, create `.planning/quick/260705-2gp-fail-proof-the-generation-flow-validate-/260705-2gp-SUMMARY.md`
</output>
