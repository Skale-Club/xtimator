---
phase: quick-260718-m2q
plan: 01
type: execute
wave: 1
depends_on: [quick-260718-k3f]
autonomous: true
requirements: []
files_modified:
  - components/workspace/estimate/estimate-floating-actions.tsx
  - components/workspace/estimate/estimate-editor.tsx
  - tests/unit/components/estimate-floating-actions.test.tsx

must_haves:
  truths:
    - "The floating pill has a view-mode toggle offering the OTHER mode: shows 'Full page' while in full-width, 'Full width' while in full-page"
    - "'Full width' (default) renders the estimate document exactly as today (fills the content column)"
    - "'Full page' constrains the document to a US-Letter-like width (max-w-[816px]) centered in the column, so it reads like the PDF page"
    - "The invoice panel and Generate-invoice row follow the same width constraint so nothing pokes out past the page edge"
    - "The choice persists across reloads (localStorage), initialized in an effect so SSR/hydration markup stays deterministic"
    - "The toggle renders only when the new props are provided (backward-compat, same pattern as onOpenSettings)"
    - "The gear remains the LEFTMOST pill child (existing DOCUX-01 order test must keep passing)"
  artifacts:
    - path: "components/workspace/estimate/estimate-floating-actions.tsx"
      provides: "viewMode/onViewModeChange props + toggle button between gear and linkClientSlot"
      contains: "onViewModeChange"
    - path: "components/workspace/estimate/estimate-editor.tsx"
      provides: "viewMode state + localStorage persistence + conditional max-w wrapper around document/invoices"
      contains: "max-w-[816px]"
    - path: "tests/unit/components/estimate-floating-actions.test.tsx"
      provides: "Toggle behavior tests (renders/hidden/click/target-label)"
      contains: "view mode toggle"
  key_links:
    - from: "estimate-editor.tsx viewMode state"
      to: "EstimateFloatingActions toggle"
      via: "viewMode + onViewModeChange props (same optional-prop pattern as onOpenSettings)"
      pattern: "viewMode="
    - from: "estimate-editor.tsx viewMode state"
      to: "document width"
      via: "conditional wrapper className mx-auto w-full max-w-[816px] around EstimateDocument + IssuedInvoicesPanel + GenerateInvoiceDialog row"
      pattern: "max-w-\\[816px\\]"
---

<objective>
Add a viewing-mode option to the estimate page's floating action pill: "Full width" (current behavior, default) and "Full page" (document constrained to a letter-page width, centered — print-preview-like). User request; names self-explanatory.
</objective>

<context>
- The pill (estimate-floating-actions.tsx) renders [gear] [linkClientSlot] [refineSlot] [Photos] [Send]; gear-leftmost order is asserted by tests/unit/components/estimate-floating-actions.test.tsx (DOCUX-01).
- estimate-editor.tsx is the only consumer; it renders EstimateDocument + IssuedInvoicesPanel + GenerateInvoiceDialog row + the pill inside a space-y-3 div.
- Read-only versions never render the pill (isCurrent gate) — the persisted preference still applies to the document wrapper, which is consistent.
- Pill labels are untranslated English ("Photos", "Send") — the new labels follow suit.
- 816px = 8.5in US Letter at 96dpi.
- localStorage init must run in useEffect (not useState initializer) to avoid SSR hydration mismatch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle button on pill + width wrapper in editor + tests</name>
  <files>components/workspace/estimate/estimate-floating-actions.tsx, components/workspace/estimate/estimate-editor.tsx, tests/unit/components/estimate-floating-actions.test.tsx</files>
  <action>
1. estimate-floating-actions.tsx:
   - Export type `EstimateViewMode = 'width' | 'page'`.
   - New optional props: `viewMode?: EstimateViewMode`, `onViewModeChange?: (mode: EstimateViewMode) => void`. Render the toggle only when BOTH are provided, positioned directly AFTER the gear (gear stays leftmost).
   - Button: ghost, rounded-full, gap-1.5, h-3.5 w-3.5 lucide icon + label of the TARGET mode — in 'width' mode show `File` icon + "Full page"; in 'page' mode show `StretchHorizontal` icon + "Full width". onClick calls onViewModeChange with the other mode.
2. estimate-editor.tsx:
   - `const [viewMode, setViewMode] = useState<EstimateViewMode>('width')`; mount effect reads localStorage key `estimate-view-mode` and applies 'page' if stored; `handleViewModeChange` sets state + writes localStorage (try/catch).
   - Wrap EstimateDocument + IssuedInvoicesPanel + GenerateInvoiceDialog row in `<div className={viewMode === 'page' ? 'mx-auto w-full max-w-[816px]' : undefined}>`.
   - Pass viewMode + onViewModeChange to EstimateFloatingActions.
3. tests: add a `describe('EstimateFloatingActions view mode toggle')` block — renders with both props; hidden when omitted; click in 'width' mode calls onViewModeChange('page'); labels show target mode ("Full page" in width mode, "Full width" in page mode); gear still precedes the toggle (order intact).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/components/estimate-floating-actions.test.tsx</automated>
  </verify>
  <done>Toggle works per spec; default unchanged; persistence wired; all pill tests (old + new) green; scoped typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code+tests, then planning artifacts) — DO NOT PUSH</name>
  <files>components/workspace/estimate/estimate-floating-actions.tsx, components/workspace/estimate/estimate-editor.tsx, tests/unit/components/estimate-floating-actions.test.tsx</files>
  <action>
- Stage exactly the three files; commit on `dev`: `feat(quick-260718-m2q): add Full width / Full page view mode toggle to estimate pill`
- Create SUMMARY.md, add STATE.md Quick Tasks row, commit: `docs(quick-260718-m2q): add quick-task plan/summary and STATE row`
- Do NOT push.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- `npx vitest run tests/unit/components/estimate-floating-actions.test.tsx` → all pass (existing 6 + new)
- Default render byte-equivalent to today (viewMode 'width' → wrapper className undefined)
</verification>

<success_criteria>
Pill exposes a self-explanatory view-mode toggle; "Full page" renders the estimate as a centered letter-width page; "Full width" stays the default and identical to current; preference persists; existing tests keep passing; commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-m2q-estimate-view-mode-full-page-toggle/260718-m2q-SUMMARY.md`
</output>
