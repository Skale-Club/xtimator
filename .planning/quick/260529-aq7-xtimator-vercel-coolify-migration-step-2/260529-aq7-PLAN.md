---
phase: quick-260529-aq7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - package-lock.json
  - bun.lock
  - .github/workflows/cron-jobs.yml
  - vercel.json
  - docker-compose.yml
  - Caddyfile
  - next.config.ts
autonomous: true
requirements: [MIGRATE-S2-DOCKER, MIGRATE-S2-LOCKFILE, MIGRATE-S2-CRONS, MIGRATE-S2-CLEANUP, MIGRATE-S2-CSP]

must_haves:
  truths:
    - "The container builds on node:24-alpine across all three stages (deps, builder, runner)"
    - "package-lock.json is in sync with package.json so `npm ci` resolves cleanly in the container"
    - "The 4 cron jobs run on GitHub Actions schedules, hitting production endpoints with Bearer CRON_SECRET"
    - "vercel.json no longer declares any crons"
    - "Phase 68 Caddy/compose artifacts are gone (Coolify uses Traefik, not Caddy)"
    - "next.config.ts CSP no longer references va.vercel-scripts.com"
  artifacts:
    - path: "Dockerfile"
      provides: "Container build on node:24-alpine, all stages, everything else intact"
      contains: "FROM node:24-alpine"
    - path: ".github/workflows/cron-jobs.yml"
      provides: "4 scheduled jobs curling prod cron endpoints with Bearer CRON_SECRET"
      contains: "Authorization: Bearer"
    - path: "vercel.json"
      provides: "Vercel config with crons array removed (rest preserved for §7)"
    - path: "next.config.ts"
      provides: "CSP script-src without va.vercel-scripts.com"
  key_links:
    - from: ".github/workflows/cron-jobs.yml"
      to: "https://xtimator.com/api/cron/*"
      via: "curl with Authorization: Bearer ${{ secrets.CRON_SECRET }} against ${{ secrets.SITE_URL }}"
      pattern: "secrets.CRON_SECRET"
    - from: "Dockerfile"
      to: "node:24-alpine base layer (shared across Coolify host apps)"
      via: "FROM directive on every stage"
      pattern: "FROM node:24-alpine"
---

<objective>
Make the Xtimator repo container-ready for the SHARED Coolify host (188.245.112.3, Traefik + Let's Encrypt). This is §2 of the Vercel→Coolify migration: pure repo edits, no Coolify-side config, no DNS, no cutover.

Five focused edits:
1. Bump the Dockerfile base image to node:24-alpine on all stages (host standardizes on it for layer reuse).
2. Verify/regenerate package-lock.json so the npm-based container build resolves cleanly, and remove the now-stale bun.lock.
3. Replace the 4 Vercel crons with GitHub Actions scheduled workflows that curl prod endpoints with Bearer CRON_SECRET, and strip the crons array from vercel.json.
4. Delete obsolete Phase 68 artifacts (docker-compose.yml, Caddyfile) that Traefik replaces and that could confuse Coolify build-pack auto-detection.
5. Drop the dead va.vercel-scripts.com entry from the CSP report-only policy.

Purpose: Unblock the Coolify build path. After this, the repo can be built by Coolify's Dockerfile build pack and crons keep running independent of Vercel.
Output: Updated Dockerfile, synced package-lock.json, deleted bun.lock, new .github/workflows/cron-jobs.yml, slimmed vercel.json, deleted docker-compose.yml + Caddyfile, cleaned next.config.ts.

Locked user decisions honored:
- Deploy target = SHARED Coolify host (Traefik + Let's Encrypt), NOT the Phase 68 dedicated-VM + Caddy/compose stack → Task 3 deletes Caddy/compose.
- The 4 Vercel crons migrate to GitHub Actions scheduled workflows → Task 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

Key project rules in play:
- SECRET HANDLING (CLAUDE.md, CRITICAL): NEVER write a real secret to any file. CRON_SECRET and SITE_URL come from GitHub repo secrets, referenced only as ${{ secrets.CRON_SECRET }} / ${{ secrets.SITE_URL }}. gitleaks pre-commit hook will block the commit if a real secret slips in.
- NO Docker on this dev machine (Phase 68-03). Do NOT plan or run a local `docker build` / `docker compose` as a verify step. Verify via file-content greps and `npm` lockfile checks only.

<interfaces>
<!-- Cron auth contract the GitHub Actions workflow must satisfy. Confirmed against codebase. -->

From lib/auth/cron-auth.ts — the auth scheme all 4 cron routes use:
```typescript
// Returns true only when the Authorization header EXACTLY equals `Bearer ${CRON_SECRET}`.
// Constant-time compare (timingSafeEqual). Returns false if CRON_SECRET unset or header absent.
export function isAuthorizedCron(request: Request): boolean
```

All 4 routes are GET handlers with this exact guard (confirmed via grep):
```typescript
export async function GET(request: Request) {
  if (!process.env.CRON_SECRET) return 503  // "CRON_SECRET not configured"
  if (!isAuthorizedCron(request)) return 401 // "Unauthorized"
  ...
}
```
→ The workflow must send `Authorization: Bearer <CRON_SECRET>` and use HTTP GET (curl default). No POST, no extra headers required.

The 4 jobs + UTC schedules (from current vercel.json crons array):
| Endpoint                              | UTC cron      |
|---------------------------------------|---------------|
| /api/cron/expire-trials               | 0 1 * * *     |
| /api/cron/cleanup-orphan-projects     | 0 3 * * *     |
| /api/cron/cleanup-whatsapp-sessions   | 0 4 * * *     |
| /api/cron/trial-warning-emails        | 0 9 * * *     |

Base URL: must be domain-stable. Use ${{ secrets.SITE_URL }} (expected value https://xtimator.com) — NEVER a hardcoded preview URL.
</interfaces>

<house_style>
<!-- Mirror the existing GitHub Actions style. From .github/workflows/supabase-keepalive.yml: -->
- `on: schedule: - cron: "..."` + `workflow_dispatch:` for manual runs.
- `permissions: contents: read` (least privilege).
- `runs-on: ubuntu-latest`, `timeout-minutes: 5`.
- curl with `--silent --show-error`, `--write-out "%{http_code}"`, `--max-time`, and a small retry loop; treat 200 as success and `::error::` + non-zero exit on failure.
- Reference repo secrets/vars via `${{ secrets.* }}` / `${{ vars.* }}` with sensible fallbacks where appropriate.
</house_style>

<bun_lock_note>
<!-- Verified during planning. Safe to delete bun.lock. -->
Only Bun references in the repo are: vercel.json (build/dev/install commands — left intact for §7 teardown) and supabase/audits/run-prod-readiness.sh (uses `bunx supabase`, which downloads on-demand and does NOT read bun.lock). package.json has no `packageManager` field. Coolify builds via Dockerfile → `npm ci` against package-lock.json. Nothing consumes the bun.lock lockfile itself, so deleting it removes ambiguity without breaking any consumer.
</bun_lock_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Dockerfile to node:24-alpine + sync npm lockfile + delete bun.lock</name>
  <files>Dockerfile, package-lock.json, bun.lock</files>
  <action>
    Two coupled changes that both feed the same Coolify `npm ci` build path; commit together.

    A) Dockerfile base bump (MIGRATE-S2-DOCKER):
    - Replace all three `FROM node:22-alpine` lines with `FROM node:24-alpine`:
      - `FROM node:24-alpine AS deps`
      - `FROM node:24-alpine AS builder`
      - `FROM node:24-alpine AS runner`
    - Change NOTHING else. Keep verbatim: `apk add --no-cache libc6-compat`, the `npm ci --ignore-scripts`, the non-root nextjs user (addgroup/adduser uid/gid 1001), `USER nextjs`, `EXPOSE 3000`, the `HEALTHCHECK ... /api/health ...`, and `CMD ["node", "server.js"]`. Leave all comments intact.
    - Rationale (per locked decision): the shared Coolify host standardizes on node:24-alpine so Docker reuses one base layer across every app (Xphere already uses it).

    B) Lockfile sync (MIGRATE-S2-LOCKFILE):
    - Verify package-lock.json is in sync with package.json. Run `npm install --package-lock-only` and check whether package-lock.json changed (`git diff --stat package-lock.json`).
      - If no diff: lockfile is already in sync — good.
      - If diff: the regenerated package-lock.json IS the fix; keep it staged. (Coolify builds with `npm ci`, which fails hard on lockfile/manifest drift, so an in-sync lock is mandatory.)
    - This machine has NO Docker — do NOT attempt a container build. The `npm install --package-lock-only` resolution (and the absence of drift) is the verification.

    C) Delete the stale bun.lock (MIGRATE-S2-CLEANUP, lockfile half):
    - `git rm bun.lock` (or delete the file and stage the removal).
    - Per <bun_lock_note>: nothing reads bun.lock; Coolify uses the Dockerfile/npm path. Removing it eliminates lockfile ambiguity. vercel.json's Bun build/dev/install commands stay for now (§7 teardown).

    Commit atomically, e.g.: `chore(deploy): bump Dockerfile to node:24-alpine, sync npm lockfile, drop bun.lock`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$d = Get-Content Dockerfile -Raw; if (($d | Select-String -Pattern 'FROM node:24-alpine' -AllMatches).Matches.Count -ne 3) { throw 'expected 3 node:24-alpine FROM lines' }; if ($d -match 'node:22-alpine') { throw 'node:22-alpine still present' }; if (Test-Path bun.lock) { throw 'bun.lock still exists' }; if ($d -notmatch 'USER nextjs' -or $d -notmatch 'libc6-compat' -or $d -notmatch '/api/health' -or $d -notmatch 'node`", `"server.js') { throw 'a required Dockerfile invariant is missing' }; npm install --package-lock-only --ignore-scripts; if ((git status --porcelain package-lock.json)) { Write-Host 'package-lock.json regenerated (now in sync) — keep it staged' } else { Write-Host 'package-lock.json already in sync' }; Write-Host 'OK'"</automated>
  </verify>
  <done>Dockerfile has exactly 3 `FROM node:24-alpine` lines and zero `node:22-alpine`; libc6-compat, non-root nextjs user, HEALTHCHECK /api/health and `CMD ["node","server.js"]` all intact; bun.lock no longer exists; package-lock.json resolves cleanly with `npm install --package-lock-only` (no remaining drift).</done>
</task>

<task type="auto">
  <name>Task 2: Add GitHub Actions cron workflow + strip crons from vercel.json</name>
  <files>.github/workflows/cron-jobs.yml, vercel.json</files>
  <action>
    Migrate the 4 Vercel crons to a GitHub Actions scheduled workflow (MIGRATE-S2-CRONS), then remove the crons array from vercel.json.

    A) Create .github/workflows/cron-jobs.yml mirroring the supabase-keepalive.yml house style:
    - `name: Scheduled Cron Jobs`
    - `on:` with `workflow_dispatch:` (manual trigger) AND `schedule:` listing all 4 UTC crons as separate `- cron:` entries:
      - `"0 1 * * *"`   # expire-trials
      - `"0 3 * * *"`   # cleanup-orphan-projects
      - `"0 4 * * *"`   # cleanup-whatsapp-sessions
      - `"0 9 * * *"`   # trial-warning-emails
    - `permissions: contents: read`
    - One job (`runs-on: ubuntu-latest`, `timeout-minutes: 5`) that, for each cron, derives which endpoint(s) to hit from `${{ github.event.schedule }}` and curls the matching production endpoint. RECOMMENDED simplest robust approach: a single step with a small bash `case "$SCHEDULE" in ... esac` that maps each schedule string to its endpoint path, plus a `workflow_dispatch` fallback that runs ALL four endpoints (so manual runs exercise everything).
    - Each curl:
      - GET (default), against `${{ secrets.SITE_URL }}` joined with the endpoint path. Strip any trailing slash from SITE_URL (mirror keepalive's `supabase_url="${SUPABASE_URL%/}"`).
      - Header: `Authorization: Bearer ${{ secrets.CRON_SECRET }}` (passed via an env var in the step, e.g. `CRON_SECRET: ${{ secrets.CRON_SECRET }}` and `SITE_URL: ${{ secrets.SITE_URL }}` — do NOT inline the secret into the run script literal).
      - `--silent --show-error --write-out "%{http_code}" --max-time 30`, small retry loop (e.g. 3 attempts with backoff, copying the keepalive pattern), treat HTTP 200 as success, `::error::` + non-zero exit otherwise.
    - Guard: if `SITE_URL` or `CRON_SECRET` is empty, `::error::` and exit 1 (same shape as keepalive's missing-secret guards).
    - SECRET HANDLING: NEVER put a real CRON_SECRET or URL literal in the file. Only `${{ secrets.CRON_SECRET }}` / `${{ secrets.SITE_URL }}`. Add a short comment noting the two repo secrets the workflow expects (CRON_SECRET, SITE_URL=https://xtimator.com) using placeholder language only.

    B) Strip crons from vercel.json (MIGRATE-S2-CRONS, removal half):
    - Remove the entire `"crons": [ ... ]` array and its 4 entries. Leave `framework`, `buildCommand`, `devCommand`, `installCommand` untouched (full Vercel teardown is §7). Ensure resulting JSON is valid (no trailing comma).

    Commit atomically, e.g.: `feat(deploy): migrate Vercel crons to GitHub Actions scheduled workflow`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$w = Get-Content .github/workflows/cron-jobs.yml -Raw; foreach ($p in @('expire-trials','cleanup-orphan-projects','cleanup-whatsapp-sessions','trial-warning-emails','secrets.CRON_SECRET','secrets.SITE_URL','Authorization: Bearer','workflow_dispatch')) { if ($w -notmatch [regex]::Escape($p)) { throw \"workflow missing: $p\" } }; foreach ($c in @('0 1 \* \* \*','0 3 \* \* \*','0 4 \* \* \*','0 9 \* \* \*')) { if ($w -notmatch $c) { throw \"workflow missing schedule: $c\" } }; $j = Get-Content vercel.json -Raw | ConvertFrom-Json; if ($j.PSObject.Properties.Name -contains 'crons') { throw 'vercel.json still has crons' }; if (-not $j.framework) { throw 'vercel.json framework key lost' }; Write-Host 'OK'"</automated>
  </verify>
  <done>.github/workflows/cron-jobs.yml exists with workflow_dispatch + all 4 UTC schedules, references all 4 endpoint paths, sends Authorization: Bearer ${{ secrets.CRON_SECRET }} against ${{ secrets.SITE_URL }}, and contains NO real secret. vercel.json is valid JSON with the crons array removed and framework/build/dev/install keys preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Delete obsolete Phase 68 Caddy/compose artifacts + clean CSP</name>
  <files>docker-compose.yml, Caddyfile, next.config.ts</files>
  <action>
    Two unrelated-but-tiny cleanups; commit together as the migration-cleanup task.

    A) Delete obsolete Phase 68 artifacts (MIGRATE-S2-CLEANUP, artifacts half):
    - `git rm docker-compose.yml`
    - `git rm Caddyfile`
    - Rationale (per locked decision): the SHARED Coolify host uses Traefik + Let's Encrypt, replacing Caddy entirely. A stray docker-compose.yml can also derail Coolify's build-pack auto-detection.
    - FLAG FOR LATER (§4, not this plan): the Coolify app must be created with build pack explicitly set to "Dockerfile" so Coolify uses our multi-stage build instead of auto-detecting. Note this in the SUMMARY; do NOT act on it here.

    B) Cosmetic CSP cleanup (MIGRATE-S2-CSP):
    - In next.config.ts, in the `cspReportOnly` array's `script-src` line, remove ` https://va.vercel-scripts.com` (no Vercel Analytics component ships). The line becomes:
      `"script-src 'self' 'unsafe-inline' https://js.stripe.com https://challenges.cloudflare.com",`
    - Touch nothing else in next.config.ts (output: 'standalone', images.remotePatterns, securityHeaders, headers() all stay).

    Commit atomically, e.g.: `chore(deploy): remove Phase 68 Caddy/compose artifacts and dead Vercel CSP entry`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path docker-compose.yml) { throw 'docker-compose.yml still exists' }; if (Test-Path Caddyfile) { throw 'Caddyfile still exists' }; $n = Get-Content next.config.ts -Raw; if ($n -match 'va.vercel-scripts.com') { throw 'va.vercel-scripts.com still in CSP' }; if ($n -notmatch \"output: 'standalone'\" -or $n -notmatch 'remotePatterns' -or $n -notmatch 'js.stripe.com') { throw 'next.config.ts invariant lost' }; Write-Host 'OK'"</automated>
  </verify>
  <done>docker-compose.yml and Caddyfile no longer exist; next.config.ts CSP script-src no longer references va.vercel-scripts.com while retaining js.stripe.com + challenges.cloudflare.com and all other config (output standalone, remotePatterns, securityHeaders). SUMMARY notes the §4 Coolify "build pack = Dockerfile" follow-up.</done>
</task>

</tasks>

<verification>
File-content + lockfile checks only (NO Docker on this machine):
- Dockerfile: 3× `FROM node:24-alpine`, 0× `node:22-alpine`; libc6-compat, USER nextjs, HEALTHCHECK /api/health, CMD node server.js all present.
- bun.lock, docker-compose.yml, Caddyfile: all absent.
- package-lock.json: `npm install --package-lock-only --ignore-scripts` produces no further drift.
- .github/workflows/cron-jobs.yml: 4 schedules, 4 endpoints, Bearer ${{ secrets.CRON_SECRET }}, ${{ secrets.SITE_URL }}, workflow_dispatch; zero real secrets (gitleaks pre-commit must pass).
- vercel.json: valid JSON, no crons key, framework/build/dev/install preserved.
- next.config.ts: no va.vercel-scripts.com; output standalone + remotePatterns + js.stripe.com retained.
</verification>

<success_criteria>
- Container build targets node:24-alpine on all stages with every Phase 68 hardening invariant intact.
- npm lockfile is authoritative and in sync; bun.lock removed.
- The 4 cron jobs run on GitHub Actions schedules against https://xtimator.com (via secrets.SITE_URL) with Bearer CRON_SECRET, and no longer appear in vercel.json.
- Caddy/compose artifacts deleted; CSP no longer references the dead Vercel Analytics host.
- No real secrets committed (gitleaks clean). Three atomic commits, one per task.
- Out of scope (NOT done here): Coolify app/build-pack config, DNS, env capture, cutover, full Vercel teardown — those are §3–§7.
</success_criteria>

<output>
After completion, create `.planning/quick/260529-aq7-xtimator-vercel-coolify-migration-step-2/260529-aq7-SUMMARY.md`.
Be sure the SUMMARY flags the §4 follow-up: the Coolify app must be created with build pack explicitly = Dockerfile.
</output>
