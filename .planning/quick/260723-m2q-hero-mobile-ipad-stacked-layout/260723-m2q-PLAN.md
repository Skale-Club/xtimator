---
phase: quick-260723-m2q
plan: 01
type: execute
wave: 1
depends_on: [quick-260723-h3v]
autonomous: true
requirements: []
files_modified:
  - components/landing/hero-section.tsx
  - app/globals.css

must_haves:
  truths:
    - "Below lg (1024px) — phone AND all iPad sizes/orientations, both real touch tablets and resized desktop windows below 1024 — the hero image sits BELOW the text in normal flow (no longer an absolute overlay), the section grows to fit both instead of a fixed max-height, and the text block (pill, headline, subheadline, CTA row) is horizontally centered"
    - "At lg+ (1024px, non-touch) the desktop layout is pixel-identical to before this change: side-by-side, left-aligned, absolute-positioned image, same max-height"
    - "Real touch tablets ≥1024px wide (iPad Pro 12.9 portrait/landscape) ALSO get the stacked layout via a new pointer:coarse CSS override, since a plain lg: width query can't distinguish them from a resized desktop browser window at the same width"
    - "Mobile phone AND tablet-stacked (below lg) text and buttons are ~20% larger than the immediately-prior fix, with lg explicitly pinned back to the exact pre-existing desktop values"
    - "Mobile phone (below sm, true phone width) CTA buttons and the H1 are full width"
  key_links:
    - from: "hero-content flex-direction"
      to: "lg:flex-row (was sm:flex-row)"
      via: "moved the row/column breakpoint from 640px to 1024px so phone AND tablet widths stack"
      pattern: "flex flex-col ... lg:flex-row"
    - from: "real tablet ≥1024px wide"
      to: "forced stacked layout"
      via: "@media (min-width:1024px) and (pointer:coarse) overrides in globals.css, placed after the existing coarse blocks so they win the cascade for the properties they touch"
      pattern: "flex-direction: column !important"
---

<objective>
User asked (with a screenshot of the desktop hero) to make the mobile-phone and iPad-breakpoint hero layout put the image BELOW the text instead of overlapping/beside it, center the text, let the section grow taller to fit, and give the image more room to be bigger — desktop (the "role model") stays untouched. Confirmed via AskUserQuestion: image goes below the text block; applies to iPad in BOTH orientations (creating a real conflict with a pure width-based row/column split, since iPad Pro 12.9" portrait is 1024px wide — exactly the desktop lg threshold — resolved by combining the width-based split with the existing pointer:coarse real-tablet detection already used elsewhere in this file). Mid-implementation, the user iterated rapidly with a live screenshot: mobile title needed to be ~20% bigger while occupying fewer rows, both the title and CTA buttons needed to be full-width on mobile, and finally mobile+tablet text/buttons needed a full ~20% size bump (not just the title) with desktop staying exactly as before.
</objective>

<context>
- Combines with quick-260723-h3v (the immediately-prior proportion/dip fix) — this task's clamp() formulas start from THAT task's values and scale by 1.2x for the mobile+tablet range, then explicitly re-pin lg: back to h3v's exact desktop formulas (necessary because h3v's design relied on the sub-lg range topping out AT the desktop value; scaling sub-lg past the desktop value reintroduces a step at the 1024 boundary — an intentional, expected consequence of "tablet bigger, desktop unchanged," not a re-regression of h3v's fix, which was about mismatched SAME-tier segment boundaries).
- Real touch tablets are already distinguished from resized desktop windows elsewhere in this file via `pointer: coarse` media queries (3 existing blocks tuning font/button sizes for iPad ranges) — extended that same mechanism rather than inventing a new one.
- Verified WebP/AI-pipeline-style compatibility research wasn't needed here (pure CSS), but did verify via live SSR HTML fetch (from a differently-ported dev server reachable in this environment, after discovering another session's dev server was occupying the default port) that every class compiled exactly as written, after the user reported "not seeing the changes" — confirmed the code was correct and the gap was environmental (stale view or wrong URL), not a code bug.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the JSX — stacked below lg, side-by-side unchanged at lg+</name>
  <files>components/landing/hero-section.tsx</files>
  <action>
Moved the row/column breakpoint from `sm:flex-row` (640px) to `lg:flex-row` (1024px) on .hero-content. .hero-left goes from a 55%-width left-aligned column to full-width + centered (items-center, text-center) below lg, restoring the exact original 55%-width/left-aligned classes at lg. The pill wrapper, CTA row, and both button self-align overrides that would have fought the new centering (max-lg:self-start, an unconditional self-start) were removed/adjusted so they inherit the parent's centering below lg.
The image container: was `absolute` at every breakpoint with per-breakpoint top/left/right/scale offsets; now `relative w-full aspect-[4/3]` (in-flow block) below lg, with the EXACT original absolute-positioning classes moved onto `lg:` so desktop is unchanged. object-position changed from `object-bottom` (tuned for the old overlapping-absolute design) to `object-center` for the new standalone stacked block, with `lg:object-bottom` restoring the original desktop anchor.
Section height: removed `sm:max-h-[520px] md:max-h-[620px]` (these capped the OLD side-by-side layout too short for a stacked image-below-text layout); kept `lg:max-h-[520px]` exactly as before.
  </action>
  <done>Below lg: stacked, centered, in-flow image, no height cap. At lg+: byte-for-byte the same layout as before this task.</done>
</task>

<task type="auto">
  <name>Task 2: Force the same stacking for real touch tablets ≥1024px wide</name>
  <files>app/globals.css</files>
  <action>
Block 1 (640-1023 portrait coarse): already gets the stacked layout for free from Task 1's width-based change (this range is entirely below 1024) — just removed its now-conflicting max-height and flipped align-items to center.
Block 2 (1024-1279 portrait coarse) and Block 3 (768-1279 landscape coarse): these ranges reach ABOVE 1024px, so without an explicit override they'd inherit the new lg: side-by-side desktop layout — added `!important` overrides (flex-direction: column, hero-left full-width, hero-image position:static + aspect-ratio) so real iPads in this width tier ALSO stack, while a resized DESKTOP window at the same width correctly stays side-by-side (pointer: coarse gates the whole block off non-touch). Removed the max-height caps in both, same reasoning as block 1.
  </action>
  <done>All 3 real-tablet coarse blocks produce the stacked layout across their entire width range; a same-width desktop browser window is unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile+tablet sizing iterations (title 20% bigger + fewer rows + full width, then buttons full width, then a further 20% pass on everything)</name>
  <files>components/landing/hero-section.tsx</files>
  <action>
Responding to a live screenshot mid-implementation: scaled the mobile-only H1 clamp ~20%, removed its `max-w-2xl` cap (full width so bigger text still wraps efficiently), and removed one forced `<br>` that was only ever relevant at true mobile width (letting the headline wrap naturally instead of at a fixed, not-necessarily-optimal point). Then made the H1 and both CTA buttons full-width specifically on true mobile (`w-full sm:w-auto`/`sm:w-fit`) per explicit follow-up. Then, per a final explicit request ("text and buttons need to be 20 percent bigger on mobile and tablet"), scaled EVERY mobile+tablet clamp (subheadline, both buttons' height/padding/font/min-width/icon-size) by another ~1.2x and added explicit `lg:` pins restoring the exact pre-existing desktop values wherever the scaled sub-lg formula's new ceiling now exceeds what desktop should show.
  </action>
  <done>Every clamp's lg: value hand-verified to match h3v's original desktop formula exactly; sub-lg values verified to be ~1.2x the h3v starting point.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors after every edit in this sequence
- landing-page test: 4/5 (the same pre-existing flaky AuthDialog timing test seen 3 other times this session, confirmed unrelated)
- Live SSR HTML re-fetched after each major edit round, confirming every class compiled exactly as written with no render errors — including after the user reported "not seeing changes," which this confirmed was environmental, not a code gap
</verification>

<success_criteria>
Phone and all iPad sizes/orientations show a centered, stacked (image-below-text) hero with mobile+tablet text/buttons sized up per the user's explicit follow-ups; real touch tablets get this regardless of width; a resized desktop browser window and the actual desktop breakpoint remain pixel-identical to before this task.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-m2q-hero-mobile-ipad-stacked-layout/260723-m2q-SUMMARY.md`
</output>
