---
phase: quick-260707-mv1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/prompt-builder.ts
  - lib/ai/schema.ts
  - lib/ai/providers/openrouter.ts
  - lib/ai/providers/gemini.ts
  - lib/ai/providers/anthropic.ts
  - lib/services/generate-estimate.ts
  - lib/estimate/graph/nodes/auto-refine.ts
  - tests/unit/ai/prompt-soft-prior.test.ts
autonomous: true
requirements: [QUICK-mv1-01]
must_haves:
  truths:
    - "The AI estimates the trade of the REQUESTED work faithfully — company.industry is a soft prior, never an identity (a cleaning request on an electrical-profile company produces a cleaning estimate)"
    - "detected_trade is returned by the AI and persisted to projects.project_type on KEPT generations; a mismatch vs company.industry is recorded in estimate_activity for future auto-suggestion UX"
    - "A discarded (vague-reverted) generation leaves ZERO side effects: the project name placeholder is preserved (or restored) so a future successful generation can still rename it"
    - "A kept generation renames the placeholder exactly once (existing user-set-name guard preserved)"
  artifacts:
    - path: "lib/ai/prompt-builder.ts"
      provides: "Soft-prior industry phrasing + detected_trade instruction"
    - path: "lib/services/generate-estimate.ts"
      provides: "Side-effect-free discard semantics"
  key_links:
    - from: "buildSystemPrompt"
      to: "soft prior"
      via: "primarily works in … but always estimate the requested work"
      pattern: "primarily"
    - from: "kept generation"
      to: "projects.project_type"
      via: "detected_trade persist"
      pattern: "detected_trade"
---

<objective>
Adaptive-first (user principle 2026-07-07): the tool adapts to imperfect configuration — never
the reverse. Production evidence: company "Skale Club" has industry=electrical; sofa-cleaning
requests produced "Unspecified Electrical Inquiry" / "Untitled Electrical Estimate" and
electrical-biased output, because buildSystemPrompt (lib/ai/prompt-builder.ts:64) asserts
"You are a professional estimator for a electrical business". Additionally the project rename
(lib/services/generate-estimate.ts:269-285) runs BEFORE the quality gates: the 19:45 vague
attempt renamed the placeholder to garbage, the estimate was then discarded by auto-refine
(lib/estimate/graph/nodes/auto-refine.ts → revertVagueEstimate), and the placeholder guard now
permanently blocks a correct rename on that project. Double damage; zero side effects on discard
is the rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/ai/prompt-builder.ts        (line ~64 opening paragraph; refine-mode paragraph must stay equivalent)
@lib/ai/schema.ts                (zod estimate schema, suggested_project_name at ~52)
@lib/ai/providers/openrouter.ts  (JSON schema: properties + required at ~25-27)
@lib/ai/providers/gemini.ts      (TWO schema sites ~105 and ~222)
@lib/ai/providers/anthropic.ts   (TWO schema sites ~29 and ~142)
@lib/services/generate-estimate.ts (rename block 269-285 with PLACEHOLDER_PREFIX guard; status update ~600)
@lib/estimate/graph/nodes/auto-refine.ts (revertVagueEstimate call — the discard path)
@lib/estimate/quality/revert.ts  (what revert deletes today)
@lib/estimate/adapters/default.ts (vague terminal — sets awaiting_details)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Industry as soft prior + detected_trade schema plumbing</name>
  <files>lib/ai/prompt-builder.ts, lib/ai/schema.ts, lib/ai/providers/openrouter.ts, lib/ai/providers/gemini.ts, lib/ai/providers/anthropic.ts</files>
  <action>
    1a. buildSystemPrompt generate-mode opening paragraph: replace
        "You are a professional estimator for a ${input.industry ?? 'general services'} business."
        with soft-prior phrasing (also fixes the "a electrical" grammar):
        "You are a professional estimator. The company primarily works in ${input.industry ?? 'general services'}, but ALWAYS estimate the work that is actually requested — if the request belongs to a different trade, price it faithfully for that trade at realistic US market rates."
        Keep every other sentence of the paragraph (itemization, sections, units) unchanged.
    1b. Add to the same generate-mode paragraph (next to the suggested_project_name instruction):
        "Also return detected_trade: the trade/category of the requested work itself (e.g. cleaning, electrical, plumbing, landscaping), inferred from the request content — independent of the company's primary trade."
    1c. Schema plumbing: lib/ai/schema.ts adds `detected_trade: z.string().optional()` (optional so
        legacy/cached outputs still parse). Provider JSON schemas (openrouter 1 site, gemini 2 sites'
        GENERATE schema — the refine-mode schema at ~222 only if trivially symmetric, anthropic 2
        sites likewise): add detected_trade to properties with a description; add to `required`
        ONLY where suggested_project_name is also required (mirror its treatment exactly).
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && grep -n "primarily works in" lib/ai/prompt-builder.ts && grep -rn "detected_trade" lib/ai/schema.ts lib/ai/providers/ | head -10 && npx tsc --noEmit && npx eslint lib/ai/prompt-builder.ts lib/ai/schema.ts lib/ai/providers/openrouter.ts lib/ai/providers/gemini.ts lib/ai/providers/anthropic.ts</automated></verify>
  <done>Soft-prior prompt; detected_trade flows through zod + all provider schemas; tsc/eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Zero side effects on discard + detected_trade persistence</name>
  <files>lib/services/generate-estimate.ts, lib/estimate/graph/nodes/auto-refine.ts</files>
  <action>
    GOAL (acceptance is what matters; trace the actual flow before choosing the mechanism):
    (A) A generation that ends DISCARDED (vague revert path) must leave projects.name untouched
        — the "Untitled project — …" placeholder survives so a future success can rename.
    (B) A KEPT generation renames once (existing PLACEHOLDER_PREFIX guard stays) and ALSO:
        - sets projects.project_type = detected_trade (lowercased, trimmed) when present;
        - when detected_trade is present AND company.industry is present AND they differ
          (case-insensitive), insert an estimate_activity row event_type 'trade_mismatch_detected'
          with metadata { detected: …, configured: … } — the raw material for the future
          auto-suggestion UX. No new UI in this task.
    RECOMMENDED MECHANISM (adapt if the flow disagrees): move the rename + the new project_type
    write + mismatch activity from the pre-persist position (line ~269) to the END of
    generateEstimateForProject's success path — immediately after the projects status/total
    update (~line 600), so they execute only when THIS pass fully persisted. Then close the
    refine-loop hole: in the discard path (auto-refine's revertVagueEstimate call — read
    lib/estimate/quality/revert.ts and lib/estimate/graph/nodes/auto-refine.ts), ALSO restore
    projects.name to its pre-attempt value IF the current name was written by this attempt.
    Simplest robust variant: since the rename now only happens at end-of-success, a reverted pass
    never renamed at all — verify by tracing that revert happens strictly AFTER assess and that
    a pass which will be reverted cannot have reached the end-of-success rename. If the final
    vague terminal (adapter default.ts awaiting_details path) can still follow a pass that
    renamed, handle that explicitly (restore or skip — document the choice).
    Keep the client-link block (lines ~230-267) where it is — only the rename block moves.
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && grep -n "detected_trade" lib/services/generate-estimate.ts && grep -n "trade_mismatch_detected" lib/services/generate-estimate.ts && npx tsc --noEmit && npx eslint lib/services/generate-estimate.ts lib/estimate/graph/nodes/auto-refine.ts</automated></verify>
  <done>Discarded generations leave name untouched; kept ones rename + persist project_type + log mismatch; reasoning documented in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Tests</name>
  <files>tests/unit/ai/prompt-soft-prior.test.ts</files>
  <action>
    New test file following existing prompt-builder test conventions (find existing tests that
    call buildSystemPrompt): (1) generate prompt contains "primarily works in electrical" AND the
    faithful-to-request instruction when industry=electrical; (2) contains "general services"
    fallback when industry null; (3) contains the detected_trade instruction; (4) refine-mode
    prompt unchanged in its opening (no soft-prior paragraph leak); (5) schema: parsing an AI
    payload WITH detected_trade succeeds and WITHOUT it still succeeds (optional).
    If existing generate-estimate service tests cover the rename, update them for the moved
    anchor; add a discard-path rename test only if the existing mock infrastructure makes it
    cheap — otherwise document manual verification steps in SUMMARY.
    Run targeted suites + npx tsc --noEmit.
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && npx vitest run tests/unit/ai/ && npx tsc --noEmit</automated></verify>
  <done>Prompt + schema coverage green; any rename-test updates pass.</done>
</task>

</tasks>

<verification>
tsc clean; eslint baselines; targeted suites green. Post-deploy manual check: sofa-cleaning
request on the electrical-profile company yields a cleaning-trade estimate with a coherent name.
</verification>

<success_criteria>
Configuration can no longer cause a wrong-trade estimate; discarded generations are invisible;
the data needed for future industry auto-suggestion is being captured.
</success_criteria>

<output>
After completion, create `.planning/quick/260707-mv1-adaptive-trade-inference-industry-become/260707-mv1-SUMMARY.md`
</output>
