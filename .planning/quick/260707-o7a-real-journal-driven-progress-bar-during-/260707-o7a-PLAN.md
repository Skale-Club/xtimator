---
phase: quick-260707-o7a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/actions/attempt-outcome.ts
  - lib/estimate/progress-model.ts
  - lib/estimate/poll-outcome.ts
  - components/capture/capture-processing-overlay.tsx
  - components/capture/capture-recorder.tsx
  - tests/unit/estimate/progress-model.test.ts
autonomous: true
requirements: [QUICK-o7a-01, QUICK-o7a-02]
must_haves:
  truths:
    - "The processing overlay shows a segmented progress bar whose segments are the REAL pipeline steps of the active mode, and a segment only completes when the step's succeeded event exists in the journal — never a blind animation"
    - "Within the active segment, fill = elapsed / historical-median duration for that step, hard-capped below 100% until the journal confirms — clearly estimation-from-real-data, never fake completion"
    - "The current step name + elapsed seconds are visible (English strings via t())"
    - "The live Web Speech caption is fully removed: no SpeechRecognition code, no live/interim transcript state or markup; recording shows waveform + timer + Listening indicator only"
  artifacts:
    - path: "lib/estimate/progress-model.ts"
      provides: "Pure progress computation (steps per mode, medians, fill math) — fully unit-tested"
    - path: "components/capture/capture-processing-overlay.tsx"
      provides: "Segmented real progress UI"
  key_links:
    - from: "CaptureProcessingOverlay"
      to: "journal step events"
      via: "pollEstimateOutcome onStageProgress → progress model"
      pattern: "progress-model"
    - from: "capture-recorder"
      to: "no Web Speech"
      via: "full removal"
      pattern: "SpeechRecognition"
---

<objective>
Two user directives (2026-07-07):
1. "precisa ter um progress bar real do processo, sem ser fake" — the P4 journal-first
   architecture makes this possible: pollEstimateOutcome already receives real step progress
   from pipeline_events. Design: segments = real steps; segment completion = real succeeded
   events; in-segment fill = elapsed vs historical median duration_ms (real measurements from
   pipeline_events), capped until confirmed. Honest by construction.
2. "a legenda ao vivo morre — internaliza e resolve por trás das cortinas" — remove the Web
   Speech API live caption entirely (it cannot do multilingual code-switching and showed English
   for Portuguese speech). Whisper (multilingual, auto-detect) remains the only transcription.
ALL app strings in English (t() literals).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260707-lyq-p4-core-bulletproofing-journal-first-out/260707-lyq-02-SUMMARY.md (Wave 2 — EXACT current signatures of pollEstimateOutcome/onStageProgress; READ FIRST)
@lib/actions/attempt-outcome.ts   (AttemptOutcome union; pending carries lastStep/lastStatus)
@lib/estimate/poll-outcome.ts     (journal-first tick; onStageProgress hook)
@components/capture/capture-processing-overlay.tsx (current three-dots overlay being upgraded)
@components/capture/capture-recorder.tsx (Web Speech code to REMOVE: SpeechRecognitionInstance/Ctor types, speechRecognitionRef, liveTranscript/interimTranscript state, recognition block in startRecording, stops in stopRecording/unmount cleanup, live-transcript markup in the isRecording overlay, liveTranscript/interimTranscript props through RecorderBody)
@lib/observability/pipeline-events.ts (step names)

<interfaces>
Step sequences per capture mode (only steps that emit journal events):
  audio:  save_recording → transcribe → generate_estimate
  photos: save_recording → analyze → generate_estimate
  text:   save_recording → generate_estimate
Baseline median durations (measured from production pipeline_events today; used as STATIC
fallbacks): save_recording ~2s, transcribe ~8s, analyze ~12s, generate_estimate ~35s.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Progress model + live medians</name>
  <files>lib/estimate/progress-model.ts, lib/actions/attempt-outcome.ts</files>
  <action>
    1a. New pure module lib/estimate/progress-model.ts:
        - `export type CaptureProgressMode = 'audio' | 'photos' | 'text'`
        - `export const STEP_SEQUENCES: Record<CaptureProgressMode, string[]>` per <interfaces>.
        - `export const FALLBACK_MEDIANS_MS: Record<string, number>` per <interfaces>.
        - `export function computeProgress(input: { mode: CaptureProgressMode; completedSteps: string[]; activeStep: string | null; activeStepElapsedMs: number; medians?: Record<string, number> }): { fraction: number; segments: { step: string; state: 'done' | 'active' | 'pending'; fill: number }[] }`
          Math: each step owns an equal share of the bar (or weight by median — choose equal
          shares for legibility, document why). done → fill 1; active → min(elapsed/median, 0.95);
          pending → 0. fraction = weighted sum. Pure, no Date.now() inside (elapsed passed in).
    1b. lib/actions/attempt-outcome.ts: extend the pending variant so the client can drive the
        model: pending gains `completedSteps: string[]` (steps with a succeeded event, in order)
        and `activeStepStartedAt: string | null` (created_at of the latest started event without
        a matching succeeded). Additive — do not change the other variants. Also export
        `getStepMedians(): Promise<Record<string, number>>`: service-client query over
        pipeline_events (status='succeeded', duration_ms not null, last 30 days, group by step,
        percentile_disc(0.5)) merged over FALLBACK_MEDIANS_MS; company-agnostic; auth-gated like
        getAttemptOutcome; on any failure return FALLBACK_MEDIANS_MS. One call per capture
        session (client caches in a ref), NOT per tick.
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && npx tsc --noEmit && npx eslint lib/estimate/progress-model.ts lib/actions/attempt-outcome.ts</automated></verify>
  <done>Pure model + additive pending payload + medians action; tsc/eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Real progress overlay</name>
  <files>components/capture/capture-processing-overlay.tsx, lib/estimate/poll-outcome.ts, components/capture/capture-recorder.tsx</files>
  <action>
    2a. poll-outcome.ts: forward the richer pending payload — onStageProgress now receives
        { lastStep, completedSteps, activeStepStartedAt } (adapt its current signature; update
        capture-recorder call sites consistently).
    2b. capture-processing-overlay.tsx: upgrade from three-dots to the segmented bar driven by
        props { mode, completedSteps, activeStep, activeStepStartedAt, medians }: render
        segments via computeProgress (a 250ms local timer re-computes elapsed — the TIMER is
        local but the STATE it fills toward is real and capped), current step label + elapsed
        seconds, e.g. t('Transcribing') + "18s". English step labels via inline t(): 'Saving',
        'Transcribing', 'Analyzing photos', 'Generating estimate'. Keep the calm visual language
        (reuse existing overlay styling patterns); keep the component's existing usage contract
        with capture-recorder (props additive, stage prop may remain for fallback when no
        journal data yet). Bar reaching a segment end WAITS capped at 95% of that segment until
        the journal confirms — never rolls into the next segment on its own.
    2c. capture-recorder.tsx: thread mode/attempt progress state into the overlay (completedSteps
        etc. from onStageProgress stored in state); fetch medians once per attempt via
        getStepMedians into a ref. REMOVE the Web Speech caption entirely per the file list in
        <context>: types, ref, states, recognition setup/stop calls, overlay markup
        (liveTranscript/interimTranscript), and the corresponding RecorderBody props — the
        recording overlay keeps timer + waveform + t('Listening...') pulse. Remove now-unused
        imports. The estimate-language selector and everything else stays.
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && ! grep -q "SpeechRecognition" components/capture/capture-recorder.tsx && ! grep -q "interimTranscript" components/capture/capture-recorder.tsx && grep -n "computeProgress\|progress-model" components/capture/capture-processing-overlay.tsx && npx tsc --noEmit && npx eslint components/capture/capture-recorder.tsx components/capture/capture-processing-overlay.tsx lib/estimate/poll-outcome.ts</automated></verify>
  <done>Segmented real progress live in the overlay; Web Speech fully gone; eslint baseline (NOTE: removing the caption may CLEAR some pre-existing warnings — fewer problems than baseline is acceptable, more is not).</done>
</task>

<task type="auto">
  <name>Task 3: Tests</name>
  <files>tests/unit/estimate/progress-model.test.ts</files>
  <action>
    computeProgress coverage: all-pending → fraction 0; first step done → 1/3 (audio mode);
    active step at elapsed=median → capped 0.95 fill (never 1 without confirmation); all done →
    1; text mode has 2 segments; photos mode includes analyze; medians override respected;
    elapsed beyond median stays capped. Update any existing tests broken by the poll-outcome
    signature change. Run targeted suites (tests/unit/estimate/, tests/unit/capture/) +
    npx tsc --noEmit; report counts.
  </action>
  <verify><automated>cd "C:/Users/Vanildo/Dev/xtimator" && npx vitest run tests/unit/estimate/ tests/unit/capture/ && npx tsc --noEmit</automated></verify>
  <done>Model fully covered; suites green.</done>
</task>

</tasks>

<verification>
tsc clean; eslint at-or-below baseline; targeted suites green; grep gates pass. Post-deploy
manual: record → bar advances segment-by-segment matching /admin/events timestamps; no live
caption anywhere.
</verification>

<success_criteria>
The progress a user sees is derived exclusively from real journal events + real measured
durations; the recorder never displays speech-recognition text again.
</success_criteria>

<output>
After completion, create `.planning/quick/260707-o7a-real-journal-driven-progress-bar-during-/260707-o7a-SUMMARY.md`
</output>
