---
phase: quick-260718-s8a
plan: 01
type: execute
wave: 1
depends_on: [quick-260718-t7d, quick-260718-d2f]
autonomous: true
requirements: []
files_modified:
  - components/price-book/price-book-list.tsx
  - tests/unit/price-book/price-book-list.test.tsx

must_haves:
  truths:
    - "The category-header select-all control is an icon button (ListChecks) that reads as 'select all', NOT an empty checkbox"
    - "Click selects every VISIBLE (search-filtered) item of the category; when all are already selected, click deselects them; partial selection completes to all"
    - "Per-row checkboxes left of item names unchanged (user explicitly keeps them)"
    - "Existing t7d select-all tests keep passing via the same data-testid and click-to-select semantics"
  artifacts:
    - path: "components/price-book/price-book-list.tsx"
      provides: "ListChecks icon button with state-aware color/title/aria-pressed"
      contains: "ListChecks"
  key_links:
    - from: "select-all icon button"
      to: "toggleFolderSelected"
      via: "onClick with folderSelectionState !== true"
      pattern: "toggleFolderSelected\\(folderItems"
---

<objective>
Per user request (screenshots): keep the per-row checkboxes, but the category-header select-all "cannot be an empty box — it needs to be an icon that represents select all".
</objective>

<context>
- components/price-book/price-book-list.tsx — header renders a Radix Checkbox (checked/indeterminate/false) with data-testid select-all-folder-*; rows keep their own Checkboxes.
- Replace header Checkbox with ghost icon Button (h-6 w-6, same as pencil/trash) using lucide ListChecks; muted color when nothing selected, primary when some/all; title/aria reflect Select all / Deselect all.
- tests click the testid and assert "N selected" — button semantics (click = select all unless already all) keep them green.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap header checkbox for ListChecks icon button + tests</name>
  <files>components/price-book/price-book-list.tsx, tests/unit/price-book/price-book-list.test.tsx</files>
  <action>
1. Import ListChecks; in the header, replace the Checkbox with a ghost icon Button: onClick → toggleFolderSelected(folderItems, folderSelectionState(folderItems) !== true); className switches text-muted-foreground ↔ text-primary; aria-label kept, aria-pressed = all-selected, title Select all/Deselect all; same data-testid. Keep row Checkboxes untouched.
2. Tests: add toggle coverage — click selects all ("2 selected"), second click deselects (bulk bar gone); partial selection (one row checked) then icon click completes to all.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.ci.json && npx vitest run tests/unit/price-book/</automated>
    <manual>Header shows a select-all icon (not an empty box); toggles selection of the category's visible items</manual>
  </verify>
  <done>Icon button in place, all price-book tests green including new toggle tests.</done>
</task>

<task type="auto">
  <name>Task 2: Commit (code only, then planning artifacts) — DO NOT PUSH</name>
  <files>components/price-book/price-book-list.tsx, tests/unit/price-book/price-book-list.test.tsx</files>
  <action>
- Stage exactly the two files; commit on `dev`: `feat(quick-260718-s8a): category select-all is a ListChecks icon button, not an empty checkbox`
- Then SUMMARY.md + STATE.md row; commit: `docs(quick-260718-s8a): add quick-task plan/summary and STATE row`
- Do NOT push.
  </action>
  <verify>
    <automated>git show --pretty=format: --name-only HEAD~1 # code commit touches exactly 2 files</automated>
  </verify>
  <done>Two local commits on dev; nothing pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.ci.json` → 0 errors
- price-book suites green (existing t7d/d2f tests untouched + new toggle tests)
</verification>

<success_criteria>
Category header shows a recognizable select-all icon with toggle behavior; row checkboxes unchanged; commits local only.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-s8a-price-book-select-all-icon/260718-s8a-SUMMARY.md`
</output>
