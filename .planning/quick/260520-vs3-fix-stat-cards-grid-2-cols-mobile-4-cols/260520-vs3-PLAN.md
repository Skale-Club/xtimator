---
phase: quick
plan: 260520-vs3
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/stat-cards.tsx
  - components/dashboard/stat-card.tsx
autonomous: true
requirements: [VS3-01]
must_haves:
  truths:
    - "On mobile (<lg), stat cards display in a 2-column grid (not 1 column)"
    - "On desktop (lg+), stat cards display in a 4-column grid"
    - "The value number is visibly larger than before (text-4xl, not text-3xl)"
  artifacts:
    - path: "components/dashboard/stat-cards.tsx"
      provides: "Grid wrapper with grid-cols-2 lg:grid-cols-4"
    - path: "components/dashboard/stat-card.tsx"
      provides: "Value typography at text-4xl"
  key_links:
    - from: "components/dashboard/stat-cards.tsx"
      to: "components/dashboard/stat-card.tsx"
      via: "StatCard import"
      pattern: "StatCard"
---

<objective>
Fix stat card grid layout and number font size: switch from 1-col mobile / 2-col sm to 2-col mobile / 4-col desktop, and bump value number from text-3xl to text-4xl.

Purpose: Stat cards should use all available horizontal space on mobile (2 cols) and show a more prominent number at a glance.
Output: Two modified component files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix grid layout and increase value font size</name>
  <files>components/dashboard/stat-cards.tsx, components/dashboard/stat-card.tsx</files>
  <action>
    In `components/dashboard/stat-cards.tsx`:
    - Change the section className grid from `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` to `grid-cols-2 lg:grid-cols-4`
    - Result: 2 columns starting at the smallest breakpoint (mobile), 4 columns at lg+

    In `components/dashboard/stat-card.tsx`:
    - Change the value `<p>` className from `text-3xl` to `text-4xl`
    - All other classes (`mt-3 font-mono tabular-nums leading-none`) remain unchanged
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    stat-cards.tsx grid class is `grid-cols-2 lg:grid-cols-4` (no sm: breakpoint).
    stat-card.tsx value paragraph has `text-4xl` (not text-3xl).
    TypeScript reports no errors on these files.
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — must complete with no errors in the two modified files.
Visually: on a 390px viewport the 4 stat cards render in 2 columns; on 1280px+ they render in 4 columns. Numbers appear larger than before.
</verification>

<success_criteria>
- `grid-cols-1` and `sm:grid-cols-2` are gone from stat-cards.tsx
- `grid-cols-2 lg:grid-cols-4` is present in stat-cards.tsx
- `text-4xl` replaces `text-3xl` in stat-card.tsx
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/quick/260520-vs3-fix-stat-cards-grid-2-cols-mobile-4-cols/260520-vs3-SUMMARY.md`
</output>
