---
milestone: v4.5
audited: 2026-06-21
status: passed
scores:
  requirements: 18/18
  phases: 5/5
  integration: 6/6
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: all (99-103)
    items:
      - "Nyquist VALIDATION.md frontmatter still says nyquist_compliant: false across all 5 phases — the validation strategies were authored and followed (Wave-0 RED tests exist, all green, full suite deterministic 3x), but the frontmatter flag was never flipped to true. Bookkeeping only; the actual Nyquist coverage is in place."
  - phase: 100-output-guardrails
    items:
      - "GUARD-04 INFO-1: the totals_discrepancy metric payload is emitted to structured logs (console.info '[totals_discrepancy]') rather than attached to the Langfuse trace / pipeline_events row. The correlation id itself IS wired across Sentry/pipeline_events/Langfuse; only the discrepancy metric is log-only. Documented design intent (pipeline_events has no free-form metadata column)."
  - phase: cross-repo
    items:
      - "Two pre-existing non-test working-tree files (components/landing/hero-section.tsx, next-env.d.ts) and an interleaved unrelated fix(ui) commit (c05cc34e) were present during the run — not part of v4.5, left untouched."
nyquist:
  compliant_phases: []
  partial_phases: [99, 100, 101, 102, 103]
  missing_phases: []
  overall: partial (flag-only — coverage present, frontmatter not flipped)
---

# Milestone v4.5 — Estimate Engine Robustness & Reliability Harness — Audit

**Status: PASSED** · 18/18 requirements satisfied · 5/5 phases verified · 6/6 integration chains wired · 3/3 E2E flows intact.

## Definition of done

Make the AI estimate generation/editing core (audio + image + text) bulletproof: one unified multimodal ingestion path, always-validated output, isolated/recoverable failures, and an evaluation harness that catches regressions before production. **Achieved.**

## Requirements coverage (3-source cross-reference)

All 18 requirements are `satisfied`: each has VERIFICATION status `passed`, appears in a phase SUMMARY's `requirements-completed`, and is checked `[x]` in REQUIREMENTS.md. No orphans, no unsatisfied, no partial.

| Category | REQ-IDs | Phase | Status |
|----------|---------|-------|--------|
| HARD (pipeline hardening) | HARD-01, HARD-02 | 101 | satisfied |
| | HARD-03, HARD-04 | 99 | satisfied |
| | HARD-05, HARD-06, HARD-07 | 102 | satisfied |
| GUARD (output guardrails) | GUARD-01, GUARD-02, GUARD-03, GUARD-04 | 100 | satisfied |
| UNIFY (modality unification) | UNIFY-01, UNIFY-02, UNIFY-03 | 101 | satisfied |
| EVAL (test/eval harness) | EVAL-01, EVAL-02, EVAL-03, EVAL-04 | 103 | satisfied |

## Phase verifications

| Phase | Name | VERIFICATION | Score |
|-------|------|--------------|-------|
| 99 | Unified Error Model + Provider-Fallback Wrapper | passed | 13/13 |
| 100 | Output Guardrails (schema, anchoring, totals, correlation) | passed | 17/17 |
| 101 | Unified Multimodal Ingestion + Refine Through the Graph | passed | 5/5 |
| 102 | Resilience Hardening (batch reporting, configurable cap + recourse, replay-safe TTL) | passed | 7/7 |
| 103 | Eval/Test Harness + CI Regression Gate | passed | 5/5 |

## Cross-phase integration (integration-checker: PASS)

All 6 chains wired in real source:
1. Refine inherits Phase-99 fallback + Phase-100 guardrails (getAIProviderWithFallback + withSchemaRetry). ✓
2. Full generate chain: fallback → anchoring → totals authority → correlation → vagueness → configurable cap. ✓
3. Typed markers (ProvidersUnavailableError/InvalidEstimateOutputError) → FailureReason (provider_unavailable/invalid_output) in both generate + refine nodes, byte-identical. ✓
4. requestedAt threads Inngest entry → graph state → WhatsApp TTL sites (no Date.now() re-mint). ✓
5. Eval harness exercises the REAL unified engine (generateEstimateForProject NOT mocked; provider seam mocked). ✓
6. needs-details recourse banner reads the awaiting_details status the default adapter writes. ✓

E2E flows intact: web generate, refine (synchronous inline preview), WhatsApp.

## Suite health

`npx vitest run` is deterministic-green across 3 consecutive runs: 250 test files / 1732 passed. A new secret-free CI regression gate (`.github/workflows/test.yml`) runs scoped typecheck (`tsconfig.ci.json`, green) + the full unit+eval suite twice, failing on any regression.

## Verdict

No critical gaps, no broken wiring, no unsatisfied requirements. Minor tech debt (Nyquist frontmatter flags not flipped; totals-discrepancy log-only) is non-blocking and tracked above. **Milestone v4.5 is ready to complete.**

## Deferred human UAT (non-blocking)

- Live provider-outage fallback delivery (HARD-03/UNIFY-03) in staging.
- Editor refine end-to-end (record/photo/text → preview → save) in staging.
- needs-details banner appears + CTA regenerates in staging (HARD-06).
- WhatsApp partial-batch reply wording on a real device (HARD-05).
- CI gate goes red on a deliberately-broken metric PR (EVAL-04).
