---
phase: 1001-seo-foundation-crawlability-structured-data-content-architec
plan: "04"
type: execute
wave: 3
depends_on: ["1001-03"]
files_modified:
  - app/page.tsx
  - components/landing/top-nav.tsx
  - components/landing/top-nav-auth.tsx
  - scripts/lighthouse.mjs
  - scripts/seo-smoke.mjs
  - docs/seo-launch-runbook.md
  - package.json
  - package-lock.json
  - tests/unit/seo/home-cacheability.test.ts
autonomous: true
requirements: [SEO-05, SEO-06]
user_setup:
  - service: Google Search Console
    why: "Domain ownership and sitemap submission require the owner's account/DNS access."
    dashboard_config:
      - task: "Verify xtimator.com as a domain property"
        location: "Google Search Console → Add property → Domain"
        details: "Complete the DNS verification using the production DNS provider."
      - task: "Submit the production sitemap and inspect representative URLs"
        location: "Google Search Console → Sitemaps and URL Inspection"
        details: "Submit https://xtimator.com/sitemap.xml; inspect the homepage and one industry URL after deployment."
  - service: Meta Sharing Debugger
    why: "Meta controls its scraper cache."
    dashboard_config:
      - task: "Rescrape representative public URLs"
        location: "Meta Sharing Debugger"
        details: "Rescrape the homepage and one blog or industry URL after deployment; confirm og:url, og:type, image, title, and description."
must_haves:
  truths:
    - Anonymous acquisition responses are publicly cacheable.
    - Signed-in users retain useful navigation after client auth resolution.
    - CI and production smoke checks catch SEO regressions.
    - Operators have a concrete 30/60/90-day measurement runbook.
  artifacts:
    - scripts/seo-smoke.mjs
    - docs/seo-launch-runbook.md
  key_links:
    - Lighthouse and smoke scripts run against representative public URLs.
    - Runbook references the exact production routes and pass thresholds.
---

<objective>
Make acquisition pages fast and turn SEO quality into an operationally verified gate.

Purpose: Metadata without cacheability, performance, deployment checks, and measurement will decay.
Output: cache-safe homepage auth enhancement, stronger Lighthouse/smoke scripts, and launch/measurement runbook.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1001-seo-foundation-crawlability-structured-data-content-architec/1001-CONTEXT.md
@app/page.tsx
@components/landing/top-nav.tsx
@components/landing/top-nav-auth.tsx
@scripts/lighthouse.mjs

Decisions covered: D-01, D-02, D-03, D-13, D-14
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore anonymous homepage cacheability</name>
  <files>app/page.tsx, components/landing/top-nav.tsx, components/landing/top-nav-auth.tsx, tests/unit/seo/home-cacheability.test.ts</files>
  <read_first>app/page.tsx, components/landing/landing-page.tsx, components/landing/top-nav.tsx, components/landing/top-nav-auth.tsx, lib/supabase/client.ts</read_first>
  <action>Remove the request-bound `supabase.auth.getUser()` dependency from the homepage server render so anonymous HTML can be statically rendered or safely revalidated. Preserve signed-in navigation via a minimal client-side auth enhancement after hydration, without hiding core landing content or changing CTA behavior. Keep branding/landing-content caching semantics explicit and verify production response no longer emits private/no-store solely because of auth.</action>
  <verify>npx vitest run tests/unit/seo/home-cacheability.test.ts && npm run build</verify>
  <acceptance_criteria>
    - `app/page.tsx` performs no request-bound auth read.
    - Anonymous homepage HTML contains all acquisition content.
    - Signed-in nav enhancement is isolated to the navigation component.
    - A source/behavior test prevents reintroducing server auth into the public page.
  </acceptance_criteria>
  <done>Homepage is cacheable for anonymous traffic while authenticated navigation still resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Add repeatable SEO gates and operational runbook</name>
  <files>scripts/lighthouse.mjs, scripts/seo-smoke.mjs, docs/seo-launch-runbook.md, package.json</files>
  <read_first>scripts/lighthouse.mjs, package.json, README-DEPLOY.md</read_first>
  <action>Install `lighthouse` and `chrome-launcher` as pinned dev dependencies using the repository's npm lockfile. Make Lighthouse fail when SEO is below 95 or performance/accessibility below 85 on homepage and one representative industry/blog URL. Add a production HTTP smoke script asserting robots/sitemap 200, canonical agreement, noindex on representative private routes, OG completeness, JSON-LD parseability, and non-private cache headers on acquisition pages. Add package scripts. Write an exact runbook for Search Console/Bing ownership, sitemap submission, URL inspection, Rich Results/schema validation, Meta rescrape, baseline keyword/query export, conversion annotation, and 30/60/90-day review. Distinguish deploy-time pass criteria from lagging organic outcomes.</action>
  <verify>node scripts/seo-smoke.mjs https://xtimator.com --allow-predeploy-failures && node scripts/lighthouse.mjs http://localhost:9633/ http://localhost:9633/industries/general-contractors</verify>
  <acceptance_criteria>
    - Scripts exit non-zero on threshold or metadata-policy failure.
    - Runbook contains owner, exact URL/tool, expected result, and rollback/escalation for each launch check.
    - 30/60/90-day metrics include indexed URLs, impressions, clicks, non-brand queries, CTR, and acquisition conversion.
  </acceptance_criteria>
  <done>SEO quality is repeatably verifiable in CI, production, and post-launch operations.</done>
</task>

</tasks>

<verification>
- [ ] Cacheability test and production build pass.
- [ ] Lighthouse thresholds pass locally on representative pages.
- [ ] Production smoke script passes after deployment.
- [ ] Search Console and Meta manual steps are clearly separated as user setup.
</verification>

<success_criteria>
- SEO-05 and SEO-06 are fully satisfied.
- The phase has both immediate technical gates and lagging outcome measurement.
</success_criteria>

<output>
After completion, create `1001-04-SUMMARY.md`.
</output>
