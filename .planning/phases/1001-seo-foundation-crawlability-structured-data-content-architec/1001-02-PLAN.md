---
phase: 1001-seo-foundation-crawlability-structured-data-content-architec
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/blog/page.tsx
  - app/blog/[slug]/page.tsx
  - components/seo/json-ld.tsx
  - lib/seo/structured-data.ts
  - tests/unit/seo/public-metadata.test.ts
  - tests/unit/seo/structured-data.test.ts
autonomous: true
requirements: [SEO-02, SEO-03]
user_setup: []
must_haves:
  truths:
    - Public pages emit complete canonical, Open Graph, and Twitter metadata.
    - Homepage emits Organization, WebSite, and SoftwareApplication JSON-LD.
    - Blog posts emit Article and BreadcrumbList JSON-LD.
    - No fake Facebook App ID is emitted.
  artifacts:
    - components/seo/json-ld.tsx
    - lib/seo/structured-data.ts
  key_links:
    - Structured data derives branding and canonical URLs from existing sources.
    - Blog schema derives title, dates, image, and description from the rendered post.
---

<objective>
Complete search/social metadata and add standards-valid structured data.

Purpose: Make Xtimator understandable to search engines and social scrapers with consistent, non-duplicated facts.
Output: complete metadata, JSON-LD builders/component, and parse/contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1001-seo-foundation-crawlability-structured-data-content-architec/1001-CONTEXT.md
@app/layout.tsx
@app/blog/[slug]/page.tsx
@lib/platform-config.ts

Decisions covered: D-07, D-08, D-09
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit complete public and social metadata</name>
  <files>app/layout.tsx, app/page.tsx, app/blog/page.tsx, app/blog/[slug]/page.tsx, tests/unit/seo/public-metadata.test.ts</files>
  <read_first>lib/seo/metadata.ts, app/layout.tsx, app/blog/page.tsx, app/blog/[slug]/page.tsx</read_first>
  <action>Use the shared metadata factory to emit unique titles/descriptions, self-canonicals, complete Open Graph (`url`, `type`, `siteName`, locale, image URL/width/height/alt), and Twitter cards. Keep descriptions concise. Give blog index route-specific metadata. Article metadata must include canonical URL and article title/description/image without dropping required parent fields. Add optional `fb:app_id` only when an actual configured value exists; absence must omit the tag.</action>
  <verify>npx vitest run tests/unit/seo/public-metadata.test.ts</verify>
  <acceptance_criteria>
    - Meta debugger-required `og:url` and `og:type` are present.
    - Homepage, blog index, and article canonicals are self-referencing.
    - Social images declare 1200×630 metadata and non-empty alt text.
    - No placeholder Facebook App ID exists in source or rendered metadata.
  </acceptance_criteria>
  <done>Representative public routes expose complete, unique search and social metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add reusable, validated JSON-LD</name>
  <files>components/seo/json-ld.tsx, lib/seo/structured-data.ts, app/page.tsx, app/blog/[slug]/page.tsx, tests/unit/seo/structured-data.test.ts</files>
  <read_first>app/page.tsx, app/blog/[slug]/page.tsx, lib/platform-config.ts, lib/queries/blog.ts</read_first>
  <action>Implement typed builders for Organization, WebSite, SoftwareApplication, Article, and BreadcrumbList plus a server-rendered `JsonLd` component. Serialize safely so `<` cannot terminate the script. Values must match visible content and canonical metadata. Render the three acquisition schemas on home and Article/Breadcrumb on posts. Do not claim ratings, pricing, or features not visibly supported.</action>
  <verify>npx vitest run tests/unit/seo/structured-data.test.ts && npm run build</verify>
  <acceptance_criteria>
    - Every JSON-LD script parses as JSON.
    - Required schema properties and canonical IDs/URLs are asserted.
    - Script serialization escapes unsafe `<` characters.
    - Tests prove article schema uses the post's real dates and content metadata.
  </acceptance_criteria>
  <done>Search engines receive valid schema that matches page content.</done>
</task>

</tasks>

<verification>
- [ ] Metadata and JSON-LD tests pass.
- [ ] Production build passes.
- [ ] Raw server HTML contains canonical, complete OG/Twitter tags, and JSON-LD.
</verification>

<success_criteria>
- SEO-02 and SEO-03 are fully satisfied.
- Meta Sharing Debugger warnings are resolved except conditional `fb:app_id`.
</success_criteria>

<output>
After completion, create `1001-02-SUMMARY.md`.
</output>
