---
phase: 94-estimate-invoice-decoupling
plan: 06
type: execute
wave: 5
depends_on: ["94-01", "94-03", "94-04", "94-05"]
files_modified:
  - supabase/migrations/20260619000003_phase94_backfill_invoices.sql
  - app/estimate/[token]/pay/route.ts
  - tests/unit/billing/estimate-pay.test.ts
  - components/share/estimate-view.tsx
  - app/estimate/[token]/page.tsx
  - lib/billing/connect-webhook.ts
  - scripts/seed-demo-workspace.mjs
autonomous: true
requirements: [INVOICE-07, INVOICE-01]

must_haves:
  truths:
    - "A backfill migration creates one invoices row (kind=full, status=paid) per already-paid estimate so payment history is preserved"
    - "The old /estimate/[token]/pay Checkout Session route is deleted and its test is removed"
    - "The share page no longer renders the old Checkout PayNowButton — payment now flows through issued-invoice hosted links"
    - "Existing estimates load on the share page without error after the migration"
  artifacts:
    - path: "supabase/migrations/20260619000003_phase94_backfill_invoices.sql"
      provides: "Backfill of paid estimates into invoices"
      contains: "INSERT INTO public.invoices"
  key_links:
    - from: "supabase/migrations/20260619000003_phase94_backfill_invoices.sql"
      to: "estimates (payment_status='paid')"
      via: "INSERT ... SELECT backfill"
      pattern: "payment_status"
---

<objective>
Complete the decoupling (INVOICE-07): backfill one `invoices` row per already-paid estimate, retire the old `/estimate/[token]/pay` Checkout Session route (and delete its test), and remove the now-superseded Checkout `PayNowButton` from the share page so customer payment flows exclusively through the new issued-invoice hosted links. Existing estimates must load without error.

Purpose: Migrate history forward and remove the dead Checkout path. Sequenced LAST so the backfill test (Plan 01), the invoice.paid webhook (Plan 03), the share-page issued-invoice links (Plan 04), and the consolidate removal (Plan 05) are all in place first.
Output: 1 backfill migration + route deletion + test deletion + share-page Checkout removal + optional Connect-webhook checkout-case cleanup + demo-seed coherence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-estimate-invoice-decoupling/94-CONTEXT.md
@.planning/phases/94-estimate-invoice-decoupling/94-RESEARCH.md

<interfaces>
<!-- The retirement targets + the backfill source columns. -->

Backfill source — estimates table (paid rows carry, from Phase 70):
- payment_status ('paid' on paid estimates), payment_amount_cents (int cents), currency_code, id, company_id, paid_at
- The new invoices columns to fill: estimate_id, company_id, kind='full', amount_cents (= payment_amount_cents), currency_code, status='paid', paid_at, created_at

Backfill test contract (tests/unit/billing/invoices-backfill-migration.test.ts from Plan 01) expects the file at:
  supabase/migrations/20260619000003_phase94_backfill_invoices.sql
and expects: INSERT INTO public.invoices ... SELECT ... FROM estimates WHERE payment_status = 'paid', with kind 'full' and status 'paid'.

Route to delete: app/estimate/[token]/pay/route.ts (the POST Checkout Session handler).
Test to delete: tests/unit/billing/estimate-pay.test.ts.

Share page (components/share/estimate-view.tsx) — the OLD Checkout block to remove:
- import { PayNowButton } from '@/components/estimate/pay-now-button' (:10)
- the Card block at ~:231-253 that renders <PayNowButton .../> (keep the issued-invoice links added in Plan 04)
- PaymentSuccessBanner/PaymentCanceledNotice imports (:12-13) driven by stripeState (the ?stripe=success param produced ONLY by the deleted Checkout redirect)

Share page server (app/estimate/[token]/page.tsx) — the stripeState derivation at :74-79 + the stripeState={...} prop on <EstimateView /> (:104). If the stripeState prop is dropped from EstimateView, remove it here too.

Connect webhook (lib/billing/connect-webhook.ts) — OPTIONAL cleanup:
- the checkout.session.completed case is now dead for estimates (invoices ride invoice.paid). Leaving it is harmless. Action specifies the safe choice.

Demo seed (scripts/seed-demo-workspace.mjs:280-288) — writes workflow_status/consolidated_*; columns are dormant (NOT dropped in this phase) so this still runs. Optional coherence update.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill migration — one invoices row per paid estimate (INVOICE-07 / D-22)</name>
  <read_first>
    - tests/unit/billing/invoices-backfill-migration.test.ts (the RED contract from Plan 01 — this task makes it green; match the file path + assertions exactly)
    - supabase/migrations/20260619000001_phase94_invoices.sql (the invoices columns from Plan 01 — the backfill must satisfy the CHECKs: amount_cents > 0, kind IN (...), status IN (...))
    - supabase/migrations/20260517000001_phase70_stripe_connect_columns.sql (confirm the estimates payment_* column names the backfill reads)
  </read_first>
  <action>
    Create `supabase/migrations/20260619000003_phase94_backfill_invoices.sql`:
    ```sql
    -- Phase 94 (INVOICE-07 / D-22): preserve payment history by creating one
    -- invoices row (kind='full', status='paid') per already-paid estimate.
    -- Idempotent: skip estimates that already have a paid full invoice (re-run safe).
    -- Only backfills rows with a positive amount (the amount_cents CHECK requires > 0).
    INSERT INTO public.invoices (
      estimate_id, company_id, kind, amount_cents, currency_code,
      project_name, status, stripe_invoice_id, paid_at, created_at
    )
    SELECT
      e.id,
      e.company_id,
      'full',
      e.payment_amount_cents,
      COALESCE(e.currency_code, 'USD'),
      p.name,
      'paid',
      NULL,
      COALESCE(e.paid_at, e.updated_at, now()),
      COALESCE(e.paid_at, e.updated_at, now())
    FROM estimates e
    LEFT JOIN projects p ON p.id = e.project_id
    WHERE e.payment_status = 'paid'
      AND e.payment_amount_cents IS NOT NULL
      AND e.payment_amount_cents > 0
      AND NOT EXISTS (
        SELECT 1 FROM public.invoices i
        WHERE i.estimate_id = e.id AND i.kind = 'full' AND i.status = 'paid'
      );
    ```
    Notes: `stripe_invoice_id` is NULL for backfilled rows (the partial unique index `idx_invoices_stripe_invoice_id` only indexes non-null values, so multiple NULLs are fine). The `NOT EXISTS` guard makes the migration idempotent. Do NOT run db push.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/invoices-backfill-migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260619000003_phase94_backfill_invoices.sql` exists
    - It contains `INSERT INTO public.invoices`, `FROM estimates`, `payment_status = 'paid'`, `'full'`, and `'paid'`
    - It contains a `NOT EXISTS` idempotency guard and `payment_amount_cents > 0`
    - `npx vitest run tests/unit/billing/invoices-backfill-migration.test.ts` exits 0
  </acceptance_criteria>
  <done>The backfill migration creates a paid full invoice per paid estimate, idempotently; the Plan 01 backfill test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the /estimate/[token]/pay Checkout route + delete its test</name>
  <read_first>
    - app/estimate/[token]/pay/route.ts (the route being deleted — confirm nothing else imports it)
    - tests/unit/billing/estimate-pay.test.ts (the test being deleted with the route)
    - .planning/phases/94-estimate-invoice-decoupling/94-RESEARCH.md ("Retire entirely" table — D-23)
  </read_first>
  <action>
    Delete the file `app/estimate/[token]/pay/route.ts` entirely (remove the route). Delete the file `tests/unit/billing/estimate-pay.test.ts` (it tests the retired route — RESEARCH "Retire entirely"). Use the filesystem (e.g. `rm`) to remove both files.

    After deletion, grep the repo to confirm no remaining import or fetch targets the deleted route module or `/estimate/${...}/pay`. The OLD `PayNowButton` component (`components/estimate/pay-now-button.tsx`) POSTs to `/estimate/[token]/pay`; it is removed from the share page in Task 3. Delete the `pay-now-button.tsx` component file too ONLY if nothing else imports it after Task 3 (grep first; if other surfaces import it, leave it).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - The path `app/estimate/[token]/pay/route.ts` no longer exists (deleted)
    - `tests/unit/billing/estimate-pay.test.ts` no longer exists (deleted)
    - `grep -rn "token./pay\|\\[token\\]/pay\|pay-now-button" app/ lib/` returns no live route/fetch references to the deleted Checkout route (component reference in estimate-view.tsx is removed in Task 3)
    - `npx vitest run` exits 0 (the deleted test no longer runs; nothing else broke)
  </acceptance_criteria>
  <done>The Checkout pay route and its test are removed; no code references the dead route.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the old Checkout PayNowButton + dead stripeState banner from the share page</name>
  <read_first>
    - components/share/estimate-view.tsx (the import :10, the banner imports :12-13, the PayNowButton Card block ~:231-253, and the issued-invoice links added in Plan 04 — keep those)
    - app/estimate/[token]/page.tsx (the stripeState derivation :74-79 and the stripeState prop on EstimateView :104)
    - components/estimate/pay-now-button.tsx (confirms it targets the retired route)
  </read_first>
  <action>
    In `components/share/estimate-view.tsx`:
    - Remove the `import { PayNowButton } from '@/components/estimate/pay-now-button'` line (:10).
    - Remove the `<PayNowButton .../>` usage and the surrounding Card block condition that gated it (~:231-253). The issued-invoice pay links added in Plan 04 are the replacement payment surface — KEEP those.
    - The `?stripe=success|canceled` URL param was produced ONLY by the deleted Checkout redirect, so `stripeState` is now always null. Remove the `PaymentSuccessBanner` / `PaymentCanceledNotice` imports (:12-13) and their render, and remove the `stripeState` prop from `EstimateViewProps` + the destructure.
    - In `app/estimate/[token]/page.tsx`: remove the `stripeState` derivation (:74-79) and the `stripeState={stripeState}` prop on `<EstimateView />` (:104), since the prop is gone.

    Net result: the share page shows the issued-invoice "Pay {kind} — $X" links (Plan 04) and no longer shows the old full-amount Checkout button or the Checkout-redirect banners. Keep the smallest coherent change — do not touch unrelated share UI.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PayNowButton" components/share/estimate-view.tsx` returns NO hits
    - `grep "stripeState" components/share/estimate-view.tsx` returns NO hits and `grep "stripeState" app/estimate/[token]/page.tsx` returns NO hits
    - `grep "hosted_invoice_url" components/share/estimate-view.tsx` STILL returns a hit (Plan 04 issued-invoice links preserved)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>The share page no longer renders the Checkout PayNowButton or the dead stripeState banners; issued-invoice pay links remain.</done>
</task>

<task type="auto">
  <name>Task 4: Optional cleanup — Connect-webhook checkout case + demo-seed coherence</name>
  <read_first>
    - lib/billing/connect-webhook.ts (the now-dead checkout.session.completed case; the live path is invoice.paid from Plan 03)
    - tests/unit/webhooks/connect-events.test.ts (Plan 03 rewrite — confirm it does NOT depend on the checkout.session.completed case before removing it)
    - scripts/seed-demo-workspace.mjs (lines 280-288 — workflow_status/consolidated_* writes; columns are dormant so this still runs)
  </read_first>
  <action>
    1. Connect webhook: the `checkout.session.completed` case in `handleConnectEvent` is now dead (no Checkout sessions are created for estimates after Task 2). Remove the `case 'checkout.session.completed':` line and its `handleCheckoutSessionCompleted` function ONLY IF the Plan 03 connect-events test no longer asserts that path (verify by reading the test first). If the test still has a smoke assertion for it, leave the case in place (harmless dead code is acceptable; do not break the suite). Prefer removal for cleanliness when safe.

    2. Demo seed coherence (optional, low-risk): in `scripts/seed-demo-workspace.mjs`, the `workflow_status`/`consolidated_at`/`consolidated_by` inserts (lines 280-288) still work because the columns are dormant (not dropped). No change is required. If you want coherence with the always-editable model, you MAY simplify the insert to drop those three fields (they default correctly), but this is not required for correctness. Do NOT remove the demo estimate creation itself.

    Whichever choices you make, the full suite must stay green.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite green)
    - If the checkout.session.completed case was removed: `grep "checkout.session.completed" lib/billing/connect-webhook.ts` returns no hits AND the connect-events test still passes. If it was kept: the suite still passes.
    - `scripts/seed-demo-workspace.mjs` still references valid columns (no reference to a column that was dropped — none were dropped in this phase)
  </acceptance_criteria>
  <done>Dead Checkout webhook code is removed when safe; the demo seed remains valid; the full suite is green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run` green (estimate-pay.test.ts gone; backfill test green; connect invoice.paid green).
- The pay route path is deleted; no live references to `/estimate/[token]/pay`.
- The backfill migration is idempotent and only touches paid estimates with positive amounts.
</verification>

<success_criteria>
- One paid full invoice backfilled per already-paid estimate (history preserved).
- Old Checkout pay route + test retired; share page pays via issued-invoice hosted links.
- Existing estimates load on the share page without error.
</success_criteria>

<output>
After completion, create `.planning/phases/94-estimate-invoice-decoupling/94-06-SUMMARY.md`
</output>
