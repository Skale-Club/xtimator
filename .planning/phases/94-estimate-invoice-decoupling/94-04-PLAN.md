---
phase: 94-estimate-invoice-decoupling
plan: 04
type: execute
wave: 3
depends_on: ["94-02"]
files_modified:
  - components/workspace/estimate/generate-invoice-dialog.tsx
  - components/workspace/estimate/issued-invoices-panel.tsx
  - components/workspace/estimate/estimate-tab.tsx
  - components/workspace/project-workspace.tsx
  - app/(app)/projects/[id]/page.tsx
  - components/share/estimate-view.tsx
  - lib/queries/share.ts
autonomous: false
requirements: [INVOICE-03, INVOICE-06]

must_haves:
  truths:
    - "From the estimate editor the owner can open a Generate Invoice dialog, pick a deposit % or full amount, and trigger generateInvoice"
    - "Issued invoices are surfaced inline in the editor as 'Invoice issued: $X · {status}' with links to the hosted page + PDF"
    - "Editing an estimate after an invoice is issued does not change the issued invoice amount shown (it reads the stored snapshot)"
    - "The public share page surfaces a pay link (hosted_invoice_url) for each open issued invoice"
  artifacts:
    - path: "components/workspace/estimate/generate-invoice-dialog.tsx"
      provides: "Deposit %/full picker that calls generateInvoice"
    - path: "components/workspace/estimate/issued-invoices-panel.tsx"
      provides: "Inline issued-invoice list in the editor"
    - path: "lib/queries/share.ts"
      provides: "Issued invoices joined into the share payload"
  key_links:
    - from: "components/workspace/estimate/generate-invoice-dialog.tsx"
      to: "lib/actions/invoice.ts"
      via: "generateInvoice server action call"
      pattern: "generateInvoice"
    - from: "app/(app)/projects/[id]/page.tsx"
      to: "lib/queries/invoice.ts"
      via: "getInvoicesByEstimateId server fetch passed to the workspace"
      pattern: "getInvoicesByEstimateId"
    - from: "components/share/estimate-view.tsx"
      to: "invoices.hosted_invoice_url"
      via: "rendered pay links"
      pattern: "hosted_invoice_url"
---

<objective>
Build the generate-invoice surface (D-18) and the issued-invoice display (D-19) in the estimate editor, and surface "Pay deposit / Pay balance" links on the public share page (D-discretion → yes). This is the user-facing half of the invoice feature — it consumes the `generateInvoice` action + `getInvoicesByEstimateId` query from Plan 02.

Purpose: Let owners issue deposit/balance/full invoices from the editor and see issued invoices inline so editing-after-issue is never confusing; let clients pay from the share link they already have.
Output: 2 new components + wiring through the workspace data flow + a share-page pay-links section + a share-query join.

This plan has a human-verify checkpoint (UI/flow correctness) — `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-estimate-invoice-decoupling/94-CONTEXT.md
@.planning/phases/94-estimate-invoice-decoupling/94-RESEARCH.md

<interfaces>
<!-- The action + query this UI consumes (from Plan 02) and the data-flow seams. -->

From lib/actions/invoice.ts (Plan 02):
```typescript
export async function generateInvoice(estimateId: string, opts: { kind: 'deposit'|'balance'|'full'; depositPct?: number }): Promise<{ data: { hostedInvoiceUrl: string|null; invoicePdfUrl: string|null } } | { error: string }>
export async function getInvoicesForEstimate(estimateId: string): Promise<{ data: InvoiceRow[] } | { error: string }>
```

From lib/queries/invoice.ts (Plan 02):
```typescript
export interface InvoiceRow { id; estimate_id; company_id; kind: 'deposit'|'balance'|'full'; amount_cents: number; currency_code: string; status: 'draft'|'open'|'paid'|'void'|'uncollectible'; stripe_invoice_id: string|null; hosted_invoice_url: string|null; invoice_pdf_url: string|null; paid_at: string|null; created_at: string }
export async function getInvoicesByEstimateId(supabase, estimateId): Promise<InvoiceRow[]>
```

From lib/money/currency.ts:
```typescript
export function formatMinorUnits(cents, currencyCode): string   // for "Invoice issued: $X"
```

Data flow (server → client) — the seam to thread invoices through:
- app/(app)/projects/[id]/page.tsx (server) fetches currentEstimate via getCurrentEstimate(supabase, project.id) at line 154, then renders <ProjectWorkspace currentEstimate={...} allVersions={...} ... />
- components/workspace/project-workspace.tsx passes currentEstimate/allVersions down; EstimateTab is rendered inside the OverviewTab path (it receives currentEstimate as `currentEstimate`)
- components/workspace/estimate/estimate-tab.tsx renders <EstimateEditor estimate={currentEstimate} ... />

Share page seam:
- lib/queries/share.ts → getEstimateByShareToken(token) builds ShareEstimateData. Add an `invoices` array to the payload.
- components/share/estimate-view.tsx receives `estimate` (ShareEstimateData['estimate']); the Pay-Now Card block is at lines 231-253. Add issued-invoice pay links near it. (The OLD PayNowButton is removed in Plan 06 — do NOT remove it here; just ADD the issued-invoice links.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GenerateInvoiceDialog + IssuedInvoicesPanel components</name>
  <files>components/workspace/estimate/generate-invoice-dialog.tsx, components/workspace/estimate/issued-invoices-panel.tsx</files>
  <read_first>
    - components/workspace/estimate/estimate-floating-actions.tsx (the existing AlertDialog + Button patterns + Lucide icon usage; reuse Button/Dialog primitives the same way)
    - components/ui/dialog.tsx (the shadcn Dialog primitive shape) and components/ui/button.tsx (variants: 'primary', 'secondary', 'outline')
    - lib/actions/invoice.ts (generateInvoice signature — Plan 02)
    - lib/queries/invoice.ts (InvoiceRow — Plan 02)
    - lib/money/currency.ts (formatMinorUnits)
    - lib/i18n/use-translation.ts (the t() hook — wrap user-facing strings in t() per project i18n convention)
  </read_first>
  <action>
    Create `components/workspace/estimate/generate-invoice-dialog.tsx` (`'use client'`):
    - Props: `{ estimateId: string; currencyCode: string; estimateTotalCents: number; onIssued?: () => void }`.
    - A Dialog triggered by a `<Button variant="primary">` labeled t('Generate invoice') with a FileText/Receipt Lucide icon.
    - Inside: a 3-button segmented control [Full | Deposit | Balance]. Full uses the whole total. Deposit and Balance both reveal preset percent chips [10, 20, 30, 50] plus a numeric input (1–99). This makes deposit+balance from one estimate reachable (D-16).
    - Live preview using formatMinorUnits: Full → `formatMinorUnits(estimateTotalCents, currencyCode)`; Deposit → `formatMinorUnits(Math.round(estimateTotalCents * pct / 100), currencyCode)`; Balance → `formatMinorUnits(estimateTotalCents - Math.round(estimateTotalCents * pct / 100), currencyCode)`. (Preview is approximate for 0-decimal currencies; the action computes exact cents via splitDepositBalance.)
    - An "Issue invoice" button that calls `generateInvoice(estimateId, { kind: 'full' })`, `{ kind: 'deposit', depositPct }`, or `{ kind: 'balance', depositPct }`. On `{ error }` → `toast.error(error)`. On `{ data }` → `toast.success('Invoice issued')`, close the dialog, then `router.refresh()` AFTER close (Pitfall 5 close-then-refresh convention), and call `onIssued?.()`. Disable the button while pending (useState pending flag).

    Create `components/workspace/estimate/issued-invoices-panel.tsx` (`'use client'`):
    - Props: `{ invoices: InvoiceRow[] }`.
    - If `invoices.length === 0` render nothing (null).
    - Else render a small Card listing each invoice: `Invoice issued: {formatMinorUnits(inv.amount_cents, inv.currency_code)} · {inv.kind} · {inv.status}` with a "View invoice" link to `inv.hosted_invoice_url` (target=_blank rel=noopener) and a "PDF" link to `inv.invoice_pdf_url` when present. Use a status color hint (paid → success token, open → muted). Wrap labels in t().
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Files `components/workspace/estimate/generate-invoice-dialog.tsx` and `components/workspace/estimate/issued-invoices-panel.tsx` exist, both start with `'use client'`
    - `grep "generateInvoice" components/workspace/estimate/generate-invoice-dialog.tsx` returns a hit
    - `grep -E "kind: 'full'|kind: 'deposit'|kind: 'balance'" components/workspace/estimate/generate-invoice-dialog.tsx` returns hits for all three kinds
    - `grep "hosted_invoice_url" components/workspace/estimate/issued-invoices-panel.tsx` returns a hit
    - `grep "formatMinorUnits" components/workspace/estimate/issued-invoices-panel.tsx` returns a hit
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both editor invoice components compile and call the Plan 02 action/query types correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Thread issued invoices through the workspace and mount the editor surfaces</name>
  <files>app/(app)/projects/[id]/page.tsx, components/workspace/project-workspace.tsx, components/workspace/estimate/estimate-tab.tsx, components/workspace/estimate/estimate-editor.tsx</files>
  <read_first>
    - app/(app)/projects/[id]/page.tsx (lines 150-185 — where getCurrentEstimate is fetched and ProjectWorkspace is rendered)
    - components/workspace/project-workspace.tsx (the props interface lines 26-45 and where currentEstimate/allVersions are passed to children)
    - components/workspace/estimate/estimate-tab.tsx (renders EstimateEditor — pass invoices through)
    - components/workspace/estimate/estimate-editor.tsx (the editor render block lines 286-322 — mount IssuedInvoicesPanel near the document and the GenerateInvoiceDialog as a primary action)
    - lib/queries/invoice.ts (getInvoicesByEstimateId — Plan 02)
  </read_first>
  <action>
    1. In `app/(app)/projects/[id]/page.tsx` after line 154 (`const currentEstimate = await getCurrentEstimate(...)`), add:
       ```typescript
       import { getInvoicesByEstimateId } from '@/lib/queries/invoice'
       // ...
       const issuedInvoices = currentEstimate ? await getInvoicesByEstimateId(supabase, currentEstimate.id) : []
       ```
       Pass `issuedInvoices={issuedInvoices}` to `<ProjectWorkspace />`.

    2. In `components/workspace/project-workspace.tsx`: add `issuedInvoices: InvoiceRow[]` to `ProjectWorkspaceProps` (import the type from `@/lib/queries/invoice`), destructure it, and pass it down to the EstimateTab render site (search for `currentEstimate={`). If EstimateTab is reached via OverviewTab, thread it through OverviewTab's props too.

    3. In `components/workspace/estimate/estimate-tab.tsx`: add `issuedInvoices: InvoiceRow[]` to `EstimateTabProps`, destructure, and pass `issuedInvoices={issuedInvoices}` to `<EstimateEditor />`.

    4. In `components/workspace/estimate/estimate-editor.tsx`:
       - Add `issuedInvoices: InvoiceRow[]` to `EstimateEditorProps` (import InvoiceRow from `@/lib/queries/invoice`), destructure it.
       - Import `IssuedInvoicesPanel` and `GenerateInvoiceDialog`.
       - In the render block (after `<EstimateDocument .../>`, before/with `<EstimateFloatingActions />`), render `<IssuedInvoicesPanel invoices={issuedInvoices} />` and a `<GenerateInvoiceDialog estimateId={estimate.id} currencyCode={state.currency_code} estimateTotalCents={Math.round(state.total * 100)} onIssued={() => router.refresh()} />`. Use `state.total` (current editor total) for the dialog preview only — the issued invoice snapshot comes from the server (D-07).

    Do NOT remove the existing floating-actions Save/Discard. (The consolidate buttons in floating-actions are removed in Plan 05 — this plan only ADDS the invoice surface.)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "getInvoicesByEstimateId" app/(app)/projects/[id]/page.tsx` returns a hit and `issuedInvoices=` is passed to ProjectWorkspace
    - `grep "issuedInvoices" components/workspace/project-workspace.tsx` returns hits (prop added + threaded)
    - `grep "issuedInvoices" components/workspace/estimate/estimate-tab.tsx` returns a hit
    - `grep "IssuedInvoicesPanel" components/workspace/estimate/estimate-editor.tsx` and `grep "GenerateInvoiceDialog" components/workspace/estimate/estimate-editor.tsx` both return hits
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Issued invoices are server-fetched and flow to the editor; the editor renders the generate-invoice trigger and the issued-invoice panel.</done>
</task>

<task type="auto">
  <name>Task 3: Surface issued-invoice pay links on the public share page</name>
  <files>lib/queries/share.ts, components/share/estimate-view.tsx</files>
  <read_first>
    - lib/queries/share.ts (the ShareEstimateData shape lines 13-56 and getEstimateByShareToken body — add an invoices array)
    - components/share/estimate-view.tsx (lines 44-62 props interface + lines 231-253 the Pay-Now Card block)
    - lib/queries/invoice.ts (getInvoicesByEstimateId — but the share query uses requireServiceClient; call it with the service client or inline the select)
  </read_first>
  <action>
    1. In `lib/queries/share.ts`: extend `ShareEstimateData['estimate']` (or add a sibling field) with `invoices: { id: string; kind: string; amount_cents: number; currency_code: string; status: string; hosted_invoice_url: string | null }[]`. In `getEstimateByShareToken`, after loading the estimate, fetch its invoices via the service client: `svc.from('invoices').select('id, kind, amount_cents, currency_code, status, hosted_invoice_url').eq('estimate_id', estimate.id).in('status', ['open','paid'])`. Attach the array to the returned payload. Only expose the 6 safe fields listed (no stripe_customer_id, no stripe_invoice_id).

    2. In `components/share/estimate-view.tsx`: add `invoices` to the destructured `estimate` usage. Below the existing Pay-Now Card (around line 253), render an "Invoices" section: for each invoice with `status === 'open'` and a non-null `hosted_invoice_url`, render a button/link "Pay {kind} — {formatMinorUnits(amount_cents, currency_code)}" that opens `hosted_invoice_url` (target=_blank rel=noopener). For `status === 'paid'` invoices, render a muted "Paid: {kind} — {amount}" line. If there are no invoices, render nothing. Import `formatMinorUnits` from `@/lib/money/currency`.

    Do NOT remove the existing PayNowButton block — Plan 06 retires it. Keep both visible for this wave (the issued-invoice links are additive).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/share-query.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from('invoices')" lib/queries/share.ts` returns a hit and selects `hosted_invoice_url`
    - `grep "invoices" lib/queries/share.ts` shows the new field on ShareEstimateData
    - `grep "hosted_invoice_url" components/share/estimate-view.tsx` returns a hit
    - The share query exposes ONLY the 6 safe invoice fields (no `stripe_customer_id`/`stripe_invoice_id` in the share select for invoices)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run tests/unit/share-query.test.ts` exits 0 (note: this test's fixtures are updated in Plan 05; if it is RED here purely due to consolidated-field fixtures, that is acceptable and Plan 05 makes it green — but the invoices addition itself must not break compilation)
  </acceptance_criteria>
  <done>The share payload carries safe issued-invoice fields and the share page renders pay links for open invoices.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — generate-invoice flow + issued-invoice display</name>
  <files>none (verification only — no files modified)</files>
  <read_first>
    - components/workspace/estimate/generate-invoice-dialog.tsx (the surface under test)
    - components/workspace/estimate/issued-invoices-panel.tsx (the inline display under test)
    - components/share/estimate-view.tsx (the share-page pay links under test)
  </read_first>
  <what-built>
    The estimate editor now has a "Generate invoice" dialog (Full / Deposit / Balance with % chips + live preview) wired to the generateInvoice server action, an inline "Invoice issued: $X · {status}" panel, and the public share page now shows pay links for open invoices.
  </what-built>
  <action>
    PAUSE for the human to verify the generate-invoice flow against `npm run dev`. Do NOT modify code in this task — this is a verification gate. Present the how-to-verify steps below and wait for the resume signal before continuing the phase.
  </action>
  <how-to-verify>
    Requires a connected Stripe account in test mode (Settings → Payments shows Connected). With `npm run dev`:
    1. Open a project with an estimate. Confirm a "Generate invoice" button is visible in the editor.
    2. Click it, choose Deposit, pick 30% — confirm the preview shows ~30% of the total and the implied balance.
    3. Issue it. Confirm a toast, and that the editor now shows "Invoice issued: $X · open" with working "View invoice" (opens the Stripe-hosted page) and "PDF" links.
    4. Edit the estimate total (change an item price) and save. Confirm the issued-invoice line still shows the ORIGINAL issued amount (snapshot frozen — D-07/INVOICE-06).
    5. Re-open the dialog, choose Balance / the remainder, issue it — confirm a second invoice row appears.
    6. Open the public share link for that estimate — confirm "Pay deposit" / "Pay balance" links render and open the Stripe-hosted invoice pages.
  </how-to-verify>
  <verify>
    <automated>MISSING — human checkpoint; verification is manual per the how-to-verify steps (no automated command)</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms the generate-invoice dialog issues a deposit AND a balance invoice from one estimate
    - Human confirms the issued-invoice amount does NOT change after editing the estimate (snapshot frozen)
    - Human confirms the share page renders working "Pay deposit/balance" hosted links
  </acceptance_criteria>
  <done>Human has typed "approved" (or issues were captured and addressed) for the full generate-invoice + issued-invoice + share-link flow.</done>
  <resume-signal>Type "approved" or describe issues (e.g. preview math wrong, snapshot not frozen, share links missing).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean across the new components + wiring.
- The editor shows the generate-invoice trigger and issued invoices; the share page shows pay links.
- Issued-invoice amounts read the stored snapshot, not the live estimate total.
</verification>

<success_criteria>
- Owner can issue full/deposit/balance invoices from the editor and see them inline.
- Editing the estimate after issue does not change the issued invoice amount.
- Clients can pay open invoices from the share link.
</success_criteria>

<output>
After completion, create `.planning/phases/94-estimate-invoice-decoupling/94-04-SUMMARY.md`
</output>
