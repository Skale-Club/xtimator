---
phase: 94-estimate-invoice-decoupling
plan: 05
type: execute
wave: 4
depends_on: ["94-01", "94-04"]
files_modified:
  - supabase/migrations/20260619000002_phase94_drop_consolidate_index.sql
  - lib/actions/estimate.ts
  - lib/queries/estimate.ts
  - lib/queries/share.ts
  - app/estimate/[token]/page.tsx
  - app/api/estimates/[id]/send/route.ts
  - app/api/estimates/[id]/send-sms/route.ts
  - app/api/estimates/[id]/send-whatsapp/route.ts
  - app/api/estimates/[id]/refine/route.ts
  - app/api/estimates/[id]/pdf/route.ts
  - lib/actions/whatsapp-inbox.ts
  - components/workspace/estimate/estimate-editor.tsx
  - components/workspace/estimate/estimate-floating-actions.tsx
  - components/workspace/estimate/use-estimate-reducer.ts
  - components/workspace/estimate/estimate-header.tsx
  - components/workspace/send/send-tab.tsx
  - components/workspace/project-header.tsx
  - components/workspace/estimate-version-context.tsx
  - tests/unit/share-query.test.ts
  - tests/unit/utils/estimate-template.test.ts
autonomous: true
requirements: [INVOICE-01]

must_haves:
  truths:
    - "saveEstimate no longer blocks writes on a consolidated estimate — there is no workflow_status pre-check"
    - "The share page, send/send-sms/send-whatsapp/refine/pdf routes no longer require workflow_status = 'consolidated'"
    - "consolidateEstimate and createNewDraftVersion server actions are deleted, and the editor no longer references them"
    - "The one_active_draft_per_project unique index is dropped so a project can have an always-editable current estimate"
    - "The WhatsApp inbox lists sendable estimates without requiring workflow_status = 'consolidated'"
    - "The full vitest suite is green (minus the pay-route test deleted in Plan 06) — no consolidate references break compilation"
  artifacts:
    - path: "supabase/migrations/20260619000002_phase94_drop_consolidate_index.sql"
      provides: "DROP INDEX one_active_draft_per_project"
      contains: "DROP INDEX"
  key_links:
    - from: "lib/actions/estimate.ts"
      to: "saveEstimate"
      via: "removal of the workflow_status write-block"
      pattern: "saveEstimate"
    - from: "app/estimate/[token]/page.tsx"
      to: "live estimate render"
      via: "removed consolidated notFound gate"
      pattern: "notFound"
---

<objective>
Retire the estimate "consolidate" lock so estimates are always editable (INVOICE-01, D-01..D-05). Drop the single-draft unique index, remove every server-side `workflow_status === 'consolidated'` gate, delete the `consolidateEstimate` + `createNewDraftVersion` server actions and their UI (consolidate button, draft-lock card, workflow badge, version-switch chrome), fix the WhatsApp inbox sendability filter, and update the test fixtures — all in one coherent pass that keeps the suite green.

Purpose: This is the highest-risk wave. The concept is woven through ~18 files. Sequenced AFTER the additive invoice work so the suite is green going in. KEY DECISION (Claude's discretion per RESEARCH Open Question 2, confirmed by re-grep): KEEP the `workflow_status`/`consolidated_at`/`consolidated_by` columns DORMANT (do NOT drop them) and KEEP `is_current`/`version`. Dropping `workflow_status` would break the WhatsApp send flow (lib/whatsapp/confirm-actions.ts writes it), the MCP read tool (lib/mcp/tools/read.ts selects it), the generate-estimate service (lib/services/generate-estimate.ts inserts it), and lib/estimate/profile-field-map.ts — none of which RESEARCH enumerated. The NOT-NULL DEFAULT 'draft' keeps all existing INSERTs valid. We only remove the *reads/gates* that enforce the lock.
Output: 1 small migration (drop index only) + gate/UI/action removals across the enumerated files + 2 fixture updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-estimate-invoice-decoupling/94-CONTEXT.md
@.planning/phases/94-estimate-invoice-decoupling/94-RESEARCH.md

<interfaces>
<!-- The exact gate/action/UI locations (verified by grep against the live tree on 2026-06-19). -->

GATES TO DELETE (each is a `if (estimate.workflow_status !== 'consolidated') return <error/notFound>`):
- lib/actions/estimate.ts:86-94   — the saveEstimate write-block (selects workflow_status, returns "This estimate is consolidated...")
- app/estimate/[token]/page.tsx:56-59 — `if (data.estimate.workflow_status !== 'consolidated') notFound()`
- app/api/estimates/[id]/send/route.ts:101-107 — 409 unless consolidated
- app/api/estimates/[id]/send-sms/route.ts:63 (select) + :75-76 (gate)
- app/api/estimates/[id]/send-whatsapp/route.ts:53 (select) + :63-68 (gate)
- app/api/estimates/[id]/refine/route.ts:122-127 — 409 when consolidated
- app/api/estimates/[id]/pdf/route.ts:38-44 — 409 unless consolidated

SERVER ACTIONS TO DELETE (lib/actions/estimate.ts):
- :651-704 consolidateEstimate (whole function + its "Action 7" comment block)
- :706-837 createNewDraftVersion (whole function + its "Action 8" comment block)

TYPE FIELDS TO REMOVE (lib/queries/estimate.ts Estimate interface):
- :32-35 workflow_status, consolidated_at, consolidated_by (the 3 fields + their JSDoc) — KEEP is_current, version, payment_status, paid_at, payment_amount_cents

SHARE QUERY (lib/queries/share.ts):
- :13 Omit<…, 'share_token' | 'consolidated_by'> → drop 'consolidated_by' from the Omit
- :179 destructure that strips consolidated_by → remove _consolidatedBy from the destructure
- :211 getShareLinkState select('share_expires_at, workflow_status') → select only 'share_expires_at'

UI (consolidate-specific):
- components/workspace/estimate/estimate-editor.tsx — imports of consolidateEstimate/createNewDraftVersion (:9-10), handleConsolidate (:185-194), handleNewVersion (:211-224), isReadOnly derived from workflow_status (:158), the setSlot workflowStatus field (:271), the EstimateFloatingActions workflowStatus + onConsolidate + onNewVersion props (:309-321)
- components/workspace/estimate/estimate-floating-actions.tsx — ConsolidateAlert (:106-131), the consolidated-branch render (:178-259), workflowStatus/onConsolidate/onNewVersion/isNewVersionPending props (:30-42, :137-149)
- components/workspace/estimate/use-estimate-reducer.ts — workflow_status in EstimateEditorState (:35), in initState (:142, :168)
- components/workspace/estimate/estimate-header.tsx — the "Read-only | consolidated" Badge (:63-65) and the version Select (:42-53) [versioning retired]
- components/workspace/send/send-tab.tsx — isDraft = workflow_status !== 'consolidated' (:56), the draft-lock Card (:85-94), disabled={isDraft} on SendActionsMenu (:80) + SendForm (:114)
- components/workspace/project-header.tsx — the workflow status pill (:80-97) and the version selector dropdown (:53-78) [versioning retired]
- components/workspace/estimate-version-context.tsx — workflowStatus field in VersionSlot (:9)

WHATSAPP INBOX (must change so estimates stay sendable):
- lib/actions/whatsapp-inbox.ts:196 — `.eq('workflow_status', 'consolidated')` filter in listSendableEstimates → REMOVE the filter (every estimate is now sendable; without removal the inbox shows zero estimates). Keep the rest of the query.

LEAVE UNTOUCHED (dormant column references — do NOT break these):
- lib/whatsapp/confirm-actions.ts (writes workflow_status — harmless no-op against the default; leave as-is)
- lib/services/generate-estimate.ts (inserts workflow_status:'draft' — column still exists, leave as-is)
- lib/mcp/tools/read.ts (selects workflow_status — column still exists, leave as-is)
- lib/estimate/profile-field-map.ts (maps workflow_status — column still exists, leave as-is)
- components/workspace/estimate/estimate-document.tsx (a comment only — leave as-is)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the single-draft index + remove all server-side gates and the two consolidate actions</name>
  <read_first>
    - lib/actions/estimate.ts (read the WHOLE file — Tasks delete the :86-94 block and the two whole actions at :651-837; verify exact current line numbers before editing since prior plans may have shifted them)
    - supabase/migrations/20260520000003_estimate_workflow_status.sql (defines `one_active_draft_per_project` — the index name to drop)
    - app/estimate/[token]/page.tsx, app/api/estimates/[id]/send/route.ts, send-sms/route.ts, send-whatsapp/route.ts, refine/route.ts, pdf/route.ts (the 6 gate locations)
    - tests/unit/actions/estimate-save-no-gate.test.ts (the RED contract from Plan 01 — this task makes it green)
  </read_first>
  <action>
    1. Create `supabase/migrations/20260619000002_phase94_drop_consolidate_index.sql`:
    ```sql
    -- Phase 94 (INVOICE-01 / D-03): retire the single-draft lock so estimates are always editable.
    -- The unique index enforced "one active draft per project" — it blocks the always-editable model.
    -- workflow_status / consolidated_* columns are intentionally LEFT DORMANT (still NOT NULL DEFAULT 'draft')
    -- so the WhatsApp send flow, MCP read tool, and generate-estimate service keep compiling.
    DROP INDEX IF EXISTS one_active_draft_per_project;
    ```
    Do NOT drop workflow_status/consolidated_at/consolidated_by. Do NOT run db push.

    2. In `lib/actions/estimate.ts`:
       - DELETE the `saveEstimate` write-block at ~:86-94 (the `// SEED-028 Phase A: block writes...` comment, the `supabase.from('estimates').select('workflow_status')...single()` query, and the `if (workflowRow?.workflow_status === 'consolidated') return {...}` check). saveEstimate proceeds straight to the math recalc.
       - DELETE the entire `consolidateEstimate` function and its "Action 7" comment header (~:650-704).
       - DELETE the entire `createNewDraftVersion` function and its "Action 8" comment header (~:706-837).
       - Remove any now-unused imports caused by those deletions (e.g. if `shareLinkExpiryFromNow` becomes unused — verify; markAsSentAction still uses it, so likely keep it). Leave `createBlankEstimate`'s `workflow_status: 'draft'` insert as-is (column still exists).

    3. Remove each server-side gate (delete the `if (... !== 'consolidated') return/notFound` block; where a select adds `workflow_status` solely for the gate, drop that column from the select):
       - app/estimate/[token]/page.tsx: delete the `if (data.estimate.workflow_status !== 'consolidated') notFound()` block (~:56-59). The live estimate now renders (D-05). Keep the expired-link handling above it.
       - app/api/estimates/[id]/send/route.ts: delete the 409 consolidated gate (~:101-107).
       - app/api/estimates/[id]/send-sms/route.ts: delete the gate (~:75-76) and drop `workflow_status` from the select at ~:63.
       - app/api/estimates/[id]/send-whatsapp/route.ts: delete the gate (~:63-68) and drop `workflow_status` from the select at ~:53.
       - app/api/estimates/[id]/refine/route.ts: delete the consolidated 409 (~:122-127). KEEP the `!estimate.is_current` check (that is versioning, not consolidate — but since versioning UI is retired and is_current stays true for the single estimate, it is harmless; leave it).
       - app/api/estimates/[id]/pdf/route.ts: delete the 409 consolidated gate (~:38-44).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/actions/estimate-save-no-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260619000002_phase94_drop_consolidate_index.sql` exists and contains `DROP INDEX IF EXISTS one_active_draft_per_project`
    - `grep "This estimate is consolidated" lib/actions/estimate.ts` returns NO hits
    - `grep -E "export async function consolidateEstimate|export async function createNewDraftVersion" lib/actions/estimate.ts` returns NO hits
    - `grep -rn "workflow_status !== 'consolidated'" app/` returns NO hits
    - `grep "notFound" app/estimate/[token]/page.tsx` — the consolidated-specific notFound is gone (the not-found-for-missing-data path may remain)
    - The drop migration does NOT contain `DROP COLUMN workflow_status` (column stays dormant)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run tests/unit/actions/estimate-save-no-gate.test.ts` exits 0
  </acceptance_criteria>
  <done>The single-draft index is dropped, all 6 server gates + the save write-block are gone, and both consolidate actions are deleted; the save-no-gate test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove consolidate UI + version-switch chrome; fix WhatsApp inbox sendability</name>
  <read_first>
    - components/workspace/estimate/estimate-editor.tsx (the imports, handleConsolidate, handleNewVersion, isReadOnly, setSlot, and EstimateFloatingActions props)
    - components/workspace/estimate/estimate-floating-actions.tsx (ConsolidateAlert + the consolidated-branch + the props)
    - components/workspace/estimate/use-estimate-reducer.ts (EstimateEditorState.workflow_status + initState)
    - components/workspace/estimate/estimate-header.tsx (the consolidated Badge + version Select)
    - components/workspace/send/send-tab.tsx (isDraft + draft-lock Card + disabled props)
    - components/workspace/project-header.tsx (workflow pill + version dropdown)
    - components/workspace/estimate-version-context.tsx (VersionSlot.workflowStatus)
    - lib/actions/whatsapp-inbox.ts (listSendableEstimates :191-198 — the .eq('workflow_status','consolidated') filter)
  </read_first>
  <action>
    Work top-down so types resolve. The single estimate is always editable (is_current stays true, version stays 1+).

    1. `components/workspace/estimate/use-estimate-reducer.ts`: remove `workflow_status` from `EstimateEditorState` (:35) and from BOTH `initState` assignments (:142 default, :168 from estimate). Keep `is_current` and `version`.

    2. `components/workspace/estimate-version-context.tsx`: remove `workflowStatus: string` from `VersionSlot`.

    3. `components/workspace/estimate/estimate-floating-actions.tsx`: DELETE `ConsolidateAlert` (the whole helper), DELETE the `if (workflowStatus === 'consolidated') { ... }` branch (the whole block), and remove `workflowStatus`, `onConsolidate`, `onNewVersion`, `isNewVersionPending` from both the props interface and the destructure. The component now renders ONLY the draft branch (Save draft + Discard + secondary actions). Remove the now-unused `Lock` import if unused. (The "Generate invoice" CTA lives in the editor via Plan 04's GenerateInvoiceDialog, not here.)

    4. `components/workspace/estimate/estimate-editor.tsx`: remove the `consolidateEstimate, createNewDraftVersion` imports (:9-10); delete `handleConsolidate` and `handleNewVersion`; change `isReadOnly` to `const isReadOnly = !state.is_current` (drop the `|| state.workflow_status === 'consolidated'`); remove `workflowStatus` from the `setSlot({...})` call and from its dependency array; remove `workflowStatus`, `onConsolidate`, `onNewVersion`, `isNewVersionPending` from the `<EstimateFloatingActions />` props. Remove now-unused `isNewVersionPending` state. Keep the GenerateInvoiceDialog/IssuedInvoicesPanel added in Plan 04.

    5. `components/workspace/estimate/estimate-header.tsx`: remove the `isCurrent && isReadOnly` "Read-only | consolidated" Badge (:63-65). Since versioning is retired, also remove the version `<Select>` block (:42-53) and the unused `versions`/`onVersionChange` props if nothing else uses them — verify callers; if the component is still imported with those props, keep the props but render nothing for the version selector. Prefer the smallest change that compiles.

    6. `components/workspace/send/send-tab.tsx`: remove `const isDraft = estimate.workflow_status !== 'consolidated'` (:56); remove the `{isDraft && (<Card>...draft...</Card>)}` block (:85-94); change `disabled={isDraft}` to `disabled={false}` (or drop the prop) on both `<SendActionsMenu>` (:80) and `<SendForm>` (:114). Remove the now-unused `Lock` import.

    7. `components/workspace/project-header.tsx`: remove the "Workflow status" pill block (:80-97) and the version selector dropdown (:53-78). Keep the status label pill (estimate_ready / In progress). The header becomes title + status pill only. Remove now-unused imports (Lock, CircleDot, ChevronDown, Check) if they become unused.

    8. `lib/actions/whatsapp-inbox.ts`: in `listSendableEstimates`, REMOVE the `.eq('workflow_status', 'consolidated')` line (~:196). Every estimate is now sendable, so the filter would (post-removal) return zero rows for new estimates. Keep the rest of the query (company_id, project_id IN, order, limit). Update the JSDoc "consolidated estimates" wording to "estimates" if you wish (optional).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "onConsolidate\|handleConsolidate\|ConsolidateAlert\|onNewVersion\|handleNewVersion\|createNewDraftVersion\|consolidateEstimate" components/ lib/actions/estimate.ts` returns NO hits
    - `grep "workflow_status" components/workspace/estimate/use-estimate-reducer.ts` returns NO hits
    - `grep "workflowStatus" components/workspace/estimate-version-context.tsx` returns NO hits
    - `grep "workflow_status" components/workspace/send/send-tab.tsx` returns NO hits
    - `grep "workflowStatus\|Consolidated · v\|Draft · v" components/workspace/project-header.tsx` returns NO hits
    - `grep "workflow_status" lib/actions/whatsapp-inbox.ts` returns NO hits (filter removed)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All consolidate UI + version-switch chrome is removed, the editor compiles against the slimmed state, and the WhatsApp inbox lists every estimate as sendable.</done>
</task>

<task type="auto">
  <name>Task 3: Update test fixtures that reference dropped type fields</name>
  <read_first>
    - tests/unit/share-query.test.ts (lines 80-86 fixture + :104-110 consolidated_by strip assertion + :152-158 stateRow.workflow_status)
    - tests/unit/utils/estimate-template.test.ts (lines 100-102 the workflow_status/consolidated_at/consolidated_by fixture fields)
    - lib/queries/estimate.ts (the Estimate type AFTER Task 1 removed the 3 fields — fixtures must match)
    - lib/queries/share.ts (getShareLinkState no longer selects workflow_status after Task 1)
  </read_first>
  <action>
    1. `tests/unit/utils/estimate-template.test.ts`: remove the `workflow_status: 'draft'`, `consolidated_at: null`, `consolidated_by: null` lines from the mock estimate (~:100-102) so the fixture matches the slimmed `Estimate` type.

    2. `tests/unit/share-query.test.ts`:
       - Remove `workflow_status: 'consolidated'` and `consolidated_by: 'staff-user-123'` from the estimate fixture (~:80-86).
       - Remove/replace the assertion `expect('consolidated_by' in result!.estimate).toBe(false)` (~:104-110): since `consolidated_by` is no longer in the type, the strip is moot. Keep the `share_token` strip assertion. If the test title says "strips internal fields (share_token, consolidated_by)", rename it to "(share_token)" and drop the consolidated_by expectation.
       - In the `installMock({ stateRow: { ... workflow_status: 'consolidated' } })` calls (~:152-158), remove `workflow_status` from stateRow (getShareLinkState now selects only `share_expires_at`). Keep the `share_expires_at` expiry-state assertions.

    Also remove `lib/queries/estimate.ts` Estimate fields and `lib/queries/share.ts` Omit/destructure/select per the interfaces block IF not already done in Task 1 (Task 1 owns estimate.ts gates + actions; the TYPE field removal in estimate.ts:32-35 and the share.ts :13/:179/:211 edits belong here if Task 1 left them — do them now). Specifically:
       - lib/queries/estimate.ts: delete `workflow_status`, `consolidated_at`, `consolidated_by` from the Estimate interface (and their JSDoc).
       - lib/queries/share.ts: drop `'consolidated_by'` from the `Omit` (:13); remove `_consolidatedBy` from the destructure (:179); change the getShareLinkState select to `'share_expires_at'` only (:211).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/share-query.test.ts tests/unit/utils/estimate-template.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "workflow_status|consolidated_at|consolidated_by" lib/queries/estimate.ts` returns NO hits
    - `grep "consolidated_by" lib/queries/share.ts` returns NO hits
    - `grep "workflow_status" lib/queries/share.ts` returns NO hits (getShareLinkState select cleaned)
    - `grep -E "workflow_status|consolidated_by" tests/unit/share-query.test.ts tests/unit/utils/estimate-template.test.ts` returns NO hits
    - `npx tsc --noEmit` exits 0
    - `npx vitest run tests/unit/share-query.test.ts tests/unit/utils/estimate-template.test.ts` exits 0
  </acceptance_criteria>
  <done>The Estimate type and share query no longer carry consolidate fields; both affected test fixtures match and pass.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite green gate</name>
  <read_first>
    - .planning/phases/94-estimate-invoice-decoupling/94-VALIDATION.md (the full-suite-per-wave-merge sampling rule + the 1516 baseline note)
  </read_first>
  <action>
    Run the full unit suite. The ONLY expected delta vs the pre-phase 1516 baseline is the new invoice tests (added, green) and any test still pending its Plan 06 deletion (`tests/unit/billing/estimate-pay.test.ts` is deleted in Plan 06, not here — if it is still green here that is fine; if Plan 06 ran first it is gone). Fix any compilation or assertion fallout from the consolidate removal that the per-file checks missed — e.g. a test importing the deleted `consolidateEstimate`/`createNewDraftVersion` (grep `tests/` first; RESEARCH found none, re-verify), or a snapshot referencing the removed workflow badge. Do NOT delete `tests/unit/billing/estimate-pay.test.ts` here (Plan 06 owns the pay-route retirement). Do NOT modify the `it.todo` in tests/unit/whatsapp/send-route.test.ts:10 (it is inert and Plan 06/coherence can address it).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite green)
    - `grep -rn "consolidateEstimate\|createNewDraftVersion" tests/` returns NO hits (no test imports the deleted actions)
    - No new failing files vs the documented baseline other than intentional additions
  </acceptance_criteria>
  <done>The full unit suite is green after the consolidate removal.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run` green.
- `grep -rn "workflow_status !== 'consolidated'" app/ lib/ components/` returns no hits.
- The drop migration drops ONLY the index (no column drops).
</verification>

<success_criteria>
- Estimates are always editable: no save write-block, no consolidated gates on share/send/refine/pdf, no consolidate actions or UI, index dropped.
- WhatsApp inbox still lists sendable estimates.
- Full suite green; workflow_status columns left dormant so dependent code keeps compiling.
</success_criteria>

<output>
After completion, create `.planning/phases/94-estimate-invoice-decoupling/94-05-SUMMARY.md`
</output>
