---
phase: 29-frictionless-project-creation-client-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/schemas/project.ts
  - components/projects/step-client-select.tsx
  - components/projects/new-project-wizard.tsx
  - app/(app)/clients/[id]/page.tsx
  - components/workspace/overview-tab.tsx
  - lib/actions/project.ts
autonomous: true
requirements:
  - CLIENTASSOC-01
  - CLIENTASSOC-02
  - CLIENTASSOC-04
must_haves:
  truths:
    - "User can create a project without selecting a client"
    - "User can add a new project from client detail page that is pre-linked to that client"
    - "User can link a client to a project from the project Overview tab"
  artifacts:
    - path: "components/projects/step-client-select.tsx"
      provides: "No client option in wizard client selector"
    - path: "lib/actions/project.ts"
      provides: "createProjectWithClientAction and linkProjectToClient server actions"
    - path: "app/(app)/clients/[id]/page.tsx"
      provides: "New Project button on client detail"
    - path: "components/workspace/overview-tab.tsx"
      provides: "Link Client card when project has no client"
  key_links:
    - from: "step-client-select.tsx"
      to: "projectSchema"
      via: "form.setValue('clientId', undefined)"
      pattern: "setValue.*undefined"
    - from: "clients/[id]/page.tsx"
      to: "createProjectWithClientAction"
      via: "server action call + router.push"
      pattern: "createProjectWithClientAction"
    - from: "overview-tab.tsx"
      to: "linkProjectToClient"
      via: "popover client selector + action call"
      pattern: "linkProjectToClient"
---

<objective>
Remove the mandatory client step from project creation. Users can create a project without selecting a client, add a "New Project" button on client detail page, and link a client from the project Overview tab.
</objective>

<context>
@.planning/phases/29-frictionless-project-creation-client-linking/29-CONTEXT.md
@.planning/phases/27-capture-schema-migration/27-01-SUMMARY.md
@.planning/phases/28-unified-capture-screen/28-01-SUMMARY.md
@lib/schemas/project.ts
@components/projects/step-client-select.tsx
@components/projects/new-project-wizard.tsx
@lib/actions/project.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make client optional in wizard UI (CLIENTASSOC-01)</name>
  <files>lib/schemas/project.ts, components/projects/step-client-select.tsx, components/projects/new-project-wizard.tsx</files>
  <action>
    1. In step-client-select.tsx, add a CommandItem at the top of the client list: "No client (continue without linking)" with value="no-client"
    2. When this item is selected, call form.setValue('clientId', undefined) to clear the selection (undefined is treated as optional by zod)
    3. Update the PopoverTrigger button to show "No client" when selectedClientId is undefined
    4. Change wizard header from "Select a Client" to "Select a client (optional)"
    5. In new-project-wizard.tsx, remove the form.trigger(['clientId']) validation call since client is now optional - the form should submit even without a client selection
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
    - Wizard shows "No client" option at top of client list
    - User can complete wizard without selecting any client
    - User is redirected to capture screen after creating client-less project
  </done>
</task>

<task type="auto">
  <name>Task 2: Add New Project button on client detail page (CLIENTASSOC-02)</name>
  <files>app/(app)/clients/[id]/page.tsx, lib/actions/project.ts</files>
  <action>
    1. In project.ts actions, add createProjectWithClientAction(clientId) that creates a project pre-linked to that client and returns the project
    2. In the client detail page, add a "New Project" button at the top (above the Client info card or in the header next to ClientDetailActions)
    3. The button should call createProjectWithClientAction and then router.push to /projects/{projectId}/capture
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
    - Client detail page shows "New Project" button
    - Clicking it creates a project linked to that client
    - Navigates directly to capture screen without showing client selection step
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Link Client card in project Overview (CLIENTASSOC-04)</name>
  <files>components/workspace/overview-tab.tsx, lib/actions/project.ts</files>
  <action>
    1. In project.ts, add linkProjectToClient(projectId, clientId) server action that updates project.client_id
    2. In overview-tab.tsx, add LinkClientCard component that conditionally renders when project.client is null
    3. The card should show: "This project is not linked to a client" with a "Link Client" button
    4. The button opens a Popover with a simple client search/select (reusing StepClientSelect pattern but without inline create for simplicity)
    5. After selecting a client, call linkProjectToClient and refresh the page to hide the card
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
    - Projects without a linked client show "Link Client" card in Overview tab
    - User can select a client from the card and successfully link them
    - Projects with an existing client do not show the card
  </done>
</task>

</tasks>

<verification>
- Build completes without errors
- All three success criteria from the roadmap are met
- No regressions to existing functionality
</verification>

<success_criteria>
1. A user can complete the new project wizard and reach the capture screen without selecting or creating a client — the client field is optional, not blocking
2. On any client detail page, a "New Project" button creates a new project pre-linked to that client and navigates directly to the capture screen without showing a client selection step
3. A project with no linked client shows a visible "Link client" card in the Overview tab, and the user can link a client from that card
4. A project that already has a linked client does not show the "Link client" card in Overview
</success_criteria>

<output>
After completion, create `.planning/phases/29-frictionless-project-creation-client-linking/29-01-SUMMARY.md`
</output>