---
phase: 165-atomic-save-version-authority
plan: 02
type: execute
wave: 2
depends_on:
  - "165-01"
files_modified:
  - components/workspace/estimate/use-estimate-reducer.ts
  - components/workspace/estimate/estimate-editor.tsx
  - lib/actions/estimate.ts
  - tests/unit/workspace/estimate-reducer-mark-saved.test.ts
  - tests/unit/workspace/estimate-editor-conflict.test.tsx
autonomous: true
requirements:
  - SAVE-03
  - SAVE-04
  - SAVE-05
  - SAVE-07
must_haves:
  truths:
    - "After a successful save, MARK_SAVED remaps every temp- section/item id to the server-assigned uuid from the RPC id_map — a second immediate save re-UPDATEs those rows instead of re-INSERTing them (no churn, no duplicate-row window)."
    - "MARK_SAVED adopts the server-computed totals (subtotal/tax_amount/discount_amount/total/balance_due) returned by saveEstimate, so the editor snaps to the persisted truth immediately after save even for per-category tax_config companies the reducer can't compute locally."
    - "A keystroke that lands DURING an in-flight save keeps the editor dirty (dirty-epoch): MARK_SAVED only clears isDirty when the edit epoch at save-dispatch time still equals the current epoch — no stranded keystroke behind a false-clean state."
    - "The reducer's live flat-tax preview honors per-line taxable (a taxable=false line contributes zero to the flat taxable base), matching 165-01's server flat-path change — so the taxable toggle visibly changes the previewed tax in the flat mode."
    - "On a genuine conflict OR estimate_not_current, autosave PAUSES via a latch (mirroring 164-02's lockedByServer) and the user gets a non-destructive resolution (reload-latest OR keep-mine) — resolving never silently discards edits, and repeated failing saves/toast-stacking cannot occur."
  artifacts:
    - path: "components/workspace/estimate/use-estimate-reducer.ts"
      provides: "MARK_SAVED with {updated_at, idMap, totals, epoch}; recalculate honors taxable in flat path; a monotonic editEpoch"
    - path: "lib/actions/estimate.ts"
      provides: "saveEstimate return extended additively with the full totals breakdown"
  key_links:
    - from: "components/workspace/estimate/estimate-editor.tsx"
      to: "use-estimate-reducer.ts MARK_SAVED"
      via: "dispatch MARK_SAVED with the RPC id_map + totals + the captured epoch"
      pattern: "id_map|idMap"
---

<objective>
Close the client half of audit § B (SAVE-03/04/05 + SAVE-07-client). 165-01 landed the atomic RPC returning `id_map` + `updated_at` (saveEstimate now returns `{ data: { total, updated_at, id_map } }`), a distinct `estimate_conflict`/`estimate_not_current` SQLSTATE, and the server flat-path taxable fix. This plan wires the client to that contract: temp-id remap (kills the churn audit B5), server-total adoption + flat-path taxable in the preview (SAVE-07 client, audit B8), dirty-epoch (kills the false-clean audit B4), and a non-destructive conflict/not-current UX (kills the toast-storm + forced-discard audit B2/B6).

REQUIRED READING: .planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md § B + 165-01-SUMMARY.md. Current client seams (post-164-02/165-01): reducer MARK_SAVED (use-estimate-reducer.ts:108, dispatched at estimate-editor.tsx:323) sets only isDirty:false+updated_at; recalculate (:147-185) flat-tax at :172 IGNORES taxable (comment :151-152 defers to server); editor has lockedByServer latch (:239) + runSave (:287, snapshots state at :290) + autosave effect (:456, depends on whole state). saveEstimate returns id_map at estimate.ts:274 but only `total` for totals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md
@.planning/phases/165-atomic-save-version-authority/165-01-SUMMARY.md
@components/workspace/estimate/use-estimate-reducer.ts
@components/workspace/estimate/estimate-editor.tsx
@lib/actions/estimate.ts

<interfaces>
saveEstimate return (additive extension in lib/actions/estimate.ts — the action already has the full engineResult; NOTE the in-scope locals are camelCase (Info 6): taxAmount :105, discountAmount :104, balanceDue :107, and the existing keys map updated_at: savedUpdatedAt / id_map: result.id_map):
```ts
return { data: { total, updated_at: savedUpdatedAt, id_map: result.id_map, subtotal, tax_amount: taxAmount, discount_amount: discountAmount, balance_due: balanceDue } }
```
(Existing callers reading .data.total/.updated_at/.id_map unaffected.)

MARK_SAVED (reducer) — extend the action + handler. ALL THREE new fields are OPTIONAL (Blocker 1): there is a SECOND dispatch site, handlePresentationSettingsChange (estimate-editor.tsx:353, the 164-02 locked re-baseline), which calls savePresentationSettings returning only `{data:{updated_at}}` — no idMap/totals. Making the fields required would break tsc there. The handler (real location :490-491, NOT :108 which is the type decl — Info 7) must no-op each field when absent:
```ts
| { type: 'MARK_SAVED'; updated_at: string; idMap?: Record<string,string>; totals?: { subtotal:number, tax_amount:number, discount_amount:number, total:number, balance_due:number }; savedEpoch?: number }
```
Handler:
1. Remap: when idMap present+non-empty, walk state.sections; for each section/item whose id is in idMap, replace with idMap[id]; leave real ids untouched. Absent idMap → skip remap (SAVE-03).
2. Adopt totals from the payload when present; absent → keep current totals (the gear/presentation path doesn't change priced content). (SAVE-07)
3. Dirty-epoch (SAVE-04): keep a monotonic `editEpoch` on state. MARK_SAVED clears isDirty when `savedEpoch === state.editEpoch` OR `savedEpoch` is undefined (the presentation re-baseline path preserves today's 164-02 clear-isDirty behavior); otherwise leave isDirty true (an edit landed mid-save). Always adopt updated_at regardless.

recalculate flat-tax (SAVE-07 client): change :172 to accumulate a taxable base excluding taxable===false lines AND prorate the discount exactly like 165-01's landed server flat path (compute-totals.ts:155-175) — subtract the taxable-share of the global discount, NOT the full discount, to avoid a mixed-taxable preview diverging or going negative (Warning 4):
```ts
const taxableBase = sections.reduce((sum, s) => sum + s.items.reduce((t, i) => t + ((i.taxable ?? true) ? i.total : 0), 0), 0)
const proratedDisc = subtotal > 0 ? Math.round(discount_amount * (taxableBase / subtotal) * 100) / 100 : 0
const tax_amount = Math.round((taxableBase - proratedDisc) * state.tax_rate * 100) / 100
```
Keep it flat-only (the reducer has no tax_config — per-category still resolves on save via total adoption). The all-taxable case stays byte-identical (taxableBase===subtotal → proratedDisc===full discount). Update the stale ":151-152 corrected server-side" comment.

runSave (editor): capture `const savedEpoch = stateRef.current.editEpoch` BEFORE `await saveEstimate(...)`; pass it + result.id_map + the totals into MARK_SAVED. Map the new errors:
- 'estimate_conflict' (the existing conflict flag): set a `conflictPending` latch. Autosave-effect participation (Warning 5): add `conflictPending` to the autosave effect's DEP ARRAY (:468, currently [state, isContentReadOnly, runSave]) — not just the guard body — so flipping it re-runs the effect and its cleanup cancels any already-scheduled timer (a guard-body-only check would let one more save fire). Do NOT fold conflictPending into isContentReadOnly (runSave early-returns on isContentReadOnly :288, which would block the "keep mine" retry). Surface exactly ONE non-stacking notice with two choices: "Reload latest" (destructive, existing handleDiscard→INIT) AND "Keep my changes". 
- "Keep my changes" implementation (Blocker 2 — the P0002 branch returns no server updated_at, so re-baselining to it is impossible): add an optional `force` param to runSave that sends `expectedUpdatedAt: null` in the payload (stateToSavePayload/runSave gains the override) → the RPC skips the compare-and-set (migration :109 `p_expected_updated_at IS NULL`) and last-writer-wins on content: the current user's full editor payload is saved (bumping updated_at via trigger; returns fresh updated_at + id_map for MARK_SAVED). The keep-mine handler clears conflictPending, then calls runSave({force:true}). DOCUMENT that this overwrites the other tab's changes — an explicit user choice, non-destructive to THE CURRENT USER's edits (which is exactly SAVE-05/audit-B2's intent: never silently discard the current user's work). Reload-latest remains the "take theirs" exit.
- 'estimate_not_current': terminal — CAN fold into isContentReadOnly like a lock (a superseded version is never editable); banner "This version was superseded; create a new version", no autosave loop.

editEpoch increment (Warning 3 — CRITICAL, missing one re-introduces the false-clean bug): bump editEpoch in EVERY reducer branch that sets isDirty:true — all 15, not the partial list: UPDATE_ITEM, UPDATE_FIELD, UPDATE_SECTION_TITLE (:310), ADD_ITEM, REMOVE_ITEM, ADD_SECTION, REMOVE_SECTION, REORDER_*, UPDATE_DISCOUNT (:463), UPDATE_DEPOSIT (:474), UPDATE_TAX_RATE (:483), UPDATE_PRESENTATION_SETTINGS (:488), APPLY_PRICE_BOOK_ITEM (:576), APPLY_REFINEMENT. CENTRALIZE if feasible (a helper that sets isDirty:true and increments editEpoch together). Do NOT bump in MARK_SAVED/INIT. Add a reducer test asserting each isDirty-setting action increments the epoch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: saveEstimate return + reducer MARK_SAVED (remap + totals + epoch) + flat taxable</name>
  <files>lib/actions/estimate.ts, components/workspace/estimate/use-estimate-reducer.ts, tests/unit/workspace/estimate-reducer-mark-saved.test.ts</files>
  <read_first>
    - lib/actions/estimate.ts (the saveEstimate success return ~:274 + the engineResult totals in scope)
    - components/workspace/estimate/use-estimate-reducer.ts (FULL — state shape :39, MARK_SAVED :108, recalculate :147-185, every content-mutating action for the epoch increment)
  </read_first>
  <action>
1. Extend saveEstimate's success return with subtotal/tax_amount/discount_amount/balance_due (already computed).
2. Add `editEpoch: number` to EstimateEditorState (init 0); increment it in ALL 15 isDirty-setting actions (interface Warning-3 list) — centralize the isDirty:true+epoch++ if feasible. Also reconcile the second dispatcher: handlePresentationSettingsChange (:353) keeps dispatching MARK_SAVED with only {updated_at} (the optional fields make this valid).
3. Rewrite MARK_SAVED handler (:490-491) per interface: optional-field no-ops, remap via idMap, adopt totals, epoch-gated isDirty clear.
4. recalculate flat-tax honors taxable WITH server-mirrored discount proration per interface.
5. Tests: (a) MARK_SAVED remaps a temp- section+item to real ids; (b) adopts server totals; (c) epoch mismatch (edit during save) leaves isDirty true; epoch match clears it; MARK_SAVED with no savedEpoch (presentation path) clears isDirty; (d) recalculate: a taxable=false line reduces previewed tax; a mixed-taxable estimate with a global discount matches the server prorated value (not negative); all-taxable byte-identical; (e) every isDirty-setting action increments editEpoch.
  </action>
  <acceptance_criteria>
    - All 5 reducer test groups GREEN; all-taxable recalculate byte-identical
    - `npx tsc --noEmit -p tsconfig.ci.json` exits 0
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/workspace/estimate-reducer-mark-saved.test.ts</automated></verify>
  <done>The reducer remaps ids, adopts truth, honors the toggle, and can't false-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Editor wiring — epoch capture, id_map/totals dispatch, conflict/not-current latch + non-destructive resolution</name>
  <files>components/workspace/estimate/estimate-editor.tsx, tests/unit/workspace/estimate-editor-conflict.test.tsx</files>
  <read_first>
    - components/workspace/estimate/estimate-editor.tsx (FULL — runSave :287, the lockedByServer latch :239 + its autosave-guard usage, the conflict branch :309, the MARK_SAVED dispatch :323, the autosave effect :456)
  </read_first>
  <action>
1. In runSave: capture savedEpoch before the await; dispatch MARK_SAVED with {updated_at, idMap: result.data.id_map, totals: {...result.data}, savedEpoch}.
2. Add a `conflictPending` latch (mirror lockedByServer). On 'estimate_conflict'/conflict → set it; on 'estimate_not_current' → set a not-current latch/banner. Add both to the autosave effect's early-return guard.
3. Non-destructive conflict resolution: keep the existing "Reload latest" (destructive) AND add "Keep my changes" (re-baseline expectedUpdatedAt to the server's latest updated_at and retry the save once). Ensure a single non-stacking notice.
4. Tests (jsdom/RTL — reuse the existing editor test harness): a conflict result → autosave pauses (no repeated saveEstimate calls on subsequent state changes while conflictPending) + one notice; not_current → banner, no loop; a mid-save edit → the editor stays dirty and re-saves after the in-flight save resolves.
  </action>
  <acceptance_criteria>
    - Conflict pauses autosave (asserted: no re-fire while pending); non-destructive "keep mine" path exists; not_current handled like a lock
    - `npm test` GREEN (or targeted + editor suites green + only the documented load-flake); `npx tsc --noEmit -p tsconfig.ci.json` exits 0
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/workspace/estimate-editor-conflict.test.tsx</automated></verify>
  <done>Conflicts pause and resolve without discarding; the session-poisoning UX is gone client-side too.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/workspace/estimate-reducer-mark-saved.test.ts tests/unit/workspace/estimate-editor-conflict.test.tsx
npx tsc --noEmit -p tsconfig.ci.json
npm test
```
Regression contracts: 165-01's RPC/return/trigger untouched; 164-02's lockedByServer latch + savePresentationSettings + gear carve-out preserved; GUARD-03 (reducer preview stays a PREVIEW — server totals are authoritative and adopted on save); all-taxable preview byte-identical.
</verification>

<success_criteria>
- SAVE-03 (remap/no churn), SAVE-04 (dirty-epoch), SAVE-05 (non-destructive conflict), SAVE-07 (client preview parity + taxable toggle) all TRUE. Phase 165 fully complete (2/2 plans).
</success_criteria>

<output>
Create `.planning/phases/165-atomic-save-version-authority/165-02-SUMMARY.md`: the MARK_SAVED contract, the epoch mechanism, the flat-taxable preview change, the conflict/not-current UX, and test evidence. Mark SAVE-03/04/05/07 complete in REQUIREMENTS.md.
</output>
