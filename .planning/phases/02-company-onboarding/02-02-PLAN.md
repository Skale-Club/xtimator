---
phase: 02-company-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/onboarding/page.tsx
  - components/onboarding/onboarding-wizard.tsx
  - components/onboarding/onboarding-card.tsx
  - components/onboarding/step-indicator.tsx
  - components/onboarding/step-business-info.tsx
  - components/onboarding/step-brand-identity.tsx
  - components/onboarding/step-address-defaults.tsx
  - components/onboarding/industry-selector.tsx
  - components/onboarding/color-picker.tsx
  - components/onboarding/logo-uploader.tsx
autonomous: true
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03, ONBOARD-04, ONBOARD-05, ONBOARD-06, ONBOARD-08]

must_haves:
  truths:
    - "User sees a 3-step wizard at /onboarding with step indicator"
    - "Step 1 collects company name (required), owner, phone, email, website (optional)"
    - "Step 2 shows industry icon card grid with 8+Other options and color swatches"
    - "Step 2 has logo upload circle with preview/change/remove"
    - "Step 3 has address fields and pre-filled defaults (0% tax, Net 30, 1 year, 30 days)"
    - "Skip for now link appears below the card"
    - "Back and Next navigation works across all steps"
  artifacts:
    - path: "components/onboarding/onboarding-wizard.tsx"
      provides: "Main wizard client component with step state and form"
      min_lines: 80
    - path: "components/onboarding/step-brand-identity.tsx"
      provides: "Step 2 with industry selector, color picker, logo uploader"
      min_lines: 40
    - path: "components/onboarding/industry-selector.tsx"
      provides: "Icon card grid for industry selection"
      min_lines: 30
    - path: "components/onboarding/color-picker.tsx"
      provides: "Preset swatches + custom hex"
      min_lines: 30
    - path: "components/onboarding/logo-uploader.tsx"
      provides: "Avatar circle upload component"
      min_lines: 30
  key_links:
    - from: "components/onboarding/onboarding-wizard.tsx"
      to: "lib/schemas/onboarding.ts"
      via: "import onboardingSchema, STEP_FIELDS"
      pattern: "import.*onboardingSchema.*from.*lib/schemas/onboarding"
    - from: "components/onboarding/step-brand-identity.tsx"
      to: "lib/industries.ts"
      via: "import INDUSTRIES"
      pattern: "import.*INDUSTRIES.*from.*lib/industries"
    - from: "components/onboarding/onboarding-wizard.tsx"
      to: "lib/actions/company.ts"
      via: "import createOrUpdateCompany"
      pattern: "import.*createOrUpdateCompany.*from.*lib/actions/company"
---

<objective>
Build the complete onboarding wizard UI at `/onboarding` with all 3 steps, navigation, validation, and visual components. This plan creates all UI components; the server action for persistence is wired in Plan 03.

Purpose: Give new users a polished onboarding experience that captures business identity.
Output: 10 component files and updated page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-onboarding/02-CONTEXT.md
@.planning/phases/02-company-onboarding/02-RESEARCH.md
@.planning/phases/02-company-onboarding/02-UI-SPEC.md
@.planning/phases/02-company-onboarding/02-VALIDATION.md
@.planning/phases/02-company-onboarding/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (will exist by the time this runs) -->
```typescript
// lib/industries.ts
export interface Industry {
  id: string
  label: string
  icon: string  // Lucide icon component name
  projectTypes: string[]
}
export const INDUSTRIES: Industry[]  // 8 entries

// lib/schemas/onboarding.ts
export const onboardingSchema: z.ZodObject<...>
export type OnboardingValues = z.infer<typeof onboardingSchema>
export const STEP_FIELDS: Record<1 | 2 | 3, string[]>
```

<!-- From existing codebase -->
```typescript
// components/auth/auth-card.tsx
export function AuthCard({ children }: { children: React.ReactNode })
// Pattern: centered card with logo + wordmark above, max-w-[400px]

// lib/supabase/client.ts
export function createClient()  // browser-side Supabase client for Storage uploads

// lib/supabase/server.ts
export async function createClient()  // server-side Supabase client

// Auth form pattern (from login page):
// - useForm with zodResolver, mode: "onBlur" for D-14
// - useTransition for isPending
// - Form > FormField > FormItem > FormLabel > FormControl > FormMessage
// - min-h-[44px] on inputs for UX-02
// - Loader2 spinner on submit button
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OnboardingCard, StepIndicator, and page shell</name>
  <files>
    components/onboarding/onboarding-card.tsx,
    components/onboarding/step-indicator.tsx,
    app/onboarding/page.tsx
  </files>
  <read_first>
    - components/auth/auth-card.tsx (visual pattern to adapt -- wider 600px variant per D-02)
    - app/onboarding/page.tsx (current placeholder to replace)
    - .planning/phases/02-company-onboarding/02-UI-SPEC.md (layout contract, step indicator spec, copywriting)
    - .planning/phases/02-company-onboarding/02-CONTEXT.md (D-01, D-02, D-04, D-05)
  </read_first>
  <action>
    **OnboardingCard** (`components/onboarding/onboarding-card.tsx`):
    - Client component wrapping a wider card variant of AuthCard per D-02.
    - Same centered layout: `flex min-h-screen flex-col items-center justify-center bg-muted/40 px-4`.
    - Logo SVG + "Xtimator" wordmark above card (copy the exact SVG and wordmark from `auth-card.tsx`) per D-05.
    - `mb-12` (48px gap per UI spec 2xl spacing) between wordmark block and card.
    - Card: `w-full max-w-[600px] rounded-xl shadow-sm` (wider than auth's 400px).
    - Props: `children: React.ReactNode`.
    - Below the Card, render a `skipAction` prop slot for the "Skip for now" link (16px margin-top per UI spec).

    **StepIndicator** (`components/onboarding/step-indicator.tsx`):
    - Client component. Props: `currentStep: number` (1-3), `onStepClick: (step: number) => void`.
    - Renders 3 numbered circles connected by horizontal lines.
    - Active step: `bg-primary text-primary-foreground` filled circle with step number.
    - Completed step (step < currentStep): `bg-primary text-primary-foreground` with Lucide `Check` icon (16px).
    - Future step: `border border-border text-muted-foreground` outline with step number.
    - Lines between circles: `h-px bg-border flex-1`. Completed line segments use `bg-primary`.
    - Each circle is a button with `aria-label="Step {N} of 3: {title}"` where titles are "Business Information", "Brand Identity", "Address & Defaults".
    - Active step has `aria-current="step"`.
    - Circles are 32px diameter. Text is 14px.
    - Clicking a step circle calls `onStepClick(stepNumber)` per D-04 (clickable stepper dots).

    **Page** (`app/onboarding/page.tsx`):
    - Keep as server component with auth gate (existing `getClaims()` pattern).
    - If no claims, `redirect('/auth/login')`.
    - Render `<OnboardingWizard />` (client component, built in Task 2).
    - Import OnboardingWizard dynamically or directly.
  </action>
  <verify>
    <automated>tsc --noEmit && bun build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/onboarding/onboarding-card.tsx contains `max-w-[600px]`
    - components/onboarding/onboarding-card.tsx contains `Xtimator`
    - components/onboarding/onboarding-card.tsx contains `bg-muted/40`
    - components/onboarding/step-indicator.tsx contains `aria-current="step"`
    - components/onboarding/step-indicator.tsx contains `aria-label`
    - components/onboarding/step-indicator.tsx contains `onStepClick`
    - app/onboarding/page.tsx contains `getClaims()`
    - app/onboarding/page.tsx contains `OnboardingWizard`
  </acceptance_criteria>
  <done>OnboardingCard renders wider centered card with logo/wordmark. StepIndicator shows 3 clickable step circles with a11y. Page gates auth and renders wizard shell.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding wizard + all 3 step components</name>
  <files>
    components/onboarding/onboarding-wizard.tsx,
    components/onboarding/step-business-info.tsx,
    components/onboarding/step-brand-identity.tsx,
    components/onboarding/step-address-defaults.tsx,
    components/onboarding/industry-selector.tsx,
    components/onboarding/color-picker.tsx,
    components/onboarding/logo-uploader.tsx
  </files>
  <read_first>
    - lib/schemas/onboarding.ts (onboardingSchema, OnboardingValues, STEP_FIELDS -- from Plan 01)
    - lib/industries.ts (INDUSTRIES, Industry -- from Plan 01)
    - app/(auth)/login/page.tsx (react-hook-form + zod pattern, FormField structure, useTransition)
    - components/auth/auth-card.tsx (visual reference)
    - lib/supabase/client.ts (browser-side Supabase client for logo upload)
    - .planning/phases/02-company-onboarding/02-UI-SPEC.md (full interaction contract, copywriting, color palette, accessibility)
    - .planning/phases/02-company-onboarding/02-CONTEXT.md (D-01 through D-14)
    - .planning/phases/02-company-onboarding/02-RESEARCH.md (Pattern 1 multi-step form, Pattern 2 Storage upload, Pitfalls 1-6)
  </read_first>
  <action>
    **OnboardingWizard** (`components/onboarding/onboarding-wizard.tsx`):
    - `'use client'` component. This is the main orchestrator.
    - Single `useForm<OnboardingValues>` with `resolver: zodResolver(onboardingSchema)`, `mode: "onBlur"` per D-14, and `defaultValues` matching schema defaults.
    - State: `currentStep` (1-3), `logoFile: File | null`, `logoPreview: string | null`.
    - `useTransition` for `isPending` state on submit.
    - `handleNext()`: validate current step fields via `form.trigger(STEP_FIELDS[currentStep])`. If valid, `setCurrentStep(prev => prev + 1)`.
    - `handleBack()`: `setCurrentStep(prev => prev - 1)`. No validation.
    - `handleStepClick(step)`: direct jump, no validation (per D-04). `setCurrentStep(step)`.
    - `handleSkip()`: call `createOrUpdateCompany` server action with `{ companyName: form.getValues('companyName') || 'My Company' }` per D-03 and Pitfall 5 (NOT NULL constraint). Redirect handled by server action.
    - `handleComplete()`: validate Step 3, then submit. Upload logo if `logoFile` exists (call `uploadLogo` helper). Then call `createOrUpdateCompany` with all form values + `logoUrl`. Show toast on success/error per UI spec copywriting.
    - Renders: `OnboardingCard` wrapping `StepIndicator` + step content + navigation footer.
    - Step content cross-fades: wrap in a div with `transition-opacity duration-150 ease-in-out`.
    - Navigation footer: flex justify-between. Step 1: only "Next" button. Steps 2-3: "Back" (ghost variant) + "Next"/"Complete Setup". All buttons `min-h-[44px]` per UX-02.
    - Below card: "Skip for now" as a button with `variant="link"` and `text-muted-foreground`, centered.
    - On step change, focus the step title heading (accessibility per UI spec).
    - NOTE: The `createOrUpdateCompany` server action import will error until Plan 03 creates it. Create a temporary stub at `lib/actions/company.ts` with: `'use server'; export async function createOrUpdateCompany(data: any) { redirect('/dashboard') }` so the build does not fail. Plan 03 replaces this stub.

    **StepBusinessInfo** (`components/onboarding/step-business-info.tsx`):
    - Props: `form: UseFormReturn<OnboardingValues>`.
    - Title: "Business Information" (h2, 20px semibold per UI spec heading role).
    - Subtitle: "Tell us about your business. Only the company name is required." (text-muted-foreground, 14px).
    - Fields using shadcn Form/FormField/FormItem/FormLabel/FormControl/FormMessage:
      - Company Name (required): `<Input placeholder="e.g. Smith Painting LLC" className="min-h-[44px]" />`
      - Owner Name: `<Input placeholder="e.g. John Smith" className="min-h-[44px]" />`
      - Phone: `<Input type="tel" placeholder="(555) 123-4567" className="min-h-[44px]" />`
      - Email: `<Input type="email" placeholder="john@smithpainting.com" className="min-h-[44px]" />`
      - Website: `<Input type="url" placeholder="https://smithpainting.com" className="min-h-[44px]" />`
    - Layout: `space-y-4` (16px gap per UI spec md spacing).

    **StepBrandIdentity** (`components/onboarding/step-brand-identity.tsx`):
    - Props: `form: UseFormReturn<OnboardingValues>`, `logoFile: File | null`, `logoPreview: string | null`, `onLogoChange: (file: File | null, preview: string | null) => void`.
    - Title: "Brand Identity". Subtitle: "Choose your industry and customize your brand."
    - Three sections separated by `<Separator />`:
      1. "Select Your Industry" label + `<IndustrySelector />` -- passes `value={form.watch('industry')}`, `customValue={form.watch('customIndustry')}`, `onChange` and `onCustomChange` that call `form.setValue(...)`.
      2. "Brand Color" label + `<ColorPicker />` -- passes `value={form.watch('brandPrimaryColor')}`, `onChange` that calls `form.setValue('brandPrimaryColor', color)`.
      3. "Upload Logo" label + `<LogoUploader />` -- passes `preview={logoPreview}`, `companyInitial={form.watch('companyName')?.charAt(0)?.toUpperCase() || '?'}`, `onFileSelect` and `onRemove`.

    **IndustrySelector** (`components/onboarding/industry-selector.tsx`):
    - Props: `value: string`, `customValue: string`, `onChange: (id: string) => void`, `onCustomChange: (value: string) => void`.
    - Import `INDUSTRIES` from `@/lib/industries`.
    - Render a grid: `grid grid-cols-2 sm:grid-cols-3 gap-3` (responsive).
    - For each industry in INDUSTRIES: a card/button with Lucide icon (24px) + label. Use dynamic icon import: import all needed icons from lucide-react (`SprayCan, Paintbrush, TreePine, Zap, Wrench, Hammer, Home, Fan, MoreHorizontal`) and create an `ICON_MAP: Record<string, LucideIcon>` mapping icon name to component.
    - Card styling: `p-3 rounded-lg border cursor-pointer text-center flex flex-col items-center gap-2 transition-colors`.
    - Default: `border-border bg-card`. Selected: `border-primary border-2 bg-primary/5`.
    - 9th "Other" card: icon `MoreHorizontal`, label "Other".
    - When "Other" selected, reveal below the grid: `<Input placeholder="Enter your industry" className="min-h-[44px] mt-3" autoFocus />` per D-07.
    - Clicking a non-Other card clears customValue.
    - Container: `role="radiogroup"`. Each card: `role="radio"`, `aria-checked={selected}`.

    **ColorPicker** (`components/onboarding/color-picker.tsx`):
    - Props: `value: string`, `onChange: (color: string) => void`.
    - 10 preset swatches per UI spec palette:
      ```
      const PRESET_COLORS = [
        { name: 'Teal', hex: '#0D9488' },
        { name: 'Blue', hex: '#2563EB' },
        { name: 'Indigo', hex: '#4F46E5' },
        { name: 'Purple', hex: '#7C3AED' },
        { name: 'Rose', hex: '#E11D48' },
        { name: 'Orange', hex: '#EA580C' },
        { name: 'Amber', hex: '#D97706' },
        { name: 'Emerald', hex: '#059669' },
        { name: 'Slate', hex: '#475569' },
        { name: 'Zinc', hex: '#3F3F46' },
      ]
      ```
    - Swatches: `w-8 h-8 rounded-full cursor-pointer` with `style={{ backgroundColor: hex }}`. Selected: `ring-2 ring-primary ring-offset-2`.
    - 11th "Custom" swatch: gradient/rainbow circle with `MoreHorizontal` icon or a "+" symbol. On click, toggle a hex input below (or use Popover per UI spec).
    - Custom hex input: `<Input />` with "#" prefix, validate `/^#[0-9A-Fa-f]{6}$/`. On valid blur, call `onChange`. On invalid: toast "Invalid color code. Please enter a 6-digit hex value like #0D9488."
    - Container: `role="radiogroup"`. Each swatch: `role="radio"`, `aria-checked`, `aria-label="{color name}"`.
    - Layout: `flex flex-wrap gap-2`.

    **LogoUploader** (`components/onboarding/logo-uploader.tsx`):
    - Props: `preview: string | null`, `companyInitial: string`, `onFileSelect: (file: File, preview: string) => void`, `onRemove: () => void`.
    - 80px circle (Avatar component, size="lg" or custom `w-20 h-20`).
    - Empty state: `bg-muted` circle with Lucide `Camera` icon (24px, `text-muted-foreground`). Below: "Upload Logo" text.
    - Filled state: image in circle via `AvatarImage`. Below: "Change" and "Remove" text links (`text-sm text-muted-foreground hover:text-foreground cursor-pointer`).
    - Hidden `<input type="file" accept=".png,.jpg,.jpeg" />` triggered by clicking the circle.
    - `role="button"` and `aria-label="Upload company logo"` on the circle.
    - On file select: validate size <= 2MB (else toast "Logo must be under 2MB. Please choose a smaller file."), validate type PNG/JPG (else toast "Unsupported image format. Please upload a PNG or JPG file."). If valid, create object URL preview, call `onFileSelect(file, objectUrl)`.
    - On remove: call `onRemove()`. Revoke object URL if applicable.
    - NOTE: Actual Supabase upload happens in the wizard on submit, not here. This component only handles local preview.

    **StepAddressDefaults** (`components/onboarding/step-address-defaults.tsx`):
    - Props: `form: UseFormReturn<OnboardingValues>`.
    - Title: "Address & Defaults". Subtitle: "Add your business address and set default estimate terms."
    - Two sections separated by `<Separator />`:
      1. Address section: address (full width), city + state + zip (3-column grid on desktop, stacked on mobile), license number, insurance info.
      2. Defaults section: tax rate (number input, suffix "%" label), payment terms (text input, pre-filled "Net 30"), warranty terms (text input, pre-filled "1 year"), validity period (number input, suffix "days").
    - All fields use FormField/FormItem/FormLabel/FormControl/FormMessage pattern.
    - All inputs: `className="min-h-[44px]"` per UX-02.
    - Pre-filled from form defaultValues (0, "Net 30", "1 year", 30) per D-13.
  </action>
  <verify>
    <automated>tsc --noEmit && bun build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/onboarding/onboarding-wizard.tsx contains `'use client'`
    - components/onboarding/onboarding-wizard.tsx contains `useForm<OnboardingValues>`
    - components/onboarding/onboarding-wizard.tsx contains `mode: "onBlur"`
    - components/onboarding/onboarding-wizard.tsx contains `STEP_FIELDS`
    - components/onboarding/onboarding-wizard.tsx contains `Skip for now`
    - components/onboarding/industry-selector.tsx contains `role="radiogroup"`
    - components/onboarding/industry-selector.tsx contains `INDUSTRIES`
    - components/onboarding/color-picker.tsx contains `#0D9488`
    - components/onboarding/color-picker.tsx contains `role="radiogroup"`
    - components/onboarding/logo-uploader.tsx contains `accept=`
    - components/onboarding/logo-uploader.tsx contains `2MB` or `2 * 1024 * 1024`
    - components/onboarding/step-business-info.tsx contains `Business Information`
    - components/onboarding/step-brand-identity.tsx contains `Brand Identity`
    - components/onboarding/step-address-defaults.tsx contains `Address & Defaults`
    - components/onboarding/step-address-defaults.tsx contains `Net 30`
    - lib/actions/company.ts exists (stub or full implementation)
    - `bun build` completes without errors
  </acceptance_criteria>
  <done>All 7 onboarding components render correctly. Wizard navigates between 3 steps with validation. Industry selector shows 8+Other cards. Color picker shows 10 presets + custom. Logo uploader handles file selection/preview/remove with size/type validation. Build succeeds.</done>
</task>

</tasks>

<verification>
- `bun build` succeeds with no errors
- `bun test` passes (no regressions from Plan 01 tests)
- Visual: `/onboarding` page renders centered card with step indicator, form fields, navigation
</verification>

<success_criteria>
- 3-step wizard renders at /onboarding with all form fields per UI spec
- Step navigation works (Next validates, Back preserves data, dots are clickable)
- Industry selector shows 8 icon cards + Other with custom input reveal
- Color picker shows 10 preset swatches + custom hex input
- Logo uploader validates file size/type and shows preview
- Skip for now link appears below card
- Build succeeds with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-onboarding/02-02-SUMMARY.md`
</output>
