---
phase: "32-text-input-route"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "app/projects/[id]/describe/page.tsx"
  - "app/projects/[id]/describe/describe-client.tsx"
  - "components/projects/text-describe.tsx"
autonomous: true
requirements:
  - "TEXT-01"
  - "TEXT-02"
  - "TEXT-03"
  - "TEXT-04"
  - "TEXT-05"

must_haves:
  truths:
    - "User can type a job description in a large textarea"
    - "User can save description and generate estimate in one action"
    - "Estimate generation runs the same pipeline as audio recording"
    - "Route is mobile-responsive with large tap targets"
  artifacts:
    - path: "app/projects/[id]/describe/page.tsx"
      provides: "Server-side route shell for /describe"
      min_lines: 20
    - path: "app/projects/[id]/describe/describe-client.tsx"
      provides: "Client wrapper rendering TextDescribe"
      min_lines: 10
    - path: "components/projects/text-describe.tsx"
      provides: "Text input UI: textarea + generate button"
      min_lines: 80
  key_links:
    - from: "components/projects/text-describe.tsx"
      to: "lib/actions/recording.ts"
      via: "createTextRecording(projectId, text)"
      pattern: "createTextRecording"
    - from: "components/projects/text-describe.tsx"
      to: "/api/generate-estimate"
      via: "fetch POST after recording creation"
      pattern: "fetch.*api/generate-estimate"
    - from: "app/projects/[id]/describe/page.tsx"
      to: "components/projects/text-describe.tsx"
      via: "DescribeClient wrapper"
      pattern: "DescribeClient"
---

<objective>
Create the `/projects/[id]/describe` route with a large textarea for text-based project input. Save typed text as a recording transcript and trigger the same estimate generation pipeline as audio.

Purpose: Gives users a dedicated, focused text-entry surface — no audio recorder, no photos, just typing. Triggers the same pipeline as audio via `createTextRecording` + `/api/generate-estimate`.
</objective>

<context>
@.planning/ROADMAP.md (Phase 32 goal + success criteria)
@.planning/phases/31-wizard-modality-selection/31-01-SUMMARY.md (wizard routes to /describe when inputMode=text)
@.planning/phases/28-unified-capture-screen/28-01-SUMMARY.md (createTextRecording + hasAnyInput pattern)
@app/(capture)/projects/[id]/capture/page.tsx (route shell pattern)
@app/(capture)/projects/[id]/capture/capture-client.tsx (client wrapper pattern)
@components/capture/capture-recorder.tsx (text-only path in handleGenerate, createTextRecording import)

## Key Interfaces

From lib/actions/recording.ts:
```typescript
// Already exists from Phase 28
export async function createTextRecording(
  projectId: string,
  transcript: string,
): Promise<{ data?: Recording; error?: string }>
```

From app/api/generate-estimate/route.ts:
```typescript
// POST /api/generate-estimate
// Body: { projectId: string }
// Returns: { estimateId: string; clientSuggestion?: string }
// Guard: requires at least one recording or photo per project
```

From components/workspace/estimate/client-suggestion-toast.ts:
```typescript
export function storeClientSuggestion(projectId: string, suggestion: string | null): void
```

## Success Criteria (from ROADMAP.md)

1. Route `/projects/[id]/describe` displays a textarea with placeholder showing example job descriptions
2. Textarea is large enough for at least 10 lines with comfortable line height
3. "Save & Generate Estimate" creates a recording with typed text as `transcript` (storage_path null, duration_seconds null)
4. Estimate generation pipeline runs identically to audio route
5. Route is mobile-responsive with touch-friendly tap targets (minimum 44px)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create describe route page.tsx</name>
  <files>app/projects/[id]/describe/page.tsx, app/projects/[id]/describe/describe-client.tsx</files>
  <action>
    Create the route shell following the same pattern as app/(capture)/projects/[id]/capture/page.tsx:
    
    - **app/projects/[id]/describe/page.tsx**: Async server component that fetches project + company, checks auth, renders `<DescribeClient>`
    - **app/projects/[id]/describe/describe-client.tsx**: 'use client' wrapper that renders `<TextDescribe>` with project + companyId props
    
    Route group: `(capture)` so it uses the same full-screen layout (no sidebar/topnav) as /capture.
    
    Pattern: identical to capture/page.tsx + capture-client.tsx — just swap the component names.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "projects/\[id\]/describe"</automated>
  </verify>
  <done>Route page.tsx builds, describe-client.tsx exists, both in (capture) route group</done>
</task>

<task type="auto">
  <name>Task 2: Build TextDescribe component</name>
  <files>components/projects/text-describe.tsx</files>
  <action>
    Build a focused text-input component:

    **Layout (mobile-first, full-screen within (capture) shell):**
    - Header bar: project name (truncated) + back link to project workspace
    - Main area: large textarea (min 10 lines = ~200px min-height), full width, comfortable padding
    - Footer: "Save & Generate Estimate" button (w-full, size="lg", min 44px tap target)
    
    **Textarea specs (per TEXT-01, TEXT-02):**
    - `min-h-[200px]` for 10+ lines
    - `rows={12}` for comfortable default
    - Line-height: `leading-relaxed` (1.625)
    - Placeholder: "Describe the job here — for example: 'Pressure wash the driveway and two-car garage, remove mold from the north-facing fascia boards, and seal all concrete surfaces. Approximately 1,800 sq ft total.'"
    - `resize-none` to prevent breaking layout
    - Focus ring via `focus:ring-2 focus:ring-ring`
    
    **Button behavior (per TEXT-03, TEXT-04):**
    - On click: set loading state, call `createTextRecording(projectId, text)`, then fetch `/api/generate-estimate` (same pattern as capture-recorder handleGenerate text-only path)
    - On success: call `storeClientSuggestion`, router.push to `/projects/${projectId}?tab=estimate&estimate=${data.estimateId}`
    - On error: show toast error, clear loading state
    - Disabled when textarea is empty (trimmed length = 0)
    
    **Mobile-first (per TEXT-05):**
    - All interactive elements: minimum 44px touch target
    - Button: h-12 (48px), w-full
    - Textarea: comfortable on mobile, full-width
    - Padding: px-4 py-3 (mobile-safe)
    
    **Dependencies to import:**
    - `createTextRecording` from '@/lib/actions/recording'
    - `storeClientSuggestion` from '@/components/workspace/estimate/client-suggestion-toast'
    - `createClient` from '@/lib/supabase/client'
    - `useRouter` from 'next/navigation'
    - `Button` from '@/components/ui/button'
    - `toast` from 'sonner'
    - `ArrowLeft` from 'lucide-react' (back link)
    - `Loader2` from 'lucide-react' (loading state)
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "text-describe|Cannot find"</automated>
  </verify>
  <done>
    TextDescribe component: large textarea (12 rows, 200px min-height), placeholder example, Save & Generate button disabled at empty, enabled with text, mobile tap targets ≥44px
  </done>
</task>

<task type="auto">
  <name>Task 3: Build and verify</name>
  <files>package.json (implicit)</files>
  <action>
    Run `npm run build` to verify:
    - TypeScript compiles without errors
    - Route /projects/[id]/describe is included in build output
    - No missing imports or type errors
    
    Check for TEXT-01 through TEXT-05 coverage:
    - TEXT-01: Route exists ✓
    - TEXT-02: Placeholder on textarea ✓
    - TEXT-03: createTextRecording called with text (storage_path null) ✓
    - TEXT-04: /api/generate-estimate called after recording ✓
    - TEXT-05: Mobile tap targets (h-12 buttons, comfortable textarea) ✓
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build succeeds, all 5 TEXT requirements covered by implementation</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with no errors
- [ ] Route `/projects/[id]/describe` appears in build output
- [ ] TypeScript types compile without errors
- [ ] Textarea has `rows={12}` (10+ lines), `min-h-[200px]`, placeholder with example job description
- [ ] Button is `w-full h-12` (44px+ tap target), disabled when textarea empty
- [ ] `createTextRecording` called with text (no storage_path, no duration)
- [ ] `/api/generate-estimate` POST called after recording creation
- [ ] On success: router.push to estimate editor
- [ ] On error: toast notification shown
</verification>

<success_criteria>
User visits `/projects/[id]/describe`, sees a large textarea, types a job description, taps "Save & Generate Estimate", and lands in the estimate editor with a generated estimate — identical outcome to audio recording but without any audio or photos.
</success_criteria>

<output>
After completion, create `.planning/phases/32-text-input-route/32-01-SUMMARY.md`
</output>
