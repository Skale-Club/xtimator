---
phase: 133-editor-ui
plan: 02
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - components/workspace/estimate/use-estimate-reducer.ts
  - components/workspace/estimate/estimate-document.tsx
  - components/workspace/estimate/item-card-mobile.tsx
  - components/workspace/estimate/estimate-editor.tsx
autonomous: true
requirements: [PUI-01]
must_haves:
  truths:
    - "Each line item in the editor (desktop table row + mobile card) exposes a per-line discount input and a taxable toggle"
    - "Editing a line's discount or taxable updates the live client-side preview total via the reducer (optimistic) and is included in the save payload"
    - "An unedited line (taxable on, discount 0) renders and saves with no-op defaults — byte-identical to today"
    - "The controls work on mobile (iOS Safari + Android Chrome): 44px tap targets, numeric inputMode for the discount field, no layout break at 360px width"
  artifacts:
    - path: "components/workspace/estimate/use-estimate-reducer.ts"
      provides: "EditorItem carries taxable/tax_category/discount/cost/markup_pct; UPDATE_ITEM handles the new fields; recalculate is a client preview only"
      contains: "taxable"
    - path: "components/workspace/estimate/estimate-document.tsx"
      provides: "Desktop SortableDocumentItemRow gains discount + taxable controls; DocumentItem type widened; per-line controls dispatch UPDATE_ITEM"
      contains: "taxable"
    - path: "components/workspace/estimate/item-card-mobile.tsx"
      provides: "Mobile card gains discount input + taxable toggle with mobile-safe inputs"
      contains: "taxable"
  key_links:
    - from: "estimate-document.tsx SortableDocumentItemRow"
      to: "reducer UPDATE_ITEM"
      via: "dispatch({ type: 'UPDATE_ITEM', field: 'discount'|'taxable' })"
      pattern: "field: 'discount'|'taxable'"
    - from: "estimate-editor.tsx stateToSavePayload"
      to: "saveEstimate (Plan 01)"
      via: "item payload includes discount + taxable"
      pattern: "taxable"
---

<objective>
Add the PER-LINE pricing controls to the estimate editor: a line `discount` input and a `taxable` toggle on both the desktop row (`SortableDocumentItemRow` inside estimate-document.tsx) and the mobile card (`ItemCardMobile`). Thread the new fields through the reducer state, the live-preview recompute, and the save payload so they reach the Wave-1 server action.

IMPORTANT discovery (from codebase read): the editor's REAL desktop line-item row is `SortableDocumentItemRow` defined INSIDE `components/workspace/estimate/estimate-document.tsx` (NOT the standalone `item-row.tsx`, which is used only by the legacy `section-card.tsx` path that the editor does not render). The mobile row IS `item-card-mobile.tsx`. The planning hint named `item-row.tsx`; the truthful, wired target is `SortableDocumentItemRow` + `ItemCardMobile`. Edit those.

GUARD-03 reminder: the reducer's `recalculate` is an OPTIMISTIC CLIENT PREVIEW only. The persisted + displayed-after-save totals always come from the Wave-1 server recompute (`saveEstimate` → `computeEstimateTotals`). This plan does NOT need the preview to be byte-perfect with the server (the server is authoritative on reload) — but it should be close so the editor feels live.

Purpose: the user can mark a line non-taxable and apply a line discount inline.
Output: per-line discount + taxable controls on desktop and mobile, wired end-to-end into the save payload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/133-editor-ui/133-01-PLAN.md

<read_first>
- components/workspace/estimate/use-estimate-reducer.ts — EditorItem (L11-21); UPDATE_ITEM action type (L64) + reducer case (L226-246); recalculate (L103-129) — the client preview to extend; initState item mapping (L188-198); ADD_ITEM default item (L257-267) + ADD_SECTION (L302-313); APPLY_REFINEMENT (L391-401) + APPLY_PRICE_BOOK_ITEM (L423-433).
- components/workspace/estimate/estimate-document.tsx — SortableDocumentItemRow (L422-~560): drag handle, description PriceBookCombobox, qty input, unit Select, unit_price MoneyInput, then the line total + remove. ADD the discount input + taxable toggle here. DocumentItem type (L266-276) — widen it. The desktop table header (L717-727) — add column header(s). The stateToDocumentData converter lives in estimate-editor.tsx (see below). INLINE_INPUT_CLS is the shared input class. DOC_LABELS (L60-170) + DocLabels interface (L171-206) — add `lineDiscount` + `taxable` labels for en/pt/es (do the i18n inline here, same pattern; Plan 03 adds the summary-panel labels).
- components/workspace/estimate/item-card-mobile.tsx — the mobile card; the grid at L75-115 (Qty/Unit/Unit Price) + the footer row L117-135. Add a discount field + taxable toggle. onUpdate signature L20-21 must widen to accept 'discount'|'taxable'. Match the existing `min-h-[44px]` tap-target + numeric input idiom.
- components/workspace/estimate/estimate-editor.tsx — stateToDocumentData (L34-68) + stateToSavePayload (L74-103): the item mapping in BOTH must carry discount + taxable (+ tax_category/cost/markup_pct passthrough so nothing is lost on save). The save payload feeds Plan-01's saveEstimate.
- components/workspace/estimate/item-row.tsx — DO NOT rely on this being the editor row; reference it only as a shadcn/RHF idiom example. It is the legacy section-card path; editing it is OPTIONAL (only if you also want the legacy path consistent — not required for PUI-01).
- components/ui/money-input.tsx + components/ui/switch.tsx (or checkbox) — the controls to reuse for the discount amount + taxable toggle.
</read_first>

<interfaces>
Reducer EditorItem (current → widened):
```typescript
export interface EditorItem {
  id: string; description: string; quantity: number; unit: string | null
  unit_price: number; total: number; sort_order: number
  price_source: 'price_book' | 'ai_estimate' | 'researched' | null
  isManuallyEdited?: boolean
  // NEW (all optional, no-op defaults):
  taxable?: boolean            // default true
  tax_category?: 'labor' | 'materials' | 'other' | null
  discount?: number            // line discount amount, default 0
  cost?: number | null
  markup_pct?: number | null
}
```
UPDATE_ITEM field union widens from `'description' | 'quantity' | 'unit' | 'unit_price'` to also include `'discount' | 'taxable'` (tax_category/cost/markup_pct are passthrough-only this phase — no dedicated control required, but carry them through converters so refine/price-book/save don't drop them).

Client preview recompute (recalculate): line total becomes `round2(qty × unit_price) − (discount ?? 0)`, matching the engine's `lineNet`. Subtotal/discount/tax stay as today's preview math (the server is authoritative). Do NOT attempt per-category tax in the preview — non-taxable lines may still show in the flat preview tax; that's acceptable because the server recompute corrects it on save/reload (document this in a comment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen reducer — EditorItem fields + UPDATE_ITEM + recalculate preview + converters carry-through</name>
  <files>components/workspace/estimate/use-estimate-reducer.ts, components/workspace/estimate/estimate-editor.tsx</files>
  <action>
    1. use-estimate-reducer.ts: add the optional fields to `EditorItem` (taxable, tax_category, discount, cost, markup_pct per the interface above). Widen the `UPDATE_ITEM` action `field` union to include `'discount' | 'taxable'`. In the UPDATE_ITEM reducer case, when `field === 'discount'` coerce to a number (default 0); when `field === 'taxable'` store the boolean. Keep `isManuallyEdited = true` ONLY for `unit_price` (do not flip it for discount/taxable — they are not price-source overrides).
    2. recalculate (L103-129): change the per-item line total to `Math.round(i.quantity * i.unit_price * 100) / 100 - (i.discount ?? 0)`. Leave subtotal/discount_amount/tax_amount/total preview formulas as-is. Add a comment: "Client preview only — server (saveEstimate → computeEstimateTotals) is authoritative; non-taxable lines are corrected server-side on save/reload."
    3. initState item mapping (L188-198): read the new fields off the server row with defaults: `taxable: (i as { taxable?: boolean }).taxable ?? true`, `tax_category: (i as ...).tax_category ?? null`, `discount: (i as ...).discount ?? 0`, `cost: (i as ...).cost ?? null`, `markup_pct: (i as ...).markup_pct ?? null`. (The EstimateItem query type is widened in Task 3 of this plan if needed, or cast like the existing estimate_date pattern.)
    4. ADD_ITEM (L257-267) + ADD_SECTION default item (L302-313): set `taxable: true, discount: 0` on the new blank item (other new fields omitted/null).
    5. APPLY_PRICE_BOOK_ITEM (L423-433) + APPLY_REFINEMENT item map (L391-401): preserve existing taxable/discount on the item (do not reset them) — spread the existing item first.
    6. estimate-editor.tsx: in BOTH stateToDocumentData (L55-65 item map) and stateToSavePayload (L91-100 item map), add `discount: i.discount ?? 0`, `taxable: i.taxable ?? true`, `tax_category: i.tax_category ?? null`, `cost: i.cost ?? null`, `markup_pct: i.markup_pct ?? null` so nothing is dropped on save.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "use-estimate-reducer.ts|estimate-editor.tsx" || echo "no reducer/editor type errors"</automated>
  </verify>
  <done>EditorItem carries the new optional fields; UPDATE_ITEM handles 'discount'/'taxable'; recalculate subtracts line discount; converters carry all new fields; no tsc errors in the two files.</done>
</task>

<task type="auto">
  <name>Task 2: Desktop row — add discount input + taxable toggle to SortableDocumentItemRow + DocumentItem type + header + DOC_LABELS</name>
  <files>components/workspace/estimate/estimate-document.tsx</files>
  <action>
    1. Widen `DocumentItem` (L266-276) with the optional fields: `taxable?: boolean; tax_category?: 'labor'|'materials'|'other'|null; discount?: number; cost?: number | null; markup_pct?: number | null`.
    2. DocLabels interface (L171-206) + DOC_LABELS (en/pt/es, L60-170): add `lineDiscount` and `taxable` labels. en: `lineDiscount: 'Disc.'`, `taxable: 'Tax'`. pt: `lineDiscount: 'Desc.'`, `taxable: 'Imposto'`. es: `lineDiscount: 'Desc.'`, `taxable: 'Impuesto'`. (Short labels — the row is dense.)
    3. In `SortableDocumentItemRow` (L422-~560), after the unit_price `<td>` and before the line-total/remove cells, add TWO new `<td>`s:
       - Discount: a MoneyInput (or number input with currency) bound to `item.discount ?? 0`, dispatching `UPDATE_ITEM` field `'discount'`. Use the same `INLINE_INPUT_CLS`, `w-20`, right-aligned.
       - Taxable: a small checkbox/Switch bound to `item.taxable ?? true`, dispatching `UPDATE_ITEM` field `'taxable'`. Provide an accessible label (aria-label `L.taxable`).
    4. Update BOTH desktop table headers — the editable header at L717-727 — adding `<th>{L.lineDiscount}</th>` and `<th>{L.taxable}</th>` in the matching column positions. (The read-only/share header at L748-755 does NOT need the inputs — leave the share view as-is for this phase; deposit/discount display in the share doc is Phase 134.)
    5. Default discipline: `item.taxable ?? true` and `item.discount ?? 0` everywhere so an item with no new fields renders exactly as today (checkbox checked, discount blank/0). Do not show a non-zero discount visual when 0.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate && npx tsc --noEmit 2>&1 | grep "estimate-document.tsx" || echo "no estimate-document type errors"</automated>
  </verify>
  <done>Desktop editable row renders a discount input + taxable toggle wired to UPDATE_ITEM; DocumentItem widened; headers added in en/pt/es; tsc clean on the file; existing estimate tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile card — add discount input + taxable toggle to ItemCardMobile (mobile-safe)</name>
  <files>components/workspace/estimate/item-card-mobile.tsx</files>
  <action>
    1. Widen the `onUpdate` prop type (L20-21) field union to include `'discount' | 'taxable'`.
    2. Add a second compact row below the existing Qty/Unit/Unit-Price grid (L75-115): a 2-column grid with a Discount field (left) and a Taxable toggle (right).
       - Discount: MoneyInput bound to `item.discount ?? 0` → `onUpdate('discount', value)`. Set `inputMode="decimal"` on the underlying input if MoneyInput supports it; label `Discount` via the same `text-[10px] uppercase` label pattern used for Qty/Unit.
       - Taxable: a Switch (or checkbox) bound to `item.taxable ?? true` → `onUpdate('taxable', checked)`, with a visible `Taxable` label and `min-h-[44px]` tap target (mirror the existing remove-button 44px rule).
    3. MOBILE CONSTRAINTS (CLAUDE.md — iOS Safari + Android Chrome): tap targets ≥ 44px; numeric/decimal inputMode on the discount field; the new row must NOT break layout at 360px width (use the same `grid gap-2` idiom; avoid fixed widths that overflow). Keep the card single-column stacked.
    4. Default discipline: `item.taxable ?? true` (toggle on), `item.discount ?? 0` (blank/0) so an unedited item looks identical to today.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "item-card-mobile.tsx" || echo "no item-card-mobile type errors"</automated>
  </verify>
  <done>ItemCardMobile renders a discount input + taxable toggle below the qty/unit/price grid, mobile-safe (44px targets, decimal inputMode, no 360px overflow), wired to onUpdate; tsc clean on the file.</done>
</task>

</tasks>

<verification>
- EditorItem + DocumentItem + onUpdate carry the new fields.
- Desktop SortableDocumentItemRow + mobile ItemCardMobile both expose discount + taxable controls.
- Reducer UPDATE_ITEM handles 'discount'/'taxable'; recalculate subtracts line discount (preview).
- Converters (stateToDocumentData + stateToSavePayload) carry all new fields into the save payload.
- No-op defaults keep unedited items byte-identical.
- Mobile: 44px tap targets, decimal inputMode, no 360px layout break.
</verification>

<success_criteria>
- Per-line discount + taxable controls visible and functional on desktop and mobile.
- Save payload includes per-item discount + taxable (+ passthrough fields) reaching saveEstimate.
- `npx vitest run tests/unit/estimate` green; tsc clean on all four touched files.
</success_criteria>

<output>
After completion, create `.planning/phases/133-editor-ui/133-02-SUMMARY.md`
</output>
