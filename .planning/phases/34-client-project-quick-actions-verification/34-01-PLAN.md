---
phase: 34-client-project-quick-actions-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "New Project button exists on client detail page and creates a project pre-linked to that client"
    - "Project wizard allows creating a project without selecting a client (client field is optional)"
    - "Link Client card appears in project Overview when project has no linked client"
    - "AI client extraction toast appears after estimate generation when client is detected"
  artifacts: []
  key_links: []
---

<objective>
Verify all CLIENTASSOC features from v1.5 work correctly:
1. New Project button on client detail page (CLIENTASSOC-02)
2. Client optional in wizard (CLIENTASSOC-01)
3. Link Client card in Project Overview (CLIENTASSOC-04)
4. AI client extraction toast (CLIENTASSOC-03)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-frictionless-project-creation-client-linking/29-01-SUMMARY.md
@.planning/phases/30-ai-client-extraction/30-01-SUMMARY.md

Phase 29 implemented:
- components/clients/client-new-project-button.tsx
- components/workspace/link-client-card.tsx
- components/projects/step-client-select.tsx
- components/projects/new-project-wizard.tsx
- lib/actions/project.ts (createProjectWithClientAction, linkProjectToClient)

Phase 30 implemented:
- components/workspace/estimate/client-suggestion-toast.ts
- lib/ai/types.ts (suggested_client_name in EstimateOutput)
- lib/ai/providers/anthropic.ts
- lib/ai/providers/gemini.ts
- app/api/generate-estimate/route.ts (clientSuggestion in response)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: New Project button on client detail page</name>
  <what-built>CLIENTASSOC-02: New Project button on client detail page</what-built>
  <how-to-verify>
    1. Navigate to a client detail page: /clients/{client_id}
    2. Verify a "New Project" button exists in the page header (next to Edit/Delete actions)
    3. Click the "New Project" button
    4. Verify it navigates to the project capture screen with the client pre-linked
    5. Check that the created project has the correct client_id in the database
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Client optional in wizard</name>
  <what-built>CLIENTASSOC-01: Client optional in project creation wizard</what-built>
  <how-to-verify>
    1. Start a new project from dashboard (/projects/new or "New Project" button)
    2. In the client selection step, verify "No client" or similar option exists at the top
    3. Select "No client" and complete the wizard without selecting a client
    4. Verify the project is created successfully without a client_id
    5. Navigate to the project Overview and verify no client is linked
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Link Client card in Project Overview</name>
  <what-built>CLIENTASSOC-04: Link Client card in project Overview tab</what-built>
  <how-to-verify>
    1. Navigate to a project that has no client linked (create one if needed without selecting client)
    2. Go to the project workspace Overview tab
    3. Verify a "Link Client" card is visible (shown only when project.client is null)
    4. Click the "Link Client" action
    5. Verify a client selector popover appears
    6. Select a client and verify the project is now linked to that client
    7. Refresh the page and verify the link persists
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: AI client extraction toast</name>
  <what-built>CLIENTASSOC-03: AI client suggestion toast after estimate generation</what-built>
  <how-to-verify>
    1. Create a project and record/generate an estimate with audio/text that mentions a client name
      (or use existing project with known client in transcript/description)
    2. Generate the estimate (click "Generate Estimate")
    3. After estimate is generated, check for a toast notification showing the detected client name
    4. Verify the toast shows "Link to [Client Name]" or similar action
    5. Click the accept action and verify the project is linked to that client
    6. Verify the toast does NOT automatically link without user action
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Each task has a human verification checkpoint. All 4 CLIENTASSOC features must be verified.
</verification>

<success_criteria>
All 4 checkpoints approved - all CLIENTASSOC features work as expected:
- [ ] CLIENTASSOC-02: New Project button creates client-linked project
- [ ] CLIENTASSOC-01: Wizard allows client-optional project creation
- [ ] CLIENTASSOC-04: Link Client card appears and works in Overview
- [ ] CLIENTASSOC-03: AI client suggestion toast appears and links correctly
</success_criteria>

<output>
After completion, create `.planning/phases/34-client-project-quick-actions-verification/34-01-SUMMARY.md`
</output>