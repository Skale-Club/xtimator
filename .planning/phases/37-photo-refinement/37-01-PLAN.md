---
phase: 37-photo-refinement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/workspace/estimate/refine-estimate-panel.tsx
  - app/api/estimates/[id]/refine/photo/route.ts
autonomous: true
requirements:
  - REFINE-06
  - REFINE-07
must_haves:
  truths:
    - "User can click photo button in refine panel to upload photos"
    - "Uploaded photos are analyzed via Claude Vision"
    - "Photo analysis results are used as context in refinement pipeline"
    - "Photo refinement creates new estimate version (v+1)"
  artifacts:
    - path: "components/workspace/estimate/refine-estimate-panel.tsx"
      provides: "Photo upload button + collapsible section in refinement panel"
      min_lines: 30
    - path: "app/api/estimates/[id]/refine/photo/route.ts"
      provides: "Photo refinement API: Claude Vision analysis + refine pipeline"
      exports: ["POST"]
  key_links:
    - from: "refine-estimate-panel.tsx"
      to: "/api/estimates/[id]/refine/photo"
      via: "fetch with FormData"
      pattern: "POST.*FormData"
    - from: "app/api/estimates/[id]/refine/photo/route.ts"
      to: "lib/ai"
      via: "refineEstimate call"
      pattern: "refineEstimate.*EstimateOutput"
---

<objective>
Add photo refinement to estimate editor — users can upload photos that are analyzed via Claude Vision and used as context to refine the estimate.

Purpose: Complete the iterative refinement feature (text + voice + photo) per v1.8 roadmap. Photo refinement enables users to show, not tell — upload a photo of a problem area and have Claude Vision analyze it to generate refinement instructions.
Output: Photo upload section in RefineEstimatePanel, new /api/estimates/[id]/refine/photo endpoint
</objective>

<context>
@.planning/phases/35-text-refinement/35-01-SUMMARY.md — Text refinement implementation
@.planning/phases/36-voice-refinement/36-01-SUMMARY.md — Voice refinement implementation

From existing codebase:
- components/workspace/estimate/refine-estimate-panel.tsx — Refinement panel with text + voice input
- components/workspace/estimate/voice-refine-recorder.tsx — Voice recorder pattern to follow
- app/api/analyze-photos/route.ts — Claude Vision integration to reuse
- lib/ai/types.ts — RefineEstimateInput type
- lib/ai/providers/anthropic.ts — refineEstimate implementation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add photo upload section to RefineEstimatePanel</name>
  <files>components/workspace/estimate/refine-estimate-panel.tsx</files>
  <action>
    Add a collapsible photo upload section in RefineEstimatePanel below the voice recorder section:

    1. Import CameraIcon from lucide-react
    2. Add photoExpanded state (boolean, default false)
    3. Add isUploadingPhotos state (boolean, default false)
    4. Add handlePhotoRefine function that:
       - Takes photos (File array) as input
       - Creates FormData with 'photos' field
       - POSTs to /api/estimates/{estimateId}/refine/photo
       - On success: shows toast "Estimate refined — v{version}", collapses section, triggers router.refresh()
       - On error: shows toast error message
    5. Add photo toggle button below voice recorder (mirrors voice pattern):
       - Shows CameraIcon + "Or upload photos to refine"
       - Click expands photo upload section
    6. Photo upload section (when photoExpanded=true):
       - Simple file input with accept="image/*" multiple
       - Upload button triggers handlePhotoRefine with selected files
       - Shows loading spinner while uploading
       - Collapse chevron to close
    7. Disable photo upload while isLoading (text/voice refinement in progress)

    Pattern: Follow VoiceRefineRecorder collapsible section pattern.
  </action>
  <verify>
    <automated>grep -n "CameraIcon\|photoExpanded\|handlePhotoRefine" components/workspace/estimate/refine-estimate-panel.tsx</automated>
  </verify>
  <done>Photo button visible, expands upload section, uploads photos and triggers refinement</done>
</task>

<task type="auto">
  <name>Task 2: Create photo refinement API route</name>
  <files>app/api/estimates/[id]/refine/photo/route.ts</files>
  <action>
    Create POST /api/estimates/[id]/refine/photo endpoint:

    1. Auth check: getClaims() + company lookup (same as voice refine)
    2. Estimate validation: getEstimateById, verify is_current=true and ownership
    3. Photo validation:
       - Extract FormData, require 'photos' field
       - Validate at least 1 photo, max 5 photos
       - Validate file types (image/*)
    4. Upload photos to Supabase Storage:
       - Path: {companyId}/refine-photos/{estimateId}-{timestamp}-{index}.jpg
       - Use service client for storage operations
    5. Claude Vision analysis:
       - For each photo: download from storage, call Claude Vision
       - Reuse contractor-focused prompt from /api/analyze-photos/route.ts
       - Collect all descriptions into photoDescriptions array
    6. Build instruction from photo descriptions:
       - Create instruction: "Based on the uploaded photos: {descriptions join with '; '}"
    7. Call refineEstimate (same pipeline as text/voice):
       - Fetch existing estimate via getEstimateById
       - Fetch price book items
       - Call aiProvider.refineEstimate with existingEstimate + generated instruction + priceBookItems
    8. Version management (same as text refine):
       - Mark old version is_current=false
       - Insert new version with version+1, is_current=true
    9. Return { success, newVersion, photoDescriptions }
    10. Clean up storage (non-blocking) after analysis

    Dependencies: lib/ai/index.ts for getAIProvider, lib/ai/types.ts for EstimateOutput
  </action>
  <verify>
    <automated>ls -la app/api/estimates/[id]/refine/photo/route.ts</automated>
  </verify>
  <done>Route accepts photo FormData, analyzes via Claude Vision, creates new estimate version</done>
</task>

</tasks>

<verification>
[ ] RefineEstimatePanel has photo button that expands upload section
[ ] User can select multiple photos and trigger refinement
[ ] Photo refinement API analyzes photos via Claude Vision
[ ] Photo descriptions used as context in refinement pipeline
[ ] New estimate version created with incremented version number
[ ] Success toast shows new version number
[ ] Old version preserved (is_current=false)
</verification>

<success_criteria>
Measurable completion:
- Photo upload button visible in refinement panel
- Clicking uploads photos to /api/estimates/[id]/refine/photo
- API runs Claude Vision on photos, returns descriptions
- Refinement produces new estimate version (v+1)
- User sees success toast with version number
</success_criteria>

<output>
After completion, create `.planning/phases/37-photo-refinement/37-01-SUMMARY.md`
</output>