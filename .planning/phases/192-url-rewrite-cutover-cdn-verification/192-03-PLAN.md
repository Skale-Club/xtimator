---
phase: 192-url-rewrite-cutover-cdn-verification
plan: 03
type: execute
wave: 3
depends_on: ["192-02"]
files_modified:
  - .planning/phases/192-url-rewrite-cutover-cdn-verification/192-03-SUMMARY.md
autonomous: true
requirements: [URL-02]

must_haves:
  truths:
    - "The reversible-record table exists in the production database before any row is rewritten"
    - "No production row is rewritten while writers would still persist absolute URLs"
    - "Every production row that held an absolute Supabase asset URL now holds the same-origin form"
    - "Re-running the rewrite reports zero changes and exits zero"
    - "A full revert restores every rewritten value byte-for-byte, proven by execution, not by argument"
  artifacts:
    - path: ".planning/phases/192-url-rewrite-cutover-cdn-verification/192-03-SUMMARY.md"
      provides: "The batch id, the census before/after, and the rollback rehearsal result"
      contains: "batch_id"
  key_links:
    - from: "scripts/rewrite-asset-urls.ts --apply"
      to: "public.storage_url_rewrites (production)"
      via: "one audit row per rewritten row, inserted before the update"
      pattern: "storage_url_rewrites"
---

<objective>
Execute the URL-02 rewrite against **PRODUCTION DATA**. Apply the audit-table
migration by hand, run the census, dry-run, apply, prove idempotency by
re-running, then prove the reversible record actually reverses by reverting the
whole batch and re-applying it.

Purpose: this is the milestone's only step that mutates production business
rows. Everything the previous two plans built exists so that this step is
mechanical and undoable.

Output: production rows rewritten, one audit batch recorded, and a summary that
carries the batch id and the before/after census.

**This plan writes to production. Do not improvise. If a gate fails, STOP and
report — do not work around it.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/192-url-rewrite-cutover-cdn-verification/192-01-SUMMARY.md
@.planning/phases/192-url-rewrite-cutover-cdn-verification/192-02-SUMMARY.md
@docs/STORAGE-MIGRATION.md
@scripts/rewrite-asset-urls.ts

<interfaces>
<!-- Operating facts. Verified 2026-08-06. -->

- `.env.local` in this repo points at the **production** Supabase project
  (`NEXT_PUBLIC_SUPABASE_URL=https://prmqgcrnpuvpzruyzvuv.supabase.co`). Running
  the script locally with no extra configuration therefore hits production
  directly. That is intended here — and it is why `--confirm-project` exists.
- **Migrations in this repo are applied to production BY HAND. The deploy
  pipeline (GitHub Actions → GHCR → Coolify) never runs them.** The Supabase MCP
  server `f2b95485` is mapped to this production project and is the normal way
  to apply one; the Supabase SQL editor is the fallback.
- Commands (from Plan 02):
  ```
  npm run rewrite:asset-urls -- --preflight --dump <path>
  npm run rewrite:asset-urls                                   # dry run
  npm run rewrite:asset-urls -- --apply --confirm-project <ref>
  npm run rewrite:asset-urls -- --revert-latest --confirm-project <ref>
  ```
- Scratchpad for all dumps and logs (NEVER the repo — these contain tenant data):
  `C:\Users\Vanildo\AppData\Local\Temp\claude\C--Users-Vanildo-Dev-xtimator\<session>\scratchpad`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the audit-table migration to production, then preflight</name>
  <files>(no repo files — production schema + scratchpad dump)</files>
  <action>
1. Read `192-01-SUMMARY.md` for the exact migration filename that was created
   (the date-counter may have been bumped).
2. Apply that migration to the production Supabase project via the Supabase MCP
   server mapped to `prmqgcrnpuvpzruyzvuv` (`apply_migration`). If MCP is
   unavailable, paste the file into the Supabase SQL editor. Either way this is
   a **hand application** — the pipeline will not do it for you, and the code
   about to run depends on the table existing.
3. Verify by querying the table: it must exist, be empty, and have RLS enabled
   with no policies.
4. Run preflight with a dump into the scratchpad:
   ```bash
   npm run rewrite:asset-urls -- --preflight --dump "<scratchpad>/192-pre-state.json"
   ```
5. Read the output carefully and record it verbatim for the summary:
   - the resolved project ref (must be the production ref),
   - the audit-table BLOCKER check → must pass,
   - the Phase-190 `getPublicUrl` BLOCKER check → must pass,
   - the Phase-191 R2 WARN → record whichever it says; a WARN does **not** block
     (the proxy's Supabase read-through fallback covers an empty R2), but it must
     be reported honestly in the summary and it matters for Plan 05's payoff,
   - the per-target census counts. Reconcile them against the measured
     production scope in `CONTEXT.md`: **11 occurrences across 4 columns** -
     `companies.logo_url` 1, `platform_branding.logo_url` 1,
     `platform_branding.og_image_url` 1, `platform_branding.landing_content` 8.
     The other four targets measured **zero** and must report zero;
     - the EXCLUDED read-only line for `company_price_book.image_url`: **293
     non-null rows, 0 Supabase URLs** (all external `images.pexels.com` stock
     photos). It must appear as excluded-with-numbers, never as a target. If the
     Supabase-URL count there is anything other than 0, STOP - something wrote a
     Supabase URL into that column and the exclusion needs re-deciding.

   A census that disagrees with those numbers means the world changed since the
   measurement. Reconcile before applying; do not just proceed with a different
   count.

**STOP conditions.** If either BLOCKER fails, do not proceed and do not
"temporarily" bypass it. Report which one failed and why — a failing Phase-190
check means writers would re-persist absolute URLs and the rewrite would drift
straight back.
  </action>
  <verify>
    <automated>npm run rewrite:asset-urls -- --preflight > "$TMPDIR/p3-preflight.log" 2>&1; echo "EXIT=$?"; grep -iE "blocker|census|project" "$TMPDIR/p3-preflight.log"</automated>
  </verify>
  <done>Migration applied to production and verified present with RLS on and zero policies; preflight exits 0 with both BLOCKER checks passing; the pre-state dump exists in the scratchpad (not in the repo); census counts recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Dry-run, apply, and prove idempotency</name>
  <files>(no repo files — production data)</files>
  <action>
1. **Dry run.** `npm run rewrite:asset-urls` with no flags. Save the full output
   to the scratchpad. Read every planned change. Sanity-check by hand:
   - every `newValue` starts with `/storage/` followed by one of the five
     allowlisted buckets;
   - no `blog_posts` row pointing at a third-party host appears in the plan;
   - the `platform_branding.landing_content` entry appears exactly once, as a
     whole-document RECORD covering 8 value-level leaf rewrites (not one audit
     entry per nested field, and not a blind document substitution);
   - the totals add up to **11 occurrences across 4 targets**, matching
     `CONTEXT.md`. A different total is a stop-and-reconcile, not a proceed.
   If anything looks wrong, STOP. A wrong dry run is free; a wrong apply is not.

2. **Apply.**
   ```bash
   npm run rewrite:asset-urls -- --apply --confirm-project prmqgcrnpuvpzruyzvuv
   ```
   Capture the `batch_id` printed at the START of the run — record it
   immediately, in the summary, before doing anything else. It is the handle for
   the entire rollback.

3. **Idempotency proof.** Re-run the dry run. It must report **0 changes** and
   exit 0. Then re-run `--apply --confirm-project <ref>` a second time: it must
   also report 0 changes, insert **no** new audit rows, and exit 0. Verify by
   counting rows in `storage_url_rewrites` before and after — the count must be
   identical.

4. **Verify every value individually.** At 11 occurrences there is no excuse
   for sampling - "N rows updated" is not acceptable evidence at this size.
   Enumerate all 11 rewritten values (3 scalar columns + the 8 leaves inside
   `landing_content`), confirm each is the relative `/storage/{bucket}/{key}`
   form, and confirm the audit table holds `old_value` = the original absolute
   URL for each. Paste the full 11-line list into the summary; Plan 04 fetches
   every one of them through the proxy.
  </action>
  <verify>
    <automated>npm run rewrite:asset-urls > "$TMPDIR/p3-rerun.log" 2>&1; echo "EXIT=$?"; grep -iE "0 change|total|no changes" "$TMPDIR/p3-rerun.log"</automated>
  </verify>
  <done>Batch id recorded; every target row that held an absolute Supabase asset URL now holds `/storage/{bucket}/{key}`; a second dry run reports 0 changes and a second `--apply` adds no audit rows; audit row count equals the number of rewritten rows.</done>
</task>

<task type="auto">
  <name>Task 3: Rollback rehearsal — revert the whole batch, verify, re-apply</name>
  <files>(no repo files — production data)</files>
  <action>
The reversible record is only a claim until it is executed. Prove it now, while
the change is fresh and the pre-state dump is still on disk.

1. **Revert.**
   ```bash
   npm run rewrite:asset-urls -- --revert-latest --confirm-project prmqgcrnpuvpzruyzvuv
   ```
   It must exit 0 with **zero drifted rows** (nothing has changed the rows in the
   minutes since the apply). Any drift here means something else is writing to
   these rows concurrently — STOP and investigate before proceeding.

2. **Verify the restore is exact.** Re-read every target row and diff against
   `<scratchpad>/192-pre-state.json` from Task 1. The comparison must be exact,
   including all 8 leaves of the `landing_content` document. The whole
   pre-state is a single small artifact at this size - compare it in full, not
   by sample.
   Not "looks the same" — a byte/deep-equal comparison. Write a throwaway
   comparison snippet in the scratchpad if needed; do not add it to the repo.

3. **Re-apply.**
   ```bash
   npm run rewrite:asset-urls -- --apply --confirm-project prmqgcrnpuvpzruyzvuv
   ```
   This produces a NEW `batch_id`. Record it — this is the batch that Plans 04
   and 05 verify against, and the one an operator would revert if the phase is
   abandoned. Confirm the earlier batch's rows are stamped `reverted_at`.

4. Re-run the dry run once more: 0 changes.

Record in the summary: both batch ids, which one is live, the drift count from
the rehearsal (must be 0), and the result of the exact-restore comparison.
  </action>
  <verify>
    <automated>npm run rewrite:asset-urls > "$TMPDIR/p3-final.log" 2>&1; echo "EXIT=$?"; grep -icE "change" "$TMPDIR/p3-final.log"; grep -iE "0 change|no changes|total" "$TMPDIR/p3-final.log"</automated>
  </verify>
  <done>Revert executed with 0 drifted rows; restored values verified deep-equal to the pre-state dump; re-apply completed under a new batch id; final dry run reports 0 changes; both batch ids and the rehearsal result recorded in the summary.</done>
</task>

</tasks>

<verification>
- `public.storage_url_rewrites` in production holds one row per rewritten row for the live batch, with `reverted_at is null`.
- The superseded rehearsal batch is fully stamped `reverted_at`.
- No repo file changed by this plan except the summary — `git status` shows only `.planning/` additions.
- The `--dump` pre-state JSON lives in the scratchpad and is NOT in the repo (`git status --porcelain | grep -i pre-state` returns nothing).
- No secret, service key, or project credential appears in the summary.
</verification>

<success_criteria>
- All 11 measured occurrences across the 4 non-empty targets now hold the same-origin form, verified individually rather than by row count; the four zero-count targets report zero, and `company_price_book.image_url` (293 rows / 0 Supabase URLs) was never touched.
- The rewrite is proven idempotent by execution (second run: 0 changes, 0 new audit rows).
- The rollback is proven real by execution (full revert, exact-restore comparison, re-apply).
- The live batch id is recorded where an operator can find it.
</success_criteria>

<output>
After completion, create `.planning/phases/192-url-rewrite-cutover-cdn-verification/192-03-SUMMARY.md`.
It MUST contain: the migration filename applied, the live `batch_id`, the
superseded rehearsal `batch_id`, the before/after census per target, the drift
count from the rehearsal, the Phase-191 R2 WARN/PASS state, and the exact
one-command rollback line an operator would run.
</output>
