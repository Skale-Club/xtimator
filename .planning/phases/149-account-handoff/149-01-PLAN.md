---
phase: 149
slug: account-handoff
title: Account Handoff
milestone: v4.14 Admin Sales Mode
status: complete
committed: 645f8ba2
---

# Phase 149 — Account Handoff

## Goal

Super-admin can hand off a demo account to the street-sale prospect by sending them
an owner-level invite from the admin panel. No role selection UI — always 'owner'.

## Tasks

| ID | Task | Status |
|----|------|--------|
| T-01 | Extend InviteEmailContext.role to accept 'owner' | complete |
| T-02 | Add 'company.handoff' to AuditAction union | complete |
| T-03 | handoffDemoCompany server action (lib/actions/admin-handoff.ts) | complete |
| T-04 | HandoffButton client component (app/admin/companies/handoff-button.tsx) | complete |
| T-05 | Wire HandoffButton into admin/companies/page.tsx Demo Accounts section | complete |

## Files Changed

- `lib/email/invite-emails.ts` — role union extended with 'owner'
- `lib/admin/audit-log.ts` — 'company.handoff' AuditAction added
- `lib/actions/admin-handoff.ts` — new; handoffDemoCompany server action
- `app/admin/companies/handoff-button.tsx` — new; dialog + send invite button
- `app/admin/companies/page.tsx` — Demo Accounts section with HandoffButton per row

## Key Decisions

- Reused existing company_invites table and sendInviteEmail — no new email infra
- 'owner' role bypasses the inviteMember zod enum (admin.add guard) by using
  a separate requireAdmin()-gated action
- Handoff logs audit action so there is a trail of who handed off which account
- Demo accounts section only appears when at least one company has demo_estimate_quota IS NOT NULL
