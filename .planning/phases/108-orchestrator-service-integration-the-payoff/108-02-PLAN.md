---
phase: 108-orchestrator-service-integration-the-payoff
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/estimate/quality/vagueness.ts
  - tests/unit/estimate/vagueness-flagged-unpriced.test.ts
autonomous: true
requirements: [RFALL-02]
must_haves:
  truths:
    - "A fully-empty estimate (no items, or every item $0 → total 0) is STILL vague (block → needs-details)"
    - "A partially-priced estimate (total > 0) with one flagged unpriced item is NOT vague (allow → proceeds)"
    - "The existing WhatsApp/needs-details vagueness behavior is unchanged for total<=0 / no-items"
  artifacts:
    - path: "lib/estimate/quality/vagueness.ts"
      provides: "isVagueEstimate refined to distinguish empty vs flagged-unpriced; total>0 never blocked"
      contains: "isVagueEstimate"
  key_links:
    - from: "lib/estimate/quality/vagueness.ts isVagueEstimate"
      to: "the assess node + eval metrics + auto-refine"
      via: "shared channel-neutral gate"
      pattern: "isVagueEstimate"
---

<objective>
Refine the vagueness gate so the never-$0 fallback ladder (Plan 108-03/04) is never blocked
when at least one line is priced. Today `isVagueEstimate` returns vague when `total <= 0 OR no
items`. The refinement: keep blocking a genuinely valueless estimate (no items, or total 0) but
ALLOW a partially-priced estimate (total > 0) that carries a flagged unpriced line among priced
ones. Keep the change minimal; do NOT regress the WhatsApp/needs-details paths.

Purpose: RFALL-02 — the Ellen rule, refined: empty estimate blocks (needs-details), a single
flagged unpriced item among priced ones does not block.
Output: refined `isVagueEstimate` + focused unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-orchestrator-service-integration-the-payoff/108-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Current gate (extracted from codebase) — the ONLY thing this plan changes. -->

From lib/estimate/quality/vagueness.ts:
```typescript
export type VagueCheckEstimate = {
  total: number | null
  sections: Array<{ items?: Array<unknown> | null }> | null
}
export function isVagueEstimate(e: VagueCheckEstimate | null): boolean {
  if (e == null) return true
  const hasTotal = e.total != null && e.total > 0
  const hasItems = (e.sections ?? []).some((s) => (s?.items?.length ?? 0) > 0)
  return !hasTotal || !hasItems
}
```

Consumers (DO NOT regress):
- lib/estimate/graph/nodes/assess.ts — re-reads persisted estimate (total + nested item ids) and calls isVagueEstimate.
- tests/eval/metrics.ts scorePersistedEstimate — reuses isVagueEstimate verbatim.
- The WhatsApp ask-details re-export (Phase 94) — total<=0 / no-items must still read vague.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refine isVagueEstimate to allow total>0 with a flagged unpriced item</name>
  <files>lib/estimate/quality/vagueness.ts, tests/unit/estimate/vagueness-flagged-unpriced.test.ts</files>
  <read_first>
    - lib/estimate/quality/vagueness.ts (the WHOLE file — the entire gate is ~30 lines)
    - lib/estimate/graph/nodes/assess.ts (the consumer that re-reads { total, sections:[{items:[{id}]}] })
    - tests/eval/metrics.ts (scorePersistedEstimate — the other consumer; its shape must stay compatible)
    - tests/eval/fixtures/cases.ts (the 'vague-do-some-work' case: sections:[] / no items → must STAY isVague:true)
  </read_first>
  <behavior>
    - Test 1 (still vague): { total: 0, sections: [] } → true (no items / total 0 → block).
    - Test 2 (still vague): { total: null, sections: [{ items: [] }] } → true (no items).
    - Test 3 (still vague): { total: 0, sections: [{ items: [{}, {}] }] } → true (items exist but total 0 → genuinely valueless / all-$0).
    - Test 4 (NOT vague — the new case): { total: 250, sections: [{ items: [{}, {}, {}] }] } → false (priced, items present; a flagged unpriced line among them must not block since total>0).
    - Test 5 (regression — null estimate): isVagueEstimate(null) → true.
    - Test 6 (regression — happy path unchanged): { total: 1000, sections: [{ items: [{}] }] } → false.
  </behavior>
  <action>
    The minimal refinement: the CURRENT logic already returns false for total>0 AND items-present, and
    true for total<=0 OR no-items. The behavior the context requires ("total > 0 already passes today;
    the refinement ensures a partially-priced estimate is never blocked just because one line is flagged")
    is ALREADY satisfied by `!hasTotal || !hasItems` for the total>0 case — the flagged unpriced line is
    just another item, so it does not flip hasItems or hasTotal.

    Therefore the change is NARROW: confirm and LOCK the contract with explicit tests, and make the intent
    explicit in the gate. Update the doc comment to state the two distinguished cases (empty/all-$0 → vague;
    partially-priced with a flagged unpriced line → not vague). If the current implementation already
    yields the Test 1-6 verdicts (it does), the code body stays functionally identical — do NOT add a
    spurious branch that could regress. If a flagged-unpriced item is later modeled with unit_price 0
    among priced items, Test 4 (total>0) already passes; no $0-per-item special-case is needed because the
    gate keys on the AGGREGATE total, not per-item prices. Document this reasoning in the comment so a
    future reader does not "fix" it back into a per-item block.

    Author `tests/unit/estimate/vagueness-flagged-unpriced.test.ts` covering Test 1-6 exactly. These tests
    are the RFALL-02 contract lock — they must assert the empty-vs-flagged distinction precisely.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/vagueness-flagged-unpriced.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - isVagueEstimate allows total>0 with a flagged item: `{ total: 250, sections: [{ items: [{},{},{}] }] }` → false (Test 4 green)
    - isVagueEstimate blocks empty/all-$0: `{ total: 0, sections: [] }` → true AND `{ total: 0, sections: [{ items: [{},{}] }] }` → true
    - grep: the doc comment in vagueness.ts mentions "flagged" or "partially-priced" (intent documented) — `grep -ci "flagged\|partially" lib/estimate/quality/vagueness.ts` >= 1
    - `npx vitest run tests/unit/estimate/vagueness-flagged-unpriced.test.ts` passes
    - Regression: `npx vitest run tests/unit/estimate tests/eval tests/unit/whatsapp` green (no needs-details regression)
  </acceptance_criteria>
  <done>isVagueEstimate distinguishes a fully-empty/all-$0 estimate (vague) from a partially-priced estimate with a flagged unpriced line (not vague, since total>0); WhatsApp/eval/needs-details paths unchanged; contract locked by tests.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/estimate/vagueness-flagged-unpriced.test.ts` green.
- Regression: `npx vitest run tests/eval tests/unit/whatsapp tests/unit/estimate` green — the 'vague-do-some-work' eval case stays isVague:true and the WhatsApp ask-details path is unchanged.
- `npx tsc --noEmit` clean on vagueness.ts + the new test.
</verification>

<success_criteria>
- A fully-empty / all-$0 estimate is still vague (block → needs-details).
- A partially-priced estimate (total>0) with a flagged unpriced line is NOT vague (allow → proceeds).
- No regression to the assess node, eval metrics, or WhatsApp ask-details consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/108-orchestrator-service-integration-the-payoff/108-02-SUMMARY.md`.
</output>
