---
phase: 30-ai-client-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/types.ts
  - lib/ai/normalize.ts
  - lib/ai/providers/anthropic.ts
  - lib/ai/providers/gemini.ts
  - app/api/generate-estimate/route.ts
  - components/capture/capture-recorder.tsx
  - components/workspace/estimate/client-suggestion-toast.ts
  - components/workspace/estimate/estimate-tab.tsx
  - components/workspace/estimate/estimate-editor.tsx
  - lib/actions/project.ts
requirements:
  - CLIENTASSOC-03
requirements_addressed:
  - CLIENTASSOC-03
autonomous: true
must_haves:
  truths:
    - "When AI detects a client name for a client-less project, the user sees a non-blocking suggestion after estimate generation"
    - "Accepting a matched existing-client suggestion links the project to that client"
    - "Dismissing or ignoring the suggestion makes no project or client changes"
    - "When no client name is detected, the estimate generation flow is unchanged"
    - "When a project already has a linked client, no AI client suggestion is shown"
  artifacts:
    - path: "lib/ai/types.ts"
      provides: "Optional suggested_client_name field on EstimateOutput"
    - path: "lib/ai/normalize.ts"
      provides: "Safe normalization of optional suggested_client_name"
    - path: "lib/ai/providers/anthropic.ts"
      provides: "Claude tool schema and prompt description for optional client detection"
    - path: "lib/ai/providers/gemini.ts"
      provides: "Gemini function declaration and description for optional client detection"
    - path: "app/api/generate-estimate/route.ts"
      provides: "clientSuggestion response payload with conservative existing-client match"
    - path: "components/capture/capture-recorder.tsx"
      provides: "Capture generation handoff that can show the client suggestion after navigation"
    - path: "components/workspace/estimate/client-suggestion-toast.ts"
      provides: "Shared toast helper for matched/no-match client suggestions"
    - path: "components/workspace/estimate/estimate-tab.tsx"
      provides: "Workspace generation toast for client suggestions"
    - path: "lib/actions/project.ts"
      provides: "Accepted suggestion uses explicit linkProjectToClient action"
  key_links:
    - from: "lib/ai/providers/anthropic.ts"
      to: "lib/ai/types.ts"
      via: "suggested_client_name"
      pattern: "suggested_client_name"
    - from: "lib/ai/providers/gemini.ts"
      to: "lib/ai/types.ts"
      via: "suggested_client_name"
      pattern: "suggested_client_name"
    - from: "app/api/generate-estimate/route.ts"
      to: "components/workspace/estimate/estimate-tab.tsx"
      via: "clientSuggestion response field"
      pattern: "clientSuggestion"
    - from: "components/workspace/estimate/estimate-tab.tsx"
      to: "lib/actions/project.ts"
      via: "linkProjectToClient"
      pattern: "linkProjectToClient"
    - from: "components/capture/capture-recorder.tsx"
      to: "components/workspace/estimate/estimate-tab.tsx"
      via: "suggestion handoff after redirect"
      pattern: "clientSuggestion"
---

<objective>
Implement Phase 30: AI Client Extraction. After estimate generation, if AI clearly detects a client name for a project with no linked client, show a non-blocking toast that lets the user accept an existing-client match or review/create when no match exists. Never silently create or link a client.
</objective>

<context>
@.planning/phases/30-ai-client-extraction/30-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@app/api/generate-estimate/route.ts
@lib/ai/types.ts
@lib/ai/normalize.ts
@lib/ai/providers/anthropic.ts
@lib/ai/providers/gemini.ts
@components/capture/capture-recorder.tsx
@components/workspace/estimate/estimate-tab.tsx
@components/workspace/link-client-card.tsx
@lib/actions/project.ts
@app/api/clients/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AI estimate output with optional client detection</name>
  <read_first>
    - lib/ai/types.ts
    - lib/ai/normalize.ts
    - lib/ai/providers/anthropic.ts
    - lib/ai/providers/gemini.ts
    - .planning/phases/30-ai-client-extraction/30-CONTEXT.md
  </read_first>
  <files>lib/ai/types.ts, lib/ai/normalize.ts, lib/ai/providers/anthropic.ts, lib/ai/providers/gemini.ts</files>
  <action>
    1. In `lib/ai/types.ts`, add `suggested_client_name?: string | null` to `EstimateOutput`.
    2. In `lib/ai/normalize.ts`, normalize `raw.suggested_client_name` into `suggested_client_name`.
       - If the raw value is a non-empty string after `.trim()`, store the trimmed string.
       - Otherwise store `null`.
    3. In `lib/ai/providers/anthropic.ts`, update the `create_estimate` tool schema:
       - Add optional property `suggested_client_name` with `type: 'string'`.
       - Description: "The customer/homeowner/business name explicitly mentioned in the transcript, description, or photo analysis. Return an empty string or omit when no clear client name is present. Do not infer from generic project titles or addresses."
       - Do not add it to `required`.
    4. In `lib/ai/providers/gemini.ts`, update `createEstimateDeclaration.parameters.properties` with optional `suggested_client_name` using `Type.STRING` and the same description.
       - Do not add it to `required`.
  </action>
  <acceptance_criteria>
    - `lib/ai/types.ts` contains `suggested_client_name?: string | null`
    - `lib/ai/normalize.ts` contains `suggested_client_name`
    - `lib/ai/normalize.ts` contains `.trim()`
    - `lib/ai/providers/anthropic.ts` contains `suggested_client_name`
    - `lib/ai/providers/anthropic.ts` does not include `suggested_client_name` in the `required` array
    - `lib/ai/providers/gemini.ts` contains `suggested_client_name`
    - `lib/ai/providers/gemini.ts` does not include `suggested_client_name` in the `required` array
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Return conservative clientSuggestion from estimate generation</name>
  <read_first>
    - app/api/generate-estimate/route.ts
    - lib/actions/project.ts
    - app/api/clients/route.ts
    - .planning/REQUIREMENTS.md
    - .planning/phases/30-ai-client-extraction/30-CONTEXT.md
  </read_first>
  <files>app/api/generate-estimate/route.ts</files>
  <action>
    1. In `app/api/generate-estimate/route.ts`, add a small helper near the route handler:
       - `normalizeClientNameForMatch(name: string): string`
       - It should lowercase, trim, collapse repeated whitespace to one space, and remove periods and commas.
    2. After `const aiEstimate = await provider.generateEstimate(estimateInput)`, build `clientSuggestion`.
    3. Only build a suggestion when all are true:
       - `client` is `null`
       - `aiEstimate.suggested_client_name?.trim()` is non-empty
    4. Query company clients with `supabase.from('clients').select('id, name').eq('company_id', companyId)` and find the first client where normalized existing name equals normalized suggested name.
    5. Return the response payload as:
       - `{ estimateId, version: nextVersion, clientSuggestion }`
       - `clientSuggestion` is `null` when suppressed or not detected.
       - When present: `{ detectedName: string, matchedClientId: string | null, matchedClientName: string | null }`.
    6. Do not insert, update, or create any client records in this route.
    7. Do not update `projects.client_id` in this route.
  </action>
  <acceptance_criteria>
    - `app/api/generate-estimate/route.ts` contains `normalizeClientNameForMatch`
    - `app/api/generate-estimate/route.ts` contains `clientSuggestion`
    - `app/api/generate-estimate/route.ts` contains `matchedClientId`
    - `app/api/generate-estimate/route.ts` still returns `estimateId`
    - `app/api/generate-estimate/route.ts` still returns `version: nextVersion`
    - `app/api/generate-estimate/route.ts` does not call `.insert(` on `clients`
    - `app/api/generate-estimate/route.ts` does not update `{ client_id`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Show non-blocking client suggestion toast after generation</name>
  <read_first>
    - components/workspace/estimate/estimate-tab.tsx
    - components/capture/capture-recorder.tsx
    - components/workspace/link-client-card.tsx
    - lib/actions/project.ts
    - .planning/phases/30-ai-client-extraction/30-CONTEXT.md
  </read_first>
  <files>components/workspace/estimate/client-suggestion-toast.ts, components/workspace/estimate/estimate-tab.tsx, components/workspace/estimate/estimate-editor.tsx, components/capture/capture-recorder.tsx, lib/actions/project.ts</files>
  <action>
    1. Define a local or shared type for the API response:
       - `estimateId: string`
       - `version: number`
       - `clientSuggestion: null | { detectedName: string; matchedClientId: string | null; matchedClientName: string | null }`
    2. Create or update `components/workspace/estimate/client-suggestion-toast.ts` with the shared `ClientSuggestion`, `GenerateEstimateResponse`, `storeClientSuggestion`, `popStoredClientSuggestion`, and `showClientSuggestionToast` helpers.
    3. In `components/workspace/estimate/estimate-tab.tsx`, after `/api/generate-estimate` succeeds, parse the JSON body and call a helper that shows a `sonner` toast when `clientSuggestion` is present.
    3. For matched suggestions:
       - Toast title: `Detected client: ${detectedName}`
       - Description mentions the matched existing client name.
       - Action label: `Link`
       - Action calls `linkProjectToClient(projectId, matchedClientId)`, shows success/error toast, and calls `router.refresh()` on success.
    4. For no-match suggestions:
       - Toast title: `Detected client: ${detectedName}`
       - Description says no existing client matched.
       - Action label: `Review`
       - Action navigates to `/clients/new` or another existing client creation path if available; if no existing route supports prefilling, route to `/clients`.
    5. In `components/workspace/estimate/estimate-editor.tsx`, show the same non-blocking suggestion toast after estimate regeneration.
    6. In `components/capture/capture-recorder.tsx`, preserve the suggestion across the redirect to `/projects/${projectId}?tab=estimate&estimate=${estimateId}`.
       - Use `sessionStorage` with key `xtimator:client-suggestion:${projectId}` before `router.push`.
       - Store the `clientSuggestion` JSON only when it is present.
       - Add a small effect in `EstimateTab` that reads and removes that key once and shows the same toast.
    7. Dismissing the toast must not call any action and must not mutate project or client data.
  </action>
  <acceptance_criteria>
    - `components/workspace/estimate/client-suggestion-toast.ts` imports `linkProjectToClient`
    - `components/workspace/estimate/estimate-tab.tsx` contains `clientSuggestion`
    - `components/workspace/estimate/estimate-tab.tsx` contains `toast(`
    - `components/workspace/estimate/client-suggestion-toast.ts` contains `sessionStorage`
    - `components/workspace/estimate/client-suggestion-toast.ts` contains `linkProjectToClient`
    - `components/capture/capture-recorder.tsx` contains `xtimator:client-suggestion`
    - `components/capture/capture-recorder.tsx` contains `clientSuggestion`
    - `components/workspace/estimate/estimate-editor.tsx` contains `clientSuggestion`
    - `components/workspace/estimate/client-suggestion-toast.ts` contains `/clients`
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run `npm run build`.
- Verify `CLIENTASSOC-03` is satisfied:
  1. Clientless project + AI detected name returns `clientSuggestion`.
  2. Linked project suppresses `clientSuggestion`.
  3. No detected name returns `clientSuggestion: null`.
  4. Accepting a matched suggestion calls `linkProjectToClient`.
  5. Dismissal has no side effects.
</verification>

<success_criteria>
1. When the AI detects a client name in the transcript, description, or photo analysis, a toast notification appears after estimate generation with the detected name and does not interrupt the editor.
2. The user can accept a matched existing-client suggestion and link the project, or review/create when no match exists.
3. If AI does not detect a client name, no toast appears and the flow remains identical to today.
4. No clients are silently created and no projects are silently linked.
</success_criteria>

<output>
After completion, create `.planning/phases/30-ai-client-extraction/30-01-SUMMARY.md`.
</output>
