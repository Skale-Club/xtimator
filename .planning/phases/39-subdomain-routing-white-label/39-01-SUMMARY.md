---
phase: 39-subdomain-routing-white-label
plan: 01
subsystem: infra
tags: [nextjs, middleware, proxy, white-label, custom-domain, headers, typescript]

requires:
  - phase: 38-custom-domain-db-settings-ui
    provides: "custom_domain column on companies table — the domain value that routes to this pass-through"

provides:
  - "Custom host detection in proxy.ts (before updateSession) — sets x-white-label: 1 request header for custom domain /estimate/* paths"
  - "EstimateView whiteLabelMode prop — conditional footer suppression when whiteLabelMode=true"
  - "app/estimate/[token]/page.tsx reads x-white-label header via next/headers and passes whiteLabelMode to EstimateView"
  - "7 unit tests covering DOMAIN-03 host detection logic and DOMAIN-04 whiteLabelMode prop behavior"

affects:
  - any future phase extending white-label rendering (branding, CSS, logo)
  - 39-02 if it exists (settings UI for custom domain — Plan 01 covers routing; data layer from Phase 38)

tech-stack:
  added: []
  patterns:
    - "x-white-label request header pattern: proxy sets header → RSC reads via headers() → prop passed to client component"
    - "NextResponse.next({ request: { headers: requestHeaders } }) for forwarding headers to Server Components — NOT res.headers.set()"
    - "Custom host detection: !includes(localhost) && !endsWith(.vercel.app) && !== appHost — three-condition guard"
    - "NEXT_PUBLIC_APP_HOST env var for primary host comparison with 'xtimator.com' fallback"

key-files:
  created:
    - tests/unit/custom-domain-routing.test.ts
  modified:
    - proxy.ts
    - components/share/estimate-view.tsx
    - app/estimate/[token]/page.tsx

key-decisions:
  - "Custom host detection block inserted BEFORE updateSession() call in proxy.ts — estimate path bypasses session refresh to avoid auth redirect on unauthenticated visitors"
  - "No DB lookup of company by host in proxy — token uniqueness guarantees correct estimate is served, avoids unnecessary round-trip"
  - "Inline isCustomHost() function in tests (not imported from proxy.ts) — avoids mocking NextRequest, matches middleware.test.ts precedent"
  - "whiteLabelMode defaults to false in EstimateView — preserves existing behavior for all xtimator.com visitors"

patterns-established:
  - "Request header forwarding pattern: set on requestHeaders, forward via NextResponse.next({ request: { headers: requestHeaders } })"
  - "White-label conditional rendering: {!whiteLabelMode && (<Component />)} wrapper pattern"

requirements-completed: [DOMAIN-03, DOMAIN-04]

duration: 8min
completed: 2026-05-10
---

# Phase 39 Plan 01: Subdomain Routing White-Label Summary

**Custom domain pass-through in proxy.ts with x-white-label header forwarding and EstimateView footer suppression for white-label estimate rendering**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-10T18:20:41Z
- **Completed:** 2026-05-10T18:28:16Z
- **Tasks:** 5
- **Files modified:** 4

## Accomplishments
- Inserted custom host detection block in proxy.ts before updateSession() — custom domains serving /estimate/* get x-white-label: 1 set in forwarded request headers
- Added whiteLabelMode prop to EstimateView with conditional footer rendering — "Generated by Xtimator" hidden when whiteLabelMode=true
- Wired proxy signal through estimate page.tsx via next/headers — reads x-white-label header and passes whiteLabelMode prop to EstimateView
- 7 unit tests GREEN: 4 covering DOMAIN-03 host detection logic, 3 covering DOMAIN-04 whiteLabelMode boolean behavior

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave 0 failing test stubs** - `a3770fb` (test)
2. **Task 2: proxy.ts custom host detection + x-white-label header** - `acad211` (feat)
3. **Task 3: EstimateView whiteLabelMode prop + conditional footer** - `7891a65` (feat)
4. **Task 4: estimate page.tsx x-white-label header read + whiteLabelMode prop** - `04c94af` (feat)
5. **Task 5: Update test stubs to real assertions — GREEN** - `6a028b5` (feat)

## Files Created/Modified
- `tests/unit/custom-domain-routing.test.ts` - 7 tests: isCustomHost() inline pure function + whiteLabelMode boolean logic
- `proxy.ts` - Custom host detection block before updateSession(); NextResponse.next with x-white-label header forwarding
- `components/share/estimate-view.tsx` - whiteLabelMode?: boolean prop + {!whiteLabelMode && (...)} footer conditional
- `app/estimate/[token]/page.tsx` - headers import from next/headers; x-white-label read; whiteLabelMode={isWhiteLabel} prop

## Decisions Made
- Custom host detection placed BEFORE updateSession() in proxy so unauthenticated estimate visitors are not redirected to login
- No DB lookup to resolve company by host — estimate token uniqueness guarantees the correct estimate is served without a round-trip
- Tests use inline isCustomHost() pure function mirroring proxy.ts logic (not importing from proxy) — consistent with middleware.test.ts pattern that avoids mocking NextRequest

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- git stash during baseline measurement temporarily reverted test file to stubs; re-applied real assertions immediately after stash pop

## User Setup Required
None - no external service configuration required. No new npm packages, no DB changes, no env vars required beyond the optional NEXT_PUBLIC_APP_HOST.

## Next Phase Readiness
- DOMAIN-03 and DOMAIN-04 complete: custom domain estimates render white-labeled from any custom host pointing to the Vercel deployment
- Phase 38 data layer (saveCustomDomain action + custom_domain column) is the prerequisite for the settings UI that lets users configure their custom domain
- No blockers

---
*Phase: 39-subdomain-routing-white-label*
*Completed: 2026-05-10*
