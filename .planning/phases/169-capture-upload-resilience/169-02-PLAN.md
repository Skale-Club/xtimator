---
phase: 169-capture-upload-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/inngest/functions/storage-orphan-cleanup.ts
  - lib/inngest/functions/index.ts
  - app/api/inngest/route.ts
  - lib/storage/index.ts
  - lib/storage/supabase-provider.ts
  - tests/unit/inngest/storage-orphan-cleanup.test.ts
autonomous: true
requirements:
  - CAPT-04
must_haves:
  truths:
    - "A scheduled Inngest cron (registered in BOTH lib/inngest/functions/index.ts AND app/api/inngest/route.ts's functions array) reconciles the audio and photos buckets against their DB rows."
    - "An object referenced by ANY table is NEVER deleted — including photos referenced by photos.storage_path, price-book images under {companyId}/price-book/ (referenced by company_price_book.image_url with NO photos row), recordings referenced by recordings.storage_path, and WhatsApp audio under {companyId}/whatsapp/ (referenced by whatsapp_messages.media_url while recordings.storage_path is null)."
    - "The walk reaches actual FILE objects at their real 3-level depth ({companyId}/{projectId|whatsapp|price-book|...}/{file}), rejoins the FULL key (prefix + '/' + leaf name) before row-matching, and pages past 100 entries per level."
    - "Only objects older than 24h (by updatedAt — created_at proxy for never-touched objects, documented) with no reference anywhere are deleted, with a defensive row re-check immediately before each delete."
    - "The sweep is never-throw per page/object and emits a summary log {bucket, scanned, skippedProtectedPrefix, deleted}."
  artifacts:
    - path: "lib/inngest/functions/storage-orphan-cleanup.ts"
      provides: "storageOrphanCleanupJob — daily cron, 3-level paged walk, protected-prefix skips, union row-matching"
    - path: "lib/storage/index.ts"
      provides: "additive list() pagination options ({limit, offset}) + createdAt on ListedObject (optional fields — no breaking change)"
  key_links:
    - from: "app/api/inngest/route.ts"
      to: "lib/inngest/functions/storage-orphan-cleanup.ts"
      via: "explicit import + functions array entry (mirroring cleanupAudioJob which appears in BOTH files)"
      pattern: "storageOrphanCleanupJob"
---

<objective>
Close audit finding F5 (CAPT-04): permanent storage orphans in the audio and photos buckets. REWRITTEN after Opus plan-check found 5 blockers in v1: (1) photos bucket is SHARED with price-book images ({companyId}/price-book/{ts}-{itemId}.{ext}, referenced by company_price_book.image_url, no photos row — lib/actions/price-book.ts:190-202, :254-266); (2) audio bucket is SHARED with WhatsApp inbox audio ({companyId}/whatsapp/{msgId}.{ext}, referenced by whatsapp_messages.media_url while the recordings row has storage_path NULL — lib/estimate/adapters/whatsapp.ts:189-199, :223-229); (3) the Inngest serve handler app/api/inngest/route.ts:12-44 registers functions EXPLICITLY — barrel export alone never schedules anything; (4) real keys are 3 levels deep and Supabase .list() is non-recursive folder-style; (5) StorageProvider.list() returns LEAF names while storage_path columns store FULL keys (supabase-provider.ts:82-88 vs recording.ts:278 / photo.ts:63).

SCOPE FENCE (updated): this plan MAY additively extend lib/storage/index.ts + supabase-provider.ts (list pagination options + createdAt field) — verified disjoint from 169-01, which only ADDS lib/storage/upload-with-retry.ts and edits capture components. Do NOT touch upload paths, capture components, or cleanup-audio.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md
@lib/inngest/functions/cleanup-audio.ts
@lib/inngest/functions/index.ts
@app/api/inngest/route.ts
@lib/storage/index.ts
@lib/storage/supabase-provider.ts
@lib/actions/price-book.ts
@lib/estimate/adapters/whatsapp.ts

<interfaces>
Storage abstraction extension (ADDITIVE — no breaking change; convention at cleanup-audio.ts:20-22 forbids calling supabase.storage directly, so the abstraction must carry what the cron needs):
```ts
// lib/storage/index.ts
export interface ListedObject { name: string; size?: number; updatedAt?: string; createdAt?: string; isFolder?: boolean }
// list gains optional paging: list(bucket, prefix?, opts?: { limit?: number; offset?: number })
```
supabase-provider.ts: map created_at; set isFolder = entry.id == null (Supabase folder placeholders have null id/metadata); pass limit/offset through to .list().

Sweep algorithm per bucket:
1. PROTECTED PREFIXES (hard skip, counted): audio → `whatsapp/`; photos → `price-book/`. Never enumerate or delete inside them.
2. 3-level paged walk: list root (companies, page loop limit 100/offset) → per company, list level-2 entries → skip protected prefixes → per non-protected level-2 folder, page its FILES (isFolder=false).
3. Full key = `${companyId}/${folder}/${leafName}`. Age gate: updatedAt older than 24h (documented as created_at proxy — never-touched orphans satisfy updatedAt≈createdAt).
4. Reference check (union): audio → recordings.storage_path = key; photos → photos.storage_path = key. (Protected prefixes already excluded the whatsapp/price-book reference classes; the union check is belt for any future path shape — implement as a per-bucket matcher function so adding tables is one line.)
5. Defensive re-check of row absence immediately before each delete (TOCTOU belt).
6. Never-throw per page and per object (log + continue); step.run per bucket; summary log per must_haves.
Cron: daily, offset from cleanup-audio's hour. Export name: storageOrphanCleanupJob (house ...Job convention). Register in BOTH index.ts and app/api/inngest/route.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive storage-list extension</name>
  <files>lib/storage/index.ts, lib/storage/supabase-provider.ts</files>
  <read_first>
    - lib/storage/index.ts (FULL) + lib/storage/supabase-provider.ts (FULL — list at :82-88)
  </read_first>
  <action>
Extend per interface: optional createdAt + isFolder on ListedObject; optional {limit, offset} on list; provider maps created_at, derives isFolder from null id, passes paging to .list(). All additive/optional — existing call sites compile unchanged (tsc proves).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.ci.json` exits 0 with NO changes to any existing list() call site
    - Provider unit coverage (extend existing storage tests if present, else inline in Task 2's suite): isFolder true for null-id entries; full paging passthrough
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit -p tsconfig.ci.json</automated></verify>
  <done>The abstraction carries what reconciliation needs; no direct supabase.storage calls required.</done>
</task>

<task type="auto">
  <name>Task 2: Reconciliation cron (3-level walk, protected prefixes, union matcher)</name>
  <files>lib/inngest/functions/storage-orphan-cleanup.ts, tests/unit/inngest/storage-orphan-cleanup.test.ts</files>
  <read_first>
    - lib/inngest/functions/cleanup-audio.ts (FULL — cron/service-client/never-throw conventions)
    - lib/actions/recording.ts:270-285 and lib/actions/photo.ts:55-70 (full-key storage_path shapes)
    - lib/estimate/adapters/whatsapp.ts:185-230 and lib/actions/price-book.ts:185-270 (the two protected reference classes)
  </read_first>
  <action>
Implement the sweep per interface. Tests (mock service client + a fake StorageProvider):
(a) 3-level orphan >24h with no row → deleted (full-key rejoin asserted — the mock row-check receives `{companyId}/{projectId}/{file}`, never a leaf);
(b) object with a recordings/photos row → NOT deleted;
(c) fresh object <24h → NOT deleted;
(d) `{companyId}/whatsapp/x.ogg` → NEVER enumerated for deletion (protected prefix), even with no recordings.storage_path match;
(e) `{companyId}/price-book/y.jpg` → NEVER deleted, even with no photos row;
(f) >100 files under one folder → paging loop covers all (mock returns 100 then remainder);
(g) list error on one company prefix → sweep continues, summary emitted;
(h) defensive re-check: row appears between scan and delete → NOT deleted.
  </action>
  <acceptance_criteria>
    - All 8 test cases GREEN — (d) and (e) are the Opus-blocker regression locks
    - Folder placeholders (isFolder) never treated as deletable objects
    - Summary log includes skippedProtectedPrefix count
    - `npm test` GREEN
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/inngest/storage-orphan-cleanup.test.ts</automated></verify>
  <done>Orphans die; every referenced object class provably survives.</done>
</task>

<task type="auto">
  <name>Task 3: Register the cron in BOTH registration points</name>
  <files>lib/inngest/functions/index.ts, app/api/inngest/route.ts</files>
  <read_first>
    - app/api/inngest/route.ts (FULL — import list :12-26 + functions array :30-44; mirror cleanupAudioJob's dual presence)
  </read_first>
  <action>
Add storageOrphanCleanupJob to the barrel (index.ts) AND to route.ts (import + functions array entry). Diff on each file = 1 import + 1 entry.
  </action>
  <acceptance_criteria>
    - `grep -c "storageOrphanCleanupJob" app/api/inngest/route.ts` = 2 (import + array)
    - `grep -c "storageOrphanCleanupJob" lib/inngest/functions/index.ts` ≥ 1
    - `npm test` GREEN, `npx tsc --noEmit -p tsconfig.ci.json` exits 0
  </acceptance_criteria>
  <verify><automated>npm test</automated></verify>
  <done>The cron actually fires.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/inngest/storage-orphan-cleanup.test.ts
npx tsc --noEmit -p tsconfig.ci.json
npm test
```
Regression contracts: cleanup-audio.ts untouched; no direct supabase.storage calls (grep in the new file → only the StorageProvider abstraction); protected-prefix tests (d)/(e) present and green.
</verification>

<success_criteria>
- Phase 169 criterion 4 TRUE: both buckets reconciled; price-book images, WhatsApp audio, and every row-referenced object provably untouchable
</success_criteria>

<output>
Create `.planning/phases/169-capture-upload-resilience/169-02-SUMMARY.md`: the protected-prefix + union-matcher design, walk/paging mechanics, the 8-case test table, and registration evidence.
</output>
