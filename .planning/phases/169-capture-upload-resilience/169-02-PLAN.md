---
phase: 169-capture-upload-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/inngest/functions/storage-orphan-cleanup.ts
  - lib/inngest/functions/index.ts
  - tests/unit/inngest/storage-orphan-cleanup.test.ts
autonomous: true
requirements:
  - CAPT-04
must_haves:
  truths:
    - "A scheduled Inngest cron lists the audio AND photos buckets and deletes objects older than 24h that have NO matching DB row (recordings.storage_path / photos.storage_path)."
    - "An object WITH a matching row is NEVER deleted — proven by test."
    - "The out-of-credits orphan path (file uploaded, row never created) is covered: such an object IS deleted after the age threshold."
    - "The cron is never-throw per bucket page (one bad page cannot abort the sweep) and logs a summary count."
  artifacts:
    - path: "lib/inngest/functions/storage-orphan-cleanup.ts"
      provides: "weekly-or-daily cron reconciling storage vs rows for both buckets"
  key_links:
    - from: "lib/inngest/functions/index.ts"
      to: "lib/inngest/functions/storage-orphan-cleanup.ts"
      via: "function registration (same list as cleanup-audio)"
      pattern: "storageOrphanCleanup"
---

<objective>
Close audit finding F5 (CAPT-04): permanent storage orphans. Audio objects whose row-create failed (including the credit-gate ordering: upload precedes checkCredits, so out-of-credits users leave full orphaned blobs) are invisible to the row-driven cleanup-audio cron; photo objects have NO cleanup at all. Build a bucket-reconciliation cron.

Evidence (REQUIRED READING): .planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md § F5. Existing pattern to mirror: lib/inngest/functions/cleanup-audio.ts (cron config, service client, batching, never-throw discipline). SCOPE FENCE: do NOT modify cleanup-audio.ts, capture-recorder.tsx, or lib/storage (those belong to 169-01 running in parallel) — this plan only ADDS the new function + registers it in index.ts (the one shared file; keep that edit to a single import + array entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md
@lib/inngest/functions/cleanup-audio.ts
@lib/inngest/functions/index.ts

<interfaces>
Algorithm per bucket (audio → recordings.storage_path; photos → photos.storage_path):
1. Page through storage objects via the service client's storage list API (per top-level company prefix — list root prefixes first, then page each; Supabase list is prefix-scoped, non-recursive).
2. For each object older than 24h (created_at from list metadata), check row existence by exact storage_path match.
3. No row → remove the object. Any error on a single object/page → log and continue (never-throw per unit).
4. step.run per bucket (checkpointing); emit a final summary log {bucket, scanned, deleted}.
Schedule: daily cron (mirror cleanup-audio's cron syntax/hour, offset to avoid overlap).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconciliation cron for both buckets</name>
  <files>lib/inngest/functions/storage-orphan-cleanup.ts, lib/inngest/functions/index.ts, tests/unit/inngest/storage-orphan-cleanup.test.ts</files>
  <read_first>
    - lib/inngest/functions/cleanup-audio.ts (FULL — cron/config/service-client/batch conventions)
    - lib/inngest/functions/index.ts (registration list)
  </read_first>
  <action>
Implement per the interface algorithm. Tests (mock service client + storage list): (a) orphan >24h → removed; (b) object WITH matching row → NOT removed; (c) object <24h (fresh upload racing its row) → NOT removed; (d) list error on one prefix → sweep continues, summary still emitted. Register in index.ts.
  </action>
  <acceptance_criteria>
    - All 4 test cases GREEN; safety case (b) explicitly asserted
    - Never-throw: a thrown storage error cannot fail the whole function run
    - index.ts diff = 1 import + 1 array entry only
    - `npm test` GREEN, `npx tsc --noEmit -p tsconfig.ci.json` exits 0
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/inngest/storage-orphan-cleanup.test.ts</automated></verify>
  <done>No storage object leaks money forever.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/inngest/storage-orphan-cleanup.test.ts
npx tsc --noEmit -p tsconfig.ci.json
npm test
```
</verification>

<success_criteria>
- Phase 169 criterion 4 TRUE: orphans reconciled for both buckets, row-holding objects untouchable
</success_criteria>

<output>
Create `.planning/phases/169-capture-upload-resilience/169-02-SUMMARY.md` with the sweep algorithm, safety guarantees, and test evidence.
</output>
