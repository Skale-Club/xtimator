---
phase: 20-price-book-crud-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/price-book/price-book-list.test.tsx
  - tests/unit/schemas/price-book.test.ts
  - lib/schemas/price-book.ts
  - lib/queries/price-book.ts
  - lib/actions/price-book.ts
autonomous: true
requirements: [PB-01, PB-02, PB-03, PB-04, PB-06, PB-07]

must_haves:
  truths:
    - "Test files exist and fail (RED) with expect.fail() stubs before any implementation"
    - "priceBookItemSchema validates category, name, unit (optional), unit_price (coerced number ≥ 0), notes (optional)"
    - "getPriceBookItems query function is typed and exported from lib/queries/price-book.ts"
    - "createPriceBookItem, updatePriceBookItem, deletePriceBookItem server actions are exported from lib/actions/price-book.ts"
    - "npx vitest run tests/unit/price-book exits with failures (Wave 0 RED state)"
  artifacts:
    - path: "tests/unit/price-book/price-book-list.test.tsx"
      provides: "Failing stubs for PB-01..PB-07"
    - path: "tests/unit/schemas/price-book.test.ts"
      provides: "Failing stubs for schema validation"
    - path: "lib/schemas/price-book.ts"
      provides: "priceBookItemSchema + PriceBookItemFormValues type"
    - path: "lib/queries/price-book.ts"
      provides: "getPriceBookItems function + PriceBookItem interface"
    - path: "lib/actions/price-book.ts"
      provides: "createPriceBookItem, updatePriceBookItem, deletePriceBookItem"
  key_links:
    - from: "lib/actions/price-book.ts"
      to: "lib/schemas/price-book.ts"
      via: "import PriceBookItemFormValues"
      pattern: "PriceBookItemFormValues"
    - from: "lib/queries/price-book.ts"
      to: "company_price_book table"
      via: "supabase.from('company_price_book')"
      pattern: "company_price_book"
---

<objective>
Create the data-layer contracts and Wave 0 failing test stubs for Phase 20.

Purpose: Establish the TypeScript interfaces, zod schema, query function, and server actions that Wave 1 client components will consume. Test stubs go RED first (Nyquist compliance) so Wave 1 implementation turns them GREEN.

Output:
- tests/unit/price-book/price-book-list.test.tsx (RED stubs)
- tests/unit/schemas/price-book.test.ts (RED stubs)
- lib/schemas/price-book.ts (priceBookItemSchema + PriceBookItemFormValues)
- lib/queries/price-book.ts (getPriceBookItems)
- lib/actions/price-book.ts (createPriceBookItem, updatePriceBookItem, deletePriceBookItem)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-price-book-crud-ui/20-CONTEXT.md
@.planning/phases/20-price-book-crud-ui/20-RESEARCH.md
</context>

<interfaces>
<!-- Canonical patterns the executor must replicate exactly. No exploration needed. -->

From lib/schemas/client.ts (template for price-book schema):
```typescript
import { z } from 'zod'
export const clientSchema = z.object({
  name: z.string().min(1, 'Client name is required'),
  email: z.string().email('Invalid email').optional().or(z.literal('')),
  // ... other optional fields use .optional().or(z.literal(''))
})
export type ClientFormValues = z.infer<typeof clientSchema>
```

From lib/actions/client.ts (template for server actions):
```typescript
'use server'
import { createClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'
import type { ClientFormValues } from '@/lib/schemas/client'

async function getAuthContext() {
  const supabase = await createClient()
  const { data: claimsData } = await supabase.auth.getClaims()
  const claims = claimsData?.claims ?? null
  if (!claims) return { error: 'Not authenticated' as const }
  const { data: company } = await supabase
    .from('companies').select('id').eq('user_id', claims.sub).single()
  if (!company) return { error: 'No company found' as const }
  return { supabase, company }
}
```

From lib/queries/clients.ts (template for query function):
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
export interface ClientWithCount { id: string; name: string; ... }
export async function getClients(supabase: SupabaseClient, companyId: string): Promise<ClientWithCount[]> {
  const { data } = await supabase.from('clients').select(...).eq('company_id', companyId).order('name')
  return data ?? []
}
```

From tests/unit/clients/client-list.test.tsx (test template structure):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
vi.mock('next/navigation', () => ({ useRouter: () => ({ refresh: vi.fn(), push: vi.fn() }), usePathname: () => '/...' }))
vi.mock('next/link', () => ({ default: ({ children, href, ...props }: any) => <a href={href} {...props}>{children}</a> }))
vi.mock('sonner', () => ({ toast: { success: vi.fn(), error: vi.fn() } }))
```

From tests/unit/schemas/client.test.ts (schema test template):
```typescript
import { describe, it, expect } from 'vitest'
import { clientSchema } from '@/lib/schemas/client'
describe('clientSchema', () => {
  it('valid client with only name passes validation', () => {
    const result = clientSchema.safeParse({ name: 'Acme Corp' })
    expect(result.success).toBe(true)
  })
})
```

company_price_book table columns (from Phase 19 migration):
- id: UUID (pk)
- company_id: UUID FK → companies.id
- category: TEXT NOT NULL
- name: TEXT NOT NULL
- unit: TEXT (nullable)
- unit_price: NUMERIC(12,2) NOT NULL
- notes: TEXT (nullable)
- created_at: TIMESTAMPTZ DEFAULT now()
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test stubs (RED) for price-book-list and schema</name>
  <files>tests/unit/price-book/price-book-list.test.tsx, tests/unit/schemas/price-book.test.ts</files>
  <read_first>
    - tests/unit/clients/client-list.test.tsx (mock structure, render pattern, describe/it shape)
    - tests/unit/schemas/client.test.ts (schema test pattern)
    - .planning/phases/20-price-book-crud-ui/20-VALIDATION.md (exact test IDs and commands)
  </read_first>
  <behavior>
    Wave 0 stubs MUST use expect.fail('not implemented') so they are RED before implementation.

    price-book-list.test.tsx stubs (follow client-list.test.tsx mock structure exactly):
    - vi.mock('next/navigation', ...) — useRouter returning { refresh: vi.fn(), push: vi.fn() }
    - vi.mock('next/link', ...) — passthrough anchor
    - vi.mock('sonner', ...) — toast stubs
    - vi.mock('@/lib/actions/price-book', ...) — createPriceBookItem, updatePriceBookItem, deletePriceBookItem all vi.fn()
    - vi.mock('@/components/price-book/price-book-item-dialog', ...) — PriceBookItemDialog returns null stub
    - mockItems fixture: array of 3 PriceBookItem objects spanning 2 categories:
      - { id: '1', company_id: 'c1', category: 'Labor', name: 'General Labor', unit: 'hr', unit_price: 75, notes: null, created_at: '2026-01-01' }
      - { id: '2', company_id: 'c1', category: 'Labor', name: 'Supervisor', unit: 'hr', unit_price: 120, notes: 'Lead only', created_at: '2026-01-02' }
      - { id: '3', company_id: 'c1', category: 'Materials', name: 'PVC Pipe 2in', unit: 'ft', unit_price: 3.5, notes: null, created_at: '2026-01-03' }

    Stubs to write (all call expect.fail('not implemented')):
    - 'renders empty state when items array is empty' — PB-06
    - 'renders category headers for each distinct category' — PB-01
    - 'items sorted alphabetically within each category' — PB-01/D-04
    - 'search filters items by name' — PB-07
    - 'search filters items by category' — PB-07
    - '"no results" state appears when search matches nothing' — PB-07
    - 'add dialog opens when Add Item button clicked' — PB-02
    - 'edit dialog opens when Edit selected from dropdown' — PB-03
    - 'delete AlertDialog appears when Delete selected from dropdown' — PB-04
    - 'delete calls deletePriceBookItem and refreshes on confirm' — PB-04

    price-book.test.ts stubs (follow client.test.ts pattern):
    - 'valid item with category, name, unit_price passes' — expect.fail
    - 'missing category fails with "Category is required"' — expect.fail
    - 'missing name fails with "Item name is required"' — expect.fail
    - 'unit_price coerces string "42.50" to number 42.5' — expect.fail
    - 'unit_price of -1 fails with "Price must be 0 or greater"' — expect.fail
    - 'unit and notes can be empty string (optional fields)' — expect.fail
  </behavior>
  <action>
    Create directory tests/unit/price-book/ if it does not exist.

    Create tests/unit/price-book/price-book-list.test.tsx:
    ```typescript
    import { describe, it, expect, vi } from 'vitest'
    import { render, screen, fireEvent } from '@testing-library/react'
    import type { PriceBookItem } from '@/lib/queries/price-book'

    vi.mock('next/navigation', () => ({
      useRouter: () => ({ refresh: vi.fn(), push: vi.fn() }),
      usePathname: () => '/settings/price-book',
    }))

    vi.mock('next/link', () => ({
      default: ({ children, href, ...props }: any) => (
        <a href={href} {...props}>{children}</a>
      ),
    }))

    vi.mock('sonner', () => ({
      toast: { success: vi.fn(), error: vi.fn() },
    }))

    vi.mock('@/lib/actions/price-book', () => ({
      createPriceBookItem: vi.fn().mockResolvedValue({ data: {} }),
      updatePriceBookItem: vi.fn().mockResolvedValue({ data: {} }),
      deletePriceBookItem: vi.fn().mockResolvedValue({ data: { deleted: true } }),
    }))

    vi.mock('@/components/price-book/price-book-item-dialog', () => ({
      PriceBookItemDialog: ({ open }: { open: boolean }) =>
        open ? <div data-testid="price-book-dialog">Dialog Open</div> : null,
    }))

    const mockItems: PriceBookItem[] = [
      { id: '1', company_id: 'c1', category: 'Labor', name: 'General Labor', unit: 'hr', unit_price: 75, notes: null, created_at: '2026-01-01' },
      { id: '2', company_id: 'c1', category: 'Labor', name: 'Supervisor', unit: 'hr', unit_price: 120, notes: 'Lead only', created_at: '2026-01-02' },
      { id: '3', company_id: 'c1', category: 'Materials', name: 'PVC Pipe 2in', unit: 'ft', unit_price: 3.5, notes: null, created_at: '2026-01-03' },
    ]

    describe('PriceBookList', () => {
      it('renders empty state when items array is empty', () => {
        expect.fail('not implemented')
      })

      it('renders category headers for each distinct category', () => {
        expect.fail('not implemented')
      })

      it('items sorted alphabetically within each category', () => {
        expect.fail('not implemented')
      })

      it('search filters items by name', () => {
        expect.fail('not implemented')
      })

      it('search filters items by category', () => {
        expect.fail('not implemented')
      })

      it('"no results" state appears when search matches nothing', () => {
        expect.fail('not implemented')
      })

      it('add dialog opens when Add Item button clicked', () => {
        expect.fail('not implemented')
      })

      it('edit dialog opens when Edit selected from dropdown', () => {
        expect.fail('not implemented')
      })

      it('delete AlertDialog appears when Delete selected from dropdown', () => {
        expect.fail('not implemented')
      })

      it('delete calls deletePriceBookItem and refreshes on confirm', () => {
        expect.fail('not implemented')
      })
    })
    ```

    Create tests/unit/schemas/price-book.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { priceBookItemSchema } from '@/lib/schemas/price-book'

    describe('priceBookItemSchema', () => {
      it('valid item with category, name, unit_price passes', () => {
        expect.fail('not implemented')
      })

      it('missing category fails with "Category is required"', () => {
        expect.fail('not implemented')
      })

      it('missing name fails with "Item name is required"', () => {
        expect.fail('not implemented')
      })

      it('unit_price coerces string "42.50" to number 42.5', () => {
        expect.fail('not implemented')
      })

      it('unit_price of -1 fails with "Price must be 0 or greater"', () => {
        expect.fail('not implemented')
      })

      it('unit and notes can be empty string (optional fields)', () => {
        expect.fail('not implemented')
      })
    })
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/unit/price-book tests/unit/schemas/price-book.test.ts 2>&1 | tail -20</automated>
    Tests must be RED (failures) — expect.fail() stubs confirm Nyquist compliance. Exit code non-zero is expected.
  </verify>
  <done>
    - tests/unit/price-book/price-book-list.test.tsx exists with 10 describe/it blocks all calling expect.fail()
    - tests/unit/schemas/price-book.test.ts exists with 6 describe/it blocks all calling expect.fail()
    - npx vitest run tests/unit/price-book exits non-zero (tests are RED)
  </done>
</task>

<task type="auto">
  <name>Task 2: Data layer contracts — schema, query, server actions</name>
  <files>lib/schemas/price-book.ts, lib/queries/price-book.ts, lib/actions/price-book.ts</files>
  <read_first>
    - lib/schemas/client.ts (zod schema conventions: .optional().or(z.literal('')), export pattern)
    - lib/queries/clients.ts (SupabaseClient import, interface definitions, query function shape)
    - lib/actions/client.ts (getAuthContext helper, 'use server' directive, revalidatePath, discriminated return)
    - types/database.types.ts (verify Database['public']['Tables']['company_price_book'] shape)
    - .planning/phases/20-price-book-crud-ui/20-RESEARCH.md (Pattern 2, Pattern 3, Pattern 4, Pitfall 1, Pitfall 2)
  </read_first>
  <action>
    Create lib/schemas/price-book.ts:
    ```typescript
    import { z } from 'zod'

    export const priceBookItemSchema = z.object({
      category: z.string().min(1, 'Category is required'),
      name: z.string().min(1, 'Item name is required').max(200),
      unit: z.string().optional().or(z.literal('')),
      unit_price: z.coerce.number().min(0, 'Price must be 0 or greater'),
      notes: z.string().optional().or(z.literal('')),
    })

    export type PriceBookItemFormValues = z.infer<typeof priceBookItemSchema>
    ```

    Key: use z.coerce.number() (not z.number()) so HTML input string values auto-coerce (Pitfall 1 from RESEARCH.md).

    Create lib/queries/price-book.ts:
    ```typescript
    import type { SupabaseClient } from '@supabase/supabase-js'

    export interface PriceBookItem {
      id: string
      company_id: string
      category: string
      name: string
      unit: string | null
      unit_price: number
      notes: string | null
      created_at: string
    }

    export async function getPriceBookItems(
      supabase: SupabaseClient,
      companyId: string
    ): Promise<PriceBookItem[]> {
      const { data } = await supabase
        .from('company_price_book')
        .select('id, company_id, category, name, unit, unit_price, notes, created_at')
        .eq('company_id', companyId)
        .order('category')
        .order('name')
      return (data as PriceBookItem[]) ?? []
    }
    ```

    Note: unit is nullable in the migration (no NOT NULL constraint), so unit: string | null.

    Create lib/actions/price-book.ts:
    ```typescript
    'use server'

    import { createClient } from '@/lib/supabase/server'
    import { revalidatePath } from 'next/cache'
    import type { PriceBookItemFormValues } from '@/lib/schemas/price-book'

    async function getAuthContext() {
      const supabase = await createClient()
      const { data: claimsData } = await supabase.auth.getClaims()
      const claims = claimsData?.claims ?? null
      if (!claims) return { error: 'Not authenticated' as const }

      const { data: company } = await supabase
        .from('companies')
        .select('id')
        .eq('user_id', claims.sub)
        .single()

      if (!company) return { error: 'No company found' as const }

      return { supabase, company }
    }

    export async function createPriceBookItem(formData: PriceBookItemFormValues) {
      const ctx = await getAuthContext()
      if ('error' in ctx) return { error: ctx.error }
      const { supabase, company } = ctx

      const { data, error } = await supabase
        .from('company_price_book')
        .insert({
          company_id: company.id,
          category: formData.category,
          name: formData.name,
          unit: formData.unit || null,
          unit_price: formData.unit_price,
          notes: formData.notes || null,
        })
        .select()
        .single()

      if (error) return { error: 'Failed to create item. Please try again.' }

      revalidatePath('/settings/price-book')
      return { data }
    }

    export async function updatePriceBookItem(
      itemId: string,
      formData: PriceBookItemFormValues
    ) {
      const ctx = await getAuthContext()
      if ('error' in ctx) return { error: ctx.error }
      const { supabase } = ctx

      const { data, error } = await supabase
        .from('company_price_book')
        .update({
          category: formData.category,
          name: formData.name,
          unit: formData.unit || null,
          unit_price: formData.unit_price,
          notes: formData.notes || null,
        })
        .eq('id', itemId)
        .select()
        .single()

      if (error) return { error: 'Failed to update item. Please try again.' }

      revalidatePath('/settings/price-book')
      return { data }
    }

    export async function deletePriceBookItem(itemId: string) {
      const ctx = await getAuthContext()
      if ('error' in ctx) return { error: ctx.error }
      const { supabase } = ctx

      const { error } = await supabase
        .from('company_price_book')
        .delete()
        .eq('id', itemId)

      if (error) return { error: 'Failed to delete item. Please try again.' }

      revalidatePath('/settings/price-book')
      return { data: { deleted: true } }
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
    TypeScript must compile cleanly for the three new files. Zero errors on these files.
  </verify>
  <acceptance_criteria>
    - lib/schemas/price-book.ts contains `export const priceBookItemSchema` and `export type PriceBookItemFormValues`
    - lib/schemas/price-book.ts contains `z.coerce.number()` (not `z.number()`) for unit_price
    - lib/schemas/price-book.ts unit and notes fields contain `.optional().or(z.literal(''))`
    - lib/queries/price-book.ts contains `export interface PriceBookItem` with `unit: string | null`
    - lib/queries/price-book.ts contains `export async function getPriceBookItems`
    - lib/queries/price-book.ts contains `.from('company_price_book')`
    - lib/actions/price-book.ts starts with `'use server'`
    - lib/actions/price-book.ts exports `createPriceBookItem`, `updatePriceBookItem`, `deletePriceBookItem`
    - lib/actions/price-book.ts contains `revalidatePath('/settings/price-book')`
    - lib/actions/price-book.ts contains `getAuthContext` function with `supabase.auth.getClaims()`
    - `npx tsc --noEmit` exits 0 (no TypeScript errors in the new files)
  </acceptance_criteria>
  <done>
    Schema, query, and server action files created with correct types, patterns, and no TypeScript errors.
  </done>
</task>

</tasks>

<verification>
After Wave 0 completion:
- npx vitest run tests/unit/price-book exits non-zero (RED stubs confirm Nyquist)
- npx tsc --noEmit exits 0 (data layer types compile cleanly)
- All 5 files exist at the paths listed in files_modified
</verification>

<success_criteria>
- tests/unit/price-book/price-book-list.test.tsx has 10 failing stubs (RED — expect.fail)
- tests/unit/schemas/price-book.test.ts has 6 failing stubs (RED — expect.fail)
- lib/schemas/price-book.ts: priceBookItemSchema + PriceBookItemFormValues exported; z.coerce.number() used
- lib/queries/price-book.ts: PriceBookItem interface + getPriceBookItems function exported
- lib/actions/price-book.ts: all 3 server actions exported; 'use server' at top; revalidatePath('/settings/price-book') in each mutation
- Build compiles (npx tsc --noEmit exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/20-price-book-crud-ui/20-01-SUMMARY.md`
</output>
