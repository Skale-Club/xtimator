---
phase: 76.2-settings-admin-persistence-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/apply-migration-76-2.mjs
  - types/database.types.ts
  - lib/actions/settings.ts
  - app/admin/companies/actions.ts
  - components/settings/delivery-settings-form.tsx
  - components/settings/estimate-terms-form.tsx
autonomous: false
requirements:
  - SETTINGS-FIX-01
  - SETTINGS-FIX-02
  - SETTINGS-FIX-03
  - SETTINGS-FIX-04
  - SETTINGS-FIX-05

must_haves:
  truths:
    - "All 6 columns (digital_signature_enabled, estimate_terms_enabled, estimate_terms_text, email_delivery_enabled, sms_delivery_enabled, ai_model_override) exist in the companies table in Supabase"
    - "database.types.ts Database['public']['Tables']['companies']['Row'] contains all 6 columns"
    - "npx tsc --noEmit exits 0 with no source-level errors"
    - "updateDeliverySettings, updateEstimateTerms, and setCompanyModelOverride console.error log on DB failure before returning the error value"
    - "All 3 settings pages persist form values across hard refresh"
  artifacts:
    - path: "scripts/apply-migration-76-2.mjs"
      provides: "Node/pg script that applies 3 pending migrations idempotently"
    - path: "types/database.types.ts"
      provides: "Authoritative generated TypeScript types with all 6 columns in companies Row"
    - path: "lib/actions/settings.ts"
      provides: "updateDeliverySettings and updateEstimateTerms with console.error logging before error return"
    - path: "app/admin/companies/actions.ts"
      provides: "setCompanyModelOverride with console.error logging before error return"
  key_links:
    - from: "lib/actions/settings.ts"
      to: "companies table"
      via: "supabase.from('companies').update({ email_delivery_enabled, sms_delivery_enabled, digital_signature_enabled })"
      pattern: "email_delivery_enabled|sms_delivery_enabled|digital_signature_enabled"
    - from: "lib/actions/settings.ts"
      to: "companies table"
      via: "supabase.from('companies').update({ estimate_terms_enabled, estimate_terms_text })"
      pattern: "estimate_terms_enabled|estimate_terms_text"
    - from: "app/admin/companies/actions.ts"
      to: "companies table"
      via: "svc.from('companies').update({ ai_model_override })"
      pattern: "ai_model_override"
---

<objective>
Apply 3 pending migrations to Supabase, regenerate TypeScript types, add error logging to server actions, and confirm all settings pages persist correctly.

Purpose: 6 columns were added to the codebase via migrations that were never applied to the Supabase project. Postgres silently rejects writes to non-existent columns, causing settings to appear to save but never persist. The code and types are structurally correct — only the DB schema is behind.

Output: Supabase database with all 6 columns, authoritative database.types.ts, server actions with visible error logging, verified persistence across 3 settings pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76.2-settings-admin-persistence-fix/DEBUG-CONTEXT.md
@.planning/phases/76.2-settings-admin-persistence-fix/76.2-VALIDATION.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Migration apply pattern from scripts/apply-migration-76-01.mjs: -->
<!-- pg.Client with DATABASE_URL + port :5432 (session-mode pooler) -->
<!-- Check supabase_migrations.schema_migrations before applying (idempotent) -->
<!-- BEGIN/COMMIT transaction, INSERT migration record on success -->

Migrations to apply in order:
1. supabase/migrations/20260519000002_digital_signature_and_estimate_terms.sql
   - ALTERs companies: digital_signature_enabled BOOL NOT NULL DEFAULT FALSE,
     estimate_terms_enabled BOOL NOT NULL DEFAULT FALSE, estimate_terms_text TEXT
   - Creates estimate_signatures table + RLS policies
2. supabase/migrations/20260519000003_estimate_deliveries.sql
   - ALTERs companies: email_delivery_enabled BOOL NOT NULL DEFAULT FALSE,
     sms_delivery_enabled BOOL NOT NULL DEFAULT FALSE
   - Creates estimate_deliveries table + RLS policies
3. supabase/migrations/20260520000001_companies_ai_model_override.sql
   - ALTERs companies: ai_model_override TEXT NULL + index

Current lib/actions/settings.ts state:
- updateDeliverySettings (lines 186-210): returns { error } on DB failure, no console.error — needs logging added
- updateEstimateTerms (lines 162-183): returns { error } on DB failure, no console.error — needs logging added

Current app/admin/companies/actions.ts state:
- setCompanyModelOverride: returns { ok: false, message } on DB failure, no console.error — needs logging added

types/database.types.ts: already manually extended with all 6 columns — regenerate after migrations to get authoritative file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run migration script for all 3 pending migrations</name>
  <files>scripts/apply-migration-76-2.mjs</files>
  <read_first>
    - scripts/apply-migration-76-01.mjs (reference pattern)
    - supabase/migrations/20260519000002_digital_signature_and_estimate_terms.sql
    - supabase/migrations/20260519000003_estimate_deliveries.sql
    - supabase/migrations/20260520000001_companies_ai_model_override.sql
  </read_first>
  <action>
Create `scripts/apply-migration-76-2.mjs` following the exact pattern from `scripts/apply-migration-76-01.mjs`, extended to apply all 3 pending migrations in sequence. Each migration block must:
1. Query `supabase_migrations.schema_migrations` for its version — skip if already present (idempotent)
2. Execute the SQL file content in a `BEGIN`/`COMMIT` transaction
3. `INSERT INTO supabase_migrations.schema_migrations (version, name, statements) VALUES ($1, $2, $3)` on success

Migrations to apply in order:
- version `20260519000002`, name `digital_signature_and_estimate_terms`
- version `20260519000003`, name `estimate_deliveries`
- version `20260520000001`, name `companies_ai_model_override`

Use `const sessionUrl = dbUrl.replace(/:6543\b/, ':5432')` to force session-mode pooler (matches reference pattern). After writing the script, run: `node scripts/apply-migration-76-2.mjs`

After the script succeeds, run this verification SQL via the Supabase MCP `execute_sql` tool:
```sql
SELECT column_name
FROM information_schema.columns
WHERE table_name = 'companies'
  AND column_name IN (
    'digital_signature_enabled','estimate_terms_enabled','estimate_terms_text',
    'email_delivery_enabled','sms_delivery_enabled','ai_model_override'
  )
ORDER BY column_name;
```
Must return exactly 6 rows. If fewer, inspect which migration failed before proceeding to Task 2.
  </action>
  <verify>
    <automated>node scripts/apply-migration-76-2.mjs</automated>
  </verify>
  <done>Script exits 0; each of the 3 migrations logged as "applied" or "already recorded — skipping"; Supabase MCP SELECT returns 6 rows for the 6 column names</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate database.types.ts, add console.error to all 3 settings actions, fix form component types</name>
  <files>types/database.types.ts, lib/actions/settings.ts, app/admin/companies/actions.ts, components/settings/delivery-settings-form.tsx, components/settings/estimate-terms-form.tsx</files>
  <read_first>
    - types/database.types.ts (verify all 6 columns present after regeneration)
    - lib/actions/settings.ts (lines 162-213 — updateEstimateTerms and updateDeliverySettings)
    - app/admin/companies/actions.ts (find setCompanyModelOverride error return path)
    - components/settings/delivery-settings-form.tsx (check for type errors against companies columns)
    - components/settings/estimate-terms-form.tsx (check for type errors against companies columns)
  </read_first>
  <action>
**Step A — Regenerate types (SETTINGS-FIX-02):**

Use the Supabase MCP `generate_typescript_types` tool to regenerate `types/database.types.ts`. Write the full output to `types/database.types.ts`, replacing the current file.

After writing, confirm all 6 columns appear in the `companies` `Row` block:
`digital_signature_enabled: boolean`, `estimate_terms_enabled: boolean`, `estimate_terms_text: string | null`, `email_delivery_enabled: boolean`, `sms_delivery_enabled: boolean`, `ai_model_override: string | null`

If any column is missing, stop — Task 1 migration may not have applied. Do not continue until all 6 are present.

After regenerating, proactively check for TypeScript errors in the two form components that reference these columns:
```
npx tsc --noEmit 2>&1 | grep -E "delivery-settings-form|estimate-terms-form"
```
If errors appear, read the flagged lines in each component and fix them — most likely causes are a property name mismatch (e.g. `email_delivery` vs `email_delivery_enabled`) or a type mismatch (`boolean | undefined` vs `boolean`). Fix only the erroring lines; do not refactor the components.

**Step B — Add console.error logging and verify toast surface (SETTINGS-FIX-04):**

In `lib/actions/settings.ts`, add one `console.error` line before each DB error return in these two functions:

In `updateEstimateTerms` — immediately before `return { error: 'Failed to save estimate terms. Please try again.' }`:
```typescript
console.error('[settings] updateEstimateTerms DB error:', error)
```

In `updateDeliverySettings` — immediately before `return { error: 'Failed to save delivery settings. Please try again.' }`:
```typescript
console.error('[settings] updateDeliverySettings DB error:', error)
```

In `app/admin/companies/actions.ts`, find `setCompanyModelOverride`. Locate the error branch that returns `{ ok: false, message }` (or equivalent). Immediately before that return statement, add:
```typescript
console.error('[admin] setCompanyModelOverride DB error:', error)
```

Do not change any other logic, signatures, or return values in any of the three functions.

**Step B (sub-step) — Confirm form components surface errors as visible toast/alert:**

Read `components/settings/delivery-settings-form.tsx` and `components/settings/estimate-terms-form.tsx`. For each component, verify the `onSubmit` handler both:
1. Checks the error return value from the server action (e.g. `if (result.error)`)
2. Displays it to the user as a visible toast or alert (e.g. `toast.error(result.error)`)

At the time this plan was written, both components already satisfy this: each calls `toast.error(result.error)` in the error branch and `toast.success(...)` in the success branch. Confirm this is still true after any type fixes applied in Step A.

If either component silently ignores the error return (no `if (result.error)` check, or no visible feedback call), add the missing display path — the expected pattern is:
```typescript
if (result.error) {
  toast.error(result.error)
} else {
  toast.success('...')
}
```
Do not change anything else in the component.

**Step C — TypeScript check (SETTINGS-FIX-03):**

Run `npx tsc --noEmit`. The only acceptable errors are `.next/dev/types/validator.ts` lines referencing missing route `.js` files — those are pre-existing generated artifacts. No source-level errors may remain.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v ".next/dev/types"</automated>
    <automated>grep -c "console\.error" lib/actions/settings.ts app/admin/companies/actions.ts</automated>
  </verify>
  <done>types/database.types.ts companies.Row contains all 6 columns; lib/actions/settings.ts contains console.error lines for both updateDeliverySettings and updateEstimateTerms; app/admin/companies/actions.ts contains console.error in setCompanyModelOverride; delivery-settings-form.tsx and estimate-terms-form.tsx surface server action errors as visible toast/alert (confirmed present — both use toast.error(result.error) in the error branch); delivery-settings-form.tsx and estimate-terms-form.tsx have no tsc errors; tsc produces no source-level errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual persistence verification — 3 settings pages</name>
  <files>none</files>
  <action>Run `npm run dev` and manually verify each page listed in how-to-verify. No code changes needed — this task confirms the fix works end-to-end in the browser.</action>
  <verify>
    <automated>MANUAL — see how-to-verify below</automated>
  </verify>
  <done>All 3 affected settings pages persist values after save + hard refresh with no regressions on other settings pages</done>
  <what-built>
    All 3 pending migrations applied to Supabase. companies table now has all 6 columns. database.types.ts regenerated to match. console.error logging added to updateDeliverySettings and updateEstimateTerms in lib/actions/settings.ts, and to setCompanyModelOverride in app/admin/companies/actions.ts. Form component type errors (if any) resolved. Both delivery-settings-form.tsx and estimate-terms-form.tsx confirmed to surface server action errors as visible toast/alert.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and test each page:

    **Page 1: /settings/delivery**
    1. Open http://localhost:3000/settings/delivery
    2. Toggle "Digital signature" ON, "Email delivery" ON, "SMS delivery" ON
    3. Click "Save Delivery Settings" — expect green success toast
    4. Hard refresh (Ctrl+Shift+R or Cmd+Shift+R)
    5. PASS: All 3 toggles must still be ON

    **Page 2: /settings/estimate-templates**
    1. Open http://localhost:3000/settings/estimate-templates
    2. Toggle "Enable estimate terms" ON
    3. Type into the terms textarea: "Payment due within 30 days."
    4. Click "Save Terms" — expect green success toast
    5. Hard refresh
    6. PASS: Toggle still ON, textarea still contains "Payment due within 30 days."

    **Page 3: /admin/companies/[id]**
    1. Open http://localhost:3000/admin/companies
    2. Click any company to open its detail page
    3. Set AI model override field to: `anthropic/claude-sonnet-4`
    4. Save — expect success message
    5. Hard refresh
    6. PASS: Model override field still shows `anthropic/claude-sonnet-4`

    **Regression check:**
    Open http://localhost:3000/settings — confirm general company settings (name, address) still save and reload correctly.
  </how-to-verify>
  <resume-signal>Type "verified" if all 3 pages persist correctly, or describe which page failed and what happened (include any error toasts or console output)</resume-signal>
</task>

</tasks>

<verification>
Phase complete when:
- `node scripts/apply-migration-76-2.mjs` exits 0 — 3 migrations applied (SETTINGS-FIX-01)
- Supabase MCP SELECT returns 6 rows for the 6 column names (SETTINGS-FIX-01)
- `npx tsc --noEmit 2>&1 | grep -v ".next/dev/types"` produces no output (SETTINGS-FIX-02, SETTINGS-FIX-03)
- `grep -c "console\.error" lib/actions/settings.ts app/admin/companies/actions.ts` returns at least 1 match per file (SETTINGS-FIX-04)
- Manual: all 3 pages verified by user (SETTINGS-FIX-05)
</verification>

<success_criteria>
- SETTINGS-FIX-01: information_schema.columns returns 6 rows for the 6 column names on companies table
- SETTINGS-FIX-02: database.types.ts companies.Row contains all 6 columns after regeneration
- SETTINGS-FIX-03: npx tsc --noEmit exits 0 with no source-level TypeScript errors (including delivery-settings-form.tsx and estimate-terms-form.tsx)
- SETTINGS-FIX-04: updateDeliverySettings, updateEstimateTerms (lib/actions/settings.ts) and setCompanyModelOverride (app/admin/companies/actions.ts) all log console.error on DB failure; delivery-settings-form.tsx and estimate-terms-form.tsx surface server action errors as visible toast/alert
- SETTINGS-FIX-05: /settings/delivery, /settings/estimate-templates, /admin/companies/[id] all persist after save + hard refresh
</success_criteria>

<output>
After completion, create `.planning/phases/76.2-settings-admin-persistence-fix/76.2-01-SUMMARY.md`
</output>
