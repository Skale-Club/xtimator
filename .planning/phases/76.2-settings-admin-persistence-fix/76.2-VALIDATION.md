---
phase: 76.2
slug: settings-admin-persistence-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-21
---

# Phase 76.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | TypeScript compiler (tsc) + Supabase MCP SQL |
| **Config file** | tsconfig.json |
| **Quick run command** | `npx tsc --noEmit 2>&1 | head -20` |
| **Full suite command** | `npm run test -- --run` |
| **Estimated runtime** | ~10 seconds (tsc) |

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit`
- **After every plan wave:** Run `npm run test -- --run`
- **Before `/gsd:verify-work`:** Full suite must be green + manual persistence verification
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 76.2-01-01 | 01 | 1 | SETTINGS-FIX-01 | sql | Supabase MCP: SELECT column_name FROM information_schema.columns WHERE table_name = 'companies' | N/A | ⬜ pending |
| 76.2-01-02 | 01 | 1 | SETTINGS-FIX-02 | type-check | `npx tsc --noEmit` | ✅ | ⬜ pending |
| 76.2-01-03 | 01 | 1 | SETTINGS-FIX-03 | type-check | `npx tsc --noEmit` exits 0 | ✅ | ⬜ pending |
| 76.2-01-04 | 01 | 1 | SETTINGS-FIX-04 | code-review | grep `ok: false` in settings.ts and admin actions | ✅ | ⬜ pending |
| 76.2-01-05 | 01 | 1 | SETTINGS-FIX-05 | manual | Persistence verification for 3 pages | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

No Wave 0 needed — no new test files required. All verification is via `tsc --noEmit` (existing) and manual persistence checks.

*Existing TypeScript infrastructure covers the framework requirement.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `/settings/delivery` persists toggles | SETTINGS-FIX-05 | Requires browser + live DB | Fill form → save → hard refresh → values match |
| `/settings/estimate-templates` persists terms | SETTINGS-FIX-05 | Requires browser + live DB | Fill terms → save → hard refresh → values match |
| `/admin/companies/[id]` persists model override | SETTINGS-FIX-05 | Requires browser + live DB | Set override → save → hard refresh → value matches |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or manual-only documented
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s (tsc)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
