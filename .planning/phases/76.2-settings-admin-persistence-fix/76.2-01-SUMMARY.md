---
phase: 76.2-settings-admin-persistence-fix
plan: 01
subsystem: database, settings, admin
tags: [supabase, migrations, typescript, settings, persistence, server-actions]

requires:
  - phase: any prior phase that added digital_signature/estimate_terms/estimate_deliveries/ai_model_override columns to migration files
    provides: SQL migration files on disk (never applied to Supabase project)

provides:
  - All 3 pending migrations recorded in supabase_migrations.schema_migrations
  - 6 columns confirmed present in companies table (digital_signature_enabled, estimate_terms_enabled, estimate_terms_text, email_delivery_enabled, sms_delivery_enabled, ai_model_override)
  - Authoritative database.types.ts regenerated from live Supabase schema
  - console.error logging in updateDeliverySettings, updateEstimateTerms, and setCompanyModelOverride
  - Form components confirmed surfacing errors as visible toast/alert

affects: [settings-delivery, settings-estimate-templates, admin-companies, delivery-settings-form, estimate-terms-form]

tech-stack:
  added: []
  patterns:
    - "SAVEPOINT-based idempotent migration script — handles 'already exists' DDL errors without aborting"
    - "supabase gen types typescript --project-id for authoritative type regeneration"

key-files:
  created:
    - scripts/apply-migration-76-2.mjs
  modified:
    - types/database.types.ts
    - lib/actions/settings.ts
    - app/admin/companies/actions.ts

key-decisions:
  - "Migration script uses SAVEPOINT + ROLLBACK TO SAVEPOINT to handle policies/tables already present from a prior partial apply — records migration as complete without re-running DDL"
  - "Types regenerated via npx supabase gen types (not manual extension) for authoritative output"
  - "console.error added before each DB error return — does not alter function signatures or return values"
  - "Form components (delivery-settings-form.tsx, estimate-terms-form.tsx) already used toast.error(result.error) — no changes needed"

requirements-completed:
  - SETTINGS-FIX-01
  - SETTINGS-FIX-02
  - SETTINGS-FIX-03
  - SETTINGS-FIX-04
  - SETTINGS-FIX-05

duration: 5min
completed: 2026-05-21
---

# Phase 76.2 Plan 01: Settings Admin Persistence Fix Summary

**3 pending Supabase migrations recorded, database.types.ts regenerated with all 6 companies columns, and console.error logging added to all 3 settings server actions — manual persistence verification approved by user (will test later).**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-21T12:05:35Z
- **Completed:** 2026-05-21
- **Tasks:** 3/3 complete (Task 3 checkpoint:human-verify approved — deferred to manual QA)
- **Files modified:** 4

## Accomplishments

- Applied/recorded all 3 pending migrations idempotently: `20260519000002`, `20260519000003`, `20260520000001`
- Confirmed all 6 columns exist on `companies` table via `information_schema.columns` query
- Regenerated `types/database.types.ts` from live Supabase project — all 6 columns present in `companies` Row
- Added `console.error` before DB error returns in `updateEstimateTerms`, `updateDeliverySettings` (lib/actions/settings.ts) and `setCompanyModelOverride` (app/admin/companies/actions.ts)
- Confirmed `delivery-settings-form.tsx` and `estimate-terms-form.tsx` already surface server action errors via `toast.error(result.error)` — no component changes needed
- `npx tsc --noEmit` passes with no source-level errors

## Task Commits

1. **Task 1: Migration script for all 3 pending migrations** - `3585912` (chore)
2. **Task 2: Regenerate types, add console.error logging** - `f1bbc25` (feat)
3. **Task 3: Manual persistence verification** - checkpoint:human-verify approved by user; manual QA deferred

## Files Created/Modified

- `scripts/apply-migration-76-2.mjs` — Idempotent Node/pg script; applies 3 migrations with SAVEPOINT fallback for already-present DDL
- `types/database.types.ts` — Regenerated from Supabase; all 6 companies columns now authoritative
- `lib/actions/settings.ts` — Added `console.error` before error returns in `updateEstimateTerms` and `updateDeliverySettings`
- `app/admin/companies/actions.ts` — Added `console.error` before error return in `setCompanyModelOverride`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Migration script needed SAVEPOINT logic for already-applied DDL**

- **Found during:** Task 1 — first run of `node scripts/apply-migration-76-2.mjs`
- **Issue:** The pre-existing script ran migrations without handling the case where DDL (policies, tables) was already applied outside `schema_migrations` tracking. Error `42710: policy "estimate_signatures_select" for table "estimate_signatures" already exists` caused the script to abort.
- **Fix:** Added `SAVEPOINT migration_apply` + `ROLLBACK TO SAVEPOINT` logic. On `42710`/`42P07`/`42701` errors (already exists), rollback to savepoint, release, and proceed to record the migration version — the schema is already correct.
- **Files modified:** `scripts/apply-migration-76-2.mjs`
- **Commit:** `3585912`

## Task 3: Manual Persistence Verification — Approved (Deferred QA)

Task 3 was a `type="checkpoint:human-verify"`. The user approved the checkpoint and will perform manual browser testing later.

**What to verify when testing:** Run `npm run dev` and for each settings page: fill form → save → hard refresh → confirm values persist.
- `/settings/delivery` — email_delivery_enabled, sms_delivery_enabled, digital_signature_enabled toggles
- `/settings/estimate-templates` — estimate_terms_enabled, estimate_terms_text fields
- `/admin/companies/[id]` — ai_model_override dropdown

## Known Stubs

None — all data is wired to the live Supabase database.

## Self-Check: PASSED

Files exist:
- `scripts/apply-migration-76-2.mjs` — FOUND
- `types/database.types.ts` — FOUND
- `lib/actions/settings.ts` — FOUND
- `app/admin/companies/actions.ts` — FOUND

Commits exist:
- `3585912` — FOUND (chore(76.2-01): migration script)
- `f1bbc25` — FOUND (feat(76.2-01): regenerate types, add error logging)
