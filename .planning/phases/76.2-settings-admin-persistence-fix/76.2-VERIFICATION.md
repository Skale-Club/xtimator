---
phase: 76.2-settings-admin-persistence-fix
verified: 2026-05-21T00:00:00Z
status: human_needed
score: 4/5 must-haves verified
re_verification: false
human_verification:
  - test: "Persistence check — /settings/delivery"
    expected: "Toggle Digital signature ON, Email delivery ON, SMS delivery ON → Save → Hard refresh → all 3 toggles still ON"
    why_human: "Requires browser + live Supabase session; cannot verify toggle state or DB round-trip via code inspection alone"
  - test: "Persistence check — /settings/estimate-templates"
    expected: "Toggle Enable estimate terms ON, type 'Payment due within 30 days.' → Save → Hard refresh → toggle still ON, textarea still contains the text"
    why_human: "Requires browser + live Supabase session"
  - test: "Persistence check — /admin/companies/[id]"
    expected: "Set AI model override to 'anthropic/claude-sonnet-4' → Save → Hard refresh → field still shows the value"
    why_human: "Requires browser + live Supabase session and an admin-role user"
  - test: "Regression check — /settings general company settings"
    expected: "Company name and address still save and reload correctly after this phase's changes"
    why_human: "Requires browser + live Supabase session"
---

# Phase 76.2: Settings Admin Persistence Fix — Verification Report

**Phase Goal:** Every field in every settings page and admin panel actually persists to the database after save + refresh. Root cause: 3 migrations never applied to Supabase, database.types.ts not regenerated, TypeScript errors in server actions.
**Verified:** 2026-05-21
**Status:** human_needed — 4/5 automated truths verified; SETTINGS-FIX-05 requires browser testing
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 6 columns exist in the companies table in Supabase | VERIFIED | `node scripts/apply-migration-76-2.mjs` ran live: all 3 migrations "already recorded — skipping"; column check returned 6/6: `ai_model_override, digital_signature_enabled, email_delivery_enabled, estimate_terms_enabled, estimate_terms_text, sms_delivery_enabled` |
| 2 | database.types.ts companies.Row contains all 6 columns with correct types | VERIFIED | `types/database.types.ts` lines 163–193: `ai_model_override: string \| null`, `digital_signature_enabled: boolean`, `email_delivery_enabled: boolean`, `estimate_terms_enabled: boolean`, `estimate_terms_text: string \| null`, `sms_delivery_enabled: boolean` — all present in Row, Insert, and Update blocks |
| 3 | `npx tsc --noEmit` exits 0 with no source-level errors | VERIFIED | Command produced no output (empty stdout) when filtering `.next/dev/types` lines |
| 4 | updateDeliverySettings, updateEstimateTerms, and setCompanyModelOverride log console.error on DB failure before returning the error value | VERIFIED | `lib/actions/settings.ts` line 179: `console.error('[settings] updateEstimateTerms DB error:', error)` immediately before return; line 206: `console.error('[settings] updateDeliverySettings DB error:', error)` immediately before return. `app/admin/companies/actions.ts` line 52: `console.error('[admin] setCompanyModelOverride DB error:', error)` immediately before return. All 3 are placed inside the `if (error)` block, before the return statement. |
| 5 | All 3 settings pages persist form values across hard refresh | NEEDS HUMAN | Cannot verify browser state or DB round-trip programmatically — see Human Verification Required section |

**Score:** 4/5 automated truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/apply-migration-76-2.mjs` | Node/pg script that applies 3 pending migrations idempotently | VERIFIED | File exists, 130 lines, full SAVEPOINT logic, queries `supabase_migrations.schema_migrations` before each migration, runs live column check at end |
| `types/database.types.ts` | Authoritative generated TypeScript types with all 6 columns in companies Row | VERIFIED | All 6 columns present with correct types in Row (non-optional) and Insert/Update (optional) blocks |
| `lib/actions/settings.ts` | updateDeliverySettings and updateEstimateTerms with console.error logging before error return | VERIFIED | Both functions write all relevant columns and have console.error in the error branch before the return |
| `app/admin/companies/actions.ts` | setCompanyModelOverride with console.error logging before error return | VERIFIED | Line 52 has console.error before `return { ok: false, message: error.message }` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `lib/actions/settings.ts` | companies table | `supabase.from('companies').update({ email_delivery_enabled, sms_delivery_enabled, digital_signature_enabled })` | WIRED | Lines 197–203: all 3 delivery columns written in the update call |
| `lib/actions/settings.ts` | companies table | `supabase.from('companies').update({ estimate_terms_enabled, estimate_terms_text })` | WIRED | Lines 172–175: both terms columns written in the update call |
| `app/admin/companies/actions.ts` | companies table | `svc.from('companies').update({ ai_model_override })` | WIRED | Line 48: `ai_model_override` written directly in the update call |
| `components/settings/delivery-settings-form.tsx` | `lib/actions/settings.ts` | `import { updateDeliverySettings }` + call in onSubmit | WIRED | Line 9 import, line 41 call; error branch calls `toast.error(result.error)` |
| `components/settings/estimate-terms-form.tsx` | `lib/actions/settings.ts` | `import { updateEstimateTerms }` + call in onSubmit | WIRED | Line 9 import, line 40 call; error branch calls `toast.error(result.error)` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `delivery-settings-form.tsx` | `email_delivery_enabled`, `sms_delivery_enabled`, `digital_signature_enabled` | `updateDeliverySettings` server action → Supabase `companies.update` | Yes — direct DB write, columns confirmed live in schema | FLOWING |
| `estimate-terms-form.tsx` | `estimate_terms_enabled`, `estimate_terms_text` | `updateEstimateTerms` server action → Supabase `companies.update` | Yes — direct DB write, columns confirmed live in schema | FLOWING |
| `app/admin/companies/actions.ts` | `ai_model_override` | `setCompanyModelOverride` → Supabase `companies.update` | Yes — direct DB write, column confirmed live in schema | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Migration script exits 0, all 3 migrations already recorded | `node scripts/apply-migration-76-2.mjs` (with DATABASE_URL) | "already recorded — skipping" x3; "Column verification passed: 6/6 columns present" | PASS |
| TSC compiles clean | `npx tsc --noEmit \| grep -v ".next/dev/types"` | No output (exit 0) | PASS |
| console.error count per file | `grep -c "console.error" lib/actions/settings.ts app/admin/companies/actions.ts` | settings.ts: 2, actions.ts: 1 | PASS |
| Form components surface errors as toast | grep `toast.error(result.error)` in both form components | delivery-settings-form.tsx line 43, estimate-terms-form.tsx line 42 | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SETTINGS-FIX-01 | 76.2-01 | information_schema.columns returns 6 rows for the 6 column names on companies table | SATISFIED | Live script run: 6/6 columns confirmed; all 3 migration versions recorded in schema_migrations |
| SETTINGS-FIX-02 | 76.2-01 | database.types.ts companies.Row contains all 6 columns after regeneration | SATISFIED | `types/database.types.ts` lines 163–193: all 6 columns with correct types |
| SETTINGS-FIX-03 | 76.2-01 | npx tsc --noEmit exits 0 with no source-level TypeScript errors | SATISFIED | Command produced no output |
| SETTINGS-FIX-04 | 76.2-01 | updateDeliverySettings, updateEstimateTerms (lib/actions/settings.ts) and setCompanyModelOverride (app/admin/companies/actions.ts) all log console.error on DB failure; form components surface errors as visible toast/alert | SATISFIED | console.error confirmed at lines 179, 206 (settings.ts) and 52 (companies/actions.ts); toast.error confirmed in both form components |
| SETTINGS-FIX-05 | 76.2-01 | /settings/delivery, /settings/estimate-templates, /admin/companies/[id] all persist after save + hard refresh | NEEDS HUMAN | Code-level prerequisites all verified; actual persistence across browser sessions requires manual testing |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

No TODO/FIXME/placeholder comments, empty return stubs, or hardcoded empty data detected in any of the 4 modified files.

---

### Human Verification Required

#### 1. Persistence check — /settings/delivery

**Test:** Run `npm run dev`. Open `http://localhost:3000/settings/delivery`. Toggle "Digital signature" ON, "Email delivery" ON, "SMS delivery" ON. Click "Save Delivery Settings". Expect a green success toast. Hard refresh (Ctrl+Shift+R). Verify all 3 toggles are still ON.
**Expected:** All 3 toggles remain ON after hard refresh.
**Why human:** Requires browser + live Supabase session to verify the round-trip DB write and UI reload.

#### 2. Persistence check — /settings/estimate-templates

**Test:** Open `http://localhost:3000/settings/estimate-templates`. Toggle "Enable estimate terms" ON. Type "Payment due within 30 days." into the terms textarea. Click "Save Terms". Expect a green success toast. Hard refresh. Verify toggle is still ON and textarea still contains the text.
**Expected:** Toggle ON and textarea text preserved after hard refresh.
**Why human:** Requires browser + live Supabase session.

#### 3. Persistence check — /admin/companies/[id]

**Test:** Open `http://localhost:3000/admin/companies`. Click any company. Set AI model override field to `anthropic/claude-sonnet-4`. Save. Expect success message. Hard refresh. Verify the field still shows `anthropic/claude-sonnet-4`.
**Expected:** Model override field value persists after hard refresh.
**Why human:** Requires browser + live Supabase session and admin-role user.

#### 4. Regression check — /settings general settings

**Test:** Open `http://localhost:3000/settings`. Edit the company name or address field. Save. Hard refresh. Verify the values still match what was saved.
**Expected:** General settings (name, address) continue to persist correctly — no regression introduced by this phase.
**Why human:** Requires browser + live Supabase session.

---

### Gaps Summary

No automated gaps found. All code-verifiable requirements (SETTINGS-FIX-01 through SETTINGS-FIX-04) are satisfied:

- The 3 Supabase migrations are live and all 6 companies columns confirmed present via a live DB query.
- `types/database.types.ts` was regenerated from the live schema — all 6 columns have correct TypeScript types.
- `npx tsc --noEmit` exits clean with no source-level errors.
- All 3 server actions have `console.error` logging correctly positioned before their error returns.
- Both form components import their server actions, call them in `onSubmit`, and surface errors via `toast.error(result.error)`.

The only outstanding item is SETTINGS-FIX-05, which by design requires a browser and cannot be verified by code inspection. The infrastructure for persistence is fully in place — whether the end-to-end flow works in the browser is the final gate.

---

_Verified: 2026-05-21_
_Verifier: Claude (gsd-verifier)_
