---
phase: 167-billing-cost-integrity
plan: 02
type: execute
wave: 3
depends_on:
  - "167-01"
  - "168-01"
files_modified:
  - lib/inngest/functions/analyze-photos.ts
  - tests/unit/inngest/analyze-photos-cost.test.ts
autonomous: true
requirements:
  - BILL-03
must_haves:
  truths:
    - "The per-photo analyzePhotoOR call in the analyze-photos worker passes a costContext of {attemptId, companyId, projectId} from the job payload — so each vision ai_cost_events row carries the JOB's attemptId (not a random uuid) and the real company/project ids."
    - "The photo_batch record-credit-debit read-back (which sums ai_cost_events by attempt_id) now finds the vision rows and records the real summed cost instead of a permanently-null read."
    - "The partial unique index from 167-01 (WHERE operation_type IN ('audio_minutes','estimate')) does NOT constrain these vision rows — a multi-photo batch legitimately writes several 'vision' rows sharing one attemptId, which the read-back sums into one photo_batch debit."
    - "multimodal.ts (refine ingest) is deliberately OUT of scope: MultimodalRawInput carries no attemptId/companyId/projectId and refine has no photo credit-debit, so threading correlation ids there would be an unbudgeted change across every caller (whatsapp/media, agent-tools/normalize-input) for ZERO measurement impact — documented, not done (Opus check)."
  artifacts:
    - path: "lib/inngest/functions/analyze-photos.ts"
      provides: "costContext threaded into the per-photo vision call"
  key_links:
    - from: "lib/inngest/functions/analyze-photos.ts"
      to: "lib/ai/openrouter-client.ts analyzePhotoOR"
      via: "analyzePhotoOR(base64, mimeType, undefined, { attemptId, companyId, projectId })  // costContext is the 4th param (model? is 3rd)"
      pattern: "attemptId"
---

<objective>
Close audit finding D4 (BILL-03): the photo-batch cost roll-up is dead code. `analyzePhotoOR` ALREADY accepts a `costContext?` param (lib/ai/openrouter-client.ts:262-266) and records the cost row with `attemptId: costContext?.attemptId ?? randomUUID()` + null company/project when absent (:358-364). But the worker calls it WITHOUT costContext (lib/inngest/functions/analyze-photos.ts:197 `analyzePhotoOR(base64, mimeType)`), so every vision cost row gets a random attemptId + null ids, and the photo_batch credit-debit read-back (:308-326, `.eq('attempt_id', attemptId)`) matches zero rows → records null cost. The fix is to pass the job's costContext at the call site. Small, surgical — no signature change needed.

REQUIRED READING: .planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md § D4. Verified working precedent: the estimate generation path threads costContext correctly (generate.ts → generate-estimate.ts), which is why its identical read-back finds the estimate cost. Vision is the odd one out.

SCOPE FENCE: this runs AFTER 168-01 (which rewrote the analyze-photos chunking). Thread costContext into the post-168-01 per-photo call inside the chunk. Do NOT touch the transcription section (167-01's) or the vision truncation logic (168-01's) — only add the costContext argument at the call site(s).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md
@lib/inngest/functions/analyze-photos.ts
@lib/ai/openrouter-client.ts
@lib/estimate/ingest/multimodal.ts

<interfaces>
The worker already has `companyId`, `projectId` (analyze-photos.ts:118) and `attemptId` (:120) in closure scope — the per-photo call at :197 is nested in the same handler, so all three are reachable after 168-01's chunk rewrite (Opus-confirmed). analyzePhotoOR's real signature is `(base64, mimeType, model?: string, costContext?: CostContext)` — costContext is the **4th** param; CostContext = `{ attemptId?, companyId?, projectId? }` (openrouter-client.ts:24-28). Pass it correctly:
```ts
const description = await analyzePhotoOR(base64, mimeType, undefined, { attemptId, companyId, projectId })
```
recordAICost is awaited INSIDE the vision step (openrouter-client.ts:357), so each cost row commits before its vision-${photo.id} step returns; the record-credit-debit step (:308-327) runs strictly after the whole chunk loop with its own 3×150ms bounded retry — no read-back race (Opus-confirmed). Verify the read-back, don't rewrite it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread costContext into the vision call(s) + prove the read-back finds rows</name>
  <files>lib/inngest/functions/analyze-photos.ts, lib/estimate/ingest/multimodal.ts, tests/unit/inngest/analyze-photos-cost.test.ts</files>
  <read_first>
    - lib/inngest/functions/analyze-photos.ts (FULL, post-168-01 — the per-photo call inside the chunk + the record-credit-debit step :300-326)
    - lib/ai/openrouter-client.ts:262-370 (analyzePhotoOR + its recordAICost with costContext) — confirm the CostContext shape
    - lib/estimate/ingest/multimodal.ts (the refine vision call)
  </read_first>
  <action>
1. Pass `{ attemptId, companyId, projectId }` to the analyzePhotoOR call in the analyze-photos worker (inside the chunked step, per photo).
2. multimodal.ts: pass available costContext to its analyzePhotoOR call.
3. Test: mock recordAICost + the ai_cost_events read; assert the per-photo vision rows are written with the job attemptId (not random), and the record-credit-debit read-back sums a non-null realCostUsd for a batch of 2 photos (the exact dead-code path the audit flagged). Assert a batch shares ONE attemptId across N vision rows and the partial unique index (167-01) does not reject them (they're 'vision' op, outside the index predicate).
  </action>
  <acceptance_criteria>
    - Vision cost rows carry the job attemptId + real company/project (test asserts)
    - photo_batch debit records non-null summed cost for a multi-photo batch (test asserts the previously-null read now returns a value)
    - `npm test` GREEN; `npx tsc --noEmit -p tsconfig.ci.json` exits 0
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/inngest/analyze-photos-cost.test.ts</automated></verify>
  <done>Photo-batch cost is real, not permanently null.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/inngest/analyze-photos-cost.test.ts
npx tsc --noEmit -p tsconfig.ci.json
npm test
```
Regression contracts: 168-01's coverage/skip-continue/truncation untouched; 167-01's transcription + partial index untouched; per-photo step.run checkpointing preserved; cross-tenant scoping preserved.
</verification>

<success_criteria>
- Phase 167 criterion 3 (BILL-03) TRUE: vision cost context threaded, photo_batch debit records real cost. Phase 167 fully complete (2/2 plans).
</success_criteria>

<output>
Create `.planning/phases/167-billing-cost-integrity/167-02-SUMMARY.md`: the call sites changed, the read-back verification, and confirmation the partial index leaves vision rows unconstrained. Mark BILL-03 complete in REQUIREMENTS.md.
</output>
