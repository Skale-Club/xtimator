---
phase: 61-production-database-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "61-03"
files_modified: []
autonomous: false
requirements:
  - PROD-DB-03
  - PROD-DB-04
gap_closure: false

must_haves:
  truths:
    - "The platform_admins table contains exactly 1 row matching skale.club@gmail.com (verified by JOIN with auth.users)"
    - "The invited admin has confirmed receipt of the invitation email (so they can sign in once Phase 62 deploys the app)"
    - "Supabase free-tier daily backups are confirmed enabled in the Dashboard with at least 2-day retention"
    - "PROD-DB-04 (PITR) is explicitly documented as DEFERRED for v3.1 per D-01 — the deferral is recorded for v3.2/paid-tier upgrade"
  artifacts: []
  key_links:
    - from: "auth.users (skale.club@gmail.com)"
      to: "platform_admins"
      via: "seed migration FK insert"
      pattern: "user_id = auth.users.id"
---

<objective>
Finalize PROD-DB-03 by re-confirming the super-admin row, optionally re-running the seed migration if it was a no-op, and documenting PROD-DB-04 backup posture (free-tier daily snapshots — PITR explicitly deferred per D-01).

Purpose: Wave 3 (per Nyquist contract). Plan 03 already ran the seed migration, but this plan is the **authoritative verification step** for PROD-DB-03 and the **deferral acknowledgment** for PROD-DB-04. Includes the fallback re-seed procedure in case Plan 02 Task 2 was skipped or out-of-order.

Output: Confirmed super-admin seed + documented free-tier backup posture. Zero git changes (the runbook artifact comes in Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-production-database-foundation/61-CONTEXT.md
@.planning/phases/61-production-database-foundation/61-RESEARCH.md
@.planning/phases/61-production-database-foundation/61-VALIDATION.md
@.planning/REQUIREMENTS.md
@supabase/ADMIN-BOOTSTRAP.md

<interfaces>
<!-- Authoritative verification SQL for PROD-DB-03 -->

```sql
-- Should return exactly 1 row with email = skale.club@gmail.com
SELECT pa.user_id, u.email, pa.created_at, pa.notes
FROM platform_admins pa
JOIN auth.users u ON u.id = pa.user_id;
```

-- Fallback re-seed (idempotent — ON CONFLICT DO NOTHING)
```sql
INSERT INTO public.platform_admins (user_id)
SELECT id FROM auth.users WHERE email = 'skale.club@gmail.com'
ON CONFLICT (user_id) DO NOTHING;
```

PROD-DB-04 STATUS: DEFERRED per CONTEXT.md D-01.
- Free tier provides automatic daily backups with 2-day retention by default
- PITR (~$100/mo add-on requiring Pro plan) NOT enabled for v3.1
- Documented for future upgrade in Plan 05's PROD-BOOTSTRAP.md runbook
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify super-admin seed (with fallback re-run if needed)</name>
  <files>(no file changes — SQL verification only)</files>
  <read_first>
    - supabase/migrations/20260503000002_seed_platform_admin.sql (the seed — exact INSERT statement to re-run if needed)
    - supabase/ADMIN-BOOTSTRAP.md (canonical manual bootstrap procedure)
    - .planning/phases/61-production-database-foundation/61-RESEARCH.md (§Pitfall 1 — empty platform_admins fallback)
  </read_first>
  <action>
    Load `PROD_DB_URL` from `.env.production` (Plan 02 / Plan 03 already established this) and run the authoritative verification.

    ```bash
    set -a; source .env.production; set +a
    # PowerShell equivalent in Plan 03 Task 1.

    # Authoritative verification:
    psql "$PROD_DB_URL" -c "
      SELECT pa.user_id, u.email, pa.created_at, pa.notes
      FROM platform_admins pa
      JOIN auth.users u ON u.id = pa.user_id;
    "
    ```

    Expected: exactly 1 row, email = `skale.club@gmail.com`.

    **If 0 rows returned** (Plan 02 Task 2 skipped OR Plan 03 ran seed before invite):
    1. Confirm the auth.users row exists:
       ```bash
       psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM auth.users WHERE email='skale.club@gmail.com';"
       ```
       MUST return 1. If 0: go back to Plan 02 Task 2 and invite the user via Dashboard.

    2. Re-run the seed manually (idempotent — ON CONFLICT DO NOTHING):
       ```bash
       psql "$PROD_DB_URL" -c "
         INSERT INTO public.platform_admins (user_id)
         SELECT id FROM auth.users WHERE email = 'skale.club@gmail.com'
         ON CONFLICT (user_id) DO NOTHING;
       "
       ```

    3. Re-run the authoritative verification (the first command in this task). Confirm 1 row now appears.

    DO NOT paste the user_id UUID anywhere outside terminal output. UUIDs are not as sensitive as DB passwords but are still production data.
  </action>
  <verify>
    <automated>psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM platform_admins pa JOIN auth.users u ON u.id = pa.user_id WHERE u.email='skale.club@gmail.com'"</automated>
  </verify>
  <acceptance_criteria>
    - `psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM platform_admins pa JOIN auth.users u ON u.id = pa.user_id WHERE u.email='skale.club@gmail.com'"` returns `1`
    - `psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM platform_admins"` returns `1` (no other rows)
    - `psql "$PROD_DB_URL" -At -c "SELECT u.email FROM platform_admins pa JOIN auth.users u ON u.id = pa.user_id"` returns `skale.club@gmail.com`
  </acceptance_criteria>
  <done>PROD-DB-03 satisfied: exactly 1 platform_admins row for skale.club@gmail.com, verified by JOIN with auth.users.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Confirm invite email + sign-in pathway (deferred to Phase 62 deploy)</name>
  <files>(none — operational acknowledgment)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (PROD-DB-03 wording: "...and can sign in to /admin once the app is deployed")
  </read_first>
  <what-built>
    PROD-DB-03's literal text requires the super-admin "can sign in to /admin once the app is deployed". Phase 62 is the deploy phase — the actual sign-in test happens there.

    In Phase 61 we verify the foundational requirements:
    1. The invited email exists in auth.users (Plan 02 Task 2 confirmed)
    2. The platform_admins row exists and joins correctly (Plan 04 Task 1 confirmed)
    3. The user has access to the invitation email (this checkpoint)

    Sign-in itself is gated on Phase 62 deployment — out of scope here, but we confirm the invitation was received so the user can act on it once Phase 62 ships.
  </what-built>
  <action>
    Confirm receipt of the Supabase invitation email at `skale.club@gmail.com`:

    1. Open the inbox for `skale.club@gmail.com`.
    2. Confirm an email exists from `noreply@mail.app.supabase.io` (or similar Supabase sender) with subject containing "invited" or "invitation".
    3. DO NOT click the magic link yet — the link is single-use, and clicking before Phase 62 deploys would leave the auth callback redirecting to a non-existent URL (likely `localhost`). Phase 62 reconfigures Site URL + Redirect URLs.
    4. Confirm the email is preserved (not deleted) so it can be acted on after Phase 62.

    Alternative if email is missing: re-send the invite from Dashboard → Authentication → Users → row context menu → "Send invitation email" / "Send magic link".
  </action>
  <how-to-verify>
    1. The invitation email is visible in the inbox of `skale.club@gmail.com`.
    2. Re-confirm the auth.users row still exists:
       ```bash
       psql "$PROD_DB_URL" -At -c "SELECT email FROM auth.users WHERE email='skale.club@gmail.com';"
       ```
       Returns: `skale.club@gmail.com`.
  </how-to-verify>
  <acceptance_criteria>
    - Invitation email received at skale.club@gmail.com (visual inbox confirmation)
    - `psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM auth.users WHERE email='skale.club@gmail.com'"` returns `1`
    - Email NOT yet acted on (magic link unused — preserved for Phase 62)
  </acceptance_criteria>
  <resume-signal>Type "invite-confirmed" when email is visible in inbox. Type "missing — resent" if invite was resent. Type "issue: {description}" otherwise.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Confirm free-tier daily backups + document PITR deferral</name>
  <files>(no file changes — Dashboard verification + deferral acknowledgment)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (PROD-DB-04 literal text — note "DEFERRED to paid tier upgrade. Free tier provides automatic daily backups...")
    - .planning/phases/61-production-database-foundation/61-CONTEXT.md (D-01: Free tier locked, no Pro plan, no PITR add-on)
    - .planning/phases/61-production-database-foundation/61-RESEARCH.md (§Pitfall 3 — PITR conflated with backups)
  </read_first>
  <action>
    Verify that the Free-tier default daily backup is enabled and acknowledge the PITR deferral.

    1. Navigate to Supabase Dashboard for the production project → **Database → Backups**.
    2. Confirm the page shows "Daily backups" with retention of approximately 2 days (Free tier default).
       - Free tier does NOT show a Point-in-Time Recovery slider — only daily snapshot list.
       - This is the EXPECTED state per CONTEXT.md D-01.
    3. The "Upgrade to enable PITR" or similar prompt may be visible — that's fine, do NOT upgrade.

    4. Acknowledge the deferral by capturing the following statement (this is logged in 61-04-SUMMARY.md, not a separate file):

       > **PROD-DB-04 STATUS: DEFERRED for v3.1.**
       > Free tier backup posture: automatic daily snapshots with 2-day retention enabled by default.
       > PITR (~$100/mo add-on on Pro plan $25/mo) NOT enabled. Plan 05 (PROD-BOOTSTRAP.md runbook)
       > will document the upgrade path for v3.2 or as growth/risk justifies.
       > Reference: 61-CONTEXT.md D-01.

    DO NOT take a screenshot containing the project ref or any secret value. If a screenshot is taken, blur or redact those before saving. (Per CONTEXT.md, screenshots are optional — not required for sign-off.)
  </action>
  <verify>
    <automated>echo "manual-dashboard-verification" # PROD-DB-04 deferral is a documented decision, not an automated check</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard → Database → Backups visibly shows daily backups enabled (operator confirms)
    - The plan is NOT upgraded to Pro (Project Settings → Billing still shows "Free Plan")
    - PROD-DB-04 deferral statement is captured for the 61-04-SUMMARY.md (operator will paste during summary writing)
    - The deferral matches REQUIREMENTS.md literal text ("DEFERRED to paid tier upgrade. Free tier provides automatic daily backups...")
  </acceptance_criteria>
  <done>Free-tier daily backups confirmed enabled; PROD-DB-04 deferral acknowledged and documented for runbook.</done>
</task>

</tasks>

<verification>
- PROD-DB-03: platform_admins has exactly 1 row matching skale.club@gmail.com (PROD-DB-03 schema requirement satisfied; sign-in test deferred to Phase 62)
- PROD-DB-04: Free-tier daily backups confirmed; PITR deferral documented per D-01
- Invitation email received; magic link preserved for Phase 62
- Zero git changes in this plan
</verification>

<success_criteria>
PROD-DB-03 and PROD-DB-04 are both formally addressed — PROD-DB-03 as PASS (seed row present), PROD-DB-04 as DEFERRED (per requirements + D-01). Plan 05 can produce the final audit + runbook.
</success_criteria>

<output>
After completion, create `.planning/phases/61-production-database-foundation/61-04-SUMMARY.md` recording:
- platform_admins count (must be 1)
- Whether the seed re-run fallback was needed (yes/no)
- Confirmation that the invitation email exists in skale.club@gmail.com inbox
- Confirmation that Dashboard → Database → Backups shows daily backups enabled
- The full PROD-DB-04 deferral statement (copy-paste from Task 3 acceptance criteria)
</output>
