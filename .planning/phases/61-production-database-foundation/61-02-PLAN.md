---
phase: 61-production-database-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "61-01"
files_modified: []
autonomous: false
requirements:
  - PROD-DB-01
gap_closure: false

must_haves:
  truths:
    - "A new Supabase project (separate from dev) is provisioned in us-east-1"
    - "The pg_cron extension is enabled BEFORE any migrations run"
    - "The production connection string + service role key + anon key are captured to local .env.production (gitignored, NEVER committed)"
    - "The pooler connection string can successfully connect to the new database"
  artifacts:
    - path: ".env.production"
      provides: "Local-only secrets manifest for production Supabase project"
      contains: "PROD_DB_URL"
      committed: false
  key_links:
    - from: ".env.production"
      to: "supabase/audits/run-prod-readiness.sh"
      via: "exported PROD_DB_URL env var"
      pattern: "export PROD_DB_URL"
---

<objective>
Provision a brand-new production Supabase project — separate organization and project from dev (`prmqgcrnpuvpzruyzvuv` is dev only) — in `us-east-1` on the Free tier per D-01. Enable `pg_cron` BEFORE applying any migrations. Capture all secrets to local `.env.production` (gitignored). This plan has NO repo file changes — outputs go to `.env.production` on the local machine only.

Purpose: Wave 1 (per Nyquist contract). Creates the empty production database that Plan 03 will push migrations into. PROD-DB-04 (PITR) is DEFERRED per D-01 — Free tier provides 2-day daily backup retention by default and is acceptable for v3.1 go-live.

Output: Live empty Supabase project + `.env.production` on local machine. Zero git changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/61-production-database-foundation/61-CONTEXT.md
@.planning/phases/61-production-database-foundation/61-RESEARCH.md
@.planning/phases/61-production-database-foundation/61-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Secrets to capture during provisioning. Names follow STATE.md SEC-03 convention. -->

`.env.production` (LOCAL ONLY — gitignored — NEVER committed) MUST contain:
- PROD_PROJECT_REF=<your-prod-project-ref>             (e.g. abc123xyz789 — visible in dashboard URL)
- PROD_DB_PASSWORD=<generated-db-password>             (generated during provisioning)
- PROD_DB_URL=postgresql://postgres.<PROD_PROJECT_REF>:<PROD_DB_PASSWORD>@aws-0-us-east-1.pooler.supabase.com:6543/postgres
- PROD_DB_URL_DIRECT=postgresql://postgres:<PROD_DB_PASSWORD>@db.<PROD_PROJECT_REF>.supabase.co:5432/postgres
- NEXT_PUBLIC_SUPABASE_URL=https://<PROD_PROJECT_REF>.supabase.co
- NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-prod-anon-key>
- SUPABASE_SERVICE_ROLE_KEY=<your-prod-service-role-key>
- SUPABASE_JWT_SECRET=<your-prod-jwt-secret>
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Provision new Supabase production project (Dashboard)</name>
  <files>(none — Dashboard operation)</files>
  <read_first>
    - .planning/phases/61-production-database-foundation/61-CONTEXT.md (decisions block: Free tier, us-east-1, no PITR)
    - .planning/phases/61-production-database-foundation/61-RESEARCH.md (§Wave 0: Provision Project for exact steps)
    - CLAUDE.md (Secret Handling section — NEVER commit secrets to git)
  </read_first>
  <what-built>
    Wave 1 prerequisite: a live, empty Supabase project in us-east-1. This step CANNOT be automated — Supabase project provisioning is a Dashboard-only operation that requires human-in-loop billing acknowledgment and password generation.
  </what-built>
  <action>
    Perform these steps in order in the Supabase Dashboard (https://supabase.com/dashboard):

    1. Sign in as `skale.club@gmail.com`.
    2. Click "New Project". If the existing dev org contains the dev project (`prmqgcrnpuvpzruyzvuv`), create a NEW organization first to ensure clean separation. Suggested org name: `xtimator-prod`.
    3. Project settings:
       - **Project name**: `xtimator` (or `xtimator-production`)
       - **Region**: `East US (North Virginia)` — i.e. `us-east-1` (D-02 — DIFFERENT from dev's us-west-2, accepted tradeoff per CONTEXT.md)
       - **Plan**: **Free** (D-01 — $0/mo, no Pro upgrade, no PITR add-on for v3.1)
       - **Database password**: generate locally via `openssl rand -base64 24` on the command line, paste into the password field. Store the plain value IMMEDIATELY in a safe place (1Password / Bitwarden / `.env.production` on local disk).
    4. Click "Create new project". Wait for provisioning to complete (~2 minutes).
    5. Once provisioned, navigate to **Database → Extensions**:
       - Search for `pg_cron`
       - Toggle the extension to "Enabled"
       - Confirm in the Enabled section. (PITFALL 2 — three migrations depend on `cron.schedule(...)`. If not enabled, Plan 03's `db push` will fail partway.)
    6. Capture all the values listed in the `<interfaces>` block above into a LOCAL file `.env.production` at the repo root. The file MUST be gitignored (it already is — `.env*` is in `.gitignore`). Values come from:
       - **PROD_PROJECT_REF**: visible in dashboard URL after `/project/` segment
       - **PROD_DB_PASSWORD**: the password you generated in step 3
       - **NEXT_PUBLIC_SUPABASE_URL** + **NEXT_PUBLIC_SUPABASE_ANON_KEY**: Project Settings → API
       - **SUPABASE_SERVICE_ROLE_KEY**: Project Settings → API → "service_role" secret (REVEAL once, copy, store)
       - **SUPABASE_JWT_SECRET**: Project Settings → API → JWT Settings → JWT Secret (REVEAL once, copy, store)
       - **PROD_DB_URL** + **PROD_DB_URL_DIRECT**: construct from the values above per the format in `<interfaces>`

    CRITICAL — NEVER paste any of these values into:
    - Any file under `.planning/`
    - Any markdown comment
    - Any commit message
    - This PLAN.md or any SUMMARY.md
    The pre-commit hook (gitleaks) will block `sb_secret_*` and `sb_publishable_*` patterns, but a leaked DB password (a 24-char base64 string) may not match a known pattern — handle manually.
  </action>
  <how-to-verify>
    Confirm the following from your local terminal (NOT from git):

    1. `cat .env.production` shows all 8 keys listed in `<interfaces>` populated with non-placeholder values
    2. `git check-ignore .env.production` outputs `.env.production` (file is gitignored)
    3. Connectivity probe (uses your local psql or run from inside Supabase Dashboard SQL Editor):
       ```bash
       psql "$PROD_DB_URL" -c "SELECT current_database();"
       ```
       Returns `postgres` (single row, no error).
    4. Confirm pg_cron is enabled:
       ```bash
       psql "$PROD_DB_URL" -c "SELECT extname FROM pg_extension WHERE extname='pg_cron';"
       ```
       Returns 1 row with value `pg_cron`.
    5. Confirm region is us-east-1: Dashboard → Project Settings → General → Region shows "East US (North Virginia)".
    6. Confirm tier is Free: Dashboard → Project Settings → Billing shows "Free Plan" (not Pro).
  </how-to-verify>
  <acceptance_criteria>
    - `git check-ignore .env.production` returns `.env.production` (NOT committed)
    - `psql "$PROD_DB_URL" -At -c "SELECT current_database()"` returns `postgres`
    - `psql "$PROD_DB_URL" -At -c "SELECT extname FROM pg_extension WHERE extname='pg_cron'"` returns `pg_cron`
    - `psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM auth.users"` returns `0` (empty fresh DB)
    - `psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name='companies'"` returns `0` (no migrations applied yet)
    - Dashboard region = us-east-1
    - Dashboard tier = Free
    - No secret values appear in `git diff` or `git status` for the working tree
  </acceptance_criteria>
  <resume-signal>Type "provisioned" when the project exists, pg_cron is enabled, and `.env.production` is populated locally. Type "issue: {description}" if any step failed.</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Invite super-admin email (Dashboard) — pre-stage auth.users row</name>
  <files>(none — Dashboard operation)</files>
  <read_first>
    - .planning/phases/61-production-database-foundation/61-CONTEXT.md (decisions block: Super-Admin Bootstrap — Dashboard invite + re-run seed)
    - .planning/phases/61-production-database-foundation/61-RESEARCH.md (§Pitfall 1 — Empty platform_admins After Seed Migration)
    - supabase/migrations/20260503000002_seed_platform_admin.sql (the seed that depends on auth.users row existing)
  </read_first>
  <what-built>
    Stage the `auth.users` row for `skale.club@gmail.com` BEFORE Plan 03 runs migrations. This lets the seed migration `20260503000002_seed_platform_admin.sql` actually insert into `platform_admins` (instead of being a silent no-op against an empty `auth.users` table — see Pitfall 1).

    Pre-staging here means Plan 03 can apply all migrations cleanly; Plan 04 just verifies the seed worked.
  </what-built>
  <action>
    In the Supabase Dashboard for the newly provisioned production project:

    1. Navigate to **Authentication → Users**.
    2. Click **"Invite user"** (button in top-right area).
    3. Enter email: `skale.club@gmail.com`
    4. Click **Send invitation**.
    5. Confirm a row now exists in the Users table with email `skale.club@gmail.com` and status `Invited` (the user has not yet accepted; the password is NOT set yet — that happens later when the user clicks the invite email link after the app deploys in Phase 62).

    The point of this step is creating the `auth.users` row NOW so that the FK target exists when the seed migration runs in Plan 03. The user does NOT need to accept the email yet — Phase 61 only needs the row to exist.

    DO NOT paste any user UUID into any committed file. The UUID is internal.
  </action>
  <how-to-verify>
    Run from your local terminal with `PROD_DB_URL` exported:

    ```bash
    psql "$PROD_DB_URL" -At -c "SELECT email FROM auth.users WHERE email='skale.club@gmail.com';"
    ```
    Returns: `skale.club@gmail.com` (single row).

    Also verify the row count is exactly 1:
    ```bash
    psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM auth.users WHERE email='skale.club@gmail.com';"
    ```
    Returns: `1`.
  </how-to-verify>
  <acceptance_criteria>
    - `psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM auth.users WHERE email='skale.club@gmail.com'"` returns `1`
    - `psql "$PROD_DB_URL" -At -c "SELECT count(*) FROM auth.users"` returns `1` (only the invited admin; no other users)
    - Dashboard → Authentication → Users shows the row with status `Invited`
  </acceptance_criteria>
  <resume-signal>Type "invited" when the dashboard shows the invited row AND the SQL queries above return 1. Type "issue: {description}" if the invite failed.</resume-signal>
</task>

</tasks>

<verification>
- New Supabase project exists in us-east-1, Free tier
- `pg_cron` extension is enabled
- `.env.production` exists locally with all 8 required keys, gitignored
- `auth.users` contains exactly 1 row for `skale.club@gmail.com`
- Zero migrations applied yet (Plan 03 handles that)
- Zero secret values in any committed file
</verification>

<success_criteria>
PROD-DB-01 satisfied. Plan 03 can `bunx supabase db push --db-url "$PROD_DB_URL"` against a clean, pg_cron-enabled, empty database with the admin email pre-staged.
</success_criteria>

<output>
After completion, create `.planning/phases/61-production-database-foundation/61-02-SUMMARY.md` recording:
- Confirmation that .env.production is populated AND gitignored (no values pasted)
- Confirmation that pg_cron is enabled
- Confirmation that auth.users has the invited row
- Region: us-east-1
- Plan: Free
- Project ref: REDACTED (do NOT write the actual ref — say "captured in .env.production")
</output>
