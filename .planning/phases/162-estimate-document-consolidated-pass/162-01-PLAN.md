---
phase: 162-estimate-document-consolidated-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/clients/client-picker.test.tsx
  - tests/unit/estimate/document-bill-to.test.tsx
  - tests/unit/components/presentation-settings-panel.test.tsx
  - tests/unit/components/estimate-floating-actions.test.tsx
  - tests/unit/estimate/inline-project-name.test.tsx
  - tests/unit/estimate/document-alignment.test.tsx
  - tests/unit/estimate/mobile-line-item.test.tsx
autonomous: true
requirements: [DOCUX-01, DOCUX-02, DOCUX-03, DOCUX-04, DOCUX-05, DOCUX-06, DOCUX-07]

must_haves:
  truths:
    - "All 7 test files exist on disk at their VALIDATION.md paths"
    - "Every test file loads cleanly under `npx vitest run <file>` — no missing imports, no syntax errors"
    - "Every DOCUX-01..07 requirement has at least one `it.todo(...)` placeholder in a matching test file"
    - "Downstream plans (162-02..05) can convert `it.todo` entries to real `it(...)` tests as they implement — RED→GREEN cycles are unblocked"
  artifacts:
    - path: "tests/unit/clients/client-picker.test.tsx"
      provides: "Scaffold for DOCUX-02, DOCUX-03 tests (client-picker variants + unlink)"
      contains: "describe('ClientPicker"
    - path: "tests/unit/estimate/document-bill-to.test.tsx"
      provides: "Scaffold for DOCUX-02 Bill To pencil affordance + picker interaction"
      contains: "describe('Bill To"
    - path: "tests/unit/components/presentation-settings-panel.test.tsx"
      provides: "Scaffold for DOCUX-01 (panel behavior + GUARD-03 static grep)"
      contains: "describe('PresentationSettingsPanel"
    - path: "tests/unit/components/estimate-floating-actions.test.tsx"
      provides: "Scaffold for DOCUX-01 (gear button placement + onOpenSettings)"
      contains: "describe('EstimateFloatingActions"
    - path: "tests/unit/estimate/inline-project-name.test.tsx"
      provides: "Scaffold for DOCUX-04 (underline + validation + error-retry)"
      contains: "describe('InlineProjectName"
    - path: "tests/unit/estimate/document-alignment.test.tsx"
      provides: "Scaffold for DOCUX-05 (padding assertions + DOM snapshot)"
      contains: "describe('EstimateDocument alignment"
    - path: "tests/unit/estimate/mobile-line-item.test.tsx"
      provides: "Scaffold for DOCUX-06 (no glass card + transparent inputs + touch targets)"
      contains: "describe('mobile line-item"
  key_links: []
---

<objective>
Wave 0 test scaffolding. Create the seven test-file skeletons that 162-VALIDATION.md's Per-Task Verification Map references, so downstream plans (162-02..05) can drive RED→GREEN cycles against real file paths from task 1. Each file is loadable by vitest without errors and enumerates the DOCUX-XX cases the phase must prove via `it.todo(...)` placeholders — no dead imports of not-yet-existing modules.

Purpose: Nyquist gate compliance. `162-VALIDATION.md` per-req commands (e.g. `npx vitest run tests/unit/clients/client-picker.test.tsx -t "unlink"`) fail today because the files don't exist; this plan makes those commands runnable (returning "no tests found matching pattern" or todo-marked) so 162-02..05 tasks can then convert `.todo` to real assertions.

Output: 7 test-scaffold files + updated `162-VALIDATION.md` frontmatter (`wave_0_complete: true`, `nyquist_compliant: true`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-estimate-document-consolidated-pass/162-CONTEXT.md
@.planning/phases/162-estimate-document-consolidated-pass/162-VALIDATION.md

<interfaces>
Existing vitest test scaffolding pattern in this repo — mirror it exactly:

From `tests/unit/estimate/presentation-settings.test.ts` (Phase 161-01 landed test):
```ts
import { describe, it, expect } from 'vitest'
import { resolvePresentationSettings } from '@/lib/estimate/presentation-settings'

describe('resolvePresentationSettings (PRESENT-01)', () => {
  it('returns defaults when raw is null', () => {
    ...
  })
})
```

For Wave 0 scaffolds, the pattern is `describe(...)` + `it.todo(...)` — no imports of not-yet-existing modules:
```ts
import { describe, it } from 'vitest'

describe('ClientPicker (DOCUX-02, DOCUX-03)', () => {
  it.todo('renders card variant with glass shell')
  it.todo('renders button variant with rounded-full pill trigger')
})
```

`it.todo(...)` prints "todo" in vitest output but does NOT fail the run — perfect for scaffolds.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client-picker + Bill To test scaffolds (DOCUX-02, DOCUX-03)</name>
  <files>tests/unit/clients/client-picker.test.tsx, tests/unit/estimate/document-bill-to.test.tsx</files>
  <read_first>
    - .planning/phases/162-estimate-document-consolidated-pass/162-VALIDATION.md (lines 41-50 — the per-req map for DOCUX-02/03)
    - tests/unit/estimate/presentation-settings.test.ts (Phase 161-01 vitest+RTL pattern reference)
  </read_first>
  <action>
    Create `tests/unit/clients/client-picker.test.tsx` with EXACTLY this content (per D-03 client-picker consolidation locked in 162-CONTEXT):

    ```tsx
    import { describe, it } from 'vitest'

    // Wave 0 scaffold — Phase 162 plans (162-02) convert these it.todo entries to
    // real tests as they implement `components/clients/client-picker.tsx` per
    // DOCUX-02 (Bill To pencil variant) and DOCUX-03 (consolidated component).
    // Every it.todo maps to a row in 162-VALIDATION.md's Per-Task Verification Map.

    describe('ClientPicker (DOCUX-02, DOCUX-03)', () => {
      it.todo('renders card variant — Card variant="glass" shell + Link Client button (mirrors LinkClientCard)')
      it.todo('renders button variant — Button rounded-full ghost UserPlus trigger (mirrors LinkClientButton)')
      it.todo('renders inline variant — bare button "No client linked" + UserPlus (mirrors LinkClientInline)')
      it.todo('renders billTo variant — Pencil icon with opacity-0 group-hover:opacity-100 focus:opacity-100 (NEW — for DOCUX-02)')
      it.todo('dispatches linkProjectToClient(projectId, clientId) on client selection (mocked)')
      it.todo('shows Unlink footer button only when currentClientId is non-null')
      it.todo('unlink calls unlinkProjectFromClient(projectId) (mocked)')
      it.todo('search filters clients by name OR email (case-insensitive)')
      it.todo('CommandEmpty renders "No clients found." when filtered list is empty')
    })
    ```

    Create `tests/unit/estimate/document-bill-to.test.tsx` with EXACTLY this content:

    ```tsx
    import { describe, it } from 'vitest'

    // Wave 0 scaffold — Phase 162 plan 162-03 converts these to real tests when it
    // wires the Bill To pencil affordance into estimate-document.tsx per DOCUX-02.

    describe('Bill To pencil affordance (DOCUX-02)', () => {
      it.todo('pencil is hidden by default (opacity-0) when the Bill To block is not hovered/focused')
      it.todo('pencil reveals (opacity-100) on hover of the Bill To block (group-hover:opacity-100)')
      it.todo('pencil reveals (opacity-100) on focus of the Bill To block (focus:opacity-100)')
      it.todo('clicking pencil opens ClientPicker Popover with variant="billTo"')
      it.todo('pencil only renders when isEditable=true AND projectId is defined (not in mode="view")')
      it.todo('selecting a client in the popover dispatches linkProjectToClient (mocked server action)')
      it.todo('DocumentClient interface includes `id: string` (compile-time assertion)')
    })
    ```

    Note: `it.todo(...)` does not fail the test run — it prints a "todo" line. Downstream plans (162-02 for ClientPicker, 162-03 for Bill To wiring) will convert each `.todo` to a real `it(...)` + implementation as they land. NO imports of not-yet-existing modules — those come with the RED tests in 162-02/03.
  </action>
  <verify>
    <automated>test -f tests/unit/clients/client-picker.test.tsx && test -f tests/unit/estimate/document-bill-to.test.tsx && npx vitest run tests/unit/clients/client-picker.test.tsx tests/unit/estimate/document-bill-to.test.tsx --reporter=default 2>&1 | grep -E "Tests\s+.*todo" </automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/unit/clients/client-picker.test.tsx` returns 0 (file exists)
    - `test -f tests/unit/estimate/document-bill-to.test.tsx` returns 0 (file exists)
    - `grep -c "it.todo" tests/unit/clients/client-picker.test.tsx` returns 9 (9 todos for DOCUX-02/03)
    - `grep -c "it.todo" tests/unit/estimate/document-bill-to.test.tsx` returns 7 (7 todos for DOCUX-02)
    - `grep -c "describe('ClientPicker" tests/unit/clients/client-picker.test.tsx` returns 1
    - `grep -c "describe('Bill To pencil affordance" tests/unit/estimate/document-bill-to.test.tsx` returns 1
    - `npx vitest run tests/unit/clients/client-picker.test.tsx tests/unit/estimate/document-bill-to.test.tsx` exits 0 with "todo" output (no test failures)
  </acceptance_criteria>
  <done>Both scaffold files exist, load cleanly under vitest, and expose 16 total `it.todo` markers covering DOCUX-02 (7) + DOCUX-03 (9).</done>
</task>

<task type="auto">
  <name>Task 2: Create panel + floating-actions test scaffolds (DOCUX-01)</name>
  <files>tests/unit/components/presentation-settings-panel.test.tsx, tests/unit/components/estimate-floating-actions.test.tsx</files>
  <read_first>
    - .planning/phases/162-estimate-document-consolidated-pass/162-VALIDATION.md (lines 43-46 — DOCUX-01 rows)
    - components/workspace/estimate/estimate-floating-actions.tsx (current pill shape — what will gain the gear button)
    - lib/estimate/presentation-settings.ts (hasEstimateBeenSentOrViewed signature — needed for PRESENT-05 notice test todo)
  </read_first>
  <action>
    Create `tests/unit/components/presentation-settings-panel.test.tsx` with EXACTLY this content:

    ```tsx
    import { describe, it } from 'vitest'

    // Wave 0 scaffold — Phase 162 plan 162-04 converts these to real tests when
    // it creates `components/workspace/estimate/presentation-settings-panel.tsx`
    // per DOCUX-01. GUARD-03 static-grep tests are grep commands in the plan's
    // acceptance criteria, not test file assertions — see 162-VALIDATION.md L44.

    describe('PresentationSettingsPanel (DOCUX-01)', () => {
      it.todo('responsive branch — renders Popover when window matches (min-width: 768px)')
      it.todo('responsive branch — renders Sheet side="bottom" when window is <768px')
      it.todo('dispatches UPDATE_PRESENTATION_SETTINGS on section-visibility Switch toggle')
      it.todo('dispatches UPDATE_PRESENTATION_SETTINGS on Tax mode RadioGroup change')
      it.todo('dispatches UPDATE_PRESENTATION_SETTINGS on Discount mode RadioGroup change')
      it.todo('dispatches UPDATE_PRESENTATION_SETTINGS on Deposit mode RadioGroup change')
      it.todo('never dispatches UPDATE_TAX_RATE / UPDATE_DISCOUNT / UPDATE_DEPOSIT (GUARD-03 — enforced statically via grep in plan acceptance criteria)')
      it.todo('sent or viewed notice — renders amber banner when estimateSentOrViewed=true')
      it.todo('sent or viewed notice — hidden when estimateSentOrViewed=false')
      it.todo('Tax mode "off" writes { tax: { mode: "off", preservedRate: <current> } } — never mutates tax_rate to 0')
      it.todo('section visibility toggle preserves other section states (immutable merge)')
    })
    ```

    Create `tests/unit/components/estimate-floating-actions.test.tsx` with EXACTLY this content:

    ```tsx
    import { describe, it } from 'vitest'

    // Wave 0 scaffold — Phase 162 plan 162-04 converts these to real tests when it
    // adds the gear icon to estimate-floating-actions.tsx per DOCUX-01.

    describe('EstimateFloatingActions gear button (DOCUX-01)', () => {
      it.todo('renders gear button when onOpenSettings prop is provided')
      it.todo('does NOT render gear button when onOpenSettings prop is undefined (backward-compat)')
      it.todo('gear opens settings — clicking gear button invokes onOpenSettings callback')
      it.todo('gear button is the LEFTMOST child of the Pill (order: [Gear] linkClientSlot Photos Send)')
      it.todo('gear button has aria-label="Settings" for screen readers')
      it.todo('gear button renders Settings icon from lucide-react (h-3.5 w-3.5)')
    })
    ```

    Note: The DOCUX-01 "Panel never dispatches UPDATE_TAX_RATE / UPDATE_DISCOUNT / UPDATE_DEPOSIT / recalculate" acceptance criterion (162-VALIDATION.md L44) is a STATIC GREP inside 162-04's task acceptance criteria — NOT an assertion in this test file — because it's a source-code property, not a runtime one. The `it.todo` on line "never dispatches..." is a placeholder for the future runtime RTL test that spies on dispatch calls.
  </action>
  <verify>
    <automated>test -f tests/unit/components/presentation-settings-panel.test.tsx && test -f tests/unit/components/estimate-floating-actions.test.tsx && npx vitest run tests/unit/components/presentation-settings-panel.test.tsx tests/unit/components/estimate-floating-actions.test.tsx --reporter=default 2>&1 | grep -E "Tests\s+.*todo"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/unit/components/presentation-settings-panel.test.tsx` returns 0
    - `test -f tests/unit/components/estimate-floating-actions.test.tsx` returns 0
    - `grep -c "it.todo" tests/unit/components/presentation-settings-panel.test.tsx` returns 11
    - `grep -c "it.todo" tests/unit/components/estimate-floating-actions.test.tsx` returns 6
    - `grep -c "describe('PresentationSettingsPanel" tests/unit/components/presentation-settings-panel.test.tsx` returns 1
    - `grep -c "describe('EstimateFloatingActions gear button" tests/unit/components/estimate-floating-actions.test.tsx` returns 1
    - `npx vitest run tests/unit/components/presentation-settings-panel.test.tsx tests/unit/components/estimate-floating-actions.test.tsx` exits 0 with "todo" output
  </acceptance_criteria>
  <done>Both DOCUX-01 scaffold files exist and load cleanly with 17 total `it.todo` markers covering panel + gear button behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Create inline-project-name + document-alignment + mobile-line-item scaffolds (DOCUX-04/05/06)</name>
  <files>tests/unit/estimate/inline-project-name.test.tsx, tests/unit/estimate/document-alignment.test.tsx, tests/unit/estimate/mobile-line-item.test.tsx</files>
  <read_first>
    - .planning/phases/162-estimate-document-consolidated-pass/162-VALIDATION.md (lines 51-58 — DOCUX-04/05/06 rows)
    - components/workspace/project-title.tsx (reference validation contract for DOCUX-04 tests)
    - components/workspace/estimate/item-card-mobile.tsx (current mobile shape — what DOCUX-06 will replace)
  </read_first>
  <action>
    Create `tests/unit/estimate/inline-project-name.test.tsx` with EXACTLY this content:

    ```tsx
    import { describe, it } from 'vitest'

    // Wave 0 scaffold — Phase 162 plan 162-03 converts these to real tests when
    // it reconciles InlineProjectName with ProjectTitle's validation contract
    // per DOCUX-04. Every todo maps to a locked criterion in 162-CONTEXT
    // (thin solid underline + validation contract preserved).

    describe('InlineProjectName (DOCUX-04)', () => {
      it.todo('solid underline — DOM contains "border-b" class and does NOT contain "decoration-dotted"')
      it.todo('underline is transparent by default and shows foreground/40 on hover (border-transparent hover:border-foreground/40)')
      it.todo('underline shows foreground/40 on keyboard focus (focus-visible:border-foreground/40)')
      it.todo('empty validation — submitting empty draft calls toast.error and stays in edit mode (no server call)')
      it.todo('200 char limit — submitting draft.length > 200 calls toast.error and stays in edit mode')
      it.todo('no-op — submitting draft === name closes edit mode without server call')
      it.todo('error retry — server error causes toast.error, reverts draft to name, keeps edit mode OPEN')
      it.todo('autofocus + select-all when entering edit mode')
      it.todo('escape key cancels edit mode and reverts draft')
      it.todo('maxLength attribute is 200 on the input element')
      it.todo('aria-label is "Project name" on the input element')
      it.todo('double-submit guard — subsequent submit while isPending returns early')
    })
    ```

    Create `tests/unit/estimate/document-alignment.test.tsx` with EXACTLY this content:

    ```tsx
    import { describe, it } from 'vitest'

    // Wave 0 scaffold — Phase 162 plan 162-03 converts these to real tests during
    // the alignment pass per DOCUX-05. The DOM-snapshot test (view mode) captures
    // its baseline the first time it runs post-alignment — expected to be a
    // one-time intentional artifact of Phase 162.

    describe('EstimateDocument alignment (DOCUX-05)', () => {
      it.todo('section padding — DocumentSectionBlock header uses px-6 sm:px-10 (not px-3)')
      it.todo('section padding — item-table row parent uses px-6 sm:px-10 (not px-3)')
      it.todo('section padding — section subtotal footer uses px-6 sm:px-10 (not px-3)')
      it.todo('section padding — add-item row uses px-6 sm:px-10 (not px-3)')
      it.todo('section padding — mobile stacked-item read-only row uses px-6 sm:px-10 (not px-3)')
      it.todo('vertical rhythm — info grid uses py-6 sm:py-8 (not pt-8 sm:pt-10 pb-5)')
      it.todo('vertical rhythm — DocumentTotals uses py-6 (not py-5)')
      it.todo('vertical rhythm — Terms section uses py-6 (not pt-4 pb-6)')
      it.todo('view mode DOM — snapshot stable post-alignment (mode="view" toMatchSnapshot)')
    })
    ```

    Create `tests/unit/estimate/mobile-line-item.test.tsx` with EXACTLY this content:

    ```tsx
    import { describe, it } from 'vitest'

    // Wave 0 scaffold — Phase 162 plan 162-05 converts these to real tests when
    // ItemCardMobile is rebuilt to doc-native transparent inputs per DOCUX-06.

    describe('mobile line-item editor (DOCUX-06)', () => {
      it.todo('no glass card — rendered DOM does NOT contain the string "glass" as a class value')
      it.todo('no glass card — no <Card> wrapper (the outer node is a <div>, not a Card)')
      it.todo('transparent inputs — description input uses INLINE_INPUT_CLS bg-transparent styling')
      it.todo('transparent inputs — qty input uses INLINE_INPUT_CLS text-right styling')
      it.todo('transparent inputs — unit-price MoneyInput uses bg-transparent border-0 shadow-none')
      it.todo('touch targets — trash button preserves min-h-[44px] min-w-[44px]')
      it.todo('touch targets — Switch container preserves min-h-[44px]')
      it.todo('row structure — border-b border-border/50 last:border-b-0 even:bg-muted/20')
      it.todo('preserves onUpdate + onRemove props signature (same as current ItemCardMobile)')
    })
    ```
  </action>
  <verify>
    <automated>test -f tests/unit/estimate/inline-project-name.test.tsx && test -f tests/unit/estimate/document-alignment.test.tsx && test -f tests/unit/estimate/mobile-line-item.test.tsx && npx vitest run tests/unit/estimate/inline-project-name.test.tsx tests/unit/estimate/document-alignment.test.tsx tests/unit/estimate/mobile-line-item.test.tsx --reporter=default 2>&1 | grep -E "Tests\s+.*todo"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/unit/estimate/inline-project-name.test.tsx` returns 0
    - `test -f tests/unit/estimate/document-alignment.test.tsx` returns 0
    - `test -f tests/unit/estimate/mobile-line-item.test.tsx` returns 0
    - `grep -c "it.todo" tests/unit/estimate/inline-project-name.test.tsx` returns 12
    - `grep -c "it.todo" tests/unit/estimate/document-alignment.test.tsx` returns 9
    - `grep -c "it.todo" tests/unit/estimate/mobile-line-item.test.tsx` returns 9
    - `npx vitest run tests/unit/estimate/inline-project-name.test.tsx tests/unit/estimate/document-alignment.test.tsx tests/unit/estimate/mobile-line-item.test.tsx` exits 0 with "todo" output (no failures)
    - Update `.planning/phases/162-estimate-document-consolidated-pass/162-VALIDATION.md` frontmatter: change `nyquist_compliant: false` → `nyquist_compliant: true` and `wave_0_complete: false` → `wave_0_complete: true`
  </acceptance_criteria>
  <done>All 3 remaining scaffold files exist (30 `it.todo` covering DOCUX-04/05/06), all 7 Wave 0 files load cleanly under vitest, and 162-VALIDATION.md's frontmatter is flipped to `nyquist_compliant: true` + `wave_0_complete: true`.</done>
</task>

</tasks>

<verification>
Run the full Wave 0 quick command from 162-VALIDATION.md:
```bash
npx vitest run tests/unit/components/presentation-settings-panel.test.tsx \
  tests/unit/clients/client-picker.test.tsx \
  tests/unit/estimate/inline-project-name.test.tsx \
  tests/unit/estimate/document-bill-to.test.tsx \
  tests/unit/estimate/document-alignment.test.tsx \
  tests/unit/estimate/mobile-line-item.test.tsx \
  tests/unit/components/estimate-floating-actions.test.tsx
```
Must exit 0. Aggregate `it.todo` count across all 7 files: 9+7+11+6+12+9+9 = **63 todos**.

Then verify VALIDATION frontmatter flip:
```bash
grep -c "nyquist_compliant: true" .planning/phases/162-estimate-document-consolidated-pass/162-VALIDATION.md   # = 1
grep -c "wave_0_complete: true" .planning/phases/162-estimate-document-consolidated-pass/162-VALIDATION.md    # = 1
```
</verification>

<success_criteria>
- 7 scaffold files exist on disk under `tests/unit/`
- All 7 files load cleanly under `npx vitest run <files>` (exit 0)
- Aggregate 63 `it.todo` markers across the 7 files (9+7+11+6+12+9+9)
- 162-VALIDATION.md frontmatter flipped to `nyquist_compliant: true` + `wave_0_complete: true`
- Downstream plans (162-02..05) can now reference these exact file paths in their `-t "..."` vitest filter arguments
</success_criteria>

<output>
After completion, create `.planning/phases/162-estimate-document-consolidated-pass/162-01-SUMMARY.md`.
</output>
