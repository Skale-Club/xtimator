---
phase: 185-paginated-editable-editor-mode
plan: 04
type: execute
wave: 3
depends_on: ["185-03"]
files_modified:
  - components/workspace/estimate/use-paginated-preview.ts
  - tests/unit/estimate/use-paginated-preview.test.ts
  - tests/unit/estimate/paginated-editing-preserved.test.tsx
  - tests/unit/estimate/share-webview-pagination-boundary.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PGMODE-03, PGMODE-05, PGBRK-01, PGBRK-04]

must_haves:
  truths:
    - "Adding/removing/reordering an item or section while paginated mode is active repaginates on the very next render, with no debounce delay."
    - "Typing in a text field repaginates only after a 400ms pause with no further change — never per-keystroke — and rapid consecutive keystrokes collapse into exactly one recomputation."
    - "An edit that moves the focused row to a different page never loses cursor focus or remounts the row — proven by a stable-key regression test, not just asserted."
    - "Reordering items/sections in paginated mode dispatches the exact same REORDER_ITEMS/REORDER_SECTIONS actions, in document order, as full-width mode — page membership is never a third dnd-kit axis."
    - "The public share webview (app/estimate/[token]/**, components/share/**) never imports lib/estimate/pagination/* or the new paginated-editor components — enforced by an automated boundary test, not a one-time manual check."
  artifacts:
    - path: "tests/unit/estimate/share-webview-pagination-boundary.test.ts"
      provides: "Static grep boundary test enforcing PGMODE-05, mirroring pagination-engine-boundary.test.ts's pattern"
      contains: "share"
    - path: "tests/unit/estimate/paginated-editing-preserved.test.tsx"
      provides: "Focus/key-stability + dnd-kit document-order regression proof for paginated mode"
  key_links:
    - from: "components/workspace/estimate/use-paginated-preview.ts"
      to: "structural-vs-text change signature"
      via: "immediate recompute on structural-signature change; 400ms debounce otherwise"
      pattern: "400"
    - from: ".planning/REQUIREMENTS.md"
      to: "PGBRK-01 / PGBRK-04 checkbox + coverage table rows"
      via: "flipped to complete, citing Plan 185-01's parity test + this plan's live end-to-end wiring as evidence"
      pattern: "PGBRK-01"
---

<objective>
Refine `usePaginatedPreview()`'s recomputation trigger from "always recompute" (Plan 185-03's correctness-first version) to the real UX contract — immediate on structural changes, debounced 400ms on text changes, with proof that focus survives a cross-page-boundary edit and that dnd-kit never sees page membership as a sorting axis. Close PGMODE-05 with an automated boundary test, and formally close PGBRK-01/04 in REQUIREMENTS.md now that the web preview genuinely consumes the shared pagination module end-to-end.

Purpose: this is the phase's final requirement-closure plan — PGMODE-03's "no per-keystroke reflow lag" / "no focus loss" clauses and PGMODE-05's "public share stays untouched" guarantee both need proof, not just architecture.
Output: debounced/immediate repagination triggering in `use-paginated-preview.ts`; a focus/dnd-kit regression test; a share-webview import-boundary test; REQUIREMENTS.md's PGMODE-01..05 + PGBRK-01/04 checkboxes flipped to complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-paginated-editable-editor-mode/185-RESEARCH.md
@.planning/REQUIREMENTS.md
@components/workspace/estimate/use-paginated-preview.ts
@components/workspace/estimate/estimate-document.tsx
@components/share/estimate-view.tsx
@tests/unit/pagination/pagination-engine-boundary.test.ts

<interfaces>
`use-paginated-preview.ts`'s shape as Plan 185-03 leaves it (this plan modifies the recompute-triggering internals only — the exported function signature `usePaginatedPreview(input): { pages: PageAssignment[] | null }` does NOT change):
```ts
export function usePaginatedPreview(input: {
  data: EstimateDocumentData
  company: DocumentCompany
  templateId: EstimateTemplateId
  preparedBy: string | null
  language: EstimateLanguage
  enabled: boolean
}): { pages: PageAssignment[] | null }
```

**Structural-signature fields** (185-RESEARCH.md's "Repagination triggers (PGMODE-03)" section — everything that changes WHICH blocks exist or their order; text content changes do NOT touch this signature):
```ts
const structuralSignature =
  data.sections.map((s) => s.id).join(',') + '|' +
  data.sections.map((s) => s.items.map((i) => i.id).join(',')).join('|') + '|' +
  [
    !!data.summary, !!data.signature, data.attachedPhotos?.length ?? 0,
    !!data.payment_terms, !!data.timeline, !!data.warranty_terms, !!data.notes,
    data.discount_amount > 0, data.tax_amount > 0, data.deposit_type !== 'none',
  ].join(',')
```
When this string differs from the previous render's value (including the very first activation), recompute IMMEDIATELY (no timer). When it is unchanged but `data` changed by reference (a text-only edit), schedule the recompute via `setTimeout(..., TEXT_DEBOUNCE_MS)` (`TEXT_DEBOUNCE_MS = 400`), clearing any pending timer on each subsequent text-only change within the window — a real debounce, not a throttle.

`estimate-document.tsx`'s existing stable-key pattern (confirmed unchanged, no code change needed here — this plan only PROVES it holds under the paginated overlay):
```tsx
{data.sections.map((section) => (
  <SortableDocumentSection key={section.id} ... />
))}
// ...per-section...
{section.items.map((item) => (
  <SortableDocumentItemRow key={item.id} ... />
))}
```

`components/share/estimate-view.tsx`'s confirmed import list today (zero pagination coupling — this plan adds a durable test, not a fix):
imports `SYSTEM_COLORS`, `lib/color/contrast`, `lib/queries/share`, `lib/i18n/*`, `lib/money/currency`, `SignaturePad`, `EstimateDocumentData`/`DocumentCompany`/`DocumentClient` (types only), `EstimateTemplateId`/`isEstimateTemplateId`/`DEFAULT_ESTIMATE_TEMPLATE_ID` — never `lib/estimate/pagination/*`, never `paginated-document-overlay`/`use-paginated-preview`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Immediate-vs-debounced repagination triggers + memoized offset derivation</name>
  <files>components/workspace/estimate/use-paginated-preview.ts, tests/unit/estimate/use-paginated-preview.test.ts</files>
  <read_first>components/workspace/estimate/use-paginated-preview.ts (Plan 185-03's version — the effect keyed on `[enabled, data, company, templateId, preparedBy, language]` to refine); .planning/research/PITFALLS.md Pitfall 5 (reflow thrash / debounce-the-derived-snapshot recommendation); 185-RESEARCH.md's "Repagination triggers (PGMODE-03)" section (this plan's `<interfaces>` block already reproduces the exact signature formula and 400ms figure)</read_first>
  <behavior>
    - Given a sequence of `data` changes where ONLY `sections[].items[].description` text differs (no id/count/presence-flag change), the hook does NOT recompute until 400ms have elapsed with no further change: advancing fake timers by 399ms after the last change triggers zero recomputations; advancing to 400ms triggers exactly one.
    - Given a `data` change where a section/item id is added, removed, or reordered (or any structural-signature presence flag flips — e.g. `discount_amount` crosses the `> 0` boundary), the hook recomputes on the very next effect flush, with zero timer delay, EVEN IF a text-debounce timer was already pending (the structural change does not need to wait out the pending text timer).
    - Multiple rapid text-only changes within the 400ms window collapse into exactly ONE recomputation, not one per change (assert via a call-count spy on the underlying pipeline).
    - The derived page-sheet pixel-offset array is memoized: re-rendering with the SAME `pages` array reference does not recompute the offsets (assert via a spy on the offset-derivation function or a render-count check).
  </behavior>
  <action>
    Inside `use-paginated-preview.ts`: compute the `structuralSignature` string exactly as specified in this plan's `<interfaces>` block, on every hook call, keeping the PREVIOUS render's signature in a `useRef`. On each effect run: if `enabled` is `false`, no-op (unchanged from Plan 185-03). If `enabled` is `true` and the signature DIFFERS from the ref's stored value (including first activation) — recompute the full pipeline immediately (no `setTimeout`), update the ref, and clear any pending debounce timer (a structural change should never be masked by an in-flight text timer). If the signature is UNCHANGED but `data` differs by reference from the last computed snapshot — clear any existing pending timer and schedule a new one via `setTimeout(recompute, TEXT_DEBOUNCE_MS)` where `const TEXT_DEBOUNCE_MS = 400` (named constant, not a magic number). Clean up the pending timer on unmount/re-run (`clearTimeout` in the effect's cleanup).

    Memoize the page-sheet pixel-offset array `PaginatedDocumentOverlay` (or this hook, whichever currently derives it — Plan 185-03's implementation) consumes, via `useMemo` keyed on the `pages` array reference, so an unrelated re-render with the same `pages` never recomputes offsets.

    Create `tests/unit/estimate/use-paginated-preview.test.ts` using `renderHook` (`@testing-library/react`) + `vi.useFakeTimers()`, mocking the dynamically-imported `@/lib/estimate/pagination/measure/browser-estimator` module (`vi.mock(...)`) so the test exercises ONLY the trigger-timing logic, not real font loading — assert every `<behavior>` bullet above via a call-count spy on the mocked provider/pipeline.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/estimate/use-paginated-preview.test.ts -x` — all pass
    - `grep -n "TEXT_DEBOUNCE_MS = 400" components/workspace/estimate/use-paginated-preview.ts` matches (named constant, not an inline magic number)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/estimate/use-paginated-preview.test.ts</automated>
  </verify>
  <done>Structural changes repaginate immediately (never waiting on a pending text timer); text changes debounce at a named 400ms constant with zero per-keystroke thrash; the pixel-offset derivation is memoized against the pages reference.</done>
</task>

<task type="auto">
  <name>Task 2: Focus/dnd-kit regression proof + PGMODE-05 boundary guard + REQUIREMENTS.md closure</name>
  <files>tests/unit/estimate/paginated-editing-preserved.test.tsx, tests/unit/estimate/share-webview-pagination-boundary.test.ts, .planning/REQUIREMENTS.md</files>
  <read_first>components/workspace/estimate/estimate-document.tsx (the `key={section.id}`/`key={item.id}` patterns cited in this plan's `<interfaces>` block, and the two DndContext/SortableContext tiers at lines ~576-616 and ~1620-1645); tests/unit/estimate/document-page-view.test.tsx (the exact mock setup — next/navigation, sonner, lib/actions/project, i18n/use-translation — to mirror for a new EstimateDocument-rendering test); tests/unit/pagination/pagination-engine-boundary.test.ts (the exact grep-boundary-test pattern to mirror for share-webview-pagination-boundary.test.ts); components/share/estimate-view.tsx's full import list (already confirmed clean — this task adds a durable test, not a fix); .planning/REQUIREMENTS.md (the PGMODE-01..05 + PGBRK-01/04 checkbox lines and the Requirement Coverage Map table, both quoted in full in 185-RESEARCH.md's Sources)</read_first>
  <action>
    Create `tests/unit/estimate/paginated-editing-preserved.test.tsx`, mirroring `tests/unit/estimate/document-page-view.test.tsx`'s mock setup (next/navigation, sonner, `lib/actions/project`, `lib/i18n/use-translation`). Render `EstimateDocument` in edit mode (`mode="edit"`, `dispatch` provided) with a multi-section fixture, wrapped in `PaginatedDocumentOverlay` with a non-null `pages` fixture (as Plan 185-03 built it), and prove TWO things:
    1. **Focus/key-stability:** capture the DOM node for a specific item row's description input (queried by its value or a stable test id, by `item.id`) BEFORE dispatching a structural edit (e.g. `ADD_ITEM` on an earlier section, which would shift later items' logical position). After the re-render, assert the SAME item's input DOM node is still present at the SAME identity (not remounted) — e.g. via a ref captured before the dispatch, or by asserting the element reference returned by a second `container.querySelector` for that same item id is IDENTICAL (`===`) to the one captured before, proving no key-driven remount occurred.
    2. **dnd-kit document-order proof:** assert the `SortableContext`'s `items` prop (or, if not directly inspectable, the DOM order of rendered section/item elements) still equals `data.sections.map(s => s.id)` / `section.items.map(i => i.id)` in DOCUMENT order — unaffected by which page each block was assigned to. The paginated overlay must never reorder, filter, or re-key based on `PageAssignment[]`.

    Create `tests/unit/estimate/share-webview-pagination-boundary.test.ts`, mirroring `tests/unit/pagination/pagination-engine-boundary.test.ts`'s exact grep pattern: recursively read every `.ts`/`.tsx` source file under `app/estimate/[token]/` and `components/share/`, and assert NONE match `/from ['"].*lib\/estimate\/pagination/`, `/from ['"].*paginated-document-overlay/`, or `/from ['"].*use-paginated-preview/`.

    Update `.planning/REQUIREMENTS.md`: flip the `PGMODE-01` through `PGMODE-05` checkboxes AND the `PGBRK-01`/`PGBRK-04` checkboxes from `- [ ]` to `- [x]`. Update the Requirement Coverage Map table rows for these 7 IDs from "Pending"/"Partial" to "Complete", adding a short citation matching the existing table's style (how `PGBRK-02`/`03`/`05`'s rows already cite "Phase 184") — cite "Phase 185" plus a one-line note that PGBRK-01/04 closure rests on Plan 185-01's deep-equal parity test + this plan's live end-to-end wiring.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/estimate/paginated-editing-preserved.test.tsx tests/unit/estimate/share-webview-pagination-boundary.test.ts -x` — all pass
    - `grep -c "\[x\] \*\*PGMODE-0" .planning/REQUIREMENTS.md` returns 5
    - `grep "PGBRK-01" .planning/REQUIREMENTS.md | head -1` starts with `- [x]`
    - `grep "PGBRK-04" .planning/REQUIREMENTS.md | head -1` starts with `- [x]`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/estimate/paginated-editing-preserved.test.tsx tests/unit/estimate/share-webview-pagination-boundary.test.ts && npx vitest run tests/unit tests/eval</automated>
  </verify>
  <done>Focus survives a cross-page-boundary structural edit (proven, not asserted-by-architecture-alone); dnd-kit's SortableContext order stays document-order-only under the paginated overlay; the share webview is provably import-free of every pagination module; REQUIREMENTS.md reflects PGMODE-01..05 and PGBRK-01/04 as complete.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit tests/eval` — full suite green (matches CI's own gate).
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
`grep -c "\[ \] \*\*PGMODE-0\|\[ \] \*\*PGBRK-01\|\[ \] \*\*PGBRK-04" .planning/REQUIREMENTS.md` returns 0 (nothing phase-185-relevant left unchecked).
</verification>

<success_criteria>
Repagination is immediate on structural change and debounced 400ms on text change, with zero per-keystroke thrash; focus/cursor survives a cross-page edit; dnd-kit reorder semantics are provably unaffected by page membership; the public share webview is provably untouched (PGMODE-05); REQUIREMENTS.md shows PGMODE-01..05 and PGBRK-01/04 as complete, closing out Phase 185 and the v4.23 milestone's pagination requirement set.
</success_criteria>

<output>
After completion, create `.planning/phases/185-paginated-editable-editor-mode/185-04-SUMMARY.md` documenting: the final immediate-vs-debounce trigger logic, the focus/dnd-kit regression test's exact assertions, the share-webview boundary test's scope, and the REQUIREMENTS.md diff (which checkboxes/table rows flipped and why) — this is the phase's closing summary for `/gsd:verify-work`.
</output>
