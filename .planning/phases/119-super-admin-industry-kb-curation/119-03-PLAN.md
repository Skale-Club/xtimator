---
phase: 119-super-admin-industry-kb-curation
plan: 03
type: execute
wave: 3
depends_on: ["119-01", "119-02"]
files_modified:
  - tests/unit/admin/knowledge-entry-form.test.tsx
  - app/admin/knowledge/page.tsx
  - app/admin/knowledge/loading.tsx
  - app/admin/knowledge/entry-form.tsx
  - app/admin/knowledge/entry-form-wrapper.tsx
  - app/admin/knowledge/entry-actions.tsx
  - app/admin/knowledge/import-card.tsx
  - app/admin/knowledge/new/page.tsx
  - app/admin/knowledge/[id]/page.tsx
  - app/admin/knowledge/[id]/edit-entry-wrapper.tsx
  - components/admin/admin-nav.tsx
autonomous: false
requirements: [KCUR-01, KCUR-03]
must_haves:
  truths:
    - "A super-admin navigates to /admin/knowledge and sees industry KB entries grouped/filterable by industry"
    - "A super-admin opens a New-entry form (industry select + title + body + source) and saves an entry"
    - "A super-admin opens an existing entry, edits it, and saves; or deletes it from the list"
    - "A super-admin picks an industry, uploads a CSV, sees a valid/invalid preview, and confirms a bulk import"
    - "The Knowledge nav item appears in the super-admin sidebar; the page is gated by requireAdmin"
  artifacts:
    - path: "app/admin/knowledge/page.tsx"
      provides: "list view — requireAdmin, service-client read, industry filter, per-row actions"
      contains: "requireAdmin"
    - path: "app/admin/knowledge/entry-form.tsx"
      provides: "the create/edit form with the INDUSTRIES Select"
      contains: "INDUSTRIES"
    - path: "app/admin/knowledge/import-card.tsx"
      provides: "industry-pick + CSV upload + preview + confirm bulk import"
      contains: "parseKnowledgeCsv"
    - path: "components/admin/admin-nav.tsx"
      provides: "Knowledge nav entry"
      contains: "/admin/knowledge"
  key_links:
    - from: "app/admin/knowledge/entry-form-wrapper.tsx"
      to: "app/admin/knowledge/actions.ts"
      via: "createEntry(data) inside useTransition"
      pattern: "createEntry"
    - from: "app/admin/knowledge/import-card.tsx"
      to: "app/admin/knowledge/actions.ts"
      via: "bulkImportEntries(industryId, rows) on confirm"
      pattern: "bulkImportEntries"
    - from: "app/admin/knowledge/page.tsx"
      to: "knowledge_entries (service client)"
      via: "requireServiceClient().from('knowledge_entries').select(...)"
      pattern: "knowledge_entries"
---

<objective>
Build the super-admin Knowledge UI that wires Plan 01's CRUD actions and Plan 02's bulk import
to a real surface — a list view at `/admin/knowledge`, a create/edit form (mirror
`app/admin/blog/`), per-row edit/delete actions, and an industry-pick + CSV-upload import card.
Plus the sidebar nav entry. This is the SUPER-ADMIN surface ONLY — NOT the company overlay
(Phase 120, a distinct tenant surface).

Purpose: KCUR-01 (the CRUD UI) + KCUR-03 (the import UI) — make the curation actions usable.
Output: the `app/admin/knowledge/` route tree + the admin-nav entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/119-super-admin-industry-kb-curation/119-RESEARCH.md
@.planning/phases/119-super-admin-industry-kb-curation/119-01-PLAN.md
@.planning/phases/119-super-admin-industry-kb-curation/119-02-PLAN.md

<interfaces>
<!-- Contracts to wire. Plans 01 + 02 must be merged first. Mirror app/admin/blog/* verbatim. -->

From app/admin/knowledge/actions.ts (Plans 01 + 02):
```typescript
export type KnowledgeActionResult = { ok: true; imported?: number } | { ok: false; message: string }
export async function createEntry(data: KnowledgeEntryInput): Promise<KnowledgeActionResult>
export async function updateEntry(id: string, data: KnowledgeEntryInput): Promise<KnowledgeActionResult>
export async function deleteEntry(id: string): Promise<KnowledgeActionResult>
export async function bulkImportEntries(industryId: string, rows: KnowledgeImportRow[]): Promise<KnowledgeActionResult>
```

From lib/schemas/knowledge.ts: `knowledgeEntrySchema`, `type KnowledgeEntryInput`
From lib/csv/knowledge-import.ts: `parseKnowledgeCsv(file)`, `type KnowledgeImportRow`, `type KnowledgeParseOutcome`
From lib/industries.ts: `INDUSTRIES` (id, label), `getIndustryLabel(value)`

Mirror targets (read and copy structure verbatim):
- app/admin/blog/page.tsx          -> list page (requireAdmin; svc.from(...).select('*').order('created_at',desc); Card+table; "New" button)
- app/admin/blog/new/page.tsx      -> new/page.tsx (requireAdmin; Card; <EntryFormWrapper/>)
- app/admin/blog/[id]/page.tsx     -> [id]/page.tsx (requireAdmin; svc select by id; notFound(); <EditEntryWrapper entry={...}/>)
- app/admin/blog/post-form.tsx     -> entry-form.tsx (useForm + zodResolver(knowledgeEntrySchema); fields below)
- app/admin/blog/post-form-wrapper.tsx -> entry-form-wrapper.tsx (useTransition; createEntry; toast; router.push('/admin/knowledge'))
- app/admin/blog/[id]/edit-post-wrapper.tsx -> [id]/edit-entry-wrapper.tsx (updateEntry by id)
- app/admin/blog/blog-post-actions.tsx -> entry-actions.tsx (delete via deleteEntry; confirm(); toast; router.refresh())
- app/admin/blog/loading.tsx       -> loading.tsx
- components/admin/admin-nav.tsx   -> add nav entry

knowledge_entries columns to render: id, industry_id, title, source, embedding (null badge), created_at.

The INDUSTRIES Select pattern (from 119-RESEARCH.md Pattern 2):
```tsx
<Select value={industryId} onValueChange={(v) => setValue('industry_id', v)}>
  <SelectTrigger><SelectValue placeholder={t('Select industry')} /></SelectTrigger>
  <SelectContent>
    {INDUSTRIES.map((ind) => (<SelectItem key={ind.id} value={ind.id}>{ind.label}</SelectItem>))}
  </SelectContent>
</Select>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: List page + entry form + edit/delete actions + nav (KCUR-01 UI)</name>
  <files>app/admin/knowledge/page.tsx, app/admin/knowledge/loading.tsx, app/admin/knowledge/entry-form.tsx, app/admin/knowledge/entry-form-wrapper.tsx, app/admin/knowledge/entry-actions.tsx, app/admin/knowledge/new/page.tsx, app/admin/knowledge/[id]/page.tsx, app/admin/knowledge/[id]/edit-entry-wrapper.tsx, components/admin/admin-nav.tsx</files>
  <read_first>
    - app/admin/blog/page.tsx, app/admin/blog/post-form.tsx, app/admin/blog/post-form-wrapper.tsx (copy verbatim, adapt fields)
    - app/admin/blog/new/page.tsx, app/admin/blog/[id]/page.tsx, app/admin/blog/[id]/edit-post-wrapper.tsx, app/admin/blog/blog-post-actions.tsx, app/admin/blog/loading.tsx
    - components/admin/admin-nav.tsx (the NAV_ITEMS array + import line to extend)
    - lib/industries.ts (INDUSTRIES, getIndustryLabel)
  </read_first>
  <action>
    Build the CRUD UI by mirroring app/admin/blog/ verbatim, swapping blog fields for KB fields.
    Every page server component calls `await requireAdmin()` FIRST and reads via `requireServiceClient()`.
    All copy wrapped in `<T>` / `t()` (English locale).

    1. `app/admin/knowledge/page.tsx` (`export const dynamic = 'force-dynamic'`): requireAdmin();
       `svc.from('knowledge_entries').select('id, industry_id, title, source, embedding, created_at').eq('scope','industry').order('created_at',{ascending:false})`.
       Render a Card+table mirroring blog/page.tsx: columns Industry (getIndustryLabel(industry_id)),
       Title (link to `/admin/knowledge/${id}`), Source, an amber `<Badge variant="secondary">Needs reindex</Badge>`
       when `embedding == null` (cheap insurance — with embed-then-insert it should never appear),
       Created, and `<EntryActions id={...} />`. A "New entry" `<Button asChild><Link href="/admin/knowledge/new">`.
       Add a simple client industry filter OR per-industry section grouping (curator works one industry
       at a time) — a `<Select>` of INDUSTRIES filtering the rows is sufficient; if you keep it server-side,
       group rows under `getIndustryLabel` headers. Render `<ImportCard/>` (Task 2) ABOVE or BESIDE the table.
       Empty state: `<T>No entries yet. Create your first industry KB entry.</T>`.

    2. `app/admin/knowledge/loading.tsx` — mirror app/admin/blog/loading.tsx.

    3. `app/admin/knowledge/entry-form.tsx` (`'use client'`) — mirror post-form.tsx with useForm +
       `zodResolver(knowledgeEntrySchema)`. Fields: `industry_id` (the INDUSTRIES Select above; on
       edit, default to initial.industry_id), `title` (Input), `body` (Textarea `min-h-[300px]`),
       `source` (Input, optional). Props: `{ initial?: { industry_id; title; body; source }, onSave, isPending }`.
       Submit button label `t('Save entry')`.

    4. `app/admin/knowledge/entry-form-wrapper.tsx` (`'use client'`) — mirror post-form-wrapper.tsx:
       useTransition; `await createEntry(data)`; on ok toast `t('Entry created.')` + `router.push('/admin/knowledge')`;
       else `toast.error(result.message)`.

    5. `app/admin/knowledge/new/page.tsx` — mirror blog/new/page.tsx: requireAdmin(); Card; `<EntryFormWrapper/>`.

    6. `app/admin/knowledge/[id]/page.tsx` — mirror blog/[id]/page.tsx: requireAdmin(); `const { id } = await params`;
       `svc.from('knowledge_entries').select('*').eq('id', id).maybeSingle()`; `if (!data) notFound()`;
       `<EditEntryWrapper entry={data} />`.

    7. `app/admin/knowledge/[id]/edit-entry-wrapper.tsx` (`'use client'`) — mirror edit-post-wrapper.tsx:
       useTransition; `await updateEntry(entry.id, data)`; toast + `router.push('/admin/knowledge')`.

    8. `app/admin/knowledge/entry-actions.tsx` (`'use client'`) — mirror blog-post-actions.tsx (delete arm only):
       `confirm(t('Delete this entry? This cannot be undone.'))` -> `await deleteEntry(id)` -> toast + router.refresh().

    9. `components/admin/admin-nav.tsx`: add `BookOpen` to the lucide-react import line, and add
       `{ href: '/admin/knowledge', label: 'Knowledge', Icon: BookOpen },` to NAV_ITEMS (place after the
       Blog entry — a sensible grouping with the other content surfaces).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "app/admin/knowledge|admin-nav" || echo "no knowledge/nav tsc errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "requireAdmin" app/admin/knowledge/page.tsx` AND `grep -q "knowledge_entries" app/admin/knowledge/page.tsx`
    - `grep -q "scope" app/admin/knowledge/page.tsx` (reads scope='industry' rows)
    - `grep -q "INDUSTRIES" app/admin/knowledge/entry-form.tsx` AND `grep -q "zodResolver(knowledgeEntrySchema)" app/admin/knowledge/entry-form.tsx`
    - `grep -q "createEntry" app/admin/knowledge/entry-form-wrapper.tsx`
    - `grep -q "updateEntry" "app/admin/knowledge/[id]/edit-entry-wrapper.tsx"`
    - `grep -q "deleteEntry" app/admin/knowledge/entry-actions.tsx`
    - `grep -q "/admin/knowledge" components/admin/admin-nav.tsx` AND `grep -q "BookOpen" components/admin/admin-nav.tsx`
    - all nine new files + admin-nav exist; `npx tsc --noEmit -p tsconfig.json` reports no NEW errors in any app/admin/knowledge/* file or admin-nav.tsx
  </acceptance_criteria>
  <done>The /admin/knowledge list + create/edit form + delete action + sidebar nav are wired to Plan-01's actions; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import card (industry pick + CSV upload + preview + confirm) (KCUR-03 UI)</name>
  <files>app/admin/knowledge/import-card.tsx, app/admin/knowledge/page.tsx, tests/unit/admin/knowledge-entry-form.test.tsx</files>
  <read_first>
    - app/admin/knowledge/page.tsx (where <ImportCard/> mounts — from Task 1)
    - lib/csv/knowledge-import.ts (parseKnowledgeCsv + KnowledgeParseOutcome + KnowledgeImportRow)
    - app/admin/knowledge/actions.ts (bulkImportEntries signature)
    - lib/industries.ts (INDUSTRIES for the industry Select)
    - components/price-book/import-wizard (skim — the simplified 2-step pick->preview shape; do NOT copy the 4-step folder/locale complexity)
  </read_first>
  <behavior>
    - ImportCard renders an INDUSTRIES Select + a file <input type="file" accept=".csv"> + (after parse) a
      preview line "{validCount} valid, {invalidCount} invalid, {inFileDuplicateCount} duplicate" + a
      Confirm button disabled until an industry is chosen AND validCount > 0.
    - On file change -> `parseKnowledgeCsv(file)`; on `ok:false` show the fatal detail via toast; on ok show counts.
    - Confirm -> `bulkImportEntries(industryId, rows.filter(valid).map(r => r.values))` inside useTransition;
      on ok toast `Imported {imported} entries` + router.refresh(); on fail toast the message.
    - The component is a pure unit: parse + the confirm-enabled predicate are testable without a live action.
  </behavior>
  <action>
    1. CREATE `app/admin/knowledge/import-card.tsx` (`'use client'`). A glass `<Card>` titled
       `<T>Bulk import</T>`. State: `industryId` (string), `outcome` (KnowledgeParseOutcome | null),
       `isPending` (useTransition). Render:
       - the INDUSTRIES `<Select value={industryId} onValueChange={setIndustryId}>` (same pattern as the form).
       - `<input type="file" accept=".csv" onChange={onFile}>`.
       - when `outcome?.ok`, a preview `<p>` with validCount / invalidCount / inFileDuplicateCount.
       - when `outcome?.ok === false`, render `outcome.detail` (or toast it).
       - a `<Button disabled={!industryId || !(outcome?.ok && outcome.validCount > 0) || isPending} onClick={onConfirm}>`.
       onFile: read `e.target.files?.[0]`, `setOutcome(await parseKnowledgeCsv(file))`.
       onConfirm: `const rows = outcome.ok ? outcome.rows.filter(r => r.errors.length === 0 && !r.isDuplicateInFile).map(r => r.values) : []`;
       `startTransition(async () => { const res = await bulkImportEntries(industryId, rows);
         if (res.ok) { toast.success(t('Imported {n} entries').replace('{n}', String(res.imported ?? rows.length))); router.refresh() }
         else toast.error(res.message) })`.
       Export `<ImportCard/>` and mount it in `app/admin/knowledge/page.tsx` (Task 1 left the slot).

       Optionally factor the confirm-enabled predicate into a tiny exported pure helper
       `canConfirmImport(industryId: string, outcome: KnowledgeParseOutcome | null): boolean` so the
       test asserts the gate deterministically without rendering the heavy file-input component.

    2. CREATE `tests/unit/admin/knowledge-entry-form.test.tsx` (covers the import gate predicate +
       the form's industry options — the deterministic seams). Assert:
       - `canConfirmImport('', { ok:true, validCount:3, ... })` is false (no industry).
       - `canConfirmImport('plumbing', { ok:true, validCount:0, ... })` is false (no valid rows).
       - `canConfirmImport('plumbing', { ok:true, validCount:3, ... })` is true.
       - `canConfirmImport('plumbing', null)` is false (nothing parsed).
       (If you do not extract the helper, instead render ImportCard with parseKnowledgeCsv mocked and
       assert the Confirm button's disabled state across those four states — but the pure helper is preferred.)
  </action>
  <verify>
    <automated>npx vitest run tests/unit/admin/knowledge-entry-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "parseKnowledgeCsv" app/admin/knowledge/import-card.tsx`
    - `grep -q "bulkImportEntries" app/admin/knowledge/import-card.tsx`
    - `grep -q "accept=\".csv\"" app/admin/knowledge/import-card.tsx`
    - `grep -q "INDUSTRIES" app/admin/knowledge/import-card.tsx`
    - `grep -q "ImportCard" app/admin/knowledge/page.tsx` (mounted on the list page)
    - `npx vitest run tests/unit/admin/knowledge-entry-form.test.tsx` GREEN (all four gate cases)
    - `npx tsc --noEmit -p tsconfig.json` no NEW errors in app/admin/knowledge/import-card.tsx
  </acceptance_criteria>
  <done>The import card lets a super-admin pick an industry, upload a CSV, preview counts, and confirm a bulk import wired to bulkImportEntries; the confirm gate is unit-proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the end-to-end super-admin Knowledge flow</name>
  <action>Human verification only — no code is written in this task. Confirm the steps in how-to-verify below pass.</action>
  <what-built>
    The full super-admin Knowledge surface: a list at /admin/knowledge (grouped/filterable by industry),
    a create/edit form (industry select + title + body + source) wired to createEntry/updateEntry,
    per-row delete, a CSV bulk-import card wired to bulkImportEntries, and the sidebar nav entry — all
    gated by requireAdmin and writing scope='industry' rows with embeddings via the service client.
  </what-built>
  <how-to-verify>
    Prerequisite: the Phase 117/118 migrations must be deployed to the target env (CI->GHCR->Coolify) and
    the OpenRouter key configured (Phase 118) so embed() works. (Open question in 119-RESEARCH.md — verify
    before this checkpoint; on a non-migrated env inserts hit a missing table.)
    1. As a super-admin, visit /admin/knowledge — the Knowledge nav item appears; the list renders (empty is fine).
    2. Click New entry, pick an industry (e.g. Upholstery & Carpet Cleaning), enter a title + body + source, Save.
       Confirm it appears in the list under that industry with NO "Needs reindex" badge (embedding populated).
    3. Open the entry, edit the body, Save — confirm it still has no reindex badge (re-embedded).
    4. Delete the entry from the list — confirm it disappears.
    5. In the Bulk import card, pick an industry, upload a small CSV (title,body,source), confirm the preview
       counts, click Confirm — confirm N entries appear, each with an embedding (no reindex badge).
    6. (Optional, staging) Ask a related trade question through retrieve()/answer() and confirm a curated
       entry surfaces — proving the embedding is live (the manual-only check from 119-VALIDATION.md).
  </how-to-verify>
  <verify><automated>Manual checkpoint — no automated command; gated on human approval.</automated></verify>
  <done>The super-admin verified create/edit/delete + bulk import end-to-end against a migrated env.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/admin/knowledge-entry-form.test.tsx` green; full `npx vitest run` no regressions.
- `npx tsc --noEmit -p tsconfig.json` clean across the new app/admin/knowledge/* tree + admin-nav.
- Every page server component calls requireAdmin() FIRST and reads via requireServiceClient().
- The form Select is driven by INDUSTRIES; create/edit/delete/import wire to the Plan 01/02 actions.
- The Knowledge nav entry is present; this is the super-admin surface ONLY (no tenant/overlay code).
</verification>

<success_criteria>
- KCUR-01: a super-admin can create/edit/delete industry KB entries from /admin/knowledge.
- KCUR-03: a super-admin can pick an industry, upload a CSV, preview, and bulk-import.
- The sidebar exposes Knowledge; the human-verify checkpoint confirms the end-to-end flow.
</success_criteria>

<output>
After completion, create `.planning/phases/119-super-admin-industry-kb-curation/119-03-SUMMARY.md`
</output>
