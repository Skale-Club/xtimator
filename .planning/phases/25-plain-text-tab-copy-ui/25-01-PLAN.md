---
phase: 25-plain-text-tab-copy-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/utils/estimate-template.ts
  - tests/unit/utils/estimate-template.test.ts
autonomous: true
requirements:
  - PLAINTEXT-01
  - PLAINTEXT-02
  - PLAINTEXT-04

must_haves:
  truths:
    - "buildItemsBreakdown formats sections as [Section Title] headers with items as 'description: $price' on each line"
    - "buildItemsBreakdown separates section blocks with a single blank line"
    - "buildItemsBreakdown filters out sections with zero items"
    - "buildItemsBreakdown handles an empty sections array without crashing (returns empty string)"
    - "resolveTemplate can accept items_breakdown built by buildItemsBreakdown and embed it correctly"
  artifacts:
    - path: "lib/utils/estimate-template.ts"
      provides: "buildItemsBreakdown() exported function"
      exports: ["buildItemsBreakdown"]
    - path: "tests/unit/utils/estimate-template.test.ts"
      provides: "Unit tests for buildItemsBreakdown"
      contains: "describe('buildItemsBreakdown'"
  key_links:
    - from: "tests/unit/utils/estimate-template.test.ts"
      to: "lib/utils/estimate-template.ts"
      via: "import { buildItemsBreakdown }"
      pattern: "import.*buildItemsBreakdown.*from.*estimate-template"
    - from: "lib/utils/estimate-template.ts"
      to: "lib/utils/format.ts"
      via: "import { formatCurrency }"
      pattern: "import.*formatCurrency.*from.*format"
---

<objective>
Add `buildItemsBreakdown(estimate: EstimateWithSections): string` to the existing template utility and cover it with unit tests using a TDD RED→GREEN cycle.

Purpose: Plan 02 (PlainTextCard) calls this function to populate `items_breakdown` before passing to `resolveTemplate()`. It must be ready and tested before the component is built.

Output: `buildItemsBreakdown` exported from `lib/utils/estimate-template.ts`, green unit tests in `tests/unit/utils/estimate-template.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-plain-text-tab-copy-ui/25-CONTEXT.md

<interfaces>
<!-- Key types the executor needs. Read directly from codebase. -->

From lib/utils/estimate-template.ts (current state — full file):
```typescript
export interface TemplateData {
  client_name: string
  company_name: string
  owner_name: string
  total: string          // pre-formatted, e.g. "$1,250.00"
  items_breakdown: string // multi-line text block
}

export interface EstimateTemplate {
  greeting: string | null
  opener: string | null
  closer: string | null
  signature: string | null
}

export const TEMPLATE_DEFAULTS = {
  greeting: 'Hey {client_name},',
  opener: "Thank you for reaching out to {company_name}! Here is your estimate:",
  closer: "Let me know if you have any questions...",
  signature: 'Best regards,\n{owner_name}\n{company_name}',
} as const

export function resolveTemplate(template: EstimateTemplate, data: TemplateData): string
```

From lib/queries/estimate.ts:
```typescript
export interface EstimateSection {
  id: string
  estimate_id: string
  company_id: string
  title: string
  sort_order: number
  subtotal: number
}

export interface EstimateItem {
  id: string
  section_id: string
  company_id: string
  description: string
  quantity: number
  unit: string | null
  unit_price: number
  total: number        // use this for the price display
  sort_order: number
  price_source: 'price_book' | 'ai_estimate' | null
}

export interface EstimateWithSections extends Estimate {
  sections: (EstimateSection & { items: EstimateItem[] })[]
}
```

From lib/utils/format.ts:
```typescript
export function formatCurrency(value: number): string  // uses Intl.NumberFormat en-US, USD, 2 decimals
```

From tests/unit/utils/estimate-template.test.ts (current imports at top — extend this file):
```typescript
import { describe, it, expect } from 'vitest'
import { resolveTemplate, TEMPLATE_DEFAULTS } from '@/lib/utils/estimate-template'
import type { EstimateTemplate, TemplateData } from '@/lib/utils/estimate-template'
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Add failing buildItemsBreakdown tests</name>
  <files>tests/unit/utils/estimate-template.test.ts</files>
  <read_first>
    - tests/unit/utils/estimate-template.test.ts (current file — MUST read before editing)
    - lib/utils/estimate-template.ts (to confirm buildItemsBreakdown does NOT yet exist)
    - lib/utils/format.ts (confirm formatCurrency signature)
  </read_first>
  <action>
Extend `tests/unit/utils/estimate-template.test.ts` with a new `describe('buildItemsBreakdown', ...)` block. Add these imports at the top alongside the existing ones:

```typescript
import { buildItemsBreakdown } from '@/lib/utils/estimate-template'
import type { EstimateWithSections } from '@/lib/queries/estimate'
```

Then add the following describe block AFTER the existing `resolveTemplate` describe block (do not modify existing tests):

```typescript
describe('buildItemsBreakdown', () => {
  const SAMPLE_ESTIMATE: EstimateWithSections = {
    id: 'est-1',
    project_id: 'proj-1',
    company_id: 'co-1',
    version: 1,
    is_current: true,
    share_token: 'tok',
    status: 'draft',
    summary: null,
    notes: null,
    timeline: null,
    payment_terms: null,
    warranty_terms: null,
    subtotal: 205,
    discount_type: null,
    discount_value: 0,
    discount_amount: 0,
    tax_rate: 0,
    tax_amount: 0,
    total: 205,
    sent_at: null,
    viewed_at: null,
    responded_at: null,
    client_response: null,
    created_at: '2026-01-01',
    updated_at: '2026-01-01',
    sections: [
      {
        id: 'sec-1',
        estimate_id: 'est-1',
        company_id: 'co-1',
        title: 'Upholstery Cleaning',
        sort_order: 0,
        subtotal: 120,
        items: [
          {
            id: 'item-1',
            section_id: 'sec-1',
            company_id: 'co-1',
            description: 'King Mattress',
            quantity: 1,
            unit: null,
            unit_price: 120,
            total: 120,
            sort_order: 0,
            price_source: null,
          },
        ],
      },
      {
        id: 'sec-2',
        estimate_id: 'est-1',
        company_id: 'co-1',
        title: 'Carpet Cleaning',
        sort_order: 1,
        subtotal: 85,
        items: [
          {
            id: 'item-2',
            section_id: 'sec-2',
            company_id: 'co-1',
            description: 'Small room',
            quantity: 1,
            unit: null,
            unit_price: 85,
            total: 85,
            sort_order: 0,
            price_source: null,
          },
        ],
      },
    ],
  }

  it('formats section title as [Section Title] on its own line', () => {
    const output = buildItemsBreakdown(SAMPLE_ESTIMATE)
    expect(output).toContain('[Upholstery Cleaning]')
    expect(output).toContain('[Carpet Cleaning]')
  })

  it('formats each item as "description: $price" with colon separator', () => {
    const output = buildItemsBreakdown(SAMPLE_ESTIMATE)
    expect(output).toContain('King Mattress: $120.00')
    expect(output).toContain('Small room: $85.00')
  })

  it('separates section blocks with a blank line', () => {
    const output = buildItemsBreakdown(SAMPLE_ESTIMATE)
    expect(output).toContain('[Upholstery Cleaning]\nKing Mattress: $120.00\n\n[Carpet Cleaning]')
  })

  it('returns empty string for estimate with no sections', () => {
    const empty: EstimateWithSections = { ...SAMPLE_ESTIMATE, sections: [] }
    expect(buildItemsBreakdown(empty)).toBe('')
  })

  it('filters out sections with no items', () => {
    const withEmpty: EstimateWithSections = {
      ...SAMPLE_ESTIMATE,
      sections: [
        { id: 'sec-0', estimate_id: 'est-1', company_id: 'co-1', title: 'Empty Section', sort_order: 0, subtotal: 0, items: [] },
        ...SAMPLE_ESTIMATE.sections,
      ],
    }
    const output = buildItemsBreakdown(withEmpty)
    expect(output).not.toContain('[Empty Section]')
    expect(output).toContain('[Upholstery Cleaning]')
  })
})
```

Run the test and CONFIRM it fails (RED) because `buildItemsBreakdown` does not exist yet:
```
npx vitest run tests/unit/utils/estimate-template.test.ts 2>&1 | head -30
```
The test must fail with an import or "not exported" error — NOT a pass.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/utils/estimate-template.test.ts 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/utils/estimate-template.test.ts contains `describe('buildItemsBreakdown'`
    - tests/unit/utils/estimate-template.test.ts contains `import { buildItemsBreakdown } from '@/lib/utils/estimate-template'`
    - running the test suite exits with non-zero status (RED phase — buildItemsBreakdown not yet implemented)
    - the existing resolveTemplate tests are unchanged (all 6 existing tests still present)
  </acceptance_criteria>
  <done>Five new failing tests for buildItemsBreakdown exist; existing resolveTemplate tests untouched.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement buildItemsBreakdown + pass all tests</name>
  <files>lib/utils/estimate-template.ts</files>
  <read_first>
    - lib/utils/estimate-template.ts (current file — MUST read before editing)
    - lib/utils/format.ts (confirm formatCurrency export)
    - tests/unit/utils/estimate-template.test.ts (read failing tests to understand exact expected output format)
  </read_first>
  <action>
Add the following to `lib/utils/estimate-template.ts`. Place the import at the top of the file (after the existing comment block, before the interfaces) and the function at the bottom of the file after `resolveTemplate`.

Add import at top:
```typescript
import type { EstimateWithSections } from '@/lib/queries/estimate'
import { formatCurrency } from '@/lib/utils/format'
```

Add function at bottom of file:
```typescript
/**
 * Build the items_breakdown string for use in resolveTemplate().
 * Format (per D-02 / SEED-004):
 *   [Section Title]
 *   Item description: $120.00
 *
 *   [Next Section]
 *   Item description: $85.00
 *
 * Sections with no items are filtered out. Empty estimate returns ''.
 */
export function buildItemsBreakdown(estimate: EstimateWithSections): string {
  return estimate.sections
    .filter((section) => section.items.length > 0)
    .map((section) => {
      const header = `[${section.title}]`
      const items = section.items
        .map((item) => `${item.description}: ${formatCurrency(item.total)}`)
        .join('\n')
      return `${header}\n${items}`
    })
    .join('\n\n')
}
```

After implementing, run the test suite and confirm GREEN:
```
npx vitest run tests/unit/utils/estimate-template.test.ts
```
All tests must pass, including the 6 existing resolveTemplate tests.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/utils/estimate-template.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/utils/estimate-template.ts exports `buildItemsBreakdown` (grep: `export function buildItemsBreakdown`)
    - lib/utils/estimate-template.ts imports `formatCurrency` from `@/lib/utils/format` (grep: `import.*formatCurrency.*from.*format`)
    - lib/utils/estimate-template.ts imports `EstimateWithSections` from `@/lib/queries/estimate`
    - `npx vitest run tests/unit/utils/estimate-template.test.ts` exits 0 with all 11 tests passing (6 existing + 5 new)
    - `npx vitest run` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>buildItemsBreakdown implemented and all 11 tests pass including the 5 new buildItemsBreakdown tests.</done>
</task>

</tasks>

<verification>
Run full unit suite:
```
npx vitest run
```
All tests green. `lib/utils/estimate-template.ts` exports `buildItemsBreakdown`. TypeScript compiles without errors (`npx tsc --noEmit`).
</verification>

<success_criteria>
- `buildItemsBreakdown` exported from `lib/utils/estimate-template.ts`
- 5 new unit tests all GREEN
- All 6 existing resolveTemplate tests still GREEN
- Full Vitest suite exits 0
- Output matches SEED-004 reference format: `[Section Title]\nitem: $price\n\n[Next Section]\n...`
</success_criteria>

<output>
After completion, create `.planning/phases/25-plain-text-tab-copy-ui/25-01-SUMMARY.md`
</output>
