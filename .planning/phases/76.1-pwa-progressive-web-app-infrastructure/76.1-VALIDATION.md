---
phase: 76.1
slug: pwa-progressive-web-app-infrastructure
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-21
---

# Phase 76.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | vitest.config.ts |
| **Quick run command** | `npx vitest run tests/unit/pwa/` |
| **Full suite command** | `npm run test -- --run` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run tests/unit/pwa/`
- **After every plan wave:** Run `npm run test -- --run`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 76.1-01-01 | 01 | 1 | PWA-01, PWA-06 | unit | `npx vitest run tests/unit/pwa/` | ❌ W0 | ⬜ pending |
| 76.1-01-02 | 01 | 1 | PWA-04 | unit | `npx vitest run tests/unit/pwa/install-prompt.test.tsx` | ❌ W0 | ⬜ pending |
| 76.1-02-01 | 02 | 2 | PWA-05 | unit | `npx vitest run tests/unit/pwa/offline-indicator.test.tsx` | ✅ | ⬜ pending |
| 76.1-02-02 | 02 | 2 | PWA-01, PWA-02, PWA-03 | manual | Lighthouse audit + device install test | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/pwa/install-prompt.test.tsx` — unit stubs for PWA-04 (estimate gate, dismissal)
- [ ] `tests/unit/pwa/offline-indicator.test.tsx` — unit stubs for PWA-05 (CTA disabling)

*Existing vitest infrastructure covers the framework requirement.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Lighthouse PWA audit passes | PWA-01 | No automated Lighthouse in CI | Run DevTools → Lighthouse → Mobile → PWA; all checks must be green |
| iOS Safari "Add to Home Screen" | PWA-01 | Requires physical device or simulator | Navigate to app on iOS Safari, tap Share → Add to Home Screen; icon + splash correct |
| Android Chrome install prompt | PWA-01 | Requires device or Chrome DevTools | DevTools → Application → Manifest → "Add to homescreen" |
| Offline cache read-only | PWA-02, PWA-03 | Requires manual network throttling | Open /estimates, kill network, reload — list must render from cache |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
