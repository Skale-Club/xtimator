---
phase: 99-unified-error-model-provider-fallback
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/ai/with-fallback.test.ts
  - tests/unit/ai/transcribe-fallback.test.ts
  - tests/unit/ai/gemini-adapter.test.ts
  - tests/unit/estimate/failure-mapping.test.ts
  - tests/unit/api/refine-error-surface.test.ts
  - tests/unit/estimate/never-throw.test.ts
autonomous: true
requirements: [HARD-03, HARD-04]

must_haves:
  truths:
    - "The seven Wave-0 test files exist and FAIL (RED) because their target source modules do not exist yet"
    - "Each Wave-0 test names the exact behavior an implementation task must make GREEN"
  artifacts:
    - path: "tests/unit/ai/with-fallback.test.ts"
      provides: "callWithFallback RED tests: primary-success, fallback-fired, both-fail, single-call"
      contains: "callWithFallback"
    - path: "tests/unit/ai/transcribe-fallback.test.ts"
      provides: "transcribeAudioOR RED tests: key-absent path preserved + failure-based fallback"
      contains: "transcribeAudioOR"
    - path: "tests/unit/estimate/failure-mapping.test.ts"
      provides: "FailureReason -> XtimatorError map + strict-superset RED tests"
      contains: "failureReasonToXtimatorError"
    - path: "tests/unit/api/refine-error-surface.test.ts"
      provides: "refine route returns typed JSON {error, code} not opaque 500"
      contains: "code"
  key_links:
    - from: "tests/unit/ai/with-fallback.test.ts"
      to: "lib/ai/with-fallback.ts"
      via: "import { callWithFallback }"
      pattern: "from '@/lib/ai/with-fallback'"
    - from: "tests/unit/estimate/failure-mapping.test.ts"
      to: "lib/estimate/failure.ts"
      via: "import { failureReasonToXtimatorError, failureReasonToChannelCopy }"
      pattern: "from '@/lib/estimate/failure'"
---

<objective>
Create the Wave-0 failing test scaffold (Nyquist) for Phase 99 before any implementation lands. These RED tests pin the exact behavioral contracts for HARD-03 (shared OpenRouter->Gemini fallback) and HARD-04 (typed FailureReason union + failure->XtimatorError mapping + refine error-surface fix), so Wave-1 plans 99-01 and 99-02 implement against tests rather than exploring.

Purpose: Lock the contracts the implementation must satisfy; guarantee every Wave-1 task has an automated verify that is RED now and GREEN after.
Output: 5 new test files + 2 extended test files, all RED (or with new RED cases) because target source modules (`lib/ai/with-fallback.ts`, `lib/estimate/failure.ts`, `analyzePhotoGemini`) do not exist yet and the refine route still 500s.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/99-unified-error-model-provider-fallback/99-RESEARCH.md
@.planning/phases/99-unified-error-model-provider-fallback/99-VALIDATION.md

<interfaces>
<!-- Contracts these RED tests assert. Wave-1 plans 99-01/99-02 implement them. -->

Wrapper (lib/ai/with-fallback.ts — to be created in 99-01):
```typescript
export interface FallbackOutcome<T> { result: T; servedBy: 'primary' | 'fallback'; fallbackFired: boolean }
export async function callWithFallback<T>(args: {
  op: string
  primary: () => Promise<T>
  fallback: () => Promise<T>
}): Promise<FallbackOutcome<T>>
```

Failure model (lib/estimate/failure.ts — to be created in 99-02):
```typescript
export type FailureReason =
  | 'no_usable_input' | 'transcription_failed' | 'vision_failed'
  | 'generation_failed' | 'invalid_output' | 'provider_unavailable'
export function failureReasonToXtimatorError(reason: FailureReason, detail?: string): XtimatorError
export function failureReasonToChannelCopy(reason: FailureReason, language?: 'en'|'pt'|'es'): string
```

Existing types to import (already present):
- `XtimatorError` from '@/lib/errors' — has `.type` (ErrorType), `.surface` (Surface), `.statusCode`, `.code`
- `statusByType` from '@/lib/errors/codes' — { internal:500, offline:503, bad_request:400, ... }
- `transcribeAudioOR(audioBlob: Blob, ext: string, model?: string)` from '@/lib/ai/openrouter-client'
- `GeminiAdapter`, `analyzePhotoGemini(base64, mimeType)` (vision, to be added in 99-01) from '@/lib/ai/providers/gemini'

Existing test mock pattern (copy from tests/unit/ai/gemini-adapter.test.ts lines 1-17):
vi.mock('@google/genai', ...), vi.mock('@/lib/platform-config', () => ({ getIntegrationKey: vi.fn() })), explicit `import { describe, expect, it, vi, beforeEach } from 'vitest'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests for callWithFallback wrapper (HARD-03)</name>
  <read_first>
    - .planning/phases/99-unified-error-model-provider-fallback/99-RESEARCH.md (Pattern 1: callWithFallback; Validation Architecture test map)
    - tests/unit/ai/gemini-adapter.test.ts (lines 1-17 — exact vitest import + vi.mock pattern to mirror)
  </read_first>
  <files>tests/unit/ai/with-fallback.test.ts</files>
  <action>
    Create `tests/unit/ai/with-fallback.test.ts`. Start with `import { describe, expect, it, vi, beforeEach } from 'vitest'` (explicit — globals fail tsc per Decision Phase-22). Import the not-yet-existing module: `import { callWithFallback } from '@/lib/ai/with-fallback'`.
    Write a `describe('callWithFallback', ...)` with these exact `it` titles so the VALIDATION.md `-t` selectors resolve:
    - `it('primary success — returns servedBy:primary, fallbackFired:false, fallback NOT called')`: primary = `vi.fn().mockResolvedValue('A')`, fallback = `vi.fn().mockResolvedValue('B')`. Expect `result === 'A'`, `servedBy === 'primary'`, `fallbackFired === false`, and `expect(fallback).not.toHaveBeenCalled()`.
    - `it('single call — primary invoked exactly once on success (QA-03)')`: same as above; assert `expect(primary).toHaveBeenCalledTimes(1)` and `expect(fallback).toHaveBeenCalledTimes(0)`.
    - `it('fallback fired — primary throws, returns fallback result with servedBy:fallback, fallbackFired:true')`: primary = `vi.fn().mockRejectedValue(new Error('primary down'))`, fallback = `vi.fn().mockResolvedValue('B')`. Expect `result === 'B'`, `servedBy === 'fallback'`, `fallbackFired === true`, `expect(fallback).toHaveBeenCalledTimes(1)`.
    - `it('both fail — re-throws the PRIMARY error, not the fallback error')`: primary rejects with `new Error('PRIMARY_ERR')`, fallback rejects with `new Error('FALLBACK_ERR')`. Use `await expect(callWithFallback({ op:'generate', primary, fallback })).rejects.toThrow('PRIMARY_ERR')`.
    These MUST be RED now (module does not exist) — do NOT stub the module.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai/with-fallback.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists and imports `callWithFallback` from '@/lib/ai/with-fallback'
    - Contains the 4 `it` titles matching VALIDATION selectors: "primary success", "single call", "fallback fired", "both fail"
    - Run is RED: fails to resolve '@/lib/ai/with-fallback' (or assertion failure), NOT a passing suite
  </acceptance_criteria>
  <done>RED test file present with 4 cases; vitest run fails on missing module.</done>
</task>

<task type="auto">
  <name>Task 2: RED tests for transcription failure-fallback + extend Gemini vision (HARD-03)</name>
  <read_first>
    - lib/ai/openrouter-client.ts (lines 62-107 — transcribeAudioOR: key-absent branch + Whisper fetch)
    - lib/ai/providers/gemini.ts (lines 39-57 — transcribeAudioGemini shape that analyzePhotoGemini mirrors)
    - tests/unit/ai/gemini-adapter.test.ts (lines 1-40 — mock harness to extend)
  </read_first>
  <files>tests/unit/ai/transcribe-fallback.test.ts, tests/unit/ai/gemini-adapter.test.ts</files>
  <action>
    (a) Create `tests/unit/ai/transcribe-fallback.test.ts`. Explicit vitest import. `vi.mock('@/lib/platform-config', () => ({ getIntegrationKey: vi.fn() }))` and `vi.mock('@/lib/ai/providers/gemini', () => ({ transcribeAudioGemini: vi.fn().mockResolvedValue('gemini transcript') }))`. Use `global.fetch = vi.fn()` to drive the Whisper path. Import `transcribeAudioOR` from '@/lib/ai/openrouter-client'.
    Write `describe('transcribeAudioOR fallback', ...)` with:
    - `it('key-absent path preserved — no OpenAI key delegates straight to Gemini')`: `getIntegrationKey` returns null for 'openai'. Expect result === 'gemini transcript' and `fetch` NOT called.
    - `it('failure-based fallback — OpenAI key present but Whisper fetch fails -> Gemini transcript')`: `getIntegrationKey('openai')` returns a key, `fetch` resolves `{ ok:false, status:500, text: async()=>'err' }` (or rejects). Expect result === 'gemini transcript' (Gemini fallback fired). THIS case is RED today because transcribeAudioOR currently throws on Whisper failure with no failure-based fallback.
    Build the Blob arg via `new Blob(['x'], { type: 'audio/webm' })`, ext `'webm'`.
    (b) Extend `tests/unit/ai/gemini-adapter.test.ts`: add `describe('analyzePhotoGemini vision', ...)` containing `it('vision — returns a description from base64 + mimeType')`. Import `analyzePhotoGemini` from '@/lib/ai/providers/gemini' (does not exist yet -> RED). Drive the existing `mockGeminiGenerate` to resolve `{ text: 'a wall with cracks' }`; call `analyzePhotoGemini('aGVsbG8=', 'image/jpeg')`; expect result === 'a wall with cracks' and that `mockGeminiGenerate` was called with a `contents` array containing an object `{ inlineData: { mimeType: 'image/jpeg', data: 'aGVsbG8=' } }`.
    Keep the existing gemini-adapter tests untouched and green.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai/transcribe-fallback.test.ts tests/unit/ai/gemini-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ai/transcribe-fallback.test.ts` exists with both "key-absent path preserved" and "failure-based fallback" cases
    - The "failure-based fallback" case is RED (current transcribeAudioOR has no failure-based fallback)
    - gemini-adapter.test.ts has a `analyzePhotoGemini vision` case that is RED (export does not exist); pre-existing cases still pass
  </acceptance_criteria>
  <done>Two transcription cases + one vision case authored; new cases RED, existing cases green.</done>
</task>

<task type="auto">
  <name>Task 3: RED tests for failure mapping, refine error-surface, never-throw provider_unavailable (HARD-04)</name>
  <read_first>
    - lib/errors/codes.ts (ErrorType, Surface, statusByType — the targets the map asserts against)
    - lib/errors/index.ts (XtimatorError shape: .type/.surface/.statusCode/.code)
    - tests/unit/estimate/never-throw.test.ts (existing ENGINE-04 invariant shape to extend)
    - app/api/estimates/[id]/refine/route.ts (lines 285-291 — current bespoke 500 catch)
  </read_first>
  <files>tests/unit/estimate/failure-mapping.test.ts, tests/unit/api/refine-error-surface.test.ts, tests/unit/estimate/never-throw.test.ts</files>
  <action>
    (a) Create `tests/unit/estimate/failure-mapping.test.ts`. Explicit vitest import. Import `failureReasonToXtimatorError, failureReasonToChannelCopy, type FailureReason` from '@/lib/estimate/failure' (does not exist -> RED). Import `XtimatorError` from '@/lib/errors'.
    - `describe('failureReasonToXtimatorError')` -> `it('maps each FailureReason to the expected ErrorType/status')`: assert for each reason the returned `.type` and `.statusCode` and `.surface === 'estimates'`, per this table: `no_usable_input`->bad_request/400, `transcription_failed`->internal/500, `vision_failed`->internal/500, `generation_failed`->internal/500, `invalid_output`->internal/500, `provider_unavailable`->offline/503. Assert the result `instanceof XtimatorError`.
    - `it('superset — generation_failed and no_usable_input remain valid FailureReason values')`: build a `const reasons: FailureReason[] = ['generation_failed','no_usable_input']` and assert each maps without throwing. (This guards the strict-superset migration — the only two values produced today.)
    - `describe('failureReasonToChannelCopy')` -> `it('returns non-empty human copy for generation_failed and no_usable_input')`: assert both return non-empty strings (Wave-1 sources the EXACT current WhatsApp strings; here just assert non-empty so copy is regression-gated by channel-adapter.test.ts).
    (b) Create `tests/unit/api/refine-error-surface.test.ts`. This asserts the refine handler returns a typed JSON envelope `{ error, code }` (not the bespoke `{ error }` 500) when an inner step throws. Approach: import the route POST handler, mock its auth + dependencies so it reaches a thrown error, and assert the response JSON has a `code` field and status maps via statusByType. Because full route mocking is heavy, the MINIMAL RED assertion: mock `@/lib/ai/openrouter-client` `transcribeAudioOR` to reject, drive a request with an audio file, and `expect((await res.json()).code).toBeDefined()`. Title: `it('refine returns typed JSON {error, code}, not opaque 500, on a thrown inner error')`. This is RED today (route returns `{ error }` with no `code`). If wiring the full handler proves infeasible in unit scope, fall back to asserting the contract at the `asResponse` boundary: `import { asResponse }` and `expect((await asResponse(new Error('x')).json()).code).toBe('internal:unknown')` PLUS a `it.todo` documenting the route-level assertion — but PREFER the route-level test.
    (c) Extend `tests/unit/estimate/never-throw.test.ts`: add `it('both providers down -> resolves to { failure: { reason: provider_unavailable } }, never throws')`. Construct the generate node / graph scenario already used in the file, but make the underlying AI call reject on BOTH primary and fallback; assert the graph result carries `failure.reason === 'provider_unavailable'` and that `graph.invoke` did NOT throw. This is RED until 99-01 wires callWithFallback->provider_unavailable into generate.ts. Keep all existing never-throw cases green.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/failure-mapping.test.ts tests/unit/api/refine-error-surface.test.ts tests/unit/estimate/never-throw.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `failure-mapping.test.ts` exists, imports from '@/lib/estimate/failure', asserts all 6 reason->ErrorType/status rows and the superset case; RED on missing module
    - `refine-error-surface.test.ts` exists and asserts a `code` field is present on the error response; RED today
    - never-throw.test.ts has a new `provider_unavailable` both-down case; RED today; pre-existing cases pass
  </acceptance_criteria>
  <done>Three HARD-04 test artifacts authored; new cases RED; existing never-throw cases green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/ai/with-fallback.test.ts tests/unit/ai/transcribe-fallback.test.ts tests/unit/estimate/failure-mapping.test.ts tests/unit/api/refine-error-surface.test.ts` — all RED (missing modules / unimplemented behavior).
- `npx vitest run tests/unit/ai/gemini-adapter.test.ts tests/unit/estimate/never-throw.test.ts` — pre-existing cases GREEN, new cases RED.
- No watch flags; each command finishes < 30s.
</verification>

<success_criteria>
- 5 new + 2 extended test files exist on disk.
- Every Wave-1 task in 99-01 and 99-02 has a corresponding RED test selectable by `-t`.
- No source modules created in this plan (test-only).
</success_criteria>

<output>
After completion, create `.planning/phases/99-unified-error-model-provider-fallback/99-00-SUMMARY.md`.
</output>
