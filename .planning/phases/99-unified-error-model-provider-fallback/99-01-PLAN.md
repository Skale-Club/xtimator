---
phase: 99-unified-error-model-provider-fallback
plan: 01
type: execute
wave: 1
depends_on: ["99-00"]
files_modified:
  - lib/ai/with-fallback.ts
  - lib/ai/providers/gemini.ts
  - lib/ai/provider-with-fallback.ts
  - lib/ai/openrouter-client.ts
  - lib/services/generate-estimate.ts
autonomous: true
requirements: [HARD-03]

must_haves:
  truths:
    - "Every AI call path (generate, transcribe, vision) attempts OpenRouter first and falls back to Gemini exactly once on failure"
    - "On primary success no fallback fires and exactly one AI call is made (QA-03)"
    - "When both providers fail the original primary error is re-thrown so the existing never-throw graph node converts it to a failure state"
    - "Transcription still short-circuits straight to Gemini when no OpenAI key is configured (key-absent path preserved)"
    - "Gemini vision (analyzePhotoGemini) exists and produces a description from base64 + mimeType"
  artifacts:
    - path: "lib/ai/with-fallback.ts"
      provides: "callWithFallback generic wrapper returning { result, servedBy, fallbackFired }"
      min_lines: 20
      exports: ["callWithFallback", "FallbackOutcome"]
    - path: "lib/ai/providers/gemini.ts"
      provides: "analyzePhotoGemini(base64, mimeType) vision fallback mirroring transcribeAudioGemini"
      contains: "analyzePhotoGemini"
    - path: "lib/ai/provider-with-fallback.ts"
      provides: "getAIProviderWithFallback(companyId) — OpenRouter primary + Gemini fallback for generate/refine"
      contains: "getAIProviderWithFallback"
  key_links:
    - from: "lib/services/generate-estimate.ts"
      to: "lib/ai/provider-with-fallback.ts"
      via: "getAIProviderWithFallback(companyId).generateEstimate(input)"
      pattern: "getAIProviderWithFallback"
    - from: "lib/ai/openrouter-client.ts"
      to: "lib/ai/with-fallback.ts"
      via: "callWithFallback inside transcribeAudioOR and analyzePhotoOR"
      pattern: "callWithFallback"
    - from: "lib/ai/openrouter-client.ts"
      to: "lib/ai/providers/gemini.ts"
      via: "dynamic import of analyzePhotoGemini / transcribeAudioGemini as the fallback thunk"
      pattern: "analyzePhotoGemini"
---

<objective>
Implement HARD-03: one shared OpenRouter->Gemini provider-fallback policy applied uniformly to all AI call paths (generate, transcribe, vision; refine inherits via the same fallback-aware provider). Adds the generic `callWithFallback` wrapper, the Gemini vision fallback `analyzePhotoGemini`, a thin `getAIProviderWithFallback(companyId)` so generate/refine call sites are a one-line swap, and wires transcription + vision to fall back on FAILURE (not just key-absence).

Purpose: Every AI path degrades the same way; the refine path (Phase 101) and observability seam (Phase 100) inherit fallback for free.
Output: `lib/ai/with-fallback.ts`, `analyzePhotoGemini`, `lib/ai/provider-with-fallback.ts`, wrapped `transcribeAudioOR`/`analyzePhotoOR`, and `generate-estimate.ts` swapped onto the fallback-aware provider. Makes the Wave-0 HARD-03 tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/99-unified-error-model-provider-fallback/99-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Concrete signatures the executor builds against — no codebase exploration needed. -->

Existing primary providers (do NOT modify):
- `OpenRouterAdapter` from '@/lib/ai/providers/openrouter' — implements AIProvider: `generateEstimate(input)`, `refineEstimate(input)`.
- `GeminiAdapter` from '@/lib/ai/providers/gemini' — implements AIProvider: `generateEstimate`, `refineEstimate` ALREADY EXIST (lines 59-228). DO NOT rebuild Gemini chat/refine.
- `getAIProvider(companyId?)` from '@/lib/ai' — resolves primary OpenRouter model (per-company override). Keep UNTOUCHED (model selection is orthogonal).
- `transcribeAudioGemini(audioBlob: Blob, ext: string)` from '@/lib/ai/providers/gemini' (lines 39-57) — transcription fallback (exists).

Existing call sites:
- `lib/services/generate-estimate.ts:180-181`: `const provider = await getAIProvider(companyId); const aiEstimate = await provider.generateEstimate(estimateInput)`.
- `lib/ai/openrouter-client.ts:62-107` `transcribeAudioOR(audioBlob, ext, model)`: key-absent branch (lines 67-74) delegates to transcribeAudioGemini; key-present path does Whisper fetch and THROWS on non-2xx (lines 88-91).
- `lib/ai/openrouter-client.ts:120-181` `analyzePhotoOR(base64, mimeType, model)`: OpenRouter vision fetch, throws on non-2xx, NO fallback.

Gemini vision shape to add (proven by transcribeAudioGemini — byte-identical inlineData with an image MIME):
```typescript
export async function analyzePhotoGemini(base64: string, mimeType: string): Promise<string> {
  const apiKey = await getIntegrationKey('gemini')
  if (!apiKey) throw new Error('Gemini API key not configured')
  const ai = new GoogleGenAI({ apiKey })
  const response = await ai.models.generateContent({
    model: 'gemini-2.5-flash',
    contents: [
      { text: PHOTO_PROMPT },
      { inlineData: { mimeType, data: base64 } },
    ],
  })
  return (response.text ?? '').trim()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: callWithFallback wrapper + analyzePhotoGemini vision fallback</name>
  <read_first>
    - tests/unit/ai/with-fallback.test.ts (the RED contract from 99-00 — make GREEN)
    - tests/unit/ai/gemini-adapter.test.ts (the analyzePhotoGemini vision RED case)
    - lib/ai/providers/gemini.ts (lines 1-57 — imports + transcribeAudioGemini to mirror)
    - .planning/phases/99-unified-error-model-provider-fallback/99-RESEARCH.md (Pattern 1 + Pattern 2)
  </read_first>
  <files>lib/ai/with-fallback.ts, lib/ai/providers/gemini.ts</files>
  <behavior>
    - callWithFallback: primary success -> { result, servedBy:'primary', fallbackFired:false }, fallback NOT called, primary called once.
    - callWithFallback: primary throws -> runs fallback once -> { result, servedBy:'fallback', fallbackFired:true }.
    - callWithFallback: both throw -> re-throws the ORIGINAL primary error (not the fallback error).
    - analyzePhotoGemini: returns the trimmed text from a Gemini generateContent call whose contents include `{ inlineData: { mimeType, data: base64 } }`.
  </behavior>
  <action>
    (a) Create `lib/ai/with-fallback.ts` exporting `interface FallbackOutcome<T> { result: T; servedBy: 'primary' | 'fallback'; fallbackFired: boolean }` and `async function callWithFallback<T>(args: { op: string; primary: () => Promise<T>; fallback: () => Promise<T> }): Promise<FallbackOutcome<T>>`. Body: `try { const result = await args.primary(); return { result, servedBy: 'primary', fallbackFired: false } } catch (primaryErr) { try { const result = await args.fallback(); return { result, servedBy: 'fallback', fallbackFired: true } } catch { throw primaryErr } }`. The wrapper NEVER swallows the final failure — re-throw the PRIMARY error (so callers map it to 'provider_unavailable'). Keep `op` only for future logging; do not add a retry loop. Server-only file (no 'use client').
    (b) Add `analyzePhotoGemini(base64: string, mimeType: string): Promise<string>` to `lib/ai/providers/gemini.ts`, mirroring `transcribeAudioGemini` (lines 39-57) exactly: read key via `getIntegrationKey('gemini')`, throw 'Gemini API key not configured' if absent, `new GoogleGenAI({ apiKey })`, `ai.models.generateContent({ model: 'gemini-2.5-flash', contents: [{ text: PHOTO_PROMPT }, { inlineData: { mimeType, data: base64 } }] })`, return `(response.text ?? '').trim()`. Reuse the same contractor PHOTO_PROMPT copy used by analyzePhotoOR (define a module-local `const PHOTO_PROMPT` in gemini.ts with the same wording).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai/with-fallback.test.ts tests/unit/ai/gemini-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/ai/with-fallback.ts` exports `callWithFallback` + `FallbackOutcome`; all 4 with-fallback.test.ts cases GREEN
    - `analyzePhotoGemini` exported from gemini.ts; vision case GREEN; pre-existing gemini-adapter cases still GREEN
    - No retry loop in the wrapper; fallback called at most once
  </acceptance_criteria>
  <done>Wrapper + vision fallback land; their Wave-0 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire transcription + vision to failure-based fallback</name>
  <read_first>
    - tests/unit/ai/transcribe-fallback.test.ts (the RED contract — make GREEN)
    - lib/ai/openrouter-client.ts (lines 62-181 — transcribeAudioOR + analyzePhotoOR to wrap)
    - .planning/phases/99-unified-error-model-provider-fallback/99-RESEARCH.md (Pitfall 1 — preserve key-absent branch; Code Examples — analyzePhotoOR wrap sketch)
  </read_first>
  <files>lib/ai/openrouter-client.ts</files>
  <behavior>
    - transcribeAudioOR: no OpenAI key -> Gemini directly (key-absent branch unchanged), fetch NOT called.
    - transcribeAudioOR: OpenAI key present but Whisper fetch fails -> Gemini transcript (failure-based fallback fires).
    - analyzePhotoOR: OpenRouter vision fails -> analyzePhotoGemini result.
    - On primary success neither path calls Gemini.
  </behavior>
  <action>
    In `lib/ai/openrouter-client.ts`:
    (a) `transcribeAudioOR`: KEEP the existing key-absent short-circuit FIRST (lines 67-74 — `if (!apiKey) { const { transcribeAudioGemini } = await import('@/lib/ai/providers/gemini'); return transcribeAudioGemini(audioBlob, ext) }`). Extract the key-PRESENT Whisper fetch body (lines 76-106) into a local `async function whisperPrimary(): Promise<string>`. Then wrap: `const { callWithFallback } = await import('@/lib/ai/with-fallback'); const { result } = await callWithFallback({ op: 'transcribe', primary: whisperPrimary, fallback: async () => { const { transcribeAudioGemini } = await import('@/lib/ai/providers/gemini'); return transcribeAudioGemini(audioBlob, ext) } }); return result`. Preserve the langfuse generation trace inside whisperPrimary so success-path observability is unchanged.
    (b) `analyzePhotoOR`: extract the current OpenRouter vision fetch body (lines 125-181) into a local `async function visionPrimary(): Promise<string>` (keep its langfuse trace). Then `const { callWithFallback } = await import('@/lib/ai/with-fallback'); const { result } = await callWithFallback({ op: 'vision', primary: visionPrimary, fallback: async () => { const { analyzePhotoGemini } = await import('@/lib/ai/providers/gemini'); return analyzePhotoGemini(base64, mimeType) } }); return result`.
    Keep all imports dynamic for Gemini (bundle leanness, per CLAUDE.md server-only convention). Signatures of both exported functions are UNCHANGED so all downstream callers (transcribe-audio job, whatsapp adapter, normalize, refine route) inherit fallback with zero edits.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai/transcribe-fallback.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `transcribe-fallback.test.ts` GREEN: key-absent path preserved AND failure-based fallback fires
    - `transcribeAudioOR` and `analyzePhotoOR` exported signatures unchanged
    - Both call `callWithFallback`; Gemini imports remain dynamic
  </acceptance_criteria>
  <done>Transcription + vision fall back on failure; key-absent branch intact; tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fallback-aware provider for generate/refine + wire generate call site</name>
  <read_first>
    - tests/unit/estimate/never-throw.test.ts (the both-down provider_unavailable case — make GREEN)
    - tests/unit/ai/provider-factory.test.ts (must stay GREEN — getAIProvider untouched)
    - lib/services/generate-estimate.ts (lines 180-181 — the swap site)
    - lib/ai/index.ts (getAIProvider — DO NOT modify; import it)
    - lib/ai/providers/openrouter.ts and gemini.ts (the two AIProvider impls)
  </read_first>
  <files>lib/ai/provider-with-fallback.ts, lib/services/generate-estimate.ts</files>
  <behavior>
    - getAIProviderWithFallback(companyId).generateEstimate: OpenRouter primary, Gemini fallback once; on both-down re-throws (so generate.ts catch -> failure state).
    - getAIProviderWithFallback(companyId).refineEstimate: same fallback policy (refine route will consume this in Phase 101; provide it now).
    - generate-estimate.ts uses the fallback-aware provider; happy path still exactly 1 AI call.
  </behavior>
  <action>
    (a) Create `lib/ai/provider-with-fallback.ts` exporting `async function getAIProviderWithFallback(companyId?: string): Promise<AIProvider>` (AIProvider type from '@/lib/ai/provider.interface'). Implementation: resolve the primary via the EXISTING `getAIProvider(companyId)` (keeps per-company model selection). Lazily build the Gemini fallback `const { GeminiAdapter } = await import('@/lib/ai/providers/gemini'); const fallbackProvider = new GeminiAdapter()`. Return an object implementing AIProvider whose:
       - `generateEstimate(input)` = `(await callWithFallback({ op:'generate', primary: () => primary.generateEstimate(input), fallback: () => fallbackProvider.generateEstimate(input) })).result`
       - `refineEstimate(input)` = `(await callWithFallback({ op:'refine', primary: () => primary.refineEstimate(input), fallback: () => fallbackProvider.refineEstimate(input) })).result`
       Import `callWithFallback` from '@/lib/ai/with-fallback'. `companyId` stays a param/closure — NEVER read from LLM output (multi-tenant invariant). Keep Gemini import dynamic.
    (b) In `lib/services/generate-estimate.ts` line 180: change `const provider = await getAIProvider(companyId)` to `const provider = await getAIProviderWithFallback(companyId)` and add `import { getAIProviderWithFallback } from '@/lib/ai/provider-with-fallback'`. Leave line 181 (`provider.generateEstimate(estimateInput)`) unchanged. Do NOT touch the refine route here (Phase 101 owns refine's architecture; this phase only made the provider available + fixes refine's error surface in 99-02).
    Note: do NOT write 'gemini' into pipeline_events.provider this phase (the column union cannot hold it — Phase 100/GUARD-04). The wrapper returns servedBy/fallbackFired as a seam only.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/never-throw.test.ts tests/unit/ai/provider-factory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/ai/provider-with-fallback.ts` exports `getAIProviderWithFallback`; generate path falls back once then re-throws on both-down
    - `never-throw.test.ts` both-providers-down case GREEN: result is `{ failure: { reason: 'provider_unavailable' } }` (reason set by 99-02's generate.ts mapping; coordinate — if 99-02 not yet merged, the generate.ts catch still yields a failure state, just assert no-throw + presence here and the typed reason in 99-02)
    - `provider-factory.test.ts` stays GREEN (getAIProvider unchanged)
    - generate-estimate.ts imports getAIProviderWithFallback; happy path = 1 AI call (no fallback on success)
  </acceptance_criteria>
  <done>Fallback-aware provider exists and is wired into generate; provider_unavailable both-down resolves to failure state without throwing.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/ai tests/unit/estimate` — all GREEN (HARD-03 Wave-0 tests now pass; invariants stay green).
- Manual seam check: `callWithFallback` returns `{ servedBy, fallbackFired }`; no 'gemini' written to pipeline_events.provider.
- `npx tsc --noEmit` clean for the touched files.
</verification>

<success_criteria>
- generate, transcribe, vision all attempt OpenRouter then Gemini exactly once on failure; refine has a fallback-aware provider ready (consumed in Phase 101).
- Happy path = exactly 1 AI call (QA-03), fallback not invoked on success.
- Key-absent transcription branch preserved; multi-tenant companyId stays closure/param.
- `getAIProvider` untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/99-unified-error-model-provider-fallback/99-01-SUMMARY.md`.
</output>
