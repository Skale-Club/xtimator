---
phase: 174-tenant-cutover-whatsapp-reenable
plan: 06
type: execute
wave: 3
depends_on:
  - "174-04"
files_modified:
  - lib/inngest/functions/transcribe-audio.ts
  - lib/inngest/functions/analyze-photos.ts
  - lib/inngest/functions/generate-estimate.ts
autonomous: true
requirements:
  - TNT-01
must_haves:
  truths:
    - "Every notify() call in the 3 AI-job Inngest functions (6 call sites total: ai_job.failed + ai_job.completed x3) now passes copyContext."
    - "ai_job.* events remain _dropped-category (all channels false, per event-types.ts) — this sweep does NOT change delivery behavior, it only prepares these call sites structurally for the day EVENT_CATEGORIES re-enables them."
  artifacts: []
  key_links: []
---

<objective>
Wave 3 (TNT-01 mechanical sweep, part B — AI job lifecycle): sweep the 6 `notify()` call sites across `lib/inngest/functions/transcribe-audio.ts`, `analyze-photos.ts`, and `generate-estimate.ts` (each has an `ai_job.failed` and an `ai_job.completed` call site, following an identical pattern) onto the `copyContext` seam.

Note: `ai_job.failed`/`ai_job.completed` map to the `_dropped` `EventCategory` (`lib/notifications/event-types.ts`) — `resolveChannels()` returns all-false for these today, so `notify()` returns `{skipped: 'channel_disabled'}` before ever reaching the copyContext resolution block. This sweep is still required for TNT-01's "ALL notify() call sites" completeness mandate and costs nothing at runtime today; it also means these 6 sites are the LOWEST-risk part of the whole sweep (currently inert).

Purpose: complete the mechanical sweep at zero runtime risk (these call sites don't deliver today regardless of this change).
Output: 6 call sites pass `copyContext`, identical pattern across all 3 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@lib/inngest/functions/transcribe-audio.ts
@lib/inngest/functions/analyze-photos.ts
@lib/inngest/functions/generate-estimate.ts
@lib/notifications/event-types.ts

<scope_fence>
- Purely mechanical: add ONE field (`copyContext`) to each existing `notify({...})` call. Do NOT change `title`/`body`/anything else.
- Do NOT touch `event-types.ts` — `ai_job.*`'s `_dropped` category is unrelated to this milestone and out of scope to change.
- Do NOT touch the Inngest step/retry/idempotency logic surrounding these notify() calls — untouched.
</scope_fence>

<interfaces>
The canonical transform (worked example from `lib/inngest/functions/transcribe-audio.ts`'s `ai_job.failed` site):

BEFORE:
```typescript
const copy = buildNotificationCopy('ai_job.failed', {
  jobType: 'Audio transcription',
  errorMessage: error instanceof Error ? error.message : String(error),
})
void notify({
  companyId,
  userId,
  eventType: 'ai_job.failed',
  title: copy.title,
  body: copy.body,
  // ...remaining fields unchanged
})
```

AFTER:
```typescript
const ctx = {
  jobType: 'Audio transcription',
  errorMessage: error instanceof Error ? error.message : String(error),
}
const copy = buildNotificationCopy('ai_job.failed', ctx)
void notify({
  companyId,
  userId,
  eventType: 'ai_job.failed',
  title: copy.title,
  body: copy.body,
  copyContext: ctx,
  // ...remaining fields unchanged
})
```

Every call site in this plan (re-verify exact line numbers against the LIVE files):

**lib/inngest/functions/transcribe-audio.ts**: `ai_job.failed` (~line 143, ctx `{jobType: 'Audio transcription', errorMessage}`), `ai_job.completed` (~line 403, ctx `{jobType: 'Audio transcription'}`)

**lib/inngest/functions/analyze-photos.ts**: `ai_job.failed` (~line 90, ctx `{jobType: 'Photo analysis', errorMessage}`), `ai_job.completed` (~line 421, ctx `{jobType: 'Photo analysis'}`)

**lib/inngest/functions/generate-estimate.ts**: `ai_job.failed` (~line 80, ctx `{jobType: 'Estimate generation', errorMessage}`), `ai_job.completed` (~line 280, ctx `{jobType: 'Estimate generation'}`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep transcribe-audio.ts + analyze-photos.ts (4 call sites)</name>
  <files>lib/inngest/functions/transcribe-audio.ts, lib/inngest/functions/analyze-photos.ts</files>
  <read_first>
    - lib/inngest/functions/transcribe-audio.ts (the `ai_job.failed` and `ai_job.completed` notify blocks)
    - lib/inngest/functions/analyze-photos.ts (the `ai_job.failed` and `ai_job.completed` notify blocks)
  </read_first>
  <action>
    Apply the `<interfaces>` transform to all 4 call sites (2 per file). No import changes needed.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/transcribe-short-circuit.test.ts tests/unit/billing/derived-duration.test.ts tests/unit/inngest/analyze-photos-structured.test.ts tests/unit/inngest/analyze-photos-cost.test.ts tests/unit/inngest/analyze-photos-coverage.test.ts tests/unit/api/analyze-photos-dispatch.test.ts tests/unit/api/analyze-photos-quota.test.ts && node -e "const fs=require('fs'); for (const f of ['lib/inngest/functions/transcribe-audio.ts','lib/inngest/functions/analyze-photos.ts']) { const c=(fs.readFileSync(f,'utf8').match(/copyContext:/g)||[]).length; if (c<2) { console.error(f,'expected >=2 copyContext occurrences, found',c); process.exit(1); } } console.log('ok');"</automated>
  </verify>
  <done>All 4 call sites pass copyContext; existing transcribe/analyze-photos test suites stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep generate-estimate.ts (2 call sites) + full-directory verify</name>
  <files>lib/inngest/functions/generate-estimate.ts</files>
  <read_first>
    - lib/inngest/functions/generate-estimate.ts (the `ai_job.failed` and `ai_job.completed` notify blocks)
  </read_first>
  <action>
    Apply the `<interfaces>` transform to both call sites. No import changes needed. Then run the full `tests/unit/inngest` and `tests/unit/notifications` directories plus `tsc --noEmit` as a final consolidated check for this plan's 6 total call sites.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/inngest/generate-estimate-job.test.ts tests/unit/api/generate-estimate-quota.test.ts tests/unit/api/generate-estimate-dispatch.test.ts tests/unit/inngest tests/unit/notifications && npx tsc --noEmit -p tsconfig.ci.json && node -e "const fs=require('fs'); const c=(fs.readFileSync('lib/inngest/functions/generate-estimate.ts','utf8').match(/copyContext:/g)||[]).length; if (c<2) { console.error('expected >=2 copyContext occurrences, found',c); process.exit(1); } console.log('ok');"</automated>
  </verify>
  <done>All 6 AI-job call sites across the 3 files pass copyContext; the full inngest + notifications suites and tsc are green.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/inngest tests/unit/notifications
npx tsc --noEmit -p tsconfig.ci.json
```
</verification>

<success_criteria>
- All 6 call sites across the 3 AI-job Inngest functions pass `copyContext`.
- Zero behavior change: `ai_job.*` events remain `_dropped` (undeliverable) exactly as before; every existing test passes unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/174-tenant-cutover-whatsapp-reenable/174-06-SUMMARY.md`.
</output>
