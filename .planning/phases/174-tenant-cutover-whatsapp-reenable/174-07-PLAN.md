---
phase: 174-tenant-cutover-whatsapp-reenable
plan: 07
type: execute
wave: 4
depends_on:
  - "174-04"
  - "174-05"
files_modified:
  - lib/whatsapp/handler.ts
  - app/admin/billing/actions.ts
  - app/estimate/[token]/actions.ts
  - tests/unit/notifications/event-sources.test.ts
autonomous: true
requirements:
  - TNT-01
must_haves:
  truths:
    - "Every notify() call in lib/whatsapp/handler.ts, app/admin/billing/actions.ts, and app/estimate/[token]/actions.ts (5 call sites total) now passes copyContext, so an admin's DB template edit for whatsapp.inbound, admin.tier_changed, admin.bonus_credits_granted, estimate.viewed, estimate.accepted, and estimate.declined takes effect for these call sites."
    - "This is the LAST plan in the sweep — after this plan lands, ALL 18 notify() call sites across all 9 files pass copyContext (TNT-01 complete); copy.ts is used only to compute the fallback title/body at each site and as the resolver's internal last-resort, never as the sole rendering source."
  artifacts: []
  key_links:
    - from: "app/estimate/[token]/actions.ts"
      to: "lib/notifications/dispatch.ts (notify)"
      via: "copyContext: ctx on estimate.viewed / estimate.accepted / estimate.declined"
      pattern: "copyContext:"
---

<objective>
Wave 4 (TNT-01 mechanical sweep, part C — final part, completes the sweep): sweep the last 5 `notify()` call sites — `lib/whatsapp/handler.ts` (1, `whatsapp.inbound`), `app/admin/billing/actions.ts` (2, `admin.tier_changed`/`admin.bonus_credits_granted`), and `app/estimate/[token]/actions.ts` (2, `estimate.viewed`/`estimate.accepted`/`estimate.declined` — 2 call sites cover 3 event types since accept/decline share one call site with a computed `eventType`) — onto the `copyContext` seam.

**Revision note (plan-checker BLOCKER 2):** this plan and Plan 174-05 both extend `tests/unit/notifications/event-sources.test.ts` (174-05 for the `connect-webhook.ts`-covered tests, this plan for the `admin/billing/actions.ts` + `estimate/[token]/actions.ts`-covered tests). Both plans were originally assigned to the SAME parallel Wave 3, which is a race on a shared file — two executors editing the same file concurrently in different worktrees/contexts is undefined behavior for merge purposes. This plan now moves to **Wave 4** and depends on **`174-05`** (in addition to `174-04`), serializing the two `event-sources.test.ts` edits. Plan 174-06 (the AI-job Inngest sweep) touches no shared test file and correctly stays in Wave 3.

This plan completes TNT-01: after this lands, all 9 files / 18 call sites identified by the architecture research pass `copyContext`, and `copy.ts` survives only as (a) each call site's fallback-title/body computation and (b) the resolver's internal last-resort — never the sole rendering source for a tenant notification.

Purpose: finish the sweep; `estimate.viewed`/`estimate.accepted`/`estimate.declined` are the highest-traffic events in this milestone (the demo-critical estimate lifecycle), so this is also the highest-VALUE part of the sweep.
Output: 5 call sites pass `copyContext`; TNT-01 is fully satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@lib/whatsapp/handler.ts
@app/admin/billing/actions.ts
@app/estimate/[token]/actions.ts
@tests/unit/notifications/event-sources.test.ts

<scope_fence>
- Purely mechanical: add ONE field (`copyContext`) to each existing `notify({...})` call. Do NOT change `title`/`body`/`metadata`/`channels`/anything else.
- `lib/whatsapp/handler.ts` uses DYNAMIC imports (`await import(...)`) for `notify`/`buildNotificationCopy` inside a try block — no NEW import statement is needed for `copyContext` (it's just a field on the params object passed to the already-imported `notify`), but re-verify the surrounding code before editing.
- `whatsapp.inbound` is `_dropped`-category (same as the AI-job events in Plan 174-06) — this call site is currently inert at runtime; sweeping it is for TNT-01 completeness, zero runtime risk.
- Before editing `tests/unit/notifications/event-sources.test.ts`, confirm Plan 174-05's edits to that SAME file (its `handleConnectEvent` test extensions) are already present/committed — this plan depends on 174-05 precisely so its own edits land on top of, not in conflict with, 174-05's.
</scope_fence>

<interfaces>
The canonical transform (worked example from `app/estimate/[token]/actions.ts`'s `estimate.viewed` site):

BEFORE:
```typescript
const copy = buildNotificationCopy('estimate.viewed', {
  estimateNumber: estimateRow.estimate_number ?? undefined,
  clientName: estimateRow.client_name ?? undefined,
})
void notify({
  companyId: estimateRow.company_id,
  userId: null,
  eventType: 'estimate.viewed',
  title: copy.title,
  body: copy.body,
  // ...remaining fields unchanged
})
```

AFTER:
```typescript
const ctx = {
  estimateNumber: estimateRow.estimate_number ?? undefined,
  clientName: estimateRow.client_name ?? undefined,
}
const copy = buildNotificationCopy('estimate.viewed', ctx)
void notify({
  companyId: estimateRow.company_id,
  userId: null,
  eventType: 'estimate.viewed',
  title: copy.title,
  body: copy.body,
  copyContext: ctx,
  // ...remaining fields unchanged
})
```

Every call site in this plan (re-verify exact line numbers against the LIVE files):

**lib/whatsapp/handler.ts** (~line 441-461, dynamic imports): `whatsapp.inbound` — ctx currently `{ whatsappFrom: ownerPhone }`

**app/admin/billing/actions.ts**: `admin.tier_changed` (~line 69, ctx `{tierFrom: previous?.tier ?? 'unknown', tierTo: tier}`), `admin.bonus_credits_granted` (~line 144, ctx `{credits: units}`)

**app/estimate/[token]/actions.ts**: `estimate.viewed` (~line 53, ctx `{estimateNumber: estimateRow.estimate_number ?? undefined, clientName: estimateRow.client_name ?? undefined}`), `estimate.accepted`/`estimate.declined` — ONE call site with a computed `eventType` variable (~line 167, ctx `{estimateNumber: est.estimate_number ?? undefined, clientName: est.client_name ?? undefined}`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep lib/whatsapp/handler.ts + app/admin/billing/actions.ts (3 call sites)</name>
  <files>lib/whatsapp/handler.ts, app/admin/billing/actions.ts</files>
  <read_first>
    - lib/whatsapp/handler.ts (the `whatsapp.inbound` notify block — note the dynamic `await import(...)` pattern)
    - app/admin/billing/actions.ts (FULL — both `forceTier` and `grantBonusCredits`)
    - tests/unit/notifications/event-sources.test.ts's `forceTier fires notify(...)` and `grantBonusCredits fires notify(...)` tests (the coverage these 2 call sites already have) — this file already carries Plan 174-05's edits (its dependency); do not revert or conflict with them
  </read_first>
  <action>
    Apply the `<interfaces>` transform to all 3 call sites. For `lib/whatsapp/handler.ts`, keep the dynamic-import style intact — only the `notify({...})` params object gains the `copyContext` field, no new import line is needed.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/whatsapp/handler.test.ts tests/unit/whatsapp/handler-intent-routing.test.ts tests/unit/whatsapp/handler-inngest-dispatch.test.ts tests/unit/notifications/event-sources.test.ts && node -e "const fs=require('fs'); const cases=[['lib/whatsapp/handler.ts',1],['app/admin/billing/actions.ts',2]]; for (const [f,min] of cases) { const c=(fs.readFileSync(f,'utf8').match(/copyContext:/g)||[]).length; if (c<min) { console.error(f,'expected >=',min,'copyContext occurrences, found',c); process.exit(1); } } console.log('ok');"</automated>
  </verify>
  <done>All 3 call sites pass copyContext; existing whatsapp handler + event-sources test suites (including 174-05's prior edits) stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep app/estimate/[token]/actions.ts (2 call sites) — completes TNT-01</name>
  <files>app/estimate/[token]/actions.ts, tests/unit/notifications/event-sources.test.ts</files>
  <read_first>
    - app/estimate/[token]/actions.ts (FULL — `logEstimateView` and `respondToEstimate`)
    - tests/unit/notifications/event-sources.test.ts's `estimate.viewed instrumentation` and `estimate.accepted / estimate.declined instrumentation` describe blocks — you are EXTENDING these, on top of Plan 174-05's already-landed edits to this same file
  </read_first>
  <action>
    Apply the `<interfaces>` transform to both call sites. Extend `event-sources.test.ts`'s existing `logEstimateView` and `respondToEstimate` tests to assert the captured `notify()` call now includes a `copyContext` key (same `.find(...)` + property-inspection pattern as the rest of the file — do not switch to strict full-object equality).

    Then run the FULL project notifications suite as the final gate for the whole sweep (Plans 174-05/06/07 combined): `npx vitest run tests/unit/notifications` — every test across every swept file must be green. Run `npx tsc --noEmit -p tsconfig.ci.json`. As a final structural sanity check, grep the whole repo to confirm no `notify(` call site outside test files still lacks `copyContext` where it has a sibling `buildNotificationCopy(` call (a residual/missed site would indicate an incomplete sweep — investigate and fix before considering this plan done).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications && npx tsc --noEmit -p tsconfig.ci.json && node -e "const fs=require('fs'); const c=(fs.readFileSync(\"app/estimate/[token]/actions.ts\",'utf8').match(/copyContext:/g)||[]).length; if (c<2) { console.error('expected >=2 copyContext occurrences, found',c); process.exit(1); } console.log('ok');"</automated>
  </verify>
  <done>Both estimate-actions call sites pass copyContext, proven by explicit assertions in event-sources.test.ts; the full notifications suite and tsc are green — TNT-01 is complete across all 9 files / 18 call sites.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/notifications tests/unit/whatsapp
npx tsc --noEmit -p tsconfig.ci.json
```
</verification>

<success_criteria>
- All 5 call sites in this plan pass `copyContext`.
- TNT-01 is fully complete: all 9 files / 18 `notify()` call sites identified by the architecture research now pass `copyContext`; `copy.ts` is used only as each call site's fallback source and the resolver's internal last-resort.
- Zero behavior change: every existing test in the affected suites passes, plus new `copyContext`-presence assertions in `event-sources.test.ts`, landing cleanly on top of Plan 174-05's edits to the same file (no merge conflict, since the two plans are now serialized).
</success_criteria>

<output>
After completion, create `.planning/phases/174-tenant-cutover-whatsapp-reenable/174-07-SUMMARY.md`.
</output>
