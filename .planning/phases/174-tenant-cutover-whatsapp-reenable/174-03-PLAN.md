---
phase: 174-tenant-cutover-whatsapp-reenable
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/notifications/preferences.ts
  - tests/unit/notifications/preferences.test.ts
  - lib/notifications/whatsapp-registry.ts
  - tests/unit/notifications/whatsapp-registry.test.ts
autonomous: true
requirements:
  - TNT-02
  - TNT-03
must_haves:
  truths:
    - "A tenant who has recorded whatsapp_opt_in_at AND enabled whatsapp in their category preferences now gets whatsapp:true from resolveChannels() — the D-15 forced-off gate no longer overrides a genuine opt-in."
    - "A tenant who has NOT recorded whatsapp_opt_in_at still gets whatsapp:false regardless of category toggle state — the pre-existing consent gate is untouched by lifting D-15."
    - "Every non-whatsapp channel resolution (in_app, email, sms) is provably unaffected by the D-15 removal — the existing preference-matrix test suite stays green byte-unmodified."
    - "Before this gate lifts, any PRE-EXISTING notification_preferences row with a non-null whatsapp_opt_in_at (set at some point before D-15 existed, when the column was dead data) is identified and its count documented — lifting D-15 makes such a row's WhatsApp delivery live immediately (subject to the category toggle + a Meta-approved, correctly-schema'd template existing), not gradually or behind any other gate this plan adds."
    - "getApprovedTemplateForEvent's return value now carries the approved template's expected {{n}} variable count — sourced from the DB row's variables_schema.length when an approved row exists, or 2 (matching titleBodyVars' own output) for the static REGISTRY fallback — so Plan 174-04 can build the runtime mismatch guard on top of it."
  artifacts:
    - path: "lib/notifications/preferences.ts"
      provides: "resolveChannels() with the D-15 unconditional 'whatsapp = false' override removed; the whatsapp_opt_in_at consent gate remains the sole whatsapp gate beyond category preference"
    - path: "lib/notifications/whatsapp-registry.ts"
      provides: "NotificationTemplate.expectedVariableCount: number — populated from variables_schema.length (DB) or 2 (static REGISTRY)"
      exports: ["NotificationTemplate", "getTemplateForEvent", "getApprovedTemplateForEvent"]
  key_links:
    - from: "lib/notifications/preferences.ts (resolveChannels)"
      to: "lib/notifications/dispatch.ts (notify, whatsapp branch)"
      via: "the channels.whatsapp boolean this phase can now return true for an opted-in tenant"
      pattern: "whatsapp_opt_in_at"
    - from: "lib/notifications/whatsapp-registry.ts (getApprovedTemplateForEvent)"
      to: "lib/notifications/dispatch.ts (notify, whatsapp branch — wired in Plan 174-04)"
      via: "tpl.expectedVariableCount, read by the Plan-174-04 runtime guard before every WhatsApp send"
      pattern: "expectedVariableCount"
---

<objective>
TNT-03 groundwork: (1) lift the D-15 forced-off gate on tenant proactive WhatsApp in `preferences.ts` — a prior-milestone decision ("old preferences cannot continue proactive sends") that this milestone's owner-confirmed decision explicitly reverses ("Tenant proactive WhatsApp notifications ARE re-enabled this milestone via the EXISTING HSM registry"). (2) Extend `whatsapp-registry.ts`'s `NotificationTemplate` with the expected `{{n}}` variable count the Pitfall-3 runtime guard (Plan 174-04) needs — sourced from the `variables_schema` column that already exists on `whatsapp_notification_templates` (migration `20260621000003`) but is currently unused by `getApprovedTemplateForEvent`.

Both changes are independent, self-contained, and touch no shared files with Plan 174-01/174-02 — safe to run in parallel Wave 1.

**Revision note (plan-checker FLAG 4):** lifting D-15 is not merely a code change with zero immediate effect — ANY `notification_preferences` row that already carries a non-null `whatsapp_opt_in_at` (dead data while D-15 forced whatsapp off, but the column itself long predates D-15 per Phase 104's NOTIF-05 consent work) goes LIVE the moment this gate lifts, for any tenant whose `whatsapp` category toggle is also on. Task 1 below adds an explicit check for this BEFORE shipping the gate lift, so the count (zero or otherwise) is a known, documented fact in the SUMMARY rather than an unverified assumption. This is orthogonal to — and does NOT rely on — Plan 174-04's `expectedVariableCount` guard, which provides NO additional dormancy for a stale opted-in row mapped to one of the 5 static `REGISTRY` fallback entries (see 174-04's objective for why).

Purpose: make the preference matrix return a genuine opt-in signal for WhatsApp, and make the approved-template resolver carry the data Plan 174-04's send-time guard needs — without touching dispatch.ts itself (that wiring is Plan 174-04's job).
Output: `resolveChannels()` respects a real WhatsApp opt-in; `getApprovedTemplateForEvent`/`getTemplateForEvent` both return `expectedVariableCount`; the stale-opt-in-row count is known and documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@lib/notifications/preferences.ts
@lib/notifications/whatsapp-registry.ts
@tests/unit/notifications/preferences.test.ts
@tests/unit/notifications/whatsapp-registry.test.ts
@supabase/migrations/20260621000003_whatsapp_notification_templates.sql

<scope_fence>
- Do NOT touch `dispatch.ts` here — Plan 174-04 (Wave 2, depends on this plan) is the sole consumer of both `expectedVariableCount` and the lifted D-15 gate's downstream effect.
- Do NOT build any admin UI for authoring `variables_schema` — out of scope; an admin populates it via existing `lib/actions/admin-whatsapp-templates.ts` (`createTemplate`'s `variables_schema` param already exists) or direct SQL. This plan only makes the REGISTRY read and expose it.
- Do NOT touch the verified-phone gate, `resolveOwnerPhone`, or the SMS consent gate — untouched, unrelated to TNT-03.
- variables_schema's exact per-element shape (string names? {name, position} objects?) is NOT standardized anywhere in this codebase today — treat it as `unknown[]`; only `.length` matters for this guard.
- The stale-opt-in-row check (Task 1) is a READ-ONLY query/count — do NOT write, clear, or otherwise mutate any `whatsapp_opt_in_at` value found. If any stale rows exist, document the count and defer any remediation decision to the user/operator — this plan does not decide whether to revoke them.
</scope_fence>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift the D-15 forced-off WhatsApp gate + check for stale pre-existing opt-ins</name>
  <files>lib/notifications/preferences.ts, tests/unit/notifications/preferences.test.ts</files>
  <read_first>
    - lib/notifications/preferences.ts (FULL — locate the `// D-15:` comment and its `whatsapp = false` line inside `resolveChannels`)
    - tests/unit/notifications/preferences.test.ts (FULL — you are EXTENDING this file; no existing test currently asserts the D-15 override, so none should break)
  </read_first>
  <behavior>
    - `resolveChannels(eventType, userId)` where the user's stored prefs have `whatsapp_opt_in_at: '<ISO timestamp>'` AND `categories[category].whatsapp === true` → `result.whatsapp === true` (THE gap this task closes — today this returns `false` unconditionally because of D-15; RED before the fix, GREEN after).
    - `resolveChannels(eventType, userId)` where the user's stored prefs have `whatsapp_opt_in_at: null` (no consent timestamp) but `categories[category].whatsapp === true` → `result.whatsapp === false` — the pre-existing consent gate (`if (!userPrefs?.whatsapp_opt_in_at) whatsapp = false`) still holds; lifting D-15 must NOT bypass consent.
    - `resolveChannels(eventType, userId)` with no stored prefs row at all (defaults apply) → `result.whatsapp === false` — `DEFAULT_PREFERENCES` already default whatsapp off; unaffected by this change.
    - All 4 non-whatsapp channels (`inApp`, `email`, `sms` — the 3 already covered by the existing test suite) are UNCHANGED by this edit — run the full existing `preferences.test.ts` suite and confirm zero regressions (TNT-02).
  </behavior>
  <action>
    In `lib/notifications/preferences.ts`, remove (or comment out with a dated historical note, matching this codebase's convention of leaving a breadcrumb — check nearby code style; a clean removal with an updated doc comment is preferred) the unconditional `whatsapp = false` line under the `// D-15:` comment inside `resolveChannels`. Update the function's leading doc comment (the "Resolution order" list) to remove the now-stale "5. D-15: WhatsApp is always forced false..." step and renumber the remaining steps. The `!userPrefs?.whatsapp_opt_in_at` consent gate immediately above stays completely untouched — that is the ONLY gate whatsapp now passes through beyond category preference + the `override` param.

    Add the new test cases to `tests/unit/notifications/preferences.test.ts` per `<behavior>`, as new `it()`s appended to the existing `describe('lib/notifications/preferences — 4 channels + opt-in gate (NOTIF-02 RED)', ...)` block (or a new sibling describe block — either is fine, just don't touch existing tests). Run the FULL file and confirm every pre-existing test (including the Phase-77 and Phase-104 describe blocks) still passes.

    **Stale opt-in check (plan-checker FLAG 4 — do this BEFORE committing the gate removal, as a pre-flight fact-finding step, not a code change):** using the project's Supabase MCP access or `requireServiceClient()` in a throwaway script/query, count rows in `notification_preferences` WHERE `whatsapp_opt_in_at IS NOT NULL`. This is a READ-ONLY check — do not mutate anything found. Record the exact count (and, if non-zero and feasible, the affected `user_id`s) in this plan's SUMMARY.md under a dedicated "Stale WhatsApp opt-ins" heading. If the count is non-zero, explicitly flag in the SUMMARY that those tenants' WhatsApp delivery goes live immediately upon this gate lifting, gated only by (a) their `whatsapp` category toggle and (b) a Meta-approved, correctly-`variables_schema`'d template existing for the event (per Plan 174-04's guard) — this is a fact to surface to the user/operator, not a blocker to this plan's commit.

    Commit: `feat(174-03): lift D-15 forced-off WhatsApp gate — opt-in now reaches resolveChannels (TNT-03)`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications/preferences.test.ts</automated>
  </verify>
  <done>An opted-in tenant's category-enabled whatsapp preference now resolves true; a non-opted-in tenant still resolves false; every other channel/category combination is unchanged; the stale-opt-in-row count is known and documented in the SUMMARY (zero or otherwise).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: expectedVariableCount on NotificationTemplate (Pitfall 3 guard data)</name>
  <files>lib/notifications/whatsapp-registry.ts, tests/unit/notifications/whatsapp-registry.test.ts</files>
  <read_first>
    - lib/notifications/whatsapp-registry.ts (FULL)
    - tests/unit/notifications/whatsapp-registry.test.ts (FULL — you are EXTENDING this file; existing tests assert specific fields like `templateName`/`languageCode`/`variables(...)`, not full-object equality, so adding a new field is additive-safe, but re-verify this by reading the assertions, not by assumption)
    - supabase/migrations/20260621000003_whatsapp_notification_templates.sql (the `variables_schema jsonb NOT NULL DEFAULT '[]'::jsonb` column this task reads)
  </read_first>
  <behavior>
    - `getApprovedTemplateForEvent(eventType)` where the mocked approved DB row includes `variables_schema: ['title', 'body']` → the returned `NotificationTemplate.expectedVariableCount === 2`.
    - `getApprovedTemplateForEvent(eventType)` where the mocked approved DB row's `variables_schema` is `[]` (the column's own default — an admin hasn't configured it yet) → `expectedVariableCount === 0` — this genuinely protects a DB-APPROVED-but-unconfigured row (Plan 174-04's guard will refuse to send it until an admin populates a matching schema). NOTE: this protection does NOT extend to the 5 static `REGISTRY` fallback entries below, which hardcode `expectedVariableCount: 2` regardless — see Task 2's last bullet and Plan 174-04's objective for the honest scope of this guard.
    - `getApprovedTemplateForEvent(eventType)` where `variables_schema` is missing/null/not-an-array on the row (defensive) → `expectedVariableCount === 0`, never throws.
    - `getApprovedTemplateForEvent` falling back to the static map (no approved row, or no service client, or a throwing query — the 3 existing fallback branches) → the returned template's `expectedVariableCount === 2`, matching `titleBodyVars`'s own fixed 2-element output, so the guard is a structural NO-OP for unmigrated/static entries (preserves today's exact send behavior for every event currently in `REGISTRY` — real dormancy for these 5 events comes from the opt-in consent gate and Meta's approval gate, not from this field).
    - `getTemplateForEvent(eventType)` (the sync static-only function) → returned template (when mapped) also carries `expectedVariableCount: 2`.
  </behavior>
  <action>
    In `lib/notifications/whatsapp-registry.ts`: add `expectedVariableCount: number` to the `NotificationTemplate` interface with a doc comment explaining its purpose (Phase 174 TNT-03 / Pitfall 3 — the runtime guard Plan 174-04 builds on top of this field; NOTE the doc comment should explicitly say this field is a no-op for the static `REGISTRY` fallback, which hardcodes it to match `titleBodyVars`'s own output). Add `expectedVariableCount: 2` to every static `REGISTRY` entry (matches `titleBodyVars`'s fixed 2-element output — `estimate.accepted`, `estimate.declined`, `payment.received`, `quota.exhausted`, `trial.expiring_3d`). In `getApprovedTemplateForEvent`, add `variables_schema` to the `.select('template_name, language_code, variables_schema')` column list; when building the returned template from an approved row, compute `expectedVariableCount` as `Array.isArray(data.variables_schema) ? data.variables_schema.length : 0` (defensive against `null`/non-array values). The static-fallback return paths (no client, no approved row, thrown error) all funnel through the existing `getTemplateForEvent(eventType)` call, which now already carries `expectedVariableCount: 2` from the REGISTRY update above — no separate handling needed there.

    Add the new test cases to `tests/unit/notifications/whatsapp-registry.test.ts` per `<behavior>`, extending the existing `makeTemplateClient` mock's `maybeSingle` response objects to include `variables_schema` where relevant. Run the full file, confirm every pre-existing test (which asserts `templateName`/`languageCode`/`variables(...)` but not the full object shape) still passes unmodified. Commit: `feat(174-03): expectedVariableCount on NotificationTemplate — sourced from variables_schema (TNT-03 / Pitfall 3)`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications/whatsapp-registry.test.ts</automated>
  </verify>
  <done>Every NotificationTemplate (DB-approved or static-fallback) carries expectedVariableCount; DB rows read the real variables_schema.length, static entries hardcode 2, and an empty/missing schema on a DB-approved row safely yields 0 (dormant-until-configured) — while the SUMMARY is explicit that this is a no-op for the 5 static REGISTRY entries.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/notifications/preferences.test.ts tests/unit/notifications/whatsapp-registry.test.ts
npx tsc --noEmit -p tsconfig.ci.json
```
</verification>

<success_criteria>
- An opted-in tenant's WhatsApp preference now resolves true; a non-opted-in tenant's still resolves false; the rest of the preference matrix is provably unchanged.
- The count of pre-existing (stale) `whatsapp_opt_in_at` rows is known and documented before the gate lift ships.
- `NotificationTemplate.expectedVariableCount` exists on every code path (DB-approved and static-fallback), sourced correctly from `variables_schema.length` or hardcoded to `2` for legacy static entries — documented honestly as a no-op for the latter.
</success_criteria>

<output>
After completion, create `.planning/phases/174-tenant-cutover-whatsapp-reenable/174-03-SUMMARY.md`.
</output>
