---
phase: 174-tenant-cutover-whatsapp-reenable
plan: 05
type: execute
wave: 3
depends_on:
  - "174-04"
files_modified:
  - lib/quota.ts
  - lib/billing/credit-ledger.ts
  - lib/billing/connect-webhook.ts
  - tests/unit/notifications/event-sources.test.ts
autonomous: true
requirements:
  - TNT-01
must_haves:
  truths:
    - "Every notify() call in lib/quota.ts, lib/billing/credit-ledger.ts, and lib/billing/connect-webhook.ts (7 call sites total) now passes copyContext, so an admin's DB template edit for quota.80pct, quota.exhausted, payment.received, and payment.refunded takes effect for these call sites."
    - "The title/body params passed to each notify() call are computed EXACTLY as before (buildNotificationCopy unchanged) — copyContext is purely additive, zero behavior change when no DB template row exists."
  artifacts: []
  key_links:
    - from: "lib/billing/connect-webhook.ts"
      to: "lib/notifications/dispatch.ts (notify)"
      via: "copyContext: ctx on all 3 payment.received/payment.refunded call sites"
      pattern: "copyContext:"
---

<objective>
Wave 3 (TNT-01 mechanical sweep, part A — billing/quota subsystem): sweep the 7 `notify()` call sites across `lib/quota.ts` (2), `lib/billing/credit-ledger.ts` (2), and `lib/billing/connect-webhook.ts` (3) onto the `copyContext` seam, using the SAME raw ctx object each site already builds for `buildNotificationCopy` — no new fields, no enrichment logic here (Plan 174-04 already made `notify()` internally apply `buildFullCopyContext`, so this sweep is purely mechanical).

Purpose: let an admin's DB template edit for `quota.80pct`/`quota.exhausted`/`payment.received`/`payment.refunded` actually take effect.
Output: 7 call sites pass `copyContext`; `copy.ts` at these sites is now used ONLY to compute the fallback `title`/`body` params (unchanged), never as the sole source of truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@lib/quota.ts
@lib/billing/credit-ledger.ts
@lib/billing/connect-webhook.ts
@tests/unit/notifications/event-sources.test.ts

<scope_fence>
- This is a MECHANICAL sweep: add ONE field (`copyContext`) to each existing `notify({...})` call. Do NOT change `title`/`body`/`channels`/`metadata`/`linkUrl` or any other existing field. Do NOT change what `buildNotificationCopy` is called with — that computation stays exactly as-is (it now feeds the FALLBACK title/body, not the only source).
- Do NOT touch `dispatch.ts`, `preferences.ts`, `whatsapp-registry.ts`, `copy-context.ts`, or `notification-emails.ts` — all already shipped in Waves 1-2.
- The `copyContext` value passed at each site is the SAME ctx object already being built for `buildNotificationCopy` — do not hand-enrich it, do not call `buildFullCopyContext` from these call sites (that enrichment happens centrally inside `notify()`, per Plan 174-04).
</scope_fence>

<interfaces>
The canonical transform, applied identically at every call site in this plan (worked example from `lib/quota.ts`):

BEFORE:
```typescript
const copy = buildNotificationCopy('quota.80pct', {
  quotaPercent: Math.floor(newPct),
})
void notify({
  companyId,
  userId: userId ?? null,
  eventType: 'quota.80pct',
  title: copy.title,
  body: copy.body,
  linkUrl: '/settings/billing',
  metadata: { dedupe_key: `quota-80-${companyId}-${month}` },
})
```

AFTER — extract the ctx literal into a named `ctx` variable (so it can be reused for both `buildNotificationCopy` AND `copyContext`), add ONE field:
```typescript
const ctx = { quotaPercent: Math.floor(newPct) }
const copy = buildNotificationCopy('quota.80pct', ctx)
void notify({
  companyId,
  userId: userId ?? null,
  eventType: 'quota.80pct',
  title: copy.title,
  body: copy.body,
  copyContext: ctx,
  linkUrl: '/settings/billing',
  metadata: { dedupe_key: `quota-80-${companyId}-${month}` },
})
```

Every call site in this plan, by file, current eventType and ctx shape (re-verify exact line numbers against the LIVE files — these are approximate anchors from the current codebase):

**lib/quota.ts** (`notifyQuotaThresholds`, ~line 245-295):
- `quota.80pct` — ctx currently `{ quotaPercent: Math.floor(newPct) }`
- `quota.exhausted` — ctx currently `{}`

**lib/billing/credit-ledger.ts** (~line 185-235):
- `quota.exhausted` — ctx currently `{}`
- `quota.80pct` — ctx currently `{ quotaPercent: 0 }`

**lib/billing/connect-webhook.ts** (3 sites, ~line 140-400):
- `payment.received` (checkout.session.completed handler, ~line 146) — ctx currently `{ amountUSD, projectName: project?.name ?? undefined }`
- `payment.received` (invoice.paid handler, ~line 292) — ctx currently `{ amountUSD, projectName: updated.project_name ?? undefined }`
- `payment.refunded` (~line 395) — ctx currently `{ amountUSD, projectName: (project as {name?:string}|null)?.name ?? undefined }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep lib/quota.ts + lib/billing/credit-ledger.ts (4 call sites)</name>
  <files>lib/quota.ts, lib/billing/credit-ledger.ts</files>
  <read_first>
    - lib/quota.ts (FULL — `notifyQuotaThresholds` function)
    - lib/billing/credit-ledger.ts (FULL — the quota-threshold notify block)
  </read_first>
  <action>
    Apply the `<interfaces>` transform to all 4 call sites (2 per file): extract each inline ctx literal into a named `const ctx = {...}` variable immediately before the existing `buildNotificationCopy(eventType, ctx)` call, then add `copyContext: ctx` to the corresponding `notify({...})` call. No import changes needed (both files already import `notify` and `buildNotificationCopy`; `copyContext` is an existing optional field on `NotifyParams` since Phase 172). Do not touch `notifyOps(...)` calls (platform-scoped, unrelated pipeline — `lib/quota.ts` has one, leave it untouched).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/quota.test.ts tests/unit/quota-price-research.test.ts tests/unit/billing/credit-ledger.test.ts tests/unit/billing/credit-low-notify.test.ts tests/unit/billing/auto-topup.test.ts && node -e "const fs=require('fs'); for (const f of ['lib/quota.ts','lib/billing/credit-ledger.ts']) { const c=(fs.readFileSync(f,'utf8').match(/copyContext:/g)||[]).length; if (c<2) { console.error(f,'expected >=2 copyContext occurrences, found',c); process.exit(1); } } console.log('ok');"</automated>
  </verify>
  <done>All 4 call sites pass copyContext; existing quota/credit-ledger test suites stay green; each file has at least 2 copyContext occurrences.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep lib/billing/connect-webhook.ts (3 call sites) + verify full subsystem</name>
  <files>lib/billing/connect-webhook.ts, tests/unit/notifications/event-sources.test.ts</files>
  <read_first>
    - lib/billing/connect-webhook.ts (FULL — the 2 `payment.received` handlers + the 1 `payment.refunded` handler)
    - tests/unit/notifications/event-sources.test.ts (the `handleConnectEvent(checkout.session.completed)` and `handleConnectEvent(invoice.paid)` describe blocks — you are EXTENDING these, not replacing them)
  </read_first>
  <action>
    Apply the `<interfaces>` transform to all 3 call sites in `lib/billing/connect-webhook.ts`. Note two of these already have `amountUSD`/`projectName` as genuinely-sparse (possibly-`undefined`) fields — this is EXACTLY the carry-forward-(a) case Plan 174-01/174-04 already close centrally; do not add any special-casing here, just extract the existing ctx literal into a named variable and pass it through unchanged.

    Extend `tests/unit/notifications/event-sources.test.ts`'s existing `handleConnectEvent(checkout.session.completed)` and `handleConnectEvent(invoice.paid)` tests (or add a new assertion within them) to confirm the captured `notify()` call now includes a `copyContext` key matching the ctx shape passed to `buildNotificationCopy` for that event — use the same `.find(...)` + property-inspection pattern already used in this file (do not switch to strict `toHaveBeenCalledWith` full-object equality, which would be brittle against the file's other fields). Run the full `event-sources.test.ts` suite plus the connect-webhook-adjacent billing suites.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications/event-sources.test.ts tests/unit/notifications && npx tsc --noEmit -p tsconfig.ci.json</automated>
  </verify>
  <done>All 3 connect-webhook.ts call sites pass copyContext, proven by an explicit assertion in event-sources.test.ts; the full notifications test suite and tsc are green.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/quota.test.ts tests/unit/billing/credit-ledger.test.ts tests/unit/notifications/event-sources.test.ts tests/unit/notifications
npx tsc --noEmit -p tsconfig.ci.json
```
</verification>

<success_criteria>
- All 7 call sites across `lib/quota.ts`, `lib/billing/credit-ledger.ts`, and `lib/billing/connect-webhook.ts` pass `copyContext`.
- Zero behavior change: every existing test in the affected suites passes byte-unmodified except the new `copyContext`-presence assertions added to `event-sources.test.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/174-tenant-cutover-whatsapp-reenable/174-05-SUMMARY.md`.
</output>
