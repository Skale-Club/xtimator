---
phase: 157-admin-nav-reorg-naming-fixes
plan: 03
type: execute
wave: 2
depends_on: ["157-02"]
files_modified:
  - tests/unit/admin/companies-support-mode-button.test.ts
  - tests/unit/support-mode-layout.test.ts
  - tests/e2e/support-mode.spec.ts
autonomous: true
requirements: [NAV-03, NAMING-02]
must_haves:
  truths:
    - "Unit tests assert the NEW user-facing copy ('View as Company', 'Exit view') rather than the retired 'Support Mode' strings"
    - "The e2e Support Mode spec's Playwright selectors/body-text regexes match the new copy so the test doesn't silently pass against stale strings"
    - "Structural/import assertions (component names, redirect-path checks, audit-log literal checks) remain untouched, since those target internal naming that this milestone explicitly keeps"
  artifacts:
    - path: "tests/unit/admin/companies-support-mode-button.test.ts"
      provides: "Button-label regex updated to /View as Company/"
      contains: "View as Company"
    - path: "tests/unit/support-mode-layout.test.ts"
      provides: "Banner-copy regex updated to /Viewing/ (or equivalent new-copy match), no longer asserting the retired 'Support Mode' prefix text"
      contains: "Viewing"
    - path: "tests/e2e/support-mode.spec.ts"
      provides: "Playwright regexes retargeted to /View as Company/ and /Exit view/"
      contains: "View as Company"
  key_links:
    - from: "tests/unit/admin/companies-support-mode-button.test.ts"
      to: "app/admin/companies/support-mode-button.tsx"
      via: "readFileSync + regex assertion on rendered button text"
      pattern: "View as Company"
    - from: "tests/e2e/support-mode.spec.ts"
      to: "components/admin/support-mode-banner.tsx"
      via: "Playwright body-text/button-role assertions"
      pattern: "Exit view"
---

<objective>
Update the 3 test files whose STRING assertions hard-code the retired "Support Mode" user-facing copy, retargeting them to the new "View as Company" / "Exit view" copy landed in Plan 02. Structural assertions (component names, imports, redirect paths, audit-log action literals) are explicitly left untouched, since those verify internal naming this milestone does not change.

Purpose: Without this plan, Plan 02's rename would leave 3 test files asserting strings that no longer exist in the source, silently failing (or already failing) the test suite — this closes that gap per NAMING-02's "Tests to update" list in 157-CONTEXT.md. NAV-03 is included in this plan's requirements list for completeness (its own CONTEXT.md-flagged test — grep for `/admin/legal` in `tests/` — was confirmed empty during planning research: no test currently references the old admin route or "Legal Pages" label, so no file changes are needed for NAV-03; this plan's actual file changes are 100% NAMING-02 test updates).
Output: All 3 support-mode test files pass against Plan 02's new copy; no structural/internal-naming assertion is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/157-admin-nav-reorg-naming-fixes/157-CONTEXT.md
@.planning/phases/157-admin-nav-reorg-naming-fixes/157-02-SUMMARY.md
</context>

<interfaces>
<!-- Exact current test source, read directly from the repo this session. Use these as the literal starting point — no exploration needed. -->

### `tests/unit/admin/companies-support-mode-button.test.ts` (103 lines) — 1 assertion to update
The relevant `it` block (around line 45-51):
```ts
  it('label reads "Support Mode →"', () => {
    try {
      expect(readButton()).toMatch(/Support Mode/)
    } catch {
      expect.fail('Wave 0: app/admin/companies/support-mode-button.tsx not yet written')
    }
  })
```
Change the regex `/Support Mode/` to `/View as Company/` and update the test name string to reflect the new label. Everything else in this file (the `readActions`/`readPage` helpers, the `startSupportSessionAction` import check, the `Eye` icon check, the `'use client'`/`useTransition`/`toast.error` structural check, the `SupportModeButton` identifier checks in the `page.tsx` describe block) stays UNCHANGED — those check internal/structural contracts, not copy.

### `tests/unit/support-mode-layout.test.ts` (106 lines) — 1 assertion to update
The relevant `it` block (around line 87-93):
```ts
  it('contains the locked "Support Mode" copy per UI-SPEC', () => {
    try {
      expect(readBanner()).toMatch(/Support Mode/)
    } catch {
      expect.fail('Wave 0: components/admin/support-mode-banner.tsx not yet written')
    }
  })
```
Change the regex `/Support Mode/` to match the new banner copy (e.g. `/Viewing/` — matches "Viewing <strong>{companyName}</strong> as {adminEmail}."), and update the test name string. Everything else (the `getSupportModeSession`/`getActiveCompany` ordering checks, the `ShieldCheck` icon check, the `endSupportSession` form-action check, the `redirect('/admin/companies')` check in `lib/auth/support-mode.ts`) stays UNCHANGED.

### `tests/e2e/support-mode.spec.ts` (48 lines) — 3 assertions to update
```ts
    const supportModeLink = page.getByRole('button', { name: /Support Mode/ }).first()
    test.skip(
      (await supportModeLink.count()) === 0,
      'No companies with a Support Mode row action found — seed at least one company for this e2e test'
    )
    await supportModeLink.click()

    await page.waitForURL(/\/dashboard/, { timeout: 10000 })
    const bodyText = await page.locator('body').innerText()
    expect(bodyText).toMatch(/Support Mode/)

    await page.getByRole('button', { name: /Exit Support Mode/ }).click()
```
Change:
- `page.getByRole('button', { name: /Support Mode/ })` -> `page.getByRole('button', { name: /View as Company/ })` (rename the local variable `supportModeLink` if desired, e.g. to `viewAsCompanyLink` — optional, executor's discretion, but keep it consistent within the file if renamed).
- `expect(bodyText).toMatch(/Support Mode/)` -> `expect(bodyText).toMatch(/Viewing/)` (matches the new banner copy "Viewing {companyName} as {adminEmail}.").
- `page.getByRole('button', { name: /Exit Support Mode/ })` -> `page.getByRole('button', { name: /Exit view/ })`.
- The `test.skip` message string ("No companies with a Support Mode row action found...") may be updated for readability but is not a required assertion — update it to reference "View as Company" for consistency if changed.
- `test.describe('Support Mode (SUPPORT-01, SUPPORT-02)', ...)` — the describe block NAME can stay as-is (it's a requirement-ID label, not user-facing copy) or be updated to reference "View as Company" in a trailing comment; not a hard requirement either way.
- `page.goto('/admin/companies')` and `page.waitForURL(/\/admin\/companies/)` — UNCHANGED (navigation targets, not copy).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retarget unit test string assertions to "View as Company" / "Exit view"</name>
  <read_first>tests/unit/admin/companies-support-mode-button.test.ts, tests/unit/support-mode-layout.test.ts, app/admin/companies/support-mode-button.tsx, components/admin/support-mode-banner.tsx, .planning/phases/157-admin-nav-reorg-naming-fixes/157-CONTEXT.md (lines 132-136, tests to update)</read_first>
  <files>tests/unit/admin/companies-support-mode-button.test.ts, tests/unit/support-mode-layout.test.ts</files>
  <action>
1. In `tests/unit/admin/companies-support-mode-button.test.ts`, find the `it('label reads "Support Mode →"', ...)` block. Change its assertion from `expect(readButton()).toMatch(/Support Mode/)` to `expect(readButton()).toMatch(/View as Company/)`. Update the test's name string from `'label reads "Support Mode →"'` to `'label reads "View as Company →"'`. Leave every other `it` block in this file (imports, `Eye` icon, `'use client'`/`useTransition`/`toast.error` structural checks, `SupportModeButton` identifier checks in the `page.tsx` describe block, row-ordering checks) completely unchanged.

2. In `tests/unit/support-mode-layout.test.ts`, find the `it('contains the locked "Support Mode" copy per UI-SPEC', ...)` block. Change its assertion from `expect(readBanner()).toMatch(/Support Mode/)` to `expect(readBanner()).toMatch(/Viewing/)`. Update the test's name string from `'contains the locked "Support Mode" copy per UI-SPEC'` to `'contains the new "Viewing ... as ..." copy (View as Company rename)'`. Leave every other `it` block in this file (the `getSupportModeSession`/`getActiveCompany` ordering checks, `ShieldCheck` icon check, `endSupportSession` form-action check, the `memberships=[]` check, the DemoBanner/TrialBanner isolation check, and the `redirect('/admin/companies')` check against `lib/auth/support-mode.ts`) completely unchanged.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/admin/companies-support-mode-button.test.ts tests/unit/support-mode-layout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "View as Company" tests/unit/admin/companies-support-mode-button.test.ts` matches at least once.
    - `grep -c "toMatch(/Support Mode/)" tests/unit/admin/companies-support-mode-button.test.ts` returns 0.
    - `grep -n "Viewing" tests/unit/support-mode-layout.test.ts` matches at least once.
    - `grep -c "toMatch(/Support Mode/)" tests/unit/support-mode-layout.test.ts` returns 0.
    - `grep -n "SupportModeButton\|startSupportSessionAction\|getSupportModeSession\|endSupportSession" tests/unit/admin/companies-support-mode-button.test.ts tests/unit/support-mode-layout.test.ts` still matches (structural/internal-name assertions untouched).
    - `npx vitest run tests/unit/admin/companies-support-mode-button.test.ts tests/unit/support-mode-layout.test.ts` exits 0 (all tests pass against Plan 02's landed source).
  </acceptance_criteria>
  <done>Both unit test files assert the new "View as Company"/"Viewing" copy and pass against the Plan 02 source; every structural/internal-naming assertion in both files is byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget e2e Playwright spec to new copy</name>
  <read_first>tests/e2e/support-mode.spec.ts, app/admin/companies/support-mode-button.tsx, components/admin/support-mode-banner.tsx, .planning/phases/157-admin-nav-reorg-naming-fixes/157-CONTEXT.md (line 136)</read_first>
  <files>tests/e2e/support-mode.spec.ts</files>
  <action>
In `tests/e2e/support-mode.spec.ts`, make these 3 changes:

1. Change `page.getByRole('button', { name: /Support Mode/ }).first()` to `page.getByRole('button', { name: /View as Company/ }).first()`.
2. Change `expect(bodyText).toMatch(/Support Mode/)` to `expect(bodyText).toMatch(/Viewing/)`.
3. Change `page.getByRole('button', { name: /Exit Support Mode/ }).click()` to `page.getByRole('button', { name: /Exit view/ }).click()`.

Leave unchanged: `page.goto('/admin/companies')`, `page.waitForURL(/\/dashboard/, ...)`, `page.waitForURL(/\/admin\/companies/, ...)`, the `test.skip` env-var gating logic, the `test.describe`/`test` name strings (requirement-ID labels, not user-facing copy — update only if it improves clarity, not required), and the login-flow steps at the top of the test.
  </action>
  <verify>
    <automated>grep -n "View as Company\|Exit view\|Viewing" tests/e2e/support-mode.spec.ts; grep -c "Support Mode" tests/e2e/support-mode.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "name: /View as Company/" tests/e2e/support-mode.spec.ts` matches.
    - `grep -n "toMatch(/Viewing/)" tests/e2e/support-mode.spec.ts` matches.
    - `grep -n "name: /Exit view/" tests/e2e/support-mode.spec.ts` matches.
    - `grep -c "Support Mode" tests/e2e/support-mode.spec.ts` returns 0 in selector/assertion positions (a residual match inside an unchanged `test.describe`/comment string is acceptable since those are requirement-ID labels, not functional assertions — verify by inspection that no `getByRole`/`toMatch` call still references the literal "Support Mode" string).
    - `page.goto('/admin/companies')` and both `page.waitForURL(...)` calls are byte-identical to before (`grep -n "page.goto('/admin/companies')\|waitForURL" tests/e2e/support-mode.spec.ts` unchanged from pre-edit content).
  </acceptance_criteria>
  <done>The e2e spec's 3 Playwright selectors/assertions target "View as Company", "Viewing", and "Exit view" respectively; navigation/env-gating logic is untouched.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/admin/companies-support-mode-button.test.ts tests/unit/support-mode-layout.test.ts tests/unit/support-mode.test.ts` — the third file (`support-mode.test.ts`, asserting audit-log literals `company.support_mode_start`/`company.support_mode_end`) is included as a regression check only; it requires NO edits in this plan and must still pass unmodified, proving the internal-naming boundary held. The e2e spec (`tests/e2e/support-mode.spec.ts`) is env-gated (`TEST_ADMIN_EMAIL`/`TEST_ADMIN_PASSWORD`) and will skip in CI without those vars — confirm via `npx playwright test tests/e2e/support-mode.spec.ts --list` that the spec still parses and its test names enumerate correctly.
</verification>

<success_criteria>
- `tests/unit/admin/companies-support-mode-button.test.ts` and `tests/unit/support-mode-layout.test.ts` pass and assert the new "View as Company"/"Viewing" copy; zero remaining `toMatch(/Support Mode/)` assertions in either file.
- `tests/e2e/support-mode.spec.ts` targets "View as Company"/"Viewing"/"Exit view" in its 3 copy-dependent assertions; navigation/env-gating untouched.
- `tests/unit/support-mode.test.ts` (audit-log literal assertions) requires zero changes and still passes, confirming internal naming was never touched.
</success_criteria>

<output>
After completion, create `.planning/phases/157-admin-nav-reorg-naming-fixes/157-03-SUMMARY.md`
</output>
