---
phase: 124-ai-sdk-chat-backend
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - app/api/chat/route.ts
  - tests/unit/chat/route.test.ts
  - tests/unit/chat/credit-reuse.test.ts
autonomous: true
requirements: [CHATBE-02, CHATMETER-01]
must_haves:
  truths:
    - "POST /api/chat authenticates the owner (Supabase) and resolves the active company before streaming"
    - "The route streams a tool-calling turn via streamText with the neutral tools and returns toUIMessageStreamResponse"
    - "On finish, the new assistant/tool turn is persisted via the Phase-123 appendMessage helper (conversation created if conversationId is absent)"
    - "The route contains NO recordCreditDebit / grantCredits call — generation debits inside the existing Inngest job; the conversation turn is absorbed (CHATMETER-01)"
  artifacts:
    - path: "app/api/chat/route.ts"
      provides: "POST handler — owner auth → resolve company → streamText(tools) → persist in onFinish"
      exports: ["POST"]
      min_lines: 40
  key_links:
    - from: "app/api/chat/route.ts"
      to: "lib/chat/provider.ts + lib/chat/tools.ts"
      via: "resolveChatModel(companyId) + buildChatTools(ctx)"
      pattern: "resolveChatModel|buildChatTools"
    - from: "app/api/chat/route.ts"
      to: "lib/queries/chat.ts"
      via: "appendMessage / createConversation in onFinish"
      pattern: "appendMessage|createConversation"
    - from: "app/api/chat/route.ts"
      to: "streamText().toUIMessageStreamResponse"
      via: "convertToModelMessages(messages) + stopWhen: stepCountIs(5)"
      pattern: "toUIMessageStreamResponse"
---

<objective>
Build the `POST /api/chat` route: authenticate the owner, resolve the active company + the owner's industries/language, stream a tool-calling conversation via `streamText` with the Plan-01 tools, and persist the new turn through the Phase-123 helpers in `onFinish`. Prove (statically) that the route adds NO credit debit — generation debits inside its existing Inngest job and the conversation turn is absorbed.

Purpose: This is where the chat becomes capable (CHATBE-02) and where credit reuse is locked-in by ABSENCE (CHATMETER-01 — no double-debit, Pitfall 4).
Output: `app/api/chat/route.ts`; passing route unit test (MockLanguageModelV3) + the static credit-reuse assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-ai-sdk-chat-backend/124-RESEARCH.md
@.planning/phases/124-ai-sdk-chat-backend/124-VALIDATION.md

# Plan 01 outputs this route consumes
@lib/chat/provider.ts
@lib/chat/tools.ts
@lib/chat/system-prompt.ts

# Persistence helpers (Phase 123) + auth/company precedents
@lib/queries/chat.ts
@lib/queries/active-company.ts
@app/api/generate-estimate/route.ts

<interfaces>
<!-- Plan-01 exports (built in 124-01) -->
From @/lib/chat/provider: `resolveChatModel(companyId?: string, deps?: { modelOverride?: LanguageModelV3 }): Promise<LanguageModelV3>`
From @/lib/chat/tools:    `buildChatTools(ctx: { companyId: string; supabase: SupabaseClient; industries: string[]; language?: 'en'|'pt'|'es' }): ToolSet`
From @/lib/chat/system-prompt: `CHAT_SYSTEM_PROMPT: string`

<!-- Phase-123 persistence (lib/queries/chat.ts) — resolve company_id INTERNALLY via getActiveCompanyId(); caller passes conversationId/role/parts -->
```typescript
function createConversation(userId: string, title?: string | null): Promise<ChatConversationRow | null>  // row has .id
function appendMessage(args: { conversationId: string; role: 'user'|'assistant'|'tool'|'system'; parts: unknown; attachments?: unknown }): Promise<ChatMessageRow | null>
```

<!-- Owner auth + active company (mirror app/api/generate-estimate/route.ts) -->
```typescript
// const supabase = await createClient()            // @/lib/supabase/server
// const { data } = await supabase.auth.getClaims() // claims.sub = userId; null → 401
// const companyId = await getActiveCompanyId()     // @/lib/queries/active-company; null → 400/redirect
```

<!-- AI SDK v6 (verified ai@6.0.209) -->
```typescript
import { streamText, convertToModelMessages, stepCountIs, type UIMessage } from 'ai'
streamText({ model, system, messages: convertToModelMessages(messages), tools, stopWhen: stepCountIs(5) })
  .toUIMessageStreamResponse({ originalMessages: messages, onFinish: async ({ messages: full }) => { /* persist tail */ } })
// TEST: import { MockLanguageModelV3 } from 'ai/test'; import { simulateReadableStream } from 'ai'
```

Industries/language read (mirror lib/whatsapp/intent-router.ts L300-314): service-client select
`companies.industries, default_estimate_language` by company id; industries ?? []; language ∈ {'en','pt','es'} else undefined.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/api/chat/route.ts — owner-auth → streamText(tools) → persist in onFinish (CHATBE-02)</name>
  <read_first>
    - app/api/generate-estimate/route.ts (owner auth via supabase.auth.getClaims; getActiveCompanyId; the 401/Node-runtime posture)
    - lib/queries/chat.ts (createConversation + appendMessage signatures + the company-id-resolved-internally contract)
    - lib/whatsapp/intent-router.ts L300-314 (industries/language read to mirror)
    - 124-RESEARCH.md "Pattern 1" + "Pitfall 3" (persist in onFinish, not mid-stream) + "Pitfall 6" (Node runtime, NOT edge) + "Code Examples" (MockLanguageModelV3 test seam)
  </read_first>
  <files>app/api/chat/route.ts, tests/unit/chat/route.test.ts</files>
  <behavior>
    - POST with no auth claims → 401 (no stream, no tool build)
    - POST with auth but no active company → 4xx (no stream)
    - POST authed: builds tools with the trusted companyId + the owner's industries/language, calls streamText with CHAT_SYSTEM_PROMPT + convertToModelMessages(messages) + stopWhen stepCountIs(5), returns toUIMessageStreamResponse
    - onFinish: when conversationId is absent, createConversation is called first; the new assistant/tool tail is persisted via appendMessage
    - the handler runs on the default Node runtime (NO `export const runtime = 'edge'`)
  </behavior>
  <action>
    Create `app/api/chat/route.ts`. Do NOT set `export const runtime = 'edge'` (Pitfall 6 — the neutral tools use node:crypto + the inngest client + the service client; default Node runtime is required).

    `export async function POST(req: Request)`:
      1. Auth the owner: `const supabase = await createClient()` (@/lib/supabase/server); `const { data: claimsData } = await supabase.auth.getClaims()`; `const claims = claimsData?.claims ?? null`; if `!claims?.sub` → return `new Response('Unauthorized', { status: 401 })` (or the repo's XtimatorError/asResponse pattern — match generate-estimate). `const userId = claims.sub as string`.
      2. Resolve the active company: `const companyId = await getActiveCompanyId()` (@/lib/queries/active-company); if `!companyId` → return a 400 (no active company).
      3. Read the owner's retrieval scope with the SERVICE client (mirror intent-router L300-314): select `industries, default_estimate_language` from `companies` where id = companyId; `const industries = company?.industries ?? []`; coerce language to 'en'|'pt'|'es'|undefined.
      4. Parse the body: `const { messages, conversationId }: { messages: UIMessage[]; conversationId?: string } = await req.json()`.
      5. `const model = await resolveChatModel(companyId)` (from @/lib/chat/provider).
      6. `const tools = buildChatTools({ companyId, supabase, industries, language })` (from @/lib/chat/tools). NOTE: pass a SERVICE supabase client for the data-read tools (the neutral query fns expect a client that can read company rows — use createServiceClient()/requireServiceClient(), NOT the RLS-bound request client; mirror lib/queries/chat.ts + query-company-data.ts posture). Resolve this once and reuse for step 3's read.
      7. `const result = streamText({ model, system: CHAT_SYSTEM_PROMPT, messages: convertToModelMessages(messages), tools, stopWhen: stepCountIs(5) })`.
      8. `return result.toUIMessageStreamResponse({ originalMessages: messages, onFinish: async ({ messages: full }) => { ... } })`.
         In onFinish (Pitfall 3 — persist here, NOT mid-stream):
           - Resolve the conversation id: if `conversationId` is falsy, `const conv = await createConversation(userId); convId = conv?.id`; bail if null. Else use the passed `conversationId`.
           - Persist the NEW tail: compute the messages in `full` that are NOT in `originalMessages` (e.g. `full.slice(messages.length)`), and for each, `await appendMessage({ conversationId: convId, role: m.role as ChatRole, parts: m.parts })`. (appendMessage resolves company_id internally via getActiveCompanyId — do NOT pass companyId.)
         Wrap onFinish persistence in try/catch + console.warn so a persistence hiccup never breaks the already-streamed response.

      Add NO recordCreditDebit / grantCredits / checkCredits-debit call anywhere in this file (CHATMETER-01 / Pitfall 4 — generation debits inside its Inngest job; the conversation is absorbed). A best-effort recordAICost row is explicitly OUT for v1 (Open Question 3).

      Make the model injectable for tests: read it from `resolveChatModel(companyId, deps)` where the route can be unit-tested by mocking `@/lib/chat/provider` to return a MockLanguageModelV3 — OR mock the module directly in the test (preferred, matches repo style). Do NOT add a public test-only query param.

    Write `tests/unit/chat/route.test.ts` (vitest), mirroring the repo mock style:
      - `vi.mock('@/lib/supabase/server', ...)` → auth.getClaims returns { claims: { sub: 'user-1' } } (and a no-claims variant for the 401 case).
      - `vi.mock('@/lib/queries/active-company', () => ({ getActiveCompanyId: vi.fn(() => 'company-SECRET') }))`.
      - `vi.mock('@/lib/supabase/service', ...)` → chainable client returning a companies row { industries: ['plumbing'], default_estimate_language: 'en' }.
      - `vi.mock('@/lib/chat/provider', ...)` → resolveChatModel returns a `new MockLanguageModelV3({ doStream: async () => ({ stream: simulateReadableStream({ chunks: [ {type:'text-start',id:'t1'}, {type:'text-delta',id:'t1',delta:'Done.'}, {type:'text-end',id:'t1'}, {type:'finish',finishReason:'stop',usage:{inputTokens:1,outputTokens:1,totalTokens:2}} ] }) }) })` (from `ai/test` + `ai`).
      - `vi.mock('@/lib/chat/tools', () => ({ buildChatTools: vi.fn(() => ({})) }))`.
      - `vi.mock('@/lib/queries/chat', () => ({ createConversation: vi.fn(() => ({ id: 'conv-1' })), appendMessage: vi.fn() }))`.
      Assert: (a) no-claims POST → 401 and streamText path not reached; (b) authed POST returns a streaming Response (status 200, a body); (c) after the stream drains, appendMessage was called (await the response body / onFinish); (d) when conversationId is absent, createConversation was called before appendMessage. Drain the response stream in the test so onFinish fires (e.g. `await new Response(res.body).text()` or read the reader to completion).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/chat/route.test.ts</automated>
  </verify>
  <done>route.test.ts green: 401 without auth; authed POST streams via streamText(MockLanguageModelV3) + toUIMessageStreamResponse; onFinish persists the new tail via appendMessage (createConversation first when conversationId absent). No `runtime = 'edge'`. tools built with the trusted companyId + owner industries/language.</done>
</task>

<task type="auto">
  <name>Task 2: Static credit-reuse assertion — the route adds NO debit (CHATMETER-01)</name>
  <read_first>
    - app/api/chat/route.ts (the file written in Task 1)
    - 124-RESEARCH.md "Pattern 4: Credit reuse" + "Pitfall 4" (generation debits in the Inngest job; conversation absorbed; route must add no debit)
    - lib/inngest/functions/generate-estimate.ts (confirm the record-credit-debit step lives HERE, not in the route — for the SUMMARY rationale; do not modify)
  </read_first>
  <files>tests/unit/chat/credit-reuse.test.ts</files>
  <action>
    Write `tests/unit/chat/credit-reuse.test.ts` (vitest) as a STATIC-SOURCE assertion (no model, no network): read the route source with node fs (`readFileSync('app/api/chat/route.ts','utf8')` — resolve the path from the repo root) and assert it contains NONE of the debit/grant identifiers:
      - `recordCreditDebit`
      - `grantCredits`
      - `consumeCredits`
    i.e. for each forbidden token, assert `source.includes(token) === false`. Add a comment citing Pitfall 4: the chat reuses the existing debit — generation debits inside the Inngest job's `record-credit-debit` step (lib/inngest/functions/generate-estimate.ts); the conversation turn is absorbed per v4.7; adding a debit here would double-charge generation or wrongly charge the conversation.

    (Optional, additive — only if it stays simple: also assert the route does NOT import from `@/lib/billing/credit-ledger` a debit symbol. Keep the core assertion the substring check above.)
  </action>
  <verify>
    <automated>npx vitest run tests/unit/chat/credit-reuse.test.ts</automated>
  </verify>
  <done>credit-reuse.test.ts green: proves app/api/chat/route.ts contains no recordCreditDebit/grantCredits/consumeCredits — CHATMETER-01's no-double-debit invariant is locked by a regression test.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/chat` green (provider + tools from Plan 01, route + credit-reuse from this plan, plus the pre-existing chat-queries/migration tests).
- `npx vitest run` full suite green (baseline ~321 files / 2251+ passed).
- `npx tsc --noEmit` clean.
- grep `app/api/chat/route.ts`: contains `resolveChatModel`, `buildChatTools`, `streamText`, `toUIMessageStreamResponse`, `appendMessage`; contains NO `recordCreditDebit`/`grantCredits`; contains NO `runtime = 'edge'`.
</verification>

<success_criteria>
1. `POST /api/chat` authenticates the owner + resolves the active company, then streams a tool-calling turn via `streamText` with the neutral tools and `toUIMessageStreamResponse` — CHATBE-02.
2. The new assistant/tool turn persists via the Phase-123 `appendMessage` in `onFinish` (conversation created when absent) — backend is self-contained/testable without the Phase-125 UI.
3. The route adds NO credit debit (proven by `credit-reuse.test.ts`); generation debits inside its Inngest job, the conversation turn is absorbed — CHATMETER-01.
4. Runs on the Node runtime (no edge opt-in); the neutral tools' node:crypto/inngest/service-client work.
</success_criteria>

<output>
After completion, create `.planning/phases/124-ai-sdk-chat-backend/124-02-SUMMARY.md`.
</output>
