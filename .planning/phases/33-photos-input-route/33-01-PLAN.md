---
phase: "33-photos-input-route"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "app/projects/[id]/photos-input/page.tsx"
  - "app/projects/[id]/photos-input/photos-input-client.tsx"
  - "components/projects/photos-input.tsx"
autonomous: true
requirements:
  - "PHOTO-01"
  - "PHOTO-02"
  - "PHOTO-03"
  - "PHOTO-04"

must_haves:
  truths:
    - "User can upload photos directly on a dedicated route"
    - "Generate from Photos button appears when at least 1 photo is uploaded"
    - "Running the pipeline lands user in estimate editor"
  artifacts:
    - path: "app/projects/[id]/photos-input/page.tsx"
      provides: "Server-side route shell for /photos-input"
      min_lines: 20
    - path: "app/projects/[id]/photos-input/photos-input-client.tsx"
      provides: "Client wrapper rendering PhotosInput"
      min_lines: 10
    - path: "components/projects/photos-input.tsx"
      provides: "Photo upload UI + generate button"
      min_lines: 60
  key_links:
    - from: "components/projects/photos-input.tsx"
      to: "/api/generate-estimate"
      via: "fetch POST after photos uploaded"
      pattern: "fetch.*api/generate-estimate"
    - from: "app/projects/[id]/photos-input/page.tsx"
      to: "components/projects/photos-input.tsx"
      via: "PhotosInputClient wrapper"
      pattern: "PhotosInputClient"
---

<objective>
Create the `/projects/[id]/photos-input` route with direct photo upload. Users upload photos and click "Generate from Photos" to run the Claude Vision pipeline and generate an estimate.

Purpose: Gives users a focused, dedicated photos-only capture surface. Reuses the existing generate-estimate pipeline which already supports photos-only (per Phase 28). Reuses PhotoDropZone from workspace.
</objective>

<context>
@.planning/ROADMAP.md (Phase 33 goal + success criteria)
@.planning/phases/31-wizard-modality-selection/31-01-SUMMARY.md (wizard routes to /photos-input when inputMode=photos)
@.planning/phases/32-text-input-route/32-01-SUMMARY.md (route shell pattern)
@app/(capture)/projects/[id]/capture/page.tsx (route shell pattern)
@components/workspace/photos/photo-drop-zone.tsx (reused PhotoDropZone component)
@app/api/generate-estimate/route.ts (already supports photos-only: `hasPhotos = photos.length > 0`)

## Key Interfaces

From app/api/generate-estimate/route.ts:
```typescript
// POST /api/generate-estimate
// Body: { projectId: string }
// Returns: { estimateId: string; version: number; clientSuggestion?: ... }
// Guard: requires at least one recording OR photo per project
// Already supports photos-only path (line 88): hasPhotos = photos.length > 0
```

## Success Criteria (from ROADMAP.md)

1. Route `/projects/[id]/photos-input` displays a direct photo upload zone without requiring navigation through the workspace
2. The PhotoDropZone component is reused from the existing workspace
3. A "Generate from Photos" button is visible and prominent as soon as at least 1 photo is uploaded
4. Clicking the button runs the Claude Vision pipeline to analyze the photos and generate the estimate (no transcript required)
5. The user lands in the estimate editor with the generated result, same as audio/text flows
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create photos-input route shell</name>
  <files>app/projects/[id]/photos-input/page.tsx, app/projects/[id]/photos-input/photos-input-client.tsx</files>
  <action>
    Create the route shell following the same pattern as describe/ and capture/ routes:

    - **app/projects/[id]/photos-input/page.tsx**: Async server component that fetches project + company, checks auth, renders `<PhotosInputClient>`
    - **app/projects/[id]/photos-input/photos-input-client.tsx**: 'use client' wrapper that renders `<PhotosInput>` with project + companyId props

    Route group: `(capture)` so it uses the same full-screen layout (no sidebar/topnav) as /capture and /describe.

    Pattern: identical to describe/page.tsx + describe-client.tsx — just swap the component names.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "photos-input"</automated>
  </verify>
  <done>Route page.tsx builds, photos-input-client.tsx exists, both in (capture) route group</done>
</task>

<task type="auto">
  <name>Task 2: Build PhotosInput component</name>
  <files>components/projects/photos-input.tsx</files>
  <action>
    Build a focused photos-input component that reuses PhotoDropZone:

    **Layout (mobile-first, full-screen within (capture) shell):**
    - Header bar: project name (truncated) + back link to project workspace
    - Main area: PhotoDropZone with project context
    - Footer: "Generate from Photos" button (prominent when photos > 0)

    **Component structure:**
    - Use existing `PhotoDropZone` from `@/components/workspace/photos/photo-drop-zone`
    - Track uploaded photos in local state
    - Show uploaded photos as thumbnails (grid layout)
    - "Generate from Photos" button: h-12 (48px), w-full, bg-primary, prominent
    - Button disabled when photoCount === 0
    - Button enabled when photoCount >= 1

    **Generate flow (per PHOTO-04):**
    - On click: set loading state, fetch POST `/api/generate-estimate` with { projectId }
    - On success: router.push to `/projects/${projectId}?tab=estimate&estimate=${data.estimateId}`
    - On error: show toast error, clear loading state

    **Dependencies to import:**
    - `PhotoDropZone` from '@/components/workspace/photos/photo-drop-zone'
    - `createClient` from '@/lib/supabase/client'
    - `useRouter` from 'next/navigation'
    - `Button` from '@/components/ui/button'
    - `toast` from 'sonner'
    - `ArrowLeft` from 'lucide-react' (back link)
    - `Loader2` from 'lucide-react' (loading state)
    - `X` from 'lucide-react' (remove photo)
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "photos-input|Cannot find"</automated>
  </verify>
  <done>
    PhotosInput component: PhotoDropZone renders, thumb gallery shows uploaded, Generate button disabled at 0 photos, enabled with photos, taps generate runs API, lands in estimate editor
  </done>
</task>

<task type="auto">
  <name>Task 3: Build and verify</name>
  <files>package.json (implicit)</files>
  <action>
    Run `npm run build` to verify:
    - TypeScript compiles without errors
    - Route /projects/[id]/photos-input is included in build output
    - No missing imports or type errors
    
    Check for PHOTO-01 through PHOTO-04 coverage:
    - PHOTO-01: Route exists (direct upload zone visible) ✓
    - PHOTO-02: PhotoDropZone reused ✓
    - PHOTO-03: Generate button visible when photos >= 1 ✓
    - PHOTO-04: /api/generate-estimate called, lands in editor ✓
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build succeeds, all 4 PHOTO requirements covered by implementation</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with no errors
- [ ] Route `/projects/[id]/photos-input` appears in build output
- [ ] TypeScript types compile without errors
- [ ] PhotoDropZone renders in component
- [ ] Uploaded photos shown as thumbnails
- [ ] "Generate from Photos" button disabled when 0 photos
- [ ] "Generate from Photos" button enabled when >= 1 photo
- [ ] Button click calls POST /api/generate-estimate
- [ ] On success: router.push to estimate editor
- [ ] On error: toast notification shown
</verification>

<success_criteria>
User visits `/projects/[id]/photos-input`, sees a photo upload zone, uploads one or more photos, taps "Generate from Photos", and lands in the estimate editor with a generated estimate — same outcome as audio/text flows but using only photos.
</success_criteria>

<output>
After completion, create `.planning/phases/33-photos-input-route/33-01-SUMMARY.md`
</output>