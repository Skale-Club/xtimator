---
phase: 166-ai-reliability-output-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/providers/openrouter.ts
  - lib/ai/providers/gemini.ts
  - lib/ai/needs-details.ts
  - lib/estimate/price-research/adapters/openrouter-web.ts
  - lib/estimate/price-research/adapters/anthropic-web.ts
  - lib/ai/with-fallback.ts
  - tests/unit/ai/openrouter-timeout.test.ts
  - tests/unit/ai/openrouter-truncation.test.ts
  - tests/unit/ai/tool-schema-fields.test.ts
autonomous: true
requirements:
  - AIREL-01
  - AIREL-02
  - AIREL-03
  - AIREL-05
must_haves:
  truths:
    - "Every AI fetch in lib/ai and lib/estimate/price-research carries an AbortSignal timeout — a repo-wide grep finds NO fetch to an AI endpoint without one."
    - "A finish_reason==='length' generation response throws a typed TruncatedOutputError (NOT the generic malformed-JSON error), and callWithFallback logs/propagates it distinguishably."
    - "Generation max_tokens is 8192 on OpenRouter and capped equivalently on Gemini (maxOutputTokens 8192) — the two providers have symmetric output budgets."
    - "The live OpenRouter estimateToolSchema declares taxable, tax_category, cost, markup_pct with the same types/enums as the zod schema (lib/ai/schema.ts) — schema parity test proves it."
    - "Generation and refine run at temperature 0.3 on BOTH providers."
  artifacts:
    - path: "lib/ai/providers/openrouter.ts"
      provides: "timeout, finish_reason handling, TruncatedOutputError, max_tokens 8192, 4 new tool-schema fields, temperature 0.3"
    - path: "lib/ai/with-fallback.ts"
      provides: "TruncatedOutputError export (or re-export) + unchanged fallback semantics"
  key_links:
    - from: "lib/ai/providers/openrouter.ts"
      to: "lib/ai/schema.ts"
      via: "tool schema mirrors zod schema fields (parity test)"
      pattern: "markup_pct"
---

<objective>
Close audit findings C1/C2/C3/C5 (AIREL-01/02/03/05): bound every AI fetch, make truncation visible and typed, ask the primary provider for the full pricing schema, and pin temperature. AIREL-04 (consistency checks in lib/services/generate-estimate.ts) is deliberately NOT in this plan — it lands in 166-02 to avoid file contention with Phase 168.

Evidence base (REQUIRED READING): .planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md § C. Key facts: the ONLY unbounded fetches are providers/openrouter.ts:191 (primary generation!), needs-details.ts:87, price-research/adapters/openrouter-web.ts:154, anthropic-web.ts:66 (SDK default 10min); finish_reason is never read — a length-stop parses as malformed JSON and falls to Gemini (masking the cause) or, if truncation lands on valid JSON, persists a silent partial estimate; max_tokens 4096 (openrouter.ts:166) vs a 40-50-item estimate needing ~2,900-4,400 tokens; the live tool schema (openrouter.ts:23-77) is missing taxable/tax_category/cost/markup_pct that gemini.ts:144-154 and zod schema.ts:31-43 both carry; no temperature is set anywhere on the estimate paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md
@lib/ai/providers/openrouter.ts
@lib/ai/with-fallback.ts
@lib/ai/schema.ts

<interfaces>
```ts
// with-fallback.ts (or a sibling errors module) — marker error, same pattern as InvalidEstimateOutputError
export class TruncatedOutputError extends Error {
  readonly truncated = true
  constructor(op: string) { super(`AI output truncated (finish_reason=length) during ${op}`) }
}
```
Timeout constant: reuse/create ONE exported constant (e.g. `AI_CHAT_TIMEOUT_MS = 120_000` — openrouter-client.ts already uses 120s; do not invent a second number). Fallback semantics: TruncatedOutputError still triggers the Gemini fallback (today's behavior, now diagnosable) — do NOT make it terminal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeouts everywhere (AIREL-01)</name>
  <files>lib/ai/providers/openrouter.ts, lib/ai/needs-details.ts, lib/estimate/price-research/adapters/openrouter-web.ts, lib/estimate/price-research/adapters/anthropic-web.ts, tests/unit/ai/openrouter-timeout.test.ts</files>
  <read_first>
    - lib/ai/openrouter-client.ts (the existing AbortSignal.timeout pattern + its timeout constants — MATCH this style)
    - lib/estimate/price-research/adapters/anthropic-web.ts (SDK client construction)
  </read_first>
  <action>
1. openrouter.ts callTool fetch (:191): add `signal: AbortSignal.timeout(120_000)`. Import/share the constant with openrouter-client.ts if exported there; otherwise export one from openrouter.ts and leave a one-line comment naming the sibling.
2. needs-details.ts:87: same, but this path is best-effort never-throw — verify the existing catch swallows AbortError (it must degrade, not throw).
3. openrouter-web.ts:154: add timeout; the adapter is never-throw — an abort becomes a research miss (existing catch).
4. anthropic-web.ts: pass `timeout: 120_000` (and keep default maxRetries) to `new Anthropic({...})`.
5. Test (mock fetch that never resolves, vi.useFakeTimers or AbortSignal assertion): callTool passes an AbortSignal; on abort the error propagates so callWithFallback runs the Gemini fallback — assert fallback is invoked.
  </action>
  <acceptance_criteria>
    - `grep -n "AbortSignal" lib/ai/providers/openrouter.ts lib/ai/needs-details.ts lib/estimate/price-research/adapters/openrouter-web.ts` → 3+ hits
    - anthropic-web SDK client constructed with an explicit timeout
    - Timeout test GREEN; `npm test` GREEN (no regression in existing provider tests)
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/ai/openrouter-timeout.test.ts</automated></verify>
  <done>No unbounded AI fetch remains.</done>
</task>

<task type="auto">
  <name>Task 2: finish_reason + max_tokens + TruncatedOutputError (AIREL-02)</name>
  <files>lib/ai/providers/openrouter.ts, lib/ai/providers/gemini.ts, lib/ai/with-fallback.ts, tests/unit/ai/openrouter-truncation.test.ts</files>
  <read_first>
    - lib/ai/providers/openrouter.ts:79-97 (response type) and :160-270 (callTool + parse)
    - lib/ai/providers/gemini.ts:165-178 (generationConfig)
  </read_first>
  <action>
1. Extend OpenRouterChatResponse choices with `finish_reason?: string`.
2. In callTool, BEFORE JSON.parse of tool arguments: if `choice.finish_reason === 'length'` throw `new TruncatedOutputError(op)`. Keep the existing malformed-JSON error for genuine parse failures.
3. Raise `max_tokens` 4096 → 8192 (:166). Gemini: set `maxOutputTokens: 8192` in generationConfig (today it has none — model default; make the budgets symmetric).
4. TruncatedOutputError lives beside InvalidEstimateOutputError (with-fallback.ts or its errors module) and is exported. callWithFallback: no semantic change (a primary throw already falls back once) — but add the error's name to the existing fallback log line so the cause is diagnosable (the audit showed truncation masquerading as "malformed tool-call arguments").
5. Tests: (a) response with finish_reason 'length' + truncated JSON → TruncatedOutputError (not SyntaxError-wrapped); (b) finish_reason 'length' + VALID JSON → still TruncatedOutputError (this is the silent-partial killer); (c) finish_reason 'stop'/'tool_calls' + valid JSON → parses normally.
  </action>
  <acceptance_criteria>
    - Test (b) is present and GREEN — valid-but-truncated JSON can no longer persist silently
    - max_tokens 8192 on OpenRouter, maxOutputTokens 8192 on Gemini
    - `npm test` GREEN
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/ai/openrouter-truncation.test.ts</automated></verify>
  <done>Truncation is typed, visible, and cannot silently persist a partial estimate.</done>
</task>

<task type="auto">
  <name>Task 3: Tool-schema pricing fields + pinned temperature (AIREL-03/05)</name>
  <files>lib/ai/providers/openrouter.ts, lib/ai/providers/gemini.ts, tests/unit/ai/tool-schema-fields.test.ts</files>
  <read_first>
    - lib/ai/providers/openrouter.ts:23-77 (estimateToolSchema)
    - lib/ai/providers/gemini.ts:144-154 (the four fields' declarations — COPY these semantics)
    - lib/ai/schema.ts:31-43 (zod truth: all four optional; tax_category enum labor|materials|other; numbers nonnegative)
  </read_first>
  <action>
1. Add to the OpenRouter item schema: `taxable` (boolean), `tax_category` (enum labor/materials/other), `cost` (number ≥0), `markup_pct` (number ≥0) — optional (NOT in `required`), with the same one-line descriptions the Gemini schema uses (classification-only; server computes — do not weaken the ENG-01 "AI has no calculator" language).
2. Pin `temperature: 0.3` in the OpenRouter callTool body and Gemini generationConfig — generation AND refine paths (both flow through the same callTool/adapters; verify refine hits it too).
3. Parity test: statically import the OpenRouter tool schema object and assert its item properties are a SUPERSET of ['description','quantity','unit','unit_price','price_source','taxable','tax_category','cost','markup_pct'] and that tax_category enum === ['labor','materials','other'] — locking future drift (this is the exact drift class the audit caught).
  </action>
  <acceptance_criteria>
    - Parity test GREEN; `grep -n "temperature" lib/ai/providers/openrouter.ts lib/ai/providers/gemini.ts` shows 0.3 on both
    - Four fields NOT added to required[] (retrocompat with models omitting them — zod already tolerates omission)
    - `npm test` GREEN
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/ai/tool-schema-fields.test.ts</automated></verify>
  <done>Primary path asks for the full pricing schema at deterministic temperature.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/ai/
npx tsc --noEmit -p tsconfig.ci.json
npm test
```
Grep gates: no AI fetch without signal — `grep -rn "fetch(" lib/ai/ lib/estimate/price-research/ | grep -v test` each hit's call site must show a signal (manual scan in SUMMARY); `grep -c "markup_pct" lib/ai/providers/openrouter.ts` ≥ 1.
Regression contracts (audit § final): callWithFallback still falls back exactly once; InvalidEstimateOutputError semantics untouched; BYOK apiKeyOverride path untouched.
</verification>

<success_criteria>
- AIREL-01/02/03/05 all TRUE per phase success criteria 1-3 & 5 (criterion 4 = 166-02)
</success_criteria>

<output>
Create `.planning/phases/166-ai-reliability-output-integrity/166-01-SUMMARY.md` with per-file changes, the fetch-timeout sweep table, and test evidence.
</output>
