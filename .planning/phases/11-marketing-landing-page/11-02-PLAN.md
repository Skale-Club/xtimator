---
phase: 11-marketing-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - app/page.tsx
  - components/landing/landing-nav.tsx
  - components/landing/hero-section.tsx
  - components/landing/product-mockup.tsx
  - components/landing/how-it-works-section.tsx
  - components/landing/features-section.tsx
  - components/landing/footer-minimal.tsx
  - app/globals.css
autonomous: false
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05

must_haves:
  truths:
    - "Visitor navigating to / sees a hero section with headline 'From Job Site to Professional Estimate in 5 Minutes', subheadline, and 'Get Started Free' CTA button"
    - "Visitor sees a 'How It Works' section with 3 step cards (Record Walkthrough, Add Photos, Receive Estimate)"
    - "Visitor sees a Features section with exactly 4 feature cards (AI Generation, Branded PDF, Shareable Links, Mobile-First)"
    - "Sticky nav shows logo, anchor links, Sign In ghost button, and Get Started primary button"
    - "Mobile nav opens as a Sheet drawer triggered by a hamburger icon (< md breakpoint)"
    - "Hero CTA links to /auth/signup; Sign In links to /auth/login"
    - "Page renders in dark mode with #406EF1 primary color, near-black background, muted-foreground body text"
    - "All interactive elements have visible focus rings; hover states are gated with @media (hover:hover)"
    - "Product mockup SVG is visible in hero on desktop (lg+); stacks below CTA on mobile"
    - "Footer shows '2026 EstimateBuilder Pro. All rights reserved.'"
  artifacts:
    - path: "app/page.tsx"
      provides: "Root page — static server component rendering LandingPage composition"
      contains: "getBranding"
    - path: "components/landing/landing-nav.tsx"
      provides: "Sticky nav with scroll detection, mobile Sheet drawer"
      contains: "use client"
    - path: "components/landing/hero-section.tsx"
      provides: "Hero section with headline, subheadline, gradient text, CTA, radial backdrop"
      contains: "Get Started Free"
    - path: "components/landing/product-mockup.tsx"
      provides: "Stylized SVG/CSS waveform → estimate composition"
      contains: "prefers-reduced-motion"
    - path: "components/landing/how-it-works-section.tsx"
      provides: "3-step card grid with number badges, icons, titles, descriptions"
      contains: "how-it-works"
    - path: "components/landing/features-section.tsx"
      provides: "4-card features grid with icon + title + benefit copy"
      contains: "features"
    - path: "components/landing/footer-minimal.tsx"
      provides: "Minimal footer with copyright"
      contains: "2026 EstimateBuilder Pro"
    - path: "app/globals.css"
      provides: "card-hover-gradient CSS class for gradient border hover treatment"
      contains: "card-hover-gradient"
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/landing-nav.tsx"
      via: "appName prop passed from getBranding() server-side call"
      pattern: "appName"
    - from: "components/landing/landing-nav.tsx"
      to: "components/ui/sheet.tsx"
      via: "Sheet, SheetTrigger, SheetContent imports for mobile drawer"
      pattern: "Sheet"
    - from: "components/landing/hero-section.tsx"
      to: "/auth/signup"
      via: "href on the primary CTA button"
      pattern: "auth/signup"
    - from: "components/landing/how-it-works-section.tsx"
      to: "id='how-it-works'"
      via: "section element id attribute for anchor scroll target"
      pattern: "how-it-works"
    - from: "components/landing/features-section.tsx"
      to: "id='features'"
      via: "section element id attribute for anchor scroll target"
      pattern: id-features
---

<objective>
Build the full marketing landing page UI — all section components plus the updated root page and minimal CSS addition. This plan assembles the landing page from existing design-system primitives (Button, Card, Sheet from components/ui/) following the UI-SPEC.md contracts for copy, color, layout, and interactions.

Purpose: Deliver LAND-01 through LAND-05 — the complete public landing page experience that explains the product and drives sign-up.
Output: 7 new/modified files. `app/page.tsx` composed from 5 new section components under `components/landing/`. One CSS class added to `app/globals.css` for the gradient border hover treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-marketing-landing-page/11-CONTEXT.md
@.planning/phases/11-marketing-landing-page/11-UI-SPEC.md
@.planning/phases/11-marketing-landing-page/11-RESEARCH.md

<!-- MANDATORY: Invoke these skills before implementing UI components (D-15, LAND-05) -->
<!-- Skill 1: https://skills.sh/vercel-labs/agent-skills/web-design-guidelines -->
<!-- Skill 2: https://skills.sh/nextlevelbuilder/ui-ux-pro-max-skill/ui-ux-pro-max -->
<!-- Quality bar: Linear, Vercel, Supabase landing pages -->

<interfaces>
<!-- Key existing interfaces the executor must know. No codebase exploration needed. -->

From components/ui/button.tsx (variants already styled on #406EF1 tokens):
- `variant="default"` → bg-primary text-primary-foreground (the #406EF1 blue CTA)
- `variant="ghost"` → transparent with hover:bg-accent
- `size="sm"` | `size="default"` | `size="lg"` | `size="icon"`

From components/ui/card.tsx:
- `<Card>` — bg-card border border-border rounded-[--radius-lg]
- `<CardHeader>` — px-6 pt-6 pb-0 (or similar)
- `<CardContent>` — px-6 pb-6

From components/ui/sheet.tsx:
- `<Sheet>`, `<SheetTrigger>`, `<SheetContent side="right">`, `<SheetClose>`
- Handles focus trap and aria-modal automatically (Radix Dialog)

From lib/platform-config.ts (server-only — CANNOT import in client components):
- `getBranding(): Promise<{ appName: string, logoUrl: string | null, primaryColor: string | null }>`
- Must be called in app/page.tsx (server component) and appName passed as prop to LandingNav

From app/globals.css (token reference):
- `--primary: 224 86% 60%` — #406EF1 blue
- `--background: 240 10% 3.9%` — near-black
- `--card: 240 10% 3.9%` — slightly elevated surface
- `--border: 240 3.7% 15.9%` — subtle border
- `--foreground: 0 0% 98%` — near-white text
- `--muted-foreground: 240 5% 64.9%` — muted gray text
- `--radius-lg`, `--radius-md`, `--radius-sm` — border radius scale
- `--shadow-xs` through `--shadow-lg` — shadow scale
- `--font-size-sm` (14px), `--font-size-base` (16px), `--font-size-xl` (20px)
- `--font-weight-bold` (700), `--font-weight-normal` (400)
- `--tracking-tight` (-0.01em), `--tracking-tighter` (-0.02em)

Current app/page.tsx (to be fully replaced):
```typescript
import { redirect } from "next/navigation"
export default function RootPage() {
  redirect("/auth/login")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add globals.css card-hover-gradient class + build landing section components</name>
  <files>
    app/globals.css,
    components/landing/landing-nav.tsx,
    components/landing/hero-section.tsx,
    components/landing/product-mockup.tsx,
    components/landing/how-it-works-section.tsx,
    components/landing/features-section.tsx,
    components/landing/footer-minimal.tsx
  </files>
  <read_first>
    - app/globals.css — locate the end of the file to append the new CSS class; verify existing token names match what components will use
    - components/ui/button.tsx — verify variant names and size options
    - components/ui/card.tsx — verify Card/CardHeader/CardContent export names
    - components/ui/sheet.tsx — verify Sheet/SheetTrigger/SheetContent export names
    - .planning/phases/11-marketing-landing-page/11-UI-SPEC.md — copy/icon/layout contracts (single source of truth)
    - .planning/phases/11-marketing-landing-page/11-RESEARCH.md — code patterns (scroll detection, gradient backdrop, badge, gradient text)
  </read_first>
  <action>
**Step A — app/globals.css: append card-hover-gradient class**

Append to the END of app/globals.css (after all existing content):

```css
/* Phase 11: Landing page gradient border hover treatment (ui-ux-pro-max) */
.card-hover-gradient {
  position: relative;
}
@media (hover: hover) {
  .card-hover-gradient:hover::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    padding: 1px;
    background: linear-gradient(135deg, hsl(var(--primary) / 0.4) 0%, hsl(218 85% 73% / 0.4) 100%);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    pointer-events: none;
  }
}
```

<!-- step -->

**Step B — components/landing/landing-nav.tsx**

Create `components/landing/landing-nav.tsx` as a `'use client'` component.

```typescript
'use client'

import { useState, useEffect } from 'react'
import Link from 'next/link'
import { Menu, X, Zap } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Sheet, SheetTrigger, SheetContent, SheetClose } from '@/components/ui/sheet'
import { cn } from '@/lib/utils'

interface LandingNavProps {
  appName: string
}

export function LandingNav({ appName }: LandingNavProps) {
  const [scrolled, setScrolled] = useState(false)

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 0)
    window.addEventListener('scroll', onScroll, { passive: true })
    return () => window.removeEventListener('scroll', onScroll)
  }, [])

  return (
    <header
      className={cn(
        'fixed top-0 z-50 w-full h-16 flex items-center transition-all duration-200',
        scrolled
          ? 'backdrop-blur-md bg-background/80 border-b border-border'
          : 'bg-transparent border-transparent'
      )}
    >
      <nav className="mx-auto w-full max-w-[1200px] px-6 flex items-center justify-between">
        {/* Logo + wordmark */}
        <Link href="/" className="flex items-center gap-2 text-foreground hover:text-foreground/90 transition-colors">
          <Zap className="h-5 w-5 text-primary" aria-hidden="true" />
          <span className="font-bold text-[length:var(--font-size-xl)] tracking-[var(--tracking-tight)]">
            {appName}
          </span>
        </Link>

        {/* Desktop nav — hidden below md */}
        <div className="hidden md:flex items-center gap-6">
          <a href="#how-it-works" className="text-[length:var(--font-size-base)] text-muted-foreground hover:text-foreground transition-colors">
            How It Works
          </a>
          <a href="#features" className="text-[length:var(--font-size-base)] text-muted-foreground hover:text-foreground transition-colors">
            Features
          </a>
        </div>

        {/* Desktop CTAs — hidden below md */}
        <div className="hidden md:flex items-center gap-3">
          <Button variant="ghost" size="sm" asChild>
            <Link href="/login">Sign In</Link>
          </Button>
          <Button size="sm" asChild>
            <Link href="/auth/signup">Get Started</Link>
          </Button>
        </div>

        {/* Mobile hamburger — visible below md */}
        <Sheet>
          <SheetTrigger asChild>
            <Button
              variant="ghost"
              size="icon"
              className="md:hidden min-h-[44px] min-w-[44px]"
              aria-label="Open navigation"
            >
              <Menu className="h-5 w-5" />
            </Button>
          </SheetTrigger>
          <SheetContent side="right" className="w-full sm:w-80 pt-12">
            <div className="flex flex-col gap-4 px-2">
              <SheetClose asChild>
                <a href="#how-it-works" className="text-[length:var(--font-size-base)] text-muted-foreground hover:text-foreground transition-colors py-2">
                  How It Works
                </a>
              </SheetClose>
              <SheetClose asChild>
                <a href="#features" className="text-[length:var(--font-size-base)] text-muted-foreground hover:text-foreground transition-colors py-2">
                  Features
                </a>
              </SheetClose>
              <div className="pt-4 flex flex-col gap-3 border-t border-border">
                <Button variant="ghost" className="w-full justify-center min-h-[44px]" asChild>
                  <Link href="/login">Sign In</Link>
                </Button>
                <Button className="w-full justify-center min-h-[44px]" asChild>
                  <Link href="/auth/signup">Get Started Free</Link>
                </Button>
              </div>
            </div>
          </SheetContent>
        </Sheet>
      </nav>
    </header>
  )
}
```

<!-- step -->

**Step C — components/landing/product-mockup.tsx**

Create `components/landing/product-mockup.tsx` as a server component (no client interactivity needed — animation via CSS only).

```typescript
export function ProductMockup() {
  return (
    <div className="relative flex items-center justify-center">
      {/* Outer glow */}
      <div
        className="relative rounded-[2rem] border border-border overflow-hidden"
        style={{
          boxShadow: '0 0 60px hsl(var(--primary) / 0.15)',
          width: '280px',
          height: '460px',
          background: 'hsl(var(--card))',
        }}
      >
        {/* Phone frame header */}
        <div className="flex items-center justify-center pt-4 pb-2">
          <div className="w-20 h-1.5 rounded-full bg-border" />
        </div>

        {/* Content area */}
        <div className="flex flex-col items-center justify-center h-[calc(100%-48px)] gap-8 px-6">
          {/* State A: Audio waveform */}
          <div className="flex flex-col items-center gap-3">
            <p className="text-[length:var(--font-size-sm)] text-muted-foreground tracking-widest uppercase">Recording</p>
            <div className="flex items-end gap-1.5 h-12" aria-hidden="true">
              {[40, 70, 55, 90, 65, 80, 50].map((h, i) => (
                <div
                  key={i}
                  className="w-2 rounded-full bg-primary motion-safe:animate-[waveform_0.8s_ease-in-out_infinite_alternate]"
                  style={{
                    height: `${h}%`,
                    animationDelay: `${i * 0.1}s`,
                  }}
                />
              ))}
            </div>
          </div>

          {/* Arrow between states */}
          <div className="flex items-center gap-2 text-muted-foreground">
            <div className="h-px w-8 bg-border" />
            <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
              <path d="M1 6h10M7 2l4 4-4 4" stroke="hsl(var(--primary))" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
            </svg>
            <div className="h-px w-8 bg-border" />
          </div>

          {/* State B: Estimate preview */}
          <div className="flex flex-col gap-2 w-full">
            <p className="text-[length:var(--font-size-sm)] text-muted-foreground tracking-widest uppercase text-center">Estimate</p>
            <div className="flex flex-col gap-2">
              <div className="h-2 rounded-full bg-muted-foreground/30 w-full" />
              <div className="h-2 rounded-full bg-muted-foreground/20 w-3/4" />
              <div className="h-2 rounded-full bg-muted-foreground/20 w-5/6" />
              <div className="h-2 rounded-full bg-primary/30 w-1/2" />
            </div>
          </div>
        </div>
      </div>
    </div>
  )
}
```

Add the `@keyframes waveform` animation to `app/globals.css` (append after the card-hover-gradient class added in Step A):

```css
/* Phase 11: Product mockup waveform pulse */
@media (prefers-reduced-motion: no-preference) {
  @keyframes waveform {
    from { transform: scaleY(0.4); }
    to   { transform: scaleY(1); }
  }
}
```

<!-- step -->

**Step D — components/landing/hero-section.tsx**

Create `components/landing/hero-section.tsx` as a server component.

```typescript
import Link from 'next/link'
import { Button } from '@/components/ui/button'
import { ProductMockup } from './product-mockup'

export function HeroSection() {
  return (
    <section
      className="relative min-h-[90vh] flex items-center pt-16"
      style={{
        background: 'radial-gradient(ellipse 80% 50% at 50% -20%, hsl(var(--primary) / 0.12), transparent)',
      }}
    >
      <div className="mx-auto w-full max-w-[1200px] px-6 py-24">
        <div className="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
          {/* Left: text + CTA */}
          <div className="flex flex-col gap-6">
            {/* Eyebrow */}
            <p className="text-[length:var(--font-size-sm)] font-[var(--font-weight-normal)] tracking-[0.04em] text-primary uppercase">
              AI-Powered Estimating
            </p>

            {/* Headline */}
            <h1
              className="font-[var(--font-weight-bold)] leading-[1.15] tracking-[var(--tracking-tighter)] text-foreground"
              style={{ fontSize: 'clamp(1.875rem, 4vw + 1rem, 3.25rem)' }}
            >
              From Job Site to Professional Estimate in{' '}
              <span
                style={{
                  background: 'linear-gradient(135deg, hsl(var(--primary)) 0%, hsl(218 85% 73%) 100%)',
                  WebkitBackgroundClip: 'text',
                  WebkitTextFillColor: 'transparent',
                  backgroundClip: 'text',
                }}
              >
                5 Minutes
              </span>
            </h1>

            {/* Subheadline */}
            <p className="text-[length:var(--font-size-base)] font-[var(--font-weight-normal)] leading-[1.6] text-muted-foreground max-w-lg">
              EstimateBuilder Pro uses AI to turn your audio walkthroughs and job site photos into branded, client-ready estimates — without touching a keyboard.
            </p>

            {/* CTAs */}
            <div className="flex flex-col sm:flex-row gap-3 pt-2">
              <Button size="lg" className="min-h-[44px] text-base font-semibold" asChild>
                <Link href="/auth/signup">Get Started Free</Link>
              </Button>
              <Button variant="ghost" size="lg" className="min-h-[44px] text-base" asChild>
                <a href="#how-it-works">See How It Works</a>
              </Button>
            </div>
          </div>

          {/* Right: product mockup */}
          <div className="flex justify-center lg:justify-end">
            <div className="w-full max-w-[80%] lg:max-w-none flex justify-center">
              <ProductMockup />
            </div>
          </div>
        </div>
      </div>
    </section>
  )
}
```

<!-- step -->

**Step E — components/landing/how-it-works-section.tsx**

Create `components/landing/how-it-works-section.tsx` as a server component.

```typescript
import { Mic, Camera, Sparkles } from 'lucide-react'
import { Card, CardContent } from '@/components/ui/card'
import { cn } from '@/lib/utils'

const STEPS = [
  {
    number: '01',
    icon: Mic,
    title: 'Record a Walkthrough',
    description: 'Walk the job site and narrate what you see. Works on iOS and Android — no app install needed.',
  },
  {
    number: '02',
    icon: Camera,
    title: 'Add Job Site Photos',
    description: 'Snap or upload photos. AI reads the images and adds context to your estimate automatically.',
  },
  {
    number: '03',
    icon: Sparkles,
    title: 'Receive Your Estimate',
    description: 'AI generates a complete, itemized estimate. Review, adjust, and send — all in under 5 minutes.',
  },
]

export function HowItWorksSection() {
  return (
    <section id="how-it-works" className="py-24">
      <div className="mx-auto w-full max-w-[1200px] px-6">
        {/* Section header */}
        <div className="text-center mb-16 flex flex-col gap-3">
          <p className="text-[length:var(--font-size-sm)] font-[var(--font-weight-normal)] tracking-[0.04em] text-primary uppercase">
            How It Works
          </p>
          <h2 className="text-[length:var(--font-size-xl)] sm:text-3xl font-[var(--font-weight-bold)] tracking-[var(--tracking-tight)] text-foreground">
            Three Steps. Zero Typing.
          </h2>
        </div>

        {/* Steps grid */}
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-6 lg:gap-8 relative">
          {/* Connector line (desktop only) */}
          <div
            className="hidden sm:block absolute top-10 left-[calc(33.3%+1.5rem)] right-[calc(33.3%+1.5rem)] h-px border-t border-dashed border-border"
            aria-hidden="true"
          />

          {STEPS.map((step) => {
            const Icon = step.icon
            return (
              <Card
                key={step.number}
                className={cn('card-hover-gradient transition-shadow duration-200 hover:shadow-md')}
              >
                <CardContent className="pt-6 flex flex-col gap-4">
                  {/* Number badge */}
                  <div className="flex items-center gap-3">
                    <span className="w-9 h-9 rounded-full border border-primary/60 flex items-center justify-center text-primary font-bold text-[length:var(--font-size-sm)] flex-shrink-0">
                      {step.number}
                    </span>
                    <Icon className="h-6 w-6 text-primary" aria-hidden="true" />
                  </div>

                  {/* Title */}
                  <h3 className="text-[length:var(--font-size-xl)] font-[var(--font-weight-bold)] tracking-[var(--tracking-tight)] text-foreground">
                    {step.title}
                  </h3>

                  {/* Description */}
                  <p className="text-[length:var(--font-size-base)] font-[var(--font-weight-normal)] leading-[1.5] text-muted-foreground">
                    {step.description}
                  </p>
                </CardContent>
              </Card>
            )
          })}
        </div>
      </div>
    </section>
  )
}
```

<!-- step -->

**Step F — components/landing/features-section.tsx**

Create `components/landing/features-section.tsx` as a server component.

```typescript
import { Brain, FileText, Share2, Smartphone } from 'lucide-react'
import { Card, CardContent } from '@/components/ui/card'
import { cn } from '@/lib/utils'

const FEATURES = [
  {
    icon: Brain,
    title: 'AI Estimate Generation',
    body: 'Claude AI converts your audio and photos into a professional, line-item estimate — no templates, no data entry.',
  },
  {
    icon: FileText,
    title: 'Branded PDF Output',
    body: 'Every estimate exports as a polished PDF with your logo, colors, and business details — ready to hand to a client.',
  },
  {
    icon: Share2,
    title: 'Shareable Links',
    body: 'Send a clean, mobile-friendly estimate link your clients can open on any device — no PDF attachments needed.',
  },
  {
    icon: Smartphone,
    title: 'Mobile-First Design',
    body: 'Built for job sites. Record audio, take photos, and send estimates from your phone — even on iOS Safari.',
  },
]

export function FeaturesSection() {
  return (
    <section id="features" className="py-24 bg-card/30">
      <div className="mx-auto w-full max-w-[1200px] px-6">
        {/* Section header */}
        <div className="text-center mb-16 flex flex-col gap-3">
          <p className="text-[length:var(--font-size-sm)] font-[var(--font-weight-normal)] tracking-[0.04em] text-primary uppercase">
            Why EstimateBuilder Pro
          </p>
          <h2 className="text-[length:var(--font-size-xl)] sm:text-3xl font-[var(--font-weight-bold)] tracking-[var(--tracking-tight)] text-foreground">
            Built for the Field, Polished for the Office
          </h2>
        </div>

        {/* Features grid */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {FEATURES.map((feature) => {
            const Icon = feature.icon
            return (
              <Card
                key={feature.title}
                className={cn('card-hover-gradient transition-all duration-200 h-full')}
              >
                <CardContent className="pt-6 flex flex-col gap-3 h-full">
                  <Icon className="h-5 w-5 text-primary" aria-hidden="true" />
                  <h3 className="text-[length:var(--font-size-xl)] font-[var(--font-weight-bold)] tracking-[var(--tracking-tight)] text-foreground">
                    {feature.title}
                  </h3>
                  <p className="text-[length:var(--font-size-base)] font-[var(--font-weight-normal)] leading-[1.5] text-muted-foreground flex-1">
                    {feature.body}
                  </p>
                </CardContent>
              </Card>
            )
          })}
        </div>
      </div>
    </section>
  )
}
```

<!-- step -->

**Step G — components/landing/footer-minimal.tsx**

Create `components/landing/footer-minimal.tsx` as a server component.

```typescript
import { Zap } from 'lucide-react'

interface FooterMinimalProps {
  appName: string
}

export function FooterMinimal({ appName }: FooterMinimalProps) {
  return (
    <footer className="py-8 border-t border-border">
      <div className="mx-auto w-full max-w-[1200px] px-6 flex flex-col sm:flex-row items-center justify-between gap-4">
        <div className="flex items-center gap-2 text-muted-foreground">
          <Zap className="h-4 w-4 text-primary" aria-hidden="true" />
          <span className="text-[length:var(--font-size-sm)] font-[var(--font-weight-normal)]">{appName}</span>
        </div>
        <p className="text-[length:var(--font-size-sm)] font-[var(--font-weight-normal)] tracking-[0.04em] text-muted-foreground">
          © 2026 EstimateBuilder Pro. All rights reserved.
        </p>
      </div>
    </footer>
  )
}
```

<!-- step -->

**Anti-patterns to avoid (from RESEARCH.md):**
- Do NOT call `getBranding()` inside landing-nav.tsx (it's `server-only`) — appName must come as a prop
- Do NOT use `dark:*` Tailwind variants — use semantic tokens only (`text-foreground`, `bg-background`, `bg-card`, `text-muted-foreground`)
- Do NOT hardcode `#406EF1` — always use `hsl(var(--primary))` or Tailwind `text-primary` / `bg-primary`
- Do NOT add `'use client'` to section components (hero, how-it-works, features, footer) — only landing-nav needs it
- Do NOT install Framer Motion or any animation library — use CSS only
- All hover states must be inside `@media (hover: hover)` — either via `card-hover-gradient` class (done in Step A) or `[@media(hover:hover)]:hover:shadow-md` Tailwind arbitrary variant
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    All 7 component files exist. app/globals.css has card-hover-gradient class and waveform keyframe. Full test suite passes. TypeScript build has no errors (`npx tsc --noEmit`).
  </done>
  <acceptance_criteria>
    - components/landing/landing-nav.tsx exists and contains string "'use client'"
    - components/landing/landing-nav.tsx contains string "Sheet"
    - components/landing/hero-section.tsx exists and contains string "Get Started Free"
    - components/landing/hero-section.tsx contains string "auth/signup"
    - components/landing/product-mockup.tsx exists and contains string "prefers-reduced-motion"
    - components/landing/how-it-works-section.tsx exists and contains string "how-it-works"
    - components/landing/how-it-works-section.tsx contains string "Three Steps"
    - components/landing/features-section.tsx exists and contains string "id=\"features\""
    - components/landing/features-section.tsx contains string "AI Estimate Generation"
    - components/landing/features-section.tsx contains string "Branded PDF Output"
    - components/landing/features-section.tsx contains string "Shareable Links"
    - components/landing/features-section.tsx contains string "Mobile-First Design"
    - components/landing/footer-minimal.tsx exists and contains string "2026 EstimateBuilder Pro"
    - app/globals.css contains string "card-hover-gradient"
    - app/globals.css contains string "waveform"
    - `npm test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace app/page.tsx with landing page composition</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx — current file (contains redirect to be removed)
    - lib/platform-config.ts — verify getBranding() signature and return type
    - components/landing/landing-nav.tsx — verify LandingNav props interface (appName: string)
    - components/landing/footer-minimal.tsx — verify FooterMinimal props interface (appName: string)
  </read_first>
  <action>
Replace the entire content of `app/page.tsx` with:

```typescript
import { getBranding } from '@/lib/platform-config'
import { LandingNav } from '@/components/landing/landing-nav'
import { HeroSection } from '@/components/landing/hero-section'
import { HowItWorksSection } from '@/components/landing/how-it-works-section'
import { FeaturesSection } from '@/components/landing/features-section'
import { FooterMinimal } from '@/components/landing/footer-minimal'

export default async function RootPage() {
  const branding = await getBranding()
  const appName = branding.appName ?? 'EstimateBuilder Pro'

  return (
    <main className="min-h-screen bg-background text-foreground">
      <LandingNav appName={appName} />
      <HeroSection />
      <HowItWorksSection />
      <FeaturesSection />
      <FooterMinimal appName={appName} />
    </main>
  )
}
```

Remove the old `import { redirect } from "next/navigation"` line entirely. The new file must NOT contain any `redirect()` call.

The component is an async server component (`export default async function`) because `getBranding()` is async. The null-coalescing fallback `?? 'EstimateBuilder Pro'` handles the case where branding has not been configured in the admin panel yet.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>app/page.tsx is an async server component importing from components/landing/* with no redirect() call. Full test suite passes. `npx tsc --noEmit` exits 0.</done>
  <acceptance_criteria>
    - app/page.tsx does NOT contain string "redirect"
    - app/page.tsx contains string "getBranding"
    - app/page.tsx contains string "LandingNav"
    - app/page.tsx contains string "HeroSection"
    - app/page.tsx contains string "HowItWorksSection"
    - app/page.tsx contains string "FeaturesSection"
    - app/page.tsx contains string "FooterMinimal"
    - app/page.tsx contains string "async function RootPage"
    - `npm test` exits 0
    - `npx tsc --noEmit` exits 0 (TypeScript strict mode clean)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of landing page</name>
  <what-built>
    Complete marketing landing page at `/`:
    - Sticky nav with scroll detection, desktop + mobile (Sheet drawer) layout
    - Hero section: headline "From Job Site to Professional Estimate in 5 Minutes" with gradient "5 Minutes", subheadline, "Get Started Free" CTA → /auth/signup, product mockup SVG with waveform animation
    - How It Works section with 3 step cards (Record Walkthrough, Add Photos, Receive Estimate)
    - Features section with 4 cards (AI Generation, Branded PDF, Shareable Links, Mobile-First)
    - Minimal footer with copyright
    - Middleware: / is public for unauthenticated, redirects authenticated users to /dashboard
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` — server starts on http://localhost:9633
    2. Open http://localhost:9633/ while LOGGED OUT — confirm the landing page renders (NOT a redirect to /login)
    3. Scroll down — nav should gain backdrop-blur + border-b background on scroll
    4. Click "How It Works" anchor — page should smooth-scroll to the 3-step section
    5. Click "Features" anchor — page should smooth-scroll to the 4-card features grid
    6. Confirm hero headline contains "5 Minutes" in gradient blue/light-blue text
    7. Confirm CTA "Get Started Free" links to /auth/signup (right-click → inspect or hover to check href)
    8. Confirm "Sign In" button links to /login
    9. Open DevTools → Mobile emulation (iPhone 12 or similar) — confirm hamburger icon appears, no desktop nav
    10. Tap hamburger → Sheet drawer opens from right with nav items + full-width CTAs
    11. Confirm touch targets (CTA buttons, hamburger) are at least 44px tall in DevTools
    12. Verify visual quality: dark near-black background, #406EF1 blue accents on badges/CTAs/icons, muted gray body text
    13. Hover over How It Works cards and Features cards — gradient border should appear on hover (desktop only)
    14. Log IN to the app, then navigate to http://localhost:9633/ — confirm redirect to /dashboard fires
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
  <action>Human verification step — no automated action. Run dev server and perform manual checks per how-to-verify list.</action>
  <files>N/A</files>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>User confirms landing page renders correctly at /, all sections visible, mobile nav works, visual quality meets production bar.</done>
</task>

</tasks>

<verification>
- `npm test` — full suite green (middleware tests + brand token tests + all others)
- `npx tsc --noEmit` — TypeScript strict mode clean
- Manual: http://localhost:9633/ renders landing page while logged out
- Manual: http://localhost:9633/ redirects to /dashboard while logged in
- Manual: Mobile emulation shows Sheet drawer, 44px+ touch targets
- Manual: Visual quality meets production bar (dark theme, #406EF1 accents, gradient text on headline)
- Grep `app/page.tsx` for `redirect` — must return 0 matches
- Grep `components/landing/features-section.tsx` for each of 4 feature titles — all present
</verification>

<success_criteria>
- Visitor navigating to / sees the full landing page — not a redirect
- Hero headline, 3-step "How It Works", 4-card Features, sticky nav, and footer all visible
- Page is functional on mobile (Sheet drawer, 44px touch targets)
- Visual design: dark background, #406EF1 primary color, gradient text on hero headline
- Authenticated users are redirected to /dashboard when visiting /
- All tests pass; TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/11-marketing-landing-page/11-02-SUMMARY.md`
</output>
