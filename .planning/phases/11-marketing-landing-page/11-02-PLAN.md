---
phase: 11-marketing-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - playwright.config.ts
  - components/landing/landing-page.tsx
  - components/landing/hero-section.tsx
  - components/landing/how-it-works-section.tsx
  - components/landing/features-section.tsx
  - components/landing/final-cta-section.tsx
  - components/landing/landing-footer.tsx
  - tests/e2e/landing-page.spec.ts
autonomous: true
requirements:
  - LAND-04
  - LAND-05
must_haves:
  truths:
    - "Landing page has no horizontal overflow at mobile widths around 390px"
    - "Hero CTAs remain visible above the fold on mobile Safari and Android Chrome emulation"
    - "Landing page visual system uses near-black surfaces with #406EF1 / #7FA4F4 accents and clear contrast"
    - "Playwright config includes CI-friendly mobile browser projects for this route"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Desktop + iOS Safari + Android Chrome emulation projects"
    - path: "tests/e2e/landing-page.spec.ts"
      provides: "Mobile and palette assertions for LAND-04/LAND-05"
    - path: "components/landing/hero-section.tsx"
      provides: "Above-the-fold responsive CTA layout"
    - path: "components/landing/features-section.tsx"
      provides: "Responsive benefit grid without overflow"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/e2e/landing-page.spec.ts"
      via: "named mobile projects"
      pattern: "Mobile Safari|Pixel"
    - from: "components/landing/*"
      to: ".planning/phases/11-marketing-landing-page/11-UI-SPEC.md"
      via: "palette, spacing, and typography contract"
      pattern: "#406EF1|#7FA4F4|near-black"
---

<objective>
Harden the landing page for mobile breakpoints and raise the visual quality bar to the phase-11 dark-mode marketing spec.

Purpose: Finish the responsive and aesthetic requirements after the public-route baseline exists.
Output: Mobile Playwright projects, mobile route assertions, and polished landing components aligned with the UI contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-marketing-landing-page/11-RESEARCH.md
@.planning/phases/11-marketing-landing-page/11-UI-SPEC.md
@.planning/phases/11-marketing-landing-page/11-VALIDATION.md
@playwright.config.ts
@tests/e2e/landing-page.spec.ts
</context>

<interfaces>
Playwright currently has one project only:

```ts
projects: [
  {
    name: 'chromium',
    use: { ...devices['Desktop Chrome'] },
  },
]
```

Phase-11 mobile validation contract:
- Add iOS Safari emulation and Android Chrome emulation.
- Keep base URL `http://localhost:9633`.
- Preserve CI-friendly single-worker execution unless there is a proven reason to change it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mobile-project coverage and responsive assertions</name>
  <files>playwright.config.ts, tests/e2e/landing-page.spec.ts</files>
  <read_first>
    - playwright.config.ts
    - tests/e2e/landing-page.spec.ts
    - .planning/phases/11-marketing-landing-page/11-VALIDATION.md
  </read_first>
  <behavior>
    - Playwright runs the landing-page spec on desktop plus mobile Safari and Android Chrome emulation.
    - Tests assert no horizontal overflow, hero CTA visibility above the fold, and section visibility on mobile.
    - Brand/palette assertion verifies the page does not regress to a light background or unbranded accent treatment.
  </behavior>
  <action>
    1. Extend `playwright.config.ts` with two additional projects using built-in device presets for iPhone Safari and Pixel/Android Chrome.
    2. Expand `tests/e2e/landing-page.spec.ts` with:
       - `mobile`: no horizontal scroll (`document.documentElement.scrollWidth <= window.innerWidth`).
       - `mobile`: primary CTA group visible without scrolling on a mobile viewport.
       - `brand palette`: computed body/section background is non-white and CTA/button styling resolves through the blue brand token.
    3. Keep route-specific assertions targeted to `/` so the new mobile coverage stays fast enough for the validation cadence in `11-VALIDATION.md`.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/landing-page.spec.ts -g mobile</automated>
    <automated>npm run test:e2e -- tests/e2e/landing-page.spec.ts -g "brand palette"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` includes desktop plus two mobile projects.
    - `tests/e2e/landing-page.spec.ts` contains mobile-specific assertions, not just desktop smoke tests.
    - Mobile checks remain scoped to the landing route and are runnable in CI.
  </acceptance_criteria>
  <done>Responsive coverage for LAND-04 exists and is wired into the standard E2E entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Raise the landing page to the Phase 11 visual contract</name>
  <files>components/landing/landing-page.tsx, components/landing/hero-section.tsx, components/landing/how-it-works-section.tsx, components/landing/features-section.tsx, components/landing/final-cta-section.tsx, components/landing/landing-footer.tsx</files>
  <read_first>
    - .planning/phases/11-marketing-landing-page/11-UI-SPEC.md
    - app/globals.css
    - components/ui/button.tsx
    - components/ui/card.tsx
  </read_first>
  <behavior>
    - Near-black background and layered dark surfaces feel intentional, not default Tailwind gray.
    - Headline hierarchy, spacing rhythm, and card density match the UI spec.
    - Focus-visible states are obvious on every interactive element.
    - Mobile layout remains clean and unclipped after the visual pass.
  </behavior>
  <action>
    1. Refine the landing components to match the UI contract's palette, spacing, and type scale using semantic tokens and limited inline gradients where needed.
    2. Apply production-quality polish to hero framing, card treatment, and CTA emphasis without adding deferred sections.
    3. Keep the visual language practical and field-oriented; avoid abstract illustrations or generic enterprise filler.
    4. During execution, use the repo's available design skills for taste calibration if needed, but keep code changes local to the landing components.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/landing-page.spec.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Landing page visually matches the dark-mode contract from `11-UI-SPEC.md`.
    - All CTAs and links expose visible focus states.
    - Mobile layouts at common widths remain readable and overflow-free after polish.
    - Full Vitest suite still passes before phase verification.
  </acceptance_criteria>
  <done>The landing page satisfies LAND-04 and LAND-05 with both responsive coverage and production-grade visual treatment.</done>
</task>

</tasks>

<notes>
- Do not move color ownership back into `app/globals.css`; Phase 10 already established the shared token baseline.
- Keep mobile coverage practical: route-specific assertions, not screenshot sprawl.
- Manual verification on real iPhone and Android hardware is still required before phase sign-off.
</notes>
