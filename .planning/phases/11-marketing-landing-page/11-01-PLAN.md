---
phase: 11-marketing-landing-page
plan: 01
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - proxy.ts
  - lib/supabase/proxy.ts
  - app/page.tsx
  - components/landing/landing-page.tsx
  - components/landing/hero-section.tsx
  - components/landing/how-it-works-section.tsx
  - components/landing/features-section.tsx
  - components/landing/final-cta-section.tsx
  - components/landing/landing-footer.tsx
  - tests/unit/middleware.test.ts
  - tests/e2e/landing-page.spec.ts
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
must_haves:
  truths:
    - "Unauthenticated visit to / renders a public landing page instead of redirecting to /auth/login"
    - "Authenticated visit to / keeps the fast path by redirecting to /dashboard from proxy-layer routing"
    - "Hero section includes headline, subheadline, and signup/login CTA pair"
    - "How It Works renders the fixed 3-step flow: audio -> photos -> estimate"
    - "Features grid highlights AI generation, branded PDF, share link, and mobile-first use"
  artifacts:
    - path: "app/page.tsx"
      provides: "Root server component rendering the public landing page"
    - path: "proxy.ts"
      provides: "Authenticated-user redirect from / to /dashboard while leaving / public"
    - path: "components/landing/landing-page.tsx"
      provides: "Top-level landing composition"
    - path: "tests/e2e/landing-page.spec.ts"
      provides: "Public-route smoke coverage for hero, steps, features, and root behavior"
  key_links:
    - from: "proxy.ts"
      to: "lib/supabase/proxy.ts"
      via: "existing session refresh and auth claims flow"
      pattern: "updateSession"
    - from: "app/page.tsx"
      to: "components/landing/landing-page.tsx"
      via: "root route server rendering"
      pattern: "LandingPage"
    - from: "tests/e2e/landing-page.spec.ts"
      to: "/"
      via: "Playwright route assertions"
      pattern: "page.goto('/')"
---

<objective>
Establish the first public marketing surface at `/` and move authenticated root redirects out of the page component so visitors see a real landing page while signed-in users still reach the app immediately.

Purpose: Deliver the core phase-11 product story and routing behavior before mobile-only polish work.
Output: Public landing page sections, root-route proxy redirect logic, and baseline automated coverage for the new route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-marketing-landing-page/11-RESEARCH.md
@.planning/phases/11-marketing-landing-page/11-UI-SPEC.md
@.planning/phases/11-marketing-landing-page/11-VALIDATION.md
@app/page.tsx
@proxy.ts
@lib/supabase/proxy.ts
@tests/unit/middleware.test.ts
@tests/e2e/auth.spec.ts
</context>

<interfaces>
Current root route contract:

```tsx
// app/page.tsx
export default function RootPage() {
  redirect('/auth/login')
}
```

Current auth gate contract:

```ts
// lib/supabase/proxy.ts
if (!claims && !isAuthRoute && !isPublicEstimate) {
  url.pathname = '/login'
  return NextResponse.redirect(url)
}
```

Phase-11 requirement change:
- `/` becomes public for visitors.
- Authenticated users requesting `/` are redirected to `/dashboard` before page render.
- No new hardcoded brand palette constants outside the landing page implementation; prefer semantic tokens and the Phase 10 `--primary` baseline.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Open the root route and add routing safety coverage</name>
  <files>proxy.ts, lib/supabase/proxy.ts, tests/unit/middleware.test.ts, tests/e2e/landing-page.spec.ts</files>
  <read_first>
    - proxy.ts
    - lib/supabase/proxy.ts
    - tests/unit/middleware.test.ts
    - tests/e2e/auth.spec.ts
    - tests/e2e/admin-gate.spec.ts
  </read_first>
  <behavior>
    - Unauthenticated visit to `/` is allowed through the auth gate.
    - Authenticated visit to `/` redirects to `/dashboard`.
    - Existing protected-route behavior for `/dashboard` and `/onboarding` stays intact.
    - Baseline landing e2e covers hero visibility and that `/` no longer redirects visitors to `/auth/login`.
  </behavior>
  <action>
    1. Update the route-classification logic so `/` is treated as public without accidentally opening protected app routes.
    2. Add the authenticated-root fast path in `proxy.ts`, using the existing proxy/session flow instead of introducing a second middleware file.
    3. Expand `tests/unit/middleware.test.ts` to lock the new public-root rule and keep `/dashboard` and `/onboarding` protected.
    4. Create `tests/e2e/landing-page.spec.ts` with these baseline cases:
       - `hero`: visiting `/` shows the landing headline and CTA pair.
       - `how it works`: all 3 step labels are visible.
       - `features`: the 4 product benefit cards are visible.
       - `unauthenticated root`: visiting `/` does not land on `/auth/login`.
       - `authenticated root redirect`: env-gated if necessary; use the same live-login pattern already present in admin tests rather than inventing a fake session harness.
  </action>
  <verify>
    <automated>npm test -- tests/unit/middleware.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/landing-page.spec.ts -g hero</automated>
  </verify>
  <acceptance_criteria>
    - `/` is explicitly covered as public in tests.
    - Authenticated-root redirect logic lives in `proxy.ts`, not a second middleware entrypoint.
    - `tests/e2e/landing-page.spec.ts` exists and contains named coverage for hero, how-it-works, features, and root behavior.
    - No regression in `/dashboard` and `/onboarding` protection.
  </acceptance_criteria>
  <done>Root-route protection rules are updated and covered before the landing UI is wired in.</done>
</task>

<task type="auto">
  <name>Task 2: Build the public landing page sections</name>
  <files>app/page.tsx, components/landing/landing-page.tsx, components/landing/hero-section.tsx, components/landing/how-it-works-section.tsx, components/landing/features-section.tsx, components/landing/final-cta-section.tsx, components/landing/landing-footer.tsx</files>
  <read_first>
    - app/page.tsx
    - app/globals.css
    - components/ui/button.tsx
    - components/ui/card.tsx
    - .planning/phases/11-marketing-landing-page/11-UI-SPEC.md
  </read_first>
  <behavior>
    - Hero includes strong headline, subheadline, and two clear CTAs for signup and login.
    - How It Works uses the fixed 3-step narrative: record audio on site, add photos, receive AI-generated estimate.
    - Features grid contains exactly the four benefits called out by LAND-03.
    - Final CTA and footer close the page without adding deferred sections like pricing or testimonials.
  </behavior>
  <action>
    1. Replace `app/page.tsx` redirect-only implementation with a server component that renders `LandingPage`.
    2. Build a small `components/landing/*` set rather than putting the whole page in one file, but keep the split shallow and phase-scoped.
    3. Use dark-first semantic tokens and the Phase 10 primary color through project utilities like `bg-primary`, `text-primary`, `ring-ring`, `bg-card`, `border-border`, and `text-muted-foreground`.
    4. Ensure the copy is concrete and field-work practical, matching the research and UI-spec tone instead of generic B2B SaaS filler.
    5. Keep the section order fixed: Hero -> How It Works -> Features -> Final CTA -> Footer.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/landing-page.spec.ts -g "hero|how it works|features"</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` no longer calls `redirect('/auth/login')`.
    - The landing page renders the three required content sections plus final CTA and footer.
    - The page consumes existing semantic tokens instead of introducing a new global palette system.
    - Deferred scope stays deferred: no pricing table, testimonial carousel, or product video.
  </acceptance_criteria>
  <done>The root route now renders the production landing-page baseline and covers LAND-01 through LAND-03.</done>
</task>

</tasks>

<notes>
- Use the existing `proxy.ts` entrypoint, not `middleware.ts`; this repo already standardized on the proxy file in Phase 8.
- If the authenticated positive E2E cannot run in all environments, keep the test env-gated rather than deleting the scenario from the suite.
- Keep the design implementation intentionally minimal in wave 1; Phase 11 plan 02 handles mobile-project coverage and visual polish.
</notes>
