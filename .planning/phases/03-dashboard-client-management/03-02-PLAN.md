---
phase: 03-dashboard-client-management
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/(app)/dashboard/page.tsx
  - components/dashboard/stat-cards.tsx
  - components/dashboard/stat-card.tsx
  - components/dashboard/project-list.tsx
  - components/dashboard/project-table-row.tsx
  - components/dashboard/project-card.tsx
  - components/dashboard/project-actions.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-08

must_haves:
  truths:
    - "Dashboard shows 4 stat cards with real data from the database"
    - "Project list displays all projects with name, client, type, status badge, total, and date"
    - "User can search projects by name or client name and see results filtered live"
    - "User can filter projects by status using tab buttons"
    - "User can sort projects by date, value, or name"
    - "Each project has View, Edit, Delete, Duplicate quick actions"
    - "Delete shows a confirmation dialog before executing"
    - "A New Project button is prominently visible"
    - "Empty state shows CTA when no projects exist"
  artifacts:
    - path: "app/(app)/dashboard/page.tsx"
      provides: "Dashboard server component page"
      contains: "DashboardPage"
    - path: "components/dashboard/stat-cards.tsx"
      provides: "4-card stats grid"
      contains: "StatCards"
    - path: "components/dashboard/project-list.tsx"
      provides: "Interactive project list with search, filter, sort"
      contains: "ProjectList"
    - path: "components/dashboard/project-actions.tsx"
      provides: "Quick action dropdown per project"
      contains: "ProjectActions"
  key_links:
    - from: "app/(app)/dashboard/page.tsx"
      to: "lib/queries/dashboard.ts"
      via: "getDashboardStats and getProjects calls"
      pattern: "getDashboardStats|getProjects"
    - from: "components/dashboard/project-actions.tsx"
      to: "lib/actions/project.ts"
      via: "deleteProjectAction and duplicateProjectAction"
      pattern: "deleteProjectAction|duplicateProjectAction"
    - from: "components/dashboard/project-list.tsx"
      to: "components/dashboard/status-badge.tsx"
      via: "import StatusBadge"
      pattern: "StatusBadge"
---

<objective>
Build the complete dashboard page with stat cards showing real database metrics and the full project list with search, status filtering, sorting, and quick actions.

Purpose: This is the primary landing page after login -- the user's command center showing project overview and enabling quick navigation to any project.

Output: Fully functional `/dashboard` page with stat cards, project list (table on desktop, cards on mobile), search, filter tabs, sort controls, quick actions, and empty states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-client-management/03-CONTEXT.md
@.planning/phases/03-dashboard-client-management/03-RESEARCH.md
@.planning/phases/03-dashboard-client-management/03-01-SUMMARY.md

@lib/queries/dashboard.ts
@lib/actions/project.ts
@components/dashboard/status-badge.tsx
@components/dashboard/empty-state.tsx
@components/app-shell/nav-items.ts
@app/(app)/layout.tsx
@supabase/migrations/20260409000001_initial_schema.sql

<interfaces>
<!-- From Plan 01: lib/queries/dashboard.ts -->
```typescript
export interface DashboardStats {
  totalProjects: number
  pendingEstimates: number
  acceptedEstimates: number
  totalRevenue: number
}

export interface ProjectWithClient {
  id: string
  name: string
  project_type: string | null
  status: string
  total: number
  created_at: string
  client: { id: string; name: string } | null
}

export async function getDashboardStats(supabase: SupabaseClient, companyId: string): Promise<DashboardStats>
export async function getProjects(supabase: SupabaseClient, companyId: string): Promise<ProjectWithClient[]>
```

<!-- From Plan 01: lib/actions/project.ts -->
```typescript
export async function deleteProjectAction(projectId: string): Promise<{ data?: { deleted: true }; error?: string }>
export async function duplicateProjectAction(projectId: string): Promise<{ data?: any; error?: string }>
```

<!-- From Plan 01: components/dashboard/status-badge.tsx -->
```typescript
export function StatusBadge({ status }: { status: string }): JSX.Element
```

<!-- From Plan 01: components/dashboard/empty-state.tsx -->
```typescript
export function EmptyState({ icon, title, description, actionLabel?, actionHref?, onClearFilter? }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stat cards and dashboard page server component</name>
  <files>
    components/dashboard/stat-card.tsx,
    components/dashboard/stat-cards.tsx,
    app/(app)/dashboard/page.tsx
  </files>
  <read_first>
    lib/queries/dashboard.ts,
    lib/supabase/server.ts,
    app/(app)/layout.tsx,
    components/dashboard/status-badge.tsx,
    components/dashboard/empty-state.tsx,
    supabase/migrations/20260409000001_initial_schema.sql
  </read_first>
  <action>
    **1. Create StatCard** (`components/dashboard/stat-card.tsx`):
    - Non-client component (no 'use client' needed, purely presentational)
    - Accept props: `{ icon: LucideIcon; label: string; value: string | number; className?: string }`
    - Render: shadcn/ui `Card` with `CardContent` containing:
      - Top row: icon (h-5 w-5, text-muted-foreground) + label (text-sm text-muted-foreground)
      - Bottom: value in `text-2xl font-bold`

    **2. Create StatCards** (`components/dashboard/stat-cards.tsx` — per D-06):
    - Import `StatCard`, `DashboardStats` from respective modules
    - Import icons: `FolderOpen, Clock, CheckCircle, DollarSign` from lucide-react
    - Accept props: `{ stats: DashboardStats }`
    - Render 4 StatCards in `grid grid-cols-1 sm:grid-cols-2 gap-4`:
      - Total Projects: icon=FolderOpen, value=stats.totalProjects
      - Pending Estimates: icon=Clock, value=stats.pendingEstimates
      - Accepted: icon=CheckCircle, value=stats.acceptedEstimates
      - Total Revenue: icon=DollarSign, value formatted as USD using `new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 0 }).format(stats.totalRevenue)`

    **3. Create Dashboard Page** (`app/(app)/dashboard/page.tsx` — per D-07):
    - Server component (async function, no 'use client')
    - Auth + company fetch (same pattern as layout but needed for company_id):
      ```typescript
      const supabase = await createClient()
      const { data: claimsData } = await supabase.auth.getClaims()
      const claims = claimsData?.claims ?? null
      if (!claims) redirect('/auth/login')
      const { data: company } = await supabase.from('companies').select('id').eq('user_id', claims.sub).single()
      if (!company) redirect('/onboarding')
      ```
    - Fetch: `const stats = await getDashboardStats(supabase, company.id)`
    - Fetch: `const projects = await getProjects(supabase, company.id)`
    - Render:
      - Page header: `<h1 className="text-2xl font-bold">Dashboard</h1>` with a `<Link href="/projects/new">` Button labeled "+ New Project" on the right (per D-15, DASH-06) — use `flex items-center justify-between` layout
      - `<StatCards stats={stats} />`
      - `<ProjectList projects={projects} />` (from next task)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - components/dashboard/stat-card.tsx contains `export function StatCard` and `Card` import
    - components/dashboard/stat-cards.tsx contains `export function StatCards` and `DashboardStats`
    - components/dashboard/stat-cards.tsx contains `grid grid-cols-1 sm:grid-cols-2`
    - components/dashboard/stat-cards.tsx contains `Intl.NumberFormat` for currency formatting
    - app/(app)/dashboard/page.tsx contains `export default async function`
    - app/(app)/dashboard/page.tsx contains `getDashboardStats` and `getProjects`
    - app/(app)/dashboard/page.tsx contains `getClaims()`
    - app/(app)/dashboard/page.tsx contains `<StatCards` and `<ProjectList`
    - app/(app)/dashboard/page.tsx contains `New Project` button/link text
  </acceptance_criteria>
  <done>
    Dashboard page loads with 4 stat cards showing real data and passes the project array to ProjectList.
  </done>
</task>

<task type="auto">
  <name>Task 2: Project list with search, filter, sort, and quick actions</name>
  <files>
    components/dashboard/project-list.tsx,
    components/dashboard/project-table-row.tsx,
    components/dashboard/project-card.tsx,
    components/dashboard/project-actions.tsx
  </files>
  <read_first>
    lib/queries/dashboard.ts,
    lib/actions/project.ts,
    components/dashboard/status-badge.tsx,
    components/dashboard/empty-state.tsx,
    app/(app)/dashboard/page.tsx
  </read_first>
  <action>
    **1. Create ProjectActions** (`components/dashboard/project-actions.tsx` — per D-13, D-14, DASH-07, DASH-08):
    - "use client" component
    - Accept props: `{ projectId: string; projectName: string }`
    - Render shadcn/ui `DropdownMenu` with `DropdownMenuTrigger` (MoreHorizontal icon button), `DropdownMenuContent`:
      - "View" item: `<Link href={/projects/${projectId}}>` (wrapped in DropdownMenuItem)
      - "Edit" item: `<Link href={/projects/${projectId}}>` (same destination for now, Phase 4 builds workspace)
      - Separator
      - "Duplicate" item: calls `duplicateProjectAction(projectId)` from `lib/actions/project.ts`, shows `toast.success('Project duplicated')` on success or `toast.error(result.error)` on failure
      - Separator
      - "Delete" item: `className="text-red-600"`, opens AlertDialog
    - AlertDialog (per D-14): title "Delete Project", description `Are you sure you want to delete "${projectName}"? This action cannot be undone.`, Cancel button, Delete button (variant="destructive") that calls `deleteProjectAction(projectId)`, shows toast on result
    - Use `useState` for AlertDialog open state, `useTransition` for pending states on delete/duplicate

    **2. Create ProjectTableRow** (`components/dashboard/project-table-row.tsx`):
    - Accept props: `{ project: ProjectWithClient }`
    - Render shadcn/ui `TableRow` with `TableCell` for each column:
      - Name: `font-medium`, linked to `/projects/${project.id}`
      - Client: `project.client?.name ?? '—'` in `text-muted-foreground`
      - Type: `project.project_type ?? '—'`
      - Status: `<StatusBadge status={project.status} />`
      - Total: formatted as USD with `Intl.NumberFormat`
      - Date: `new Date(project.created_at).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`
      - Actions: `<ProjectActions projectId={project.id} projectName={project.name} />`

    **3. Create ProjectCard** (`components/dashboard/project-card.tsx` — mobile card per D-08):
    - Accept props: `{ project: ProjectWithClient }`
    - Render shadcn/ui `Card` with:
      - Top row: project name (font-medium, linked) + ProjectActions dropdown
      - Middle: client name, project type, date — all `text-sm text-muted-foreground`
      - Bottom row: `<StatusBadge>` + total amount (font-semibold)
    - Compact layout for mobile, good touch targets

    **4. Create ProjectList** (`components/dashboard/project-list.tsx` — per D-08, D-10, D-11, D-12, D-21, D-23):
    - "use client" component
    - Accept props: `{ projects: ProjectWithClient[] }`
    - **State:**
      - `search: string` (default '')
      - `statusFilter: string` (default 'all')
      - `sortBy: string` (default 'newest')
    - **Search** (per D-10, DASH-03): shadcn/ui `Input` with Search icon (lucide), placeholder "Search projects...", filters `projects` where `project.name` or `project.client?.name` includes search term (case-insensitive)
    - **Status filter tabs** (per D-11, DASH-04): horizontal row of buttons/tabs: `All, Draft, Processing, Ready, Sent, Accepted, Declined, Archived`. Use `overflow-x-auto whitespace-nowrap` for mobile scroll. Active tab highlighted. Filter matching `project.status`
    - **Sort dropdown** (per D-12, DASH-05): shadcn/ui `Select` with options:
      - `newest` (default): sort by created_at descending
      - `oldest`: sort by created_at ascending
      - `highest`: sort by total descending
      - `alphabetical`: sort by name ascending
    - **Computed filtered list**: `useMemo` chain: filter by search -> filter by status -> sort
    - **Rendering:**
      - If `projects.length === 0` (no projects at all): render `<EmptyState icon={FolderOpen} title="No projects yet" description="Create your first project to get started" actionLabel="New Project" actionHref="/projects/new" />` (per D-21)
      - If filtered list is empty but projects exist: render `<EmptyState icon={Search} title="No projects match your search" description="Try a different search term or clear filters" onClearFilter={() => { setSearch(''); setStatusFilter('all') }} />` (per D-23)
      - Otherwise:
        - Desktop (per D-08): `<div className="hidden md:block">` containing shadcn/ui `Table` with `TableHeader` (columns: Name, Client, Type, Status, Total, Date, Actions) and `TableBody` mapping to `<ProjectTableRow>`
        - Mobile (per D-08): `<div className="md:hidden space-y-3">` mapping to `<ProjectCard>`
    - Layout: search + sort on same row (`flex gap-2`), filter tabs below, then list
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - components/dashboard/project-list.tsx contains `'use client'` and `export function ProjectList`
    - components/dashboard/project-list.tsx contains `useState` for search, statusFilter, sortBy
    - components/dashboard/project-list.tsx contains `useMemo` for filtered/sorted list
    - components/dashboard/project-list.tsx contains `Search projects` or `Search` placeholder text
    - components/dashboard/project-list.tsx contains status filter values: `draft` and `accepted` and `sent`
    - components/dashboard/project-list.tsx contains sort options: `newest` and `oldest` and `highest` and `alphabetical`
    - components/dashboard/project-list.tsx contains `hidden md:block` and `md:hidden` for responsive switch
    - components/dashboard/project-list.tsx contains `<EmptyState` for both no-projects and no-results states
    - components/dashboard/project-list.tsx contains `<Table` for desktop view
    - components/dashboard/project-actions.tsx contains `'use client'` and `DropdownMenu` and `AlertDialog`
    - components/dashboard/project-actions.tsx contains `deleteProjectAction` and `duplicateProjectAction`
    - components/dashboard/project-actions.tsx contains `text-red-600` or `destructive` for delete styling
    - components/dashboard/project-table-row.tsx contains `TableRow` and `StatusBadge` and `ProjectActions`
    - components/dashboard/project-table-row.tsx contains `Intl.NumberFormat` for currency
    - components/dashboard/project-card.tsx contains `Card` and `StatusBadge` and `ProjectActions`
  </acceptance_criteria>
  <done>
    The dashboard project list is fully interactive: search filters by name/client, status tabs filter by project status, sort dropdown reorders the list, each project row has View/Edit/Delete/Duplicate actions, delete shows confirmation dialog, empty states render appropriately.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` completes successfully (pages render as server components)
- `/dashboard` URL renders stat cards and project list (or empty state for new users)
- On mobile viewport (375px), cards render instead of table, bottom nav is visible
</verification>

<success_criteria>
- Dashboard page shows 4 stat cards with real data from Supabase queries (DASH-01)
- Project list shows all projects with name, client, type, status badge, total, date (DASH-02)
- Search filters projects by name or client name in real time (DASH-03)
- Status filter tabs filter the project list (DASH-04)
- Sort dropdown reorders by newest, oldest, highest value, alphabetical (DASH-05)
- "+ New Project" button is prominently placed at top of page (DASH-06)
- Each project has View, Edit, Delete, Duplicate quick actions (DASH-07)
- Delete shows AlertDialog confirmation before executing (DASH-08)
- Empty state with CTA shows when no projects exist
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-client-management/03-02-SUMMARY.md`
</output>
