# Phase 186: Webview Design Polish - Context

**Gathered:** 2026-07-27
**Status:** Ready for planning
**Source:** Owner conversation (2026-07-27) + v4.23 research

<domain>
## Phase Boundary

Design refinement pass on the benchmark webview (both templates, mobile included), with refinements propagating to the PDF through the shared engine so the surfaces stay unified. Requirement: POLISH-01. Runs LAST (after 185) to avoid re-touching files 183/185 modify. Depends on: Phase 182 (tokens); sequenced after 185.

</domain>

<decisions>
## Implementation Decisions

- Owner: "também acho que precisamos melhorar um pouco o design do webview" — a refinement pass, not a redesign. The webview stays the benchmark; improvements flow through shared tokens so PDF + paginated mode inherit them.
- Scope: typography rhythm/hierarchy, spacing consistency, table readability, totals block emphasis, terms cards, photo grid, signature block styling (landed in 183), header/branding treatment — both templates, light theme (share page forces light), mobile breakpoints included.
- ABSORB THE OWNER'S PENDING REFERENCE IMAGE for the paginated-mode UI if it has arrived by this phase; if not, apply best-judgment polish and document exactly which token/component adjustments map to reference-driven tweaks later.
- Hard constraints: no layout-structure changes that alter the Phase 184 atomic-block inventory or break-rule semantics; pagination tests stay green; existing share-page e2e/visual baselines updated intentionally (never silently).
- i18n: any new user-facing string goes through the shared label maps (en/pt/es).
- Public share URLs/behavior byte-compatible (PGMODE-05 stays true).

### Claude's Discretion
- Specific visual refinements (use the frontend-design/ui-ux skills' sensibilities: contrast, hierarchy, spacing scale) — polished but conservative; this is a professional B2B document.
- Whether small copy improvements ride along (through label maps only).

</decisions>

<canonical_refs>
## Canonical References

- `.planning/research/FEATURES.md` — document design conventions for estimates/invoices
- `lib/estimate/document/` — tokens (the polish lever)
- `components/share/estimate-view.tsx`, `components/share/estimate-document-modern.tsx`, `components/workspace/estimate/estimate-document.tsx`
- `components/pdf/*` — must stay in sync via tokens
- `tests/e2e/visual/share.spec.ts` — visual baselines (note: contains stale payment-UI assertions from removed features — update or quarantine intentionally)

</canonical_refs>

<specifics>
## Specific Ideas

- Owner screenshots (2026-07-27) show current state: solid blue ESTIMATE banner, blue section headers, zebra tables — refine from this baseline.
- Keep "Generated by {appName}" footer behavior (white-label flag) unchanged.

</specifics>

<deferred>
## Deferred Ideas

- Dark-theme share page — out of scope (page forces light).
- Landing/marketing design — unrelated.

</deferred>

---

*Phase: 186-webview-design-polish*
*Context gathered: 2026-07-27 via owner conversation + milestone research*
