---
phase: 186-webview-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/workspace/estimate/estimate-document.tsx
  - components/workspace/estimate/item-card-mobile.tsx
  - components/share/estimate-document-modern.tsx
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "Classic webview item rows show clearly distinguishable alternating shading in every rendering context (desktop + mobile, edit + view)"
    - "The grand total is the single most visually emphasized number on the Classic document — no longer tied at the same size/weight as the client or project name"
    - "Classic's title band and both templates' section headers share one consistent letter-spacing scale instead of two unrelated choices"
    - "No file under components/pdf/ or lib/estimate/pagination/ is modified by this plan; the estimate unit suite stays green with only reviewed, intentional snapshot updates"
  artifacts:
    - path: "components/workspace/estimate/estimate-document.tsx"
      provides: "Classic webview zebra-contrast, grand-total emphasis, and title-band/section-header letter-spacing polish"
    - path: "components/workspace/estimate/item-card-mobile.tsx"
      provides: "Mobile-edit zebra-contrast bump, Phase 162-3c document-native styling otherwise preserved"
    - path: "components/share/estimate-document-modern.tsx"
      provides: "Modern webview section-header letter-spacing consistency (hairline identity untouched)"
  key_links:
    - from: "components/workspace/estimate/estimate-document.tsx (zebra + grand-total classNames)"
      to: "tests/unit/estimate/document-alignment.test.tsx (DOM snapshot)"
      via: "intentional, human-reviewed snapshot update (never a silent `-u` with no diff review)"
      pattern: "bg-muted/40|text-3xl font-extrabold"
---

<objective>
Refine the Classic webview's typographic hierarchy, table zebra-contrast, and grand-total emphasis, and align both templates' title/section-header letter-spacing — the first of two polish plans closing out POLISH-01 (v4.23's final requirement). Webview-only: zero PDF or pagination-formula files touched, so no geometry-lockstep update is required for this plan.

Purpose: the owner's screenshots (2026-07-27) showed a solid-blue ESTIMATE banner, blue section headers, and zebra tables that read correctly but lack refinement — this plan tightens contrast and hierarchy on exactly those surfaces without altering layout structure, PDF geometry, or pagination behavior.
Output: components/workspace/estimate/estimate-document.tsx, components/workspace/estimate/item-card-mobile.tsx, components/share/estimate-document-modern.tsx updated; tests/unit/estimate/document-alignment.test.tsx snapshot intentionally refreshed and reviewed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-webview-design-polish/186-CONTEXT.md
@lib/estimate/document/tokens.ts

<interfaces>
<!-- Exact citations the executor needs — no exploration required. -->

Classic webview zebra call sites (components/workspace/estimate/estimate-document.tsx):
- Line ~293 (SortableDocumentItemRow, desktop EDIT `<tr>`):
  `className="border-b border-border/50 group even:bg-muted/20"`
- Line ~565 (mobile READ-ONLY stacked row):
  `` className={`${SECTION_PX} py-2.5 border-b border-border/50 last:border-b-0 even:bg-muted/20`} ``
- Line ~641 (desktop READ-ONLY `<tr>`):
  `` className={`border-b border-border/50 last:border-0 ${idx % 2 === 1 ? 'bg-muted/20' : ''}`} ``

Mobile-edit zebra (components/workspace/estimate/item-card-mobile.tsx:98):
`className="border-b border-border/50 last:border-b-0 even:bg-muted/20 px-6 sm:px-10 py-2.5 space-y-1.5"`
— Phase 162-3c comment (file lines 18-23, 52-61) explicitly locks this component to "document-native, no glass card wrapper" — do NOT add border/rounded/shadow here, ONLY the zebra opacity value changes.

Modern webview — confirmed NO zebra by design (estimate-document-modern.tsx:274 comment: "Desktop: table with hairline dividers, no zebra striping") — do not add one.

Grand total (components/workspace/estimate/estimate-document.tsx:909-915):
```
<div className="flex justify-between items-baseline pt-3 border-t-2 border-foreground">
  <span className="text-2xl font-bold select-none">{L.grandTotal}</span>
  <span className="text-2xl font-bold tabular-nums" style={{ color: brandText }}>
    {fmt(data.total)}
  </span>
</div>
```
For comparison, the SAME `text-2xl font-bold` weight is already used for client name (line ~1586) and project name (line ~1523) — the total has no more visual priority than a person's name today.

Classic PDF's grand-total row (components/pdf/shared/pdf-totals-block.tsx:109-116) is a SEPARATE, already-emphasized (bordered box) treatment — left untouched this plan. `grandTotalHeightPt` (lib/estimate/pagination/blocks-from-model.ts:234, `8 * 2 + 2 + 4 = 22`) is a fixed padding/border/margin sum with NO font-size term — bumping the PDF's font size would silently under-measure this formula. This plan touches webview font-size only, never the PDF's.

Title band (components/workspace/estimate/estimate-document.tsx:1500-1511) currently `tracking-wide`; the document's OWN labels (PROJECT/BILL TO at 1517/1573, terms labels via TermsBlock:1080) all use `tracking-widest uppercase`. Classic section header title is at line 524 (`font-semibold text-base`, no tracking, on the brand-fill bar). Modern's ESTIMATE label is estimate-document-modern.tsx:174-179 (`tracking-[0.2em]`, already fine); its section header is line 248 (`font-semibold text-base`, no tracking).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classic zebra-row contrast bump (desktop + mobile, edit + view)</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/workspace/estimate/item-card-mobile.tsx</files>
  <action>
    Bump the alternating-row tint from `bg-muted/20` (too subtle to scan quickly, per the owner's "zebra tables" refinement note) to `bg-muted/40` at all 4 cited call sites (3 in estimate-document.tsx + 1 in item-card-mobile.tsx — see `<interfaces>` above for exact current strings). Do not change anything else in either className string — only the opacity suffix (`/20` -> `/40`).

    Do NOT touch components/share/estimate-document-modern.tsx (Modern is deliberately zebra-free, a documented template distinction, not an oversight) and do NOT touch any file under components/pdf/ — Classic PDF's own zebra fill (`components/pdf/estimate-pdf.tsx:272-274`, `tableRowAlt.backgroundColor: '#f9fafb'`) already renders at comparable-or-greater contrast than the webview's PRE-polish 20%-opacity tint when blended against white (verified via manual color-blend comparison during planning — leaving it as-is avoids an unnecessary PDF touch for a surface that isn't actually the weak one).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-alignment.test.tsx -u</automated>
    Review the snapshot diff before proceeding — it must show ONLY `bg-muted/20` -> `bg-muted/40` string changes, nothing structural. Then run:
    <automated>npx vitest run tests/unit/estimate</automated>
    Then grep-verify: `grep -c "bg-muted/40" components/workspace/estimate/estimate-document.tsx` returns `3`, `grep -c "bg-muted/20" components/workspace/estimate/estimate-document.tsx` returns `0`; same pair against item-card-mobile.tsx expecting `1` / `0`.
  </verify>
  <done>All 4 zebra call sites use bg-muted/40; zero remaining bg-muted/20 zebra references in either file; document-alignment snapshot intentionally updated and reviewed; full estimate unit suite green; `git diff --stat` shows only these 2 files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Classic grand-total visual emphasis</name>
  <files>components/workspace/estimate/estimate-document.tsx</files>
  <action>
    In `DocumentTotals` (lines 909-915, see `<interfaces>` above), bump the grand-total row from `text-2xl font-bold` to `text-3xl font-extrabold`, and change the row's divider from `border-t-2 border-foreground` to a brand-colored top rule via inline style (reuse the same `brandText` value already applied to the value span: `style={{ borderTopColor: brandText }}` alongside a plain `border-t-2` class with no `border-foreground`), so the total's separator ties to brand identity rather than a neutral black rule.

    Keep the row's existing single-line inline flex layout (`flex justify-between items-baseline pt-3`) exactly as-is — do not restructure into a stacked/hero layout (that is Modern's distinct, already-shipped treatment; `components/pdf/shared/pdf-totals-block.tsx`'s file-header comment documents this Classic/Modern divergence as deliberate — preserve it, don't collapse the two designs into one look).

    No PDF change and no change to components/share/estimate-document-modern.tsx (Modern's hero total is untouched, already emphasized by design).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-totals-view.test.tsx tests/unit/estimate/document-alignment.test.tsx -u</automated>
    Review the snapshot diff — only grand-total size/weight/border-color classes should change. Then:
    <automated>npx vitest run tests/unit/estimate</automated>
    Grep-verify: `grep -n "text-3xl font-extrabold" components/workspace/estimate/estimate-document.tsx` matches the grand-total value span.
  </verify>
  <done>Grand total renders visibly larger/heavier than surrounding text in both edit and view mode; document-totals-view + alignment tests pass with a reviewed, intentional snapshot update; zero files under components/pdf/ touched by this task.</done>
</task>

<task type="auto">
  <name>Task 3: Title-band and section-header letter-spacing consistency (both templates)</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/share/estimate-document-modern.tsx</files>
  <action>
    Classic: bump the ESTIMATE title band's `tracking-wide` (line ~1506) to `tracking-widest`, matching every other label-role text in the same document (PROJECT/BILL TO, terms labels — see `<interfaces>`). Add `tracking-wide` to the Classic section-header title (line ~524, `font-semibold text-base` on the brand-fill bar) so the document's 2-tier heading hierarchy (title > section title) reads as one coherent type scale.

    Modern: add `tracking-wide` to its section-header title (estimate-document-modern.tsx line ~248, `font-semibold text-base`) for the same 2-tier-hierarchy reason. Its ESTIMATE label (line ~174-179, already `tracking-[0.2em]`) needs no change. Stay inside Modern's existing hairline/serif idiom — tracking only, no color/fill change.

    Spacing audit (document, do not "fix" anything): confirm via `tests/unit/estimate/document-alignment.test.tsx` (DOCUX-05, Phase 162-03) that `SECTION_PX` (`px-6 sm:px-10`) and the `py-6`/`py-6 sm:py-8` vertical rhythm are ALREADY uniformly applied across info-grid/totals/terms/photos. Record this as a explicit "no defect found" note in this plan's SUMMARY rather than inventing an unnecessary spacing change.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-alignment.test.tsx -u</automated>
    Review the diff for tracking-class-only changes. Then:
    <automated>npx vitest run tests/unit/estimate</automated>
    Grep-verify the title band and both section headers picked up the new tracking classes, e.g. `grep -n "tracking-widest" components/workspace/estimate/estimate-document.tsx` includes the ESTIMATE title band line.
  </verify>
  <done>Classic's title band and both templates' section headers share one consistent letter-spacing scale; alignment test snapshot intentionally reviewed/updated; unit suite green; no PDF files touched; SUMMARY documents the spacing audit's "no defect found" result.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/estimate` (full suite) after all 3 tasks — must be green with only the reviewed, intentional `document-alignment.test.tsx` / `document-totals-view.test.tsx` snapshot updates staged. Run `git diff --stat` and confirm ONLY the 3 files_modified above changed — zero files under `components/pdf/` or `lib/estimate/pagination/` appear.
</verification>

<success_criteria>
- Classic zebra rows read with clearly distinguishable alternating shading everywhere (desktop/mobile, edit/view).
- The grand total is visually the most emphasized number on the Classic document.
- Both templates' title bands and section headers share one letter-spacing scale.
- Zero PDF or pagination-formula files touched; estimate unit suite green with reviewed snapshot updates only.
</success_criteria>

<output>
After completion, create `.planning/phases/186-webview-design-polish/186-01-SUMMARY.md`
</output>
