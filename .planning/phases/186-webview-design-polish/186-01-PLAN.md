---
phase: 186-webview-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/workspace/estimate/estimate-document.tsx
  - components/workspace/estimate/item-card-mobile.tsx
  - components/share/estimate-document-modern.tsx
  - tests/unit/estimate/mobile-line-item.test.tsx
  - tests/unit/estimate/__snapshots__/document-alignment.test.tsx.snap
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "Desktop table rows (Classic) show clearly distinguishable alternating shading; zebra is a desktop-table-only pattern going forward — mobile lists drop it entirely (decided per plan-checker Blocker 5, applied here for the workspace-editor mobile-EDIT list; the customer-facing mobile-VIEW list gets its own card treatment in Plan 186-02 Task 2)"
    - "The grand total is the single most visually emphasized number on the Classic document — no longer tied at the same size/weight as the client or project name"
    - "Classic's title band AND both variants of its section-header title (editable input + read-only span) share one consistent letter-spacing scale with Modern's section header"
    - "item-card-mobile.tsx's pre-existing 'no Card wrapper, no rounded-lg, no glass' contract (Phase 162-05/DOCUX-06, tests/unit/estimate/mobile-line-item.test.tsx:39-63) is provably unbroken by this plan's zebra removal"
    - "No file under components/pdf/ or lib/estimate/pagination/ is modified by this plan; the estimate unit suite stays green with only reviewed, intentional snapshot updates"
  artifacts:
    - path: "components/workspace/estimate/estimate-document.tsx"
      provides: "Classic webview desktop-only zebra bump, grand-total emphasis, and title-band/section-header letter-spacing polish"
    - path: "components/workspace/estimate/item-card-mobile.tsx"
      provides: "Mobile-edit zebra removed entirely (cards-win decision), Phase 162-3c/162-05 document-native styling otherwise fully preserved"
    - path: "components/share/estimate-document-modern.tsx"
      provides: "Modern webview section-header letter-spacing consistency (hairline identity untouched)"
    - path: "tests/unit/estimate/mobile-line-item.test.tsx"
      provides: "Row-structure assertion updated to expect zebra ABSENCE; no-Card-wrapper assertions (lines 39-63) verified still green, unmodified"
  key_links:
    - from: "components/workspace/estimate/estimate-document.tsx (desktop zebra + grand-total classNames)"
      to: "tests/unit/estimate/document-alignment.test.tsx (DOM snapshot)"
      via: "intentional, human-reviewed snapshot update (never a silent `-u` with no diff review)"
      pattern: "even:bg-muted/40|text-3xl font-extrabold"
    - from: "components/workspace/estimate/item-card-mobile.tsx (zebra class removed)"
      to: "tests/unit/estimate/mobile-line-item.test.tsx:124-134"
      via: "manually updated title + assertion, reviewed"
      pattern: "not.toMatch(/bg-muted/)"
---

<objective>
Refine the Classic webview's typographic hierarchy, desktop-table zebra-contrast, and grand-total emphasis, align both templates' title/section-header letter-spacing, and remove zebra from the workspace editor's mobile-EDIT item list (a "cards win, mobile drops zebra" decision that applies consistently across every mobile surface — the customer-facing mobile-VIEW list's matching card treatment lands in Plan 186-02 Task 2). First of two polish plans closing out POLISH-01. Webview-only: zero PDF or pagination-formula files touched, so no geometry-lockstep update is required for this plan.

Purpose: the owner's screenshots (2026-07-27) showed a solid-blue ESTIMATE banner, blue section headers, and zebra tables that read correctly but lack refinement — this plan tightens contrast and hierarchy on exactly those surfaces without altering layout structure, PDF geometry, or pagination behavior.
Output: components/workspace/estimate/estimate-document.tsx, components/workspace/estimate/item-card-mobile.tsx, components/share/estimate-document-modern.tsx updated; tests/unit/estimate/mobile-line-item.test.tsx updated; tests/unit/estimate/document-alignment.test.tsx snapshot intentionally refreshed and reviewed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-webview-design-polish/186-CONTEXT.md
@lib/estimate/document/tokens.ts

<interfaces>
<!-- Exact citations the executor needs — no exploration required. -->

Classic webview zebra — DESKTOP call sites ONLY (components/workspace/estimate/estimate-document.tsx). This file also contains 5 UNRELATED `hover:bg-muted/20` occurrences (lines 189, 191, 365, 389, 406, on inputs/selects) — never touch those; every edit/grep below is scoped to the `even:` prefix or the explicit ternary literal, never a bare `bg-muted/20` substring match.
- Line ~293 (SortableDocumentItemRow, desktop EDIT `<tr>`):
  `className="border-b border-border/50 group even:bg-muted/20"` → bump to `even:bg-muted/40`.
- Line ~641 (desktop READ-ONLY `<tr>`):
  `` className={`border-b border-border/50 last:border-0 ${idx % 2 === 1 ? 'bg-muted/20' : ''}`} `` → bump to `'bg-muted/40'`.
- Line ~565 (mobile READ-ONLY stacked row, `` `${SECTION_PX} py-2.5 border-b border-border/50 last:border-b-0 even:bg-muted/20` ``): DO NOT TOUCH in this plan. Its zebra is being fully REPLACED (not bumped) by a card treatment in Plan 186-02 Task 2 — bumping it here first would be redundant churn undone one wave later.

Mobile-EDIT zebra (components/workspace/estimate/item-card-mobile.tsx:98):
`className="border-b border-border/50 last:border-b-0 even:bg-muted/20 px-6 sm:px-10 py-2.5 space-y-1.5"` — REMOVE `even:bg-muted/20` entirely (do not bump to /40 — see Task 1's rationale: mobile drops zebra everywhere, desktop keeps it). Do NOT add any new border/rounded/shadow here: `tests/unit/estimate/mobile-line-item.test.tsx:49-63` (Phase 162-05/DOCUX-06) asserts NO `rounded-lg` and NO `class*="glass"` on this component's outer div — those tests are untouched by this plan and MUST still pass. This file also has 2 unrelated `hover:bg-muted/20` occurrences (lines 25, 130) — do not touch those.

`tests/unit/estimate/mobile-line-item.test.tsx` — the ONE affected test is at lines 124-134 (`'row structure — border-b border-border/50 last:border-b-0 even:bg-muted/20'`, asserting `outer.className` matches `/even:bg-muted\/20/` at line 133). The OTHER tests in this file (lines 39-63 "no glass card"/"no Card wrapper", lines 65-122 transparent-inputs/touch-targets, lines 136-161 prop-signature) are NOT touched.

Modern webview — confirmed NO zebra by design (estimate-document-modern.tsx:274 comment: "Desktop: table with hairline dividers, no zebra striping") — do not add one.

Grand total (components/workspace/estimate/estimate-document.tsx:909-915):
```
<div className="flex justify-between items-baseline pt-3 border-t-2 border-foreground">
  <span className="text-2xl font-bold select-none">{L.grandTotal}</span>
  <span className="text-2xl font-bold tabular-nums" style={{ color: brandText }}>
    {fmt(data.total)}
  </span>
</div>
```
For comparison, the SAME `text-2xl font-bold` weight is already used for client name (line ~1586) and project name (line ~1523) — the total has no more visual priority than a person's name today.

Classic PDF's grand-total row (components/pdf/shared/pdf-totals-block.tsx:109-116) is a SEPARATE, already-emphasized (bordered box) treatment — left untouched this plan. `grandTotalHeightPt` (lib/estimate/pagination/blocks-from-model.ts:234, `8 * 2 + 2 + 4 = 22`) is a fixed padding/border/margin sum with NO font-size term — bumping the PDF's font size would silently under-measure this formula. This plan touches webview font-size only, never the PDF's.

Title band (components/workspace/estimate/estimate-document.tsx:1500-1511) currently `tracking-wide`; the document's OWN labels (PROJECT/BILL TO at 1517/1573, terms labels via TermsBlock:1080) all use `tracking-widest uppercase`.

Classic section-header title has TWO variants that BOTH render depending on mode — both need the tracking class:
- Line ~521 (EDITABLE `<input>`): `className="flex-1 bg-transparent font-semibold text-base focus:outline-none placeholder:text-white/50 focus:bg-white/10 rounded px-1 min-w-0"`
- Line ~524 (READ-ONLY `<span>`): `className="flex-1 font-semibold text-base select-none"`

Modern's ESTIMATE label is estimate-document-modern.tsx:174-179 (`tracking-[0.2em]`, already fine); its section header is line 248 (`font-semibold text-base`, no tracking).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zebra-row contrast — desktop bump, mobile-EDIT-list zebra removal</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/workspace/estimate/item-card-mobile.tsx, tests/unit/estimate/mobile-line-item.test.tsx</files>
  <action>
    Design decision (plan-checker Blocker 5, DECIDED): zebra stays a DESKTOP-table-only pattern; every mobile list (workspace-editor mobile-EDIT here, customer-facing mobile-VIEW in Plan 186-02 Task 2) drops the stripe-zebra entirely in favor of a card-like separation instead. Record this decision in this plan's SUMMARY.

    1. In `components/workspace/estimate/estimate-document.tsx`, bump ONLY the 2 DESKTOP zebra call sites (see `<interfaces>` for exact current strings):
       - Line ~293 (desktop EDIT `<tr>`): `even:bg-muted/20` → `even:bg-muted/40`.
       - Line ~641 (desktop READ-ONLY `<tr>`, ternary): `'bg-muted/20'` → `'bg-muted/40'`.
       Do NOT touch line ~565 (mobile READ-ONLY row) — out of scope for this task, handled by Plan 186-02 Task 2's card treatment. Do NOT touch any `hover:bg-muted/20` occurrence in this file.

    2. In `components/workspace/estimate/item-card-mobile.tsx` line 98, REMOVE `even:bg-muted/20` entirely (no replacement class added) — the mobile-EDIT list keeps its existing `border-b border-border/50 last:border-b-0` divider, just loses the zebra tint. Do NOT add `rounded-lg`, `shadow`, or any glass/Card styling — `tests/unit/estimate/mobile-line-item.test.tsx:49-63` locks this component to "no Card wrapper, no rounded-lg, no glass" (Phase 162-05/DOCUX-06) and this task must not break it. Do NOT touch the 2 unrelated `hover:bg-muted/20` occurrences (lines 25, 130).

    3. In `tests/unit/estimate/mobile-line-item.test.tsx`, update ONLY the test at lines 124-134:
       - Retitle from `'row structure — border-b border-border/50 last:border-b-0 even:bg-muted/20'` to `'row structure — border-b border-border/50 last:border-b-0, no zebra (Phase 186: mobile rows drop the stripe-zebra pattern; only the desktop table keeps it)'`.
       - Replace the assertion `expect(outer.className).toMatch(/even:bg-muted\/20/)` (line 133) with `expect(outer.className).not.toMatch(/bg-muted/)`.
       - Keep the other 3 assertions in this same test (border-b, border-border/50, last:border-b-0 presence) unchanged.
       - Do NOT touch the "no glass card"/"no Card wrapper" tests at lines 39-63, or any other test in this file.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/mobile-line-item.test.tsx tests/unit/estimate/document-alignment.test.tsx -u</automated>
    Review the document-alignment snapshot diff — it must show ONLY the 2 desktop `bg-muted/20`→`bg-muted/40` changes in estimate-document.tsx; line 565's mobile row must appear UNCHANGED in the diff. Then:
    <automated>npx vitest run tests/unit/estimate</automated>
    Grep-verify, precisely scoped (never a bare `bg-muted/20` substring match — that also matches unrelated `hover:` states):
    <automated>grep -c "even:bg-muted/40" components/workspace/estimate/estimate-document.tsx</automated> → `1`
    <automated>grep -c "bg-muted/40" components/workspace/estimate/estimate-document.tsx</automated> → `2` (line 293's even-prefixed form + line 641's ternary)
    <automated>grep -c "even:bg-muted/20" components/workspace/estimate/estimate-document.tsx</automated> → `1` (line 565 — intentionally untouched this task)
    <automated>grep -c "hover:bg-muted/20" components/workspace/estimate/estimate-document.tsx</automated> → `5` (unchanged)
    <automated>grep -c "even:bg-muted" components/workspace/estimate/item-card-mobile.tsx</automated> → `0`
    <automated>grep -c "hover:bg-muted/20" components/workspace/estimate/item-card-mobile.tsx</automated> → `2` (unchanged)
  </verify>
  <done>Desktop zebra bumped to bg-muted/40 at exactly 2 sites in estimate-document.tsx; mobile row (line 565) left untouched, deferred to 186-02; item-card-mobile.tsx's zebra is fully removed with no replacement decoration; mobile-line-item.test.tsx's row-structure test asserts zebra absence while its no-Card-wrapper tests (lines 39-63) still pass unmodified; all unrelated hover:bg-muted/20 occurrences (5 + 2) are untouched; document-alignment snapshot intentionally updated and reviewed.</done>
</task>

<task type="auto">
  <name>Task 2: Classic grand-total visual emphasis</name>
  <files>components/workspace/estimate/estimate-document.tsx, tests/unit/estimate/__snapshots__/document-alignment.test.tsx.snap</files>
  <action>
    In `DocumentTotals` (lines 909-915, see `<interfaces>` above), bump the grand-total row from `text-2xl font-bold` to `text-3xl font-extrabold`, and change the row's divider from `border-t-2 border-foreground` to a brand-colored top rule via inline style (reuse the same `brandText` value already applied to the value span: `style={{ borderTopColor: brandText }}` alongside a plain `border-t-2` class with no `border-foreground`), so the total's separator ties to brand identity rather than a neutral black rule.

    Keep the row's existing single-line inline flex layout (`flex justify-between items-baseline pt-3`) exactly as-is — do not restructure into a stacked/hero layout (that is Modern's distinct, already-shipped treatment; `components/pdf/shared/pdf-totals-block.tsx`'s file-header comment documents this Classic/Modern divergence as deliberate — preserve it, don't collapse the two designs into one look).

    No PDF change and no change to components/share/estimate-document-modern.tsx (Modern's hero total is untouched, already emphasized by design).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-totals-view.test.tsx tests/unit/estimate/document-alignment.test.tsx -u</automated>
    Review the snapshot diff — only grand-total size/weight/border-color classes should change. Then:
    <automated>npx vitest run tests/unit/estimate</automated>
    Grep-verify: `grep -n "text-3xl font-extrabold" components/workspace/estimate/estimate-document.tsx` matches the grand-total value span.
  </verify>
  <done>Grand total renders visibly larger/heavier than surrounding text in both edit and view mode; document-totals-view + alignment tests pass with a reviewed, intentional snapshot update; zero files under components/pdf/ touched by this task.</done>
</task>

<task type="auto">
  <name>Task 3: Title-band and section-header letter-spacing consistency (both templates)</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/share/estimate-document-modern.tsx, tests/unit/estimate/__snapshots__/document-alignment.test.tsx.snap</files>
  <action>
    Classic: bump the ESTIMATE title band's `tracking-wide` (line ~1506) to `tracking-widest`, matching every other label-role text in the same document (PROJECT/BILL TO, terms labels — see `<interfaces>`). Add `tracking-wide` to BOTH variants of the Classic section-header title — the editable `<input>` at line ~521 AND the read-only `<span>` at line ~524 (see `<interfaces>` for exact current classNames) — both render the same section title depending on edit/view mode, so both need the class for the tier to apply consistently regardless of mode.

    Modern: add `tracking-wide` to its section-header title (estimate-document-modern.tsx line ~248, `font-semibold text-base`) for the same 2-tier-hierarchy reason. Its ESTIMATE label (line ~174-179, already `tracking-[0.2em]`) needs no change. Stay inside Modern's existing hairline/serif idiom — tracking only, no color/fill change.

    Spacing audit (document, do not "fix" anything): confirm via `tests/unit/estimate/document-alignment.test.tsx` (DOCUX-05, Phase 162-03) that `SECTION_PX` (`px-6 sm:px-10`) and the `py-6`/`py-6 sm:py-8` vertical rhythm are ALREADY uniformly applied across info-grid/totals/terms/photos. Record this as an explicit "no defect found" note in this plan's SUMMARY rather than inventing an unnecessary spacing change.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-alignment.test.tsx -u</automated>
    Review the diff for tracking-class-only changes. Then:
    <automated>npx vitest run tests/unit/estimate</automated>
    Grep-verify BOTH section-header variants picked up the class: `grep -n "tracking-wide" components/workspace/estimate/estimate-document.tsx` output must include a line near 521 (the input) AND a line near 524 (the span), plus the title-band line (now `tracking-widest`, which also matches a bare `tracking-wide` grep since `tracking-widest` contains that substring — confirm by eye by reading the 3 matched lines, not just the count).
  </verify>
  <done>Classic's title band, BOTH section-header variants (input + span), and Modern's section header share one consistent letter-spacing scale; alignment test snapshot intentionally reviewed/updated; unit suite green; no PDF files touched; SUMMARY documents the spacing audit's "no defect found" result.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/estimate` (full suite) after all 3 tasks — must be green with only the reviewed, intentional `document-alignment.test.tsx` snapshot updates staged and the deliberate `mobile-line-item.test.tsx` edit. Run `git diff --stat` and confirm ONLY the 5 files_modified above changed — zero files under `components/pdf/` or `lib/estimate/pagination/` appear.
</verification>

<success_criteria>
- Desktop zebra rows read with clearly distinguishable alternating shading; zebra is desktop-only going forward (mobile-EDIT list drops it here, mobile-VIEW list drops it in Plan 186-02).
- item-card-mobile.tsx's "no Card wrapper / no rounded-lg / no glass" contract remains intact and tested.
- The grand total is visually the most emphasized number on the Classic document.
- Classic's title band and BOTH section-header variants, plus Modern's section header, share one letter-spacing scale.
- Zero PDF or pagination-formula files touched; estimate unit suite green with reviewed snapshot updates only.
</success_criteria>

<output>
After completion, create `.planning/phases/186-webview-design-polish/186-01-SUMMARY.md`. Record explicitly: (1) the "zebra stays desktop-only, mobile drops it" design decision and where each mobile surface's treatment lands (this plan for mobile-EDIT removal, Plan 186-02 Task 2 for mobile-VIEW card treatment); (2) the spacing-audit "no defect found" result from Task 3.
</output>
