---
phase: 186-webview-design-polish
plan: 02
type: execute
wave: 2
depends_on: ["186-01"]
files_modified:
  - components/workspace/estimate/estimate-document.tsx
  - components/share/estimate-document-modern.tsx
  - components/pdf/shared/pdf-terms-section.tsx
  - components/pdf/shared/pdf-signature-block.tsx
  - components/pdf/estimate-pdf.tsx
  - lib/estimate/document/tokens.ts
  - tests/unit/estimate/pagination-tokens.test.ts
  - tests/unit/estimate/__snapshots__/document-alignment.test.tsx.snap
  - tests/e2e/visual/share.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "Terms entries (Estimate Terms/Payment/Timeline/Warranty/Notes) render with a matching subtle tint/accent behind the terms/signature region in both webview templates, not as plain paragraphs"
    - "The Classic PDF's terms cards and signature block carry a matching subtle brand-tint background, sourced from ONE shared lib/estimate/document/tokens.ts helper (cardTintFill) — never two independently hand-typed hex literals"
    - "cardTintFill degrades safely: a malformed/non-hex brandColor returns undefined, so cardFill/backgroundColor is omitted and both the webview and PDF render exactly today's plain output — proven by a dedicated unit test"
    - "Modern's PDF and webview terms/signature stay fill-free (hairline-only), preserving its documented template identity — no new fill is added there"
    - "The customer-facing (view-mode) photo grid and mobile item list read as polished cards (zebra dropped there, per the 186-01 decision); the workspace editor's ItemCardMobile mobile-EDIT list is untouched by THIS plan (its zebra removal already happened in 186-01)"
    - "lib/estimate/pagination/blocks-from-model.ts is provably untouched (empty git diff) despite the PDF StyleSheet edits in this plan — proving the change was truly geometry-safe"
    - "The two stale ?stripe=success/canceled visual-baseline tests are removed with a documented reason, never silently, and the file's own coverage-matrix docblock count is corrected"
    - "POLISH-01 is marked complete in .planning/REQUIREMENTS.md with the honest rationale (webview-only polish + one geometry-safe PDF token; PDF font/box propagation deferred) recorded in both the traceability row and the SUMMARY"
  artifacts:
    - path: "lib/estimate/document/tokens.ts"
      provides: "CARD_TINT_ALPHA_HEX + cardTintFill(brandColor) — the one shared, guarded brand-tint source for terms/signature backgrounds, consumed by both the webview and the Classic PDF"
    - path: "components/pdf/shared/pdf-terms-section.tsx"
      provides: "optional cardFill prop on PdfTermsCard (the LIVE per-card renderer) — background-color-only, zero geometry impact"
    - path: "components/pdf/shared/pdf-signature-block.tsx"
      provides: "optional cardFill prop on PdfSignatureBlock — background-color-only, zero geometry impact"
    - path: "components/pdf/estimate-pdf.tsx"
      provides: "Classic-only cardFill wiring at the 2 REAL call sites (PdfTermsCard ~578, PdfSignatureBlock ~591) — PdfTermsSection is dead code, not touched"
    - path: "tests/unit/estimate/pagination-tokens.test.ts"
      provides: "unit coverage for cardTintFill, including the malformed-brand-color fallback case"
    - path: "tests/e2e/visual/share.spec.ts"
      provides: "stale payment-banner baselines removed, with a documented reason; docblock count corrected to 10"
    - path: ".planning/REQUIREMENTS.md"
      provides: "POLISH-01 closed out — checkbox, honest-rationale traceability row, coverage footer"
  key_links:
    - from: "lib/estimate/document/tokens.ts (cardTintFill)"
      to: "components/pdf/estimate-pdf.tsx (lines ~578, ~591) + components/workspace/estimate/estimate-document.tsx"
      via: "shared import, not a duplicated literal or duplicated regex guard"
      pattern: "cardTintFill"
    - from: "components/pdf/shared/pdf-terms-section.tsx (cardFill prop on PdfTermsCard)"
      to: "lib/estimate/pagination/blocks-from-model.ts (termsCardBaseHeightPt formula)"
      via: "verified NO reference exists — background-color-only change requires no formula update"
      pattern: "cardFill"
---

<objective>
Give terms entries and the signature block a matching subtle tint/accent behind their region across both webview templates, give the customer-facing photo grid and mobile item list a polished card treatment, propagate the tint to the Classic PDF's terms/signature blocks through ONE new shared, guarded token helper (zero geometry impact), and close out the milestone: intentionally handle the two stale payment-banner visual baselines and mark POLISH-01 complete with an honest rationale.

Purpose: closes the milestone's last requirement. Terms/signature currently render as bare, unstyled text — this plan gives them a visible boundary, and (per ROADMAP Phase 186 success criterion #2) routes the one genuinely cross-surface value — the brand-tint background — through a single tokens.ts helper rather than duplicating a hex literal (and its validity guard) in the webview and the PDF independently.

Note on scope (Warning 8): the webview's terms entries are separated by `space-y-4` gaps on their parent container, while the PDF's terms cards abut with only a first-card top-margin bonus (a pre-existing structural difference between the two renderers, unrelated to and unchanged by this plan) — so "matching tint" means the SAME background color/accent appears behind each region, not that the two renderers produce identical card boundaries. Record this divergence in the SUMMARY.

Output: components/workspace/estimate/estimate-document.tsx, components/share/estimate-document-modern.tsx, components/pdf/shared/pdf-terms-section.tsx, components/pdf/shared/pdf-signature-block.tsx, components/pdf/estimate-pdf.tsx, lib/estimate/document/tokens.ts updated; tests/unit/estimate/pagination-tokens.test.ts gains cardTintFill coverage; tests/e2e/visual/share.spec.ts's stale tests removed; .planning/REQUIREMENTS.md POLISH-01 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-webview-design-polish/186-CONTEXT.md
@.planning/phases/186-webview-design-polish/186-01-SUMMARY.md
@lib/estimate/document/tokens.ts
@lib/estimate/pagination/blocks-from-model.ts

<interfaces>
<!-- Exact citations the executor needs — no exploration required. -->

Classic webview terms — 5 wrapper divs, GENUINELY BARE (no className at all today), direct styling is safe (components/workspace/estimate/estimate-document.tsx):
- Line ~1731 `data-page-block-id="terms-estimate"`
- Line ~1742 `data-page-block-id="terms-payment"`
- Line ~1757 `data-page-block-id="terms-timeline"`
- Line ~1771 `data-page-block-id="terms-warranty"`
- Line ~1786 `data-page-block-id="terms-notes"`
The existing hex-alpha convention already used in this same file (line ~1689, Add-section button hover) appends `1A` directly to brandColor — do NOT copy that literal; use the new `cardTintFill(brandColor)` helper instead (see below) so the alpha suffix + validity guard live in exactly one place.

Classic webview SIGNATURE (line ~1805) — plan-checker Blocker 4: this wrapper ALREADY carries section padding + border-t + its `data-page-block-id="signature"` pagination anchor:
```
<div data-page-block-id="signature" className="px-6 sm:px-10 py-6 border-t border-border/50">
  <p className="text-xs font-semibold uppercase tracking-widest text-muted-foreground mb-3 select-none">
    {L.signedBy}
  </p>
  <div className="flex items-start gap-4">
    <img src={data.signature.signatureDataUrl} alt={L.signedBy} className="h-16 w-auto max-w-[240px] object-contain" />
    <div>
      <p className="text-base font-semibold">{data.signature.signerName}</p>
      <p className="text-sm text-muted-foreground">{formatDate(data.signature.signedAt, lang)}</p>
    </div>
  </div>
</div>
```
DO NOT touch this outer wrapper (its className/data-page-block-id is a pagination anchor). Instead, insert ONE new inner div as its single child, wrapping the label paragraph AND the flex row (both move one level deeper, otherwise unchanged) — the inner div gets `rounded-lg border border-border/50 p-4` classes plus an inline `backgroundColor: cardTintFill(brandColor)` style.

Modern webview terms — 4 bare divs (components/share/estimate-document-modern.tsx), inside the `hasTerms &&` container (line 376, untouched):
- Line ~378 (payment), ~388 (timeline), ~399 (warranty), ~409 (notes) — each a bare div wrapping a label paragraph + text paragraph.
Modern's documented identity (`lib/estimate/document/tokens.ts` `solidHeaderFill: false` for modern) is hairline/accent-only, NEVER a solid fill — apply `border-l-2 pl-4` classes plus an inline `borderLeftColor: brandColor` style DIRECTLY to these 4 bare divs (no inner-div indirection needed — they're already bare, unlike the signature block below).

Modern webview SIGNATURE (line ~424) — same Blocker-4 situation as Classic: this wrapper already carries a className with section padding + border-t. DO NOT touch it; insert ONE new inner div as its single child, wrapping the label + flex row, with `border-l-2 pl-4` classes plus an inline `borderLeftColor: brandColor` style (same inner-div pattern as Classic, but left-accent not fill).

PDF — plan-checker Blocker 1 (CRITICAL correction from an earlier draft): `PdfTermsSection` (`components/pdf/shared/pdf-terms-section.tsx`'s wrapper function) is DEAD CODE — confirmed via grep, neither `estimate-pdf.tsx` nor `estimate-pdf-modern.tsx` calls it. Both templates call `PdfTermsCard` and `PdfSignatureBlock` DIRECTLY, per-block, driven by the pagination engine's block list (Phase 184 Plan 05's per-block restructure). Do NOT wire anything through `PdfTermsSection` — it renders nothing today and wiring it would be dead work.

The REAL, LIVE call sites in `components/pdf/estimate-pdf.tsx`:
- Line 32: `import { ESTIMATE_DESIGN_TOKENS, LINE_HEIGHT, ESTIMATE_PAGE_GEOMETRY } from '@/lib/estimate/document/tokens'` — add `cardTintFill` to this import.
- Line 414: `const brandColor = company.brand_primary_color ?? SYSTEM_COLORS.primary` — already in scope for both call sites below.
- Lines 578-584, inside `case 'terms-card':` — the `PdfTermsCard({...})` object literal (props: title, text, titleColor, topMarginPt, styles) — add a `cardFill: cardTintFill(brandColor)` property to it.
- Lines 591-596, inside `case 'signature':` — the `PdfSignatureBlock({...})` object literal (props: signature, L, fmtDate, styles) — add a `cardFill: cardTintFill(brandColor)` property to it.

`components/pdf/estimate-pdf-modern.tsx` has the equivalent LIVE call sites at line ~596 (`PdfTermsCard`) and ~609 (`PdfSignatureBlock`) — this plan does NOT edit that file; its calls simply never receive `cardFill`, so Modern's PDF terms/signature stay fill-free by omission.

`components/pdf/shared/pdf-terms-section.tsx` — `PdfTermsCard` (the LIVE per-card function, lines 62-75) currently renders its root View with `style={topMarginPt ? { marginTop: topMarginPt } : undefined}`.
`components/pdf/shared/pdf-signature-block.tsx` — `PdfSignatureBlock` (lines 34-48) currently renders its root View with `style={{ marginTop: 16 }}`.

Height-formula citations to confirm are NEVER touched (lib/estimate/pagination/blocks-from-model.ts):
- `termsCardBaseHeightPt` (lines 203-204): sum of font size + spacing contribution + text margin-bottom — NOT backgroundColor.
- `signatureBaseHeightPt` (lines 206-207): sum of margin-top + font size + image height + 2x line font size — same, no color term.
This confirms a `backgroundColor`-only prop addition needs zero formula changes — verify this remains true by re-running `tests/unit/pagination/blocks-from-model.test.ts` unmodified at the end of Task 1.

`tests/unit/estimate/pagination-tokens.test.ts` — existing style to match (grouped `describe` blocks, direct `expect(...).toBe(...)` assertions; see its `photosPerRow`/`visibleSectionItems` blocks for the convention).

Photo tiles:
- Classic `AttachedPhotoThumb` (estimate-document.tsx:1290-1292): `className="aspect-square overflow-hidden rounded-lg relative group"`.
- Modern's inline tile (estimate-document-modern.tsx:452): `className="aspect-square overflow-hidden rounded-lg relative"`. Modern's existing inline hex border convention: `borderColor: '#e4e4e7'` (line 131).
- PDF photo grid (`components/pdf/shared/pdf-photo-grid.tsx`) — DO NOT TOUCH. Its fixed 150pt tile width feeds `photosPerRow()` (`lib/estimate/document/tokens.ts:113-117`) directly; any width-affecting change (even a border) risks shifting per-row photo count.

Mobile stacked list (customer-facing / VIEW-mode ONLY — plan-checker Blocker 5, DECIDED: "cards win", REPLACE the divider classes, don't add alongside):
- Classic read-only mobile row (estimate-document.tsx line ~565) — current: SECTION_PX + `py-2.5 border-b border-border/50 last:border-b-0 even:bg-muted/20`. Final (exact): SECTION_PX + `py-2.5 mx-4 my-1.5 rounded-lg border border-border/40` — drops `border-b`/`last:border-b-0`/`even:bg-muted/20` entirely (no zebra, no bottom-divider). `SECTION_PX` (`px-6 sm:px-10`) is preserved, satisfying `tests/unit/estimate/document-alignment.test.tsx:139-147`'s existing px-6/sm:px-10 presence assertion unmodified.
- Modern mobile row (estimate-document-modern.tsx line ~255) — current: `className="px-8 py-3 border-b border-border/50 last:border-b-0"`. Final (exact): `className="px-8 py-3 mx-6 my-1.5 rounded-lg border border-[#e4e4e7]"` (Modern's own existing neutral-hex border convention, not the Tailwind `border-border` token).
- Do NOT touch `components/workspace/estimate/item-card-mobile.tsx` (the workspace editor's mobile EDIT list) — its zebra removal already happened in Plan 186-01 Task 1; it needs no further change here.

Stale visual baselines (tests/e2e/visual/share.spec.ts):
- Docblock (lines 4-17): "Coverage matrix" currently lists 9 baselines + 1 brand-override smoke + 1 success banner + 1 canceled banner = 12 baselines total (lines 11-16). After removing the 2 banner tests, correct this to 9 baselines + 1 brand-override smoke = 10 baselines total — delete the 2 "+ 1 ... banner branch" bullet lines and change `12` to `10`.
- Test bodies (lines 48-72): two tests screenshot `/estimate/${seedToken}?stripe=success` and `?stripe=canceled`. Confirmed during planning: `app/estimate/[token]/page.tsx` and `app/estimate/[token]/actions.ts` never read a `stripe` search param (grep returns zero matches) — these are pure no-ops today, superseded by the Phase 94 issued-invoice pay-link UI already rendered unconditionally in `components/share/estimate-view.tsx:352-405`.

REQUIREMENTS.md citations: POLISH-01 checkbox at line 53, traceability row at line 94, coverage footer at lines 96-99/102-103.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terms/signature tint (webview both templates) + Classic PDF propagation via shared, guarded token</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/share/estimate-document-modern.tsx, components/pdf/shared/pdf-terms-section.tsx, components/pdf/shared/pdf-signature-block.tsx, components/pdf/estimate-pdf.tsx, lib/estimate/document/tokens.ts, tests/unit/estimate/pagination-tokens.test.ts</files>
  <action>
    1. In `lib/estimate/document/tokens.ts`, add (per the file's own "plain string/numeric values ONLY" rule) a shared, guarded helper: a `CARD_TINT_ALPHA_HEX = '0D'` string constant (~5% opacity), plus `export function cardTintFill(brandColor: string): string | undefined` that returns `` `${brandColor}${CARD_TINT_ALPHA_HEX}` `` ONLY when `brandColor` matches `/^#[0-9a-fA-F]{6}$/`, otherwise returns `undefined`. Document that this is the ONE place both the webview and the Classic PDF derive this color from (ROADMAP Phase 186 success criterion #2 — never two independently hand-typed hex-alpha strings or validity guards), and that `undefined` means both call sites render exactly today's plain (untinted) output rather than a garbage color value from malformed tenant data.

    2. Classic webview (`components/workspace/estimate/estimate-document.tsx`): import `cardTintFill` (it already imports `LETTER_HEIGHT_PX` from the same module, line 56 — add to that import). Apply `rounded-lg border border-border/50 p-4` classes plus an inline `backgroundColor: cardTintFill(brandColor)` style DIRECTLY to each of the 5 bare terms wrapper divs cited in `<interfaces>` (they carry no className today, so nothing is lost). For the SIGNATURE wrapper (line ~1805), do NOT touch its own className/data-page-block-id — insert one new inner div instead, exactly as specified in `<interfaces>`.

    3. Modern webview (`components/share/estimate-document-modern.tsx`): apply `border-l-2 pl-4` classes plus an inline `borderLeftColor: brandColor` style DIRECTLY to the 4 bare terms divs cited in `<interfaces>`. For the SIGNATURE wrapper (line ~424), do NOT touch its own className — insert one new inner div instead, exactly as specified in `<interfaces>`.

    4. PDF shared components: add an optional `cardFill?: string` prop to `PdfTermsCardProps` (pdf-terms-section.tsx) and `PdfSignatureBlockProps` (pdf-signature-block.tsx). When present, merge a `backgroundColor: cardFill` entry into that component's own root View's style (as an additional array entry alongside its existing conditional style, not replacing it). CRITICAL constraint — this prop must ONLY ever set `backgroundColor` (optionally `borderRadius`); NEVER add/modify any padding, margin, border-width, or font-size property here — the height-formula citations in `<interfaces>` confirm neither `termsCardBaseHeightPt` nor `signatureBaseHeightPt` references color, so this needs NO formula update, but would silently drift the moment a padding/margin/border-width/font-size property was touched instead.

    5. Wire `cardFill: cardTintFill(brandColor)` at the 2 REAL, LIVE call sites in `components/pdf/estimate-pdf.tsx` — the `PdfTermsCard` object literal inside `case 'terms-card':` (~line 578) and the `PdfSignatureBlock` object literal inside `case 'signature':` (~line 591) — see `<interfaces>` for exact context. Import `cardTintFill` alongside the existing tokens import at line 32. Do NOT touch `PdfTermsSection` (dead code, uncalled) and do NOT edit `components/pdf/estimate-pdf-modern.tsx` at all — its equivalent call sites simply never receive `cardFill`, keeping Modern's PDF terms/signature fill-free.

    6. In `tests/unit/estimate/pagination-tokens.test.ts`, add a new `describe('cardTintFill (Phase 186, POLISH-01)', ...)` block matching this file's existing style, with 2 cases: (a) a valid 6-digit hex brandColor (e.g. `'#2563eb'`) returns `` `${brandColor}${CARD_TINT_ALPHA_HEX}` ``; (b) a malformed/non-hex brandColor (e.g. `'not-a-color'`) returns `undefined`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/pagination-tokens.test.ts tests/unit/estimate/document-prepared-by-terms.test.tsx tests/unit/estimate/document-signature-view.test.tsx tests/unit/estimate/document-alignment.test.tsx tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx tests/unit/pdf/estimate-pdf-signature.test.tsx tests/unit/pdf/estimate-pdf-modern-signature.test.tsx -u</automated>
    Review any snapshot diffs — they must show ONLY className/style additions, never a structural or numeric-height change. Then, as the mandatory lockstep sanity check for this plan's one PDF StyleSheet touch:
    <automated>npx vitest run tests/unit/pdf tests/unit/pagination</automated>
    This must pass with ZERO changes needed to `tests/unit/pagination/blocks-from-model.test.ts` — if that test needs updating, the change was not actually geometry-safe; stop and re-scope to backgroundColor/borderRadius only.
  </verify>
  <done>All 5 Classic terms wrappers show a visible border + brand tint; the Classic signature wrapper's new inner div shows the same treatment, with its outer pagination-anchor div byte-unchanged; Modern's 4 terms divs + signature inner div show a left-accent border (no fill); Classic PDF terms cards + signature block render with a matching tint via cardFill sourced from cardTintFill(); Modern PDF is visually unchanged; a malformed-brandColor unit case proves the safe undefined fallback; `git diff -- lib/estimate/pagination/blocks-from-model.ts` is empty; tests/unit/pdf and tests/unit/pagination pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Photo grid frame + customer-facing mobile stacked-item-list card treatment (webview only, both templates)</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/share/estimate-document-modern.tsx, tests/unit/estimate/__snapshots__/document-alignment.test.tsx.snap</files>
  <action>
    Photo grid: add `ring-1 ring-border/50` to Classic's `AttachedPhotoThumb` tile (estimate-document.tsx:1290-1292) and `border border-[#e4e4e7]` to Modern's inline tile (estimate-document-modern.tsx:452, matching its existing inline hex convention at line 131) — frame-only, no width/height/aspect-ratio change. Leave `components/pdf/shared/pdf-photo-grid.tsx` completely untouched (see `<interfaces>` — its fixed 150pt tile width feeds photosPerRow() row-chunking directly; any width-affecting change is a real geometry hazard this plan avoids by scoping photo polish to the webview only).

    Mobile stacked list (customer-facing VIEW mode ONLY — never `item-card-mobile.tsx`, already handled in Plan 186-01): CARDS WIN, REPLACE the divider/zebra classes with the exact final classNames specified in `<interfaces>` — Classic's read-only mobile row (estimate-document.tsx line ~565) becomes SECTION_PX + `py-2.5 mx-4 my-1.5 rounded-lg border border-border/40` (no zebra, no border-b divider); Modern's mobile row (estimate-document-modern.tsx line ~255) becomes `className="px-8 py-3 mx-6 my-1.5 rounded-lg border border-[#e4e4e7]"`. Do not ADD these alongside the old classes — replace the divider/zebra classes with them.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-alignment.test.tsx -u</automated>
    Review the diff — the mobile-row assertion at lines 139-147 checks for `px-6`/`sm:px-10` presence (still true via SECTION_PX) and `px-3` absence on the first mobile row div; confirm this pre-existing assertion still passes with the new card classes in place of the old divider/zebra ones.
    <automated>npx vitest run tests/unit/estimate/document-photo-captions-view.test.tsx tests/unit/estimate</automated>
    Grep-verify zebra is now fully gone from the mobile surfaces: <automated>grep -c "even:bg-muted" components/workspace/estimate/estimate-document.tsx</automated> → `1` (only line 293, the desktop row bumped in Plan 186-01 — confirms line 565's zebra is gone here).
  </verify>
  <done>Photo tiles show a visible frame in both templates; the customer-facing mobile item list reads as separated cards with zero zebra; item-card-mobile.tsx is untouched by this task; `git diff --stat` confirms zero PDF files touched; estimate unit suite green; document-alignment's mobile-row assertion (lines 139-147) still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Milestone close-out — stale visual-baseline handling + REQUIREMENTS.md</name>
  <files>tests/e2e/visual/share.spec.ts, .planning/REQUIREMENTS.md</files>
  <action>
    In `tests/e2e/visual/share.spec.ts:48-72`, DELETE the two test blocks `share — payment success banner (?stripe=success)` and `share — payment canceled banner (?stripe=canceled)` (confirmed dead per `<interfaces>` — the share page never reads a `stripe` query param). Replace them with a single comment recording: what was removed, why (feature superseded by the Phase 94 issued-invoice pay-link UI; the query params are no-ops; the tests were redundant with the 9-baseline viewport×lang matrix above them), and that this was an intentional Phase 186 removal, not silent. Do NOT touch the "tenant brand override cascade" test below them — it exercises real behavior. Also update the file's own docblock (lines 11-16, see `<interfaces>`): delete the 2 banner-branch bullet lines and correct the total from `12 baselines total` to `10 baselines total`.

    In `.planning/REQUIREMENTS.md`: flip POLISH-01's checkbox from `- [ ]` to `- [x]` (line 53); update its Traceability row (line 94) to read exactly: `Complete (Phase 186) — webview-only polish + one geometry-safe PDF token; PDF font/box propagation deferred — requires lockstep blocks-from-model formula updates.`; update the Coverage summary footer (lines 96-99, 102-103) to reflect all 18 v1 requirements mapped and complete.
  </action>
  <verify>
    <automated>grep -c "stripe=success\|stripe=canceled" tests/e2e/visual/share.spec.ts</automated>
    Expect `0`.
    <automated>npx playwright test tests/e2e/visual/share.spec.ts --list</automated>
    Confirms the file still parses as valid Playwright test code and enumerates the reduced test set — this is the proof of a clean deletion, NOT `tsc`: bare `tsc` carries 11 pre-existing unrelated errors, and the CI-scoped `tsconfig.ci.json` excludes `tests/` entirely, so neither is a meaningful signal for this specific edit.
    <automated>grep -n "POLISH-01" .planning/REQUIREMENTS.md</automated>
    Expect the checkbox line to show `[x]` and the traceability row to show the exact honest-rationale sentence above.
    <automated>grep -c "12 baselines total" tests/e2e/visual/share.spec.ts</automated>
    Expect `0`; <automated>grep -c "10 baselines total" tests/e2e/visual/share.spec.ts</automated> expect `1`.
  </verify>
  <done>The two stale, misleading payment-banner visual tests are gone with a documented reason; the docblock's coverage-matrix count reads 10; POLISH-01 and its traceability row carry the honest rationale; REQUIREMENTS.md's coverage footer reflects 18/18 mapped + complete.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/estimate tests/unit/pdf tests/unit/pagination` after all 3 tasks — must be green with only reviewed, intentional snapshot updates staged, and `tests/unit/pagination/blocks-from-model.test.ts` completely unmodified (proving the one PDF StyleSheet touch in Task 1 was truly geometry-safe). Run `npx playwright test tests/e2e/visual/share.spec.ts --list` to prove Task 3's deletion parses cleanly. Run `git diff --stat` and confirm exactly the 10 files_modified above changed — `components/pdf/estimate-pdf.tsx` IS in scope, but ONLY for its 2 call-site lines (~578, ~591) plus 1 import line; nothing under `components/pdf/shared/pdf-photo-grid.tsx`, `components/pdf/estimate-pdf-modern.tsx`, or `lib/estimate/pagination/` appears at all.
</verification>

<success_criteria>
- Terms entries and the signature block show a matching subtle tint/accent behind their region in both webview templates; Classic's PDF equivalent carries a matching tint sourced from one shared, guarded token helper.
- Modern's PDF/webview terms/signature stay fill-free by design.
- Photo grid and customer-facing mobile list are visibly more polished, with zebra fully retired from every mobile surface; item-card-mobile.tsx (workspace editor mobile edit) untouched by this plan.
- Zero pagination-formula drift (blocks-from-model.ts untouched, its test suite green unmodified); estimate-pdf.tsx's touch is precisely scoped to 2 call sites + 1 import.
- Stale visual baselines handled intentionally, not silently; docblock count corrected.
- POLISH-01 closed out in REQUIREMENTS.md with an honest rationale — milestone v4.23 ready.
</success_criteria>

<output>
After completion, create `.planning/phases/186-webview-design-polish/186-02-SUMMARY.md`, including: (1) a milestone-readiness note — all 18 v1 requirements (ENGINE/PDFPAR/PGBRK/PGMODE/POLISH) complete across Phases 182-186, zero PDF-geometry drift introduced by the polish phase, stale visual baselines resolved intentionally, webview remains the benchmark with the PDF unified through the shared engine; (2) the exact honest-rationale sentence also recorded in REQUIREMENTS.md's traceability row (Warning 11); (3) the Classic-vs-PDF terms-card gap divergence note (Warning 8) — webview cards have visible `space-y-4` gaps, PDF cards abut with only a first-card top-margin bonus, a pre-existing structural difference this phase did not introduce or change.
</output>
