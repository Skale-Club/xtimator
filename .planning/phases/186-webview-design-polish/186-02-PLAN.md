---
phase: 186-webview-design-polish
plan: 02
type: execute
wave: 2
depends_on: ["186-01"]
files_modified:
  - components/workspace/estimate/estimate-document.tsx
  - components/share/estimate-document-modern.tsx
  - components/pdf/shared/pdf-terms-section.tsx
  - components/pdf/shared/pdf-signature-block.tsx
  - components/pdf/estimate-pdf.tsx
  - lib/estimate/document/tokens.ts
  - tests/e2e/visual/share.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "Terms entries (Estimate Terms/Payment/Timeline/Warranty/Notes) and the signature block render as visually distinct cards in both webview templates, not plain paragraphs"
    - "The Classic PDF's terms cards and signature block carry a matching subtle brand-tint background, sourced from ONE shared lib/estimate/document/tokens.ts constant — never two independently hand-typed hex literals"
    - "Modern's PDF and webview terms/signature stay fill-free (hairline-only), preserving its documented template identity — no new fill is added there"
    - "The customer-facing (view-mode) photo grid and mobile item list read as polished UI; the workspace editor's ItemCardMobile mobile-EDIT list is byte-unchanged"
    - "lib/estimate/pagination/blocks-from-model.ts is provably untouched (empty git diff) despite the PDF StyleSheet edits in this plan — proving the change was truly geometry-safe"
    - "The two stale ?stripe=success/canceled visual-baseline tests are removed with a documented reason, never silently"
    - "POLISH-01 is marked complete in .planning/REQUIREMENTS.md with an updated traceability row and coverage footer"
  artifacts:
    - path: "lib/estimate/document/tokens.ts"
      provides: "CARD_TINT_ALPHA_HEX — the one shared brand-tint alpha source for terms/signature card backgrounds, consumed by both the webview and the Classic PDF"
    - path: "components/pdf/shared/pdf-terms-section.tsx"
      provides: "optional cardFill prop on PdfTermsCard — background-color-only, zero geometry impact"
    - path: "components/pdf/shared/pdf-signature-block.tsx"
      provides: "optional cardFill prop on PdfSignatureBlock — background-color-only, zero geometry impact"
    - path: "components/pdf/estimate-pdf.tsx"
      provides: "Classic-only cardFill wiring at the PdfTermsSection/PdfSignatureBlock call sites (Modern's call sites omit the prop)"
    - path: "tests/e2e/visual/share.spec.ts"
      provides: "stale payment-banner baselines removed, with a documented reason"
    - path: ".planning/REQUIREMENTS.md"
      provides: "POLISH-01 closed out — checkbox, traceability row, coverage footer"
  key_links:
    - from: "lib/estimate/document/tokens.ts (CARD_TINT_ALPHA_HEX)"
      to: "components/pdf/estimate-pdf.tsx + components/workspace/estimate/estimate-document.tsx"
      via: "shared import, not a duplicated literal"
      pattern: "CARD_TINT_ALPHA_HEX"
    - from: "components/pdf/shared/pdf-terms-section.tsx (cardFill prop)"
      to: "lib/estimate/pagination/blocks-from-model.ts (termsCardBaseHeightPt formula)"
      via: "verified NO reference exists — background-color-only change requires no formula update"
      pattern: "cardFill"
---

<objective>
Give terms entries, the signature block, the photo grid, and the customer-facing mobile item list an intentional "card" visual treatment across both webview templates, propagate a matching subtle background tint to the Classic PDF's terms/signature blocks through ONE new shared token (zero geometry impact), and close out the milestone: intentionally handle the two stale payment-banner visual baselines and mark POLISH-01 complete.

Purpose: closes the milestone's last requirement. Terms/signature currently render as bare, unstyled text despite the product vocabulary calling them "terms cards" — this plan makes them actually look like cards, and (per ROADMAP Phase 186 success criterion #2) routes the one genuinely cross-surface value — the brand-tint background — through a single tokens.ts constant rather than duplicating a hex literal in the webview and the PDF independently.
Output: components/workspace/estimate/estimate-document.tsx, components/share/estimate-document-modern.tsx, components/pdf/shared/pdf-terms-section.tsx, components/pdf/shared/pdf-signature-block.tsx, components/pdf/estimate-pdf.tsx, lib/estimate/document/tokens.ts updated; tests/e2e/visual/share.spec.ts's stale tests removed; .planning/REQUIREMENTS.md POLISH-01 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-webview-design-polish/186-CONTEXT.md
@.planning/phases/186-webview-design-polish/186-01-SUMMARY.md
@lib/estimate/document/tokens.ts
@lib/estimate/pagination/blocks-from-model.ts

<interfaces>
<!-- Exact citations the executor needs — no exploration required. -->

Classic webview terms/signature wrapper divs (components/workspace/estimate/estimate-document.tsx), currently bare `<div>`s with only a `data-page-block-id`:
- Line ~1731 `data-page-block-id="terms-estimate"`
- Line ~1742 `data-page-block-id="terms-payment"`
- Line ~1757 `data-page-block-id="terms-timeline"`
- Line ~1771 `data-page-block-id="terms-warranty"`
- Line ~1786 `data-page-block-id="terms-notes"`
- Line ~1805 `data-page-block-id="signature"`
The existing hex-alpha convention already used in this same file (line ~1689, Add-section button hover): `` backgroundColor: `${brandColor}1A` `` — reuse this PATTERN, but source the alpha suffix from the new shared token (see below), not a re-typed literal.

Modern webview term/signature blocks (components/share/estimate-document-modern.tsx):
- Lines 377-416 (payment/timeline/warranty/notes terms), each a bare `<div>`.
- Lines 423-441 (signature block), bare `<div className="... border-t border-border/50">`.
Modern's documented identity (`lib/estimate/document/tokens.ts` `solidHeaderFill: false` for modern, line 38) is hairline/accent-only, NEVER a solid fill — do not add a background here; use a left-accent border instead.

PDF shared components (read fully before editing):
- `components/pdf/shared/pdf-terms-section.tsx` — `PdfTermsCard` (lines 62-75) renders `<View key={title} wrap={false} style={topMarginPt ? { marginTop: topMarginPt } : undefined}>`.
- `components/pdf/shared/pdf-signature-block.tsx` — `PdfSignatureBlock` (lines 34-48) renders `<View style={{ marginTop: 16 }} wrap={false}>`.
- Call sites: `components/pdf/estimate-pdf.tsx` invokes `PdfTermsSection({...})` (~line 557) and `PdfSignatureBlock({...})` (~line 591) with `brandColor` already in scope at both call sites (line 414). `components/pdf/estimate-pdf-modern.tsx` has the equivalent call sites (~line 575, ~609) — this plan does NOT edit estimate-pdf-modern.tsx; its calls simply never receive the new prop.

Height-formula citations to confirm are NEVER touched (lib/estimate/pagination/blocks-from-model.ts):
- `termsCardBaseHeightPt` (lines 203-204): `p.termsTitleFontSizePt + p.termsTitleSpacingContributionPt + p.termsTextMarginBottomPt` — references font size, spacing, margin — NOT backgroundColor.
- `signatureBaseHeightPt` (lines 206-207): `p.signatureMarginTopPt + p.termsTitleFontSizePt + p.signatureImageHeightPt + p.signatureLineFontSizePt * 2` — same, no color term.
This confirms a `backgroundColor`-only prop addition needs zero formula changes — verify this remains true by re-running `tests/unit/pagination/blocks-from-model.test.ts` unmodified at the end of Task 1.

Photo tiles:
- Classic `AttachedPhotoThumb` (estimate-document.tsx:1290-1292): `className="aspect-square overflow-hidden rounded-lg relative group"`.
- Modern's inline tile (estimate-document-modern.tsx:452): `className="aspect-square overflow-hidden rounded-lg relative"`. Modern's existing inline hex border convention: `borderColor: '#e4e4e7'` (line 131).
- PDF photo grid (`components/pdf/shared/pdf-photo-grid.tsx`) — DO NOT TOUCH. Its fixed 150pt tile width feeds `photosPerRow()` (`lib/estimate/document/tokens.ts:113-117`) directly; any width-affecting change (even a border) risks shifting per-row photo count.

Mobile stacked list (customer-facing / VIEW-mode only):
- Classic read-only mobile row: estimate-document.tsx lines ~562-578 (after Plan 186-01, className ends `...even:bg-muted/40`).
- Modern mobile row: estimate-document-modern.tsx lines ~255-259, `className="px-8 py-3 border-b border-border/50 last:border-b-0"`.
- Do NOT touch `components/workspace/estimate/item-card-mobile.tsx` (the workspace editor's mobile EDIT list) beyond what Plan 186-01 already did — it is deliberately "document-native, no card wrapper" per its own Phase 162-3c comment (file lines 18-23, 52-61).

Stale visual baselines (tests/e2e/visual/share.spec.ts:48-72): two tests screenshot `/estimate/${seedToken}?stripe=success` and `?stripe=canceled`. Confirmed during planning: `app/estimate/[token]/page.tsx` and `app/estimate/[token]/actions.ts` never read a `stripe` search param (grep returns zero matches) — these are pure no-ops today, superseded by the Phase 94 issued-invoice pay-link UI already rendered unconditionally in `components/share/estimate-view.tsx:352-405`.

REQUIREMENTS.md citations: POLISH-01 checkbox at line 53, traceability row at line 94, coverage footer at lines 96-99/102-103.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terms-card and signature-block treatment (webview both templates + Classic PDF propagation)</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/share/estimate-document-modern.tsx, components/pdf/shared/pdf-terms-section.tsx, components/pdf/shared/pdf-signature-block.tsx, components/pdf/estimate-pdf.tsx, lib/estimate/document/tokens.ts</files>
  <action>
    1. In `lib/estimate/document/tokens.ts`, add one new plain-string export (per the file's own "plain string/numeric values ONLY" rule, line 17-21):
       ```ts
       /** Phase 186 (POLISH-01) — shared brand-tint alpha suffix for "card"
        *  background fills (terms cards, signature block). Append to a
        *  company's brandColor hex, e.g. `${brandColor}${CARD_TINT_ALPHA_HEX}`.
        *  Consumed identically by the Classic webview
        *  (estimate-document.tsx) and the Classic PDF (estimate-pdf.tsx's
        *  cardFill wiring) — ROADMAP Phase 186 success criterion #2: ONE
        *  shared literal, never two independently hand-typed hex-alpha
        *  strings. */
       export const CARD_TINT_ALPHA_HEX = '0D' // ~5% opacity over white
       ```

    2. Classic webview (`components/workspace/estimate/estimate-document.tsx`): import `CARD_TINT_ALPHA_HEX` (it already imports `LETTER_HEIGHT_PX` from the same module, line 56 — add to that import). Give each of the 6 wrapper divs cited in `<interfaces>` (5 terms + signature) a card treatment: `rounded-lg border border-border/50 p-4` classes plus `style={{ backgroundColor: \`${brandColor}${CARD_TINT_ALPHA_HEX}\` }}`.

    3. Modern webview (`components/share/estimate-document-modern.tsx`): wrap each term block (lines 377-416) and the signature block (lines 423-441) with `border-l-2 pl-4` classes plus `style={{ borderLeftColor: brandColor }}` — a left-accent, NEVER a fill (preserves Modern's documented hairline-only identity).

    4. PDF shared components: add an optional `cardFill?: string` prop to `PdfTermsCardProps` (pdf-terms-section.tsx) and `PdfSignatureBlockProps` (pdf-signature-block.tsx). When present, merge `{ backgroundColor: cardFill }` into that component's own root `View`'s style. CRITICAL constraint — this prop must ONLY ever set `backgroundColor` (optionally `borderRadius`); NEVER add/modify `padding*`, `margin*`, any `border*Width`, or `fontSize` here — the height-formula citations in `<interfaces>` above confirm neither `termsCardBaseHeightPt` nor `signatureBaseHeightPt` references color, so this is the one class of PDF StyleSheet edit needing NO formula update, but would silently drift the moment a padding/margin/border-width/font-size property was touched instead.

    5. Wire `cardFill` ONLY from `components/pdf/estimate-pdf.tsx`'s `PdfTermsSection`/`PdfSignatureBlock` call sites (~lines 557, 591), passing `` `${brandColor}${CARD_TINT_ALPHA_HEX}` `` (same shared token imported from tokens.ts). Do NOT edit `components/pdf/estimate-pdf-modern.tsx` — its call sites simply never receive `cardFill`, so Modern's PDF terms/signature stay fill-free, mirroring the same Classic/Modern divergence enforced on the webview side.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-prepared-by-terms.test.tsx tests/unit/estimate/document-signature-view.test.tsx tests/unit/estimate/document-alignment.test.tsx tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx tests/unit/pdf/estimate-pdf-signature.test.tsx tests/unit/pdf/estimate-pdf-modern-signature.test.tsx -u</automated>
    Review any snapshot diffs — they must show ONLY className/style additions, never a structural or numeric-height change. Then, as the mandatory lockstep sanity check for this plan's one PDF StyleSheet touch:
    <automated>npx vitest run tests/unit/pdf tests/unit/pagination</automated>
    This must pass with ZERO changes needed to `tests/unit/pagination/blocks-from-model.test.ts` — if that test needs updating, the change was not actually geometry-safe; stop and re-scope to backgroundColor/borderRadius only.
  </verify>
  <done>All 5 Classic terms wrappers + signature wrapper show a visible card boundary (border + brand tint); Modern's equivalent shows a left-accent boundary (no fill); Classic PDF terms cards + signature block render with a matching subtle tint via the new cardFill prop sourced from CARD_TINT_ALPHA_HEX; Modern PDF is visually unchanged; `git diff -- lib/estimate/pagination/blocks-from-model.ts` is empty; tests/unit/pdf and tests/unit/pagination pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Photo grid frame + customer-facing mobile stacked-item-list polish (webview only, both templates)</name>
  <files>components/workspace/estimate/estimate-document.tsx, components/share/estimate-document-modern.tsx</files>
  <action>
    Photo grid: add `ring-1 ring-border/50` to Classic's `AttachedPhotoThumb` tile (estimate-document.tsx:1290-1292) and `border border-[#e4e4e7]` to Modern's inline tile (estimate-document-modern.tsx:452, matching its existing inline hex convention at line 131) — frame-only, no width/height/aspect-ratio change. Leave `components/pdf/shared/pdf-photo-grid.tsx` completely untouched (see `<interfaces>` — its fixed 150pt tile width feeds photosPerRow() row-chunking directly; any width-affecting change is a real geometry hazard this plan avoids by scoping photo polish to the webview only).

    Mobile stacked list (customer-facing VIEW mode ONLY — never `item-card-mobile.tsx`, see `<interfaces>`): Classic's read-only mobile row (estimate-document.tsx lines ~562-578) currently uses only a bottom-border divider; ADD `mx-4 my-1.5 rounded-lg border border-border/40` alongside its existing `${SECTION_PX} py-2.5` classes (keep `px-6 sm:px-10` — do not remove it, `tests/unit/estimate/document-alignment.test.tsx:139-147` asserts its presence) so each row reads as a separated card with a visible gap, not a flat divided list. Apply the Modern-idiomatic equivalent to its mobile row (estimate-document-modern.tsx lines ~255-259): add `mx-6 my-1.5 rounded-lg border border-[#e4e4e7]` alongside the existing `px-8 py-3` padding.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-alignment.test.tsx -u</automated>
    Review the diff — the mobile-row assertion at lines 139-147 checks for `px-6`/`sm:px-10` presence and `px-3` absence on the first mobile row div; confirm the new card-gap classes were ADDED alongside those, not swapped in place of them, so this pre-existing assertion keeps passing unmodified.
    <automated>npx vitest run tests/unit/estimate/document-photo-captions-view.test.tsx tests/unit/estimate</automated>
  </verify>
  <done>Photo tiles show a visible frame in both templates; the customer-facing mobile item list reads as separated cards; item-card-mobile.tsx is untouched by this task; `git diff --stat` confirms zero PDF files touched; estimate unit suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Milestone close-out — stale visual-baseline handling + REQUIREMENTS.md</name>
  <files>tests/e2e/visual/share.spec.ts, .planning/REQUIREMENTS.md</files>
  <action>
    In `tests/e2e/visual/share.spec.ts:48-72`, DELETE the two test blocks `share — payment success banner (?stripe=success)` and `share — payment canceled banner (?stripe=canceled)` (confirmed dead per `<interfaces>` — the share page never reads a `stripe` query param). Replace them with a single comment recording: what was removed, why (feature superseded by the Phase 94 issued-invoice pay-link UI; the query params are no-ops; the tests were redundant with the 9-baseline viewport×lang matrix above them), and that this was an intentional Phase 186 removal, not silent. Do NOT touch the "tenant brand override cascade" test below them — it exercises real behavior.

    In `.planning/REQUIREMENTS.md`: flip POLISH-01's checkbox from `- [ ]` to `- [x]` (line 53); update its Traceability row (line 94) from `Pending` to `Complete (Phase 186)`; update the Coverage summary footer (lines 96-99, 102-103) to reflect all 18 v1 requirements mapped and complete.
  </action>
  <verify>
    <automated>grep -c "stripe=success\|stripe=canceled" tests/e2e/visual/share.spec.ts</automated>
    Expect `0`.
    <automated>grep -n "POLISH-01" .planning/REQUIREMENTS.md</automated>
    Expect the checkbox line to show `[x]` and the traceability row to show `Complete`.
    <automated>npx tsc -p tsconfig.ci.json --noEmit</automated>
  </verify>
  <done>The two stale, misleading payment-banner visual tests are gone with a documented reason; POLISH-01 and its traceability row show Complete; REQUIREMENTS.md's coverage footer reflects 18/18 mapped + complete.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/estimate tests/unit/pdf tests/unit/pagination` after all 3 tasks — must be green with only reviewed, intentional snapshot updates staged, and `tests/unit/pagination/blocks-from-model.test.ts` completely unmodified (proving the one PDF StyleSheet touch in Task 1 was truly geometry-safe). Run `npx tsc -p tsconfig.ci.json --noEmit`. Run `git diff --stat` and confirm exactly the 8 files_modified above changed — nothing under `components/pdf/shared/pdf-photo-grid.tsx`, `components/pdf/estimate-pdf-modern.tsx`, or `lib/estimate/pagination/` appears.
</verification>

<success_criteria>
- Terms entries and the signature block read as cards in both webview templates; Classic's PDF equivalent carries a matching tint sourced from one shared token.
- Modern's PDF/webview terms/signature stay fill-free by design.
- Photo grid and customer-facing mobile list are visibly more polished; item-card-mobile.tsx (workspace editor mobile edit) untouched.
- Zero pagination-formula drift (blocks-from-model.ts untouched, its test suite green unmodified).
- Stale visual baselines handled intentionally, not silently.
- POLISH-01 closed out in REQUIREMENTS.md — milestone v4.23 ready.
</success_criteria>

<output>
After completion, create `.planning/phases/186-webview-design-polish/186-02-SUMMARY.md`, including a milestone-readiness note: all 18 v1 requirements (ENGINE/PDFPAR/PGBRK/PGMODE/POLISH) complete across Phases 182-186, zero PDF-geometry drift introduced by the polish phase, stale visual baselines resolved intentionally, and confirmation the webview remains the benchmark with the PDF unified through the shared engine.
</output>
