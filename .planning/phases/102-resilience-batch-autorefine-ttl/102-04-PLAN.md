---
phase: 102-resilience-batch-autorefine-ttl
plan: 04
type: execute
wave: 1
depends_on: [00]
files_modified:
  - components/workspace/needs-details-banner.tsx
  - components/workspace/overview-tab.tsx
autonomous: false
requirements: [HARD-06]

must_haves:
  truths:
    - "When a project is in awaiting_details (a vague estimate that survived the cap), a recourse banner shows in the project overview"
    - "The banner is absent when project.status !== 'awaiting_details'"
    - "The 'Add details & regenerate' CTA reuses the EXISTING CaptureModePicker generate trigger — no new generation path"
    - "Copy is wrapped in t() so pt/es are handled by the existing translation system"
    - "Reuses the existing Alert primitive + Button — no editor/workspace redesign"
  artifacts:
    - path: "components/workspace/needs-details-banner.tsx"
      provides: "NeedsDetailsBanner client component (Alert + AlertTitle + AlertDescription + Button)"
      contains: "onAddDetails"
      min_lines: 20
    - path: "components/workspace/overview-tab.tsx"
      provides: "Conditional banner render gated on project.status === 'awaiting_details', wired to the existing setModePickerOpen trigger"
      contains: "awaiting_details"
  key_links:
    - from: "components/workspace/overview-tab.tsx"
      to: "components/workspace/needs-details-banner.tsx"
      via: "render when project.status === 'awaiting_details', onAddDetails={() => setModePickerOpen(true)}"
      pattern: "awaiting_details"
    - from: "NeedsDetailsBanner CTA"
      to: "existing CaptureModePicker / generate trigger"
      via: "onAddDetails callback → setModePickerOpen(true) (the same path handleRecord uses)"
      pattern: "onAddDetails"
---

<objective>
HARD-06 (recourse half): close the web dead-end where a still-vague estimate left the user stuck. The web adapter already writes `projects.status='awaiting_details'` (and `project.status` is an existing server prop in OverviewTab scope), but nothing surfaces it. Add a small, dismissible-by-action recourse banner in OverviewTab that explains the estimate was too vague and offers an "Add details & regenerate" CTA — reusing the EXISTING `CaptureModePicker`/generate trigger and the `Alert` primitive. No new query, no hook change, no editor redesign.

Purpose: gives the user a way forward instead of a dead end (HARD-06 success criterion 2).
Output: new `NeedsDetailsBanner` component; OverviewTab renders it when `project.status === 'awaiting_details'` and wires the CTA to the existing `setModePickerOpen(true)` trigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-resilience-batch-autorefine-ttl/102-UI-SPEC.md
@.planning/phases/102-resilience-batch-autorefine-ttl/102-RESEARCH.md

@components/workspace/overview-tab.tsx
@components/ui/alert.tsx
@components/workspace/link-client-button.tsx

<interfaces>
<!-- Exact integration. Use directly. -->

overview-tab.tsx (host — already client, project in scope):
  - `project: ProjectDetail` (has `status: string`, `id: string`).
  - already owns `const [modePickerOpen, setModePickerOpen] = useState(false)` (line 54),
    `handleRecord` → `setModePickerOpen(true)` (lines 61-63), and renders `<CaptureModePicker .../>` (lines 78-82).
  - returned tree root: `<div className="space-y-6"> ... </div>` (lines 76-111). Insert the banner at the TOP, above <EstimateTab>.

alert.tsx exports: Alert (variant 'default'|'destructive'), AlertTitle, AlertDescription.

i18n: client components import `useTranslation` from `@/lib/i18n/use-translation` and call `t('English literal')`.
  The English literal IS the key; pt/es are produced by the existing translation pipeline. (See link-client-button.tsx for the exact import + usage.)

Button: `@/components/ui/button` (size="sm" common in workspace).

CTA behavior (UI-SPEC): onAddDetails → setModePickerOpen(true) (reuses the same CaptureModePicker → capture flow → /api/generate-estimate path the empty state uses). Do NOT build a new generate call.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NeedsDetailsBanner + wire it into OverviewTab gated on awaiting_details</name>
  <files>components/workspace/needs-details-banner.tsx, components/workspace/overview-tab.tsx</files>
  <read_first>
    @components/workspace/overview-tab.tsx
    @components/ui/alert.tsx
    @components/workspace/link-client-button.tsx (useTranslation import + Button usage)
    @.planning/phases/102-resilience-batch-autorefine-ttl/102-UI-SPEC.md (copy + states)
    @tests/unit/workspace/needs-details-banner.test.tsx (the RED test this turns green)
  </read_first>
  <behavior>
    - NeedsDetailsBanner renders an Alert with title + body + a Button; clicking the Button calls onAddDetails once.
    - Gated render: shown when project.status === 'awaiting_details', absent otherwise (gate lives in OverviewTab).
  </behavior>
  <action>
    1. Create `components/workspace/needs-details-banner.tsx` as a `'use client'` component:
       ```tsx
       'use client'
       import { Info } from 'lucide-react'   // match existing info/warn icon convention
       import { Alert, AlertTitle, AlertDescription } from '@/components/ui/alert'
       import { Button } from '@/components/ui/button'
       import { useTranslation } from '@/lib/i18n/use-translation'

       export function NeedsDetailsBanner({ onAddDetails }: { onAddDetails: () => void }) {
         const { t } = useTranslation()
         return (
           <Alert>
             <Info />
             <AlertTitle>{t('We need a bit more detail')}</AlertTitle>
             <AlertDescription>
               <p>{t("The estimate came out too vague. Add more about the job — materials, measurements, scope — and we'll rebuild it.")}</p>
               <Button size="sm" className="mt-2" onClick={onAddDetails}>
                 {t('Add details & regenerate')}
               </Button>
             </AlertDescription>
           </Alert>
         )
       }
       ```
       Use the EXACT i18n import path + `t()` usage from link-client-button.tsx. All three copy strings (title, body, CTA) MUST be wrapped in `t()` so the existing pipeline produces pt/es per the UI-SPEC. Reuse `Alert` (default variant) — do NOT invent a new style. Match the lucide icon convention already used for info/warning in the workspace (Info or AlertCircle).

    2. In `components/workspace/overview-tab.tsx`:
       - Import `NeedsDetailsBanner` from `./needs-details-banner`.
       - At the TOP of the returned `<div className="space-y-6">` (right after the opening tag, before `<CaptureModePicker>` or just after it), add the gated render:
         ```tsx
         {project.status === 'awaiting_details' && (
           <NeedsDetailsBanner onAddDetails={() => setModePickerOpen(true)} />
         )}
         ```
       - This reuses the EXISTING `setModePickerOpen` state + `CaptureModePicker` already mounted at lines 78-82 — no new picker, no new generate call. `project.status` is already in scope (no new prop).
       Do NOT modify EstimateTab, the capture flow, or any generation plumbing.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/workspace/needs-details-banner.test.tsx</automated>
  </verify>
  <done>needs-details-banner.test.tsx green: banner renders with title + CTA; gated wrapper hides it when status !== 'awaiting_details'; CTA click fires onAddDetails once. All copy wrapped in t(). OverviewTab renders the banner only on awaiting_details and wires it to setModePickerOpen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the recourse banner end-to-end</name>
  <action>Human verification of the recourse banner in staging — Claude has already implemented and unit-tested the component + wiring in Task 1; this confirms the live generate→awaiting_details→banner→regenerate loop and i18n.</action>
  <what-built>The web recourse banner: when a project sits in awaiting_details (vague estimate survived the auto-refine cap), OverviewTab shows a NeedsDetailsBanner (Alert + "Add details & regenerate" CTA) that reopens the existing CaptureModePicker to re-trigger generation.</what-built>
  <how-to-verify>
    1. In staging, generate an estimate from a deliberately thin/vague input (e.g. a one-word description) so the engine reverts it and sets project.status='awaiting_details'.
    2. Open the project's overview — confirm the banner appears above the (empty) estimate area with the title "We need a bit more detail" and the CTA.
    3. Switch app language to PT and ES — confirm the title/body/CTA are translated (existing pipeline).
    4. Click "Add details & regenerate" — confirm it opens the capture-mode picker (the same one the Record action uses) and that completing the flow re-runs generation; once a usable estimate is produced the banner disappears (status leaves awaiting_details).
    5. Confirm a normal (non-vague) project shows NO banner.
  </how-to-verify>
  <verify>
    <automated>npx vitest run tests/unit/workspace/needs-details-banner.test.tsx</automated>
  </verify>
  <done>Owner confirms: banner appears only on awaiting_details, copy is translated in pt/es, CTA reopens the existing picker and regeneration clears the banner, non-vague projects show nothing.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/workspace/needs-details-banner.test.tsx` green.
- `grep -n "awaiting_details" components/workspace/overview-tab.tsx` shows the gated render.
- `grep -n "useTranslation\|t(" components/workspace/needs-details-banner.tsx` confirms all copy is t()-wrapped.
- No changes to EstimateTab / capture flow / generate API (`git diff --stat` shows only the two files in files_modified).
</verification>

<success_criteria>
- A vague-after-cap project surfaces a clear recourse banner whose CTA re-enters the existing generate trigger; non-vague projects show nothing; copy is i18n-wrapped; only Alert/Button are reused (no redesign).
</success_criteria>

<output>
After completion, create `.planning/phases/102-resilience-batch-autorefine-ttl/102-04-SUMMARY.md`.
</output>
