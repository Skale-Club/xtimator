---
phase: 102-resilience-batch-autorefine-ttl
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - lib/estimate/graph/state.ts
  - lib/estimate/adapters/whatsapp.ts
  - lib/whatsapp/estimate-graph.ts
  - lib/inngest/functions/whatsapp-process.ts
  - lib/inngest/functions/generate-estimate.ts
autonomous: true
requirements: [HARD-07]

must_haves:
  truths:
    - "Both WhatsApp TTL mint sites derive expires_at from a durable, state-carried timestamp, not a fresh Date.now() inside the node"
    - "Re-invoking the graph with the same requestedAt yields an identical expires_at (replay-stable)"
    - "A direct invoker that omits requestedAt still works (Date.now() fallback) — no Invalid Date"
    - "graph-neutrality stays green: requestedAt carries no WhatsApp token"
    - "Default auto-refine + generate/MCP/refine paths do not regress"
  artifacts:
    - path: "lib/estimate/graph/state.ts"
      provides: "Neutral requestedAt annotation on core EstimateState"
      contains: "requestedAt"
    - path: "lib/estimate/adapters/whatsapp.ts"
      provides: "Both finalize TTL sites compute expires_at from state.requestedAt ?? Date.now()"
      contains: "state.requestedAt"
  key_links:
    - from: "lib/inngest/functions/whatsapp-process.ts"
      to: "lib/whatsapp/estimate-graph.ts buildEstimateGraph().invoke"
      via: "requestedAt threaded from a single handler-entry Date.now()"
      pattern: "requestedAt"
    - from: "lib/whatsapp/estimate-graph.ts"
      to: "lib/estimate/graph core invoke initial state"
      via: "requestedAt passed into the channel-neutral graph.invoke"
      pattern: "requestedAt"
    - from: "lib/estimate/adapters/whatsapp.ts finalize"
      to: "whatsapp_sessions.expires_at"
      via: "new Date((state.requestedAt ?? Date.now()) + SESSION_TTL_MINUTES*60*1000)"
      pattern: "SESSION_TTL_MINUTES"
---

<objective>
HARD-07: make WhatsApp session/awaiting-state TTLs replay-safe. Add a single neutral `requestedAt` epoch field to the core graph state, set ONCE at the Inngest handler entry (server-trusted), thread it through both graph entry points, and derive both `expires_at` mint sites from it instead of re-evaluating `Date.now()` inside finalize. On an Inngest retry/replay the timestamp is stable, so the TTL does not drift — in advance of any future per-node `step.run` decomposition (HARD-08).

Purpose: removes the replay hazard the whatsapp.ts header comment already flags (lines 34-39).
Output: `requestedAt` on EstimateState; both whatsapp.ts TTL sites derive from it; both Inngest invokes thread it. `SESSION_TTL_MINUTES` (30) unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-resilience-batch-autorefine-ttl/102-RESEARCH.md

@lib/estimate/graph/state.ts
@lib/estimate/adapters/whatsapp.ts
@lib/whatsapp/estimate-graph.ts
@lib/inngest/functions/whatsapp-process.ts
@lib/inngest/functions/generate-estimate.ts

<interfaces>
<!-- Exact sites. Use directly. -->

state.ts — EstimateState = Annotation.Root({ ... }) (channel-neutral; graph-neutrality gate greps this file).

whatsapp.ts CURRENT TTL sites (TWO):
  Line 356-358 (askDetails branch):
    const expiresAt = new Date(Date.now() + SESSION_TTL_MINUTES * 60 * 1000).toISOString()
  Line 385-387 (sendConfirmation branch):
    const expiresAt = new Date(Date.now() + SESSION_TTL_MINUTES * 60 * 1000).toISOString()
  SESSION_TTL_MINUTES = 30 (line 61). Both branches insert whatsapp_sessions with expires_at.

lib/whatsapp/estimate-graph.ts — buildEstimateGraph().invoke(initial: WhatsAppInitialState):
  - WhatsAppInitialState interface (lines 42-54)
  - core graph.invoke initial-state object (lines 87-94): { companyId, projectId, channel:'whatsapp', estimateId, estimateLanguage, isVague }

whatsapp-process.ts — whatsAppProcessJob handler (lines 64-102):
  - destructures { companyId, projectId, ownerPhone, messages } (line 68)
  - graph.invoke initial state (lines 84-95)

generate-estimate.ts — t0 = Date.now() at handler entry (line 88); graph.invoke initial state (lines 121-131).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add neutral requestedAt to core state + derive both whatsapp.ts TTL sites from it</name>
  <read_first>
    @lib/estimate/graph/state.ts
    @lib/estimate/adapters/whatsapp.ts (lines 340-433: both finalize branches)
    @tests/unit/whatsapp/replay-safe-ttl.test.ts (the RED test this turns green)
    @tests/unit/estimate/graph-neutrality.test.ts (FORBIDDEN token list — keep clean)
  </read_first>
  <behavior>
    - Same requestedAt across two finalize invocations → identical expires_at (replay-safe-ttl.test.ts).
    - expires_at === new Date(requestedAt + 30*60*1000).toISOString().
    - requestedAt omitted → falls back to Date.now() (no Invalid Date).
    - graph-neutrality.test.ts stays green (no whatsapp token added to state.ts).
  </behavior>
  <action>
    1. In `lib/estimate/graph/state.ts`, add ONE neutral annotation to `EstimateState = Annotation.Root({...})`:
       ```typescript
       /**
        * Server-trusted graph-entry timestamp (epoch ms), set ONCE from the Inngest
        * event handler entry. Replay-safe source for any TTL/expiry computed in a
        * finalize node — never re-mint Date.now() inside a node (HARD-07).
        * Channel-neutral: carries no channel token.
        */
       requestedAt: Annotation<number | undefined>(),
       ```
       Do NOT use any WhatsApp-related word in the field name or comment (FORBIDDEN: ownerPhone, WhatsAppMessage, sendWhatsAppMessage, whatsapp_, downloadWhatsAppMedia, lib/whatsapp). `requestedAt` is safe.

    2. In `lib/estimate/adapters/whatsapp.ts`, at BOTH TTL sites (askDetails ~line 356-358 and sendConfirmation ~line 385-387), replace:
       ```typescript
       const expiresAt = new Date(Date.now() + SESSION_TTL_MINUTES * 60 * 1000).toISOString()
       ```
       with:
       ```typescript
       // HARD-07: derive expiry from the durable graph-entry timestamp (replay-safe);
       // fall back to Date.now() so direct invokers (unit tests) without requestedAt still work.
       const base = state.requestedAt ?? Date.now()
       const expiresAt = new Date(base + SESSION_TTL_MINUTES * 60 * 1000).toISOString()
       ```
       `state` is already the finalize param (`async finalize(state: EstimateStateType)`); `requestedAt` is now on that type. Keep `SESSION_TTL_MINUTES = 30` unchanged.

    3. Update the file header NOTE (whatsapp.ts lines 34-39) to reflect that the TTL is now state-derived (replace "mints Date.now() inside finalize ... coalesce the TTL from state instead" with a past-tense note that this is DONE via state.requestedAt per HARD-07).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/whatsapp/replay-safe-ttl.test.ts tests/unit/estimate/graph-neutrality.test.ts</automated>
  </verify>
  <done>replay-safe-ttl.test.ts green (identical expires_at on same requestedAt; equals the requestedAt-derived value). graph-neutrality green. SESSION_TTL_MINUTES still 30.</done>
</task>

<task type="auto">
  <name>Task 2: Thread requestedAt through both Inngest entry points into the graph invoke</name>
  <read_first>
    @lib/whatsapp/estimate-graph.ts (WhatsAppInitialState + core invoke object)
    @lib/inngest/functions/whatsapp-process.ts (lines 64-102)
    @lib/inngest/functions/generate-estimate.ts (lines 82-131; t0 at 88)
    @tests/unit/whatsapp/never-reply-regression.test.ts (must stay green)
  </read_first>
  <action>
    1. In `lib/whatsapp/estimate-graph.ts`:
       - Add `requestedAt?: number` to the `WhatsAppInitialState` interface (lines 42-54).
       - In the core `graph.invoke({...})` initial-state object (lines 87-94), add `requestedAt: initial.requestedAt,` so the WhatsApp-superset value flows into the channel-neutral core state.

    2. In `lib/inngest/functions/whatsapp-process.ts` (`whatsAppProcessJob`, lines 64-102):
       - At handler entry (right after `const { companyId, projectId, ownerPhone, messages } = data`, line 68), capture a single trusted timestamp: `const requestedAt = Date.now()`. (It is captured OUTSIDE `step.run('orchestrate-estimate')` so an Inngest retry of that step reuses the same value — this is the replay-safety guarantee, mirroring generate-estimate's t0.)
       - In the `graph.invoke({...})` initial state (lines 84-95), add `requestedAt,`.

    3. In `lib/inngest/functions/generate-estimate.ts`:
       - `t0 = Date.now()` already exists at line 88 (handler entry, outside step.run). In the `graph.invoke({...})` initial-state object (lines 121-131), add `requestedAt: t0,`. (The web/MCP adapter finalize is a passthrough and does not mint a TTL, so this is harmless there but keeps the field populated consistently for any future finalize TTL.)
  </action>
  <verify>
    <automated>npx vitest run tests/unit/whatsapp/never-reply-regression.test.ts tests/unit/inngest/whatsapp-process-job.test.ts tests/unit/inngest/generate-estimate-job.test.ts</automated>
  </verify>
  <done>requestedAt flows: handler entry → WhatsAppInitialState → core invoke → finalize. never-reply-regression + both inngest job tests stay green (or fail only on pre-existing worker-reuse timeouts noted in deferred-items.md — confirm by isolation if so).</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/whatsapp/replay-safe-ttl.test.ts` green.
- `npx vitest run tests/unit/estimate/graph-neutrality.test.ts tests/unit/whatsapp/never-reply-regression.test.ts tests/unit/estimate/auto-refine-isolation.test.ts` green (invariants hold).
- `grep -n "state.requestedAt" lib/estimate/adapters/whatsapp.ts` shows BOTH TTL sites converted.
- `grep -n "requestedAt" lib/whatsapp/estimate-graph.ts lib/inngest/functions/whatsapp-process.ts lib/inngest/functions/generate-estimate.ts` shows the field threaded at all three sites.
</verification>

<success_criteria>
- requestedAt is a neutral core-state field; both WhatsApp TTL sites derive from it; both Inngest entries thread it; SESSION_TTL_MINUTES unchanged.
- Replay-safe TTL test green; all invariants green.
</success_criteria>

<output>
After completion, create `.planning/phases/102-resilience-batch-autorefine-ttl/102-01-SUMMARY.md`. Note in it that state.ts AND whatsapp.ts are also touched by Plan 03 (HARD-05) which depends on this plan.
</output>
