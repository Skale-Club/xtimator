---
phase: 102-resilience-batch-autorefine-ttl
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/estimate/auto-refine-cap.test.ts
  - tests/unit/whatsapp/replay-safe-ttl.test.ts
  - tests/unit/whatsapp/batch-reporting.test.ts
  - tests/unit/workspace/needs-details-banner.test.tsx
autonomous: true
requirements: [HARD-05, HARD-06, HARD-07]

must_haves:
  truths:
    - "Four RED test files exist and fail for the right reason (target behavior absent), not on import/compile errors"
    - "@testing-library/react is confirmed present so the banner RTL test can run"
    - "Each new test mirrors an existing harness pattern (never-reply-regression mock style; graph-neutrality static style)"
  artifacts:
    - path: "tests/unit/estimate/auto-refine-cap.test.ts"
      provides: "HARD-06 cap RED test — default cap=1 unchanged + AUTO_REFINE_MAX_ATTEMPTS=N loops N"
      contains: "checkVagueAfterAssessEdge"
    - path: "tests/unit/whatsapp/replay-safe-ttl.test.ts"
      provides: "HARD-07 RED test — same requestedAt → identical expires_at across two finalize invocations"
      contains: "requestedAt"
    - path: "tests/unit/whatsapp/batch-reporting.test.ts"
      provides: "HARD-05 RED test — partial failure builds estimate + ONE reply noting dropped item; total failure → existing no-input path, still ONE reply"
      contains: "sendWhatsAppMessage"
    - path: "tests/unit/workspace/needs-details-banner.test.tsx"
      provides: "HARD-06 recourse UI RED test — renders on awaiting_details, hidden otherwise, CTA fires trigger"
      contains: "awaiting_details"
  key_links:
    - from: "tests/unit/whatsapp/replay-safe-ttl.test.ts"
      to: "lib/whatsapp/estimate-graph.ts buildEstimateGraph().invoke"
      via: "graph invoke with requestedAt + chainable supabase mock asserting whatsapp_sessions insert expires_at"
      pattern: "buildEstimateGraph"
    - from: "tests/unit/estimate/auto-refine-cap.test.ts"
      to: "lib/estimate/graph/nodes/decide.ts checkVagueAfterAssessEdge"
      via: "direct import + env stub"
      pattern: "checkVagueAfterAssessEdge"
---

<objective>
Wave 0: author the four RED/EXTEND test files that pin Phase 102's three behaviors before any implementation exists. These tests MUST be red for the right reason (the target behavior is absent) and must compile and import cleanly so Waves 1-2 turn them green. This is the Nyquist scaffold for every downstream task's `<automated>` verify.

Purpose: every implementation task in this phase references one of these files in its `<verify>`. Writing them first guarantees feedback latency < 30s and prevents "implementation with no test".
Output: 4 new test files (3 vitest unit + 1 RTL); a confirmed `@testing-library/react` devDependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-resilience-batch-autorefine-ttl/102-RESEARCH.md
@.planning/phases/102-resilience-batch-autorefine-ttl/102-VALIDATION.md
@.planning/phases/102-resilience-batch-autorefine-ttl/102-UI-SPEC.md

# Harness patterns to mirror (read these first — copy their mock style verbatim):
@tests/unit/whatsapp/never-reply-regression.test.ts
@tests/unit/estimate/graph-neutrality.test.ts

# Source under test (read for exact signatures/shapes):
@lib/estimate/graph/nodes/decide.ts
@lib/whatsapp/estimate-graph.ts
@lib/estimate/adapters/whatsapp.ts

<interfaces>
<!-- Exact contracts the tests assert against. Use directly — no codebase exploration. -->

decide.ts (cap site — lib/estimate/graph/nodes/decide.ts:36-40):
```typescript
export function checkVagueAfterAssessEdge(state: EstimateStateType): string {
  if (!state.isVague) return 'finalize'
  if ((state.refineAttempts ?? 0) < 1) return 'autoRefine'  // ← literal 1 becomes AUTO_REFINE_MAX_ATTEMPTS in Plan 02
  return 'finalize'
}
```
Returns: 'finalize' | 'autoRefine'. Drive with crafted { isVague, refineAttempts }.

WhatsApp graph entry (lib/whatsapp/estimate-graph.ts):
```typescript
export function buildEstimateGraph(): { invoke(initial: WhatsAppInitialState): Promise<...> }
// WhatsAppInitialState: { companyId, projectId, ownerPhone, channel?, messages, currentMessage?, mediaResults?, estimateId?, estimateLanguage?, isVague?, requestedAt? (NEW, added by Plan 01) }
```
finalize (whatsapp.ts) inserts into `whatsapp_sessions` with `expires_at` (ISO string) — derived from requestedAt after Plan 01.

mediaResults reason vocabulary (whatsapp.ts processMessage): 'download_failed' | 'transcription_failed' | 'empty_transcript' | 'no_message' | 'unknown_error'

Banner (NEW — created by Plan 04): components/workspace/needs-details-banner.tsx
```typescript
// Conditionally rendered by overview-tab.tsx when project.status === 'awaiting_details'.
// Wraps Alert + Button; CTA onClick reuses the existing CaptureModePicker trigger.
export function NeedsDetailsBanner(props: { onAddDetails: () => void }): JSX.Element
```
i18n: components use `useTranslation()` → `t('English literal key')` (no bundle files; English literal IS the key).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm RTL availability + write the two graph/edge RED tests (cap + replay-safe TTL)</name>
  <read_first>
    @tests/unit/whatsapp/never-reply-regression.test.ts (mock harness to copy)
    @lib/estimate/graph/nodes/decide.ts
    @lib/whatsapp/estimate-graph.ts
    @lib/estimate/adapters/whatsapp.ts (finalize: whatsapp_sessions insert + expires_at)
    @package.json (confirm @testing-library/react present)
  </read_first>
  <action>
    1. Verify `@testing-library/react` is in package.json devDependencies (it is: `^16.3.2`, with `jsdom` + `@types/jsdom`). If missing, STOP and run `npm install -D @testing-library/react` — but expect it present; just confirm.

    2. Create `tests/unit/estimate/auto-refine-cap.test.ts` (HARD-06 cap). Import `checkVagueAfterAssessEdge` from `@/lib/estimate/graph/nodes/decide`. Because the constant is read at module load from `process.env.AUTO_REFINE_MAX_ATTEMPTS`, use `vi.resetModules()` + dynamic `await import()` inside each case so the env stub is read fresh. Cases:
       - DEFAULT (no env): `refineAttempts: undefined, isVague: true` → 'autoRefine'; `refineAttempts: 1, isVague: true` → 'finalize'. (proves cap=1 unchanged)
       - `process.env.AUTO_REFINE_MAX_ATTEMPTS = '2'`: `refineAttempts: 1, isVague: true` → 'autoRefine'; `refineAttempts: 2, isVague: true` → 'finalize'.
       - `isVague: false` always → 'finalize' regardless of cap.
       Set/reset env in beforeEach/afterEach (`delete process.env.AUTO_REFINE_MAX_ATTEMPTS`).
       RED today: the source still hard-codes `< 1`, so the `AUTO_REFINE_MAX_ATTEMPTS=2` case (refineAttempts:1 → expect 'autoRefine') FAILS. Do NOT weaken the assertion to make it pass.

    3. Create `tests/unit/whatsapp/replay-safe-ttl.test.ts` (HARD-07). COPY the mock block from never-reply-regression.test.ts verbatim (the `@/lib/whatsapp/client`, `@/lib/whatsapp/conversations`, `@/lib/services/generate-estimate`, `@/lib/ai/openrouter-client`, `@/lib/storage`, `@/lib/whatsapp/ask-details`, `@/lib/supabase/service` mocks + makeChainableClient). Capture the `whatsapp_sessions` insert payload (extend the chainable mock's `whatsapp_sessions` branch so `insert` records args into a module-level array). Drive the CONFIRM path: make `generateEstimateForProject` resolve a usable estimate and `estimateRow` non-vague so finalize hits sendConfirmation. Cases:
       - Invoke `buildEstimateGraph().invoke({ ...BASE, requestedAt: FIXED_MS, messages: [text msg] })` TWICE with the SAME `requestedAt`; assert both captured `expires_at` values are IDENTICAL and equal `new Date(FIXED_MS + 30*60*1000).toISOString()`.
       - Assert `expires_at` does NOT equal a `Date.now()`-based value taken AFTER invoke (proves it is derived from requestedAt, not re-minted). RED today: finalize mints `Date.now()`, so two invokes spaced in time differ → assertion fails until Plan 01.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/auto-refine-cap.test.ts tests/unit/whatsapp/replay-safe-ttl.test.ts</automated>
  </verify>
  <done>Both files import + compile; auto-refine-cap fails ONLY on the cap=2 case; replay-safe-ttl fails ONLY on the requestedAt-stability assertion. No import/reference errors. @testing-library/react confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Write the batch-reporting RED test + the recourse-banner RTL RED test</name>
  <read_first>
    @tests/unit/whatsapp/never-reply-regression.test.ts (mock harness + Path A/C structure)
    @lib/estimate/adapters/whatsapp.ts (mediaResults reason codes, finalize reply bodies)
    @lib/whatsapp/ask-details.ts (buildAskDetailsMessage)
    @components/workspace/link-client-button.tsx (useTranslation + Button pattern to mirror)
    @components/ui/alert.tsx
    @.planning/phases/102-resilience-batch-autorefine-ttl/102-UI-SPEC.md
  </read_first>
  <action>
    1. Create `tests/unit/whatsapp/batch-reporting.test.ts` (HARD-05). Reuse the never-reply-regression mock harness. Two cases via `buildEstimateGraph().invoke`:
       - PARTIAL failure: pass two messages — one good text (`{ id:'m1', type:'text', text:{body:'paint the fence'} }`) and one audio whose `downloadWhatsAppMedia` REJECTS (1 of 2 ok:false). Make `generateEstimateForProject` resolve a usable (non-vague) estimate. Assert: `sendWhatsAppMessage` called EXACTLY ONCE; the single reply body matches BOTH the estimate-ready copy AND a dropped-item note substring (e.g. `/couldn't process 1/i` — keep the regex tolerant since Plan 03 owns the exact wording). RED today: finalize does not append any dropped-item note → the note substring fails.
       - TOTAL failure: both messages un-ingestable (all downloads reject) → assert `sendWhatsAppMessage` called EXACTLY ONCE with the existing no-input copy (`/couldn't process your message/i`), `generateEstimateForProject` NOT called. This case should be GREEN today (it pins the never-reply invariant under all-fail) — keep it to prove Plan 03 does not regress it.
       Add a top-of-file comment noting the dropped-item note regex is intentionally loose (Plan 03 finalizes wording) and the substring assertion is the load-bearing RED.

    2. Create `tests/unit/workspace/needs-details-banner.test.tsx` (HARD-06 recourse UI). Use `@testing-library/react` (`render`, `screen`, `fireEvent`) + vitest. Mock `@/lib/i18n/use-translation` so `t` is identity (`{ t: (s: string) => s }`) — mirror how other RTL/i18n tests stub it (search existing tests for the established `use-translation` mock; if none, stub inline). Import the NOT-YET-EXISTING `NeedsDetailsBanner` from `@/components/workspace/needs-details-banner`. Cases:
       - Renders title + CTA when mounted (the banner itself is unconditional; the awaiting_details gate lives in overview-tab — but ALSO add a render test that drives the gate via a tiny inline wrapper replicating `{project.status === 'awaiting_details' && <NeedsDetailsBanner .../>}` to assert hidden when status !== 'awaiting_details').
       - Clicking the CTA button fires the `onAddDetails` callback exactly once (`const onAddDetails = vi.fn()`).
       RED today: the `@/components/workspace/needs-details-banner` module does not exist → import fails. That import failure IS the intended RED (Plan 04 creates it).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/whatsapp/batch-reporting.test.ts tests/unit/workspace/needs-details-banner.test.tsx</automated>
  </verify>
  <done>batch-reporting partial case fails ONLY on the dropped-item note substring; total-failure case green. needs-details-banner test fails ONLY because the banner module is absent (clean import-not-found RED), with assertions ready to go green once Plan 04 lands the component.</done>
</task>

</tasks>

<verification>
- All 4 files compile and run under `npx vitest run tests/unit/estimate/auto-refine-cap.test.ts tests/unit/whatsapp/replay-safe-ttl.test.ts tests/unit/whatsapp/batch-reporting.test.ts tests/unit/workspace/needs-details-banner.test.tsx`.
- Every red is a "behavior absent" red (or "module not yet created" for the banner), NOT a typo/import/harness error.
- The existing green invariant tests are untouched: `npx vitest run tests/unit/whatsapp/never-reply-regression.test.ts tests/unit/estimate/graph-neutrality.test.ts tests/unit/estimate/auto-refine-isolation.test.ts` stays green.
</verification>

<success_criteria>
- 4 new test files exist with the assertions above.
- @testing-library/react confirmed present (no install needed).
- Each downstream requirement (HARD-05/06/07) now has a failing automated check ready for its implementation plan.
</success_criteria>

<output>
After completion, create `.planning/phases/102-resilience-batch-autorefine-ttl/102-00-SUMMARY.md`.
</output>
