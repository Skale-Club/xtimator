---
phase: 183-pdf-parity-content
plan: 04
type: execute
wave: 2
depends_on: ["183-01", "183-03"]
files_modified:
  - components/pdf/shared/pdf-header.tsx
  - components/pdf/shared/pdf-info-grid.tsx
  - components/pdf/shared/pdf-footer.tsx
  - components/pdf/shared/pdf-title-banner.tsx
  - components/pdf/shared/pdf-section-block.tsx
  - components/pdf/shared/pdf-terms-section.tsx
  - components/pdf/estimate-pdf.tsx
  - components/pdf/estimate-pdf-modern.tsx
  - tests/unit/pdf/estimate-pdf-banner-fill.test.tsx
autonomous: true
requirements: [PDFPAR-01, ENGINE-03]

must_haves:
  truths:
    - "Classic PDF's ESTIMATE title now has a solid brand-color fill banner, matching Classic webview's benchmark treatment (estimate-document.tsx:1476-1486) — closing the one verified visual gap Correction 1 identifies."
    - "Modern PDF's title and section headers remain fill-free (hairline/accent-only) — explicitly UNCHANGED, guarding against Pitfall 1 (the CONTEXT.md wording that could mislead a naive read into filling Modern too)."
    - "Header, info-grid, footer, section-header+table, and terms JSX are defined ONCE under components/pdf/shared/ and consumed by both templates — no more byte-duplicated structure for these 5 regions."
  artifacts:
    - path: "components/pdf/shared/pdf-header.tsx"
      provides: "Shared company header block (logo, name, contact line, address, language badge)"
    - path: "components/pdf/shared/pdf-section-block.tsx"
      provides: "Shared section header + table header + item rows + subtotal, with a fill-vs-hairline header treatment driven by ESTIMATE_DESIGN_TOKENS.solidHeaderFill"
    - path: "tests/unit/pdf/estimate-pdf-banner-fill.test.tsx"
      provides: "Positive assertion (Classic title has backgroundColor: brandColor) + negative assertion (Modern title/section headers have no backgroundColor) — the Pitfall-1 guard rail"
  key_links:
    - from: "components/pdf/estimate-pdf.tsx"
      to: "components/pdf/shared/pdf-section-block.tsx"
      via: "import + <PdfSectionBlock ... /> replacing the inline section JSX"
      pattern: "from ['\"]\\./shared/pdf-section-block['\"]|from ['\"]@/components/pdf/shared/pdf-section-block['\"]"
    - from: "components/pdf/shared/pdf-section-block.tsx"
      to: "lib/estimate/document/tokens.ts"
      via: "reads tokens.solidHeaderFill to decide whether to apply a backgroundColor fill"
      pattern: "solidHeaderFill"
---

<objective>
Extract the 5 structurally-identical (or near-identical, differing only in style values) regions shared by `estimate-pdf.tsx` (Classic) and `estimate-pdf-modern.tsx` (Modern) into `components/pdf/shared/*`: header, info grid, footer, section block (header+table+rows+subtotal), and terms. While doing so, fix the ONE verified real gap in PDFPAR-01's parity checklist — Classic PDF's ESTIMATE title needs a solid `backgroundColor: brandColor` banner fill (today it's text-only), matching Classic webview's benchmark. Modern's title/section headers get NO fill change — they already match Modern webview's hairline design.

Purpose: Completes the structural half of ENGINE-03 (Phase 182 delivered only the font-family token layer; this is the "de-duplicate the ~860-line PDF template pair" part explicitly carried over). This is Wave 2 — depends on Plan 183-01's baseline-order regression anchor and Plan 183-03's font/tokens groundwork (`solidHeaderFill`) already existing.
Output: 6 new shared component files + both PDF templates rewritten to compose them for these 5 regions + a banner-fill regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-pdf-parity-content/183-CONTEXT.md
@.planning/phases/183-pdf-parity-content/183-RESEARCH.md

@components/pdf/estimate-pdf.tsx
@components/pdf/estimate-pdf-modern.tsx
@lib/estimate/document/tokens.ts
@tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
@tests/unit/estimate/fixtures/document-fixtures.ts
</context>

<interfaces>
<!-- ESTIMATE_DESIGN_TOKENS as of Plan 183-03 (lib/estimate/document/tokens.ts) -->
```typescript
export interface EstimateDesignTokens {
  fontFamily: string
  fontFamilyBold: string
  solidHeaderFill: boolean
}
export const ESTIMATE_DESIGN_TOKENS: Record<EstimateTemplateId, EstimateDesignTokens> = {
  classic: { fontFamily: 'Inter', fontFamilyBold: 'Inter-Bold', solidHeaderFill: true },
  modern: { fontFamily: 'Lora', fontFamilyBold: 'Lora-Bold', solidHeaderFill: false },
}
```

<!-- Both templates already compute these locals identically before their JSX return (estimate-pdf.tsx:316-334, estimate-pdf-modern.tsx:328-349) — the shared components should accept them as props, not recompute -->
```typescript
const brandColor = company.brand_primary_color ?? SYSTEM_COLORS.primary
const brandText = ensureReadableOnWhite(brandColor)   // brand color as text-on-white
const brandOnFill = readableTextColor(brandColor)     // fixed fg over a brand fill
const companyAddress = formatAddress(company)
const L = PDF_LABELS[language] ?? PDF_LABELS.en
const langLabel = LANG_INDICATOR[language] ?? 'EN'
```

<!-- CURRENT Classic title JSX (estimate-pdf.tsx:409-412) — the ONE verified gap -->
```tsx
<Text style={[styles.estimateTitle, { color: brandText }]}>{L.estimate}</Text>
```
`styles.estimateTitle` (estimate-pdf.tsx:135-140): `{ fontSize: 24, fontFamily: ...fontFamilyBold, marginBottom: 20, textAlign: 'center' }` — NO backgroundColor, NO padding for a banner look. Target: match Classic webview's `estimate-document.tsx:1476-1486` — `py-6 px-6 sm:px-10 text-center` with `backgroundColor: brandColor` on the wrapping block and `color: brandOnFill` (not `brandText`) on the heading text once it sits on a filled background (mirrors the section-header pattern already in the same file, `estimate-pdf.tsx:487-494`, which correctly uses `brandOnFill` for text over a brand fill).

<!-- CURRENT Modern title JSX (estimate-pdf-modern.tsx:419-423) — LEAVE THIS TREATMENT AS-IS, just relocate into the shared component with solidFill=false -->
```tsx
<Text style={[styles.estimateTitle, { color: brandText }]}>{L.estimate}</Text>
<View style={[styles.estimateTitleRule, { borderBottomColor: brandColor }]} />
```

<!-- CURRENT section-header divergence this MUST preserve as a per-template prop, not flatten (Correction 1) -->
Classic (estimate-pdf.tsx:487-494): `<View style={[styles.sectionHeader, { backgroundColor: brandColor }]}><Text style={[styles.sectionTitle, { color: brandOnFill }]}>{section.title}</Text></View>`
Modern (estimate-pdf-modern.tsx:499-501): `<View style={styles.sectionHeader}><Text style={styles.sectionTitle}>{section.title}</Text></View>` — styles.sectionHeader already has a static `borderBottomWidth`/`borderBottomColor`, no fill, no dynamic color override.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PdfHeader + PdfInfoGrid + PdfFooter — extract and wire into both templates</name>
  <files>components/pdf/shared/pdf-header.tsx, components/pdf/shared/pdf-info-grid.tsx, components/pdf/shared/pdf-footer.tsx, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx</files>
  <read_first>
    - components/pdf/estimate-pdf.tsx:338-407 (header JSX) and :414-469 (info-grid JSX) and :696-703 (footer) — the Classic source to extract FROM.
    - components/pdf/estimate-pdf-modern.tsx:354-417 (header) and :425-480 (info-grid) and :697-704 (footer) — the Modern source, structurally identical to Classic's, differing only in style-object VALUES (already isolated in each file's own local `styles` object) and the absence of a dynamic `color`/`borderBottomColor` override on a couple of Text/View nodes (Modern's header/companyName have no brandColor-derived override; Classic's do).
  </read_first>
  <action>
    Create 3 new files under `components/pdf/shared/`:

    1. `pdf-header.tsx` — exports `PdfHeader`, a component rendering the exact JSX structure at `estimate-pdf.tsx:340-407` (company name + link, phone/email/website joined with `  |  `, address, language badge, logo). Props: `{ company: CompanyInfo; brandText?: string; headerBorderColor?: string; companyNameColor?: string; langLabel: string; styles: { header, headerLeft, headerRight, logo, companyName, companyContact, contactLink, nameLink, langBadge } }` where `styles` is the CALLER's own local `StyleSheet.create()` output (each template keeps its own style VALUES — only the JSX structure is shared, per CONTEXT.md's discretion note). `headerBorderColor`/`companyNameColor` are optional dynamic overrides: Classic passes `brandColor` and `brandText` respectively (matching its current `style={[styles.header, { borderBottomColor: brandColor }]}` and `style={[styles.companyName, { color: brandText }]}`); Modern passes neither (undefined — its header/companyName styles are fully static already, no per-render override).

    2. `pdf-info-grid.tsx` — exports `PdfInfoGrid`, rendering the exact JSX at `estimate-pdf.tsx:415-469` (Project block + conditional Bill To block). Props: `{ L: DocumentLabels; projectName: string; projectType: string | null; estimate: { estimate_date, created_at, estimate_number, estimate_seq }; client: ClientInfo | null; fmtDate: (s: string) => string; styles: { infoRow, infoBlock, infoLabel, infoValue, infoValueLink }; clientNameFontFamily: string }` (the last prop covers Classic's `fontFamily: ESTIMATE_DESIGN_TOKENS.classic.fontFamilyBold` inline override on the client-name Text — Modern has the identical override, just with its own token value, so keep it a prop, not hardcoded).

    3. `pdf-footer.tsx` — exports `PdfFooter`, rendering the exact `<Text style={styles.footer} fixed render={...}>` at `estimate-pdf.tsx:696-703`. Props: `{ styles: { footer: object }; L: DocumentLabels }`.

    Wire all 3 into BOTH `components/pdf/estimate-pdf.tsx` and `components/pdf/estimate-pdf-modern.tsx`: import from `./shared/pdf-header`, `./shared/pdf-info-grid`, `./shared/pdf-footer`; replace the corresponding inline JSX blocks with `<PdfHeader .../>`, `<PdfInfoGrid .../>`, `<PdfFooter .../>`, passing each template's own `styles.*` sub-objects and computed locals (`brandColor`, `brandText`, `langLabel`, etc. — already computed in both files, see `<interfaces>`). Delete the now-dead inline JSX (do not leave commented-out code). Each template's own `StyleSheet.create()` block stays — only the JSX moves out.
  </action>
  <acceptance_criteria>
    - `grep -n "export function PdfHeader\|export const PdfHeader" components/pdf/shared/pdf-header.tsx` matches.
    - `grep -n "export function PdfInfoGrid\|export const PdfInfoGrid" components/pdf/shared/pdf-info-grid.tsx` matches.
    - `grep -n "export function PdfFooter\|export const PdfFooter" components/pdf/shared/pdf-footer.tsx` matches.
    - `grep -n "PdfHeader\|PdfInfoGrid\|PdfFooter" components/pdf/estimate-pdf.tsx` matches all 3.
    - `grep -n "PdfHeader\|PdfInfoGrid\|PdfFooter" components/pdf/estimate-pdf-modern.tsx` matches all 3.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-baseline-order.test.tsx tests/unit/pdf</automated>
  </verify>
  <done>PdfHeader/PdfInfoGrid/PdfFooter exist, both templates import and render them instead of inline JSX, and the Plan-183-01 baseline text-order test AND all existing PDF totals/cross-surface tests still pass with zero output change.</done>
</task>

<task type="auto">
  <name>Task 2: PdfTitleBanner + PdfSectionBlock (Classic banner fix, Correction 1) — extract and wire</name>
  <files>components/pdf/shared/pdf-title-banner.tsx, components/pdf/shared/pdf-section-block.tsx, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx, tests/unit/pdf/estimate-pdf-banner-fill.test.tsx</files>
  <read_first>
    - `<interfaces>` above (Classic/Modern title + section-header divergence, already extracted verbatim from both files).
    - components/pdf/estimate-pdf.tsx:479-553 (full section-with-items-and-subtotal JSX) and estimate-pdf-modern.tsx:490-559 (Modern's equivalent) — the source for `PdfSectionBlock`.
    - components/workspace/estimate/estimate-document.tsx:1475-1486 (Classic webview's title banner — the exact benchmark this task's Classic PDF fix targets: `py-6 px-6 sm:px-10 text-center` block with `backgroundColor: brandColor`, heading text colored `brandOnFill`).
  </read_first>
  <action>
    1. Create `components/pdf/shared/pdf-title-banner.tsx` — exports `PdfTitleBanner`. Props: `{ label: string; solidFill: boolean; brandColor: string; brandText: string; brandOnFill: string; styles: { estimateTitle: object; estimateTitleRule?: object } }`.
       - When `solidFill` is `true` (Classic): render a `<View style={{ backgroundColor: brandColor, paddingVertical: 16, paddingHorizontal: 24, marginBottom: 20 }}>` wrapping a `<Text style={[styles.estimateTitle, { color: brandOnFill, marginBottom: 0 }]}>{label}</Text>` — i.e. THIS is the fix: Classic's title gains a solid brand-fill banner (today it has neither the View wrapper nor the fill — see `<interfaces>`'s "CURRENT Classic title JSX").
       - When `solidFill` is `false` (Modern): render EXACTLY today's Modern JSX unchanged — `<Text style={[styles.estimateTitle, { color: brandText }]}>{label}</Text>` followed by `<View style={[styles.estimateTitleRule, { borderBottomColor: brandColor }]} />` (only rendered if `styles.estimateTitleRule` is provided). Do NOT add a `backgroundColor` anywhere in this branch — this is the Pitfall-1 guard.
       - Drive `solidFill` at each call site from `ESTIMATE_DESIGN_TOKENS.<template>.solidHeaderFill` (Plan 183-03).

    2. Create `components/pdf/shared/pdf-section-block.tsx` — exports `PdfSectionBlock`, covering section header + table header + item rows (with the existing `idx % 2 === 1 ? styles.tableRowAlt : {}` zebra logic, unchanged — see estimate-pdf.tsx:517-541) + section subtotal. Props include `{ section: DocumentSection; solidFill: boolean; brandColor: string; brandOnFill: string; L: DocumentLabels; fmt: (v: number) => string; styles: {...the full existing per-template style keys: sectionHeader, sectionTitle, tableHeader, tableRow, tableRowAlt, colDescription, colQty, colUnit, colUnitPrice, colTotal, tableHeaderText, tableCellText, sectionSubtotal, sectionSubtotalLabel, sectionSubtotalValue} }`. Internally: when `solidFill`, apply `{ backgroundColor: brandColor }` to the section-header View and `{ color: brandOnFill }` to its Text (Classic's current behavior, UNCHANGED — this was already correct, see `<interfaces>`); when not `solidFill`, render the header View/Text with no dynamic override at all (Modern's current behavior, UNCHANGED).

    3. Wire both into BOTH templates: replace `estimate-pdf.tsx`'s inline title (`:409-412`) with `<PdfTitleBanner label={L.estimate} solidFill={ESTIMATE_DESIGN_TOKENS.classic.solidHeaderFill} brandColor={brandColor} brandText={brandText} brandOnFill={brandOnFill} styles={{ estimateTitle: styles.estimateTitle }} />`, and its section-mapping JSX (`:486-553`) with a `.map((section) => <PdfSectionBlock key={section.id} section={section} solidFill={ESTIMATE_DESIGN_TOKENS.classic.solidHeaderFill} .../>)`. Mirror for `estimate-pdf-modern.tsx` with `ESTIMATE_DESIGN_TOKENS.modern.solidHeaderFill` (`false`) and its own `styles.estimateTitleRule`.

    4. Write `tests/unit/pdf/estimate-pdf-banner-fill.test.tsx` — call `EstimatePDF({...})`/`EstimatePDFModern({...})` directly (using `buildFixtureEstimate`/`FIXTURE_COMPANY` from Plan 183-01's fixture module), walk the returned element tree WITHOUT the text-only walker this time — write a small local recursive walker (or extend `_pdf-text-walker.ts` with a `collectStyledNodes` variant if that's cleaner) that finds the `estimateTitle`-styled `<Text>`/its wrapping `<View>` and asserts:
       - Classic: SOME ancestor View in the title's render output has a resolved `style` array/object containing `backgroundColor: '#2563eb'` (the fixture's `brand_primary_color`).
       - Modern: NO node in the title's render output (walk the whole tree, not just the title) has a `style` containing a `backgroundColor` matching the brand color — i.e. assert the ABSENCE of any brand-fill, this is the explicit Pitfall-1 negative guard the plan's must_haves require.
  </action>
  <acceptance_criteria>
    - `grep -n "export function PdfTitleBanner\|export const PdfTitleBanner" components/pdf/shared/pdf-title-banner.tsx` matches.
    - `grep -n "export function PdfSectionBlock\|export const PdfSectionBlock" components/pdf/shared/pdf-section-block.tsx` matches.
    - `grep -n "solidHeaderFill" components/pdf/estimate-pdf.tsx components/pdf/estimate-pdf-modern.tsx` matches both files.
    - `grep -n "backgroundColor: brandColor" components/pdf/shared/pdf-title-banner.tsx` matches (only inside the `solidFill` branch).
    - `grep -c "backgroundColor" components/pdf/shared/pdf-title-banner.tsx` — manually confirm (via the surrounding read) that the ONLY `backgroundColor` reference is gated behind `solidFill` — no unconditional fill.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-banner-fill.test.tsx tests/unit/pdf/estimate-pdf-baseline-order.test.tsx tests/unit/pdf</automated>
  </verify>
  <done>Classic PDF's title has a solid brand-fill banner (new, matches Classic webview benchmark); Modern PDF's title/section headers are pixel-behaviorally unchanged (no fill); both templates' section blocks render through the one shared PdfSectionBlock; the new banner-fill test asserts both the positive (Classic) and negative (Modern) cases; the baseline-order test still passes since text CONTENT/order is unchanged even though the Classic title now has a wrapping View.</done>
</task>

<task type="auto">
  <name>Task 3: PdfTermsSection — extract and wire into both templates</name>
  <files>components/pdf/shared/pdf-terms-section.tsx, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx</files>
  <read_first>
    - components/pdf/estimate-pdf.tsx:621-669 (the full terms-section JSX: outer visibility gate, Estimate Terms / Payment Terms / Timeline / Warranty / Notes, each a `<Text termsTitle>` + `<Text termsText>` pair) and estimate-pdf-modern.tsx:622-670 (Modern's structurally identical equivalent).
  </read_first>
  <action>
    Create `components/pdf/shared/pdf-terms-section.tsx` — exports `PdfTermsSection`, taking `{ company: { estimate_terms_enabled?, estimate_terms_text? }; estimate: { payment_terms, warranty_terms, timeline, notes }; resolvedSettings: ResolvedPresentationSettings; L: DocumentLabels; brandText?: string; styles: { termsSection, termsTitle, termsText } }` and reproducing the exact outer-visibility-gate + 5-block conditional structure from the read_first source (including the `isSectionVisible(resolvedSettings, ...)` calls — import `isSectionVisible` from `@/lib/estimate/presentation-settings` inside this new shared file; it is already framework-free-safe to import from a `components/pdf/shared/*` file per 183-RESEARCH.md's note that this directory is a react-pdf-only interpreter layer, not bound by the `lib/estimate/document/*` framework-free guard). Classic passes `brandText` (used to color the "Estimate Terms" title only, matching `estimate-pdf.tsx:632`); Modern passes its own `brandText` too (it has the identical override at `estimate-pdf-modern.tsx:633`) — so this prop is NOT a Classic-only divergence, both pass it, keep it required, not optional.

    Wire into both templates, replacing the inline block with `<PdfTermsSection .../>`.
  </action>
  <acceptance_criteria>
    - `grep -n "export function PdfTermsSection\|export const PdfTermsSection" components/pdf/shared/pdf-terms-section.tsx` matches.
    - `grep -n "PdfTermsSection" components/pdf/estimate-pdf.tsx components/pdf/estimate-pdf-modern.tsx` matches both files.
    - `grep -n "isSectionVisible" components/pdf/shared/pdf-terms-section.tsx` matches.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf tests/unit/pdf/estimate-pdf-baseline-order.test.tsx && npx tsc -p tsconfig.ci.json --noEmit</automated>
  </verify>
  <done>PdfTermsSection exists, both templates use it, all existing PDF/cross-surface/baseline tests stay green, tsc clean.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/pdf tests/unit/estimate/document-engine-boundary.test.ts && npx tsc -p tsconfig.ci.json --noEmit`. All green — this is the regression proof that the extraction changed structure/organization without changing rendered output (except the one intentional Classic banner-fill addition, covered by its own dedicated test). Confirm `components/pdf/estimate-pdf.tsx` and `components/pdf/estimate-pdf-modern.tsx` each shrank meaningfully (both were ~700-710 lines; after this plan they should be noticeably shorter, with the removed JSX now living in `components/pdf/shared/*`).

**Wave-2 boundary note (plan-checker WARNING 6):** No task in this plan runs `tests/unit/estimate/presentation-settings-cross-surface.test.tsx` — that test RTL-renders `estimate-document.tsx`/`estimate-document-modern.tsx`, which Plan 183-05 is concurrently editing in this SAME wave (Wave 2). Running it mid-wave against a sibling plan's partially-edited files would be flaky and non-authoritative. The orchestrator's full-suite run at the wave-2 boundary (after BOTH 183-04 and 183-05 complete — per 183-VALIDATION.md's Sampling Rate, "After every plan wave: Run the full suite command") is the authoritative check for this test, not any individual task in either plan.
</verification>

<success_criteria>
- 5 shared components exist under components/pdf/shared/, consumed by both templates for header/info-grid/footer/title/section/terms.
- Classic PDF's ESTIMATE title has a solid brand-fill banner (closes the one verified PDFPAR-01 gap).
- Modern PDF's title/section headers remain unconditionally fill-free (Pitfall-1 guarded by a dedicated negative test).
- All pre-existing PDF/cross-surface/baseline tests are unchanged and green.
</success_criteria>

<output>
After completion, create `.planning/phases/183-pdf-parity-content/183-04-SUMMARY.md`
</output>
