---
phase: 183-pdf-parity-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/estimate/fixtures/document-fixtures.ts
  - tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
autonomous: true
requirements: [PDFPAR-01, PDFPAR-02, PDFPAR-03, ENGINE-03]

must_haves:
  truths:
    - "A shared, importable fixture exists that both PDF-direct-call tests and webview RTL tests can use to build a signed, captioned-photo estimate without re-declaring the object literal in every test file."
    - "The CURRENT (pre-refactor) text-content order of both PDF templates is captured in a committed test, so Wave 2/3 template recomposition has a concrete regression anchor to diff against."
  artifacts:
    - path: "tests/unit/estimate/fixtures/document-fixtures.ts"
      provides: "Shared fixture builders + constants (estimate, company, signature, captioned photos) reused by every later signature/caption test in this phase"
    - path: "tests/unit/pdf/estimate-pdf-baseline-order.test.tsx"
      provides: "Pre-refactor text-node order assertions for Classic + Modern PDF (the Wave-0 regression anchor named in 183-VALIDATION.md)"
  key_links:
    - from: "tests/unit/pdf/estimate-pdf-baseline-order.test.tsx"
      to: "tests/unit/estimate/fixtures/document-fixtures.ts"
      via: "import { buildFixtureEstimate, FIXTURE_COMPANY } from '../estimate/fixtures/document-fixtures'"
      pattern: "from ['\"].*fixtures/document-fixtures['\"]"
---

<objective>
Lay the Wave-0 regression groundwork the rest of Phase 183 depends on: (1) ONE shared fixture module (estimate + company + a real base64-PNG signature + captioned/uncaptioned photos) that every later task in this phase reuses instead of re-declaring fixtures per test file, and (2) a committed test that locks in TODAY's (pre-refactor) text-content order for both PDF templates, so the Wave 2/3 structural recomposition of `estimate-pdf.tsx`/`estimate-pdf-modern.tsx` has a concrete "did I break anything" anchor.

Purpose: Phase 183 restructures ~860 combined lines of PDF template JSX (ENGINE-03) and adds two net-new cross-surface blocks (signature, captions). Per 183-VALIDATION.md's Wave 0 Requirements, the baseline must exist BEFORE any structural change lands. This plan produces zero behavior change — test/fixture files only.
Output: A reusable fixture module + a passing baseline-order test capturing the current PDF structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-pdf-parity-content/183-CONTEXT.md
@.planning/phases/183-pdf-parity-content/183-RESEARCH.md
@.planning/phases/183-pdf-parity-content/183-VALIDATION.md

@tests/unit/estimate/_pdf-text-walker.ts
@tests/unit/pdf/estimate-pdf-totals.test.tsx
@tests/unit/estimate/presentation-settings-cross-surface.test.tsx
@components/pdf/estimate-pdf.tsx
@components/pdf/estimate-pdf-modern.tsx
</context>

<interfaces>
<!-- Existing shared text-walker (reuse, do not reinvent) -->
From tests/unit/estimate/_pdf-text-walker.ts:
```typescript
export function flattenText(children: ReactNode): string
export function collectTextNodes(node: ReactElement | ReactNode, out: string[]): void
```

<!-- EstimatePDF/EstimatePDFModern props (components/pdf/estimate-pdf.tsx:60-72) — identical shape in both files -->
```typescript
export interface EstimatePDFProps {
  estimate: EstimateWithSections
  company: CompanyInfo   // { name, owner_name, phone, email, website, address, city, state, zip, logo_url, brand_primary_color, estimate_terms_enabled?, estimate_terms_text? }
  client: ClientInfo | null
  projectName: string
  projectType: string | null
  language?: EstimateLanguage
  preparedBy?: string | null
  attachedPhotos?: { url: string; caption: string | null }[]
}
```

<!-- EstimateWithSections fields the fixture must supply (lib/queries/estimate.ts:6-93) -->
Required scalar fields: id, project_id, company_id, currency_code, version, estimate_seq, estimate_number, estimate_date, is_current, share_token, public_slug_token, status, language, summary, notes, timeline, payment_terms, warranty_terms, subtotal, discount_type, discount_value, discount_amount, tax_rate, tax_amount, total, deposit_type, deposit_value, balance_due, sent_at, viewed_at, responded_at, client_response, created_at, updated_at, presentation_settings, sections, attachedPhotos.

<!-- formatDate (lib/estimate/document/format.ts:33-45) — the ONE date formatter every surface uses. Any test asserting on the fixture's signed date MUST compute its expectation by calling this function, never by hardcoding a formatted string (the repo has a documented history of timezone-fragile hardcoded-date assertions causing red-lock incidents — see CLAUDE.md's CI-gates note). -->
```typescript
export function formatDate(dateStr: string, lang: EstimateLanguage = 'en'): string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Shared document fixture module</name>
  <files>tests/unit/estimate/fixtures/document-fixtures.ts</files>
  <read_first>
    - tests/unit/pdf/estimate-pdf-totals.test.tsx:80-121 — the `baseEstimate(overrides)` pattern this fixture generalizes (same field set, same style of overrides param).
    - tests/unit/estimate/presentation-settings-cross-surface.test.tsx:27-136 — `baseEstimate()` + `toDocumentData()` + `COMPANY` — the pattern for converting one EstimateWithSections-shaped fixture into the flatter `EstimateDocumentData` shape webview components take. This plan's `toFixtureDocumentData()` mirrors this exactly, PLUS threads a `signature` field.
    - lib/estimate/document/model.ts:76-81 — `DocumentPhoto` (`id`, `storage_path`, `caption`, `url?`) — the shape captioned-photo fixtures must match.
    - components/share/signature-pad.tsx:88 — confirms `signature_data` is a `canvas.toDataURL('image/png')` full data URL (`data:image/png;base64,...`), 600×160px canvas (3.75:1 aspect) — the fixture's signature image constant must be a REAL valid PNG data URL (not a placeholder string), since a later Wave-3 task needs a live `renderToBuffer` smoke test against it.
    - lib/estimate/document/format.ts:33-45 (`formatDate`) — `SIGNATURE_FIXTURE.signedAt` below is deliberately DATE-ONLY (`'YYYY-MM-DD'`), not a timestamp, matching `formatDate`'s own local-midnight normalization contract (`format.ts:35-39`: a bare `YYYY-MM-DD` string parses as LOCAL midnight, avoiding the west-of-UTC off-by-one-day bug a timestamp string with an explicit UTC offset could otherwise trigger in CI). Every downstream task that asserts on the signed-date TEXT must compute its expectation via `formatDate(SIGNATURE_FIXTURE.signedAt, 'en')` — never a hardcoded formatted string.
  </read_first>
  <action>
    Create `tests/unit/estimate/fixtures/document-fixtures.ts` (new directory `tests/unit/estimate/fixtures/`). Export:

    1. `FIXTURE_COMPANY` — a `DocumentCompany`-shaped (and PDF `CompanyInfo`-compatible — same field set) const object: `{ name: 'Acme Renovations', owner_name: 'Jamie Lee', phone: '+15125550100', email: 'jamie@acme.test', website: 'https://acme.test', address: '123 Main St', city: 'Austin', state: 'TX', zip: '78701', logo_url: null, brand_primary_color: '#2563eb' }`.

    2. `buildFixtureEstimate(overrides: Record<string, unknown> = {}): Record<string, unknown>` — mirrors `estimate-pdf-totals.test.tsx`'s `baseEstimate()` shape exactly (see read_first) but richer: 2 sections (`'Labor'`, `'Materials'`) each with 1-2 items, `discount_type: 'percentage'`, `discount_value: 10`, `discount_amount: 150`, `tax_rate: 0.0825`, `tax_amount: ...`, `deposit_type: 'percent'`, `deposit_value: 30`, `balance_due: <total - deposit>`, `subtotal: 1500`, `total: <computed to stay internally consistent — do the arithmetic explicitly, do not rely on any engine import>`, `payment_terms: 'Net 30'`, `warranty_terms: '1 year labor'`, `attachedPhotos: []` (overridable), `presentation_settings: null`. Spread `...overrides` last so callers can override any field, matching the existing pattern.

    3. `toFixtureDocumentData(estimate: Record<string, unknown>): Record<string, unknown>` — mirrors `presentation-settings-cross-surface.test.tsx`'s `toDocumentData()` (see read_first) field-for-field, PLUS one new line: `signature: (estimate as { signature?: unknown }).signature ?? null,`. This lets the SAME fixture object drive both `EstimatePDF({ estimate, ... })` (PDF, direct EstimateWithSections shape) and `<EstimateDocument data={toFixtureDocumentData(estimate)} .../>` (webview, EstimateDocumentData shape) in later tasks/plans.

    4. `TINY_PNG_DATA_URL` — the constant string `'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+A8AAQUBAScY42YAAAAASUVORK5CYII='` (a well-known minimal valid 1×1 PNG). Add a code comment: "If a later renderToBuffer smoke test (Plan 183-06) throws on this constant, regenerate a fresh 1x1 PNG via `node -e \"...\"` or an online base64-PNG generator and replace this string — do not delete the smoke test."

    5. `SIGNATURE_FIXTURE` — `{ signerName: 'Jane Doe', signedAt: '2026-07-20', signatureDataUrl: TINY_PNG_DATA_URL }` — `signedAt` is DATE-ONLY on purpose (see read_first's timezone note — WARNING 8 in the checker review). Matches the `DocumentSignature` shape Plan 183-02 will add to `lib/estimate/document/model.ts` (do not import that type here yet — it doesn't exist until Plan 183-02 lands; keep this as a plain object literal so this file has zero dependency on not-yet-existing types). Add a code comment on this export: "signedAt is date-only (YYYY-MM-DD) so formatDate's local-midnight normalization keeps it deterministic across timezones/CI. Every test asserting the rendered signed-date TEXT must compute its expectation via formatDate(SIGNATURE_FIXTURE.signedAt, 'en') (or the relevant language) — never hardcode a formatted date string."

    6. `PHOTO_WITH_CAPTION` — `{ id: 'photo-1', storage_path: 'co-1/photo-1.jpg', caption: 'Front yard before demo', url: 'https://signed.example.test/photo-1.jpg' }`.

    7. `PHOTO_NO_CAPTION` — `{ id: 'photo-2', storage_path: 'co-1/photo-2.jpg', caption: null, url: 'https://signed.example.test/photo-2.jpg' }`.

    Zero framework imports (no `react`, no `@react-pdf/renderer`) — this is a plain data module, importable from both RTL webview tests and direct-call PDF tests.
  </action>
  <acceptance_criteria>
    - `grep -n "export function buildFixtureEstimate" tests/unit/estimate/fixtures/document-fixtures.ts` matches.
    - `grep -n "export function toFixtureDocumentData" tests/unit/estimate/fixtures/document-fixtures.ts` matches.
    - `grep -n "export const SIGNATURE_FIXTURE" tests/unit/estimate/fixtures/document-fixtures.ts` matches.
    - `grep -n "signedAt: '2026-07-20'" tests/unit/estimate/fixtures/document-fixtures.ts` matches (date-only, no time component).
    - `grep -n "export const PHOTO_WITH_CAPTION\|export const PHOTO_NO_CAPTION" tests/unit/estimate/fixtures/document-fixtures.ts` matches both.
    - `grep -n "from ['\"]react['\"]\|from ['\"]@react-pdf" tests/unit/estimate/fixtures/document-fixtures.ts` matches NOTHING (zero framework imports).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>File exists, exports all 7 named items, type-checks clean, has zero React/react-pdf imports, SIGNATURE_FIXTURE.signedAt is date-only.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-refactor PDF text-order baseline (Wave-0 regression anchor)</name>
  <files>tests/unit/pdf/estimate-pdf-baseline-order.test.tsx</files>
  <read_first>
    - components/pdf/estimate-pdf.tsx (full file, already read this session) — the CURRENT Classic PDF structure: header → title (`estimateTitle`, text-only, no fill — Correction 1) → info grid → summary → sections (brand-fill header) → totals (Subtotal→Discount→Tax→Total→Deposit→Balance Due) → terms → photos (bare `<Image>`, no caption) → prepared-by → footer.
    - components/pdf/estimate-pdf-modern.tsx (full file, already read this session) — the CURRENT Modern PDF structure: same order, hairline/no-fill title and section headers, hero grand total.
    - tests/unit/pdf/estimate-pdf-totals.test.tsx:22-78 — the `collectTextNodes`/`renderTexts` local-copy pattern this task follows, but importing the SHARED walker from `_pdf-text-walker.ts` instead of a local copy (per that file's own header comment: "shared helper... so both the classic + modern PDF tests AND the new cross-surface test share ONE implementation").
  </read_first>
  <action>
    Create `tests/unit/pdf/estimate-pdf-baseline-order.test.tsx`. Import `collectTextNodes` from `../estimate/_pdf-text-walker`, `buildFixtureEstimate`, `toFixtureDocumentData` is NOT needed here (PDF components take the raw `EstimateWithSections` shape directly), `FIXTURE_COMPANY` from `../estimate/fixtures/document-fixtures`. Import `EstimatePDF` and `EstimatePDFModern` default exports directly (call as plain functions, per the existing test convention — do NOT try to render to an actual PDF buffer here, this is a fast structural test).

    Build the fixture with `buildFixtureEstimate({})` (no `signature`, `attachedPhotos: []` — i.e. TODAY's shape, before Phase 183 adds anything). For EACH of Classic and Modern:
    1. Call the component function directly (mirrors `estimate-pdf-totals.test.tsx:53-78`'s `renderTexts` helper) with `{ estimate, company: FIXTURE_COMPANY, client: null, projectName: 'Baseline Test Project', projectType: null, language: 'en' }`.
    2. Walk the tree with `collectTextNodes`.
    3. Assert, using `.indexOf()`/`.lastIndexOf()` comparisons (NOT a full-array `toEqual` — matches this codebase's established convention, see `estimate-pdf-totals.test.tsx:147-155`), that today's milestone texts appear in this exact relative order: company name → `'ESTIMATE'` (the title label) → `'Project'` (info label) → `'Bill To'` is absent (client is null in this fixture — assert `texts).not.toContain('Bill To')`) → section title `'Labor'` → section title `'Materials'` → `'Subtotal'` → `'Discount'` → `'Tax'` → the grand total label (`'Total'`, via `lastIndexOf` since `'Total'` also appears as a column header) → `'Deposit'` → `'Balance Due'` → `'Payment Terms'` → `'Warranty'` (this fixture sets `warranty_terms`).
    4. Add ONE explicit negative assertion per template guarding Correction 1 / Pitfall 1: this test does NOT assert on `style` props (that's a structural-order test, not a style test — style assertions belong to Plan 183-05's dedicated banner-fill test) — skip style assertions here entirely; text-order only.
    5. Add a short header comment on the `describe` block: "PRE-REFACTOR BASELINE (Phase 183 Wave 0) — captures today's PDF text-content order before the components/pdf/shared/* extraction (Plan 183-04/183-06) and before the signature/caption/banner additions land. Plan 183-07 EXTENDS this same file with the new intentional post-refactor order — do not silently 'fix' a failure here without checking whether it's an intentional Phase 183 change."
  </action>
  <acceptance_criteria>
    - `grep -n "collectTextNodes" tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` matches (imported from `_pdf-text-walker`, not redeclared locally).
    - `grep -n "from '../estimate/fixtures/document-fixtures'" tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` matches.
    - `grep -n "PRE-REFACTOR BASELINE" tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` matches.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-baseline-order.test.tsx</automated>
  </verify>
  <done>New test file passes against the CURRENT (unmodified) estimate-pdf.tsx/estimate-pdf-modern.tsx, asserting today's text-content order for both templates.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/estimate/fixtures tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` — both tasks' output green. Run `npx tsc --noEmit` — no new type errors. No production file under `components/`, `lib/`, or `app/` is touched by this plan — confirm with `git status` showing only the two new test files.
</verification>

<success_criteria>
- `tests/unit/estimate/fixtures/document-fixtures.ts` exists and is importable by both PDF-direct-call and webview RTL test styles.
- `SIGNATURE_FIXTURE.signedAt` is date-only, and its doc comment directs downstream tests to compute expectations via `formatDate`, never hardcode a formatted string.
- `tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` passes against today's (pre-Phase-183) PDF templates and documents, in its own header comment, that Plan 183-07 will intentionally extend it.
- Zero production code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/183-pdf-parity-content/183-01-SUMMARY.md`
</output>
