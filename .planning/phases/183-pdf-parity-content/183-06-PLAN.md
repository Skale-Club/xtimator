---
phase: 183-pdf-parity-content
plan: 06
type: execute
wave: 3
depends_on: ["183-04", "183-02"]
files_modified:
  - components/pdf/shared/pdf-totals-block.tsx
  - components/pdf/shared/pdf-photo-grid.tsx
  - components/pdf/shared/pdf-signature-block.tsx
  - components/pdf/estimate-pdf.tsx
  - components/pdf/estimate-pdf-modern.tsx
  - lib/pdf/render-estimate-pdf.ts
  - tests/unit/pdf/estimate-pdf-signature.test.tsx
  - tests/unit/pdf/estimate-pdf-modern-signature.test.tsx
  - tests/unit/pdf/estimate-pdf-photo-captions.test.tsx
  - tests/unit/pdf/estimate-pdf-totals.test.tsx
  - tests/unit/pdf/estimate-pdf-modern-totals.test.tsx
autonomous: true
requirements: [PDFPAR-01, PDFPAR-02, PDFPAR-03, ENGINE-03]

must_haves:
  truths:
    - "A signed estimate's Classic and Modern PDF both render the signer's name, signed date, and a real signature <Image>, positioned between Terms and Photos."
    - "An unsigned estimate's PDF (both templates) has no signature block at all."
    - "A photo with a caption shows that caption as visible text beneath the photo, in both PDF templates' photo grids; a caption-less photo shows no caption text."
    - "Both PDF templates' discount-suffix display uses the shared isPercentageDiscount predicate instead of the inline '=== percentage' check."
    - "Classic's totals block stays a boxed row-list and Modern's stays a standalone hero total — the shared PdfTotalsBlock renders genuinely different JSX per variant, not one flattened layout (Pitfall 2)."
  artifacts:
    - path: "components/pdf/shared/pdf-signature-block.tsx"
      provides: "The one signature-block renderer both PDF templates mount, reading a data: URI straight into react-pdf's <Image>"
    - path: "components/pdf/shared/pdf-photo-grid.tsx"
      provides: "Shared photo grid with per-photo caption text, the one change point for PDFPAR-03 on the PDF side"
    - path: "components/pdf/shared/pdf-totals-block.tsx"
      provides: "variant='classic'|'modern' totals renderer preserving the boxed-vs-hero design distinction"
  key_links:
    - from: "components/pdf/estimate-pdf.tsx"
      to: "components/pdf/shared/pdf-signature-block.tsx"
      via: "<PdfSignatureBlock signature={signature} .../> mounted between PdfTermsSection and PdfPhotoGrid"
      pattern: "PdfSignatureBlock"
    - from: "lib/pdf/render-estimate-pdf.ts"
      to: "components/pdf/estimate-pdf.tsx (EstimatePDFProps.signature)"
      via: "createElement(PDFComponent, { ..., signature })"
      pattern: "signature,"
    - from: "components/pdf/shared/pdf-totals-block.tsx"
      to: "lib/estimate/discount-display.ts"
      via: "isPercentageDiscount(estimate.discount_type) replacing the inline === 'percentage' check"
      pattern: "isPercentageDiscount"
---

<objective>
Complete the ENGINE-03 shared-component extraction: totals block (Classic boxed vs Modern hero, preserved as a deliberate design variant, not flattened), photo grid (gaining PDFPAR-03 captions), and a net-new signature block (PDFPAR-02) — wired into both PDF templates via the already-threaded `signature` prop (Plan 183-02) and rendered between Terms and Photos, matching the webview's placement (Plan 183-05). Swap the remaining `discount_type === 'percentage'` inline checks in both PDF templates for the shared `isPercentageDiscount` predicate (the 2 webview call sites were already swapped in Plan 183-05).

Purpose: This is the second and final half of ENGINE-03's structural closure, and it lands PDFPAR-02/03 on the 2 PDF surfaces (Plan 183-05 already covered the 2 webview surfaces). Depends on Plan 183-04 (same 2 template files, sequential — cannot run in parallel with 183-04 since both restructure the same files) and Plan 183-02 (the `signature` prop + `isPercentageDiscount` predicate this plan consumes).
Output: 3 more shared components + both PDF templates fully composed from `components/pdf/shared/*` + signature/caption PDF tests, including a real renderToBuffer smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-pdf-parity-content/183-CONTEXT.md
@.planning/phases/183-pdf-parity-content/183-RESEARCH.md

@components/pdf/estimate-pdf.tsx
@components/pdf/estimate-pdf-modern.tsx
@lib/pdf/render-estimate-pdf.ts
@lib/estimate/discount-display.ts
@tests/unit/pdf/estimate-pdf-totals.test.tsx
@tests/unit/pdf/estimate-pdf-modern-totals.test.tsx
@tests/unit/estimate/fixtures/document-fixtures.ts
</context>

<interfaces>
<!-- After Plan 183-04, estimate-pdf.tsx/estimate-pdf-modern.tsx already compose PdfHeader/PdfInfoGrid/PdfFooter/PdfTitleBanner/PdfSectionBlock/PdfTermsSection.
     This plan's job is everything AFTER PdfTermsSection: totals (which actually renders BEFORE terms in document order — see below), photos, and the new signature block. -->

<!-- Actual document order in both templates (unchanged by 183-04): Header -> Title -> InfoGrid -> Summary -> Sections(loop) -> Totals -> Terms -> [NEW: Signature] -> Photos -> PreparedBy -> Footer -->

<!-- render-estimate-pdf.ts's signature prop, already threaded by Plan 183-02 (lib/pdf/render-estimate-pdf.ts) -->
```typescript
// EstimatePdfContext.signature: DocumentSignature | null   (already populated)
// renderEstimatePdf()'s createElement(PDFComponent, { ..., signature })   (already passed)
// EstimatePDFProps.signature does NOT exist yet -- Plan 183-02 deliberately did NOT touch
// either PDF component file (it threaded the prop via a type cast at the render-estimate-pdf.ts
// createElement call site instead, to avoid a same-wave same-file collision with Plan 183-03).
// THIS plan's Task 2 is the one that adds the real, permanent signature?: DocumentSignature | null
// field to EstimatePDFProps in both components.
```

<!-- isPercentageDiscount (Plan 183-02, lib/estimate/discount-display.ts) -->
```typescript
export function isPercentageDiscount(discountType: string | null): boolean
```

<!-- CURRENT discount-suffix check to replace, BOTH templates (estimate-pdf.tsx:569-571, estimate-pdf-modern.tsx:575-577) -->
```tsx
{estimate.discount_type === 'percentage' ? ` (${estimate.discount_value}%)` : ''}
```
becomes:
```tsx
{isPercentageDiscount(estimate.discount_type) ? ` (${estimate.discount_value}%)` : ''}
```

<!-- CURRENT Classic totals JSX (estimate-pdf.tsx:555-619) — boxed row-list, unchanged design -->
Subtotal row -> conditional Discount row -> conditional Tax row -> grandTotalRow (boxed, border-top 2px) -> conditional Deposit row -> conditional Balance Due row, all inside `totalsContainer > totalsBlock`.

<!-- CURRENT Modern totals JSX (estimate-pdf-modern.tsx:561-620) — hero grand total, unchanged design -->
Subtotal row -> conditional Discount row -> conditional Tax row -> conditional Deposit row -> conditional Balance Due row -> grandTotalBlock (standalone, large `fontSize: 30`, its own margin, NOT a bordered row) — note Modern's row order differs slightly (deposit/balance rows appear BEFORE the hero grandTotalBlock in the JSX, since the hero total is deliberately last/bottom) — preserve this EXACT existing order per template, do not unify it with Classic's.

<!-- CURRENT photo grid, both templates near-identical (estimate-pdf.tsx:671-686, estimate-pdf-modern.tsx:672-687) -->
```tsx
{isSectionVisible(resolvedSettings, 'photos') && attachedPhotos && attachedPhotos.length > 0 && (
  <View style={{ marginTop: 16 }} wrap={false}>
    <Text style={styles.termsTitle}>{L.photos}</Text>
    <View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 8, marginTop: 6 }}>
      {attachedPhotos.map((photo, i) => (
        <Image key={i} src={photo.url} style={{ width: 150, height: 150, objectFit: 'cover' }} />
      ))}
    </View>
  </View>
)}
```

<!-- Base64 signature <Image> (verified pattern, react-pdf.org/components + signature-pad.tsx:88 confirms format) -->
```tsx
{signature && (
  <View style={{ marginTop: 16 }} wrap={false}>
    <Text style={styles.termsTitle}>{L.signedBy}</Text>
    {/* eslint-disable-next-line jsx-a11y/alt-text */}
    <Image src={signature.signatureDataUrl} style={{ width: 150, height: 40, objectFit: 'contain' }} />
    <Text style={{ fontSize: 9, marginTop: 4 }}>{signature.signerName}</Text>
    <Text style={{ fontSize: 9, color: '#6b7280' }}>{fmtDate(signature.signedAt)}</Text>
  </View>
)}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PdfTotalsBlock (classic/modern variant) with isPercentageDiscount — extract and wire</name>
  <files>components/pdf/shared/pdf-totals-block.tsx, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx, tests/unit/pdf/estimate-pdf-totals.test.tsx, tests/unit/pdf/estimate-pdf-modern-totals.test.tsx</files>
  <read_first>
    - `<interfaces>` above (CURRENT Classic/Modern totals JSX, discount-suffix check) — the two source blocks to extract FROM, and the Pitfall-2 warning: these are genuinely different designs (boxed row-list vs standalone hero), not incidental style variance.
    - tests/unit/pdf/estimate-pdf-totals.test.tsx and estimate-pdf-modern-totals.test.tsx (both full files, already read this session) — these existing tests call `EstimatePDF({...})`/`EstimatePDFModern({...})` directly and assert Subtotal->Total->Deposit->Balance-Due text order. They MUST keep passing unmodified in behavior (only their own local `collectTextNodes`/`baseEstimate` helpers stay as-is — verify, do not assume — since this task changes internal composition, not the public props contract).
  </read_first>
  <action>
    1. Create `components/pdf/shared/pdf-totals-block.tsx` — exports `PdfTotalsBlock` with an explicit `variant: 'classic' | 'modern'` prop that branches to genuinely different JSX (per Pitfall 2 — do NOT build one JSX tree parameterized only by style tokens for this component). Props: `{ variant: 'classic' | 'modern'; estimate: { subtotal, discount_amount, discount_type, discount_value, tax_amount, tax_rate, total }; dep: DepositDisplay; L: DocumentLabels; brandText: string; fmt: (v: number) => string; styles: {...the full existing per-template totals style keys for that variant} }`. Internally:
       - `variant === 'classic'`: reproduce `estimate-pdf.tsx:555-619` exactly, replacing the discount-suffix line with `isPercentageDiscount(estimate.discount_type)` (import from `@/lib/estimate/discount-display`).
       - `variant === 'modern'`: reproduce `estimate-pdf-modern.tsx:561-620` exactly, same predicate swap. Preserve Modern's existing row ORDER (deposit/balance rows before the hero grandTotalBlock) exactly as today — do not reorder to match Classic.
    2. Wire into both templates: replace each template's inline totals JSX with `<PdfTotalsBlock variant="classic" .../>` / `<PdfTotalsBlock variant="modern" .../>`, passing each template's own `styles.*` totals keys and already-computed locals (`brandText`, `dep`, `fmt`, `L`).
    3. Run `estimate-pdf-totals.test.tsx` and `estimate-pdf-modern-totals.test.tsx` as-is (no edits expected — if either needs a change, it should ONLY be because the component's PUBLIC props contract (`estimate`, `company`, `client`, `projectName`, `projectType`, `language`) changed, which it must NOT have — these two files call the top-level `EstimatePDF`/`EstimatePDFModern` functions, not the new internal `PdfTotalsBlock` directly, so they should require zero changes; if they fail, that is a real regression to fix, not a test to relax).
  </action>
  <acceptance_criteria>
    - `grep -n "variant: 'classic' | 'modern'" components/pdf/shared/pdf-totals-block.tsx` matches (or equivalent literal union typing).
    - `grep -n "isPercentageDiscount" components/pdf/shared/pdf-totals-block.tsx` matches.
    - `grep -n "discount_type === 'percentage'" components/pdf/estimate-pdf.tsx components/pdf/estimate-pdf-modern.tsx components/pdf/shared/pdf-totals-block.tsx` matches NOTHING anywhere (the inline check is fully replaced, not duplicated alongside the predicate).
    - `grep -n "PdfTotalsBlock" components/pdf/estimate-pdf.tsx components/pdf/estimate-pdf-modern.tsx` matches both.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-totals.test.tsx tests/unit/pdf/estimate-pdf-modern-totals.test.tsx tests/unit/pdf/estimate-pdf-baseline-order.test.tsx</automated>
  </verify>
  <done>PdfTotalsBlock exists with a classic/modern branch preserving each template's distinct design; both templates use it; the discount-suffix check is now the shared predicate everywhere (zero remaining inline '=== percentage' checks in the PDF templates); all pre-existing/baseline totals tests pass unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PdfPhotoGrid (captions) + PdfSignatureBlock (net-new) — build and wire</name>
  <files>components/pdf/shared/pdf-photo-grid.tsx, components/pdf/shared/pdf-signature-block.tsx, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx, tests/unit/pdf/estimate-pdf-signature.test.tsx, tests/unit/pdf/estimate-pdf-modern-signature.test.tsx, tests/unit/pdf/estimate-pdf-photo-captions.test.tsx</files>
  <read_first>
    - `<interfaces>` above (CURRENT photo grid JSX in both templates; the signature-block JSX pattern).
    - components/pdf/estimate-pdf.tsx:60-72 (`EstimatePDFProps` — does NOT declare `signature` yet; Plan 183-02 deliberately left both PDF component files untouched, per its own BLOCKER-1 fix, threading the prop through a type cast at `render-estimate-pdf.ts`'s `createElement` call instead). This task is the one that adds the real `signature?: DocumentSignature | null` field to `EstimatePDFProps` in BOTH files, and destructures it in each component's function signature (`export default function EstimatePDF({ estimate, company, client, projectName, projectType, language, preparedBy, attachedPhotos, signature }: EstimatePDFProps)`).
    - 183-RESEARCH.md's "Signature data flow > `<Image>` base64 support (confirmed)" section — `signature.signatureDataUrl` is already a full `data:image/png;base64,...` string (from `estimate_signatures.signature_data`, threaded verbatim through Plan 183-02); pass it straight into react-pdf's `<Image src={...}>`, no transformation.
    - tests/unit/estimate/fixtures/document-fixtures.ts (Plan 183-01) — `SIGNATURE_FIXTURE` (uses `TINY_PNG_DATA_URL`, a real valid base64 PNG — required here since this task's tests must include at least one REAL `renderToBuffer` smoke test per 183-RESEARCH.md's confidence-raising recommendation, not just an element-tree walk).
  </read_first>
  <behavior>
    - `signature` prop present -> PDF element tree contains, between the Terms block's output and the Photos block's output: a `<Text>` node with the signer name, a `<Text>` node with the formatted signed date, and an `<Image>` node whose `src` starts with `data:image/png`.
    - `signature` prop absent/null -> no such nodes anywhere in the tree.
    - `renderToBuffer` on a `<Document>` rendering `EstimatePDF` with `SIGNATURE_FIXTURE`'s real base64 PNG succeeds and returns a non-empty `%PDF`-prefixed buffer (closes 183-RESEARCH.md's "no live render test was performed" caveat).
    - A photo with a caption produces a `<Text>` node with that caption's text; a caption-less photo produces no extra/empty `<Text>` node.
  </behavior>
  <action>
    1. Create `components/pdf/shared/pdf-photo-grid.tsx` — exports `PdfPhotoGrid`, reproducing the CURRENT photo-grid JSX from `<interfaces>` above, but wrapping EACH photo in its own `<View style={{ width: 150 }}>` containing the existing `<Image>` (unchanged size/style) PLUS a conditional caption:
       ```tsx
       <View key={i} style={{ width: 150 }}>
         {/* eslint-disable-next-line jsx-a11y/alt-text */}
         <Image src={photo.url} style={{ width: 150, height: 150, objectFit: 'cover' }} />
         {photo.caption && (
           <Text style={{ fontSize: 8, marginTop: 2, color: '#6b7280' }}>{photo.caption}</Text>
         )}
       </View>
       ```
       Props: `{ photos: { url: string; caption: string | null }[]; L: DocumentLabels; styles: { termsTitle: object } }` (reuses `termsTitle` for the "Photos" label, matching both templates' current reuse of that same style for this label).

    2. Create `components/pdf/shared/pdf-signature-block.tsx` — exports `PdfSignatureBlock`. Props: `{ signature: DocumentSignature | null; L: DocumentLabels; fmtDate: (s: string) => string; styles: { termsTitle: object } }`. Body: `if (!signature) return null;` then render exactly the "Base64 signature <Image>" JSX pattern from `<interfaces>` above (wrapped in `<View wrap={false}>` so react-pdf never splits the signature block across a page — this also directly satisfies Phase 184's later expectation that the signature is "defined as one atomic block kind").

    3. In BOTH `components/pdf/estimate-pdf.tsx` and `components/pdf/estimate-pdf-modern.tsx`: add `signature?: DocumentSignature | null` to the `EstimatePDFProps` interface (import `type { DocumentSignature } from '@/lib/estimate/document/model'` if not already present), then destructure `signature` in each component's function signature. This is the permanent declaration Plan 183-02 deliberately deferred (see read_first). Replace each template's inline photo-grid JSX with `<PdfPhotoGrid photos={attachedPhotos} .../>` (keep the existing outer `isSectionVisible(resolvedSettings, 'photos') && attachedPhotos && attachedPhotos.length > 0 &&` gate — that stays in the template file, only the inner grid JSX moves into the shared component). Insert `<PdfSignatureBlock signature={signature} .../>` as a NEW element positioned between the (now-shared) `<PdfTermsSection .../>` output and the photo-grid conditional block — it always attempts to render at that fixed position, but the component itself returns `null` when unsigned, matching the webview's Terms->Signature->Photos order from Plan 183-05.

    4. Write `tests/unit/pdf/estimate-pdf-signature.test.tsx` (Classic) and `tests/unit/pdf/estimate-pdf-modern-signature.test.tsx` (Modern). Import `buildFixtureEstimate`, `FIXTURE_COMPANY`, `SIGNATURE_FIXTURE`, `PHOTO_WITH_CAPTION`, `PHOTO_NO_CAPTION` from the Plan 183-01 fixture module, and `formatDate` from `@/lib/estimate/document/format` (never hardcode a formatted date string -- plan-checker WARNING 8). Use this EXACT verbatim call for the order/presence assertions (note `attachedPhotos` IS included here -- without it there is no `'Photos'` text to assert an order against, plan-checker WARNING 7):
       ```typescript
       const tree = EstimatePDF({
         estimate: buildFixtureEstimate({}),
         company: FIXTURE_COMPANY,
         client: null,
         projectName: 'Test',
         projectType: null,
         language: 'en',
         signature: SIGNATURE_FIXTURE,
         attachedPhotos: [PHOTO_WITH_CAPTION, PHOTO_NO_CAPTION],
       })
       ```
       Walk `tree` with `collectTextNodes` from `_pdf-text-walker.ts`. Assert: `SIGNATURE_FIXTURE.signerName` (`'Jane Doe'`) is present; `formatDate(SIGNATURE_FIXTURE.signedAt, 'en')` (computed, not hardcoded) is present; both occur AFTER `'Payment Terms'`/`'Warranty'` text and BEFORE `'Photos'` text (via `.indexOf()`/`.lastIndexOf()` comparisons). A second `it`: the SAME call shape but with `signature: null` and `attachedPhotos: []` -> assert the signer name text is absent. A THIRD `it`, in the Classic file only (`estimate-pdf-signature.test.tsx`), is the REAL render smoke test: `import { renderToBuffer } from '@react-pdf/renderer'`, `import { createElement } from 'react'`, `await renderToBuffer(createElement(EstimatePDF, { estimate: buildFixtureEstimate({}), company: FIXTURE_COMPANY, client: null, projectName: 'Test', projectType: null, language: 'en', signature: SIGNATURE_FIXTURE, attachedPhotos: [PHOTO_WITH_CAPTION, PHOTO_NO_CAPTION] }) as any)`, assert the resolved buffer is non-empty and starts with `%PDF`.

    5. Write `tests/unit/pdf/estimate-pdf-photo-captions.test.tsx`: for both `EstimatePDF`/`EstimatePDFModern`, pass `attachedPhotos: [PHOTO_WITH_CAPTION, PHOTO_NO_CAPTION]` (from the Plan 183-01 fixture module), walk with `collectTextNodes`, assert `PHOTO_WITH_CAPTION.caption` text is present exactly once, and assert no empty-string `<Text>` node is emitted for `PHOTO_NO_CAPTION` (compare the total count of empty-string entries in `texts` against a run with only `PHOTO_WITH_CAPTION` present — the count must be identical, proving the caption-less photo contributes zero extra text nodes).
  </action>
  <acceptance_criteria>
    - `grep -n "export function PdfPhotoGrid\|export const PdfPhotoGrid" components/pdf/shared/pdf-photo-grid.tsx` matches.
    - `grep -n "export function PdfSignatureBlock\|export const PdfSignatureBlock" components/pdf/shared/pdf-signature-block.tsx` matches.
    - `grep -n "if (!signature) return null" components/pdf/shared/pdf-signature-block.tsx` matches.
    - `grep -n "PdfSignatureBlock" components/pdf/estimate-pdf.tsx components/pdf/estimate-pdf-modern.tsx` matches both.
    - `grep -n "photo.caption &&" components/pdf/shared/pdf-photo-grid.tsx` matches.
    - `grep -n "renderToBuffer" tests/unit/pdf/estimate-pdf-signature.test.tsx` matches (the real smoke-test case).
    - `grep -n "estimate.signature" components/pdf/estimate-pdf.tsx components/pdf/estimate-pdf-modern.tsx components/pdf/shared/pdf-signature-block.tsx` matches NOTHING (plan-checker WARNING 7 guard -- `signature` is a top-level component PROP threaded from `render-estimate-pdf.ts`, never a field read off the `estimate` object).
    - `grep -n "formatDate(SIGNATURE_FIXTURE.signedAt" tests/unit/pdf/estimate-pdf-signature.test.tsx tests/unit/pdf/estimate-pdf-modern-signature.test.tsx` matches both files (computed date, not hardcoded -- plan-checker WARNING 8).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-signature.test.tsx tests/unit/pdf/estimate-pdf-modern-signature.test.tsx tests/unit/pdf/estimate-pdf-photo-captions.test.tsx tests/unit/pdf/estimate-pdf-baseline-order.test.tsx tests/unit/pdf tests/unit/estimate/presentation-settings-cross-surface.test.tsx && npx tsc -p tsconfig.ci.json --noEmit</automated>
  </verify>
  <done>PdfPhotoGrid renders captions conditionally; PdfSignatureBlock is net-new, gated on data presence, positioned between Terms and Photos in both templates; a real renderToBuffer smoke test with a genuine base64 PNG succeeds; all pre-existing PDF/cross-surface/baseline tests remain green; tsc clean.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/pdf tests/unit/estimate/presentation-settings-cross-surface.test.tsx tests/unit/estimate/document-engine-boundary.test.ts && npx tsc -p tsconfig.ci.json --noEmit`. All green. Confirm both PDF template files now compose ALL of `PdfHeader`/`PdfInfoGrid`/`PdfFooter`/`PdfTitleBanner`/`PdfSectionBlock`/`PdfTermsSection`/`PdfTotalsBlock`/`PdfPhotoGrid`/`PdfSignatureBlock` — i.e. `components/pdf/estimate-pdf.tsx` and `components/pdf/estimate-pdf-modern.tsx` are now thin compositions over `components/pdf/shared/*`, completing ENGINE-03.
</verification>

<success_criteria>
- Both PDF templates fully compose `components/pdf/shared/*` for every region identified in 183-RESEARCH.md's shared-component table — ENGINE-03 structurally complete.
- Signature block renders on both PDF templates when signed, absent when not, positioned between Terms and Photos (matching the webview's Plan 183-05 placement).
- Photo captions render on both PDF templates, no empty space when absent.
- Discount-suffix display uses the one shared predicate on all 4 surfaces (2 webview from Plan 183-05, 2 PDF from this plan).
- A real renderToBuffer call, using the registered fonts (Plan 183-03) and a real base64 signature image, succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/183-pdf-parity-content/183-06-SUMMARY.md`
</output>
