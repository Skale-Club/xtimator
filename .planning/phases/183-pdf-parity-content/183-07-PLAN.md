---
phase: 183-pdf-parity-content
plan: 07
type: execute
wave: 4
depends_on: ["183-01", "183-05", "183-06"]
files_modified:
  - tests/unit/estimate/document-signature-caption-cross-surface.test.tsx
  - tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
  - .planning/phases/183-pdf-parity-content/183-HUMAN-UAT.md
autonomous: false
requirements: [PDFPAR-01, PDFPAR-02, PDFPAR-03, ENGINE-03]

must_haves:
  truths:
    - "A signed, captioned-photo estimate shows identical signature facts (signer name, signed date, signature image) and identical caption text across all 4 document surfaces (Classic PDF, Modern PDF, Classic webview, Modern webview) — no surface silently omits or diverges."
    - "An unsigned estimate shows the signature block on NONE of the 4 surfaces; a caption-less photo shows no caption text on any surface."
    - "The Wave-0 pre-refactor baseline (Plan 183-01) is intentionally extended, not silently left stale, to reflect the post-refactor structure: Classic's title banner fill, and the new signature/caption nodes in their correct position."
    - "A human has visually confirmed a downloaded PDF (both templates, one signed + captioned sample) looks correct — the one verification category no automated test can cover (Pitfall 6: no StyleSheet-value test coverage exists)."
  artifacts:
    - path: "tests/unit/estimate/document-signature-caption-cross-surface.test.tsx"
      provides: "The definitive 4-surface (or 6, mirroring presentation-settings-cross-surface.test.tsx's pattern) parity proof for PDFPAR-02/03"
  key_links:
    - from: "tests/unit/estimate/document-signature-caption-cross-surface.test.tsx"
      to: "tests/unit/estimate/fixtures/document-fixtures.ts"
      via: "reuses buildFixtureEstimate/toFixtureDocumentData/SIGNATURE_FIXTURE/PHOTO_WITH_CAPTION across all render calls, one fixture driving every surface"
      pattern: "from '\\./fixtures/document-fixtures'"
---

<objective>
Close Phase 183 with the definitive cross-surface proof: one shared fixture (signed + captioned) driving all 4 document renderers, asserting the signature block and photo caption appear consistently everywhere they should and nowhere they shouldn't. Intentionally extend Plan 183-01's pre-refactor baseline test to the new post-refactor structure (Classic banner fill, signature, captions) — the one place this plan explicitly documents "this diff is expected, not a regression." Finish with a human visual-verification checkpoint for the one thing no automated test in this phase can prove: does a downloaded PDF actually look right.

Purpose: Every individual plan (183-04/05/06) proved ITS OWN surface(s) correct in isolation. This plan proves they agree with EACH OTHER, and that the Wave-0 regression anchor was updated deliberately rather than left to silently rot. This is the phase's final quality gate before /gsd:verify-work.
Output: A committed cross-surface parity test + an intentionally-updated baseline test + a human-confirmed visual check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-pdf-parity-content/183-CONTEXT.md
@.planning/phases/183-pdf-parity-content/183-RESEARCH.md

@tests/unit/estimate/presentation-settings-cross-surface.test.tsx
@tests/unit/estimate/fixtures/document-fixtures.ts
@tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
</context>

<interfaces>
<!-- The 6-surface renderAllSurfaces() pattern this plan's cross-surface test mirrors
     (tests/unit/estimate/presentation-settings-cross-surface.test.tsx:138-247, already read this session):
     Classic PDF (direct EstimatePDF call) / Modern PDF (direct EstimatePDFModern call) /
     Classic webview (RTL render of EstimateDocument mode="view") / Modern webview (RTL render
     of EstimateDocumentModern) / plain-text template / WhatsApp formatter. This plan's new test
     only needs the first 4 (signature/captions are DOCUMENT content — plain-text/WhatsApp
     summaries were never in scope for either PDFPAR-02 or PDFPAR-03, per CONTEXT.md's Deferred
     Ideas list not mentioning them and 183-RESEARCH.md's 4-surface framing throughout). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 4-surface signature + caption parity test</name>
  <files>tests/unit/estimate/document-signature-caption-cross-surface.test.tsx</files>
  <read_first>
    - tests/unit/estimate/presentation-settings-cross-surface.test.tsx (full file, already read this session) — the `renderAllSurfaces`-style pattern to follow: ONE fixture object, N render calls, one assertion block comparing all outputs.
    - tests/unit/estimate/fixtures/document-fixtures.ts (Plan 183-01) — `buildFixtureEstimate`, `toFixtureDocumentData`, `FIXTURE_COMPANY`, `SIGNATURE_FIXTURE`, `PHOTO_WITH_CAPTION`, `PHOTO_NO_CAPTION`.
    - tests/unit/pdf/estimate-pdf-signature.test.tsx, tests/unit/pdf/estimate-pdf-modern-signature.test.tsx, tests/unit/estimate/document-signature-view.test.tsx (Plans 183-06/183-05) — this new test does NOT duplicate their per-surface depth; it asserts CONSISTENCY across surfaces using the same fixture, a distinct concern from any single surface's own correctness.
  </read_first>
  <action>
    Create `tests/unit/estimate/document-signature-caption-cross-surface.test.tsx`. Import `buildFixtureEstimate`, `toFixtureDocumentData`, `FIXTURE_COMPANY`, `SIGNATURE_FIXTURE`, `PHOTO_WITH_CAPTION`, `PHOTO_NO_CAPTION` from the Plan 183-01 fixture module, `formatDate` from `@/lib/estimate/document/format`, `collectTextNodes` from `_pdf-text-walker.ts`, and `render`/`screen` from `@testing-library/react`. Build one fixture: `const estimate = buildFixtureEstimate({ signature: SIGNATURE_FIXTURE, attachedPhotos: [PHOTO_WITH_CAPTION, PHOTO_NO_CAPTION] })`.

    Render EXACTLY these 4 verbatim calls (plan-checker WARNING 7 — do not paraphrase or abbreviate the prop lists):
    ```typescript
    // Classic PDF
    const classicPdfTexts: string[] = []
    collectTextNodes(
      EstimatePDF({
        estimate, company: FIXTURE_COMPANY, client: null, projectName: 'Test',
        projectType: null, language: 'en', signature: SIGNATURE_FIXTURE,
        attachedPhotos: [PHOTO_WITH_CAPTION, PHOTO_NO_CAPTION],
      }),
      classicPdfTexts,
    )

    // Modern PDF
    const modernPdfTexts: string[] = []
    collectTextNodes(
      EstimatePDFModern({
        estimate, company: FIXTURE_COMPANY, client: null, projectName: 'Test',
        projectType: null, language: 'en', signature: SIGNATURE_FIXTURE,
        attachedPhotos: [PHOTO_WITH_CAPTION, PHOTO_NO_CAPTION],
      }),
      modernPdfTexts,
    )

    // Classic webview
    const classicShare = render(
      <EstimateDocument mode="view" data={toFixtureDocumentData(estimate)} company={FIXTURE_COMPANY} client={null}
        projectName="Test" projectType={null} language="en" estimateVersion={1} estimateSeq={1}
        estimateCreatedAt="2026-01-01T00:00:00Z" />,
    )

    // Modern webview
    const modernShare = render(
      <EstimateDocumentModern data={toFixtureDocumentData(estimate)} company={FIXTURE_COMPANY} client={null}
        projectName="Test" projectType={null} language="en" estimateVersion={1} estimateSeq={1}
        estimateCreatedAt="2026-01-01T00:00:00Z" />,
    )
    ```
    (Match the RTL prop shape actually used by `EstimateDocument`/`EstimateDocumentModern` in `document-signature-view.test.tsx`, Plan 183-05 — copy that file's exact prop list if it differs slightly from the sketch above; the point being enforced here is "real, complete calls with `attachedPhotos` and `signature` both present", not this exact literal formatting.)

    For each of the 4 surfaces, assert:
    1. The signer name (`SIGNATURE_FIXTURE.signerName`, `'Jane Doe'`) appears in that surface's rendered output.
    2. `formatDate(SIGNATURE_FIXTURE.signedAt, 'en')` (computed, never hardcoded — plan-checker WARNING 8) appears in that surface's rendered output.
    3. `PHOTO_WITH_CAPTION.caption` text appears in that surface's rendered output.
    4. No stray/empty caption artifact for `PHOTO_NO_CAPTION` (spot-check: the surface's rendered text does not contain two identical caption strings, and does not contain an obviously-broken empty bullet/paragraph — a light assertion here is fine, the per-surface tests already cover this rigorously).

    Add a SECOND fixture/assertion block, using the SAME verbatim call shapes as above with `signature: null, attachedPhotos: []` substituted in: `buildFixtureEstimate({ signature: null, attachedPhotos: [] })` — assert `SIGNATURE_FIXTURE.signerName` is ABSENT from all 4 surfaces' output (proves the "no placeholder for unsigned" rule holds consistently, not just on one surface).

    Add a structural assertion mirroring `presentation-settings-cross-surface.test.tsx:293-308`'s pattern: `readFileSync` each of the 4 template files and assert each one references `signature` (grep-style `expect(source).toMatch(/signature/)`) — a lightweight guard that a future refactor can't silently drop the signature prop/field from one surface without a test noticing.
  </action>
  <acceptance_criteria>
    - `grep -n "SIGNATURE_FIXTURE" tests/unit/estimate/document-signature-caption-cross-surface.test.tsx` matches.
    - `grep -n "EstimatePDF(\|EstimatePDFModern(\|EstimateDocument \|EstimateDocumentModern" tests/unit/estimate/document-signature-caption-cross-surface.test.tsx` matches all 4 renderer usages.
    - `grep -n "signature: null" tests/unit/estimate/document-signature-caption-cross-surface.test.tsx` matches (the unsigned-consistency case).
    - `grep -n "formatDate(SIGNATURE_FIXTURE.signedAt" tests/unit/estimate/document-signature-caption-cross-surface.test.tsx` matches (computed date, not hardcoded).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-signature-caption-cross-surface.test.tsx</automated>
  </verify>
  <done>One test file proves signature + caption presence/absence is consistent across Classic PDF, Modern PDF, Classic webview, and Modern webview, driven by ONE shared fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Intentionally extend the Wave-0 baseline to the post-refactor structure</name>
  <files>tests/unit/pdf/estimate-pdf-baseline-order.test.tsx</files>
  <read_first>
    - tests/unit/pdf/estimate-pdf-baseline-order.test.tsx (Plan 183-01, current content) — its own header comment explicitly names this task: "Plan 183-07 EXTENDS this same file with the new intentional post-refactor order."
    - Plans 183-04/183-06's `<must_haves>`/`<objective>` sections — the exact intentional deltas to add: Classic's title now has a fill (still text-order-invisible, so no NEW assertion needed there beyond what 183-04's own banner-fill test already covers — this task is about ORDER, not style); the signature block (when present) now sits between Terms and Photos; captions (when present) sit immediately after their photo.
  </read_first>
  <action>
    Extend `tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` (do not create a new file — same file, same `describe` block, matching its own doc comment's stated intent) with 2 NEW `it` blocks per template (4 total), using `buildFixtureEstimate({ signature: SIGNATURE_FIXTURE, attachedPhotos: [PHOTO_WITH_CAPTION] })`:
    1. Assert the signer name text's index is GREATER than `'Warranty'`'s index (or whichever terms label the fixture sets) and LESS than `'Photos'`'s index — i.e. Terms -> Signature -> Photos.
    2. Assert `PHOTO_WITH_CAPTION.caption` text's index is GREATER than `'Photos'`'s index (i.e. after the photos-section label, inside the grid).

    Update the file's header `describe`-block comment to note: "EXTENDED in Plan 183-07 with the post-refactor signature/caption order assertions — the original pre-refactor assertions above remain valid and unchanged (Phase 183 did not alter any of that text's presence/order, only added a Classic title fill and 2 new content blocks)."
  </action>
  <acceptance_criteria>
    - `grep -n "SIGNATURE_FIXTURE" tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` matches (new assertions added, not a rewrite).
    - `grep -n "PRE-REFACTOR BASELINE" tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` still matches (original comment preserved, not deleted).
    - `grep -n "EXTENDED in Plan 183-07" tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` matches.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-baseline-order.test.tsx</automated>
  </verify>
  <done>The same baseline file now asserts BOTH the original pre-refactor order (unchanged, still passing) AND the new post-refactor signature/caption positions, with an explicit comment distinguishing the two so a future reader never mistakes this for silent regression tolerance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual visual verification — both PDF templates, signed + captioned sample</name>
  <files>.planning/phases/183-pdf-parity-content/183-HUMAN-UAT.md</files>
  <action>
    Before presenting the checkpoint to the human, create `.planning/phases/183-pdf-parity-content/183-HUMAN-UAT.md` (UAT format — mirror `.planning/phases/182-shared-document-engine-send-path-fix/182-HUMAN-UAT.md`'s exact structure: YAML frontmatter with `status`, `phase`, `source`, `started`, `updated`, then `## Current Test`, `## Tests` (numbered, each with `expected`/`result`), `## Summary` (total/passed/issues/pending/skipped/blocked), `## Gaps`). Set `status: partial` in the frontmatter. Populate exactly these 4 test entries (plan-checker BLOCKER 4):

    1. **Spacing/typography fidelity after the StyleSheet collapse** — expected: Download the Classic PDF and the Modern PDF for the same sample estimate; compare each side-by-side against a PDF downloaded from BEFORE Phase 183 (or against the current webview render if no pre-183 PDF is available) — spacing, padding, and font rendering should look equivalent, not just structurally identical (per 183-VALIDATION.md's Manual-Only Verifications — no automated StyleSheet-value test exists). result: [pending]
    2. **Modern PDF stays hairline/fill-free** — expected: Open the Modern PDF; confirm the ESTIMATE title and every section header remain accent-colored TEXT with a thin hairline rule/border only — NO solid brand-color background fill anywhere (this is the Pitfall-1 negative case; a regression here means Plan 183-04's `PdfTitleBanner`/`PdfSectionBlock` `solidFill` wiring broke). result: [pending]
    3. **Signature image renders correctly in a real PDF** — expected: Download the PDF for a signed, captioned-photo estimate (both templates); confirm the signature image is a legible raster of the actual drawn signature (not a broken-image icon or blank box), positioned between Terms and Photos, with the signer name and signed date beside it. result: [pending]
    4. **OWNER DECISION — confirm Correction 1's scope** — expected (verbatim, do not paraphrase): "Owner confirm: CONTEXT.md said Modern gets the solid banner; source shows Modern webview never fills (estimate-document-modern.tsx:80-82) — the owner's reference screenshot was the CLASSIC template — so this phase gave the fill to Classic PDF only and locked Modern fill-free behind a negative test. Confirm or reopen." result: [pending]

    Then present the checkpoint below to the human as normal. This file persists the outcome beyond the interactive session (plan-checker BLOCKER 4 — a bare interactive checkpoint has no durable record if the environment auto-approves checkpoints without a human actually looking, per this project's own yolo-mode behavior).
  </action>
  <what-built>
    Phase 183 in full: both PDF templates now match their webview benchmarks structurally (Classic gained a solid title-banner fill; Modern's hairline design is unchanged); every document surface (2 webview + 2 PDF) shows a signature block for signed estimates and photo captions where set; the PDF template pair is now a thin composition over shared components (ENGINE-03 complete); Classic PDF renders in Inter, Modern PDF renders in Lora.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`, open an existing signed estimate with at least one captioned photo (or sign one via the public share link + `SignaturePad`, then attach a photo with a caption via the workspace editor).
    2. On the workspace editor (Classic, read-only since signed): confirm the signature block (image + name + date) appears between Terms and Photos, and the photo shows its caption underneath.
    3. Open the public share link for the SAME estimate: confirm the same signature block + caption on whichever template the company is set to (Classic or Modern — check/toggle `estimate_template_style` in company settings to see both).
    4. Download the PDF (`/api/estimates/[id]/pdf` route or the workspace "Download PDF" button) for BOTH template settings (toggle `estimate_template_style` between checks): confirm Classic PDF has a solid brand-color fill behind "ESTIMATE" (not just colored text), confirm Modern PDF's title/section headers are still hairline/no-fill (NOT solid — this is the Pitfall-1 negative check, the one most likely to have silently regressed), confirm both PDFs show the signature image + name + date between Terms and Photos, confirm the captioned photo shows its caption text in the PDF grid.
    5. Confirm both PDFs render in a real, non-monospace-fallback font (Inter for Classic — should look identical to the web's body text; a real serif — Lora — for Modern, not the default Times New Roman look).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Run the FULL suite: `npx vitest run tests/unit tests/eval && npx tsc -p tsconfig.ci.json --noEmit`. Every test in the repo — not just this phase's — must be green (this is the phase-gate full-suite run per 183-VALIDATION.md's Sampling Rate: "Before phase verification: Full suite must be green"). Confirm the discount-suffix DISPLAY predicate swap is complete on all 4 DISPLAY call sites, narrowly scoped (plan-checker BLOCKER 3): `grep -rn "discount_type === 'percentage'" components/pdf components/share components/workspace/estimate/estimate-document.tsx` returns nothing. Do NOT widen this grep to the whole `components/` tree — `components/workspace/estimate/use-estimate-reducer.ts:220`'s `if (state.discount_type === 'percentage')` is a DIFFERENT, intentionally-untouched call site (the client-preview TOTALS-MATH recompute, GUARD-03/SAVE-07 territory, mirroring the server's compute-totals.ts engine — not a display-suffix decision); a bare `components/` grep would incorrectly flag it as a missed call site.
</verification>

<success_criteria>
- Cross-surface signature + caption parity proven by one shared-fixture-driven test across all 4 document renderers.
- The Wave-0 baseline test is deliberately, visibly extended (not silently left stale) to reflect Phase 183's intentional structural changes.
- Full repo test suite + typecheck green.
- A human has visually confirmed both PDF templates' download output, explicitly checking the Pitfall-1 negative case (Modern must NOT have gained a solid fill).
</success_criteria>

<output>
After completion, create `.planning/phases/183-pdf-parity-content/183-07-SUMMARY.md`
</output>
