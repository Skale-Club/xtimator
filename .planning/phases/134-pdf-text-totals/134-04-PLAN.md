---
phase: 134-pdf-text-totals
plan: 04
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - lib/whatsapp/formatter.ts
  - lib/whatsapp/send-estimate.ts
  - lib/whatsapp/confirm-actions.ts
  - tests/unit/whatsapp/formatter.test.ts
autonomous: true
requirements: [PUI-02]

must_haves:
  truths:
    - "The plain-text estimate formatter (the channel-neutral render used by WhatsApp + MCP + the in-app Send tab) emits the ordered totals block Subtotal → Discount (only if >0) → Tax → Total → Deposit (only if set) → Balance Due (only if a deposit is set)"
    - "Deposit + balance-due lines are READ from the persisted server fields passed in (balance_due / deposit_type) via deriveDepositDisplay — no recompute"
    - "An estimate with no discount and no deposit renders the classic Subtotal/Tax/Total text, byte-identical to today (existing formatter.test.ts cases stay green)"
    - "Deposit / Balance Due labels render in en/pt/es"
    - "The data selects that feed the formatter include the deposit_type/deposit_value/balance_due columns"
  artifacts:
    - path: "lib/whatsapp/formatter.ts"
      provides: "Deposit + Balance Due text lines + deposit/balanceDue labels (en/pt/es) + deposit fields on FormatterEstimate"
      contains: "deriveDepositDisplay"
    - path: "lib/whatsapp/send-estimate.ts"
      provides: "deposit_type/deposit_value/balance_due added to the formatter estimate select"
      contains: "balance_due"
    - path: "lib/whatsapp/confirm-actions.ts"
      provides: "deposit_type/deposit_value/balance_due added to the formatter estimate select"
      contains: "balance_due"
  key_links:
    - from: "lib/whatsapp/formatter.ts"
      to: "lib/estimate/deposit-display.ts"
      via: "deriveDepositDisplay(estimate)"
      pattern: "deriveDepositDisplay"
    - from: "lib/whatsapp/send-estimate.ts (estimate select)"
      to: "lib/whatsapp/formatter.ts (FormatterEstimate)"
      via: "selected deposit columns feed the formatter"
      pattern: "balance_due"
---

<objective>
Render the v4.11 Deposit + Balance Due lines in the channel-neutral plain-text estimate
formatter (the render shared by the WhatsApp channel, the MCP channel, and the in-app Send
tab plain-text), reading PERSISTED server fields — no recompute (GUARD-03).

`lib/whatsapp/formatter.ts > formatEstimateForWhatsApp` builds the totals block (L142-162):
Subtotal (conditional) → Discount (conditional) → Tax (conditional) → blank → Total. This
plan appends, after Total: Deposit (only if set) → Balance Due (only if set), using
`deriveDepositDisplay()` (plan 01) over the persisted balance_due / deposit_type.

Two of the formatter's callers select fields EXPLICITLY (not `select('*')`), so the deposit
columns must be ADDED to those selects or the formatter receives undefined:
  - lib/whatsapp/send-estimate.ts L45-57 (the Send tab + inbox send)
  - lib/whatsapp/confirm-actions.ts L42 (the WhatsApp confirm summary)
(send-form.tsx receives a FormatterEstimate prop from the project page, which already reads
the full estimate row — no change needed there beyond the type widening.)

Retrocompat invariant: with deposit_type 'none' (or null balance_due) deriveDepositDisplay
returns showDeposit:false, so NO deposit/Balance Due lines are emitted and the text is
byte-identical to today — the existing formatter.test.ts cases must stay green unchanged.

Purpose: PUI-02 channel (3 of 3) — plain-text / WhatsApp / MCP.
Output: deposit/balance-due text lines + i18n labels + FormatterEstimate fields + select fixes + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- From plan 01 — the shared read seam. -->
From lib/estimate/deposit-display.ts:
```typescript
export function deriveDepositDisplay(row: {
  total: number; deposit_type: string | null; deposit_value: number | null; balance_due: number | null
}): { showDeposit: boolean; depositAmount: number; balanceDue: number }
```

From lib/whatsapp/formatter.ts (existing — to extend):
```typescript
export type FormatterEstimate = {
  summary: string | null
  total: number
  subtotal: number
  discount_type?: string | null
  discount_value?: number
  discount_amount?: number
  tax_rate: number
  tax_amount: number
  // ADD: deposit_type?, deposit_value?, balance_due?
  ...
  language?: 'en' | 'pt' | 'es'
  currency_code?: string | null
}
interface FormatterLabels { subtotal; discount; tax; total; ... }  // ADD deposit + balanceDue
const LABELS: Record<'en'|'pt'|'es', FormatterLabels> = {...}      // ADD deposit/balanceDue strings
// Totals emit block (L142-162): subtotal → discount → tax → '' → Total
```

From lib/whatsapp/send-estimate.ts L45-57 (explicit select feeding the formatter):
```
.select(`id, project_id, share_token, total, subtotal, tax_rate, tax_amount,
  currency_code, summary, payment_terms, timeline, language, sections:...`)
```
From lib/whatsapp/confirm-actions.ts L42:
```
.select('total, currency_code, summary, timeline, payment_terms, sections:estimate_sections(title, subtotal)')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deposit + Balance Due lines in the formatter (+ FormatterEstimate fields, labels, derive)</name>
  <read_first>lib/whatsapp/formatter.ts (whole file — types L14-43, LABELS L45-100, totals emit L142-162); tests/unit/whatsapp/formatter.test.ts (existing cases — must stay green); lib/estimate/deposit-display.ts (plan 01)</read_first>
  <files>lib/whatsapp/formatter.ts, tests/unit/whatsapp/formatter.test.ts</files>
  <behavior>
    - Existing cases (all 14 in formatter.test.ts) stay GREEN: a BASE_ESTIMATE with no deposit fields emits NO 'Deposit' and NO 'Balance Due' line (retrocompat byte-identical)
    - New Test (with deposit): { ...BASE_ESTIMATE, total: 2750, deposit_type: 'percent', deposit_value: 30, balance_due: 1925 } emits, after the Total line: a 'Deposit: -$825.00' line (2750−1925) and a 'Balance Due: $1,925.00' line
    - New Test (reads persisted, not recompute): { ...BASE, total: 2750, deposit_type: 'amount', deposit_value: 500, balance_due: 2000 } (deposit_value != total−balance_due) → 'Deposit: -$750.00' (2750−2000) and 'Balance Due: $2,000.00'
    - New Test (locale): with language 'pt' + a deposit, the lines read 'Entrada:' and 'Saldo Devedor:'; with 'es', 'Depósito:' and 'Saldo Pendiente:'
    - New Test (none → no lines): { ...BASE, deposit_type: 'none' } emits no Deposit/Balance Due line
  </behavior>
  <action>
    In lib/whatsapp/formatter.ts:
    1. Add optional fields to `FormatterEstimate`:
         deposit_type?: string | null
         deposit_value?: number | null
         balance_due?: number | null
    2. Add to `FormatterLabels`: `deposit: string` and `balanceDue: string`. Fill all three
       LABELS locales (mirror the values shipped in EstimateDocument DOC_LABELS phase 133):
         en: deposit: 'Deposit',  balanceDue: 'Balance Due'
         pt: deposit: 'Entrada',  balanceDue: 'Saldo Devedor'
         es: deposit: 'Depósito', balanceDue: 'Saldo Pendiente'
    3. Import `deriveDepositDisplay` from '@/lib/estimate/deposit-display'.
    4. In the totals block, AFTER `lines.push(\`${L.total}: ${money(estimate.total)}\`)` (L161)
       and its trailing `lines.push('')`, insert:
         const dep = deriveDepositDisplay({
           total: estimate.total,
           deposit_type: estimate.deposit_type ?? 'none',
           deposit_value: estimate.deposit_value ?? null,
           balance_due: estimate.balance_due ?? null,
         })
         if (dep.showDeposit) {
           lines.push(`${L.deposit}: -${money(dep.depositAmount)}`)
           lines.push(`${L.balanceDue}: ${money(dep.balanceDue)}`)
           lines.push('')
         }
       Place this BEFORE the closing/sign-off block (L164+). Order stays: Subtotal → Discount
       → Tax → Total → Deposit → Balance Due. Do NOT change the subtotal/discount/tax/total
       emit logic. No recompute.
    5. Add the new test cases to tests/unit/whatsapp/formatter.test.ts (do not delete or weaken
       existing assertions).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/whatsapp/formatter.test.ts</automated>
  </verify>
  <done>Formatter emits Deposit + Balance Due lines only when dep.showDeposit; values from deriveDepositDisplay; en/pt/es labels; all existing + new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add deposit columns to the selects feeding the formatter</name>
  <read_first>lib/whatsapp/send-estimate.ts (lines 44-57 — the estimate select); lib/whatsapp/confirm-actions.ts (lines 40-50 — the estimate select)</read_first>
  <files>lib/whatsapp/send-estimate.ts, lib/whatsapp/confirm-actions.ts</files>
  <action>
    The formatter now reads deposit_type/deposit_value/balance_due. These two callers select
    fields explicitly, so add the three columns to each select string:
    - lib/whatsapp/send-estimate.ts: in the `.select(\`...\`)` at L45-57, add
      `deposit_type, deposit_value, balance_due,` alongside the existing total/subtotal/tax_rate
      fields (e.g. right after `tax_rate, tax_amount,`).
    - lib/whatsapp/confirm-actions.ts: in the `.select('total, currency_code, ...')` at L42,
      add `deposit_type, deposit_value, balance_due,` (note confirm-actions also lacks
      subtotal/tax — its summary is a lighter render; only ADD the deposit columns, do not
      remove anything; the formatter's deriveDepositDisplay handles missing total gracefully via
      the `?? 'none'`/`?? null` defaults already in Task 1).
    Do NOT change discount_type handling or any other field. These are presentation reads of
    persisted columns — no recompute.
  </action>
  <verify>
    <automated>grep -c "balance_due" lib/whatsapp/send-estimate.ts lib/whatsapp/confirm-actions.ts</automated>
  </verify>
  <done>Both select strings include deposit_type, deposit_value, balance_due; grep shows balance_due in each file.</done>
</task>

</tasks>

<verification>
- `grep -n "deriveDepositDisplay" lib/whatsapp/formatter.ts` → imported + called
- `grep -n "Balance Due\|Saldo Devedor\|Saldo Pendiente" lib/whatsapp/formatter.ts` → 3 locale labels
- `grep -n "balance_due" lib/whatsapp/send-estimate.ts lib/whatsapp/confirm-actions.ts` → present in both selects
- `npx vitest run tests/unit/whatsapp/formatter.test.ts` → all existing + new cases green
- `npx vitest run tests/unit/whatsapp/send-route.test.ts` → stays green
</verification>

<success_criteria>
- Plain-text formatter emits the ordered Subtotal → Discount → Tax → Total → Deposit → Balance Due block
- Deposit + Balance Due lines emit ONLY when a deposit is set (deriveDepositDisplay.showDeposit)
- Values read from persisted server fields (no recompute)
- Legacy (no discount, no deposit) text byte-identical — existing 14 formatter tests unchanged + green
- Labels in en/pt/es; the selects feeding the formatter carry the deposit columns
</success_criteria>

<output>
After completion, create `.planning/phases/134-pdf-text-totals/134-04-SUMMARY.md`
</output>
