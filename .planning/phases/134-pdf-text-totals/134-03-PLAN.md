---
phase: 134-pdf-text-totals
plan: 03
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - components/workspace/estimate/estimate-document.tsx
  - tests/unit/estimate/document-totals-view.test.tsx
autonomous: true
requirements: [PUI-02]

must_haves:
  truths:
    - "The public shareable-link estimate (view mode) renders the ordered totals block Subtotal → Discount (only if >0) → Tax → Total → Deposit (only if set) → Balance Due (only if a deposit is set)"
    - "View-mode deposit + balance-due values are READ from the persisted server row (estimate.balance_due / deposit_type) via deriveDepositDisplay — no recompute"
    - "An estimate with no discount and no deposit renders the classic Subtotal → Tax → Total block in view mode, byte-identical to today"
    - "Edit mode (the editor) deposit/discount/balance-due UI is unchanged"
  artifacts:
    - path: "components/workspace/estimate/estimate-document.tsx"
      provides: "View-mode Deposit row in DocumentTotals (currently edit-mode only) + deriveDepositDisplay-backed Balance Due"
      contains: "deriveDepositDisplay"
    - path: "tests/unit/estimate/document-totals-view.test.tsx"
      provides: "view-mode ordered-block assertion (legacy + with-deposit), edit-mode untouched"
  key_links:
    - from: "components/workspace/estimate/estimate-document.tsx (DocumentTotals view branch)"
      to: "lib/estimate/deposit-display.ts"
      via: "deriveDepositDisplay(data)"
      pattern: "deriveDepositDisplay"
---

<objective>
Surface the v4.11 Deposit + Balance Due rows in the PUBLIC share view (and the read-only
HTML document), reading PERSISTED server totals — the renderer is presentation only (GUARD-03).

`components/share/estimate-view.tsx` renders `<EstimateDocument mode="view" .../>`, whose
`DocumentTotals` (estimate-document.tsx L932-1180) is the totals panel. Today the **Deposit
row only renders in EDIT mode** (`isEditable && dispatch`, L1101-1168); in view mode there is
NO Deposit line, and Balance Due renders only when `data.balance_due !== data.total` (L1171).

PUI-02 requires the full ordered block in view too: Subtotal → Discount → Tax → Total →
Deposit (only if set) → Balance Due (only if set). This plan adds a VIEW-MODE deposit row and
makes the view-mode deposit/balance-due read the persisted server row through
`deriveDepositDisplay()` (plan 01).

CRITICAL FENCE: do NOT change the edit-mode branches. The editor's deposit Select / discount
controls / Balance Due (phase 133) must stay byte-identical. Only the **view-mode** (`!isEditable`)
render path changes. estimate-view.tsx already plumbs deposit_type/deposit_value/deposit/
balance_due into `EstimateDocumentData` (estimate-view.tsx L114-121) — no data-plumbing change
needed there.

Purpose: PUI-02 channel (2 of 3) — the public share view.
Output: view-mode Deposit row + persisted-read Balance Due + a view-mode test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- From plan 01 — the shared read seam. -->
From lib/estimate/deposit-display.ts:
```typescript
export function deriveDepositDisplay(row: {
  total: number; deposit_type: string | null; deposit_value: number | null; balance_due: number | null
}): { showDeposit: boolean; depositAmount: number; balanceDue: number }
```

From components/workspace/estimate/estimate-document.tsx (existing):
- `EstimateDocumentData` (L320-344) ALREADY carries: total, deposit_type, deposit_value,
  deposit, balance_due, discount_amount, tax_amount, subtotal.
- DOC_LABELS (L61-191) ALREADY has en/pt/es `deposit` + `balanceDue` labels (phase 133).
- `DocumentTotals({ data, dispatch, isEditable, brandColor, L, defaultTaxRate })` (L932) —
  the `!isEditable` branches are the VIEW path to extend.
- Money formatter inside DocumentTotals: `const fmt = (v) => formatMoney(v, data.currency_code)` (L948).
- Current VIEW Balance Due (L1170-1176): renders only when `data.balance_due !== data.total`.
- Current Deposit row (L1101-1168): gated `isEditable && dispatch ? (...) : null` — VIEW gets null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: View-mode Deposit row + persisted-read Balance Due in DocumentTotals</name>
  <read_first>components/workspace/estimate/estimate-document.tsx (lines 932-1180 — the full DocumentTotals component, esp. the deposit block L1100-1176); components/share/estimate-view.tsx (lines 101-139 — how EstimateDocumentData incl deposit fields is built); lib/estimate/deposit-display.ts (plan 01)</read_first>
  <files>components/workspace/estimate/estimate-document.tsx, tests/unit/estimate/document-totals-view.test.tsx</files>
  <behavior>
    - Test 1 (legacy byte-identical, view mode): EstimateDocumentData with deposit_type 'none', discount_amount 0, tax_amount 0, balance_due === total renders Subtotal + Total only — NO Deposit row, NO Balance Due row
    - Test 2 (with deposit, view mode): data { total: 1000, deposit_type: 'percent', deposit_value: 30, deposit: 300, balance_due: 700, ... } renders, in order: Subtotal → Total → Deposit (-$300.00) → Balance Due ($700.00)
    - Test 3 (reads persisted, not recompute): data { total: 1000, deposit_type: 'amount', deposit_value: 250, balance_due: 760 } (deposit_value intentionally != total−balance_due) → Deposit row shows -$240.00 (1000−760, from deriveDepositDisplay), Balance Due $760.00
    - Test 4 (edit mode untouched): rendering DocumentTotals with isEditable true + a dispatch still shows the deposit Select (assert the edit-mode control is present) — proves the fence held
  </behavior>
  <action>
    Import `deriveDepositDisplay` from '@/lib/estimate/deposit-display' at the top of
    estimate-document.tsx.
    Inside `DocumentTotals`, compute once near `const fmt = ...` (L948):
      const dep = deriveDepositDisplay({
        total: data.total,
        deposit_type: data.deposit_type ?? 'none',
        deposit_value: data.deposit_value,
        balance_due: data.balance_due,
      })
    EDIT-MODE DEPOSIT BLOCK (L1101-1168, the `isEditable && dispatch ? (...)` branch): LEAVE
    UNCHANGED.
    Replace the current `: null` view fallback of that deposit block with a VIEW-MODE deposit
    row that renders only when `dep.showDeposit`:
      ) : dep.showDeposit ? (
        <div className="flex justify-between text-base pt-2">
          <span className="text-muted-foreground select-none">{L.deposit}</span>
          <span className="tabular-nums text-muted-foreground font-medium">-{fmt(dep.depositAmount)}</span>
        </div>
      ) : null}
    BALANCE DUE (L1170-1176): change the VIEW condition to use `dep.showDeposit` (instead of
    `data.balance_due !== data.total`) and render `fmt(dep.balanceDue)`. Keep the edit-mode
    condition (`depositTypeVal !== 'none'`) exactly as is. Net: the combined gate becomes
      (isEditable && dispatch ? depositTypeVal !== 'none' : dep.showDeposit)
      and the value shown becomes `fmt(isEditable ? data.balance_due : dep.balanceDue)`
      — or simpler: in view mode use dep.balanceDue, in edit mode keep data.balance_due.
    Order stays locked: Subtotal → Discount → Tax → grand Total → Deposit → Balance Due. Do NOT
    touch the Subtotal/Discount/Tax/grand-Total rows. No recompute — read data.* and dep.* only.
    Then write `tests/unit/estimate/document-totals-view.test.tsx` using @testing-library/react
    rendering `<EstimateDocument mode="view" .../>` (provide minimal required props: data,
    company, client null, projectName, projectType, estimateVersion, estimateCreatedAt). Assert
    on screen text for the 4 behaviors. For the legacy case assert `queryByText` for 'Deposit'
    and 'Balance Due' is null. Mind: EstimateDocument is a client component using useTranslation
    — if a provider wrapper is needed, mirror an existing estimate-document/share test's setup.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/document-totals-view.test.tsx</automated>
  </verify>
  <done>View mode renders Deposit + Balance Due only when dep.showDeposit, values from deriveDepositDisplay; edit-mode deposit Select still present (Test 4); legacy renders no extra rows; tests pass.</done>
</task>

</tasks>

<verification>
- `grep -n "deriveDepositDisplay" components/workspace/estimate/estimate-document.tsx` → imported + called in DocumentTotals
- View-mode deposit row gated on `dep.showDeposit`; Balance Due view gate uses `dep.showDeposit` + `dep.balanceDue`
- Edit-mode deposit Select block (L1101-1168) unchanged
- `npx vitest run tests/unit/estimate/document-totals-view.test.tsx` → green
- Existing editor/share tests stay green (run `npx vitest run tests/unit/share-query.test.ts tests/unit/estimates/share-link.test.ts`)
</verification>

<success_criteria>
- Public share view renders the ordered Subtotal → Discount → Tax → Total → Deposit → Balance Due block
- Deposit + Balance Due render in VIEW mode only when a deposit is set (deriveDepositDisplay)
- View-mode values read from the persisted server row (no recompute)
- Legacy (no discount, no deposit) view is byte-identical to today
- Editor (edit mode) deposit/discount/balance-due UI is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/134-pdf-text-totals/134-03-SUMMARY.md`
</output>
