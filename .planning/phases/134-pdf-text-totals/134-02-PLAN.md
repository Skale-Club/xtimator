---
phase: 134-pdf-text-totals
plan: 02
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - components/pdf/estimate-pdf.tsx
  - tests/unit/pdf/estimate-pdf-totals.test.tsx
autonomous: true
requirements: [PUI-02]

must_haves:
  truths:
    - "The branded PDF renders the ordered totals block Subtotal → Discount (only if >0) → Tax → Total → Deposit (only if set) → Balance Due (only if a deposit is set)"
    - "PDF deposit + balance-due values are READ from the persisted server row via deriveDepositDisplay (no recompute)"
    - "A PDF for an estimate with no discount and no deposit renders the classic Subtotal → Tax → Total block, byte-identical to today"
    - "Deposit / Balance Due labels render in en/pt/es"
  artifacts:
    - path: "components/pdf/estimate-pdf.tsx"
      provides: "Deposit + Balance Due rows in the totals block + deposit/balanceDue PDF labels (en/pt/es)"
      contains: "deriveDepositDisplay"
    - path: "tests/unit/pdf/estimate-pdf-totals.test.tsx"
      provides: "structural assertion of the ordered rows (legacy + with-deposit)"
  key_links:
    - from: "components/pdf/estimate-pdf.tsx"
      to: "lib/estimate/deposit-display.ts"
      via: "deriveDepositDisplay(estimate)"
      pattern: "deriveDepositDisplay"
---

<objective>
Render the v4.11 deposit + balance-due rows in the branded PDF totals block, in the locked
order, reading PERSISTED server values (GUARD-03 — the PDF is presentation only).

The PDF totals block today (components/pdf/estimate-pdf.tsx L671-719) renders Subtotal →
Discount (conditional) → Tax (conditional) → grand Total. This plan appends:
  → Deposit (only when a deposit is set)  → Balance Due (only when a deposit is set)
The deposit amount + balance due come from `deriveDepositDisplay()` (plan 01), which reads
the persisted `estimates.balance_due` / `deposit_type` — never recomputes.

Retrocompat invariant: an estimate with deposit_type 'none' produces showDeposit:false, so
NO deposit/Balance Due rows render and the PDF is byte-identical to the pre-v4.11 output.

Purpose: PUI-02 channel (1 of 3) — the PDF.
Output: deposit/balance-due rows + i18n labels + a structural test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- From plan 01 — the shared read seam. Use it; do NOT recompute. -->
From lib/estimate/deposit-display.ts:
```typescript
export function deriveDepositDisplay(row: {
  total: number; deposit_type: string | null; deposit_value: number | null; balance_due: number | null
}): { showDeposit: boolean; depositAmount: number; balanceDue: number }
```

From components/pdf/estimate-pdf.tsx (existing — totals block to EXTEND, L671-719):
```tsx
<View style={styles.totalsContainer}>
  <View style={styles.totalsBlock}>
    <View style={styles.totalsRow}>...Subtotal...</View>
    {estimate.discount_amount > 0 && (...Discount...)}
    {estimate.tax_amount > 0 && (...Tax...)}
    <View style={styles.grandTotalRow}>...grandTotal (L.grandTotal)...</View>
  </View>
</View>
```
Existing label maps: `PDF_LABELS: Record<EstimateLanguage, PdfLabels>` and `interface PdfLabels`
(L22-124) — already has subtotal/discount/tax/grandTotal. Money formatter: `const fmt = (v) => formatMoney(v, estimate.currency_code)` (L448).
`estimate` is `EstimateWithSections` (now carries deposit_type/deposit_value/balance_due via plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deposit + balanceDue labels to PdfLabels (en/pt/es)</name>
  <read_first>components/pdf/estimate-pdf.tsx (lines 22-124 — PdfLabels interface + PDF_LABELS map)</read_first>
  <files>components/pdf/estimate-pdf.tsx</files>
  <action>
    Extend `interface PdfLabels` with two fields: `deposit: string` and `balanceDue: string`.
    Add them to all three locale blocks in `PDF_LABELS` (mirror the EstimateDocument DOC_LABELS
    values already shipped in phase 133):
      en: deposit: 'Deposit',  balanceDue: 'Balance Due'
      pt: deposit: 'Entrada',  balanceDue: 'Saldo Devedor'
      es: deposit: 'Depósito', balanceDue: 'Saldo Pendiente'
  </action>
  <verify>
    <automated>grep -c "Balance Due\|Saldo Devedor\|Saldo Pendiente" components/pdf/estimate-pdf.tsx</automated>
  </verify>
  <done>`grep` returns 3 (one per locale); PdfLabels interface has deposit + balanceDue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Deposit + Balance Due rows in the PDF totals block</name>
  <read_first>components/pdf/estimate-pdf.tsx (lines 434-450 — component head + fmt; lines 671-719 — totals block to extend); lib/estimate/deposit-display.ts (plan 01)</read_first>
  <files>components/pdf/estimate-pdf.tsx, tests/unit/pdf/estimate-pdf-totals.test.tsx</files>
  <behavior>
    - Test 1 (legacy byte-identical): an estimate with deposit_type 'none', discount_amount 0, tax_amount 0 renders Subtotal + grandTotal rows and NO Deposit / NO Balance Due row (assert the rendered tree / JSON has no 'Deposit' and no 'Balance Due' text node)
    - Test 2 (with deposit): an estimate { total: 1000, deposit_type: 'percent', deposit_value: 30, balance_due: 700 } renders, in order: Subtotal, Tax (if any), Total, Deposit row with the persisted -$300.00 (1000−700), Balance Due row with $700.00
    - Test 3 (reads persisted, not recompute): given balance_due 800 with deposit_value 30 (mismatched on purpose), the Deposit row shows -$200.00 (1000−800) and Balance Due shows $800.00 — proving the PDF trusts the persisted balance_due
  </behavior>
  <action>
    At the top of the component body (near L448 where `fmt` is defined), add:
      `const dep = deriveDepositDisplay(estimate)` — import deriveDepositDisplay from
      '@/lib/estimate/deposit-display' at the top of the file.
    In the totals block, AFTER the `grandTotalRow` (the L.grandTotal row, ~L717), append two
    conditional rows INSIDE the same `styles.totalsBlock` View:
      {dep.showDeposit && (
        <View style={styles.totalsRow}>
          <Text style={styles.totalsLabel}>{L.deposit}</Text>
          <Text style={styles.totalsValue}>-{fmt(dep.depositAmount)}</Text>
        </View>
      )}
      {dep.showDeposit && (
        <View style={styles.totalsRow}>
          <Text style={styles.totalsLabel}>{L.balanceDue}</Text>
          <Text style={styles.totalsValue}>{fmt(dep.balanceDue)}</Text>
        </View>
      )}
    Order is locked: Subtotal (existing) → Discount (existing conditional) → Tax (existing
    conditional) → Total (existing grandTotalRow) → Deposit → Balance Due. Do NOT reorder the
    existing rows. Do NOT recompute any total — only read `estimate.*` and `dep.*`.
    Then write `tests/unit/pdf/estimate-pdf-totals.test.tsx`. Rendering @react-pdf to a real
    PDF is impractical headlessly — instead use React's test renderer (react-test-renderer or
    @testing-library/react `render` with the JSON tree) to assert the presence/absence and
    ORDER of the 'Deposit' / 'Balance Due' / money-string text nodes. If react-pdf primitives
    can't mount under the existing test setup, fall back to asserting via
    `react-test-renderer`'s `create(<EstimatePDF .../>).toJSON()` and walking the tree for the
    label + value strings. Cover the 3 behaviors above.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-totals.test.tsx</automated>
  </verify>
  <done>Deposit + Balance Due rows render only when dep.showDeposit; values come from deriveDepositDisplay; legacy (none) renders no extra rows; all 3 tests pass.</done>
</task>

</tasks>

<verification>
- `grep -n "deriveDepositDisplay" components/pdf/estimate-pdf.tsx` → imported + called
- `grep -n "L.deposit\|L.balanceDue" components/pdf/estimate-pdf.tsx` → both rows present
- Order in source: Subtotal → discount_amount → tax_amount → grandTotalRow → Deposit → Balance Due
- `npx vitest run tests/unit/pdf/estimate-pdf-totals.test.tsx` → green
- Existing PDF tests (tests/unit/whatsapp/pdf-delivery.test.ts) stay green
</verification>

<success_criteria>
- PDF totals block renders the ordered Subtotal → Discount → Tax → Total → Deposit → Balance Due block
- Deposit + Balance Due rows render ONLY when a deposit is set (deriveDepositDisplay.showDeposit)
- Values read from the persisted server row (no recompute)
- Legacy (no discount, no deposit) PDF is byte-identical to today
- Labels in en/pt/es
</success_criteria>

<output>
After completion, create `.planning/phases/134-pdf-text-totals/134-02-SUMMARY.md`
</output>
