---
phase: 109-durability-cost-control-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/workspace/estimate/estimate-document.tsx
  - lib/queries/estimate.ts
  - app/api/estimates/[id]/refine/route.ts
autonomous: true
requirements: [RMETER-01, RMETER-02, RMETER-03]
must_haves:
  truths:
    - "next build type-checks cleanly: no TS2322 'researched not assignable' error in the document/PDF/share render path"
    - "A persisted estimate item tagged price_source 'researched' renders in the workspace document, the share view, and the editor without a type error"
    - "The refine route re-hydrates a 'researched' item without narrowing it back to ai_estimate at the type level"
  artifacts:
    - path: "components/workspace/estimate/estimate-document.tsx"
      provides: "DocumentItem.price_source union including 'researched'"
      contains: "researched"
    - path: "lib/queries/estimate.ts"
      provides: "EstimateItem.price_source union including 'researched'"
      contains: "researched"
    - path: "app/api/estimates/[id]/refine/route.ts"
      provides: "refine existingEstimate price_source cast including 'researched'"
      contains: "researched"
  key_links:
    - from: "lib/queries/estimate.ts EstimateItem"
      to: "components/workspace/estimate/estimate-document.tsx DocumentItem"
      via: "share/estimate-view.tsx maps EstimateItem.price_source into DocumentItem"
      pattern: "price_source: i.price_source"
---

<objective>
Close the carried-over Phase-108 build-fix: widen every `price_source` union in the
document / PDF / share / query / refine render path to include `'researched'`, so
`next build` type-checks cleanly now that researched items actually occur at runtime.

`next.config.ts` does NOT set `ignoreBuildErrors`, so the un-widened unions currently
break the Docker build the moment a `researched` item is persisted (Phase 108 wired
that path live). This is the highest-value, lowest-risk item in the phase — pure type
widening, no runtime behavior change.

Purpose: Keep the production Docker build green; unblock the CI→GHCR→Coolify pipeline.
Output: Three widened unions/casts; a scoped tsc clean over the changed files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-durability-cost-control-hardening/109-CONTEXT.md
@.planning/phases/108-orchestrator-service-integration-the-payoff/108-VERIFICATION.md

<interfaces>
<!-- The canonical, already-widened source of truth (Phase 105). Mirror this. -->
From lib/ai/types.ts:
```typescript
price_source: 'price_book' | 'ai_estimate' | 'researched'  // required on every item
```

<!-- These render-path unions/casts still OMIT 'researched' and must be widened: -->
From components/workspace/estimate/estimate-document.tsx (~L274):
```typescript
price_source?: 'price_book' | 'ai_estimate' | null   // ← add 'researched'
```

From lib/queries/estimate.ts (~L64):
```typescript
price_source: 'price_book' | 'ai_estimate' | null   // ← add 'researched'
```

From app/api/estimates/[id]/refine/route.ts (~L213):
```typescript
price_source: (item.price_source ?? 'ai_estimate') as 'price_book' | 'ai_estimate'   // ← add 'researched'
```

<!-- Already CORRECT (do not touch — confirm they include 'researched'): -->
<!-- components/workspace/estimate/use-estimate-reducer.ts L19, lib/actions/estimate.ts L51 -->
<!-- item-row.tsx L90 + item-card-mobile.tsx L59 already render the 'researched' badge -->
<!-- share/estimate-view.tsx L128 maps EstimateItem.price_source → DocumentItem (covered transitively) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen all three render-path price_source unions to include 'researched'</name>
  <files>components/workspace/estimate/estimate-document.tsx, lib/queries/estimate.ts, app/api/estimates/[id]/refine/route.ts</files>
  <read_first>
    - components/workspace/estimate/estimate-document.tsx (the DocumentItem interface, ~L266-276)
    - lib/queries/estimate.ts (the EstimateItem interface, ~L54-65)
    - app/api/estimates/[id]/refine/route.ts (the existingEstimate.sections map, ~L206-216)
    - lib/ai/types.ts (~L17 — the canonical 'price_book' | 'ai_estimate' | 'researched' to mirror)
  </read_first>
  <action>
    Three one-line union widenings, mirroring the canonical lib/ai/types.ts union. NO
    runtime/logic change — type-level only:

    1. components/workspace/estimate/estimate-document.tsx ~L274 — change
       `price_source?: 'price_book' | 'ai_estimate' | null`
       to
       `price_source?: 'price_book' | 'ai_estimate' | 'researched' | null`

    2. lib/queries/estimate.ts ~L64 — change
       `price_source: 'price_book' | 'ai_estimate' | null`
       to
       `price_source: 'price_book' | 'ai_estimate' | 'researched' | null`
       (this is the source type for share/estimate-view.tsx's DocumentItem map and the
       workspace queries — widening it here keeps both consumers type-correct.)

    3. app/api/estimates/[id]/refine/route.ts ~L213 — change the cast
       `as 'price_book' | 'ai_estimate'`
       to
       `as 'price_book' | 'ai_estimate' | 'researched'`
       so a re-hydrated researched item is not narrowed back at the EstimateOutput
       boundary. (The `?? 'ai_estimate'` default is unchanged — only the cast widens.)

    Do NOT add a new badge or touch item-row.tsx / item-card-mobile.tsx (Phase 105
    already renders the 'researched' badge there). Do NOT touch use-estimate-reducer.ts
    or lib/actions/estimate.ts (already widened).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "estimate-document\.tsx|queries/estimate\.ts|refine/route\.ts" | grep -i "price_source\|researched\|TS2322" || echo "SCOPED-CLEAN: no price_source/researched type errors in the three changed files"</automated>
  </verify>
  <done>
    All three unions/casts include 'researched'. A scoped tsc shows ZERO
    price_source/researched/TS2322 errors in estimate-document.tsx, lib/queries/estimate.ts,
    and refine/route.ts (the verifier's flagged estimate-editor.tsx TS2322 is resolved
    transitively because DocumentItem now accepts 'researched'). Any pre-existing,
    unrelated repo-wide tsc errors (the long-standing es2018/tsconfig mismatches) are
    NOT introduced or widened by this change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm no remaining render-path price_source union omits 'researched'</name>
  <files>(verification only — no edits expected)</files>
  <read_first>
    - The grep output below (the full price_source surface across components/ + lib/ + app/)
  </read_first>
  <action>
    Sweep for any OTHER `price_source` union/cast in the document, PDF, share, or query
    render path that still omits `'researched'` (the CONTEXT explicitly asks for this
    sweep). Run the grep and inspect every union-literal hit:

      grep -rn "price_source" components/ lib/ app/ | grep -E "'price_book'|\"price_book\""

    For each hit that is a TYPE UNION or `as` CAST containing `'price_book' | 'ai_estimate'`,
    confirm it now also lists `'researched'`. The three from Task 1 plus the two
    already-correct ones (use-estimate-reducer.ts, lib/actions/estimate.ts) should be the
    complete set. There is NO separate @react-pdf/puppeteer render component with its own
    union (grep for *pdf* returns none — the share/workspace document component is the
    render path). If any NEW omitting union surfaces, widen it the same way and note it in
    the SUMMARY.
  </action>
  <verify>
    <automated>! grep -rn "price_source" components/ lib/ app/ | grep -E "as 'price_book' \| 'ai_estimate'$|: 'price_book' \| 'ai_estimate' \| null$|: 'price_book' \| 'ai_estimate'$" </automated>
  </verify>
  <done>
    The grep returns no `price_source` union/cast of the form `'price_book' | 'ai_estimate'`
    (or `... | null`) that omits `'researched'` anywhere in components/, lib/, or app/.
    Every render-path provenance union accepts the third value.
  </done>
</task>

</tasks>

<verification>
- Scoped tsc over the three changed files shows no price_source/researched TS2322 error.
- The sweep grep finds zero remaining omitting unions in the render path.
- The full vitest suite stays green (no runtime behavior changed — type-only edits):
  `npx vitest run` → same pass count as the Phase-108 baseline (275 files / 1924 passed).
</verification>

<success_criteria>
- `DocumentItem.price_source`, `EstimateItem.price_source`, and the refine route cast all
  include `'researched'`.
- A scoped `tsc --noEmit` is clean of price_source/researched errors in the changed files;
  the Phase-108-verifier-flagged `estimate-editor.tsx` TS2322 is resolved.
- No new repo-wide tsc errors introduced.
- Full vitest suite green at the Phase-108 baseline (type-only change → no test delta).
</success_criteria>

<output>
After completion, create `.planning/phases/109-durability-cost-control-hardening/109-01-SUMMARY.md`.
</output>
