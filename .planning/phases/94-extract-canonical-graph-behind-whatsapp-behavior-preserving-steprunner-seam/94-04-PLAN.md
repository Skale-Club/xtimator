---
phase: 94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam
plan: 04
type: execute
wave: 4
depends_on: ["94-03"]
files_modified:
  - tests/unit/inngest/whatsapp-process-job.test.ts
autonomous: true
requirements: [CHAN-01]
must_haves:
  truths:
    - "The whatsapp-process-job anchor test reads the MOVED files at their new paths and stays green with NO assertion changes"
    - "The full vitest suite is green (the behavior-preserving gate)"
    - "Only readFileSync PATHS changed in pre-existing source-text tests — never assertions (D-13 audit)"
  artifacts:
    - path: "tests/unit/inngest/whatsapp-process-job.test.ts"
      provides: "Anchor source-text test with readFileSync paths repointed to the new module homes"
      contains: "lib/estimate"
  key_links:
    - from: "tests/unit/inngest/whatsapp-process-job.test.ts"
      to: "lib/estimate/adapters/whatsapp.ts"
      via: "readFileSync of the moved WhatsApp tokens (awaiting_details/isVagueEstimate/buildAskDetailsMessage/revertVagueEstimate)"
      pattern: "lib/estimate/adapters/whatsapp"
---

<objective>
Close the behavior-preserving extraction by repointing the source-text ANCHOR test (`tests/unit/inngest/whatsapp-process-job.test.ts`) to the moved files — updating ONLY the `readFileSync` paths and the token-location reads, never the assertions — then run the full suite as the behavior-preserving gate and perform the D-13 audit (no pre-existing assertion changed; only paths moved).

Purpose: This is the #1 trap of the phase (94-RESEARCH Pitfall 1). The anchor test greps `lib/whatsapp/estimate-graph.ts` for tokens that Plan 03 moved into the core (`generateEstimateForProject`, the failure routing) and the WhatsApp adapter (`awaiting_details`, `isVagueEstimate(`, `buildAskDetailsMessage(`, `revertVagueEstimate(`). After the move those tokens live elsewhere; the test must read them at their new homes. A changed ASSERTION (not path) is the red flag that behavior changed.
Output: repointed `tests/unit/inngest/whatsapp-process-job.test.ts` + a fully green suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-CONTEXT.md
@.planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-RESEARCH.md
@.planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-VALIDATION.md
@lib/estimate/graph/index.ts
@lib/estimate/graph/nodes/generate.ts
@lib/estimate/adapters/whatsapp.ts
@lib/whatsapp/estimate-graph.ts
@lib/inngest/functions/whatsapp-process.ts

<interfaces>
<!-- Where the asserted tokens live AFTER Plan 03's move (the executor verifies actuals before editing paths). -->

Current anchor test reads (tests/unit/inngest/whatsapp-process-job.test.ts):
```typescript
const jobSrc   = readFileSync(resolve(cwd, 'lib/inngest/functions/whatsapp-process.ts'), 'utf8')  // UNCHANGED path
const graphSrc = readFileSync(resolve(cwd, 'lib/whatsapp/estimate-graph.ts'), 'utf8')             // tokens MOVED
```

Token → new home after Plan 03:
  - generateEstimateForProject( → lib/estimate/graph/nodes/generate.ts (CORE)
  - isVagueEstimate( → lib/estimate/graph/nodes/assess.ts (imports from quality/vagueness) + lib/estimate/quality/vagueness.ts
  - awaiting_details, buildAskDetailsMessage(, revertVagueEstimate( → lib/estimate/adapters/whatsapp.ts (ADAPTER)
  - generationFailed → RENAMED to failure (lib/estimate/graph/nodes/generate.ts + decide.ts) — THIS IS A CONTRACT CHANGE
  - checkGeneratedEdge / addConditionalEdges('generateEstimate', checkGeneratedEdge ...) → topology now in lib/estimate/graph/index.ts (node names may differ: generate/assess/onError)
  - step.run('orchestrate-estimate') + buildEstimateGraph( → lib/inngest/functions/whatsapp-process.ts (UNCHANGED)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the anchor test's readFileSync paths to the moved token homes</name>
  <files>tests/unit/inngest/whatsapp-process-job.test.ts</files>
  <read_first>
    - tests/unit/inngest/whatsapp-process-job.test.ts (the full anchor test — readFileSync at 24-31; the three describe blocks with token regexes at 49-82)
    - lib/estimate/graph/nodes/generate.ts (now holds generateEstimateForProject + the failure flag)
    - lib/estimate/graph/nodes/assess.ts + lib/estimate/quality/vagueness.ts (now hold isVagueEstimate)
    - lib/estimate/graph/nodes/decide.ts + lib/estimate/graph/index.ts (now hold the routing/topology)
    - lib/estimate/adapters/whatsapp.ts (now holds awaiting_details/buildAskDetailsMessage/revertVagueEstimate)
    - lib/inngest/functions/whatsapp-process.ts (jobSrc path UNCHANGED; still has step.run('orchestrate-estimate') + buildEstimateGraph()
    - .planning/phases/...94-RESEARCH.md (Pitfall 1 + Pitfall 2: the generationFailed→failure rename is an INTENTIONAL contract change — that one assertion MAY change, noted explicitly)
  </read_first>
  <action>
    Update `tests/unit/inngest/whatsapp-process-job.test.ts` so each existing assertion reads the file where its token NOW lives. This is a PATH/SOURCE-TARGET update, allowed under D-13 — preserve the assertion intent.
    1. Add `readFileSync` reads for the new files actually needed: e.g. `const generateSrc = readFileSync(... 'lib/estimate/graph/nodes/generate.ts')`, `const adapterSrc = readFileSync(... 'lib/estimate/adapters/whatsapp.ts')`, `const indexSrc = readFileSync(... 'lib/estimate/graph/index.ts')`. Keep `jobSrc` (whatsapp-process.ts) unchanged.
    2. Repoint each token regex to the new source variable WITHOUT weakening it:
       - `generateEstimateForProject(` → assert in `generateSrc`.
       - `awaiting_details`, `buildAskDetailsMessage(`, `revertVagueEstimate(` → assert in `adapterSrc`.
       - `isVagueEstimate(` → assert in `assessSrc` or `vaguenessSrc` (whichever actually contains it post-move).
       - `step.run('orchestrate-estimate'` + `buildEstimateGraph(` → KEEP asserting in `jobSrc` (unchanged).
       - The conditional-edge wiring assertion (`addConditionalEdges('generateEstimate', checkGeneratedEdge`) → update to the equivalent topology assertion against `indexSrc` for the new node names (e.g. the generate→decide→onError/assess edge). This is a TOPOLOGY-LOCATION update, not a behavior change — the routing semantics are identical.
    3. THE ONE INTENTIONAL ASSERTION CHANGE (Pitfall 2): `generationFailed` was renamed to `failure` (ENGINE-04). Update that single assertion to match the new contract (`/failure/` in generateSrc/decide) and add an inline comment: "ENGINE-04: generationFailed generalized to failure-as-state (intentional contract change, not behavior change — never-throw routing preserved; QA-01 frozen test guards the behavior)." This is the documented exception to "assertions unchanged."
    4. Do NOT delete or weaken any assertion's strength. If any OTHER assertion (beyond the generationFailed→failure rename) would need to change to pass, STOP — that signals the extraction altered behavior; reconcile in the source, not the test.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/inngest/whatsapp-process-job.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/inngest/whatsapp-process-job.test.ts` contains `readFileSync` reads of `lib/estimate/adapters/whatsapp.ts` and `lib/estimate/graph/nodes/generate.ts` (and assess/index as needed).
    - The `jobSrc` read of `lib/inngest/functions/whatsapp-process.ts` is preserved; `step.run('orchestrate-estimate'` and `buildEstimateGraph(` assertions still target `jobSrc`.
    - `grep -q "ENGINE-04" tests/unit/inngest/whatsapp-process-job.test.ts` matches (the documented generationFailed→failure rename comment).
    - `git diff tests/unit/inngest/whatsapp-process-job.test.ts` shows ONLY: new readFileSync vars, regex source-target swaps, and the single documented generationFailed→failure assertion change — NO weakened/deleted assertions.
    - `npx vitest run tests/unit/inngest/whatsapp-process-job.test.ts` exits 0.
  </acceptance_criteria>
  <done>The anchor source-text test reads tokens at their new homes and is green; the only assertion change is the documented ENGINE-04 generationFailed→failure rename; all other assertions preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green gate + D-13 behavior-preserving audit</name>
  <files>tests/unit/inngest/whatsapp-process-job.test.ts (verify-only), tests/unit/whatsapp/ask-details.test.ts (verify-only)</files>
  <read_first>
    - .planning/phases/...94-VALIDATION.md (Sampling Rate: full suite is the behavior-preserving gate; the D-13 audit requirement)
    - .planning/phases/...94-RESEARCH.md (D-13: only readFileSync paths changed in pre-existing source-text tests, never assertions)
    - tests/unit/inngest/whatsapp-process-job.test.ts + tests/unit/whatsapp/ask-details.test.ts + tests/unit/whatsapp/never-reply-regression.test.ts (the behavior-preserving anchors)
  </read_first>
  <action>
    1. Run the FULL vitest suite (`npm test`). Every pre-existing test (WhatsApp + Inngest + everything else) MUST be green, plus the 7 new Wave 0 tests. If any pre-existing test fails for a reason OTHER than a moved-file path, that is a behavior regression — fix the SOURCE (in lib/estimate/* or the adapter), not the test.
    2. Perform the D-13 audit: produce a git-diff review of ALL pre-existing test files touched this phase (primarily `tests/unit/inngest/whatsapp-process-job.test.ts`, and confirm `tests/unit/whatsapp/ask-details.test.ts` is UNCHANGED). Confirm: (a) ask-details.test.ts has ZERO changes; (b) whatsapp-process-job.test.ts changed only readFileSync paths/source-targets + the single documented ENGINE-04 rename; (c) the QA-01 never-reply-regression.test.ts assertions are unchanged since Plan 01. Record the audit result (file list + nature of each change) in the SUMMARY.
    3. Run the secret-scan gate over the new files: `grep -rEi "whsec_|sk_(test|live)_|rk_(test|live)_|sb_secret_|sk-ant-|sk-proj-|re_" lib/estimate` returns nothing.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green — the behavior-preserving gate).
    - `git diff --stat tests/unit/whatsapp/ask-details.test.ts` shows NO changes (re-export kept it green untouched).
    - `git diff tests/unit/whatsapp/never-reply-regression.test.ts` shows no assertion changes since Plan 01 (count===1 + invoke-resolves invariants intact).
    - The only pre-existing test with changes is `tests/unit/inngest/whatsapp-process-job.test.ts`, limited to readFileSync paths/source-targets + the documented ENGINE-04 rename.
    - `grep -rEi "whsec_|sk_(test|live)_|sb_secret_|sk-ant-|sk-proj-" lib/estimate` returns NOTHING.
    - SUMMARY records the D-13 audit (which pre-existing tests changed and how).
  </acceptance_criteria>
  <done>Full suite green; D-13 audit confirms only path/source-target changes (plus the documented ENGINE-04 rename) in pre-existing tests; no secrets; the extraction is provably behavior-preserving.</done>
</task>

</tasks>

<verification>
- `npm test` exits 0.
- `git diff tests/unit/whatsapp/ask-details.test.ts` is empty.
- The anchor test reads moved tokens at their new file homes and is green.
- No pre-existing assertion (other than the documented ENGINE-04 generationFailed→failure rename) changed.
</verification>

<success_criteria>
- CHAN-01: the source-text anchor test passes against the new module layout (paths updated, assertions preserved); WhatsApp behavior verified preserved by the full green suite + the frozen QA-01 test.
- D-13 audit complete and recorded: behavior-preserving extraction confirmed.
- No secrets introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-04-SUMMARY.md`
</output>
