---
phase: 94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/estimate/graph/CHECKPOINTING.md
  - tests/unit/estimate/graph-neutrality.test.ts
  - tests/unit/estimate/channel-adapter.test.ts
  - tests/unit/estimate/vagueness.test.ts
  - tests/unit/estimate/never-throw.test.ts
  - tests/unit/estimate/step-runner.test.ts
  - tests/unit/estimate/no-checkpointer.test.ts
  - tests/unit/whatsapp/never-reply-regression.test.ts
autonomous: true
requirements: [DURABLE-02, QA-01]
must_haves:
  truths:
    - "The DURABLE-02 decision artifact exists and states Inngest is the sole durability layer with no LangGraph checkpointer"
    - "All 7 Wave 0 test files exist and run (RED until later waves implement the source)"
    - "no-checkpointer test asserts the artifact is present AND the graph compile has no saver argument"
  artifacts:
    - path: "lib/estimate/graph/CHECKPOINTING.md"
      provides: "DURABLE-02 graph-Inngest checkpoint-granularity decision artifact"
      contains: "no LangGraph checkpointer"
    - path: "tests/unit/estimate/graph-neutrality.test.ts"
      provides: "ENGINE-01 static source-grep stub (RED)"
    - path: "tests/unit/estimate/channel-adapter.test.ts"
      provides: "ENGINE-02 closure-factory stub (RED)"
    - path: "tests/unit/estimate/vagueness.test.ts"
      provides: "ENGINE-03 vagueness truth-table stub (RED)"
    - path: "tests/unit/estimate/never-throw.test.ts"
      provides: "ENGINE-04 failure-as-state stub (RED)"
    - path: "tests/unit/estimate/step-runner.test.ts"
      provides: "DURABLE-01 passthroughRunner + injection stub (RED)"
    - path: "tests/unit/estimate/no-checkpointer.test.ts"
      provides: "DURABLE-02 artifact-present + no-saver stub"
    - path: "tests/unit/whatsapp/never-reply-regression.test.ts"
      provides: "QA-01 frozen never-throw/always-reply behavioral stub (RED)"
  key_links:
    - from: "tests/unit/estimate/no-checkpointer.test.ts"
      to: "lib/estimate/graph/CHECKPOINTING.md"
      via: "readFileSync of the artifact + grep for 'no LangGraph checkpointer'"
      pattern: "no LangGraph checkpointer"
---

<objective>
Land the Wave 0 safety net for the canonical-graph extraction: the DURABLE-02 decision artifact plus all seven failing test stubs enumerated in 94-VALIDATION.md. These tests must EXIST and run now (RED is expected — the source they target is implemented in Waves 2-4) so every downstream task has a concrete `<automated>` verify target and the extraction is bisectable.

Purpose: Tests-first guarantees the behavior-preserving extraction is auditable. The DURABLE-02 artifact captures the locked graph↔Inngest durability decision (D-12) before any code moves, so the no-checkpointer rule is enforced by a static test from day one.
Output: `lib/estimate/graph/CHECKPOINTING.md` + 7 test files under `tests/unit/estimate/` and `tests/unit/whatsapp/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-CONTEXT.md
@.planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-RESEARCH.md
@.planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-VALIDATION.md

<interfaces>
<!-- Target contracts the stubs reference. These do NOT exist yet (implemented in Waves 2-4); the stubs are RED. -->

Future core state (lib/estimate/graph/state.ts):
```typescript
export const EstimateState = Annotation.Root({
  companyId, projectId, channel: 'whatsapp'|'web'|'mcp',
  prompts?, estimateId?, estimateLanguage?, isVague?,
  failure?: { reason: string }, refineAttempts?: number,
})
```

Future contracts (lib/estimate/graph/types.ts):
```typescript
export interface ChannelAdapter { channel; ingest(state); finalize(state); onError(state) }
export interface StepRunner { run<T>(name: string, fn: () => Promise<T>): Promise<T> }
export const passthroughRunner: StepRunner = { run: (_n, fn) => fn() }
export function buildEstimateGraph(adapter, { runner = passthroughRunner } = {})
```

Future vagueness module (lib/estimate/quality/vagueness.ts):
```typescript
export function isVagueEstimate(e: VagueCheckEstimate | null): boolean
export type VagueCheckEstimate = { total: number | null; sections: ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the DURABLE-02 decision artifact</name>
  <files>lib/estimate/graph/CHECKPOINTING.md</files>
  <read_first>
    - .planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-RESEARCH.md (section "Decision Artifact (DURABLE-02) — ready-to-commit content")
    - .planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-CONTEXT.md (D-12)
    - lib/inngest/functions/whatsapp-process.ts (the single step.run('orchestrate-estimate') durability boundary + retries:1 + onFailure)
    - lib/whatsapp/estimate-graph.ts (line 444: graph.compile() with NO checkpointer arg)
    - CLAUDE.md (Secret Handling — NO secrets/placeholders in the artifact)
  </read_first>
  <action>
    Create `lib/estimate/graph/CHECKPOINTING.md` capturing the locked DURABLE-02 decision (D-12). Use the ready-to-commit content from 94-RESEARCH.md verbatim as the basis. The artifact MUST contain these load-bearing statements (exact phrases matter — the no-checkpointer.test.ts greps them):
    - The literal phrase "no LangGraph checkpointer".
    - "Inngest is the sole durability layer" (or "Inngest owns durability" — include the exact phrase "sole durability").
    - The whole estimate graph runs inside ONE Inngest `step.run`.
    - Why no checkpointer: MemorySaver is per-process and lost on Inngest replay (Pod B problem); Postgres/Sqlite saver duplicates Inngest's state (conflicting recovery authority).
    - Cross-message "wait for the owner's reply" stays in `whatsapp_sessions` rows + new Inngest events, NOT a LangGraph interrupt.
    - Cost trade-off accepted: a retry after a successful AI generate re-runs the whole graph and re-charges the AI call; bounded by `retries: 1`.
    - When to revisit: promote individual AI-heavy nodes to their own `step.run` via the injected `StepRunner` (Inngest injects `{ run: (name, fn) => step.run(name, fn) }`) once OBS metrics (Phase 97) show retry re-charge is material — NOT a checkpointer.
    NO secrets, keys, or signing values anywhere in the file (CLAUDE.md). Header with a one-line title + "decided 2026-06-20, v4.3".
  </action>
  <verify>
    <automated>grep -q "no LangGraph checkpointer" lib/estimate/graph/CHECKPOINTING.md && grep -qi "sole durability" lib/estimate/graph/CHECKPOINTING.md && grep -q "StepRunner" lib/estimate/graph/CHECKPOINTING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `lib/estimate/graph/CHECKPOINTING.md` exists.
    - File contains the literal string "no LangGraph checkpointer".
    - File contains "sole durability" (case-insensitive).
    - File mentions `StepRunner` and `step.run` and `whatsapp_sessions`.
    - `grep -rEi "whsec_|sk_(test|live)_|sk-ant-|sk-proj-|sb_secret_" lib/estimate/graph/CHECKPOINTING.md` returns NOTHING (no secrets).
  </acceptance_criteria>
  <done>The DURABLE-02 artifact exists at lib/estimate/graph/CHECKPOINTING.md with all required phrases and zero secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the 6 estimate-module test stubs (ENGINE-01..04, DURABLE-01, DURABLE-02)</name>
  <files>tests/unit/estimate/graph-neutrality.test.ts, tests/unit/estimate/channel-adapter.test.ts, tests/unit/estimate/vagueness.test.ts, tests/unit/estimate/never-throw.test.ts, tests/unit/estimate/step-runner.test.ts, tests/unit/estimate/no-checkpointer.test.ts</files>
  <read_first>
    - .planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — exact filenames + behaviors)
    - .planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-RESEARCH.md (Validation Architecture; the channel-neutral state table; the ChannelAdapter + StepRunner interface proposals)
    - tests/unit/whatsapp/ask-details.test.ts (the isVagueEstimate truth-table to mirror at the new path in vagueness.test.ts)
    - tests/unit/whatsapp/query-tools.test.ts (the "no tenant input field" + "every query company-scoped" template for channel-adapter.test.ts)
    - lib/whatsapp/estimate-graph.ts (current generationFailed catch at lines 254-256 + graph.compile() at 444 — the behavior the never-throw + no-checkpointer tests assert)
    - vitest.config.ts (globals true; @ alias to repo root)
  </read_first>
  <behavior>
    Each stub describes the FUTURE source it targets and fails loudly today (RED). Pattern: import explicitly from 'vitest' (config has globals but tsc needs explicit `expect`); when a target module does not yet exist, use a top-level `expect.fail('Wave 2: <module> not implemented')` or a dynamic `await import(...)` inside the test that will throw until the module lands — DO NOT statically import a non-existent module at file top (breaks module resolution / the whole run). Mirror the Phase 67/18 Wave-0 RED-stub convention.
    - graph-neutrality.test.ts (ENGINE-01): readFileSync every file under `lib/estimate/graph/` + `lib/estimate/quality/` and assert NONE contains `lib/whatsapp`, `ownerPhone`, `WhatsAppMessage`, `sendWhatsAppMessage`, `whatsapp_` , `downloadWhatsAppMedia`. Until the dir exists, glob returns empty → assert at least state.ts/types.ts exist (RED now).
    - channel-adapter.test.ts (ENGINE-02): assert `buildEstimateGraph` is importable and accepts an adapter object; assert the WhatsApp adapter factory captures companyId in closure and NO adapter edge fn schema/signature accepts a tenant/companyId input field (mirror query-tools.test.ts T-lrf-01 assertions).
    - vagueness.test.ts (ENGINE-03): copy the isVagueEstimate truth table from ask-details.test.ts but import from `@/lib/estimate/quality/vagueness` (RED until the module exists).
    - never-throw.test.ts (ENGINE-04): assert the core `generate` node, when generateEstimateForProject rejects, RESOLVES with `{ failure: { reason: ... } }` and does NOT throw; assert the `decide` edge routes a state with `failure` set to the error/onError terminal.
    - step-runner.test.ts (DURABLE-01): assert `passthroughRunner.run('x', fn)` returns `fn()`'s resolved value unchanged (no extra calls/wrapping); assert `buildEstimateGraph(adapter, { runner })` accepts an injected runner.
    - no-checkpointer.test.ts (DURABLE-02): assert `lib/estimate/graph/CHECKPOINTING.md` exists and contains "no LangGraph checkpointer" + "sole durability"; readFileSync `lib/estimate/graph/index.ts` (the factory) and assert `.compile(` appears with NO saver/checkpointer argument (regex: `.compile()` with empty or options-without-checkpointer args; assert it does NOT match `checkpointer`/`Saver`).
  </behavior>
  <action>
    Create the six files listed in 94-VALIDATION.md exactly:
    `tests/unit/estimate/graph-neutrality.test.ts`, `tests/unit/estimate/channel-adapter.test.ts`, `tests/unit/estimate/vagueness.test.ts`, `tests/unit/estimate/never-throw.test.ts`, `tests/unit/estimate/step-runner.test.ts`, `tests/unit/estimate/no-checkpointer.test.ts`.
    Each file: `import { describe, it, expect } from 'vitest'` (+ `vi` where mocking). Prefix every top-level describe title with the requirement ID (e.g. `describe('ENGINE-01: shared core neutrality', ...)`) for grep traceability (Phase 67 convention). Author REAL assertions matching the <behavior> block — these become the GREEN gate for Waves 2-4; do not weaken them to bare `expect.fail()` except as the temporary RED guard where a target module truly cannot be imported yet. The no-checkpointer.test.ts artifact-presence assertions can be GREEN immediately (Task 1 wrote the artifact).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/no-checkpointer.test.ts 2>&1 | grep -Eq "(✓|passed|PASS).*(artifact|checkpointer)|Tests" && echo ARTIFACT_TESTS_RAN; ls tests/unit/estimate/graph-neutrality.test.ts tests/unit/estimate/channel-adapter.test.ts tests/unit/estimate/vagueness.test.ts tests/unit/estimate/never-throw.test.ts tests/unit/estimate/step-runner.test.ts tests/unit/estimate/no-checkpointer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist at the exact paths above.
    - `grep -l "ENGINE-01" tests/unit/estimate/graph-neutrality.test.ts` matches (req-ID-prefixed describe).
    - `grep -q "@/lib/estimate/quality/vagueness" tests/unit/estimate/vagueness.test.ts` matches.
    - `grep -q "no LangGraph checkpointer" tests/unit/estimate/no-checkpointer.test.ts` AND `grep -q "CHECKPOINTING.md" tests/unit/estimate/no-checkpointer.test.ts` match.
    - `grep -q "passthroughRunner" tests/unit/estimate/step-runner.test.ts` matches.
    - `npx vitest run tests/unit/estimate/no-checkpointer.test.ts` collects and runs (artifact-presence assertions pass; saver-absence assertions may be RED until Wave 2 — acceptable).
    - Running `npx vitest run tests/unit/estimate` does NOT error with "Cannot find module" at collection time for the artifact test (dynamic-import or readFileSync pattern used for not-yet-existent source).
  </acceptance_criteria>
  <done>Six estimate test stubs exist, are req-ID traceable, run under vitest, and encode the real Wave 2-4 contracts (RED where source is pending, GREEN for the artifact-presence checks).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the QA-01 frozen never-throw/always-reply regression stub</name>
  <files>tests/unit/whatsapp/never-reply-regression.test.ts</files>
  <read_first>
    - .planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-RESEARCH.md (section "QA-01 frozen-test design" — the 3 paths + invariant)
    - lib/whatsapp/estimate-graph.ts (the WhatsApp-composed graph being frozen; sendError two-copy at lines 406-408; askDetails revert+session at 294-330; checkInputsEdge at 233-235)
    - tests/unit/whatsapp/ask-details.test.ts (chainable Supabase mock style to mirror)
    - tests/unit/whatsapp/query-tools.test.ts (mock style)
    - lib/whatsapp/client.ts (sendWhatsAppMessage signature to mock)
  </read_first>
  <behavior>
    `tests/unit/whatsapp/never-reply-regression.test.ts` — behavioral, invokes the WhatsApp-composed graph (`buildEstimateGraph()` from `@/lib/whatsapp/estimate-graph`) with mocked `sendWhatsAppMessage`, Supabase (chainable), and `generateEstimateForProject`/AI. Three frozen paths, each asserting EXACTLY ONE `sendWhatsAppMessage` call AND `graph.invoke(...)` RESOLVES (never rejects):
    - Path A — no usable input: messages produce all `mediaResults.ok:false` (or empty) → checkInputs → onError → exactly one sendWhatsAppMessage carrying the "couldn't process your message" no-input copy.
    - Path B — generation throws: `generateEstimateForProject` mock rejects → generate sets failure (does NOT throw) → decide → onError → exactly one sendWhatsAppMessage carrying the generation-failed copy.
    - Path C — vague estimate: AI returns total 0 / no items → assess isVague:true → finalize(askDetails) → revertVagueEstimate called + `awaiting_details` session insert + exactly one sendWhatsAppMessage (ask-details copy).
    Invariant across all three: sendWhatsAppMessage call count === 1 and invoke resolves.
  </behavior>
  <action>
    Create `tests/unit/whatsapp/never-reply-regression.test.ts`. `import { describe, it, expect, vi } from 'vitest'`. `vi.mock('@/lib/whatsapp/client')` to spy `sendWhatsAppMessage`; `vi.mock('@/lib/services/generate-estimate')` to control success/throw; `vi.mock('@/lib/supabase/service')` with a chainable mock returning the estimate shape per path. Prefix the top-level describe with `QA-01:`. This is a FROZEN test — its assertions encode the contract that survives extraction; it is RED today against the current graph state shape ONLY where the post-extraction wiring differs, otherwise it should run against the current `buildEstimateGraph()`. Author full real assertions (count===1, invoke resolves, correct copy substring per path). Use a `try { await graph.invoke(...) } catch { expect.fail('graph.invoke must never reject') }` guard for the never-reject invariant.
  </action>
  <verify>
    <automated>ls tests/unit/whatsapp/never-reply-regression.test.ts && grep -q "QA-01" tests/unit/whatsapp/never-reply-regression.test.ts && npx vitest run tests/unit/whatsapp/never-reply-regression.test.ts 2>&1 | grep -Eq "Tests|passed|failed" && echo RAN</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/whatsapp/never-reply-regression.test.ts` exists.
    - Top-level describe is prefixed `QA-01`.
    - File contains three distinct `it(` blocks for no-input / generation-throw / vague paths.
    - Each path asserts `sendWhatsAppMessage` called exactly once (e.g. `toHaveBeenCalledTimes(1)`).
    - File contains an assertion that `graph.invoke` resolves / never rejects.
    - `npx vitest run tests/unit/whatsapp/never-reply-regression.test.ts` collects and runs without a module-resolution crash.
  </acceptance_criteria>
  <done>The QA-01 frozen behavioral regression test exists, runs, and encodes the 3-path one-reply invariant that must stay green through the extraction.</done>
</task>

</tasks>

<verification>
- `ls tests/unit/estimate/*.test.ts` returns the 6 estimate stubs.
- `ls tests/unit/whatsapp/never-reply-regression.test.ts` exists.
- `lib/estimate/graph/CHECKPOINTING.md` exists with the DURABLE-02 phrases.
- `npx vitest run tests/unit/estimate tests/unit/whatsapp/never-reply-regression.test.ts` collects all files (RED expected for source-pending assertions; no collection crash).
- No secrets anywhere: `grep -rEi "whsec_|sk_(test|live)_|sk-ant-|sk-proj-|sb_secret_" lib/estimate tests/unit/estimate` returns nothing.
</verification>

<success_criteria>
- DURABLE-02 decision artifact committed with the no-checkpointer / sole-durability / StepRunner-revisit content.
- All 7 Wave 0 test files exist, are requirement-ID traceable, and run under vitest.
- Stubs are RED where they target Wave 2-4 source and GREEN for the artifact-presence checks — establishing the bisectable safety net.
</success_criteria>

<output>
After completion, create `.planning/phases/94-extract-canonical-graph-behind-whatsapp-behavior-preserving-steprunner-seam/94-01-SUMMARY.md`
</output>
