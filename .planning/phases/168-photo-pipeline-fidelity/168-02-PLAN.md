---
phase: 168-photo-pipeline-fidelity
plan: 02
type: execute
wave: 3
depends_on:
  - "166-02"
  - "168-01"
files_modified:
  - lib/services/generate-estimate.ts
  - lib/actions/attempt-outcome.ts
  - lib/estimate/poll-outcome.ts
  - components/capture/capture-processing-overlay.tsx
  - components/capture/capture-recorder.tsx
  - tests/unit/services/generate-estimate-captions.test.ts
  - tests/unit/estimate/poll-outcome-counts.test.ts
autonomous: true
requirements:
  - PHOTO-01
must_haves:
  truths:
    - "A user-entered photo caption is included (sanitized) alongside the AI description in the generation prompt's photoDescriptions — a caption like 'north wall, 12ft ceiling' reaches the estimator instead of being discarded."
    - "Captions are folded into the photoDescriptions string that prompt-builder.ts already routes through sanitizeField (untrusted job-site text — the caption gets the same prompt-injection hardening as ai_description)."
    - "The 'N of M photos analyzed' count (from 168-01's pipeline_events.metadata) is surfaced to the user in the capture progress UI when coverage is partial — completing PHOTO-02's user-visible half."
    - "A photo with no caption produces byte-identical prompt output to today (the caption addition is additive)."
  artifacts:
    - path: "lib/services/generate-estimate.ts"
      provides: "caption folded into photoDescriptions"
    - path: "lib/estimate/poll-outcome.ts"
      provides: "StageProgress carries analyzed/total counts from the journal metadata"
  key_links:
    - from: "lib/services/generate-estimate.ts"
      to: "lib/ai/prompt-builder.ts"
      via: "photoDescriptions strings (incl. caption) → sanitizeField wrapping"
      pattern: "photoDescriptions"
---

<objective>
Close audit finding E2 (PHOTO-01): user-entered photo captions never reach the estimator. generate-estimate.ts:211-213 builds photoDescriptions from `ai_description` ONLY; `photos.caption` (rendered in share/PDF) is discarded from the prompt. Also complete PHOTO-02's user-visible half: 168-01 landed the analyzed/total counts in pipeline_events.metadata but the "N of M photos analyzed" UI was deferred here.

REQUIRED READING: .planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md § E2 + § E (the caption-discard finding). Current prompt assembly: generate-estimate.ts:169-227 (hasPhotos filter :171, photoDescriptions map :211-213), prompt-builder.ts:155-162 (the <photo_description> wrapping + sanitizeField at :159).

SCOPE FENCE: this edits the PROMPT-ASSEMBLY region of generate-estimate.ts (:169-227), which is DISJOINT from 166-02's post-AI region (~:334-410, :590-598 — already landed) and from 168-01's analyze-photos.ts (untouched here). The N-of-M UI reads the metadata 168-01 added to pipeline_events. Do NOT touch the totals/consistency logic (166-02) or the vision worker (168-01/167-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md
@lib/services/generate-estimate.ts
@lib/ai/prompt-builder.ts
@lib/estimate/poll-outcome.ts
@lib/estimate/progress-model.ts

<interfaces>
Captions (PHOTO-01): NO query change needed (Opus blocker #1). Generation loads photos via `getProjectPhotos` (lib/queries/photo.ts) which does `.select('*')` and returns `Photo` with `caption: string | null` already populated — `p.caption` is available at generate-estimate.ts:211-213. (The plan originally listed lib/queries/estimate.ts — WRONG file; it never queries photos. Removed from scope.) Just fold caption into the photoDescriptions string:
```ts
const photoDescriptions = photos
  .filter((p) => p.ai_description && p.ai_description.trim().length > 0)
  .map((p, i) => {
    const cap = p.caption?.trim()
    return cap
      ? `Photo ${i + 1} (caption: ${cap}): ${p.ai_description}`
      : `Photo ${i + 1}: ${p.ai_description}`
  })
```
The whole string is wrapped in `sanitizeField(...)` at prompt-builder.ts:159, so the caption is transitively escaped + length-capped — the same untrusted-text hardening ai_description already gets. Do NOT add a separate un-sanitized path.

N-of-M UI (PHOTO-02 UI half): 168-01 records `{analyzedCount, totalCount, failedCount}` in the analyze step's pipeline_events.metadata (analyze-photos.ts:346-350 — exact key names confirmed). The journal read does NOT happen in poll-outcome.ts (Opus blocker #2) — it delegates to `getAttemptOutcome` in **lib/actions/attempt-outcome.ts**, which currently does NOT select `metadata` (:120) and whose `pending` shape carries no counts (:48-54, returned :192-198). Threading path:
  1. **lib/actions/attempt-outcome.ts**: add `metadata` to the `.select(...)`; find the analyze/succeeded row; extract analyzedCount/totalCount/failedCount; add them (optional) to the `pending` payload shape.
  2. **lib/estimate/poll-outcome.ts**: extend `StageProgress` (:54) with optional analyzedCount?/totalCount?/failedCount?; forward them in the pending branch (:182-186) through onStageProgress.
  3. **components/capture/capture-recorder.tsx**: `handleStageProgress` (:654-665) carries only completedSteps/activeStep today — add the counts to `attemptProgress` state and pass a new prop to the overlay.
  4. **components/capture/capture-processing-overlay.tsx**: the analyze-step label lives here (:102 `t('Analyzing photos')`, consumed at capture-recorder.tsx:1291-1298) — render "N of M photos analyzed" subtitle when analyzedCount < totalCount OR failedCount > 0. English via t().
Region note: this progress path is disjoint from 169-01's landed capture-recorder upload/IDB region (Opus-confirmed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Captions into the generation prompt (PHOTO-01)</name>
  <files>lib/services/generate-estimate.ts, lib/queries/estimate.ts, tests/unit/services/generate-estimate-captions.test.ts</files>
  <read_first>
    - lib/services/generate-estimate.ts:169-227 (photo query + photoDescriptions)
    - lib/queries/estimate.ts (the photos query used by generation — confirm whether caption is selected)
    - lib/ai/prompt-builder.ts:131-174 (buildUserContent — confirm the sanitizeField wrapping covers the whole photoDescriptions element)
  </read_first>
  <action>
1. Ensure the generation photos query selects `caption` (add it if missing).
2. Fold caption into photoDescriptions per interface.
3. Test: a photo with caption 'north wall, 12ft ceiling' → the built user content contains the caption inside the <photo_description> block (sanitized); a photo with no caption → byte-identical to today; a caption with injection-y chars is escaped by sanitizeField (assert the <> are escaped).
  </action>
  <acceptance_criteria>
    - Caption appears in the prompt (sanitized); no-caption case byte-identical
    - Caption goes through the SAME sanitizeField path as ai_description (no parallel unsanitized route — grep/assert)
    - `npm test` GREEN; `npx tsc --noEmit -p tsconfig.ci.json` exits 0
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/services/generate-estimate-captions.test.ts</automated></verify>
  <done>Typed context reaches the estimator.</done>
</task>

<task type="auto">
  <name>Task 2: "N of M photos analyzed" UI (PHOTO-02 UI half)</name>
  <files>lib/actions/attempt-outcome.ts, lib/estimate/poll-outcome.ts, components/capture/capture-processing-overlay.tsx, components/capture/capture-recorder.tsx, tests/unit/estimate/poll-outcome-counts.test.ts</files>
  <read_first>
    - lib/actions/attempt-outcome.ts (FULL — the .select at :120, the pending shape :48-54 returned :192-198 — THIS is where the journal metadata read must be added)
    - lib/estimate/poll-outcome.ts (StageProgress :54, the pending branch :182-186, getAttemptOutcome delegation :167)
    - components/capture/capture-processing-overlay.tsx (:102 analyze-step label, consumed at capture-recorder.tsx:1291-1298)
    - components/capture/capture-recorder.tsx (handleStageProgress :654-665 + attemptProgress state; 169-01 edited the upload/IDB region — disjoint)
    - analyze-photos.ts:346-350 (confirm the exact metadata keys analyzedCount/totalCount/failedCount)
  </read_first>
  <action>
Follow the interface's 4-step threading path exactly: (1) attempt-outcome.ts adds `metadata` to .select + extracts counts into the pending payload; (2) poll-outcome.ts StageProgress + pending branch forward them; (3) capture-recorder.tsx handleStageProgress → attemptProgress → new overlay prop; (4) capture-processing-overlay.tsx renders "N of M photos analyzed" on partial coverage (analyzedCount < totalCount OR failedCount > 0), English via t().
Tests: attempt-outcome extraction (a journal analyze row with metadata {20,35,0} → pending carries the counts); poll-outcome forwarding (mock getAttemptOutcome pending-with-counts → StageProgress carries them through onStageProgress).
  </action>
  <acceptance_criteria>
    - attempt-outcome selects metadata and surfaces counts; StageProgress carries them; keys match 168-01's exact names
    - Partial-coverage subtitle renders on the overlay (component test optional if cheap; the extraction + forwarding tests are required)
    - `npm test` GREEN; `npx tsc --noEmit -p tsconfig.ci.json` exits 0
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/estimate/poll-outcome-counts.test.ts</automated></verify>
  <done>The user sees coverage truth, closing PHOTO-02's UI half.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/services/generate-estimate-captions.test.ts tests/unit/estimate/poll-outcome-counts.test.ts
npx tsc --noEmit -p tsconfig.ci.json
npm test
```
Regression contracts: prompt-injection hardening preserved (caption through sanitizeField); no-caption prompt byte-identical; 166-02 post-AI region untouched; 168-01 worker untouched; 169-01's capture-recorder upload/IDB region untouched.
</verification>

<success_criteria>
- PHOTO-01 TRUE (captions in prompt); PHOTO-02 UI half complete (N-of-M visible). Phase 168 fully complete (2/2 plans).
</success_criteria>

<output>
Create `.planning/phases/168-photo-pipeline-fidelity/168-02-SUMMARY.md`: the caption folding, the sanitize path, the N-of-M threading, and test evidence. Confirm PHOTO-01 complete in REQUIREMENTS.md (and re-affirm PHOTO-02 now has its UI).
</output>
