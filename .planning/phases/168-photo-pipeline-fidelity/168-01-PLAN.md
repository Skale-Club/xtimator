---
phase: 168-photo-pipeline-fidelity
plan: 01
type: execute
wave: 2
depends_on:
  - "167-01"
files_modified:
  - lib/inngest/functions/analyze-photos.ts
  - lib/ai/openrouter-client.ts
  - lib/ai/providers/gemini.ts
  - lib/estimate/ingest/multimodal.ts
  - tests/unit/inngest/analyze-photos-coverage.test.ts
  - tests/unit/ai/vision-truncation.test.ts
autonomous: true
requirements:
  - PHOTO-02
  - PHOTO-03
  - PHOTO-04
must_haves:
  truths:
    - "A project with 35 photos gets ALL 35 analyzed (chunked within the job) — the first-20 cutoff is gone."
    - "Photos are loaded with 'ai_description IS NULL' filtering so a re-dispatch analyzes only the remainder — already-analyzed photos are never re-charged."
    - "One failing photo no longer rejects the batch: failures are collected, survivors proceed, the generate dispatch fires when >=1 photo succeeded; the job hard-fails ONLY when zero photos succeed."
    - "Vision responses with finish_reason 'length' are retried once at a higher cap or truncated at a sentence boundary — a mid-sentence cutoff is never persisted verbatim; Gemini vision gets an equivalent token cap."
    - "The pipeline event/journal carries analyzed/total counts so the UI can show 'N of M photos analyzed' (UI wiring lands in 168-02)."
  artifacts:
    - path: "lib/inngest/functions/analyze-photos.ts"
      provides: "chunked full-coverage loading, skip-and-continue, counts in pipeline events"
    - path: "lib/ai/openrouter-client.ts"
      provides: "analyzePhotoOR finish_reason handling + raised cap (vision section ONLY)"
  key_links:
    - from: "lib/inngest/functions/analyze-photos.ts"
      to: "lib/estimate/ingest/multimodal.ts"
      via: "same skip-and-continue failure policy (parity, audit E3)"
      pattern: "skip"
---

<objective>
Close audit findings E1/E3/E4 (PHOTO-02/03/04). PHOTO-01 (captions in the generation prompt) is plan 168-02 — do NOT touch lib/services/generate-estimate.ts here (Phase 166-02 contends on it). Sequenced after 167-01 because both touch lib/ai/openrouter-client.ts (167-01 owns the transcription section; this plan owns the VISION section — analyzePhotoOR and its Gemini fallback only).

Evidence (REQUIRED READING): .planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md § E. Key facts: load-photos is `.order('sort_order').limit(20)` (analyze-photos.ts:151) with no ai_description IS NULL filter — photos 21-50 are unreachable by ANY number of dispatches; `Promise.all` at :158 rejects on first exhausted per-photo step → whole job fails, generate never dispatches, already-persisted descriptions linger unmetered; max_tokens 300 with finish_reason never read (openrouter-client.ts:237, :278) → mid-sentence truncations persist; Gemini vision fallback has NO cap (gemini.ts:82-90); refine's ingestMultimodal (multimodal.ts:62-69) already implements skip-and-continue — the policy to mirror. Server cap: 50 photos/project (photo.ts:35). Per-photo step.run checkpointing must be preserved (it is what prevents intra-job re-charge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/audits/v4.19-ESTIMATE-DEEP-AUDIT.md
@lib/inngest/functions/analyze-photos.ts
@lib/ai/openrouter-client.ts
@lib/estimate/ingest/multimodal.ts
@lib/ai/providers/gemini.ts

<interfaces>
Coverage: replace `.limit(20)` with a full load filtered `.is('ai_description', null)` (+ keep company_id AND project_id scoping — the audit-verified cross-tenant guard MUST remain), processed in chunks of 10 sequential `Promise.allSettled` batches (bounds concurrency/memory; per-photo step.run names stay stable `vision-${photo.id}` for Inngest memoization).
Skip-and-continue: `Promise.allSettled` per chunk; collect `{succeeded: string[], failed: {photoId, reason}[]}`. After all chunks: if succeeded.length === 0 AND failed.length > 0 → throw (job fails, onFailure fires — today's semantics for total failure). Else → proceed to generate dispatch as today; record pipeline event with `{analyzedCount, totalCount, failedCount}` in metadata (poll/journal already carries metadata — verify the shape and extend additively).
Vision truncation: extend the vision response read to check finish_reason; on 'length' → ONE retry with max_tokens 600; if still 'length' → trim the text at the last sentence-ending punctuation before persisting (never persist a mid-sentence cut). Raise the base cap 300 → 450. Gemini fallback: set maxOutputTokens 450 (equivalence).
Cost note: do NOT thread costContext here — that is 167-02's single job (keeps requirement→phase mapping clean). Leave the analyzePhotoOR call signature untouched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full coverage + chunking + re-analyze filter (PHOTO-02)</name>
  <files>lib/inngest/functions/analyze-photos.ts, tests/unit/inngest/analyze-photos-coverage.test.ts</files>
  <read_first>
    - lib/inngest/functions/analyze-photos.ts (FULL)
    - lib/estimate/poll-outcome.ts + lib/estimate/progress-model.ts (journal metadata shape the counts must ride)
  </read_first>
  <action>
Implement the coverage interface: null-filtered full load (company+project scoped), chunks of 10, stable step names, counts into the pipeline event metadata (additive — existing consumers unaffected). Tests: 35 photos → all get vision steps; 20 already-analyzed + 15 null → only 15 processed; scoping still company-bound (assert query filters).
  </action>
  <acceptance_criteria>
    - `.limit(20)` gone; `.is('ai_description', null)` present; company_id scoping intact (explicit test)
    - 35-photo case fully covered across chunks; step names stable
    - Counts land in pipeline event metadata
    - `npm test` GREEN
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/inngest/analyze-photos-coverage.test.ts</automated></verify>
  <done>Every photo reachable; re-dispatch analyzes only the remainder.</done>
</task>

<task type="auto">
  <name>Task 2: Skip-and-continue failure policy (PHOTO-03)</name>
  <files>lib/inngest/functions/analyze-photos.ts</files>
  <read_first>
    - lib/estimate/ingest/multimodal.ts:55-70 (the policy to mirror)
    - lib/inngest/functions/analyze-photos.ts onFailure block (:69-114) + generate dispatch (:189)
  </read_first>
  <action>
Promise.allSettled per interface; zero-success → throw (preserving onFailure/notification semantics); partial → dispatch generate as today + failedCount in the event metadata + a structured log per failed photo. Ensure record-usage/record-credit-debit still run on partial success (they currently only run on full success — the audit flagged unmetered partial batches; fix: run them when succeeded.length > 0, units = succeeded count if the usage call is unit-aware — inspect recordUsage call and keep its existing signature semantics).
  </action>
  <acceptance_criteria>
    - 1-of-5-fails case: 4 descriptions persist, generate dispatches, usage/debit steps run, failedCount=1 recorded
    - 5-of-5-fail case: job throws, onFailure fires (existing test/behavior preserved)
    - `npm test` GREEN
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/inngest/analyze-photos-coverage.test.ts</automated></verify>
  <done>One bad photo costs one photo, not the estimate.</done>
</task>

<task type="auto">
  <name>Task 3: Vision truncation handling + cap parity (PHOTO-04)</name>
  <files>lib/ai/openrouter-client.ts, lib/ai/providers/gemini.ts, tests/unit/ai/vision-truncation.test.ts</files>
  <read_first>
    - lib/ai/openrouter-client.ts:210-330 (vision section ONLY — analyzePhotoOR, PHOTO_PROMPT, response read at :278, empty-guard :321-323)
    - lib/ai/providers/gemini.ts:60-95 (analyzePhotoGemini)
  </read_first>
  <action>
Implement the truncation interface: finish_reason check, one 600-cap retry, sentence-boundary trim fallback, base cap 450; Gemini maxOutputTokens 450. Preserve the empty-string throw (audit-verified guard). DO NOT touch transcribeAudioOR or anything 167-01 changed. Tests: finish_reason length → retry fired; still length → persisted text ends at sentence boundary; normal stop → unchanged text.
  </action>
  <acceptance_criteria>
    - Truncation tests GREEN; empty-guard behavior preserved
    - Transcription section untouched (scoped git-diff evidence in SUMMARY)
    - `npm test` GREEN
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/ai/vision-truncation.test.ts</automated></verify>
  <done>Descriptions are complete sentences from either provider.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/inngest/analyze-photos-coverage.test.ts tests/unit/ai/vision-truncation.test.ts
npx tsc --noEmit -p tsconfig.ci.json
npm test
```
Regression contracts: per-photo step.run checkpointing preserved; company+project scoping preserved (cross-tenant guard); onFailure semantics for total failure preserved; generate-estimate.ts NOT touched.
</verification>

<success_criteria>
- Phase 168 criteria 2, 3, 4 TRUE (criterion 1 = 168-02 captions + N-of-M UI)
</success_criteria>

<output>
Create `.planning/phases/168-photo-pipeline-fidelity/168-01-SUMMARY.md`: coverage mechanics, failure-policy decision table, truncation strategy, and metering-on-partial note.
</output>
