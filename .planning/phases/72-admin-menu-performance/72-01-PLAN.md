---
phase: 72-admin-menu-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/loading.tsx
  - app/admin/branding/loading.tsx
  - app/admin/seo/loading.tsx
  - app/admin/landing/loading.tsx
  - app/admin/billing/loading.tsx
  - app/admin/admins/loading.tsx
  - app/admin/integrations/loading.tsx
  - app/admin/blog/loading.tsx
  - app/admin/blog/new/loading.tsx
  - app/admin/blog/[id]/loading.tsx
  - app/admin/page.tsx
  - app/admin/branding/page.tsx
  - app/admin/seo/page.tsx
  - app/admin/landing/page.tsx
autonomous: true
requirements:
  - PERF-ADMIN-01
  - PERF-ADMIN-02

must_haves:
  truths:
    - "Clicking any admin nav link shows a skeleton immediately — no blank screen while page data loads"
    - "Every admin route segment has a loading.tsx file at its level"
    - "Stable admin pages (dashboard, branding, seo, landing) use ISR revalidate=60, not force-dynamic"
    - "Billing, blog, and admins pages keep force-dynamic (or no directive) since their data must stay current"
  artifacts:
    - path: "app/admin/loading.tsx"
      provides: "Admin dashboard skeleton (3-column stat card grid)"
    - path: "app/admin/branding/loading.tsx"
      provides: "Branding page skeleton (single full-width form card)"
    - path: "app/admin/seo/loading.tsx"
      provides: "SEO page skeleton (single form card)"
    - path: "app/admin/landing/loading.tsx"
      provides: "Landing editor skeleton (tall form card)"
    - path: "app/admin/billing/loading.tsx"
      provides: "Billing page skeleton (stat card + table)"
    - path: "app/admin/admins/loading.tsx"
      provides: "Admins page skeleton (header row + 3 rows)"
    - path: "app/admin/integrations/loading.tsx"
      provides: "Integrations page skeleton (2 cards)"
    - path: "app/admin/blog/loading.tsx"
      provides: "Blog list skeleton (header + 3 row stubs)"
    - path: "app/admin/blog/new/loading.tsx"
      provides: "New post skeleton (single form card)"
    - path: "app/admin/blog/[id]/loading.tsx"
      provides: "Edit post skeleton (single form card)"
  key_links:
    - from: "app/admin/loading.tsx"
      to: "Next.js App Router streaming"
      via: "loading.tsx automatic Suspense boundary"
      pattern: "loading\\.tsx"
    - from: "app/admin/page.tsx"
      to: "ISR cache"
      via: "export const revalidate = 60"
      pattern: "revalidate = 60"
---

<objective>
Create all missing admin route loading.tsx skeleton files so Next.js App Router streaming kicks in for every admin navigation. Also switch four stable admin pages from force-dynamic to ISR (revalidate = 60) so cached HTML is served immediately on warm navigation.

Purpose: Without loading.tsx files, Next.js cannot stream the admin shell and skeleton — the browser sees a blank screen until the page server component finishes all async fetches. Adding loading.tsx at every route segment fixes this with zero JS overhead. Switching stable pages to ISR eliminates the DB round-trip entirely on cached requests.

Output: 10 new loading.tsx files; 4 pages updated from force-dynamic to revalidate = 60.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/72-admin-menu-performance/72-CONTEXT.md
@.planning/phases/72-admin-menu-performance/72-RESEARCH.md

<interfaces>
From components/ui/skeleton.tsx:
```typescript
// Accepts any React.ComponentProps<"div">; override className to set size and color
function Skeleton({ className, ...props }: React.ComponentProps<"div">)

// Glass token override for admin dark theme (use instead of default bg-muted):
<Skeleton className="h-10 w-48 bg-[var(--glass-bg)]" />
<Skeleton className="h-64 w-full bg-[var(--glass-bg)] rounded-xl" />
```

Glass CSS tokens available in [data-theme="admin-dark"] scope:
  --glass-bg:        rgba(20, 24, 33, 0.60)
  --glass-bg-light:  rgba(255, 255, 255, 0.04)
  --glass-border:    rgba(255, 255, 255, 0.08)

Admin page skeleton shapes (from direct page inspection):
  /admin             -> flex flex-col gap-8 | h1 + grid-cols-1 sm:grid-cols-3 stat cards min-h-[120px]
  /admin/branding    -> flex flex-col gap-8 | h1 + Card variant="glass" p-6 md:p-8 form
  /admin/seo         -> flex flex-col gap-8 | h1 + Card variant="glass" p-6 md:p-8 form
  /admin/landing     -> flex flex-col gap-8 | h1 + Card variant="glass" p-6 md:p-8 tall form
  /admin/billing     -> space-y-8 | h1 + Card variant="stat" max-w-sm min-h-[120px] + Card variant="glass" table
  /admin/admins      -> space-y-8 | h1+button-row + Card variant="glass" p-6 with admin rows
  /admin/integrations -> flex flex-col gap-6 | p description + flex col of IntegrationCards h-24
  /admin/blog        -> space-y-8 | h1+button + Card variant="glass" with blog table rows h-14
  /admin/blog/new    -> flex flex-col gap-8 | h1 + Card variant="glass" p-6 md:p-8 form
  /admin/blog/[id]   -> flex flex-col gap-8 | h1 + Card variant="glass" p-6 md:p-8 form
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeleton files for all 10 admin route segments</name>
  <files>
    app/admin/loading.tsx
    app/admin/branding/loading.tsx
    app/admin/seo/loading.tsx
    app/admin/landing/loading.tsx
    app/admin/billing/loading.tsx
    app/admin/admins/loading.tsx
    app/admin/integrations/loading.tsx
    app/admin/blog/loading.tsx
    app/admin/blog/new/loading.tsx
    app/admin/blog/[id]/loading.tsx
  </files>
  <read_first>
    - components/ui/skeleton.tsx (Skeleton import path and className API)
    - app/admin/page.tsx (dashboard layout to mirror: grid-cols-1 sm:grid-cols-3 stat cards)
    - app/admin/branding/page.tsx (layout to mirror: h1 + Card variant=glass p-6 md:p-8)
    - app/admin/seo/page.tsx (layout to mirror: h1 + Card variant=glass)
    - app/admin/landing/page.tsx (layout to mirror: h1 + Card variant=glass tall form)
    - app/admin/billing/page.tsx (layout: h1 + stat Card max-w-sm + glass Card table)
    - app/admin/admins/page.tsx (layout: h1 + AddAdminDialog button + Card variant=glass admin rows)
    - app/admin/integrations/[slug]/page.tsx (layout: p desc + flex col of IntegrationCards)
    - app/admin/blog/page.tsx (layout: h1 + New post button + Card variant=glass table rows)
    - app/admin/blog/new/page.tsx (layout: h1 + Card variant=glass form)
    - app/admin/blog/[id]/page.tsx (layout: h1 + Card variant=glass form)
  </read_first>
  <action>
Create 10 new loading.tsx files. Each must:
1. Import Skeleton from '@/components/ui/skeleton'
2. Export a default function named {PageName}Loading
3. Mirror the dominant structure of its real page
4. Use className="bg-[var(--glass-bg)]" on every Skeleton (per D-03 — matches admin dark glass cards)

Exact content for each file:

--- app/admin/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function AdminDashboardLoading() {
  return (
    <div className="flex flex-col gap-8">
      <div className="flex flex-col gap-2">
        <Skeleton className="h-9 w-36 bg-[var(--glass-bg)]" />
        <Skeleton className="h-4 w-56 bg-[var(--glass-bg)]" />
      </div>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-3">
        {[0, 1, 2].map((i) => (
          <Skeleton key={i} className="min-h-[120px] rounded-xl bg-[var(--glass-bg)]" />
        ))}
      </div>
    </div>
  )
}

--- app/admin/branding/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function BrandingLoading() {
  return (
    <div className="flex flex-col gap-8">
      <div className="flex flex-col gap-2">
        <Skeleton className="h-9 w-32 bg-[var(--glass-bg)]" />
        <Skeleton className="h-4 w-72 bg-[var(--glass-bg)]" />
      </div>
      <Skeleton className="h-72 w-full bg-[var(--glass-bg)] rounded-xl" />
    </div>
  )
}

--- app/admin/seo/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function SeoLoading() {
  return (
    <div className="flex flex-col gap-8">
      <div className="flex flex-col gap-2">
        <Skeleton className="h-9 w-40 bg-[var(--glass-bg)]" />
        <Skeleton className="h-4 w-72 bg-[var(--glass-bg)]" />
      </div>
      <Skeleton className="h-56 w-full bg-[var(--glass-bg)] rounded-xl" />
    </div>
  )
}

--- app/admin/landing/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function LandingLoading() {
  return (
    <div className="flex flex-col gap-8">
      <div className="flex flex-col gap-2">
        <Skeleton className="h-9 w-56 bg-[var(--glass-bg)]" />
        <Skeleton className="h-4 w-80 bg-[var(--glass-bg)]" />
      </div>
      <Skeleton className="h-96 w-full bg-[var(--glass-bg)] rounded-xl" />
    </div>
  )
}

--- app/admin/billing/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function BillingLoading() {
  return (
    <div className="space-y-8">
      <div className="flex flex-col gap-2">
        <Skeleton className="h-9 w-24 bg-[var(--glass-bg)]" />
        <Skeleton className="h-4 w-80 bg-[var(--glass-bg)]" />
      </div>
      <Skeleton className="min-h-[120px] max-w-sm bg-[var(--glass-bg)] rounded-xl" />
      <Skeleton className="h-64 w-full bg-[var(--glass-bg)] rounded-xl" />
    </div>
  )
}

--- app/admin/admins/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function AdminsLoading() {
  return (
    <div className="space-y-8">
      <div className="flex items-start justify-between gap-4">
        <div className="space-y-2">
          <Skeleton className="h-9 w-40 bg-[var(--glass-bg)]" />
          <Skeleton className="h-4 w-72 bg-[var(--glass-bg)]" />
        </div>
        <Skeleton className="h-9 w-32 bg-[var(--glass-bg)] rounded-lg" />
      </div>
      <Skeleton className="h-48 w-full bg-[var(--glass-bg)] rounded-xl" />
    </div>
  )
}

--- app/admin/integrations/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function IntegrationsLoading() {
  return (
    <div className="flex flex-col gap-6">
      <Skeleton className="h-4 w-96 bg-[var(--glass-bg)]" />
      <div className="flex flex-col gap-4">
        <Skeleton className="h-24 w-full bg-[var(--glass-bg)] rounded-xl" />
        <Skeleton className="h-24 w-full bg-[var(--glass-bg)] rounded-xl" />
      </div>
    </div>
  )
}

--- app/admin/blog/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function BlogLoading() {
  return (
    <div className="space-y-8">
      <div className="flex items-center justify-between gap-4">
        <div className="space-y-1">
          <Skeleton className="h-9 w-32 bg-[var(--glass-bg)]" />
          <Skeleton className="h-4 w-56 bg-[var(--glass-bg)]" />
        </div>
        <Skeleton className="h-9 w-24 bg-[var(--glass-bg)] rounded-lg" />
      </div>
      <div className="flex flex-col gap-2">
        {[0, 1, 2].map((i) => (
          <Skeleton key={i} className="h-14 w-full bg-[var(--glass-bg)] rounded-lg" />
        ))}
      </div>
    </div>
  )
}

--- app/admin/blog/new/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function NewPostLoading() {
  return (
    <div className="flex flex-col gap-8">
      <Skeleton className="h-9 w-44 bg-[var(--glass-bg)]" />
      <Skeleton className="h-64 w-full bg-[var(--glass-bg)] rounded-xl" />
    </div>
  )
}

--- app/admin/blog/[id]/loading.tsx ---
import { Skeleton } from '@/components/ui/skeleton'
export default function EditPostLoading() {
  return (
    <div className="flex flex-col gap-8">
      <Skeleton className="h-9 w-40 bg-[var(--glass-bg)]" />
      <Skeleton className="h-64 w-full bg-[var(--glass-bg)] rounded-xl" />
    </div>
  )
}
  </action>
  <verify>
    <automated>find /c/Users/Vanildo/Dev/xtimator/app/admin -name "loading.tsx" | wc -l && npx --prefix /c/Users/Vanildo/Dev/xtimator tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `find app/admin -name "loading.tsx" | wc -l` returns 10
    - Every file contains `export default function` (grep confirms)
    - Every file contains `from '@/components/ui/skeleton'` (grep confirms)
    - Every file contains `bg-[var(--glass-bg)]` on at least one Skeleton (grep confirms)
    - `npx tsc --noEmit` exits 0 with no new errors referencing these files
  </acceptance_criteria>
  <done>10 loading.tsx files exist across all admin route segments; each mirrors page structure; glass token used; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Switch stable admin pages from force-dynamic to ISR revalidate=60</name>
  <files>
    app/admin/page.tsx
    app/admin/branding/page.tsx
    app/admin/seo/page.tsx
    app/admin/landing/page.tsx
  </files>
  <read_first>
    - app/admin/page.tsx (line 6: `export const dynamic = 'force-dynamic'`)
    - app/admin/branding/page.tsx (line 6: `export const dynamic = 'force-dynamic'`)
    - app/admin/seo/page.tsx (line 6: `export const dynamic = 'force-dynamic'`)
    - app/admin/landing/page.tsx (line 6: `export const dynamic = 'force-dynamic'`)
  </read_first>
  <action>
In each of the four files, make exactly one change: replace `export const dynamic = 'force-dynamic'` with `export const revalidate = 60`.

CRITICAL — do NOT keep both directives. `force-dynamic` wins over `revalidate` if both are present. Remove the `dynamic` export line and replace it with the `revalidate` line. All other file content stays unchanged.

Per D-04: these pages serve relatively stable data. 60-second ISR staleness is acceptable for a platform admin panel. On the next admin navigation after a warm cache hit, Next.js serves the cached HTML instantly rather than waiting for DB queries.

Do NOT change: app/admin/billing/page.tsx (keep force-dynamic — MRR/tier counts should be current), app/admin/admins/page.tsx (no directive — keep dynamic), app/admin/blog/page.tsx and blog sub-pages (keep force-dynamic — blog post status must reflect latest).

The revalidatePath('/admin/branding'), revalidatePath('/admin/seo'), revalidatePath('/admin/landing'), revalidatePath('/admin/page') calls already in the corresponding actions files will correctly bust the ISR cache when data changes — no action-file changes needed.
  </action>
  <verify>
    <automated>grep -n "force-dynamic\|revalidate" /c/Users/Vanildo/Dev/xtimator/app/admin/page.tsx /c/Users/Vanildo/Dev/xtimator/app/admin/branding/page.tsx /c/Users/Vanildo/Dev/xtimator/app/admin/seo/page.tsx /c/Users/Vanildo/Dev/xtimator/app/admin/landing/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep output shows `revalidate = 60` (not `force-dynamic`) in all four files
    - `grep "force-dynamic" app/admin/page.tsx app/admin/branding/page.tsx app/admin/seo/page.tsx app/admin/landing/page.tsx` returns no matches
    - app/admin/billing/page.tsx still contains `export const dynamic = 'force-dynamic'` (unchanged)
    - app/admin/blog/page.tsx still contains `export const dynamic = 'force-dynamic'` (unchanged)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>4 stable admin pages use ISR revalidate=60; 6 dynamic-data pages unchanged; TypeScript clean.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `find app/admin -name "loading.tsx" | wc -l` returns 10
2. `grep -rn "revalidate = 60" app/admin/` shows hits in page.tsx, branding/page.tsx, seo/page.tsx, landing/page.tsx
3. `grep -rn "force-dynamic" app/admin/` shows NO hits in the 4 converted pages; billing, blog pages still have it
4. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Every /admin/* route segment has a loading.tsx sibling — Next.js streams the skeleton on navigation without any blank flash (PERF-ADMIN-01, PERF-ADMIN-02)
- 4 stable admin pages use revalidate=60 ISR — cached HTML served instantly on warm requests
- No regressions: all admin pages still render their data correctly; revalidatePath() calls in action files bust ISR cache on writes
</success_criteria>

<output>
After completion, create .planning/phases/72-admin-menu-performance/72-01-SUMMARY.md
</output>
