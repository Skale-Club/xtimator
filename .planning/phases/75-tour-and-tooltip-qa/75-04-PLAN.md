---
phase: 75-tour-and-tooltip-qa
plan: 04
type: execute
wave: 4
depends_on: ["75-03"]
files_modified:
  - tests/visual/tour-uat-runbook.md
  - .planning/known-issues.md
autonomous: true
requirements:
  - TOUR-FIX-07
must_haves:
  truths:
    - "Every tooltip text string flows through t() — verified by grep across all 5 ContextualTooltip mount sites"
    - "A UAT runbook lists every tooltip + every spotlight step with a check column for EN, PT, ES"
    - "All findings (zero-or-N) are captured in .planning/known-issues.md per FIX-02 convention"
    - "Final unit + e2e test runs are green: vitest tour suites and playwright tour-flow.spec"
    - "TypeScript clean: npx tsc --noEmit reports zero errors"
  artifacts:
    - path: "tests/visual/tour-uat-runbook.md"
      provides: "Manual UAT checklist for EN/PT/ES across tour surfaces"
      min_lines: 80
    - path: ".planning/known-issues.md"
      provides: "Findings log (may already exist — append section)"
      contains: "Phase 75"
  key_links:
    - from: "tests/visual/tour-uat-runbook.md"
      to: "tests/visual/tour-inventory.md"
      via: "cross-reference"
      pattern: "tour-inventory.md"
---

<objective>
Close Phase 75: verify every tooltip string is i18n-wrapped, walk the manual UAT in EN/PT/ES, log findings, and run the final green-bar.

Purpose: TOUR-FIX-07 — manual UAT in 3 languages with findings captured.
Output: Runbook doc, known-issues entry, final test runs green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-tour-and-tooltip-qa/75-CONTEXT.md
@.planning/phases/75-tour-and-tooltip-qa/75-RESEARCH.md
@.planning/phases/75-tour-and-tooltip-qa/75-01-SUMMARY.md
@.planning/phases/75-tour-and-tooltip-qa/75-02-SUMMARY.md
@.planning/phases/75-tour-and-tooltip-qa/75-03-SUMMARY.md
@tests/visual/tour-inventory.md

@components/tour/contextual-tooltip.tsx
@components/tour/tour-spotlight.tsx
@components/app-shell/topbar.tsx
@components/app-shell/sidebar.tsx
@components/workspace/estimate/estimate-totals.tsx
@components/workspace/send/plain-text-card.tsx
@lib/i18n/use-translation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n verification + UAT runbook (TOUR-FIX-07)</name>
  <files>tests/visual/tour-uat-runbook.md</files>
  <action>
**Step A — verify i18n wrapping** at every ContextualTooltip call site. Use Grep to check that each call passes `text="..."` (a plain English string) — the translation happens inside `ContextualTooltip` via `t(text)` per the 75-02 rewrite. The 5 sites (from RESEARCH) are:

- `components/app-shell/topbar.tsx:68`
- `components/app-shell/sidebar.tsx:115` (two mounts — clients and priceBook)
- `components/workspace/estimate/estimate-totals.tsx:126`
- `components/workspace/send/plain-text-card.tsx:71`

Also confirm `t()` is used in `tour-spotlight.tsx` for step title/description and the Back/Next/Done/Skip labels (already confirmed in RESEARCH; just re-check after 75-03's edits).

If any site passes a non-string expression (variable, template literal with interpolation) — log it under "Findings" in the runbook below. Do not block the plan.

**Step B — author `tests/visual/tour-uat-runbook.md`**. Contents:

```markdown
# Phase 75 — Tour & Tooltip Manual UAT Runbook

**Purpose:** Walk every tooltip surface and every spotlight step in EN, PT-BR, and ES. Log findings in `.planning/known-issues.md` per FIX-02 convention.

**Companion doc:** `tests/visual/tour-inventory.md` (canonical inventory of mount sites).

## Pre-flight

1. Start dev server: `pnpm dev`
2. Sign in as the dev seed user.
3. Open browser devtools -> Application -> Local Storage. Confirm no `tooltip_seen_*`, `tour_completed`, or `tour_spotlight_pending` keys exist. If any do, click "Restart tour" (TourHelpButton, bottom-right) which clears all `xtimator:tour:v1:*` state, then refresh.

## Per-language checklist

Repeat the entire section for each language: **EN**, **PT-BR**, **ES**. Switch via the language toggle in the topbar.

### A. No unprompted tooltips (TOUR-FIX-02)

| Page | Expected | Result (EN / PT / ES) |
|------|----------|------------------------|
| `/dashboard` on cold load | Zero tooltips visible | [ ] / [ ] / [ ] |
| `/clients` on navigation | Zero tooltips visible | [ ] / [ ] / [ ] |
| `/price-book` on navigation | Zero tooltips visible | [ ] / [ ] / [ ] |
| `/projects/[id]` workspace | Zero tooltips visible | [ ] / [ ] / [ ] |
| `/estimate/[token]` share page | Zero tooltips visible (no ContextualTooltip mounted here, but sanity-check) | [ ] / [ ] / [ ] |

### B. Hover reveals — every ContextualTooltip site (TOUR-FIX-02, TOUR-FIX-03, TOUR-FIX-07)

For each row: hover the anchor, confirm the tooltip appears with TRANSLATED text on the expected side (auto-flipped if near viewport edge), and dismisses on hover-away.

| Anchor | Page | Side prop | Translated text appears | Auto-flips on narrow viewport | Hover-away dismisses |
|--------|------|-----------|--------------------------|-------------------------------|----------------------|
| Language toggle (topbar) | any authed page | bottom | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| Clients nav link | sidebar | right | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| Price-book nav link | sidebar | right | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| Estimate total | `/projects/[id]/estimate` | (default) | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| WhatsApp send card | `/projects/[id]/send` | (default) | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |

Narrow-viewport check: resize the window to ~600px wide, hover the Language Toggle, confirm the tooltip does not overflow the right edge (Radix `collisionPadding` should auto-flip/shift).

### C. Spotlight tour walkthrough (TOUR-FIX-05)

1. Click the help button (bottom-right "?" icon). Confirm the welcome modal appears in your language.
2. Click "Show me around" / "Vamos lá" / "Empezar el tour".
3. Walk all 5 steps: new-project -> projects -> clients -> price-book -> language-toggle.
4. Confirm each step's title + description renders in the current language.
5. On the language-toggle step, confirm the spotlight highlights the VISIBLE element (topbar on desktop, bottom-nav on mobile) — not a hidden element off-screen.
6. Press ESC mid-tour: spotlight closes; focus returns to the page (verify by pressing Tab — first focusable element receives focus, not a destroyed spotlight node).
7. Re-open via help button: confirm step 1 is shown again (restart cleared state).

| Step | Highlights correct element | Title translated | Description translated | Next/Back/Done labels translated |
|------|----------------------------|------------------|------------------------|-----------------------------------|
| new-project | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| projects | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| clients | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| price-book | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| language-toggle (DESKTOP) | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |
| language-toggle (MOBILE viewport ~390px) | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] | [ ] / [ ] / [ ] |

### D. a11y preferences (TOUR-FIX-05)

1. In devtools -> Rendering, toggle `prefers-reduced-motion: reduce`. Re-run a spotlight walkthrough — transitions should be instantaneous (no slide animation between steps).
2. Toggle `prefers-reduced-transparency: reduce`. Re-open the spotlight — the tooltip card should render with a solid background, not the glass blur surface.
3. Confirm ESC still dismisses with reduced-motion on.

| Check | EN | PT | ES |
|-------|----|----|----|
| Reduced-motion: spotlight transitions skipped | [ ] | [ ] | [ ] |
| Reduced-transparency: card surface solid | [ ] | [ ] | [ ] |
| ESC dismisses with reduced-motion on | [ ] | [ ] | [ ] |

### E. Persistence (TOUR-FIX-04)

1. Complete the spotlight tour (click "Done" on last step).
2. Hard-refresh the page. Confirm the spotlight does NOT re-appear.
3. Hover the Language Toggle tooltip. Move away. Refresh. Hover again — tooltip MUST appear again (hover tooltips have no "seen" memory by design, per owner decision).
4. Click "Restart tour" (TourHelpButton). Confirm welcome modal re-appears AND devtools shows `xtimator:tour:v1:*` keys cleared.

| Check | Result |
|-------|--------|
| Spotlight does not re-appear after completion + refresh | [ ] |
| Hover tooltips re-appear after refresh (intentional — no persistence on hover) | [ ] |
| TourHelpButton restart wipes xtimator:tour:v1:* keys | [ ] |

## Findings

Capture every observed deviation here. Severity tags: `blocker` / `major` / `minor` / `nit`. After UAT, copy the table into `.planning/known-issues.md` under a "Phase 75" heading.

| # | Severity | Page | Language(s) | Description | Repro steps |
|---|----------|------|-------------|-------------|-------------|
| 1 | | | | | |
```

End the doc with a note: "If zero findings, write 'No findings — clean UAT pass in EN/PT/ES on YYYY-MM-DD' into `.planning/known-issues.md` under a Phase 75 heading (per FIX-02 convention — log either way)."
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('tests/visual/tour-uat-runbook.md','utf8');if(s.length<2500)throw new Error('runbook too short');['TOUR-FIX-02','TOUR-FIX-05','TOUR-FIX-04','EN','PT','ES','prefers-reduced-motion','prefers-reduced-transparency','TourHelpButton','language-toggle'].forEach(t=>{if(!s.includes(t))throw new Error('missing: '+t)});console.log('OK')"</automated>
  </verify>
  <done>tests/visual/tour-uat-runbook.md exists with sections A-E and a Findings table. Runbook references tour-inventory.md and covers EN/PT/ES.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the UAT in EN/PT/ES and log findings</name>
  <what-built>
- ContextualTooltip rewritten as Radix hover/focus wrapper (75-02)
- TourSpotlight a11y + visible-target + ESC + reduced-motion gates (75-03)
- Persistence migrated to `xtimator:tour:v1:*` namespace (75-01/02)
- TourHelpButton restart wipes all tour state (75-03)
- UAT runbook ready (this plan, Task 1)
  </what-built>
  <how-to-verify>
1. Start the dev server: `pnpm dev` (and `npx inngest-cli dev` if your usual dev workflow needs it — tour QA doesn't require it).
2. Open `tests/visual/tour-uat-runbook.md` in your editor.
3. Walk every checkbox in EN, then PT-BR, then ES.
4. Pay special attention to:
   - The original owner-reported bug: Language Toggle tooltip MUST NOT appear on dashboard load in any language.
   - Mobile spotlight: at viewport ~390px, the language-toggle step MUST highlight the bottom-nav element (the visible one), not the hidden topbar one.
   - ESC dismissal works mid-spotlight.
   - Restart-tour from the help button actually wipes state (verify via devtools localStorage panel).
5. Log any findings in `.planning/known-issues.md` under a new `## Phase 75 — Tour & Tooltip QA` heading. If zero findings, write a single line: "No findings — clean UAT pass in EN/PT/ES on YYYY-MM-DD (Phase 75)".
6. Commit `.planning/known-issues.md` with message `docs(75-04): log Phase 75 UAT findings`.
  </how-to-verify>
  <resume-signal>
Type "approved" if the UAT passed (with findings logged) OR "blockers: ..." listing any blocker-severity issues that need a follow-up plan before phase close.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Final green-bar — full tour suite + typecheck</name>
  <files>(no files modified — verification only)</files>
  <action>
Run the full Phase 75 verification suite. Capture the output verbatim in 75-04-SUMMARY.md.

```bash
# Unit tests (must all pass)
npx vitest run tests/unit/tour/ --reporter=verbose

# Playwright tour spec (must all pass)
npx playwright test tests/e2e/tour-flow.spec.ts --reporter=list

# TypeScript clean
npx tsc --noEmit

# Sanity: no legacy keys anywhere in the tour module
node -e "
const fs=require('fs'),path=require('path');
const dir='components/tour';
const files=fs.readdirSync(dir).filter(f=>f.endsWith('.tsx')||f.endsWith('.ts'));
const banned=['tooltip_seen_','tour_completed','tour_spotlight_pending'];
for(const f of files){
  const s=fs.readFileSync(path.join(dir,f),'utf8');
  for(const b of banned){if(s.includes(b))throw new Error(f+' still references legacy key: '+b);}
}
console.log('OK no legacy keys in components/tour/');
"

# Sanity: zero new npm deps across the whole phase
git diff --name-only $(git merge-base HEAD main)..HEAD -- package.json package-lock.json pnpm-lock.yaml | grep -c . | grep -q ^0$ && echo 'OK no dep changes' || echo 'WARN: package files changed during phase — review'
```

If any command fails, do NOT mark the phase complete — file the failure as a finding in `.planning/known-issues.md` and request orchestrator guidance.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/tour/ && npx tsc --noEmit</automated>
  </verify>
  <done>All four sanity commands report success. Unit suites green, typecheck clean, no legacy keys in the tour module, no new dependencies.</done>
</task>

</tasks>

<verification>
```bash
# Full phase green-bar
npx vitest run tests/unit/tour/
npx playwright test tests/e2e/tour-flow.spec.ts
npx tsc --noEmit

# Runbook + known-issues exist
test -f tests/visual/tour-uat-runbook.md && test -f .planning/known-issues.md && echo "OK docs exist"
```
</verification>

<success_criteria>
- [ ] tests/visual/tour-uat-runbook.md exists with EN/PT/ES sections covering tooltips, spotlight, a11y, persistence
- [ ] .planning/known-issues.md has a Phase 75 entry (either findings table or "no findings" line)
- [ ] `npx vitest run tests/unit/tour/` — green
- [ ] `npx playwright test tests/e2e/tour-flow.spec.ts` — green (or documented skips with reason)
- [ ] `npx tsc --noEmit` — clean
- [ ] No `tooltip_seen_`, `tour_completed`, `tour_spotlight_pending` strings anywhere in `components/tour/`
- [ ] No new npm dependencies added across the entire phase
</success_criteria>

<output>
Commit messages (do NOT push):
- `docs(75-04): tour & tooltip UAT runbook`
- `docs(75-04): log Phase 75 UAT findings` (user runs this after Task 2 checkpoint)
- `chore(75-04): phase 75 close — green-bar verification`

After completion, create `.planning/phases/75-tour-and-tooltip-qa/75-04-SUMMARY.md` summarizing all 4 plans, the requirements closed (TOUR-FIX-01..07), and the final command outputs from Task 3.
</output>
