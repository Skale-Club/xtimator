---
phase: 71-glassmorphism-structural-redesign
plan: 06
type: execute
wave: 3
depends_on: ["71-02", "71-05"]
files_modified:
  - app/(app)/dashboard/page.tsx
  - app/(app)/clients/page.tsx
  - app/(app)/clients/[id]/page.tsx
  - app/(app)/projects/page.tsx
  - app/(app)/projects/new/page.tsx
  - tests/e2e/visual/dashboard.spec.ts
  - tests/e2e/visual/collections.spec.ts
autonomous: true
requirements: [REDESIGN-05, REDESIGN-09]
must_haves:
  truths:
    - "Dashboard top renders hero zone (display headline greeting + primary CTA + gradient-hero backdrop)"
    - "Dashboard shows 4 stat cards using <Card variant='stat'> with mono-font value, gradient top edge"
    - "Recent items list on dashboard is FLAT (NOT glass) per perf gate — row hover uses bg-[var(--glass-bg-light)]"
    - "/clients and /projects list pages — list rows flat, page header uses display scale + optional primary 'New' CTA"
    - "Client detail page uses glass section cards for grouped info"
    - "All Suspense streaming + loading skeletons preserved"
  artifacts:
    - path: "app/(app)/dashboard/page.tsx"
      provides: "Hero zone + 4 stat cards + flat recent list"
    - path: "app/(app)/clients/page.tsx + projects/page.tsx"
      provides: "Display-scale page headers + flat dense list rows + primary 'New' CTA"
    - path: "tests/e2e/visual/dashboard.spec.ts + collections.spec.ts"
      provides: "MINTED baselines"
  key_links:
    - from: "dashboard hero"
      to: "--gradient-hero"
      via: "absolute backdrop div"
      pattern: "gradient-hero"
    - from: "stat cards"
      to: "<Card variant='stat'>"
      via: "import + JSX usage"
      pattern: "variant=\"stat\""
---

<objective>
Apply Phase 71 to dashboard + collections (`/dashboard`, `/clients`, `/clients/[id]`, `/projects`, `/projects/new`). Hero zones, stat cards, flat dense lists per UI-SPEC. Preserve Suspense + streaming + loading skeletons (Phase 17 work).

Purpose: Dashboard is the most-viewed authenticated surface; stat cards must look premium.
Output: Restyled dashboard + collections, minted baselines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-glassmorphism-structural-redesign/71-UI-SPEC.md
@.planning/phases/71-glassmorphism-structural-redesign/71-05-SUMMARY.md
@app/(app)/dashboard/page.tsx
@app/(app)/clients/page.tsx
@app/(app)/projects/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 RED — dashboard + collections visual specs</name>
  <files>tests/e2e/visual/dashboard.spec.ts, tests/e2e/visual/collections.spec.ts</files>
  <action>
    Two specs, same pattern as app-shell.spec.ts (reuse auth fixture). Pages covered:
    - `dashboard.spec.ts`: `/dashboard` — viewports × langs (skip ES for parity with shell spec).
    - `collections.spec.ts`: `/clients`, `/projects`, `/projects/new`.

    Same `freezeAnimations` + skip-if-no-auth pattern from 71-05.
  </action>
  <verify><automated>test -f tests/e2e/visual/dashboard.spec.ts && test -f tests/e2e/visual/collections.spec.ts</automated></verify>
  <done>Specs exist.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard — hero zone + 4 stat cards + flat recent list</name>
  <files>app/(app)/dashboard/page.tsx</files>
  <action>
    Restructure page top-to-bottom:

    ```tsx
    // Hero zone
    <section className="relative isolate px-6 pt-[clamp(48px,8vw,96px)] pb-12">
      <div aria-hidden className="absolute inset-0 -z-10 gradient-hero" />
      <h1 className="text-[clamp(36px,5vw,56px)] font-semibold tracking-[-0.025em] leading-[1.05]">
        {t('dashboard.greeting', { name: user.firstName })}
      </h1>
      <p className="mt-3 text-base text-muted-foreground max-w-xl">{t('dashboard.subtitle')}</p>
      <div className="mt-6 flex gap-3">
        <Button variant="primary" size="lg" asChild>
          <Link href="/projects/new">{t('dashboard.actions.newProject')}</Link>
        </Button>
      </div>
    </section>

    // 4 stat cards
    <section className="px-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-12">
      <Card variant="stat" className="p-6">
        <p className="text-xs uppercase tracking-[0.01em] text-muted-foreground">{t('dashboard.stats.activeProjects')}</p>
        <p className="mt-2 font-mono text-3xl">{count}</p>
        <p className="mt-1 text-sm text-emerald-500">↑ {delta}%</p>
      </Card>
      {/* 3 more: estimates sent, accepted rate, revenue */}
    </section>

    // Recent items — FLAT per perf gate
    <section className="px-6">
      <h2 className="text-2xl font-semibold tracking-tight mb-4">{t('dashboard.recent')}</h2>
      <ul className="divide-y divide-border rounded-lg border border-border bg-card">
        {items.map(item => (
          <li key={item.id} className="flex items-center gap-3 h-12 px-4 hover:bg-[var(--glass-bg-light)] transition-colors">
            {/* row content */}
          </li>
        ))}
      </ul>
    </section>
    ```

    KEEP all Suspense boundaries (existing `<Suspense fallback={<DashboardSkeleton />}>`). Update DashboardSkeleton if needed to match stat-card shape, otherwise leave.

    Stat values typography: `font-mono text-3xl` (mono per UI-SPEC for currency/numbers).
  </action>
  <verify>
    <automated>grep -c "variant=\"stat\"" app/(app)/dashboard/page.tsx</automated>
    Expected: ≥ 1 (could be in loop).
  </verify>
  <done>Dashboard hero + 4 stat cards + flat list; Suspense intact.</done>
</task>

<task type="auto">
  <name>Task 3: Collections — clients + projects list + new-project + client detail</name>
  <files>
    app/(app)/clients/page.tsx,
    app/(app)/clients/[id]/page.tsx,
    app/(app)/projects/page.tsx,
    app/(app)/projects/new/page.tsx
  </files>
  <action>
    **clients/page.tsx + projects/page.tsx:** identical pattern:
    ```tsx
    <div className="px-6 py-8">
      <header className="flex items-center justify-between mb-6">
        <h1 className="text-[clamp(28px,3.5vw,40px)] font-semibold tracking-tight">{t('clients.title')}</h1>
        <Button variant="primary" asChild>
          <Link href="/clients/new">{t('clients.actions.new')}</Link>
        </Button>
      </header>
      {/* Search/filter row — flat */}
      <ul className="divide-y divide-border rounded-lg border border-border bg-card">
        {/* 40px-height rows, hover bg-[var(--glass-bg-light)] — NO glass on rows */}
      </ul>
    </div>
    ```

    **clients/[id]/page.tsx:** wrap each grouped info section in `<Card variant="glass">`:
    ```tsx
    <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 px-6 py-8">
      <Card variant="glass" className="lg:col-span-2 p-6">{/* projects table */}</Card>
      <Card variant="glass" className="p-6">{/* contact info */}</Card>
    </div>
    ```

    **projects/new/page.tsx:** form wrapped in `<Card variant="glass" className="max-w-2xl mx-auto p-8">`. Submit CTA → `<Button variant="primary">`.

    Density: keep current row heights at 40px (Linear density) per UI-SPEC.
  </action>
  <verify>
    <automated>grep -rn "variant=\"primary\"" app/(app)/clients/ app/(app)/projects/ | wc -l</automated>
    Expected: ≥ 3.
  </verify>
  <done>Collections list pages have display-scale headers + flat rows; detail/new use glass cards.</done>
</task>

<task type="auto">
  <name>Task 4: Mint baselines + commit</name>
  <files>tests/e2e/visual/__screenshots__/dashboard-*.png, collections-*.png</files>
  <action>
    ```bash
    VISUAL=1 bunx playwright test tests/e2e/visual/dashboard.spec.ts tests/e2e/visual/collections.spec.ts --grep @visual --update-snapshots
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(71-06): dashboard hero + stat cards + glass-styled collections" --files app/(app)/dashboard/ app/(app)/clients/ app/(app)/projects/ tests/e2e/visual/
    ```
  </action>
  <verify><automated>git log -1 --pretty=%s | grep -q "feat(71-06)"</automated></verify>
  <done>Baselines minted, commit landed.</done>
</task>

</tasks>

<verification>
- Manual: /dashboard renders hero + 4 stat cards (gradient top edge visible) + flat recent list
- Manual: stat card value uses mono font
- Manual: /clients + /projects render 40px-height list rows with glass-bg-light hover
- Perf: scroll /dashboard with 50+ recent items — 60fps (no blur on list rows)
</verification>

<success_criteria>
- Dashboard + 4 collection surfaces restyled
- Stat cards use variant="stat", recent list is flat
- Baselines minted
- Suspense + streaming preserved
</success_criteria>

<output>
Create `.planning/phases/71-glassmorphism-structural-redesign/71-06-SUMMARY.md`.
</output>
