---
phase: 125-chat-ui
plan: 02
type: execute
wave: 3
depends_on: ["125-01"]
files_modified:
  - components/chat/chat-composer.tsx
  - components/chat/chat-thread.tsx
  - components/chat/estimate-card.tsx
  - components/chat/chat-tool-part.tsx
  - lib/actions/chat.ts
  - tests/unit/chat/chat-composer.test.tsx
  - tests/unit/chat/estimate-card.test.tsx
autonomous: true
requirements: [CHATUI-03, CHATUI-04]

must_haves:
  truths:
    - "Owner records an audio note in the chat; it is transcribed and sent as the message text"
    - "Owner attaches a job-site photo in the chat; its analysis (plus any caption) becomes the message text"
    - "When the assistant calls createEstimate, an inline card appears and polls the generation job"
    - "On completion the card shows an 'Open in editor' button linking to the existing estimate editor"
  artifacts:
    - path: "components/chat/chat-composer.tsx"
      provides: "Textarea + audio + photo input; multimodal → normalizeChatInput → sendMessage text (CHATUI-03)"
      contains: "normalizeChatInput"
    - path: "components/chat/estimate-card.tsx"
      provides: "Inline card polling useJobStatus + Open-in-editor link (CHATUI-04)"
      contains: "useJobStatus"
    - path: "lib/actions/chat.ts"
      provides: "resolveCurrentEstimateId server action (CHATUI-04 editor link)"
      contains: "resolveCurrentEstimateId"
  key_links:
    - from: "components/chat/chat-composer.tsx"
      to: "lib/actions/chat normalizeChatInput"
      via: "audio/photo → normalizeChatInput → onSend(text)"
      pattern: "normalizeChatInput"
    - from: "components/chat/estimate-card.tsx"
      to: "/projects/[id] editor"
      via: "Link href /projects/<id>?tab=estimate&estimate=<id>"
      pattern: "tab=estimate&estimate="
    - from: "components/chat/chat-tool-part.tsx"
      to: "components/chat/estimate-card.tsx"
      via: "tool-createEstimate output-available → <EstimateCard/>"
      pattern: "EstimateCard"
---

<objective>
Complete the chat input and the generation result: CHATUI-03 multimodal input (text + audio + photo) routed through the extracted `normalizeInput` via the Plan-00 `normalizeChatInput` server action, and CHATUI-04 the inline estimate card that polls the generation job and links to the existing estimate editor. Both fill the seams Plan 01 left (`CHAT_COMPOSER_SEAM`, `ESTIMATE_CARD_SEAM`).

Purpose: Bring the chat to v1 parity — the owner can speak/photograph a job and get a real estimate they open in the editor, all without a keyboard. Reuses battle-tested capture + job-poll primitives; introduces NO new domain logic.
Output: 2 new components (composer, estimate-card) + the two seam wirings + one server action; the two remaining RED scaffolds go green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-chat-ui/125-RESEARCH.md
@hooks/use-job-status.ts
@lib/actions/chat.ts
@components/chat/chat-thread.tsx
@components/chat/chat-tool-part.tsx
@lib/agent-tools/normalize-input.ts

<interfaces>
<!-- Reuse these existing, tested primitives. Do NOT rebuild. -->

Capture primitives (components/capture/capture-recorder.tsx + the libs it imports):
```typescript
import { getSupportedAudioMimeType, getFileExtension } from '@/lib/utils/media-format'
import { compressImage } from '@/lib/utils/image-compressor'   // (file, maxPx, quality) => Blob
// MediaRecorder flow: start INSIDE a user gesture (iOS Safari — Pitfall 7); mime via getSupportedAudioMimeType()
```

Plan-00 server action (lib/actions/chat.ts):
```typescript
normalizeChatInput(
  | { kind:'audio'; base64:string; ext:string }
  | { kind:'photo'; base64:string; mimeType:string; caption?:string }
): Promise<{ ok:boolean; text:string; reason?:string }>
```

Job poll (hooks/use-job-status.ts):
```typescript
useJobStatus(jobId: string | null): {
  state: 'idle'|'processing'|'completed'|'failed'|'config_unavailable'|'not_found'
  output: unknown | null; reason: string | null
}
```

createEstimate tool output-available part shape (from the backend): `{ output: { jobId: string; status: 'queued' }, input: { projectId: string, ... } }`.

Editor route (VERIFIED — app/(app)/projects/[id]/page.tsx reads searchParams.tab + .estimate):
`/projects/<projectId>?tab=estimate&estimate=<estimateId>`

Resolve the produced estimate (mirror capture-recorder lines ~686-692): the current estimate is
`estimates` where `project_id = projectId AND is_current = true`. Reuse `getCurrentEstimate(supabase, projectId)` from `lib/queries/estimate.ts` (returns the estimate incl. its `id`).

i18n: `useTranslation()` → `t(...)`. sonner `toast` for normalize/send failures.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chat-composer (text + audio + photo via normalize) + thread wiring (CHATUI-03)</name>
  <files>components/chat/chat-composer.tsx, components/chat/chat-thread.tsx, tests/unit/chat/chat-composer.test.tsx</files>
  <read_first>
    - .planning/phases/125-chat-ui/125-RESEARCH.md (Pattern 6, Pitfall 4, Pitfall 7, the Composer code example)
    - components/capture/capture-recorder.tsx (MediaRecorder gesture-bound start, getSupportedAudioMimeType, getFileExtension, compressImage usage around lines 440-450 + 860-870)
    - lib/actions/chat.ts (normalizeChatInput)
    - components/chat/chat-thread.tsx (the CHAT_COMPOSER_SEAM left by Plan 01)
  </read_first>
  <behavior>
    - Text: typing + submit (Enter w/o shift or Send) calls `onSend(text)` and clears.
    - Audio: a Mic button starts MediaRecorder INSIDE the click handler (iOS gesture — Pitfall 7), mime via `getSupportedAudioMimeType()`; on stop → blob → base64 → `normalizeChatInput({kind:'audio', base64, ext: getFileExtension(mime)})` → on `ok` call `onSend(result.text)`, else `toast.error`.
    - Photo: a Camera/paperclip button opens a file input; on pick → `compressImage(file, 2000, 0.85)` → base64 → `normalizeChatInput({kind:'photo', base64, mimeType, caption: currentText || undefined})` → on `ok` call `onSend(result.text)` (the caption is folded into the analysis text by normalizeInput) and clear the text box, else `toast.error`.
    - A busy flag disables all inputs while normalizing OR while the parent thread `status !== 'ready'`.
    - Do NOT send raw files to the model (Pitfall 4) — always normalize-to-text then `onSend({text})` upstream.
  </behavior>
  <action>
    1. Create `components/chat/chat-composer.tsx` (`'use client'`): props `{ onSend: (text: string) => void; busy: boolean }`. Implement text/audio/photo per <behavior>, reusing the capture primitives (import `getSupportedAudioMimeType`, `getFileExtension`, `compressImage`). Helper to base64 a Blob: `await new Promise<string>((res)=>{const r=new FileReader();r.onloadend=()=>res((r.result as string).split(',')[1]);r.readAsDataURL(blob)})`. Buttons: lucide `Mic` (with a recording indicator while active), `Camera`/`Paperclip`, `Send`/`Loader2`. i18n all aria-labels + placeholder via `t(...)`. Match the whatsapp reply-box layout (`flex items-end gap-2 border-t p-3`, `Textarea` `max-h-32 min-h-[40px] flex-1 resize-none`).
    2. Wire it into `components/chat/chat-thread.tsx`: replace the inline text-only composer at `{/* CHAT_COMPOSER_SEAM */}` with `<ChatComposer onSend={(t) => submit(t)} busy={status !== 'ready'} />`. Keep the `submit` function (pre-create + sendMessage) from Plan 01 unchanged.
    3. Make `tests/unit/chat/chat-composer.test.tsx` GREEN: mock `@/lib/actions/chat` (`normalizeChatInput`) and the capture libs. Assert: typing + submit fires `onSend(text)` and clears; Send disabled when `busy`; the photo path calls `normalizeChatInput({kind:'photo', ...})` and forwards the returned text to `onSend`; a failed normalize (`ok:false`) does NOT call `onSend` (and toasts). RTL `container` style, no jest-dom.
  </action>
  <acceptance_criteria>
    - `components/chat/chat-composer.tsx` contains `normalizeChatInput`, `getSupportedAudioMimeType`, `compressImage`, and a `MediaRecorder` reference inside a click handler (gesture-bound).
    - `components/chat/chat-thread.tsx` now renders `<ChatComposer` and no longer contains the inline `CHAT_COMPOSER_SEAM` placeholder composer.
    - Composer contains NO `sendMessage({ files` (no raw-file send — Pitfall 4).
    - `npx vitest run tests/unit/chat/chat-composer.test.tsx` passes (text send + clear, busy-disabled, photo→normalize→onSend, failure path no-send).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/chat/chat-composer.test.tsx</automated>
  </verify>
  <done>The composer accepts text/audio/photo, routes media through normalizeChatInput to a text message, and is wired into the thread; composer test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: estimate-card (poll job + Open-in-editor) + tool-part wiring (CHATUI-04)</name>
  <files>components/chat/estimate-card.tsx, components/chat/chat-tool-part.tsx, lib/actions/chat.ts, tests/unit/chat/estimate-card.test.tsx</files>
  <read_first>
    - .planning/phases/125-chat-ui/125-RESEARCH.md (Pattern 7, the Estimate card code example)
    - hooks/use-job-status.ts (useJobStatus discriminated states)
    - lib/queries/estimate.ts (getCurrentEstimate — project_id + is_current)
    - components/chat/chat-tool-part.tsx (the ESTIMATE_CARD_SEAM left by Plan 01)
    - app/(app)/projects/[id]/page.tsx (editor reads searchParams.tab + .estimate — confirm the route shape)
  </read_first>
  <behavior>
    - `EstimateCard` takes `{ jobId: string; projectId: string }` and polls `useJobStatus(jobId)`.
    - `processing` → a Card with `Loader2` + `t('Generating estimate…')`.
    - `failed | config_unavailable | not_found` → a friendly error Card (i18n; surface `reason` when present, no raw `Status <code>`).
    - `completed` → resolve the current estimate id via the `resolveCurrentEstimateId(projectId)` server action, then render an `Open in editor` Button (asChild + next/link) to `/projects/<projectId>?tab=estimate&estimate=<estimateId>`. While resolving, show the processing/spinner state.
  </behavior>
  <action>
    1. Add a server action to `lib/actions/chat.ts`:
    ```typescript
    import { getCurrentEstimate } from '@/lib/queries/estimate'
    import { requireServiceClient } from '@/lib/supabase/service'
    export async function resolveCurrentEstimateId(projectId: string): Promise<string | null> {
      const claims = await getAuthClaims()
      if (!claims?.sub) return null
      const est = await getCurrentEstimate(requireServiceClient() as never, projectId)
      return est?.id ?? null
    }
    ```
       (Confirm the `getCurrentEstimate` signature is `(supabase, projectId)` — read lib/queries/estimate.ts; if it self-resolves the client, drop the client arg.)
    2. Create `components/chat/estimate-card.tsx` (`'use client'`): implement <behavior> with shadcn `Card`/`CardContent`/`CardFooter`, `useJobStatus(jobId)`, a `useState<string|null>` for the resolved estimateId + a `useEffect` that calls `resolveCurrentEstimateId(projectId)` when `job.state === 'completed'`. The completed footer: `<Button asChild><Link href={`/projects/${projectId}?tab=estimate&estimate=${estimateId}`}>{t('Open in editor')}</Link></Button>`. i18n all copy; lucide `FileText`/`Loader2`/`AlertTriangle`.
    3. Wire it into `components/chat/chat-tool-part.tsx` at the `ESTIMATE_CARD_SEAM`: for `part.type === 'tool-createEstimate'` && `state === 'output-available'`, read `jobId = (part.output as {jobId:string}).jobId` and `projectId = (part.input as {projectId:string}).projectId`, render `<EstimateCard jobId={jobId} projectId={projectId} />`. (The createEstimate tool input carries `projectId` — research Pattern 7.)
    4. Make `tests/unit/chat/estimate-card.test.tsx` GREEN: mock `hooks/use-job-status` (`useJobStatus`) and `@/lib/actions/chat` (`resolveCurrentEstimateId` → 'est-123'). Assert: `processing` → spinner/generating copy present, no link; `completed` → a link whose href contains `/projects/proj-1?tab=estimate&estimate=est-123`; `failed` → error copy, no link. RTL `container` style.
  </action>
  <acceptance_criteria>
    - `components/chat/estimate-card.tsx` contains `useJobStatus` and an href literal containing `tab=estimate&estimate=`.
    - `lib/actions/chat.ts` exports `resolveCurrentEstimateId` using `getCurrentEstimate`.
    - `components/chat/chat-tool-part.tsx` renders `<EstimateCard` for the `tool-createEstimate` `output-available` branch (the `ESTIMATE_CARD_SEAM` placeholder is gone).
    - `npx vitest run tests/unit/chat/estimate-card.test.tsx` passes (processing/completed/failed; editor link href correct).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/chat/estimate-card.test.tsx</automated>
  </verify>
  <done>createEstimate completions render an inline card that polls the job and links to the existing editor; estimate-card test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/chat` → ALL green (mapper, action, message, thread, sidebar, composer, estimate-card, scope-fence).
- `npx vitest run` → full suite green (baseline ~325 files / 2279 passed + the new chat tests).
- Static: composer routes media through `normalizeChatInput` (no `sendMessage({files})`); estimate card links to `/projects/[id]?tab=estimate&estimate=<id>`.
- `app/api/chat/route.ts` + `lib/chat/tools.ts` still untouched (scope fence green).
</verification>

<success_criteria>
- CHATUI-03: the chat input accepts text + audio + photo, each routed through the extracted normalize to a text message.
- CHATUI-04: a generation completion renders an inline estimate card with a working "Open in editor" action.
- v1 chat is feature-complete and UI-only — the backend route, tools, and engine are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/125-chat-ui/125-02-SUMMARY.md`.
</output>
