---
phase: 126-chat-access-entitlement-gate
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - components/chat/chat-upgrade-prompt.tsx
  - app/(app)/chat/[[...id]]/page.tsx
  - tests/unit/chat/chat-page-gate.test.tsx
  - tests/unit/chat/chat-access-scope.test.ts
autonomous: true
requirements: [CHATMETER-02]

must_haves:
  truths:
    - "A free-tier owner who opens /chat sees an upgrade prompt (not the chat) with a CTA to /settings/billing"
    - "A pro/business owner who opens /chat sees the chat (ChatWorkspace) unchanged"
    - "The chat page reads its OWN tier (it does NOT rely on getActiveCompany, which omits tier)"
    - "Chat is referenced ONLY under app/(app)/chat + app/api/chat — no public/share/admin route exposes it (never customer-facing)"
  artifacts:
    - path: "components/chat/chat-upgrade-prompt.tsx"
      provides: "the inline upgrade affordance rendered when chat is not entitled"
      contains: "/settings/billing"
    - path: "app/(app)/chat/[[...id]]/page.tsx"
      provides: "the page-level entitlement gate (own tier read → upgrade prompt)"
      contains: "chatEnabled"
    - path: "tests/unit/chat/chat-access-scope.test.ts"
      provides: "the static never-customer-facing structural assertion"
      contains: "ChatWorkspace"
  key_links:
    - from: "app/(app)/chat/[[...id]]/page.tsx"
      to: "lib/entitlements.ts"
      via: "getEntitlements(tier).chatEnabled gate"
      pattern: "getEntitlements\\(tier\\)\\.chatEnabled"
    - from: "app/(app)/chat/[[...id]]/page.tsx"
      to: "the companies.tier column"
      via: "its OWN service-client read (NOT getActiveCompany)"
      pattern: "select\\('tier'\\)"
    - from: "app/(app)/chat/[[...id]]/page.tsx"
      to: "components/chat/chat-upgrade-prompt.tsx"
      via: "conditional render when !chatEnabled"
      pattern: "ChatUpgradePrompt"
---

<objective>
Add the page-level entitlement gate (UX) and the owner-only / never-customer-facing structural verification. When a non-entitled owner opens `/chat`, render an inline upgrade prompt instead of a dead chat (the global UpgradeModal catches 402 only, NOT the route's 403 — so the page prompt is the PRIMARY affordance). Then prove structurally that chat is never reachable by an end customer.

Purpose: closes the CHATMETER-02 UX + verification half (the route 403 from Plan 01 is the security boundary; this is the conversion-preserving prompt + the static fence proving no public surface mounts chat). This is the FINAL plan of phase 126 and milestone v4.9.
Output: a small `ChatUpgradePrompt` component; the chat page reading its OWN tier and conditionally rendering the prompt; a page-gate unit test; a static `chat-access-scope.test.ts` asserting chat lives only under `app/(app)/chat` + `app/api/chat`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-chat-access-entitlement-gate/126-RESEARCH.md
@.planning/phases/126-chat-access-entitlement-gate/126-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration required. -->

The chat page TODAY — app/(app)/chat/[[...id]]/page.tsx (the gate inserts after companyId resolution, before the ChatWorkspace return):
```typescript
export default async function ChatPage({ params }: { params: Promise<{ id?: string[] }> }) {
  const { id } = await params
  const conversationId = id?.[0] ?? null
  const claims = await getAuthClaims()
  const userId = claims?.sub as string
  const conversations = await listConversations(userId)
  // ... thread / initialMessages ...
  return <ChatWorkspace conversations={...} activeId={...} initialMessages={...} />
}
```

PITFALL 2 (load-bearing): `getActiveCompany()` does NOT select `tier` (active-company.ts:123 selects id, name, logo_url, owner_name, theme_preference, industry, currency_code). Reading `company.tier` from it returns undefined → getEntitlements(undefined) falls back to free → chat WRONGLY blocked for paying users. The page MUST do its OWN tier read.

The PROVEN own-tier read pattern — app/(app)/layout.tsx:69-73 (service client, scoped to the active company):
```typescript
requireServiceClient().from('companies').select('tier, ...').eq('id', activeCompanyId).single()
```

Tenant resolution (cookie-trusted) — lib/queries/active-company.ts:
```typescript
export function getActiveCompanyId(): Promise<string | null>
```

The page-gate precedent — app/(app)/projects/[id]/page.tsx:128-139 computes a server-side entitlement boolean from `getEntitlements((company?.tier as string) ?? 'free').whatsappEnabled` and threads it to the UI.

The 403 from the route (Plan 01) uses the SAME upgrade destination: `upgradeUrl: '/settings/billing'`.

i18n: components render copy via `useTranslation()` from `@/lib/i18n/use-translation` (client) — tests mock it as `{ t: (s: string) => s }`. ChatUpgradePrompt is a small client component using shadcn primitives + Tailwind (per 126-RESEARCH; reuse the upgrade-button/CTA styling from components/billing/upgrade-buttons.tsx).

Static-test model — tests/unit/chat/chat-ui-scope.test.ts (readFileSync + resolve(process.cwd(), rel), pure file reads, no DB/secrets). Model the new chat-access-scope.test.ts on it.

PUBLIC / non-(app) route groups under app/ to scan in the negative assertion (everything EXCEPT app/(app) and app/api/chat): app/(auth), app/(capture), app/admin, app/blog, app/demo, app/estimate, app/oauth, app/offline, app/onboarding, app/privacy-policy, app/terms-of-service.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatUpgradePrompt component + page-level entitlement gate</name>
  <files>components/chat/chat-upgrade-prompt.tsx, app/(app)/chat/[[...id]]/page.tsx</files>
  <read_first>
    - app/(app)/chat/[[...id]]/page.tsx (the FULL file — the gate inserts after `userId` resolution, before the listConversations/ChatWorkspace render)
    - app/(app)/layout.tsx:60-78 (the proven `requireServiceClient().from('companies').select('tier').eq('id', activeCompanyId)` pattern)
    - app/(app)/projects/[id]/page.tsx:125-139 (the server-side entitlement-boolean page precedent: `getEntitlements((company?.tier as string) ?? 'free').whatsappEnabled`)
    - components/billing/upgrade-buttons.tsx (reuse the upgrade CTA button styling)
    - components/chat/chat-workspace.tsx (the header/two-pane shell, so the prompt visually fits the chat surface — do NOT edit it)
  </read_first>
  <action>
    1. Create `components/chat/chat-upgrade-prompt.tsx` — a small client component (`'use client'`) using shadcn/Tailwind. It renders a centered card/panel inside the chat surface explaining that the in-app chat is a Pro/Business feature, with a primary CTA `<Link href="/settings/billing">` (or a Button-as-link) labeled e.g. "Upgrade your plan". Use `useTranslation()` from `@/lib/i18n/use-translation` for all copy (mirror how components/chat/chat-message.tsx consumes `t`). The CTA href MUST be the literal `/settings/billing` (matches the route 403 upgradeUrl). Keep it self-contained — no props required.
    2. In `app/(app)/chat/[[...id]]/page.tsx`, add imports: `getActiveCompanyId` from `@/lib/queries/active-company`, `requireServiceClient` from `@/lib/supabase/service`, `getEntitlements` from `@/lib/entitlements`, and `ChatUpgradePrompt` from `@/components/chat/chat-upgrade-prompt`.
    3. After resolving `userId` (and before `listConversations`), insert the gate — do its OWN tier read (PITFALL 2: getActiveCompany omits tier; do NOT use it):
       ```typescript
       const companyId = await getActiveCompanyId()
       const { data: companyRow } = await requireServiceClient()
         .from('companies').select('tier').eq('id', companyId!).maybeSingle()
       const tier = (companyRow as { tier?: string | null } | null)?.tier ?? 'free'
       if (!getEntitlements(tier).chatEnabled) {
         return <ChatUpgradePrompt />
       }
       ```
       Place this BEFORE the listConversations/getConversationWithMessages calls so an unentitled owner does no conversation queries. Leave the existing ChatWorkspace render path byte-unchanged below the gate. (Auth itself stays inherited from the (app) layout — the page only needs the TIER; do not add an auth check.)
    Do NOT touch ChatWorkspace, ChatThread, or any other 124/125 file (Pitfall 4 — scope fence).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "chat-upgrade-prompt|chat/\[\[" || echo "no new tsc errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - `components/chat/chat-upgrade-prompt.tsx` exists, contains the literal `/settings/billing`, and uses `useTranslation` for copy.
    - `grep -n "getEntitlements(tier).chatEnabled" "app/(app)/chat/[[...id]]/page.tsx"` returns a hit.
    - `grep -n "select('tier')" "app/(app)/chat/[[...id]]/page.tsx"` returns a hit (the page does its OWN tier read).
    - `grep -c "getActiveCompany(" "app/(app)/chat/[[...id]]/page.tsx"` returns 0 (it uses getActiveCompanyId + a direct tier read, NEVER getActiveCompany — Pitfall 2).
    - `grep -n "ChatUpgradePrompt" "app/(app)/chat/[[...id]]/page.tsx"` returns a hit (imported + conditionally rendered).
    - `npx tsc --noEmit` reports no NEW errors in the two touched source files.
  </acceptance_criteria>
  <done>The chat page reads its own tier and renders ChatUpgradePrompt (with a /settings/billing CTA) when !chatEnabled, otherwise the unchanged ChatWorkspace; getActiveCompany is not used; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Page-gate unit test + the static never-customer-facing scope test</name>
  <files>tests/unit/chat/chat-page-gate.test.tsx, tests/unit/chat/chat-access-scope.test.ts</files>
  <read_first>
    - tests/unit/chat/chat-ui-scope.test.ts (the static-source model: readFileSync + resolve(process.cwd(), rel), pure reads, no DB/secrets)
    - tests/unit/chat/chat-message.test.tsx (the RTL render + i18n-passthrough mock model for a tsx component test: `vi.mock('@/lib/i18n/use-translation', () => ({ useTranslation: () => ({ t: (s) => s }) }))`)
    - the page + ChatUpgradePrompt from Task 1
  </read_first>
  <behavior>
    Page-gate test (chat-page-gate.test.tsx):
    - When the mocked company tier is 'free' (so getEntitlements('free').chatEnabled === false), the page returns the ChatUpgradePrompt (assert the rendered output contains the prompt's distinguishing copy / the /settings/billing CTA) and does NOT render ChatWorkspace.
    - When the mocked tier is 'pro', the page renders ChatWorkspace (assert ChatWorkspace is reached — e.g. by mocking ChatWorkspace to a sentinel and asserting it rendered).
    Static scope test (chat-access-scope.test.ts):
    - The chat PAGE file exists at app/(app)/chat/[[...id]]/page.tsx and references ChatWorkspace.
    - For each public / non-(app) route group dir that exists (app/(auth), app/(capture), app/admin, app/blog, app/demo, app/estimate, app/oauth, app/offline, app/onboarding, app/privacy-policy, app/terms-of-service), NO .tsx/.ts file under it contains `ChatWorkspace`, `@/components/chat`, or the string `/api/chat`.
    - app/api/chat/route.ts contains the auth guard (`getClaims`) AND the entitlement gate (`chat_not_on_plan`) — proving the route is owner-auth + tier-gated.
  </behavior>
  <action>
    1. Create `tests/unit/chat/chat-page-gate.test.tsx`:
       - Mock the page's dependencies: `@/lib/queries/active-company` (`getActiveCompanyId` → 'company-1'), `@/lib/supabase/service` (`requireServiceClient` → a chainable mock whose `.maybeSingle()` resolves `{ data: { tier } }`, configurable per test — model the chainable builder on tests/unit/chat/route.test.ts:73-82), `@/lib/queries/auth` (`getAuthClaims` → `{ sub: 'user-1' }`), `@/lib/queries/chat` (`listConversations` → [], `getConversationWithMessages` → null), and `@/components/chat/chat-workspace` (mock `ChatWorkspace` to a sentinel like `() => <div>WORKSPACE_SENTINEL</div>`). Use the REAL `@/lib/entitlements` (so the gate reads the actual chatEnabled values). Render the awaited page (it's an async RSC — `await ChatPage({ params: Promise.resolve({}) })` then `render(...)` the returned element).
       - Test A (free → prompt): tier 'free' → assert the rendered output contains the upgrade copy / `/settings/billing` and does NOT contain WORKSPACE_SENTINEL.
       - Test B (pro → workspace): tier 'pro' → assert the output contains WORKSPACE_SENTINEL.
    2. Create `tests/unit/chat/chat-access-scope.test.ts` modeled on chat-ui-scope.test.ts (pure readFileSync/readdir, no DB):
       - it() asserts the chat page file exists and contains `ChatWorkspace`.
       - it() walks each existing public/non-(app) route group (the list in <behavior>; skip a dir gracefully if it does not exist) collecting all .ts/.tsx files, and asserts NONE contains `ChatWorkspace`, `@/components/chat`, or `/api/chat` — proving no customer-facing/admin/auth surface mounts the chat.
       - it() asserts app/api/chat/route.ts contains BOTH `getClaims` (owner auth) AND `chat_not_on_plan` (the tier gate) — the route is owner-only + entitlement-gated.
       Add a top doc comment explaining this is the CHATMETER-02 owner-only / never-customer-facing structural fence (model the comment style on chat-ui-scope.test.ts).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/chat/chat-page-gate.test.tsx tests/unit/chat/chat-access-scope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/chat/chat-page-gate.test.tsx tests/unit/chat/chat-access-scope.test.ts` passes (free→prompt, pro→workspace, and all 3 structural assertions green).
    - chat-access-scope.test.ts asserts NO public/non-(app) route group file references `ChatWorkspace` / `@/components/chat` / `/api/chat`, and that route.ts contains both `getClaims` and `chat_not_on_plan`.
    - `npx vitest run tests/unit/chat tests/unit/entitlements.test.ts` (the phase quick-run) is fully green — the Plan-01 route gate + flag + Plan-02 page gate + scope test all pass, and the Phase-125 chat-ui-scope.test.ts still passes (no regression).
  </acceptance_criteria>
  <done>The page-gate test proves free→upgrade-prompt / pro→workspace; the static scope test proves chat is never referenced outside app/(app)/chat + app/api/chat and that the route is owner-auth + tier-gated; full chat + entitlements suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/chat tests/unit/entitlements.test.ts` is fully green (flag + route 403 + page prompt + owner-only static fence + the unchanged 124/125 scope tests).
- A free-tier owner opening /chat sees ChatUpgradePrompt (CTA → /settings/billing), not a dead chat; pro/business/trial see ChatWorkspace unchanged.
- The page reads its OWN tier via a direct companies.tier read (Pitfall 2 avoided — getActiveCompany not used).
- The structural test proves no public/share/admin/auth route mounts the chat (never customer-facing) and the route is owner-auth + tier-gated.
- No DB migration, no new dependency, no secret. Phase 126 + milestone v4.9 closed.
</verification>

<success_criteria>
- `components/chat/chat-upgrade-prompt.tsx` renders the inline upgrade affordance with a /settings/billing CTA.
- `app/(app)/chat/[[...id]]/page.tsx` gates on `getEntitlements(tier).chatEnabled` using its own tier read and renders the prompt when not entitled.
- `tests/unit/chat/chat-page-gate.test.tsx` proves free→prompt / pro→workspace; `tests/unit/chat/chat-access-scope.test.ts` proves the owner-only / never-customer-facing structure.
- CHATMETER-02 fully satisfied (route boundary from Plan 01 + page UX + structural verification here).
</success_criteria>

<output>
After completion, create `.planning/phases/126-chat-access-entitlement-gate/126-02-SUMMARY.md`
</output>
