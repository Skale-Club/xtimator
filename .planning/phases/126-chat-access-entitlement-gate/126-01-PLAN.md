---
phase: 126-chat-access-entitlement-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/entitlements.ts
  - tests/unit/entitlements.test.ts
  - app/api/chat/route.ts
  - tests/unit/chat/route.test.ts
autonomous: true
requirements: [CHATMETER-02]

must_haves:
  truths:
    - "chatEnabled is false on free, true on trial/pro/business"
    - "POST /api/chat returns 403 chat_not_on_plan for a free-tier company"
    - "POST /api/chat streams (200) for a pro/business company"
    - "The route 403 gate runs BEFORE resolveChatModel/buildChatTools — no model build for an unentitled tenant"
  artifacts:
    - path: "lib/entitlements.ts"
      provides: "chatEnabled boolean on the Entitlements type + all 4 tier literals"
      contains: "chatEnabled"
    - path: "app/api/chat/route.ts"
      provides: "the security-boundary 403 entitlement gate before model resolution"
      contains: "chat_not_on_plan"
  key_links:
    - from: "app/api/chat/route.ts"
      to: "lib/entitlements.ts"
      via: "getEntitlements(tier).chatEnabled"
      pattern: "getEntitlements\\(tier\\)\\.chatEnabled"
    - from: "app/api/chat/route.ts"
      to: "the existing companies row read"
      via: "+ tier in the .select()"
      pattern: "default_estimate_language, tier"
---

<objective>
Add the `chatEnabled` per-tier entitlement flag and enforce it at the security boundary — the `/api/chat` route. This is the REQUIRED enforcement layer (the page gate in Plan 02 is additive UX only; a determined user can POST `/api/chat` directly).

Purpose: CHATMETER-02 makes the in-app chat a Pro/Business feature. The flag is the single authoritative source; the route 403 is the boundary that actually blocks free-tier traffic before any model call or tool build happens.
Output: `chatEnabled` on `Entitlements` + all 4 tiers (free=false, trial/pro/business=true); a 403 `chat_not_on_plan` gate in `app/api/chat/route.ts`; extended unit tests proving both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-chat-access-entitlement-gate/126-RESEARCH.md
@.planning/phases/126-chat-access-entitlement-gate/126-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration required. -->

From lib/entitlements.ts (the EXISTING shape — add `chatEnabled` mirroring whatsappEnabled/pdfEnabled/priceBookEnabled/customDomainEnabled):
```typescript
export type Entitlements = {
  // ...existing fields...
  whatsappEnabled: boolean
  pdfEnabled: boolean
  priceBookEnabled: boolean
  customDomainEnabled: boolean
}
export const tiers: Record<TierName, Entitlements> = { free, trial, pro, business } as const satisfies Record<TierName, Entitlements>
export function getEntitlements(tier: string): Entitlements   // falls back to tiers.free for unknown
```
NOTE: `as const satisfies Record<TierName, Entitlements>` (line 86) means adding a field to the TYPE forces adding it to ALL 4 tier literals or compilation fails — TypeScript enforces completeness for you.

The PRECEDENT 403 channel-gate — app/api/estimates/[id]/send-whatsapp/route.ts:80-86:
```typescript
const whatsappEnabled = getEntitlements((company.tier as string) ?? 'free').whatsappEnabled
if (!whatsappEnabled) {
  return NextResponse.json({ error: 'WhatsApp delivery is not available on your current plan.' }, { status: 403 })
}
```

The 402+upgradeUrl precedent — app/api/generate-estimate/route.ts:90-97 uses `upgradeUrl: '/settings/billing'`.

The EXISTING company read in app/api/chat/route.ts (route.ts:65-77) — the gate insertion point:
```typescript
const svc = requireServiceClient()
const { data: company } = await svc
  .from('companies')
  .select('industries, default_estimate_language')   // ← add `, tier`
  .eq('id', companyId)
  .maybeSingle()
// ... industries/lang derivation ...
// gate goes HERE, before: resolveChatModel(companyId) / buildChatTools(...) / streamText(...)
```
The chat route returns bare `new Response(...)` (not NextResponse.json) for its 401/400 — match that style for the 403.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chatEnabled to Entitlements + all 4 tiers (flag + tests)</name>
  <files>lib/entitlements.ts, tests/unit/entitlements.test.ts</files>
  <read_first>
    - lib/entitlements.ts (the full file — the Entitlements type at lines 8-35, the 4 tier literals at 37-86, getEntitlements at 93-95)
    - tests/unit/entitlements.test.ts (the existing per-flag test model — see the whatsappEnabled/customDomainEnabled cases at lines 11, 55-65, and the per-tier JSON-safety loop)
  </read_first>
  <behavior>
    - tiers.free.chatEnabled === false
    - tiers.trial.chatEnabled === true
    - tiers.pro.chatEnabled === true
    - tiers.business.chatEnabled === true
    - getEntitlements('free').chatEnabled === false (resolves through the resolver, not just the literal)
    - getEntitlements('garbage').chatEnabled === false (unknown tier falls back to free)
    - getEntitlements('pro').chatEnabled === true
  </behavior>
  <action>
    1. In `lib/entitlements.ts`, add `chatEnabled: boolean` to the `Entitlements` type, immediately after the `customDomainEnabled: boolean` line, with a one-line comment: `chatEnabled: boolean // CHATMETER-02 — in-app chat is a Pro/Business feature`.
    2. Add the flag to all 4 tier literals (the `as const satisfies` will fail compilation if any is missed): `free.chatEnabled = false`; `trial.chatEnabled = true`; `pro.chatEnabled = true`; `business.chatEnabled = true`. Place each line right after that tier's `customDomainEnabled` line to mirror the existing flag ordering. Rationale (per 126-RESEARCH §"Per-tier values"): trial mirrors paid access for every flag except customDomainEnabled, so trial gets chat to showcase it; free is the gate's whole point.
    3. In `tests/unit/entitlements.test.ts`, add a describe block (or individual it() cases alongside the existing flag tests) covering the behavior above. Model them EXACTLY on the existing `'free tier has customDomainEnabled false'` / `'business tier has customDomainEnabled true'` cases. Include both literal assertions (`tiers.free.chatEnabled`) and resolver assertions (`getEntitlements('garbage').chatEnabled` falls back to false).
    Do NOT touch any other field, any quota number, or the getEntitlements implementation.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/entitlements.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/entitlements.test.ts` passes (including the new chatEnabled cases).
    - `grep -n "chatEnabled" lib/entitlements.ts` returns 5 hits (1 type field + 4 tier literals).
    - `grep -c "chatEnabled: true" lib/entitlements.ts` returns 3 (trial, pro, business); `grep -c "chatEnabled: false" lib/entitlements.ts` returns 1 (free).
    - `npx tsc --noEmit` reports no NEW errors in lib/entitlements.ts (the `as const satisfies` compiles — proof all 4 tiers got the field).
  </acceptance_criteria>
  <done>chatEnabled exists on the type and all 4 tiers (free false, trial/pro/business true); entitlements tests green; tsc clean on the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the 403 chat_not_on_plan gate to app/api/chat/route.ts (route + tests)</name>
  <files>app/api/chat/route.ts, tests/unit/chat/route.test.ts</files>
  <read_first>
    - app/api/chat/route.ts (the FULL file — the gate goes after the company read at lines 65-77, BEFORE resolveChatModel at line 85)
    - app/api/estimates/[id]/send-whatsapp/route.ts:80-86 (the verbatim 403 channel-gate precedent)
    - tests/unit/chat/route.test.ts (the FULL harness — makeServiceClientMock at lines 73-82 sets the companies row; beforeEach defaults at 119-130; the existing 401/no-company "never builds tools/streams" assertions at 133-151 are the model for the 403 case)
  </read_first>
  <behavior>
    - POST /api/chat with a company row whose tier is 'free' → 403, JSON body `{ error: 'chat_not_on_plan', upgradeUrl: '/settings/billing' }`, and neither resolveChatModel NOR buildChatTools is called (mirror the existing 401 assertion `expect(resolveChatModelMock).not.toHaveBeenCalled()`).
    - POST /api/chat with a company row whose tier is 'pro' → 200, body present, resolveChatModel called with the trusted companyId (the existing streaming test, with `tier: 'pro'` added to the mocked row).
    - The gate runs AFTER the auth 401 and the no-company 400 (those still take precedence).
  </behavior>
  <action>
    1. In `app/api/chat/route.ts`, add the import `import { getEntitlements } from '@/lib/entitlements'` alongside the other imports.
    2. Add `tier` to the EXISTING `.select('industries, default_estimate_language')` → `.select('industries, default_estimate_language, tier')`. Change ONLY this string; do not restructure the read.
    3. Immediately after the existing `industries`/`lang`/`language` derivation block (after line 76, before the `// 4. Parse the turn` comment at line 78), insert the gate:
       ```typescript
       const tier = (company as { tier?: string | null } | null)?.tier ?? 'free'
       if (!getEntitlements(tier).chatEnabled) {
         return new Response(
           JSON.stringify({ error: 'chat_not_on_plan', upgradeUrl: '/settings/billing' }),
           { status: 403, headers: { 'content-type': 'application/json' } },
         )
       }
       ```
       Use the bare `new Response(...)` form to match this file's existing 401/400 style (route.ts:52, 59). The gate MUST be before `req.json()`/`resolveChatModel`/`buildChatTools`/`streamText` so an unentitled tenant triggers no model build (matches the file's existing posture of failing fast).
    4. In `tests/unit/chat/route.test.ts`:
       - In `beforeEach`, change the default mocked companies row to include `tier: 'pro'` so all EXISTING tests (which expect 200 + tool build) keep passing: `makeServiceClientMock({ industries: ['plumbing'], default_estimate_language: 'en', tier: 'pro' })`.
       - Add a new it() `'returns 403 chat_not_on_plan and never builds tools or streams for a free-tier company'`: override `requireServiceClientMock.mockReturnValue(makeServiceClientMock({ industries: [], default_estimate_language: 'en', tier: 'free' }) as never)`, POST a userMessage, assert `res.status === 403`, parse the JSON body and assert `error === 'chat_not_on_plan'` and `upgradeUrl === '/settings/billing'`, and assert `buildChatToolsMock` and `resolveChatModelMock` were NOT called (model the assertions on the existing 401 test at lines 133-141).
       - Add a new it() `'allows a pro-tier company to stream (200)'` mirroring the existing streaming test, explicitly using a `tier: 'pro'` row.
    Do NOT touch the streamText block, the onFinish persistence block, the imports beyond adding getEntitlements, or any other file.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/chat/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/chat/route.test.ts` passes, including the new 403-free + 200-pro cases.
    - `grep -n "chat_not_on_plan" app/api/chat/route.ts` returns a hit; `grep -n "getEntitlements(tier).chatEnabled" app/api/chat/route.ts` returns a hit.
    - `grep -n "default_estimate_language, tier" app/api/chat/route.ts` returns a hit (tier added to the existing select).
    - The 403 test asserts `resolveChatModelMock` is NOT called for free tier (no model build for an unentitled tenant).
    - `npx vitest run tests/unit/chat` (full chat suite) shows no regressions — the Phase-125 scope-fence test (chat-ui-scope.test.ts) STAYS green (its assertions — toUIMessageStreamResponse, originalMessages: messages, no @/components/chat import — are unaffected by the gate insertion).
    - `npx tsc --noEmit` reports no NEW errors in app/api/chat/route.ts.
  </acceptance_criteria>
  <done>POST /api/chat 403s free-tier with `chat_not_on_plan` + upgradeUrl before any model build, 200s pro/business; route + entitlements tests green; scope-fence test still green; tsc clean.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/entitlements.test.ts tests/unit/chat` is green (entitlements flag + route gate + the Phase-125 scope fence all pass).
- Free-tier owners cannot stream chat: the route returns 403 `chat_not_on_plan` with `upgradeUrl: '/settings/billing'` before resolveChatModel/buildChatTools/streamText run.
- Pro/Business/trial owners stream normally (no behavior change for entitled tenants).
- No DB migration (entitlements are code-level), no new dependency, no secret.
</verification>

<success_criteria>
- `chatEnabled` is on `Entitlements` + all 4 tiers (free false; trial/pro/business true), enforced by `as const satisfies` and the entitlements tests.
- `app/api/chat/route.ts` enforces `getEntitlements(tier).chatEnabled` as a 403 gate before model resolution; route.test.ts proves the 403 (free, no model build) and the 200 (pro).
- CHATMETER-02 security boundary satisfied at the route. The frozen Phase-124/125 behavior (stream + persistence + UI scope fence) is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/126-chat-access-entitlement-gate/126-01-SUMMARY.md`
</output>
