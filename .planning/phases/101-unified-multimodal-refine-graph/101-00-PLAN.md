---
phase: 101-unified-multimodal-refine-graph
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/estimate/multimodal-ingest.test.ts
  - tests/unit/ai/refine-shared-prompt.test.ts
  - tests/unit/estimate/refine-node.test.ts
  - tests/unit/api/refine-route-contract.test.ts
  - tests/unit/estimate/generate-refine-equivalence.test.ts
  - tests/unit/ai/prompt-builder.test.ts
  - tests/unit/estimate/no-checkpointer.test.ts
autonomous: true
requirements: [HARD-01, HARD-02, UNIFY-01, UNIFY-02, UNIFY-03]
must_haves:
  truths:
    - "Five new RED test files exist and fail (or are skipped pending impl) — they import modules/behaviors that do not exist yet"
    - "prompt-builder.test.ts has refine-mode cases that fail until the builder gains mode:'refine'"
    - "no-checkpointer.test.ts asserts buildRefineGraph compiles without a checkpointer"
  artifacts:
    - path: "tests/unit/estimate/multimodal-ingest.test.ts"
      provides: "UNIFY-01 RED contract for ingestMultimodal"
    - path: "tests/unit/ai/refine-shared-prompt.test.ts"
      provides: "HARD-02/UNIFY-02 RED contract (no bespoke refine prompt remains)"
    - path: "tests/unit/estimate/refine-node.test.ts"
      provides: "UNIFY-03 RED contract for makeRefineNode never-throw + failure mapping"
    - path: "tests/unit/api/refine-route-contract.test.ts"
      provides: "HARD-01 RED contract for route response shape + status codes"
    - path: "tests/unit/estimate/generate-refine-equivalence.test.ts"
      provides: "Criterion 5 RED contract (shared seams exercised by both)"
  key_links:
    - from: "tests"
      to: "lib modules built in Waves 1-2"
      via: "vi.mock of target modules so test files compile before sources exist"
      pattern: "vi.mock"
---

<objective>
Wave 0 RED/EXTEND scaffold for Phase 101. Create the five new failing test files and extend two existing ones so every Phase 101 requirement has an automated verification target BEFORE any implementation lands (Nyquist). Tests are RED (failing or skipped-pending-impl) until Waves 1-2 make them GREEN.

Purpose: Lock the contracts (ingestMultimodal signature, refine-mode prompt, refine node failure mapping, route response shape, no-checkpointer) so Wave 1-2 executors implement against a fixed target.
Output: 5 new test files + 2 extended test files. No production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-unified-multimodal-refine-graph/101-CONTEXT.md
@.planning/phases/101-unified-multimodal-refine-graph/101-RESEARCH.md
@.planning/phases/101-unified-multimodal-refine-graph/101-VALIDATION.md
@tests/unit/api/refine-error-surface.test.ts
@tests/unit/ai/prompt-builder.test.ts
@tests/unit/estimate/no-checkpointer.test.ts
@tests/unit/estimate/never-throw.test.ts
@lib/ai/types.ts

<interfaces>
<!-- Contracts these tests assert against. Extracted from current source. Executors: assert these shapes. -->

ingestMultimodal (Wave 1, lib/estimate/ingest/multimodal.ts):
```typescript
export interface MultimodalRawInput {
  audio?: Array<{ blob: Blob; ext: string }>
  photos?: Array<{ base64: string; mimeType: string }>
  texts?: string[]
}
export interface MultimodalIngestResult {
  transcripts: string[]
  photoDescriptions: string[]
  texts: string[]
}
export async function ingestMultimodal(input: MultimodalRawInput): Promise<MultimodalIngestResult>
```

makeRefineNode (Wave 2, lib/estimate/graph/nodes/refine.ts):
```typescript
export const makeRefineNode: (runner: StepRunner) =>
  (state: EstimateStateType) => Promise<Partial<EstimateStateType>>
// success → { refined: EstimateOutput }
// ProvidersUnavailableError → { failure: { reason: 'provider_unavailable' } }
// InvalidEstimateOutputError → { failure: { reason: 'invalid_output' } }
// missing existingEstimate/instruction → { failure: { reason: 'no_usable_input' } }
// NEVER throws
```

buildRefineGraph (Wave 2, lib/estimate/graph/refine-graph.ts):
```typescript
export function buildRefineGraph(adapter: ChannelAdapter, opts?: { runner?: StepRunner }): CompiledGraph
```

Prompt builder refine mode (Wave 1, lib/ai/prompt-builder.ts):
```typescript
export function buildSystemPrompt(input: EstimateInput, opts?: { mode?: 'generate' | 'refine' }): string
export function buildRefineUserContent(input: EstimateInput, existingEstimate: EstimateOutput, instruction: string): string
```

Refine route response (HARD-01, app/api/estimates/[id]/refine/route.ts):
```json
{ "success": true, "refined": { /* EstimateOutput */ }, "instruction": "..." }
```
Status codes preserved: 400 (no input / bad file type), 422 (no usable instruction), 429 (rate limit), demo-guard. Failure → { error, code } via asResponse.

FailureReason union (lib/estimate/failure.ts) — use these exact strings:
'no_usable_input' | 'transcription_failed' | 'vision_failed' | 'generation_failed' | 'invalid_output' | 'provider_unavailable'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests for shared ingestion + refine node + equivalence</name>
  <read_first>
    - tests/unit/estimate/never-throw.test.ts (mocking + never-throw assertion pattern)
    - tests/unit/ai/with-fallback.test.ts (ProvidersUnavailableError / InvalidEstimateOutputError usage)
    - lib/ai/openrouter-client.ts (transcribeAudioOR / analyzePhotoOR signatures to mock)
    - lib/estimate/graph/nodes/generate.ts (failure-mapping block the refine node mirrors)
  </read_first>
  <files>tests/unit/estimate/multimodal-ingest.test.ts, tests/unit/estimate/refine-node.test.ts, tests/unit/estimate/generate-refine-equivalence.test.ts</files>
  <action>
    Create three RED test files. Use `vi.mock('@/lib/ai/openrouter-client', ...)` so the files compile before the source modules exist. Follow the Wave-0 scaffold pattern used across this repo (vi.mock the target + explicit `import { vi } from 'vitest'`).

    **multimodal-ingest.test.ts** (UNIFY-01) — `import { ingestMultimodal } from '@/lib/estimate/ingest/multimodal'`:
    - mock `transcribeAudioOR` to resolve 'TRANSCRIPT' and `analyzePhotoOR` to resolve 'DESC'.
    - Test: given `{ audio: [{ blob, ext: 'webm' }], photos: [{ base64, mimeType: 'image/jpeg' }], texts: ['  hello  '] }` returns `{ transcripts: ['TRANSCRIPT'], photoDescriptions: ['DESC'], texts: ['hello'] }` (texts trimmed + empties filtered).
    - Test: a single transcribe failure (mock rejects once) is SKIPPED not thrown — `ingestMultimodal` still resolves with the other items; `expect(...).resolves` not `.rejects`.
    - Test: a single vision failure is skipped, the good photo description still returned.
    - Test: empty input `{}` returns `{ transcripts: [], photoDescriptions: [], texts: [] }`.

    **refine-node.test.ts** (UNIFY-03) — `import { makeRefineNode } from '@/lib/estimate/graph/nodes/refine'`, mock `@/lib/ai/provider-with-fallback` (`getAIProviderWithFallback`) and `@/lib/queries/price-book` (`getPriceBookItems`):
    - Test: success → `makeRefineNode(passthroughRunner)(state)` with valid `existingEstimate` + `instruction` resolves `{ refined: <EstimateOutput> }` (mock provider.refineEstimate returns a valid EstimateOutput).
    - Test: provider throws `ProvidersUnavailableError` → returns `{ failure: { reason: 'provider_unavailable' } }` (NEVER throws — assert `.resolves`).
    - Test: provider throws `InvalidEstimateOutputError` → returns `{ failure: { reason: 'invalid_output' } }`.
    - Test: state missing `existingEstimate` or `instruction` → returns `{ failure: { reason: 'no_usable_input' } }`.
    - Test: any other error → `{ failure: { reason: 'generation_failed' } }`.

    **generate-refine-equivalence.test.ts** (criterion 5):
    - Assert both the generate path and refine path call the SHARED `buildSystemPrompt` — spy/mock `@/lib/ai/prompt-builder` and assert `buildSystemPrompt` is invoked with `{ mode: 'refine' }` on the refine path and with no opts (or `{ mode: 'generate' }`) on generate.
    - Assert the refine adapters no longer reference a bespoke prompt: read `lib/ai/providers/openrouter.ts` and `lib/ai/providers/gemini.ts` source via `fs.readFileSync` and assert the literal `'## Refinement Instruction'` is ABSENT from both (this is the deletion marker — see Pitfall 4). This test will be RED until Wave 1 deletes the bespoke prompts.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/estimate/multimodal-ingest.test.ts tests/unit/estimate/refine-node.test.ts tests/unit/estimate/generate-refine-equivalence.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Three test files exist; each fails (RED) because the target source modules/behaviors do not exist yet; no `.only`, no watch-mode flags.</done>
</task>

<task type="auto">
  <name>Task 2: RED route-contract test + EXTEND prompt-builder, refine-shared-prompt, no-checkpointer tests</name>
  <read_first>
    - tests/unit/api/refine-error-surface.test.ts (the JSON back-compat driving pattern + the multipart-hangs-in-jsdom comment at lines 16-21)
    - tests/unit/ai/prompt-builder.test.ts (existing assertions to extend without breaking)
    - tests/unit/estimate/no-checkpointer.test.ts (how it asserts compile-without-checkpointer for buildEstimateGraph)
    - lib/ai/prompt-builder.ts (Security block + sanitizeField behavior to assert)
  </read_first>
  <files>tests/unit/api/refine-route-contract.test.ts, tests/unit/ai/refine-shared-prompt.test.ts, tests/unit/ai/prompt-builder.test.ts, tests/unit/estimate/no-checkpointer.test.ts</files>
  <action>
    **refine-route-contract.test.ts** (HARD-01) — NEW. CRITICAL: drive through the JSON `{ instruction }` back-compat path only — `Request.formData()` multipart HANGS in jsdom (see refine-error-surface.test.ts:16-21). Mock the same collaborators refine-error-surface mocks (`@/lib/supabase/server`, `@/lib/queries/estimate`, `@/lib/ai`/provider-with-fallback, `@/lib/demo/guard`, `@/lib/ratelimit`). Assert:
    - Success (valid instruction, mocked refined output): response JSON is exactly `{ success: true, refined: <obj>, instruction: <string> }`; status 200.
    - Empty body / no instruction → status 400.
    - When ingestion/assembly yields no usable instruction → status 422.
    - Rate-limit denied → status 429.
    - demo-guard active → demo-guard response returned.
    - Provider failure (mock throws ProvidersUnavailableError through the graph) → response is `{ error, code }` (typed envelope from asResponse), NOT `{ success: true }`.

    **refine-shared-prompt.test.ts** (HARD-02/UNIFY-02) — NEW. Source-grep style assertions over `lib/ai/providers/openrouter.ts` and `lib/ai/providers/gemini.ts` (fs.readFileSync):
    - Assert both files' `refineEstimate` call `buildSystemPrompt(` with a `{ mode: 'refine' }`-ish argument (grep for `mode:` near `refine`, or import + spy if feasible).
    - Assert the literal bespoke marker `'## Refinement Instruction'` is ABSENT from both files.
    - Assert both files import from `@/lib/ai/prompt-builder`.

    **EXTEND prompt-builder.test.ts** — add a `describe('refine mode')` block. Assert (these are RED until Wave 1):
    - `buildSystemPrompt(input, { mode: 'refine' })` opening sentence differs from generate mode (mentions updating the existing estimate per the instruction) BUT the `## Language`, `## Your Company Price Book`, and `## Security` blocks are present and identical to generate mode.
    - `buildSystemPrompt(input)` with NO opts is byte-identical to the current generate output (regression guard — generate must not change).
    - `buildRefineUserContent(input, existingEstimate, instruction)` wraps the instruction in `<instruction>...</instruction>` and escapes angle brackets — given `instruction: '<b>do x</b>'` assert the output contains `&lt;b&gt;` and NOT a raw `<b>`.

    **EXTEND no-checkpointer.test.ts** — add a case importing `buildRefineGraph` from `@/lib/estimate/graph/refine-graph` and asserting it compiles without a checkpointer (mirror the existing buildEstimateGraph assertion). RED until Wave 2.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/api/refine-route-contract.test.ts tests/unit/ai/refine-shared-prompt.test.ts tests/unit/ai/prompt-builder.test.ts tests/unit/estimate/no-checkpointer.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>refine-route-contract + refine-shared-prompt exist and are RED; prompt-builder.test.ts has new refine-mode cases (RED for refine, GREEN for the generate-unchanged regression); no-checkpointer.test.ts asserts buildRefineGraph; no `.only`/watch flags.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/estimate/multimodal-ingest.test.ts tests/unit/estimate/refine-node.test.ts tests/unit/estimate/generate-refine-equivalence.test.ts tests/unit/api/refine-route-contract.test.ts tests/unit/ai/refine-shared-prompt.test.ts` — all RED (expected pre-impl).
- `npx vitest run tests/unit/ai/prompt-builder.test.ts` — generate-unchanged case GREEN, refine-mode cases RED.
- Existing green invariant suites still green: `npx vitest run tests/unit/estimate/graph-neutrality.test.ts tests/unit/estimate/never-throw.test.ts tests/unit/whatsapp/never-reply-regression.test.ts`.
</verification>

<success_criteria>
All five new Wave-0 test files plus the two extensions exist, compile (via vi.mock of not-yet-existing targets), and fail RED for the not-yet-implemented behaviors while leaving every pre-existing green test green. No production source touched.
</success_criteria>

<output>
After completion, create `.planning/phases/101-unified-multimodal-refine-graph/101-00-SUMMARY.md`
</output>
