---
phase: 81-company-switcher-ui-add-company-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/queries/active-company.ts
  - tests/unit/active-company-helpers.test.ts
autonomous: true
requirements: [SWITCH-04, SWITCH-02]
tags: [tdd, query, multi-tenancy]
must_haves:
  truths:
    - "getMembershipCompanies returns every company the signed-in user owns via company_members"
    - "Results are ordered by companies.created_at ASC"
    - "Result shape is { id, name, logo_url }[] — no extra columns"
    - "Returns [] when the user is unauthenticated"
    - "Uses the request-scoped Supabase client (RLS-bound, never service role)"
  artifacts:
    - path: lib/queries/active-company.ts
      provides: "getMembershipCompanies() exported alongside Phase 79 helpers"
      contains: "export async function getMembershipCompanies"
    - path: tests/unit/active-company-helpers.test.ts
      provides: "Extended test file covering single/multiple/zero membership cases for getMembershipCompanies"
      contains: "getMembershipCompanies"
  key_links:
    - from: lib/queries/active-company.ts
      to: company_members table (via supabase request client)
      via: "from('company_members').select('companies!inner(...)').eq('user_id', claims.sub).order('created_at', { foreignTable: 'companies', ascending: true })"
      pattern: "foreignTable:.*'companies'.*ascending: true"
    - from: lib/queries/active-company.ts
      to: lib/queries/auth.ts
      via: "getAuthClaims()"
      pattern: "getAuthClaims"
---

<objective>
Add `getMembershipCompanies(): Promise<Array<{ id: string; name: string; logo_url: string | null }>>` to `lib/queries/active-company.ts` so Phase 81 Plans 03+04 can render the company switcher dropdown with the live list of every company the signed-in user owns. Co-locates with Phase 79's existing `getActiveCompanyId` / `getActiveCompany` / `ACTIVE_COMPANY_COOKIE` exports (per SWITCH-02 + SWITCH-04).

Purpose: Plan 03 (CompanySelector rewrite) and Plan 04 (layout integration) both depend on this function. It's the only new query helper this phase needs.

Output: Extended `lib/queries/active-company.ts` and extended `tests/unit/active-company-helpers.test.ts`. Three new unit tests cover the three observable cases (single membership, multiple memberships, zero memberships / unauthenticated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-company-switcher-ui-add-company-flow/81-CONTEXT.md
@.planning/phases/81-company-switcher-ui-add-company-flow/81-RESEARCH.md
@.planning/phases/81-company-switcher-ui-add-company-flow/81-VALIDATION.md
@.planning/phases/79-multi-company-support-allow-one-user-to-own-and-switch-betwe/79-CONTEXT.md
@lib/queries/active-company.ts
@tests/unit/active-company-helpers.test.ts
@supabase/migrations/20260525000001_phase79_company_members.sql

<interfaces>
<!-- Existing Phase 79 exports from lib/queries/active-company.ts the new function must coexist with: -->

```ts
export const ACTIVE_COMPANY_COOKIE = 'active_company_id'
export const ACTIVE_COMPANY_COOKIE_OPTIONS = { httpOnly: true, sameSite: 'lax', path: '/', maxAge: 60 * 60 * 24 * 30 }
export async function getActiveCompanyId(): Promise<string | null>
export async function getActiveCompany(): Promise<AppCompany | null>
```

<!-- From lib/queries/auth.ts -->
```ts
export async function getAuthClaims(): Promise<JwtPayload | null>
```

<!-- The new export this plan adds: -->
```ts
export async function getMembershipCompanies(): Promise<
  Array<{ id: string; name: string; logo_url: string | null }>
>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Wave 0 — extend active-company-helpers.test.ts with failing tests for getMembershipCompanies</name>
  <files>tests/unit/active-company-helpers.test.ts</files>
  <behavior>
    Add a new `describe('getMembershipCompanies', ...)` block with three cases:

    Case A — Multiple memberships, ordered ASC by created_at:
      - Mock getAuthClaims to return { sub: 'user-1' }
      - Mock the supabase chain `.from('company_members').select(...).eq('user_id', 'user-1').order('created_at', { foreignTable: 'companies', ascending: true })` to resolve to:
        ```
        { data: [
          { companies: { id: 'c-1', name: 'Alpha', logo_url: null, created_at: '2026-01-01' } },
          { companies: { id: 'c-2', name: 'Beta', logo_url: 'https://x/b.png', created_at: '2026-02-01' } },
        ], error: null }
        ```
      - Expect: getMembershipCompanies() resolves to
        `[{ id: 'c-1', name: 'Alpha', logo_url: null }, { id: 'c-2', name: 'Beta', logo_url: 'https://x/b.png' }]`
      - Assert the .order call received `{ foreignTable: 'companies', ascending: true }` (mirrors getActiveCompanyId line 78 but ASC, per SWITCH-04).

    Case B — Single membership:
      - Mock returns one row → returns array of length 1 with `{ id, name, logo_url }` only (no `created_at` leaked to caller).

    Case C — Unauthenticated / zero memberships:
      - When getAuthClaims returns null → returns [] without touching supabase.
      - When data is null/empty → returns [].

    All three tests MUST fail initially because getMembershipCompanies is not exported yet (Wave 1 implements it). Use the same mock setup pattern as the existing tests at the top of the file (vi.mock next/headers, @/lib/supabase/server, @/lib/queries/auth).

    Implements: SWITCH-04, SWITCH-16. Per user memory `auto_approve_per_user_memory: true` — no human gate.
  </behavior>
  <action>
    Open `tests/unit/active-company-helpers.test.ts` and append a new `describe('getMembershipCompanies', ...)` block after the existing `getActiveCompanyId` / `getActiveCompany` describe blocks. Re-use the existing top-of-file vi.mock setup (no new mocks needed). Import `getMembershipCompanies` from `@/lib/queries/active-company` at the top of the file (TS will compile because vi.mock provides the path; the runtime will throw "is not a function" which is the expected RED state).

    Three `it()` cases per the behavior block above. Mock the chained supabase client returning a `from().select().eq().order()` builder whose `.order()` resolves to `{ data, error }`. Track the `.order` call args to assert ASC ordering with foreignTable.

    DO NOT implement the function yet. This task MUST end with `npx vitest run tests/unit/active-company-helpers.test.ts -t "getMembershipCompanies"` showing RED (3 failures).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/active-company-helpers.test.ts -t "getMembershipCompanies" 2>&1 | grep -E "(FAIL|3 failed|is not a function)"</automated>
  </verify>
  <done>Three failing tests under `describe('getMembershipCompanies')` exist in tests/unit/active-company-helpers.test.ts. Existing Phase 79 tests in the same file still pass. Commit: `test(81-01): RED tests for getMembershipCompanies`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Implement getMembershipCompanies in lib/queries/active-company.ts</name>
  <files>lib/queries/active-company.ts</files>
  <behavior>
    After implementation, all three tests from Task 1.1 turn GREEN.

    Function signature:
    ```ts
    export async function getMembershipCompanies(): Promise<
      Array<{ id: string; name: string; logo_url: string | null }>
    >
    ```

    Behavior:
    - Call `getAuthClaims()`; if `claims?.sub` is falsy → return `[]` (do not touch supabase).
    - Get the request-scoped client via `await createClient()` from `@/lib/supabase/server` (NEVER `requireServiceClient` — SWITCH-04 + plan-checker gate enforce this, RLS already scopes by `user_id = auth.uid()`).
    - Query: `.from('company_members').select('companies!inner(id, name, logo_url, created_at)').eq('user_id', claims.sub as string).order('created_at', { foreignTable: 'companies', ascending: true })`
    - If data is null/empty → return [].
    - Map each row to `{ id: row.companies.id, name: row.companies.name, logo_url: row.companies.logo_url }` — drop `created_at` from the public shape.

    Implements: SWITCH-04 (live query), SWITCH-02 (lives in same file as Phase 79 helpers — colocated, not in a new file).
  </behavior>
  <action>
    Append `getMembershipCompanies` to `lib/queries/active-company.ts` at the bottom of the file (after `getActiveCompany`). Use the exact RESEARCH.md Pattern 1 implementation (line 162-184 of 81-RESEARCH.md):

    ```ts
    export async function getMembershipCompanies(): Promise<
      Array<{ id: string; name: string; logo_url: string | null }>
    > {
      const claims = await getAuthClaims()
      if (!claims?.sub) return []

      const supabase = await createClient()

      const { data } = await supabase
        .from('company_members')
        .select('companies!inner(id, name, logo_url, created_at)')
        .eq('user_id', claims.sub as string)
        .order('created_at', { foreignTable: 'companies', ascending: true })

      if (!data) return []
      return data.map((row: any) => ({
        id: row.companies.id,
        name: row.companies.name,
        logo_url: row.companies.logo_url,
      }))
    }
    ```

    Do NOT redefine `ACTIVE_COMPANY_COOKIE` or `ACTIVE_COMPANY_COOKIE_OPTIONS` (SWITCH-02 — Phase 79 is the source of truth). Do NOT switch the existing `getActiveCompanyId` to ASC ordering — that function uses DESC for "most recently created" fallback per Phase 79 D-07; this new function uses ASC because the dropdown should render in stable insertion order.

    The foreign-table ORDER BY syntax must be EXACTLY `.order('created_at', { foreignTable: 'companies', ascending: true })` — per Pitfall 1 in 81-RESEARCH.md, the column name comes first and the foreign table goes in options.

    Per D-03 (Phase 79) RLS, this query is correctly RLS-scoped — never use `requireServiceClient` here (plan-checker BLOCK gate per 81-VALIDATION.md).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/active-company-helpers.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All tests in `tests/unit/active-company-helpers.test.ts` GREEN (Phase 79 existing + 3 new). `npx tsc --noEmit` clean. Commit: `feat(81-01): add getMembershipCompanies query helper`.</done>
</task>

<task type="auto">
  <name>Task 1.3: Write SUMMARY</name>
  <files>.planning/phases/81-company-switcher-ui-add-company-flow/81-01-SUMMARY.md</files>
  <action>
    Write the standard plan SUMMARY at `.planning/phases/81-company-switcher-ui-add-company-flow/81-01-SUMMARY.md` following `$HOME/.claude/get-shit-done/templates/summary.md`. Sections:
    - **What shipped:** `getMembershipCompanies()` exported from `lib/queries/active-company.ts`; 3 new unit tests in `tests/unit/active-company-helpers.test.ts`.
    - **Requirements satisfied:** SWITCH-04, SWITCH-02 (consumer side).
    - **Key decisions:** Co-located with Phase 79 helpers (not new file) per SWITCH-02; ASC ordering by `companies.created_at` (not DESC like Phase 79 fallback) for stable dropdown order; request-scoped client (RLS) — never service-role.
    - **Files touched:** lib/queries/active-company.ts (extended), tests/unit/active-company-helpers.test.ts (extended).
    - **Tests:** quick run command + result count.
    - **Followups for Plan 02+:** the `switchActiveCompany` action will need to invalidate any cache around this query — but since this function is NOT memoized in v1 (per 81-RESEARCH.md Open Question 3 recommendation: "No caching in v1"), no `revalidateTag` wiring is needed here.
  </action>
  <verify>
    <automated>test -f .planning/phases/81-company-switcher-ui-add-company-flow/81-01-SUMMARY.md</automated>
  </verify>
  <done>SUMMARY committed. Commit: `docs(81-01): summarize getMembershipCompanies plan`.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/active-company-helpers.test.ts` — all existing + 3 new tests green
- `npx tsc --noEmit` — clean
- `grep -n "getMembershipCompanies" lib/queries/active-company.ts` returns exactly one export line
- `grep -n "requireServiceClient" lib/queries/active-company.ts` shows it is only used inside the existing `loadCompanyById` unstable_cache (the new function MUST NOT add another usage)
</verification>

<success_criteria>
- [ ] `getMembershipCompanies` exported from `lib/queries/active-company.ts`
- [ ] Returns `[]` when unauthenticated
- [ ] Returns ordered ASC by `companies.created_at` (foreign-table syntax verified)
- [ ] Result shape is exactly `{ id, name, logo_url }` (no leaked columns)
- [ ] Uses request-scoped `createClient()` (NOT `requireServiceClient`)
- [ ] 3 new unit tests pass; existing Phase 79 tests still pass
- [ ] No new dependencies installed (SWITCH-19)
</success_criteria>

<commit_strategy>
- After Task 1.1: `test(81-01): RED tests for getMembershipCompanies`
- After Task 1.2: `feat(81-01): add getMembershipCompanies query helper`
- After Task 1.3: `docs(81-01): summarize getMembershipCompanies plan`
</commit_strategy>

<output>
After completion, create `.planning/phases/81-company-switcher-ui-add-company-flow/81-01-SUMMARY.md`.
</output>
