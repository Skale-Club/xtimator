---
phase: 122-channel-neutral-domain-extraction
plan: 03
type: execute
wave: 3
depends_on: ["122-01", "122-02"]
files_modified:
  - lib/agent-tools/create-estimate.ts
  - lib/agent-tools/ask-knowledge.ts
  - lib/agent-tools/index.ts
  - lib/whatsapp/intent-router.ts
autonomous: true
requirements: [NEUT-01, NEUT-04, NEUT-05]
user_setup: []

must_haves:
  truths:
    - "A neutral createEstimate({companyId, projectId, prompts?, language?}) dispatches EVENT_ESTIMATE_GENERATE and returns { jobId } — companyId is a trusted param, never an LLM tool-input field"
    - "A neutral askKnowledge(question, {industries, companyId, language}) wraps lib/knowledge/answer and never throws"
    - "WhatsApp dispatchKnowledge calls the neutral askKnowledge (behavior identical — answer was already neutral)"
    - "WhatsApp's CREATE media-ingest path stays WhatsApp-specific (NOT forced through the neutral createEstimate) — no regression"
    - "lib/agent-tools/ imports no channel token; the full WhatsApp parity suite stays green with assertions unchanged"
  artifacts:
    - path: "lib/agent-tools/create-estimate.ts"
      provides: "NEUT-01 neutral generation-dispatch (mirrors lib/mcp/tools/write.ts)"
      contains: "EVENT_ESTIMATE_GENERATE"
    - path: "lib/agent-tools/ask-knowledge.ts"
      provides: "NEUT-04 thin neutral wrapper over lib/knowledge/answer"
      contains: "answer"
  key_links:
    - from: "lib/agent-tools/create-estimate.ts"
      to: "lib/inngest/client.ts + lib/inngest/events.ts"
      via: "inngest.send EVENT_ESTIMATE_GENERATE"
      pattern: "inngest\\.send"
    - from: "lib/agent-tools/ask-knowledge.ts"
      to: "lib/knowledge/answer.ts"
      via: "delegates to answer()"
      pattern: "from '@/lib/knowledge/answer'"
    - from: "lib/whatsapp/intent-router.ts"
      to: "lib/agent-tools/ask-knowledge.ts"
      via: "dispatchKnowledge calls askKnowledge"
      pattern: "askKnowledge"
---

<objective>
NEUT-01 + NEUT-04: add the two remaining neutral capability functions to `lib/agent-tools/` — `createEstimate` (the generation-dispatch, mirroring the working MCP `create_estimate` precedent) and `askKnowledge` (a ~5-line wrapper over the already-neutral `lib/knowledge/answer`). Then re-point WhatsApp's `dispatchKnowledge` to call the neutral `askKnowledge`.

Purpose: complete the neutral capability surface that Phase 124's chat will bind as AI-SDK tools. `createEstimate` is the EVENT_ESTIMATE_GENERATE dispatch (the prompt/already-ingested path shared by web + MCP) — WhatsApp's heavier media-ingest CREATE path stays WhatsApp-specific by design (Research Pitfall 3 / Open Question 1): NEUT-01 = "no duplicated generation logic", already satisfied because every channel ends at `generateEstimateForProject`. Do NOT force WhatsApp's media-ingest through the neutral createEstimate.

Output: 2 new neutral files; `index.ts` barrel updated; `dispatchKnowledge` re-pointed; full WhatsApp parity suite green with assertions unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-channel-neutral-domain-extraction/122-RESEARCH.md
@.planning/phases/122-channel-neutral-domain-extraction/122-VALIDATION.md

# The precedent to copy for createEstimate (READ the dispatch body):
@lib/mcp/tools/write.ts
@lib/inngest/events.ts
@lib/inngest/client.ts

# The function askKnowledge wraps (already neutral, never-throws):
@lib/knowledge/answer.ts

# Where dispatchKnowledge lives (re-point its answer() call to askKnowledge):
@lib/whatsapp/intent-router.ts

# The RED tests from 122-01 this plan turns GREEN:
@tests/unit/agent-tools/create-estimate.test.ts
@tests/unit/agent-tools/ask-knowledge.test.ts

<interfaces>
From lib/inngest/events.ts:
```typescript
export const EVENT_ESTIMATE_GENERATE = 'estimate/generate.requested'
export type EstimateGeneratePayload = {
  companyId: string; projectId: string; requestId: string
  language?: 'en' | 'pt' | 'es'; prompts?: string[]
  channel?: 'web' | 'mcp'; ... // (createdByUserId, attemptId, inputType optional)
}
```
From lib/inngest/client.ts: `export const inngest` — `inngest.send({ name, id?, data }) => Promise<{ ids: string[] }>`.
From lib/knowledge/answer.ts: `export async function answer(question: string, ctx: { industries: string[]; companyId: string; language?: 'en'|'pt'|'es' }): Promise<string>` (never throws; returns FALLBACK on failure).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEUT-01 — neutral createEstimate (EVENT_ESTIMATE_GENERATE dispatch)</name>
  <read_first>
    - lib/mcp/tools/write.ts (handleCreateEstimate — copy the dispatch body, drop the MCP auth/scope/project-lookup wrapper)
    - lib/inngest/events.ts (EVENT_ESTIMATE_GENERATE + EstimateGeneratePayload)
    - lib/inngest/client.ts (the inngest singleton)
    - tests/unit/agent-tools/create-estimate.test.ts (the GREEN target — payload shape + isolation + no-id guard)
  </read_first>
  <files>lib/agent-tools/create-estimate.ts, lib/agent-tools/index.ts</files>
  <behavior>
    - createEstimate({ companyId, projectId, prompts?, language?, channel? }) calls inngest.send({ name: EVENT_ESTIMATE_GENERATE, data }) exactly once.
    - data = { companyId, projectId, requestId: randomUUID(), ...(prompts ? { prompts } : {}), ...(language ? { language } : {}), ...(channel ? { channel } : {}) }.
    - companyId in the payload equals exactly the passed companyId (trusted param — never an LLM field).
    - returns { jobId: ids[0] }; throws 'createEstimate: inngest.send returned no event id' when ids is empty.
  </behavior>
  <action>
    Create `lib/agent-tools/create-estimate.ts`:
    - `import 'server-only'` (the engine path is server-only — preserve that posture).
    - `import { randomUUID } from 'node:crypto'`, `import { inngest } from '@/lib/inngest/client'`, `import { EVENT_ESTIMATE_GENERATE, type EstimateGeneratePayload } from '@/lib/inngest/events'`. Import NO `lib/whatsapp/*`, NO `@/lib/mcp/*` (no MCP auth coupling), NO LangChain.
    - Header comment: T-lrf-01 — `companyId` is a CLOSURE/trusted param resolved upstream from the owner identity, NEVER an LLM tool-input field. This neutral fn models the prompt/already-ingested generation path (the EVENT_ESTIMATE_GENERATE path web + MCP share); WhatsApp's media-ingest CREATE path is separate and stays WhatsApp-specific (do NOT route it here).
    - Export:
      ```typescript
      export async function createEstimate(args: {
        companyId: string
        projectId: string
        prompts?: string[]
        language?: 'en' | 'pt' | 'es'
        channel?: 'web' | 'mcp'
      }): Promise<{ jobId: string }> {
        const requestId = randomUUID()
        const payload: EstimateGeneratePayload = {
          companyId: args.companyId,
          projectId: args.projectId,
          requestId,
          ...(args.prompts ? { prompts: args.prompts } : {}),
          ...(args.language ? { language: args.language } : {}),
          ...(args.channel ? { channel: args.channel } : {}),
        }
        const { ids } = await inngest.send({
          name: EVENT_ESTIMATE_GENERATE,
          id: `estimate-${args.projectId}-${requestId}`,
          data: payload,
        })
        const jobId = ids[0]
        if (!jobId) throw new Error('createEstimate: inngest.send returned no event id')
        return { jobId }
      }
      ```
    - Do NOT add any project-ownership lookup here (that belongs to each channel's pre-flight wrapper; the MCP tool does it with its auth context, WhatsApp with its own). Keep this the pure dispatch so the chat can call it after resolving its own project.

    Add `export * from './create-estimate'` to `lib/agent-tools/index.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/agent-tools/create-estimate.test.ts tests/unit/agent-tools/neutrality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/agent-tools/create-estimate.ts` exists, exports `createEstimate`, references `EVENT_ESTIMATE_GENERATE`, and contains ZERO of: `lib/whatsapp`, `@/lib/mcp`, `@langchain`, `company_id` as a schema field (grep each → 0).
    - The dispatched payload uses `args.companyId` directly (grep `companyId: args.companyId` present) — no tenant derived from input text.
    - `tests/unit/agent-tools/create-estimate.test.ts` is GREEN (dispatch payload + name === EVENT_ESTIMATE_GENERATE + non-empty requestId + { jobId } return + empty-ids guard rejects).
    - Neutrality gate green; `npx tsc --noEmit` clean on the touched files.
  </acceptance_criteria>
  <done>Neutral createEstimate dispatches EVENT_ESTIMATE_GENERATE with a trusted companyId and returns { jobId }; WhatsApp's media-ingest CREATE path untouched; NEUT-01 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NEUT-04 — neutral askKnowledge; re-point WhatsApp dispatchKnowledge to call it</name>
  <read_first>
    - lib/knowledge/answer.ts (the never-throws fn askKnowledge delegates to)
    - lib/whatsapp/intent-router.ts (dispatchKnowledge — currently calls answer() directly; re-point to askKnowledge)
    - tests/unit/agent-tools/ask-knowledge.test.ts (the GREEN target)
    - tests/unit/whatsapp/intent-router-knowledge.test.ts (parity guard for KNOWLEDGE dispatch — assertions UNCHANGED)
  </read_first>
  <files>lib/agent-tools/ask-knowledge.ts, lib/whatsapp/intent-router.ts, lib/agent-tools/index.ts</files>
  <behavior>
    - askKnowledge(question, { industries, companyId, language? }) returns answer(question, { industries, companyId, language }) — a thin pass-through that inherits answer's never-throw guarantee.
    - WhatsApp dispatchKnowledge keeps reading companies.industries + default_estimate_language, then calls askKnowledge instead of answer — the splitReply + sendOwnerReplyChunks behavior is byte-identical (same text in → same chunks out).
    - tests/unit/whatsapp/intent-router-knowledge.test.ts passes with assertions unchanged.
  </behavior>
  <action>
    Create `lib/agent-tools/ask-knowledge.ts`:
    - `import 'server-only'` (answer is server-only — preserve). `import { answer } from '@/lib/knowledge/answer'`. Import NO `lib/whatsapp/*`.
    - Header comment: NEUT-04 — channel-neutral wrapper; `industries` + `companyId` are CALLER-SUPPLIED (the retrieval scope), NEVER from the LLM. Never throws (answer never throws).
    - Export:
      ```typescript
      export async function askKnowledge(
        question: string,
        ctx: { industries: string[]; companyId: string; language?: 'en' | 'pt' | 'es' }
      ): Promise<string> {
        return answer(question, ctx)
      }
      ```

    Re-point `lib/whatsapp/intent-router.ts` `dispatchKnowledge`:
    - Change the import from `import { answer } from '@/lib/knowledge/answer'` to `import { askKnowledge } from '@/lib/agent-tools/ask-knowledge'` (remove the now-unused `answer` import if nothing else uses it).
    - In `dispatchKnowledge`, change the `await answer(normalizedText, { industries, companyId: input.companyId, language })` call to `await askKnowledge(normalizedText, { industries, companyId: input.companyId, language })`. The company read (`companies.industries, default_estimate_language`), the `splitReply`, the fallback, and `sendOwnerReplyChunks` STAY exactly as-is (Research Pitfall 5 — only the capability call moves to neutral; the reply-splitting stays in the channel).

    Add `export * from './ask-knowledge'` to `lib/agent-tools/index.ts`.

    Run the neutral test + the WhatsApp knowledge parity test. The knowledge parity test assertions must NOT change.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/agent-tools/ask-knowledge.test.ts tests/unit/whatsapp/intent-router-knowledge.test.ts tests/unit/agent-tools/neutrality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/agent-tools/ask-knowledge.ts` exists, exports `askKnowledge`, imports `answer` from `@/lib/knowledge/answer`, and contains ZERO `lib/whatsapp` references (grep → 0).
    - `lib/whatsapp/intent-router.ts` now imports `askKnowledge` from `@/lib/agent-tools/ask-knowledge` and `dispatchKnowledge` calls `askKnowledge(...)` (grep `askKnowledge` present); the company-read + `splitReply` + `sendOwnerReplyChunks` lines are unchanged.
    - `tests/unit/agent-tools/ask-knowledge.test.ts` is GREEN.
    - `tests/unit/whatsapp/intent-router-knowledge.test.ts` passes with NO `expect(...)` edits versus baseline.
    - Neutrality gate green; `npx tsc --noEmit` clean on touched files.
  </acceptance_criteria>
  <done>Neutral askKnowledge wraps answer; WhatsApp dispatchKnowledge calls the neutral wrapper with identical behavior; both the neutral test and the WhatsApp knowledge parity test green with assertions unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Full parity + neutrality gate (the NEUT-05 phase gate)</name>
  <read_first>
    - tests/unit/whatsapp/ (the full parity suite)
    - tests/unit/agent-tools/ (all 5 now expected GREEN)
  </read_first>
  <files>lib/agent-tools/index.ts</files>
  <action>
    Finalize the `lib/agent-tools/index.ts` barrel so it re-exports all four neutral modules (`create-estimate`, `query-company-data`, `normalize-input`, `ask-knowledge`) — this is the single import surface Phase 124's chat will consume.

    Then run the PHASE GATE: the full WhatsApp parity suite plus the full agent-tools suite, and confirm:
    - Every `tests/unit/whatsapp/*` test green with assertions unchanged from the pre-phase baseline (NEUT-05 — non-destructive across all four capabilities: CREATE dispatch path, QUERY, NORMALIZE, KNOWLEDGE).
    - All 5 `tests/unit/agent-tools/*` tests green (neutrality + the four capabilities).

    Finally run the full repo unit suite to confirm no cross-module regression introduced by the extraction (e.g. any test that imported the moved symbols). If the full suite surfaces the known parallel-only `mcp-route-contract.test.ts` flake, re-confirm it in isolation (it is pre-existing and out-of-scope — note in summary).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/whatsapp tests/unit/agent-tools tests/unit/knowledge</automated>
  </verify>
  <acceptance_criteria>
    - `lib/agent-tools/index.ts` re-exports `create-estimate`, `query-company-data`, `normalize-input`, `ask-knowledge` (grep each module name present).
    - `npx vitest run tests/unit/whatsapp` is fully green with NO assertion edits versus the pre-phase baseline — the load-bearing NEUT-05 proof.
    - `npx vitest run tests/unit/agent-tools` → all 5 files green.
    - The neutrality gate confirms `lib/agent-tools/` (now 4 source files + barrel) imports none of the 6 forbidden channel tokens.
    - The full `npx vitest run` is green except at most the documented pre-existing `mcp-route-contract.test.ts` parallel-only flake (re-confirmed green in isolation).
  </acceptance_criteria>
  <done>The neutral barrel exposes all four capabilities; the full WhatsApp suite is green with assertions unchanged (NEUT-05 across all capabilities); neutrality gate green; no cross-module regression.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/whatsapp tests/unit/agent-tools tests/unit/knowledge` — all green; WhatsApp assertions unchanged.
- Neutrality grep over `lib/agent-tools/`: none of `lib/whatsapp`, `ownerPhone`, `WhatsAppMessage`, `sendWhatsAppMessage`, `whatsapp_`, `downloadWhatsAppMedia`.
- `npx tsc --noEmit` clean on all touched files.
- Full suite `npx vitest run` green (modulo the documented pre-existing flake) before `/gsd:verify-work`.
</verification>

<success_criteria>
- NEUT-01: neutral `createEstimate` dispatches EVENT_ESTIMATE_GENERATE with a trusted companyId; WhatsApp media-ingest CREATE path untouched.
- NEUT-04: neutral `askKnowledge` wraps `answer`; WhatsApp dispatchKnowledge calls it with identical behavior.
- NEUT-05: the full WhatsApp parity suite stays green with assertions unchanged across all four capabilities.
- The `lib/agent-tools/` barrel is the single neutral surface Phase 124's chat will bind — and it imports no channel.
</success_criteria>

<output>
After completion, create `.planning/phases/122-channel-neutral-domain-extraction/122-03-SUMMARY.md`
</output>
