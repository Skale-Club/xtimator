# Plan 146-01: Wire isAdmin to CompanySelector

## Goal
Pass `isAdmin` from `app/(app)/layout.tsx` down through `Sidebar` → `CompanySelector` so that Phase 147 can conditionally render the "Add new company" button only for platform admins.

## Context
- `isAdmin` is already computed in `layout.tsx` line 79 via `platform_admins` table lookup
- `isAdmin` is already passed to `<Topbar>` but NOT to `<Sidebar>`
- `Sidebar` renders `CompanySelector` twice (collapsed + expanded states)
- `CompanySelector` currently always renders "Add new company" as a disabled item
- No new DB schema needed — `platform_admins` table already exists

## Tasks

### T-01: Pass isAdmin to Sidebar in layout.tsx
**File:** `app/(app)/layout.tsx`
**Change:** Add `isAdmin={isAdmin}` to the `<Sidebar>` props block (after `isDemo`).

### T-02: Add isAdmin prop to SidebarProps and thread to CompanySelector
**File:** `components/app-shell/sidebar.tsx`
**Changes:**
1. Add `isAdmin?: boolean` to the `SidebarProps` interface
2. Destructure `isAdmin` in the `Sidebar` function signature
3. Pass `isAdmin={isAdmin}` to BOTH `<CompanySelector>` calls (collapsed and expanded)

### T-03: Add isAdmin prop to CompanySelectorProps and conditionally render "Add new company"
**File:** `components/app-shell/company-selector.tsx`
**Changes:**
1. Add `isAdmin?: boolean` to the `CompanySelectorProps` interface
2. Destructure `isAdmin` in the `CompanySelector` function signature
3. Wrap the "Add new company" `DropdownMenuItem` in `{isAdmin && (...)}` — remove it from DOM entirely for non-admins (not just CSS-hidden or disabled)
4. Remove the `DropdownMenuSeparator` that precedes "Add new company" from the DOM when `!isAdmin` (only show separator when "Add new company" is visible)

### T-04: Grep for hardcoded email/user-ID auth guards
**Action:** Run `grep -r "skale.club\|hardcoded\|@gmail.com" --include="*.ts" --include="*.tsx" app/ lib/` to verify no hardcoded admin checks exist.
**Expected:** Zero matches. If any found, replace with `requireAdmin()` from `lib/auth/admin-context.ts`.

### T-05: Commit Phase 146
**Commit message:** `feat(admin): wire isAdmin to CompanySelector — Phase 146`

## Acceptance Criteria
- [ ] Non-admin user: "Add new company" item is completely absent from the CompanySelector dropdown DOM
- [ ] Admin user (in `platform_admins` table): "Add new company" item is visible and the separator above it is present
- [ ] No hardcoded email addresses used as authorization checks anywhere in app/ or lib/
- [ ] TypeScript strict mode passes (no new type errors)
- [ ] No changes to the regular onboarding flow (`/onboarding`)

## Notes
- Phase 147 will add the modal that opens when "Add new company" is clicked
- For now, Phase 146 only ensures the button appears for admins — clicking it does nothing new yet (the disabled attr can stay or be removed as part of Phase 147)
- The separator before "Add new company" should only render when `isAdmin` is true, to avoid a floating separator for non-admins
