---
phase: 135-schema-roles-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth/require-company-role.ts
  - tests/unit/actions/require-company-role.test.ts
autonomous: true
requirements: [SEAT-02]

must_haves:
  truths:
    - "requireCompanyRole(companyId, allowedRoles) resolves the caller's company_members role SERVER-SIDE (RLS-bound client, never a client-supplied role) and denies when the role is not in allowedRoles"
    - "An owner passes the owner-only gate; an admin passes the manager gate (owner|admin) but FAILS the owner-only gate; a member FAILS the manager gate; a non-member (no membership row) is DENIED"
    - "requireCompanyManager (owner|admin) and requireCompanyOwner (owner) convenience wrappers delegate to the single requireCompanyRole — role authority lives in exactly ONE place"
    - "Denial throws a typed XtimatorError (forbidden surface), not a client-trusted boolean"
  artifacts:
    - path: "lib/auth/require-company-role.ts"
      provides: "The single server-side role-authority helper + manager/owner convenience wrappers"
      exports: ["requireCompanyRole", "requireCompanyManager", "requireCompanyOwner"]
      min_lines: 40
    - path: "tests/unit/actions/require-company-role.test.ts"
      provides: "Behavioral matrix test (owner/admin/member/non-member × manager/owner gate) mocking the supabase chain"
      contains: "describe('SEAT-02"
  key_links:
    - from: "lib/auth/require-company-role.ts"
      to: "company_members"
      via: "RLS-bound server client query on (user_id = auth.uid(), company_id)"
      pattern: "from\\('company_members'\\)"
    - from: "lib/auth/require-company-role.ts"
      to: "XtimatorError"
      via: "throw forbidden on deny"
      pattern: "forbidden"
---

<objective>
Create the ONE place team-seat role authority is decided: a server-side `requireCompanyRole(companyId, allowedRoles)` helper that resolves the caller's `company_members` role from the RLS-bound server client (never trusting a role from the request body) and throws a typed `forbidden` error when the role is not allowed. Ship thin `requireCompanyManager` (owner|admin) and `requireCompanyOwner` (owner) wrappers so every later team/billing action gates through this single gate. Lock the locked role matrix with a behavioral matrix test.

Purpose: SEAT-02 — the locked matrix says owner+admin manage members/invites; owner-only for billing/seat/ownership. This helper is the only enforcement point; Phases 136-140 call it. It must be server-side and never client-trusted.
Output: `lib/auth/require-company-role.ts` + `tests/unit/actions/require-company-role.test.ts`.

SCOPE FENCE: the authorization helper + its test ONLY. NO invite/member/billing actions (later phases). Do NOT wire it into any existing action in this plan — just create the gate and prove it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# The membership-check idiom to mirror (getClaims → company_members query on user_id+company_id).
@lib/actions/active-company.ts

# Existing server-auth guard helpers — match this module shape & the `server-only` import.
@lib/auth/context.ts
@lib/auth/admin-context.ts

# The typed-error pattern requireCompanyRole throws through.
@lib/errors/index.ts

# The behavioral action-test mock pattern (vi.mock the supabase server client `from` chain).
@tests/unit/actions/invoice.test.ts

<interfaces>
<!-- Contracts the executor needs — no exploration required. -->

Supabase server client (lib/supabase/server.ts):
  createClient(): Promise<SupabaseClient>   // RLS-bound (uses the user's JWT, not service role)
  await supabase.auth.getClaims() → { data: { claims: { sub: string, email?: string } | null } }

Membership query idiom (from active-company.ts — mirror exactly):
  const { data: membership } = await supabase
    .from('company_members')
    .select('role')
    .eq('user_id', claims.sub)
    .eq('company_id', companyId)
    .maybeSingle()
  // membership === null  → caller is NOT a member of companyId → DENY

Typed errors (lib/errors):
  new XtimatorError(type, surface, message)   // type 'unauthorized' (401) | 'forbidden' (403)
  throwIfForbidden(condition, surface, message)  // throws forbidden when condition is false
  // Valid Surface values include: 'company', 'billing', 'auth'.  Use 'company'.

Existing guard module shape to match (lib/auth/admin-context.ts):
  - starts with `import 'server-only'`
  - exports a `requireX()` that throws/redirects on deny and returns a context on allow.

Locked role matrix (NON-NEGOTIABLE):
  owner            → manage members/invites: YES ; billing/seat/ownership: YES
  admin            → manage members/invites: YES ; billing/seat/ownership: NO
  member           → manage members/invites: NO  ; billing/seat/ownership: NO
  non-member/none  → everything: NO
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: requireCompanyRole helper + manager/owner convenience wrappers</name>
  <files>lib/auth/require-company-role.ts</files>
  <read_first>
    - lib/actions/active-company.ts (the getClaims → company_members membership-query idiom)
    - lib/auth/admin-context.ts (the `import 'server-only'` + `requireX()` module shape)
    - lib/errors/index.ts (XtimatorError + throwIfForbidden; surface 'company')
  </read_first>
  <behavior>
    - requireCompanyRole('co_1', ['owner']) when caller's membership.role === 'owner' → RESOLVES (returns the resolved role/context, no throw).
    - requireCompanyRole('co_1', ['owner','admin']) when role === 'admin' → RESOLVES.
    - requireCompanyRole('co_1', ['owner']) when role === 'admin' → THROWS XtimatorError (forbidden).
    - requireCompanyRole('co_1', ['owner','admin']) when role === 'member' → THROWS forbidden.
    - requireCompanyRole('co_1', anyRoles) when membership === null (non-member) → THROWS forbidden.
    - requireCompanyRole when getClaims returns no sub (unauthenticated) → THROWS unauthorized (XtimatorError type 'unauthorized').
    - requireCompanyManager(companyId) delegates to requireCompanyRole(companyId, ['owner','admin']).
    - requireCompanyOwner(companyId) delegates to requireCompanyRole(companyId, ['owner']).
    - The role is read ONLY from company_members via the RLS-bound server client — never from any argument other than companyId + allowedRoles.
  </behavior>
  <action>
    Create `lib/auth/require-company-role.ts`:
      - First line: `import 'server-only'`.
      - Import `createClient` from `@/lib/supabase/server` and `XtimatorError` from `@/lib/errors`.
      - Define and export `type CompanyRole = 'owner' | 'admin' | 'member'`.
      - Export `async function requireCompanyRole(companyId: string, allowedRoles: CompanyRole[]): Promise<{ userId: string; companyId: string; role: CompanyRole }>`:
          1. `const supabase = await createClient()`
          2. `const { data } = await supabase.auth.getClaims(); const userId = data?.claims?.sub as string | undefined`
          3. if `!userId` → `throw new XtimatorError('unauthorized', 'company', 'Not authenticated')`.
          4. Query company_members for `role` filtered by `user_id = userId` and `company_id = companyId` via `.maybeSingle()` (mirror active-company.ts).
          5. `const role = membership?.role as CompanyRole | undefined`.
          6. if `!role || !allowedRoles.includes(role)` → `throw new XtimatorError('forbidden', 'company', 'Insufficient company role')`.
          7. return `{ userId, companyId, role }`.
      - Export `requireCompanyManager(companyId: string)` = `requireCompanyRole(companyId, ['owner','admin'])`.
      - Export `requireCompanyOwner(companyId: string)` = `requireCompanyRole(companyId, ['owner'])`.
      - Add a header doc comment: "SEAT-02 — the SINGLE server-side role-authority gate for team seats. Role is resolved from company_members on the RLS-bound client; never trusted from the client. owner+admin manage members/invites; owner-only for billing/seat/ownership."
    Use the RLS-bound `createClient` (NOT requireServiceClient) so the gate reads under the caller's identity.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/xtimator" && node -e "const s=require('fs').readFileSync('lib/auth/require-company-role.ts','utf8'); for(const n of ['server-only',\"from('company_members')\".replace(/\\\\/g,''),'requireCompanyRole','requireCompanyManager','requireCompanyOwner','forbidden']){ if(!s.includes(n)){console.error('MISSING',n);process.exit(1);} } if(s.includes('requireServiceClient')){console.error('must NOT use service role');process.exit(1);} console.log('helper OK');"</automated>
  </verify>
  <done>
    `lib/auth/require-company-role.ts` exports `requireCompanyRole`, `requireCompanyManager`, `requireCompanyOwner`; reads the role from `company_members` via the RLS-bound server client (not service role); throws `unauthorized` when unauthenticated and `forbidden` when role ∉ allowedRoles; the convenience wrappers delegate to the single helper.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Behavioral matrix test (owner/admin/member/non-member × manager/owner gate)</name>
  <files>tests/unit/actions/require-company-role.test.ts</files>
  <read_first>
    - tests/unit/actions/invoice.test.ts (vi.mock of `@/lib/supabase/server` createClient + the per-table `from` mock chain)
    - lib/auth/require-company-role.ts (the module under test, written in Task 1)
  </read_first>
  <action>
    Create `tests/unit/actions/require-company-role.test.ts`.

    Mock the RLS-bound client exactly like invoice.test.ts: `vi.mock('@/lib/supabase/server', () => ({ createClient: vi.fn() }))`. Build a helper `mockSupabase({ role })` that returns a fake client whose `auth.getClaims()` resolves `{ data: { claims: { sub: 'u_1' } } }` (or `null` sub for the unauthenticated case) and whose `from('company_members').select(...).eq(...).eq(...).maybeSingle()` resolves `{ data: role ? { role } : null }`. Wire `createClient` to resolve that fake client per case.

    `describe('SEAT-02: requireCompanyRole role matrix')` with these `it()` cases (use `await expect(...).resolves` / `.rejects.toThrow()`):
      1. owner passes owner-only gate: role='owner', `requireCompanyOwner('co_1')` resolves (assert returned `.role === 'owner'`).
      2. admin passes manager gate: role='admin', `requireCompanyManager('co_1')` resolves.
      3. admin FAILS owner-only gate: role='admin', `requireCompanyOwner('co_1')` rejects (toThrow).
      4. member FAILS manager gate: role='member', `requireCompanyManager('co_1')` rejects.
      5. non-member denied: membership=null, `requireCompanyManager('co_1')` rejects.
      6. unauthenticated denied: claims.sub absent, `requireCompanyRole('co_1', ['owner','admin'])` rejects.
      7. server-side read proof: assert the mocked `from` was called with `'company_members'` (i.e. the role came from the DB, not an argument) — e.g. `expect(fromSpy).toHaveBeenCalledWith('company_members')`.

    Reset mocks in `beforeEach` (mirror invoice.test.ts). Re-import the module inside each test (or top-level import) so the per-case `createClient` mock is honored.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/xtimator" && npx vitest run tests/unit/actions/require-company-role.test.ts</automated>
  </verify>
  <done>
    `npx vitest run tests/unit/actions/require-company-role.test.ts` passes all 7 cases; the matrix proves owner/admin/member/non-member/unauthenticated behavior and that the role is read from `company_members` server-side (the `from('company_members')` call is asserted).
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/actions/require-company-role.test.ts` is green (7 cases).
- `grep -n "requireServiceClient" lib/auth/require-company-role.ts` returns NOTHING (gate reads under caller identity, never service role).
- `grep -n "requireCompanyManager\|requireCompanyOwner" lib/auth/require-company-role.ts` shows both wrappers delegate to `requireCompanyRole`.
</verification>

<success_criteria>
- A single `requireCompanyRole(companyId, allowedRoles)` server helper resolves the caller's `company_members` role from the RLS-bound client and throws a typed `forbidden` (or `unauthorized`) on deny — never trusting a client-supplied role.
- `requireCompanyManager` (owner|admin) and `requireCompanyOwner` (owner) delegate to it — role authority lives in exactly one place.
- The behavioral matrix test proves every role × capability cell incl. non-member + unauthenticated denial and that the role is read server-side.
- SEAT-02 satisfied; the gate every team/billing action in Phases 136-140 calls is ready.
</success_criteria>

<output>
After completion, create `.planning/phases/135-schema-roles-auth/135-02-SUMMARY.md`.
</output>
