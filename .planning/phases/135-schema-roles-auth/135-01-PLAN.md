---
phase: 135-schema-roles-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260628000001_phase135_team_seats_schema.sql
  - tests/unit/phase135-team-seats-migration.test.ts
autonomous: true
requirements: [SEAT-01]

must_haves:
  truths:
    - "company_members.role accepts 'owner', 'admin', and 'member' (CHECK widened from owner-only)"
    - "A company_invites table exists with id/company_id/email/role/token/status/invited_by/expires_at/created_at"
    - "RLS on company_invites lets an owner/admin of the company read & manage that company's invites; no broad anon/public write policy (token-accept is service-role only)"
    - "Existing owner rows are untouched and the migration changes no companies billing column"
    - "Re-running the migration is safe (DROP CONSTRAINT IF EXISTS before ADD; CREATE TABLE IF NOT EXISTS; CREATE INDEX IF NOT EXISTS; idempotent policy creation)"
  artifacts:
    - path: "supabase/migrations/20260628000001_phase135_team_seats_schema.sql"
      provides: "Role CHECK widening + company_invites table + RLS"
      contains: "create table if not exists public.company_invites"
    - path: "tests/unit/phase135-team-seats-migration.test.ts"
      provides: "Static SQL-contract test asserting the widened CHECK, the table shape, and RLS posture"
      contains: "describe('phase 135"
  key_links:
    - from: "20260628000001_phase135_team_seats_schema.sql"
      to: "company_members_role_check"
      via: "DROP CONSTRAINT IF EXISTS then ADD CONSTRAINT with role IN ('owner','admin','member')"
      pattern: "ADD CONSTRAINT company_members_role_check"
    - from: "company_invites.company_id"
      to: "companies(id)"
      via: "FK ON DELETE CASCADE"
      pattern: "references companies\\(id\\)"
---

<objective>
Land the v4.12 Team Seats data foundation in ONE idempotent, authored-only migration: widen the `company_members.role` CHECK from `('owner')` to `('owner','admin','member')`, and create the `company_invites` table with RLS that mirrors the Phase-79 `company_members` posture (owner/admin of the company manage its invites; the token-based accept path is service-role only). A static SQL-contract test locks the shape so the migration cannot drift.

Purpose: SEAT-01 — every downstream phase (invites, accept, member UI, seat billing) reads this schema. It must be correct, retrocompat (existing `owner` rows untouched, no `companies` billing column changed), and idempotent.
Output: `supabase/migrations/20260628000001_phase135_team_seats_schema.sql` + `tests/unit/phase135-team-seats-migration.test.ts`.

SCOPE FENCE: schema + role widening ONLY. NO invite actions, NO accept, NO UI, NO billing config, NO `requireCompanyRole` (that is Plan 02). Do NOT apply the migration to any remote — it is carried by CI→GHCR→Coolify and NEVER `supabase db push`-ed here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# The foundation to MIRROR — table, the role CHECK to widen, the RLS pattern.
@supabase/migrations/20260525000001_phase79_company_members.sql

# The idempotent ADD/DROP-named-CONSTRAINT template + authored-only header convention.
@supabase/migrations/20260627000001_phase129_advanced_pricing_schema.sql

# The static SQL-contract test template to mirror (read-file + regex assertions, incl. secret-scan).
@tests/unit/company-members-migration.test.ts

<interfaces>
<!-- Facts the executor needs — no codebase exploration required. -->

Phase-79 company_members (the table being widened):
  role TEXT NOT NULL DEFAULT 'owner' CHECK (role IN ('owner'))
  -- This is an INLINE column CHECK with NO explicit name. Postgres auto-names it
  -- `company_members_role_check`. To widen idempotently:
  --   ALTER TABLE public.company_members DROP CONSTRAINT IF EXISTS company_members_role_check;
  --   ALTER TABLE public.company_members ADD  CONSTRAINT company_members_role_check CHECK (role IN ('owner','admin','member'));

Phase-79 RLS posture to MIRROR for company_invites manage-policies:
  - `company_members_select` gates `FOR SELECT TO authenticated USING (user_id = (SELECT auth.uid()))`.
  - For company_invites, gate by a subquery proving the caller is an owner/admin MEMBER of the invite's company:
      USING ( company_id IN (
        SELECT company_id FROM public.company_members
        WHERE user_id = (SELECT auth.uid()) AND role IN ('owner','admin')
      ) )
  - The token-based ACCEPT path (Phase 137) runs via the service role, which BYPASSES RLS — so DO NOT add any anon/public SELECT-by-token policy here. Document that in a comment.

Naming/header conventions (from 20260627000001):
  - File header comment block: phase, requirement id, "Authored-only — carried by CI->GHCR->Coolify; NOT applied on the VPS (never `supabase db push`)." and "Idempotent."
  - gen_random_uuid() is the default-uuid function used project-wide.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the idempotent authored-only schema migration</name>
  <files>supabase/migrations/20260628000001_phase135_team_seats_schema.sql</files>
  <read_first>
    - supabase/migrations/20260525000001_phase79_company_members.sql (the table + the unnamed inline role CHECK to widen + the RLS pattern)
    - supabase/migrations/20260627000001_phase129_advanced_pricing_schema.sql (header convention + DROP/ADD named-CONSTRAINT idempotent template)
  </read_first>
  <action>
    Create `supabase/migrations/20260628000001_phase135_team_seats_schema.sql` (VERIFY no file with that timestamp already exists — newest is 20260627000001, so this is collision-free).

    Header comment block (mirror 20260627000001): name the phase + SEAT-01, state "Authored-only — carried by CI->GHCR->Coolify; NOT applied on the VPS (never `supabase db push`)." and "Idempotent." Use placeholders only — NO secrets anywhere.

    1. WIDEN the role CHECK (idempotent, named):
       - `ALTER TABLE public.company_members DROP CONSTRAINT IF EXISTS company_members_role_check;`
       - `ALTER TABLE public.company_members ADD CONSTRAINT company_members_role_check CHECK (role IN ('owner','admin','member'));`
       (The DEFAULT stays 'owner'. Do NOT touch the column type, the PK, or existing rows.)

    2. CREATE the invites table (idempotent):
       `CREATE TABLE IF NOT EXISTS public.company_invites (`
         `id uuid PRIMARY KEY DEFAULT gen_random_uuid(),`
         `company_id uuid NOT NULL REFERENCES companies(id) ON DELETE CASCADE,`
         `email text NOT NULL,`
         `role text NOT NULL CHECK (role IN ('admin','member')),`  -- an invite NEVER creates an owner
         `token text NOT NULL UNIQUE,`
         `status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','accepted','revoked','expired')),`
         `invited_by uuid REFERENCES auth.users(id),`
         `expires_at timestamptz NOT NULL,`
         `created_at timestamptz NOT NULL DEFAULT now()`
       `);`
       Add a COMMENT ON TABLE noting it is the Phase-135 invite store and that accept is service-role-only.

    3. INDEXES (idempotent): `CREATE INDEX IF NOT EXISTS company_invites_company_id ON public.company_invites(company_id);` and `CREATE INDEX IF NOT EXISTS company_invites_token ON public.company_invites(token);`

    4. RLS (mirror Phase-79 posture):
       - `ALTER TABLE public.company_invites ENABLE ROW LEVEL SECURITY;`
       - Make policy creation idempotent: wrap each policy in a guarded block (e.g. `DROP POLICY IF EXISTS "<name>" ON public.company_invites;` immediately before each `CREATE POLICY`, OR a `DO $$ ... IF NOT EXISTS (SELECT FROM pg_policies ...) ... $$;` block — pick the DROP-then-CREATE form for readability).
       - Three manage-policies, each scoped via the owner/admin `company_members` subquery shown in <interfaces>:
         `company_invites_select` FOR SELECT TO authenticated USING (<subquery>)
         `company_invites_insert` FOR INSERT TO authenticated WITH CHECK (<subquery>)
         `company_invites_update` FOR UPDATE TO authenticated USING (<subquery>) WITH CHECK (<subquery>)
       - NO anon/public policy and NO SELECT-by-token policy. Add a comment: "Token-based accept (Phase 137) runs via the service role, which bypasses RLS — no public/anon policy here."

    Touch NO `companies` billing column. Add NO backfill (invites start empty).
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/xtimator" && node -e "const s=require('fs').readFileSync('supabase/migrations/20260628000001_phase135_team_seats_schema.sql','utf8'); const need=['ADD CONSTRAINT company_members_role_check','create table if not exists public.company_invites','enable row level security','company_invites_select']; const low=s.toLowerCase(); for(const n of need){ if(!low.includes(n.toLowerCase())){console.error('MISSING:',n);process.exit(1);} } if(/whsec_|sk-ant-|sb_secret_|sk_(test|live)_/.test(s)){console.error('SECRET LEAK');process.exit(1);} console.log('migration OK');"</automated>
  </verify>
  <done>
    The migration file exists at the 20260628000001 timestamp; it DROP/ADDs the named `company_members_role_check` with the 3-role matrix; creates `company_invites` with all 9 columns + the two CHECKs + UNIQUE token + the two indexes; enables RLS with owner/admin-scoped select/insert/update policies and no anon/token policy; changes no companies billing column; contains no secrets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the static SQL-contract test that locks the migration shape</name>
  <files>tests/unit/phase135-team-seats-migration.test.ts</files>
  <read_first>
    - tests/unit/company-members-migration.test.ts (the read-file + regex-assert template, including the secret-scan test)
    - supabase/migrations/20260628000001_phase135_team_seats_schema.sql (the file under test, written in Task 1)
  </read_first>
  <action>
    Create `tests/unit/phase135-team-seats-migration.test.ts`, mirroring the structure of `company-members-migration.test.ts` (read the migration file once with `readFileSync`, then a `describe('phase 135 — team seats schema migration shape')` block of `it()` regex/`toContain` assertions). Point `MIGRATION_PATH` at `supabase/migrations/20260628000001_phase135_team_seats_schema.sql`.

    Assertions (exact-ish — match the SQL the migration emits, case-tolerant where the template is):
      - SEAT-01 widen: `expect(SQL).toMatch(/DROP CONSTRAINT IF EXISTS company_members_role_check/i)` AND `expect(SQL).toMatch(/ADD CONSTRAINT company_members_role_check\s+CHECK\s*\(role IN \('owner',\s*'admin',\s*'member'\)\)/i)`
      - Idempotent table: `expect(SQL).toMatch(/CREATE TABLE IF NOT EXISTS public\.company_invites/i)`
      - All 9 columns present: assert each of `company_id`, `email`, `role`, `token`, `status`, `invited_by`, `expires_at`, `created_at`, and an `id ... primary key` line.
      - Invite role can never be owner: `expect(SQL).toMatch(/role text NOT NULL CHECK \(role IN \('admin',\s*'member'\)\)/i)`
      - Status enum: `expect(SQL).toMatch(/status IN \('pending',\s*'accepted',\s*'revoked',\s*'expired'\)/i)`
      - Token unique: `expect(SQL).toMatch(/token text NOT NULL UNIQUE/i)`
      - FK cascade to companies: `expect(SQL).toMatch(/REFERENCES companies\(id\) ON DELETE CASCADE/i)`
      - RLS enabled: `expect(SQL).toMatch(/ENABLE ROW LEVEL SECURITY/i)`
      - Owner/admin manage-scope subquery: `expect(SQL).toMatch(/role IN \('owner',\s*'admin'\)/i)` AND `expect(SQL).toContain('company_invites_select')`
      - NO broad accept/anon policy: `expect(SQL).not.toMatch(/TO anon/i)` AND `expect(SQL).not.toMatch(/USING \(true\)/i)`
      - Idempotent indexes: `expect(SQL).toMatch(/CREATE INDEX IF NOT EXISTS company_invites_token/i)`
      - Retrocompat: `expect(SQL).not.toMatch(/ALTER TABLE companies[^;]*(stripe|tier|subscription)/i)` (no companies billing column touched)
      - Secret-scan (copy verbatim from the template): no `whsec_`, `sk-ant-`, `sb_secret_`, `sk_(test|live)_`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/xtimator" && npx vitest run tests/unit/phase135-team-seats-migration.test.ts</automated>
  </verify>
  <done>
    `npx vitest run tests/unit/phase135-team-seats-migration.test.ts` passes; every assertion above is present and green; the test fails loudly if the migration drifts (wrong role list, missing column, anon policy, billing-column change, or a leaked secret).
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/phase135-team-seats-migration.test.ts` is green.
- `grep -n "company_members_role_check" supabase/migrations/20260628000001_phase135_team_seats_schema.sql` shows BOTH a DROP IF EXISTS and an ADD with the 3-role matrix.
- The migration is NOT applied anywhere (no `supabase db push`); it is committed as authored-only.
- No `companies` billing column appears in the migration.
</verification>

<success_criteria>
- One idempotent authored-only migration `20260628000001_phase135_team_seats_schema.sql` widens `company_members.role` to `('owner','admin','member')` via a named DROP/ADD and creates `company_invites` (9 columns, invite-role ∈ admin|member, status ∈ pending|accepted|revoked|expired, token UNIQUE, FK CASCADE to companies, two indexes) with owner/admin-scoped RLS and no anon/token policy.
- Re-running the migration is safe (all IF EXISTS / IF NOT EXISTS / DROP-then-CREATE).
- A static contract test locks the widened CHECK, the table shape, the RLS posture, retrocompat (no billing column), and the no-secrets rule.
- SEAT-01 satisfied; foundation ready for Plan 02 and Phases 136-140.
</success_criteria>

<output>
After completion, create `.planning/phases/135-schema-roles-auth/135-01-SUMMARY.md`.
</output>
