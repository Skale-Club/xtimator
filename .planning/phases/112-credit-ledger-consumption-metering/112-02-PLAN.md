---
phase: 112-credit-ledger-consumption-metering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/billing/billing-config.ts
  - lib/entitlements.ts
  - tests/unit/billing/billing-config.test.ts
  - tests/unit/entitlements.test.ts
autonomous: true
requirements: [CREDIT-04, CREDIT-05]
must_haves:
  truths:
    - "billing_config carries an enforcementEnabled flag defaulting to false (measure-only safety)"
    - "Entitlements exposes monthlyCreditGrant on all four tiers, mirroring billing_config defaults"
  artifacts:
    - path: "lib/billing/billing-config.ts"
      provides: "enforcementEnabled flag on BillingConfig + DEFAULT_BILLING_CONFIG"
      contains: "enforcementEnabled"
    - path: "lib/entitlements.ts"
      provides: "monthlyCreditGrant field on Entitlements + 4 tiers"
      contains: "monthlyCreditGrant"
  key_links:
    - from: "DEFAULT_BILLING_CONFIG.enforcementEnabled"
      to: "checkCredits gate (Plan 03)"
      via: "default false → never block during measure-only"
      pattern: "enforcementEnabled:\\s*false"
---

<objective>
Extend the dormant Phase-111 `billing_config` with the `enforcementEnabled` flag (default `false` — the "calibrate before charging" safety) and add the `monthlyCreditGrant` entitlement field to all four tiers. These are the two config/type slots the credit-ledger helper (Plan 03) reads. No production behavior changes here — both are additive, backward-compatible (CREDIT-04, CREDIT-05 flag).

Purpose: Make `checkCredits` safe-by-default (enforcement off until Phase 116) and give entitlements a null-safe static grant shape mirroring the config defaults.
Output: modified `lib/billing/billing-config.ts`, `lib/entitlements.ts`, plus extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-credit-ledger-consumption-metering/112-RESEARCH.md

<interfaces>
From lib/billing/billing-config.ts (Phase 111 — EXTEND, do not rewrite):
```ts
export type BillingConfig = {
  markup: number              // default 4.5
  creditUnitUsd: number       // default 0.01
  tiers: Record<BillingTier, TierBilling>  // TierBilling = { monthlyCreditGrant, subscriptionPriceCents }
  meteredOperations: Record<string, boolean>
  lowBalanceThresholds: number[]
  // ... NO enforcementEnabled yet — add it
}
export const DEFAULT_BILLING_CONFIG: BillingConfig = { markup: 4.5, creditUnitUsd: 0.01, ... }
// getBillingConfig() deep-merges stored over defaults; a row written before a field existed still resolves.
```
Tiers already carry monthlyCreditGrant: free 0 / trial 2000 / pro 9000 / business 30000.

From lib/entitlements.ts (Phase 55/108 — the maxPriceResearchPerMonth Phase-108 addition is the precedent for adding a field):
```ts
export type Entitlements = { maxEstimatesPerMonth: number | null; maxPriceResearchPerMonth: number | null; ... }
export const tiers: Record<TierName, Entitlements> = { free: {...}, trial: {...}, pro: {...}, business: {...} }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add enforcementEnabled flag to billing_config</name>
  <read_first>
    - lib/billing/billing-config.ts (the BillingConfig type + DEFAULT_BILLING_CONFIG + getBillingConfig deep-merge — add one field)
    - tests/unit/billing/billing-config.test.ts (the existing test shape to extend)
  </read_first>
  <files>lib/billing/billing-config.ts, tests/unit/billing/billing-config.test.ts</files>
  <behavior>
    - getBillingConfig() returns enforcementEnabled === false by default (no stored row).
    - A stored row WITHOUT enforcementEnabled still resolves (deep-merge tolerates absence) → false.
    - A stored row WITH enforcementEnabled: true overrides the default → true.
  </behavior>
  <action>
In `lib/billing/billing-config.ts`:
1. Add `enforcementEnabled: boolean` to the `BillingConfig` type, with a doc comment: "Master charging switch (CREDIT-05). Default FALSE — debits RECORD but checkCredits NEVER blocks until Phase 116 calibration flips it on (calibrate before charging)."
2. Add `enforcementEnabled: false` to `DEFAULT_BILLING_CONFIG`.
3. The existing shallow `{ ...DEFAULT_BILLING_CONFIG, ...stored }` deep-merge in `getBillingConfig()` already carries the new field through (a top-level boolean merges fine — no change needed to the merge body; verify it does).

In `tests/unit/billing/billing-config.test.ts`, add assertions:
- `DEFAULT_BILLING_CONFIG.enforcementEnabled` is `false`.
- A round-trip via the schema/merge: a stored partial missing `enforcementEnabled` resolves to `false`; a stored `{ enforcementEnabled: true }` resolves to `true`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/billing-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `enforcementEnabled: boolean` in the BillingConfig type.
    - grep `enforcementEnabled: false` in DEFAULT_BILLING_CONFIG.
    - Test asserts default `false` and override `true`; passes GREEN.
    - No other DEFAULT_BILLING_CONFIG values changed (markup 4.5, creditUnitUsd 0.01 intact).
  </acceptance_criteria>
  <done>billing_config exposes enforcementEnabled default false; tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add monthlyCreditGrant to Entitlements + 4 tiers</name>
  <read_first>
    - lib/entitlements.ts (the Entitlements type + 4 tiers; the maxPriceResearchPerMonth Phase-108 addition is the precedent)
    - tests/unit/entitlements.test.ts (extend, mirroring existing per-tier field assertions)
    - lib/billing/billing-config.ts (DEFAULT_BILLING_CONFIG.tiers — the authoritative grant values to mirror: free 0 / trial 2000 / pro 9000 / business 30000)
  </read_first>
  <files>lib/entitlements.ts, tests/unit/entitlements.test.ts</files>
  <behavior>
    - tiers.free.monthlyCreditGrant === 0
    - tiers.trial.monthlyCreditGrant === 2000
    - tiers.pro.monthlyCreditGrant === 9000
    - tiers.business.monthlyCreditGrant === 30000
    - getEntitlements('unknown').monthlyCreditGrant === 0 (falls back to free)
  </behavior>
  <action>
In `lib/entitlements.ts`:
1. Add `monthlyCreditGrant: number` to the `Entitlements` type with a doc comment: "Static/fallback per-tier monthly credit grant (CREDIT-04). Mirrors billing_config.tiers[tier].monthlyCreditGrant — the AUTHORITATIVE runtime value is read from getBillingConfig() at grant time (BILLCFG-03, no hard-coded billing numbers). This field keeps static callers null-safe, exactly as maxPriceResearchPerMonth was added in Phase 108."
2. Add the field to all four tiers mirroring DEFAULT_BILLING_CONFIG.tiers: free 0, trial 2000, pro 9000, business 30000.

In `tests/unit/entitlements.test.ts`, add assertions for all four tiers' `monthlyCreditGrant` values and that `getEntitlements('garbage').monthlyCreditGrant === 0` (free fallback).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/entitlements.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `monthlyCreditGrant: number` in the Entitlements type.
    - All four tiers carry monthlyCreditGrant (0 / 2000 / 9000 / 30000) matching DEFAULT_BILLING_CONFIG.tiers.
    - Test asserts all four values + the free fallback; passes GREEN.
    - `npx tsc --noEmit` clean (the type is satisfied by all 4 tiers — `satisfies Record<TierName, Entitlements>`).
  </acceptance_criteria>
  <done>monthlyCreditGrant present on all 4 tiers mirroring config; tests GREEN.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/billing/billing-config.test.ts tests/unit/entitlements.test.ts` GREEN.
- `npx tsc --noEmit` clean on the two touched modules.
</verification>

<success_criteria>
- enforcementEnabled present + default false; checkCredits (Plan 03) can safely read it.
- monthlyCreditGrant present on all 4 tiers, mirroring billing_config defaults.
- Both additions backward-compatible (no existing values changed).
</success_criteria>

<output>
After completion, create `.planning/phases/112-credit-ledger-consumption-metering/112-02-SUMMARY.md`.
</output>
