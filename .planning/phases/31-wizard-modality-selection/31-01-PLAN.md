---
phase: 31-wizard-modality-selection
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - types/database.types.ts
  - lib/schemas/project.ts
  - lib/actions/project.ts
  - components/projects/new-project-wizard.tsx
  - components/projects/step-modality-select.tsx
autonomous: true
requirements:
  - WIZARD-01
  - WIZARD-02
  - WIZARD-03
  - WIZARD-04

must_haves:
  truths:
    - User sees 3 large clickable modality cards after client selection
    - Each card displays icon, label, and use case description
    - Clicking a card navigates to the correct route based on modality
    - Selected modality is stored in project record as input_mode
  artifacts:
    - path: "types/database.types.ts"
      provides: "Project row type with input_mode field"
    - path: "lib/schemas/project.ts"
      provides: "ProjectFormValues with inputMode field"
    - path: "components/projects/step-modality-select.tsx"
      provides: "Modality selection UI (3 cards)"
    - path: "components/projects/new-project-wizard.tsx"
      provides: "2-step wizard with client then modality"
    - path: "lib/actions/project.ts"
      provides: "createProjectAction with input_mode storage"
  key_links:
    - from: "step-modality-select.tsx"
      to: "new-project-wizard.tsx"
      via: "form context"
    - from: "new-project-wizard.tsx"
      to: "project.ts action"
      via: "createProjectAction call"
    - from: "new-project-wizard.tsx"
      to: "/capture, /describe, /photos-input"
      via: "router.push with conditional path"
---

<objective>
Add a second step to the new project wizard where users choose between Audio/Text/Photos as their input modality, then redirect to the appropriate route.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(app)/projects/new/page.tsx
@components/projects/new-project-wizard.tsx
@components/projects/step-client-select.tsx
@lib/schemas/project.ts
@lib/actions/project.ts
@types/database.types.ts

# Key existing patterns
- step-client-select.tsx: step component using UseFormReturn pattern
- new-project-wizard.tsx: Card wrapper, form handling, useTransition for submission
- projectSchema: zod schema with clientId, clientName fields
- createProjectAction: creates project with placeholder name, returns {data: project}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration + TypeScript types for input_mode</name>
  <files>types/database.types.ts</files>
  <action>
Add input_mode column to projects table in database types. The column should be nullable with type: 'audio' | 'text' | 'photos' | 'mixed' | null.

In database.types.ts, update the projects Table Row, Insert, and Update types to include:
- input_mode: 'audio' | 'text' | 'photos' | 'mixed' | null (as nullable)

Note: Migration SQL will be added via a separate migration step (handled by supabase db push in a follow-up step, this task just updates the TypeScript types to match the expected schema).
  </action>
  <verify>TypeScript compiles without errors on projects types</verify>
  <done>Database types updated with input_mode field on projects table</done>
</task>

<task type="auto">
  <name>Task 2: Update project schema with inputMode field</name>
  <files>lib/schemas/project.ts</files>
  <action>
Extend the projectSchema to include inputMode field. The schema should accept:
- inputMode: optional enum of 'audio' | 'text' | 'photos' | 'mixed'

Update ProjectFormValues type to include inputMode.

Update STEP_FIELDS to reflect the 2-step structure: Step 1 = clientId (optional), Step 2 = inputMode (required).
  </action>
  <verify>TypeScript compiles: import { projectSchema } from '@/lib/schemas/project' works</verify>
  <done>Project schema updated with inputMode field and 2-step validation</done>
</task>

<task type="auto">
  <name>Task 3: Create StepModalitySelect component</name>
  <files>components/projects/step-modality-select.tsx</files>
  <action>
Create a new step component that displays 3 large clickable cards for modality selection:

1. Layout: 3-column grid on desktop (grid-cols-3), stacked on mobile (grid-cols-1)
2. Each card should have:
   - Large icon (Mic, FileText, Camera from lucide-react)
   - Bold label: "Audio", "Text", "Photos"
   - Brief description: "Record a voice description", "Type a job description", "Upload photos"
   - Subtle border and hover state (border-muted, hover:border-primary)
3. Selection: Clicking a card sets form.setValue('inputMode', value) and visually highlights selected card

The component should accept:
- form: UseFormReturn<ProjectFormValues>
- No other props needed

Use the same pattern as step-client-select.tsx (UseFormReturn pattern, no internal state needed for selection).
  </action>
  <verify>npm run build succeeds</verify>
  <done>StepModalitySelect component created with 3 cards, selection updates form</done>
</task>

<task type="auto">
  <name>Task 4: Update NewProjectWizard for 2-step flow + routing</name>
  <files>components/projects/new-project-wizard.tsx</files>
  <action>
Update the wizard to support 2 steps:

1. State management: Add currentStep state (1 or 2), default to 1
2. Step 1 (current): Client selection - unchanged
3. Step 2 (new): Modality selection using StepModalitySelect component
4. Navigation: 
   - "Continue" button on step 1 goes to step 2
   - "Continue" button on step 2 triggers form submission and redirects
5. On submit (step 2):
   - Call createProjectAction with clientId and inputMode from form values
   - After project created, redirect based on input_mode:
     - 'audio' → /projects/[id]/capture
     - 'text' → /projects/[id]/describe  
     - 'photos' → /projects/[id]/photos-input
     - 'mixed' → /projects/[id]/capture (default to audio)

Update button labels:
- Step 1: "Continue to modality"
- Step 2: "Start [Audio/Text/Photos] capture" (based on selection)

Add Back button on step 2 to return to step 1.
  </action>
  <verify>npm run build succeeds</verify>
  <done>2-step wizard working with client → modality flow and conditional routing</done>
</task>

<task type="auto">
  <name>Task 5: Update createProjectAction to store input_mode</name>
  <files>lib/actions/project.ts</files>
  <action>
Update createProjectAction to:
1. Accept inputMode from formData (ProjectFormValues now includes inputMode)
2. Include input_mode in the insert query to projects table
3. The redirect URL should come from the caller (wizard), not embedded here - the action just returns the project

The action signature remains: createProjectAction(formData: ProjectFormValues)
The return remains: { data: project } or { error: string }
The insert now includes: input_mode: formData.inputMode ?? null
  </action>
  <verify>npm run build succeeds</verify>
  <done>createProjectAction stores input_mode in database</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] TypeScript types compile without errors
- [ ] Wizard shows step 1 (client select) then step 2 (modality cards)
- [ ] Clicking a modality card navigates to correct route (/capture, /describe, /photos-input)
- [ ] input_mode is stored in the project record
</verification>

<success_criteria>
User can complete 2-step wizard: (1) select or skip client, (2) choose modality (Audio/Text/Photos), then lands on appropriate capture route with input_mode saved on project.
</success_criteria>

<output>
After completion, create `.planning/phases/31-wizard-modality-selection/31-01-SUMMARY.md`
</output>