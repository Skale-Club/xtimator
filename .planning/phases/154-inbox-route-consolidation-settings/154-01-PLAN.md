---
phase: 154-inbox-route-consolidation-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/inbox/page.tsx
  - app/admin/inbox/admin-whatsapp-client.tsx
  - app/admin/inbox/admin-whatsapp-filters.tsx
  - app/admin/inbox/loading.tsx
  - app/admin/whatsapp/page.tsx
  - app/admin/whatsapp/admin-whatsapp-client.tsx
  - app/admin/whatsapp/admin-whatsapp-filters.tsx
  - app/admin/whatsapp/loading.tsx
  - components/admin/admin-nav.tsx
autonomous: true
requirements: [INBOX-01]
must_haves:
  truths:
    - "Visiting /admin/inbox shows the conversation list (no Accounts tab, no tab-switcher chrome)"
    - "Visiting the old /admin/whatsapp URL redirects to /admin/inbox"
    - "The super-admin left-nav shows exactly one 'Inbox' item, not two WhatsApp-branded items"
    - "Filter changes, pagination links, and 'Clear filters' on /admin/inbox keep the user on /admin/inbox (never /admin/whatsapp)"
  artifacts:
    - path: "app/admin/inbox/page.tsx"
      provides: "Conversations-only Inbox page (Accounts branch + tab-switcher removed)"
      contains: "listAdminWhatsAppConversations"
    - path: "app/admin/inbox/admin-whatsapp-client.tsx"
      provides: "Verbatim relocation of the conversation table + Sheet (Phase 155 will edit this)"
      contains: "loadAdminConversationThread"
    - path: "app/admin/inbox/admin-whatsapp-filters.tsx"
      provides: "Filters bar retargeted to /admin/inbox"
      contains: "/admin/inbox"
    - path: "app/admin/inbox/loading.tsx"
      provides: "Verbatim relocated loading skeleton"
    - path: "app/admin/whatsapp/page.tsx"
      provides: "Redirect stub to /admin/inbox"
      contains: "redirect('/admin/inbox')"
  key_links:
    - from: "components/admin/admin-nav.tsx"
      to: "/admin/inbox"
      via: "NAV_ITEMS href"
      pattern: "href:\\s*'/admin/inbox'"
    - from: "app/admin/inbox/admin-whatsapp-filters.tsx"
      to: "/admin/inbox"
      via: "router.replace"
      pattern: "router\\.replace\\(`?/admin/inbox"
---

<objective>
Collapse the two super-admin nav items ("WhatsApp" + "WA Templates") into a single "Inbox" nav item, relocate the conversations-only surface from `app/admin/whatsapp/` to `app/admin/inbox/` (splitting out the Accounts tab and tab-switcher chrome — NOT a byte-verbatim copy of `page.tsx`), and turn the old `/admin/whatsapp` route into a thin redirect stub.

Purpose: This is the routing/nav backbone of the Inbox consolidation (INBOX-01). It must land before Phase 155's master-detail viewer redesign can touch `admin-whatsapp-client.tsx` in its new home.
Output: `/admin/inbox` renders the conversation list (search/filters/pagination/thread-Sheet unchanged), `/admin/whatsapp` redirects there, and the nav shows one "Inbox" entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/154-inbox-route-consolidation-settings/154-CONTEXT.md
@.planning/phases/154-inbox-route-consolidation-settings/154-UI-SPEC.md
</context>

<interfaces>
<!-- Exact current source read directly from the repo this session. Executor must use these as the literal starting point — no exploration needed. -->

### Current `app/admin/whatsapp/page.tsx` (208 lines) — the file being split
Key structure to preserve/remove when assembling the new `app/admin/inbox/page.tsx`:
- Imports: `Link`, `requireAdmin`, `requireServiceClient`, `T`, `parseAdminWhatsAppFilters`/`listAdminWhatsAppConversations` from `@/lib/queries/admin-whatsapp`, `AdminWhatsAppClient` from `./admin-whatsapp-client`, `AdminWhatsAppFilters` from `./admin-whatsapp-filters`, `AdminWhatsAppAccounts` from `./admin-whatsapp-accounts` (this last import and its usage must be DROPPED — Accounts moves to Plan 02's settings page).
- `type Row = { id, company_id, contact_phone, contact_name, last_message_at, last_message_preview, last_inbound_at, unread_count, company_name }` — keep, still needed for the conversations list.
- `await requireAdmin()` — first line, keep.
- `const sp = await searchParams; const filters = parseAdminWhatsAppFilters(sp)` — keep. DROP `const tab = sp.tab === 'accounts' ? 'accounts' : 'conversations'` (no more tab param).
- `Promise.all([listAdminWhatsAppConversations(filters), ...configResult fetch, ...senderResult fetch])` — SIMPLIFY to just `const convResult = await listAdminWhatsAppConversations(filters)` (drop the two service-role fetches for `whatsapp_company_configs`/`whatsapp_authorized_senders` — those move to Plan 02's settings page; drop the `requireServiceClient()` call and its import entirely from this file since nothing else needs it).
- `pageUrl(p: number)` helper — keep but DROP the `if (tab === 'accounts') params.set('tab', 'accounts')` line and change `` `/admin/whatsapp?${params.toString()}` `` to `` `/admin/inbox?${params.toString()}` ``.
- `tabUrl(t: string)` helper — DELETE entirely (no longer needed, single view).
- Header: change `<T>WhatsApp</T>` to `<T>Inbox</T>` and the description paragraph to `<T>Platform-managed conversations across every connected channel. Read-only conversation inspection.</T>` (exact copy from UI-SPEC.md's Copywriting Contract — this is authoritative over CONTEXT.md's looser suggestion).
- The `<div className="flex gap-1 border-b">...</div>` tab-switcher block (Conversations/Accounts `Link`s) — DELETE entirely.
- The `{tab === 'accounts' ? (...) : (...)}` ternary — DELETE the ternary and the entire `tab === 'accounts'` branch; keep ONLY the body of the `else` (conversations) branch as the page's unconditional render output (filters, count paragraph, `AdminWhatsAppClient`, pagination).
- Add a "Settings" gear-style header affordance per UI-SPEC's Copywriting Contract: a `Link` to `/admin/inbox/settings` labeled "Settings" using the `Settings2` icon from `lucide-react` (reuse this icon — do not import a new gear glyph). Place it near the page header (e.g. flex row with the `<h1>`/description block, right-aligned), styled as a simple button/link consistent with other admin page header actions (muted text link or ghost `Button`, `size="sm"`, icon 16px) — this is the "gear/Settings affordance in the Inbox header" required by INBOX-03.

### Current `app/admin/whatsapp/admin-whatsapp-filters.tsx` (163 lines) — relocate + 4 literal edits
4 exact occurrences of `/admin/whatsapp` requiring change to `/admin/inbox`:
- Line 48 (`pushParam`): `` router.replace(`/admin/whatsapp?${params.toString()}`) ``
- Line 117 (dateFrom `onChange`): `` router.replace(`/admin/whatsapp?${params.toString()}`) ``
- Line 134 (dateTo `onChange`): `` router.replace(`/admin/whatsapp?${params.toString()}`) ``
- Line 144 ("Clear filters" `onClick`): `router.replace('/admin/whatsapp')`
No other changes — component name `AdminWhatsAppFilters`, props interface, and all other logic stay identical.

### Current `app/admin/whatsapp/admin-whatsapp-client.tsx` (167 lines) — byte-verbatim relocation
Zero changes to file content. Component `AdminWhatsAppClient`, the `Row` type, table + `Sheet` — untouched (Phase 155 edits this file in its NEW location `app/admin/inbox/admin-whatsapp-client.tsx`, not this phase).

### Current `app/admin/whatsapp/loading.tsx` (41 lines) — byte-verbatim relocation
Zero changes to file content. No path literals inside.

### `components/admin/admin-nav.tsx` — current full `NAV_ITEMS` array (lines 9-29) and import line 6
```ts
import { Settings2, Palette, Users, LayoutDashboard, Globe, Layout, FileText, CreditCard, Building2, Scale, ScrollText, MessageCircle, BookOpen } from 'lucide-react'
// ...
const NAV_ITEMS = [
  { href: '/admin',              label: 'Dashboard',    Icon: LayoutDashboard },
  { href: '/admin/seo',          label: 'SEO',          Icon: Globe },
  { href: '/admin/landing',      label: 'Landing Page', Icon: Layout },
  { href: '/admin/blog',         label: 'Blog',         Icon: FileText },
  { href: '/admin/knowledge',    label: 'Knowledge',    Icon: BookOpen },
  { href: '/admin/legal',        label: 'Legal Pages',  Icon: Scale },
  { href: '/admin/branding',     label: 'Branding',     Icon: Palette },
  {
    href: '/admin/integrations/ai',
    activeBase: '/admin/integrations',
    label: 'Integrations',
    Icon: Settings2,
  },
  { href: '/admin/billing',      label: 'Billing',      Icon: CreditCard },
  { href: '/admin/companies',    label: 'Companies',    Icon: Building2 },
  { href: '/admin/whatsapp',     label: 'WhatsApp',     Icon: MessageCircle },
  { href: '/admin/whatsapp-templates', label: 'WA Templates', Icon: MessageCircle },
  { href: '/admin/admins',       label: 'Admins',       Icon: Users },
  { href: '/admin/events',       label: 'Event Log',    Icon: ScrollText },
] as const
```
Replace the two lines `{ href: '/admin/whatsapp', ... }` and `{ href: '/admin/whatsapp-templates', ... }` with exactly ONE entry:
```ts
{ href: '/admin/inbox', label: 'Inbox', Icon: Inbox },
```
Add `Inbox` to the lucide-react import on line 6. `MessageCircle` is used ONLY by these two removed entries — after the edit, `MessageCircle` becomes unused; remove it from the import line (verify via grep before removing — if any other line in the file still uses `MessageCircle`, keep it).
`isActive` logic (lines 52-57) needs NO changes — `activeBase` defaults to `href` when absent, so `/admin/inbox/settings` auto-highlights the new `Inbox` entry.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Collapse nav to one Inbox item and create redirect stub for /admin/whatsapp</name>
  <read_first>components/admin/admin-nav.tsx, app/admin/whatsapp/page.tsx</read_first>
  <files>components/admin/admin-nav.tsx, app/admin/whatsapp/page.tsx</files>
  <action>
1. In `components/admin/admin-nav.tsx`: add `Inbox` to the lucide-react import on line 6 (alongside `Settings2, Palette, ...`). Replace the two NAV_ITEMS entries `{ href: '/admin/whatsapp', label: 'WhatsApp', Icon: MessageCircle }` and `{ href: '/admin/whatsapp-templates', label: 'WA Templates', Icon: MessageCircle }` with exactly one entry `{ href: '/admin/inbox', label: 'Inbox', Icon: Inbox }` in the same array position. Check whether `MessageCircle` is still referenced anywhere else in the file (grep the file for `MessageCircle`) — if the only two usages were these nav entries, remove `MessageCircle` from the lucide-react import line too. Do not add an `activeBase` field to the new entry.
2. Replace the ENTIRE content of `app/admin/whatsapp/page.tsx` (currently 208 lines) with a minimal redirect stub:
```tsx
import { redirect } from 'next/navigation'

export default function AdminWhatsAppRedirect() {
  redirect('/admin/inbox')
}
```
No `requireAdmin()` in this stub — the target page re-checks it. This file keeps its original path (it is NOT moved) so the old bookmark URL still resolves and immediately redirects.
  </action>
  <verify>
    <automated>grep -n "'/admin/inbox'" components/admin/admin-nav.tsx && grep -n "redirect('/admin/inbox')" app/admin/whatsapp/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Inbox" components/admin/admin-nav.tsx` shows both the lucide import and one NAV_ITEMS entry with `href: '/admin/inbox'`
    - `grep -c "whatsapp" components/admin/admin-nav.tsx` (case-insensitive) returns 0 matches for `/admin/whatsapp` or `/admin/whatsapp-templates` route strings (run `grep -in "/admin/whatsapp" components/admin/admin-nav.tsx` and confirm zero output)
    - `grep -n "MessageCircle" components/admin/admin-nav.tsx` returns no output (import fully removed) OR still shows a legitimate remaining usage elsewhere in the file
    - `app/admin/whatsapp/page.tsx` is exactly 6 non-empty lines and contains `redirect('/admin/inbox')`
  </acceptance_criteria>
  <done>Nav shows a single Inbox entry pointing at /admin/inbox; the old /admin/whatsapp/page.tsx is a redirect stub only.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Relocate admin-whatsapp-client.tsx and loading.tsx verbatim; relocate + retarget admin-whatsapp-filters.tsx</name>
  <read_first>app/admin/whatsapp/admin-whatsapp-client.tsx, app/admin/whatsapp/loading.tsx, app/admin/whatsapp/admin-whatsapp-filters.tsx</read_first>
  <files>app/admin/inbox/admin-whatsapp-client.tsx, app/admin/inbox/loading.tsx, app/admin/inbox/admin-whatsapp-filters.tsx</files>
  <action>
1. Create `app/admin/inbox/admin-whatsapp-client.tsx` as a byte-for-byte copy of the current `app/admin/whatsapp/admin-whatsapp-client.tsx` (167 lines) — same `'use client'` directive, same `AdminWhatsAppClient` export, same `Row` type, same `Sheet`/`MessageBubble`/`loadAdminConversationThread` usage. Zero text changes (Phase 155 edits this file later, in this new location).
2. Create `app/admin/inbox/loading.tsx` as a byte-for-byte copy of the current `app/admin/whatsapp/loading.tsx` (41 lines) — no path literals inside, so purely a relocation.
3. Create `app/admin/inbox/admin-whatsapp-filters.tsx` as a copy of the current `app/admin/whatsapp/admin-whatsapp-filters.tsx` (163 lines) with exactly these 4 literal-string edits (all `/admin/whatsapp` → `/admin/inbox`):
   - `pushParam()`: `` router.replace(`/admin/whatsapp?${params.toString()}`) `` → `` router.replace(`/admin/inbox?${params.toString()}`) ``
   - dateFrom `onChange`: `` router.replace(`/admin/whatsapp?${params.toString()}`) `` → `` router.replace(`/admin/inbox?${params.toString()}`) ``
   - dateTo `onChange`: `` router.replace(`/admin/whatsapp?${params.toString()}`) `` → `` router.replace(`/admin/inbox?${params.toString()}`) ``
   - "Clear filters" onClick: `router.replace('/admin/whatsapp')` → `router.replace('/admin/inbox')`
   Component name `AdminWhatsAppFilters` and the `AdminWhatsAppFiltersProps` interface stay unchanged.
4. Delete the three old files: `app/admin/whatsapp/admin-whatsapp-client.tsx`, `app/admin/whatsapp/loading.tsx`, `app/admin/whatsapp/admin-whatsapp-filters.tsx` (only `page.tsx` — now the redirect stub from Task 1 — remains at the old location; `admin-whatsapp-accounts.tsx` is handled by Plan 02).
  </action>
  <verify>
    <automated>test -f app/admin/inbox/admin-whatsapp-client.tsx && test -f app/admin/inbox/loading.tsx && test -f app/admin/inbox/admin-whatsapp-filters.tsx && test ! -f app/admin/whatsapp/admin-whatsapp-client.tsx && test ! -f app/admin/whatsapp/loading.tsx && test ! -f app/admin/whatsapp/admin-whatsapp-filters.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/inbox/admin-whatsapp-client.tsx` exists and contains `loadAdminConversationThread(row.id, row.company_id)` and `export function AdminWhatsAppClient`
    - `app/admin/inbox/loading.tsx` exists and contains `AdminShellSkeleton`
    - `app/admin/inbox/admin-whatsapp-filters.tsx` exists; `grep -c "/admin/inbox" app/admin/inbox/admin-whatsapp-filters.tsx` returns at least 4; `grep -c "/admin/whatsapp" app/admin/inbox/admin-whatsapp-filters.tsx` (excluding `/admin/inbox` substring false-positives — use `grep -n "/admin/whatsapp[^-]" ` or manually confirm) returns 0
    - `app/admin/whatsapp/admin-whatsapp-client.tsx`, `app/admin/whatsapp/loading.tsx`, `app/admin/whatsapp/admin-whatsapp-filters.tsx` no longer exist (`ls app/admin/whatsapp/` shows only `page.tsx` and `admin-whatsapp-accounts.tsx`)
  </acceptance_criteria>
  <done>Client table/Sheet and loading skeleton relocated verbatim; filters component relocated with all 4 path literals retargeted to /admin/inbox; old files removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Assemble the new conversations-only app/admin/inbox/page.tsx (split, not verbatim copy)</name>
  <read_first>app/admin/whatsapp/page.tsx (already deleted-in-place from Task 1 — read the interfaces block above for its pre-change content), .planning/phases/154-inbox-route-consolidation-settings/154-UI-SPEC.md</read_first>
  <files>app/admin/inbox/page.tsx</files>
  <action>
Create `app/admin/inbox/page.tsx` as a NEW server component assembled from the old `page.tsx`'s conversations-only logic (see `<interfaces>` block above for the exact old structure). Concretely:

1. Imports: `T` from `@/components/i18n/t`, `Link` from `next/link`, `Settings2` from `lucide-react`, `parseAdminWhatsAppFilters`/`listAdminWhatsAppConversations` from `@/lib/queries/admin-whatsapp`, `requireAdmin` from `@/lib/auth/admin-context`, `AdminWhatsAppClient` from `./admin-whatsapp-client`, `AdminWhatsAppFilters` from `./admin-whatsapp-filters`. Do NOT import `requireServiceClient` or `AdminWhatsAppAccounts` — this page no longer fetches accounts data.
2. `export const dynamic = 'force-dynamic'`.
3. Keep the `type Row = {...}` shape (9 fields: id, company_id, contact_phone, contact_name, last_message_at, last_message_preview, last_inbound_at, unread_count, company_name).
4. Async component: `await requireAdmin()` first line. Then `const sp = await searchParams; const filters = parseAdminWhatsAppFilters(sp)`. No `tab` variable.
5. `const convResult = await listAdminWhatsAppConversations(filters)` — single fetch, no `Promise.all`, no service-role queries.
6. `const rows: Row[] = convResult.rows.map(...)` — unchanged mapping logic from the old page.
7. `pageUrl(p: number)` helper: same filter-param logic as the old page (companyId, senderId, q, status, unreadOnly, dateFrom, dateTo, page) but returning `` `/admin/inbox?${params.toString()}` `` and with NO `tab` param handling at all.
8. No `tabUrl` helper — delete this concept entirely.
9. Render: `<div className="space-y-8">` wrapping:
   - A header row containing the `<h1><T>Inbox</T></h1>` + `<p className="text-muted-foreground"><T>Platform-managed conversations across every connected channel. Read-only conversation inspection.</T></p>` block, PLUS a "Settings" link/button to `/admin/inbox/settings` using the `Settings2` icon (16px) with the label "Settings" — this is the INBOX-03 gear affordance. Lay it out as a flex row (header text on the left, Settings link on the right) so it doesn't disrupt the existing page-header spacing convention (`space-y-2` for the title block, `space-y-8` for the page).
   - NO tab-switcher `<div>` (delete that whole block from the old page).
   - The conversations body, unconditionally (previously the `else` branch): `AdminWhatsAppFilters` with the same 7 props as before, the count paragraph (`convResult.total === 0 && !hasActiveFilters` ? "No WhatsApp conversations yet." : `${total} conversations · Page ${page} of ${pageCount}`), `<AdminWhatsAppClient conversations={rows} />`, and the Previous/Next pagination block using `pageUrl()` — all copied logic-for-logic from the old page's conversations branch, just de-nested from the ternary.
   - Keep `hasActiveFilters` computed the same way (companyId || senderId || q || status || unreadOnly || dateFrom || dateTo).
  </action>
  <verify>
    <automated>test "$(grep -c "AdminWhatsAppAccounts" app/admin/inbox/page.tsx)" = "0" && grep -n "Settings2" app/admin/inbox/page.tsx && grep -n "listAdminWhatsAppConversations" app/admin/inbox/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AdminWhatsAppAccounts" app/admin/inbox/page.tsx` returns 0 (no accounts import/usage)
    - `grep -c "requireServiceClient" app/admin/inbox/page.tsx` returns 0
    - `grep -n "tab ===" app/admin/inbox/page.tsx` returns no output (no accounts/conversations tab branching left)
    - `grep -c "/admin/inbox" app/admin/inbox/page.tsx` returns at least 2 (pageUrl + Settings link)
    - `grep -n "Settings2" app/admin/inbox/page.tsx` shows the icon imported and used
    - `grep -n "listAdminWhatsAppConversations" app/admin/inbox/page.tsx` confirms the conversations fetch is present
    - `grep -n "<T>Inbox</T>" app/admin/inbox/page.tsx` confirms the page title copy
  </acceptance_criteria>
  <done>app/admin/inbox/page.tsx renders only the conversations view (filters + table + pagination), has a Settings link to /admin/inbox/settings in its header, and contains zero Accounts-tab logic or service-role account queries.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/admin/whatsapp-filters.test.ts` — this file currently asserts against `app/admin/whatsapp/*` paths and will fail until Plan 03 updates it; that is expected at this point in the phase (Plan 03 is a separate wave). Do NOT edit test files in this plan — that is explicitly Plan 03's scope.

Manually confirm via file listing that `app/admin/whatsapp/` now contains only `page.tsx` (redirect stub) and `admin-whatsapp-accounts.tsx` (owned by Plan 02, untouched by this plan) after this plan's tasks complete.
</verification>

<success_criteria>
- `app/admin/inbox/page.tsx`, `admin-whatsapp-client.tsx`, `admin-whatsapp-filters.tsx`, `loading.tsx` all exist and are wired together (page.tsx imports both client + filters components correctly)
- `app/admin/whatsapp/page.tsx` is a 6-line redirect stub to `/admin/inbox`
- `components/admin/admin-nav.tsx` shows exactly one Inbox nav entry, zero WhatsApp/WA-Templates entries
- No file in `app/admin/inbox/` references `/admin/whatsapp` as a route string
</success_criteria>

<output>
After completion, create `.planning/phases/154-inbox-route-consolidation-settings/154-01-SUMMARY.md`
</output>
