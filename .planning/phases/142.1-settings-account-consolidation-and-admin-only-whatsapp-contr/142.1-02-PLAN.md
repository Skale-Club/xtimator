---
phase: 142.1-settings-account-consolidation-and-admin-only-whatsapp-contr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/settings/settings-nav.tsx
  - app/(app)/settings/(tabs)/account/page.tsx
  - app/(app)/settings/(tabs)/account/loading.tsx
  - app/(app)/settings/(tabs)/general/page.tsx
  - app/(app)/settings/(tabs)/general/loading.tsx
  - components/settings/profile-section.tsx
  - lib/actions/settings.ts
  - tests/unit/settings/account-consolidation.test.tsx
autonomous: true
requirements: [ACCT-01]
decisions: [D-01, D-02, D-03, D-18]
must_haves:
  truths:
    - "Tenant profile and credential controls live together under Account"
    - "General is absent from navigation and old URLs redirect to Account"
    - "The personal phone remains account data and is not a WhatsApp writer"
  artifacts:
    - path: "app/(app)/settings/(tabs)/account/page.tsx"
      provides: "Unified Account server page"
      contains: "ProfileSection"
    - path: "tests/unit/settings/account-consolidation.test.tsx"
      provides: "Navigation, redirect, composition, and writer-removal contract"
  key_links:
    - from: "app/(app)/settings/(tabs)/general/page.tsx"
      to: "/settings/account"
      via: "Next.js redirect"
      pattern: "redirect\\('/settings/account'\\)"
---

<objective>
Merge General into Account and remove the tenant WhatsApp profile writer.

Purpose: Simplify settings information architecture while closing the most direct tenant-controlled routing mutation.
Output: Unified Account page, compatibility redirect, updated skeleton/nav/actions, and regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-CONTEXT.md
@app/(app)/settings/(tabs)/general/page.tsx
@app/(app)/settings/(tabs)/account/page.tsx
@components/settings/profile-section.tsx
@components/settings/account-section.tsx
@lib/actions/settings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose profile and security under the canonical Account route</name>
  <files>app/(app)/settings/(tabs)/account/page.tsx, components/settings/profile-section.tsx, components/settings/settings-nav.tsx, lib/actions/settings.ts</files>
  <read_first>app/(app)/settings/(tabs)/general/page.tsx, app/(app)/settings/(tabs)/account/page.tsx, components/settings/profile-section.tsx, components/settings/account-section.tsx, components/settings/settings-nav.tsx, lib/actions/settings.ts</read_first>
  <action>Move the authenticated user/profile metadata loading from General into Account and render `ProfileSection` followed by `AccountSection` under one Account header. Keep only `fullName`, personal `phone`, `avatarUrl`, and `email` in the profile projection. Remove `whatsappPhone`, WhatsApp state/input/save button, and the `saveWhatsAppNumber` import/export. `updateProfile` remains responsible only for auth metadata/avatar and revalidates `/settings/account`.

Remove the General `SubNavItem`; keep Account with `ShieldCheck`, and place Account where product navigation remains coherent without changing `/settings` → Company. Use separate Profile and Security cards/sections if that best matches existing components; do not reintroduce Danger Zone.</action>
  <verify>npx vitest run tests/unit/settings/account-consolidation.test.tsx && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - settings-nav.tsx contains no `value: 'general'`
    - account/page.tsx renders both `ProfileSection` and `AccountSection`
    - profile-section.tsx contains no `whatsappPhone`, `saveWhatsAppNumber`, or `WhatsApp Number`
    - settings.ts contains no exported `saveWhatsAppNumber`
    - updateProfile revalidates `/settings/account`
  </acceptance_criteria>
  <done>Account is the single working profile/security surface with no WhatsApp routing writer.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect General and align loading/test contracts</name>
  <files>app/(app)/settings/(tabs)/general/page.tsx, app/(app)/settings/(tabs)/general/loading.tsx, app/(app)/settings/(tabs)/account/loading.tsx, tests/unit/settings/account-consolidation.test.tsx</files>
  <read_first>app/(app)/settings/(tabs)/general/loading.tsx, app/(app)/settings/(tabs)/account/loading.tsx, components/skeletons/settings-page-skeleton.tsx, tests/e2e/visual/settings.spec.ts</read_first>
  <action>Replace the General page body with an unconditional server `redirect('/settings/account')`. Remove the obsolete General loading skeleton (delete it if Next routing no longer needs it, otherwise make it a redirect-compatible empty loading boundary). Expand Account loading to represent profile photo/name/personal phone plus password/email sections and ensure no WhatsApp copy remains.

Create a unit/static contract test proving the nav removal, redirect, unified composition, Account revalidation, and absence of tenant WhatsApp profile symbols. Do not update visual baselines in this plan; execution may update the visual spec only if it asserts route names rather than screenshots.</action>
  <verify>npx vitest run tests/unit/settings/account-consolidation.test.tsx</verify>
  <acceptance_criteria>
    - general/page.tsx contains `redirect('/settings/account')`
    - account/loading.tsx represents both profile and security controls
    - No loading copy claims the personal phone enables WhatsApp
    - Contract test fails if General returns to nav or a tenant WhatsApp profile writer returns
  </acceptance_criteria>
  <done>Old links are safe, loading states match Account, and the consolidation is regression-guarded.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/settings/account-consolidation.test.tsx`
- [ ] `npx tsc --noEmit`
- [ ] `rg "saveWhatsAppNumber|whatsappPhone" components/settings app/'(app)'/settings lib/actions/settings.ts` returns no tenant profile writer
</verification>

<success_criteria>
- Account contains all personal profile and credential controls.
- General is removed from navigation and redirects.
- Profile phone cannot modify WhatsApp routing.
</success_criteria>

<output>
After completion, create `.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-02-SUMMARY.md`
</output>
