---
phase: 142.1-settings-account-consolidation-and-admin-only-whatsapp-contr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260627000001_whatsapp_admin_registry_expand.sql
  - lib/whatsapp/account-registry.ts
  - types/database.types.ts
  - tests/unit/whatsapp/admin-registry-migration.test.ts
  - tests/unit/whatsapp/account-registry.test.ts
autonomous: true
requirements: [WAADM-03, WAADM-04]
decisions: [D-11, D-12, D-18, D-19]
must_haves:
  truths:
    - "Company-level WhatsApp configuration and authorized sender identities are separate records"
    - "Existing valid routing numbers are copied without deleting the legacy source rows"
    - "Companies with multiple distinct current routing numbers are marked for admin review"
  artifacts:
    - path: "supabase/migrations/20260627000001_whatsapp_admin_registry_expand.sql"
      provides: "Idempotent expand and data migration"
      contains: "whatsapp_authorized_senders"
    - path: "lib/whatsapp/account-registry.ts"
      provides: "Single server-only read boundary for account status and authorized senders"
      exports: ["getWhatsAppAccountStatus", "findActiveWhatsAppSender"]
  key_links:
    - from: "lib/whatsapp/account-registry.ts"
      to: "whatsapp_authorized_senders"
      via: "service-role exact E.164 sender lookup"
      pattern: "from\\('whatsapp_authorized_senders'\\)"
---

<objective>
Create the normalized, admin-owned WhatsApp registry and its single server-only read boundary.

Purpose: Establish the safe expand half of the migration before tenant writers, webhook fallbacks, or legacy tables are removed.
Output: Authored migration, generated database types, registry helpers, and migration/behavior tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-CONTEXT.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-RESEARCH.md
@supabase/migrations/20260620000001_company_whatsapp_multi_user.sql
@types/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author normalized registry expand migration and data classification</name>
  <files>supabase/migrations/20260627000001_whatsapp_admin_registry_expand.sql, tests/unit/whatsapp/admin-registry-migration.test.ts</files>
  <read_first>supabase/migrations/20260510000002_phase40_whatsapp.sql, supabase/migrations/20260602000002_company_whatsapp_owner_phone.sql, supabase/migrations/20260620000001_company_whatsapp_multi_user.sql, types/database.types.ts</read_first>
  <action>Create an authored-only, idempotent migration with two deny-all-RLS tables. `whatsapp_company_configs` has `id`, unique `company_id`, `status` (`pending_review|active|suspended`), `delivery_format` (`share_link|formatted_text|pdf_attachment`), nullable `review_reason`, and timestamps. `whatsapp_authorized_senders` has `id`, `config_id`, `company_id`, nullable `user_id`, normalized `phone_e164`, `status` (`pending_review|active|suspended`), nullable `verified_at`, nullable `created_by_admin`, nullable `source_row_id`, and timestamps. Add FK constraints, an active global-phone partial unique index, company/status lookup index, and deny-all RLS with no tenant policies.

Backfill one config per distinct legacy `company_whatsapp.company_id`. Copy non-null `owner_phone` rows into senders with `source_row_id`. A company with exactly one distinct phone may preserve `active`; a company with multiple distinct phones must set its config and all copied senders to `pending_review` with an explicit review reason. Never use “first member wins”, never delete/update legacy rows, and make replay safe with `ON CONFLICT`. Add a static SQL contract test proving table shape, RLS, indexes, classification logic, legacy preservation, and absence of remote-apply commands.</action>
  <verify>npx vitest run tests/unit/whatsapp/admin-registry-migration.test.ts</verify>
  <acceptance_criteria>
    - Migration contains `CREATE TABLE IF NOT EXISTS whatsapp_company_configs`
    - Migration contains `CREATE TABLE IF NOT EXISTS whatsapp_authorized_senders`
    - Migration enables RLS on both tables and creates no authenticated tenant policy
    - Migration contains an active-phone partial unique index
    - Migration classifies multi-number companies as `pending_review`
    - Migration does not drop or truncate `company_whatsapp`
  </acceptance_criteria>
  <done>The expand schema and deterministic, non-destructive backfill are authored and locked by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add the server-only WhatsApp account registry read boundary</name>
  <files>lib/whatsapp/account-registry.ts, types/database.types.ts, tests/unit/whatsapp/account-registry.test.ts</files>
  <read_first>lib/supabase/service.ts, lib/notifications/owner-phone.ts, lib/whatsapp/sync-owner-phone.ts, types/database.types.ts</read_first>
  <action>Add `import 'server-only'` module `lib/whatsapp/account-registry.ts`. Export typed `getWhatsAppAccountStatus(companyId)` returning only `{configured, active, status, deliveryFormat}` and `findActiveWhatsAppSender(phoneE164)` returning an exact-one `{companyId, userId, phoneE164}` match or null. Normalize through one existing-compatible E.164 helper; if no suitable exported helper exists, export `normalizeWhatsAppPhone` from this module and use it everywhere later.

The sender resolver must query `status='active'`, cap at 2 rows, return null and log last-four-only on zero or ambiguity, and never expose the service client to callers. Add generated/manual database types for the two tables. Unit-test active, missing, suspended, and ambiguous matches plus the opaque company-status projection.</action>
  <verify>npx vitest run tests/unit/whatsapp/account-registry.test.ts && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - account-registry.ts contains `import 'server-only'`
    - `findActiveWhatsAppSender` never uses `.maybeSingle()` for an ambiguity-capable lookup
    - Logs do not include the full sender phone
    - Account status returns no linked number, WABA ID, token, or conversation data
  </acceptance_criteria>
  <done>All later admin, webhook, and outbound gates can consume one consistent registry abstraction.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/whatsapp/admin-registry-migration.test.ts tests/unit/whatsapp/account-registry.test.ts`
- [ ] `npx tsc --noEmit`
- [ ] Migration is authored only and legacy rows remain intact
</verification>

<success_criteria>
- Normalized config/sender registry exists with deny-all RLS.
- Valid existing data migrates deterministically and conflicts become reviewable.
- One server-only helper owns account-active and sender-resolution reads.
</success_criteria>

<output>
After completion, create `.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-01-SUMMARY.md`
</output>
