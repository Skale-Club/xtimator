---
phase: 142.1-settings-account-consolidation-and-admin-only-whatsapp-contr
plan: 05
type: execute
wave: 3
depends_on: ["142.1-01", "142.1-04"]
files_modified:
  - lib/actions/admin-whatsapp-accounts.ts
  - app/admin/whatsapp/admin-whatsapp-accounts.tsx
  - app/admin/whatsapp/page.tsx
  - lib/admin/audit-log.ts
  - tests/unit/admin/whatsapp-account-actions.test.ts
  - tests/e2e/admin-whatsapp.spec.ts
autonomous: true
requirements: [WAADM-02, WAADM-03]
decisions: [D-08, D-09, D-10, D-18]
must_haves:
  truths:
    - "Only a super-admin can provision or change WhatsApp account/sender state"
    - "Active numbers are valid E.164 and globally unique"
    - "Every provisioning mutation produces a durable admin audit event"
  artifacts:
    - path: "lib/actions/admin-whatsapp-accounts.ts"
      provides: "Admin-only provision, status, delivery-format, and removal actions"
      exports: ["saveWhatsAppAccount", "saveWhatsAppSender", "setWhatsAppSenderStatus", "removeWhatsAppSender"]
    - path: "app/admin/whatsapp/admin-whatsapp-accounts.tsx"
      provides: "Account provisioning UI inside the canonical admin surface"
  key_links:
    - from: "lib/actions/admin-whatsapp-accounts.ts"
      to: "lib/admin/audit-log.ts"
      via: "logAdminAction for each mutation"
      pattern: "logAdminAction"
---

<objective>
Add audited super-admin provisioning and status management to the WhatsApp admin surface.

Purpose: Replace removed tenant writers with one explicit platform authority path.
Output: Admin actions, provisioning UI, audit events, and authorization/validation tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-CONTEXT.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-01-SUMMARY.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-04-SUMMARY.md
@app/admin/companies/actions.ts
@lib/admin/audit-log.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement admin-only provisioning actions and audit contracts</name>
  <files>lib/actions/admin-whatsapp-accounts.ts, lib/admin/audit-log.ts, tests/unit/admin/whatsapp-account-actions.test.ts</files>
  <read_first>app/admin/companies/actions.ts, lib/actions/admin-whatsapp-templates.ts, lib/admin/audit-log.ts, lib/whatsapp/account-registry.ts, types/database.types.ts</read_first>
  <action>Create Zod-validated server actions. Every export calls `requireAdmin()` before service-client access. `saveWhatsAppAccount` validates company existence and updates config status/delivery format. `saveWhatsAppSender` validates company membership when `userId` is supplied, normalizes E.164, rejects an existing active number on another company, and creates/updates pending or active sender records. `setWhatsAppSenderStatus` permits only defined transitions and requires a verified timestamp before activation. `removeWhatsAppSender` performs a reversible suspension/soft removal rather than erasing conversation history.

Every successful mutation calls `logAdminAction` with actor, target company/sender, action names under `whatsapp.account.*` or `whatsapp.sender.*`, and safe previous/next projections. Audit metadata may include last four digits but never a full phone, token, or Meta credential. Return typed, user-safe conflicts. Tests must prove unauthenticated/non-admin denial occurs before writes, E.164/uniqueness/transition guards, company/member scope, and one audit call per success.</action>
  <verify>npx vitest run tests/unit/admin/whatsapp-account-actions.test.ts</verify>
  <acceptance_criteria>
    - Every exported mutation contains or reaches `requireAdmin()`
    - No action trusts client-supplied company/member labels or roles
    - Activation requires valid E.164, uniqueness, and verification
    - Audit metadata contains no full phone number
    - Removal preserves historical conversations
  </acceptance_criteria>
  <done>One validated, audited admin boundary owns every WhatsApp provisioning mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Add account provisioning controls to the filtered admin surface</name>
  <files>app/admin/whatsapp/admin-whatsapp-accounts.tsx, app/admin/whatsapp/page.tsx, tests/e2e/admin-whatsapp.spec.ts</files>
  <read_first>app/admin/whatsapp/page.tsx, app/admin/whatsapp/admin-whatsapp-filters.tsx, app/admin/companies/[id]/company-model-override-form.tsx, components/ui/tabs.tsx, components/ui/dialog.tsx</read_first>
  <action>Add Accounts/Provisioning and Conversations views within `/admin/whatsapp` (tabs or equivalent compact composition). Provisioning lists selected company config and senders, status/review reason, masked phone, linked member, verification state, delivery format, last activity, and actions to add/edit/activate/suspend/remove. Reuse the account filter so selecting a tenant scopes both views. Do not render Meta access tokens, WABA secrets, or full unmasked phone in list tables; reveal/edit the full number only inside the privileged form.

Use server actions with pending/toast/error states and refresh after success. Add E2E cases for adding a sender, duplicate conflict, activation/suspension, account isolation, and audit-visible success behavior where test fixtures allow.</action>
  <verify>npx vitest run tests/unit/admin/whatsapp-account-actions.test.ts && npx playwright test tests/e2e/admin-whatsapp.spec.ts</verify>
  <acceptance_criteria>
    - Provisioning and conversations share the selected company/account context
    - List rows mask phone values
    - All mutations call the dedicated admin actions
    - Pending-review conflicts are visibly actionable
    - UI has no reply/message-send control
  </acceptance_criteria>
  <done>Super-admins can fully manage account routing without tenant participation.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/admin/whatsapp-account-actions.test.ts`
- [ ] `npx playwright test tests/e2e/admin-whatsapp.spec.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>
- Provisioning mutations are admin-only, validated, unique, and audited.
- Admin can resolve migration review cases by account.
- Conversation history stays read-only.
</success_criteria>

<output>
After completion, create `.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-05-SUMMARY.md`
</output>
