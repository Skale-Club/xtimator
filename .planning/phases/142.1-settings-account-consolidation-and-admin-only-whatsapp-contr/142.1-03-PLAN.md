---
phase: 142.1-settings-account-consolidation-and-admin-only-whatsapp-contr
plan: 03
type: execute
wave: 2
depends_on: ["142.1-01", "142.1-02"]
files_modified:
  - app/(app)/whatsapp/page.tsx
  - app/(app)/whatsapp/layout.tsx
  - app/(app)/whatsapp/loading.tsx
  - components/whatsapp/whatsapp-inbox.tsx
  - lib/actions/whatsapp-inbox.ts
  - lib/queries/whatsapp-inbox.ts
  - components/workspace/project-whatsapp-card.tsx
  - components/workspace/project-workspace.tsx
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/settings/integrations/page.tsx
  - components/settings/notifications-form.tsx
  - app/(app)/settings/(tabs)/notifications/page.tsx
  - app/api/notifications/preferences/route.ts
  - lib/notifications/preferences.ts
  - tests/unit/settings/tenant-whatsapp-surface.test.ts
autonomous: true
requirements: [WAADM-01, WAADM-05]
decisions: [D-04, D-05, D-06, D-07, D-15, D-16, D-17, D-18]
must_haves:
  truths:
    - "No tenant route or project surface exposes WhatsApp conversations or provisioning"
    - "Direct legacy tenant WhatsApp access returns no protected content"
    - "WhatsApp notification preferences cannot be enabled while historical consent data remains stored"
  artifacts:
    - path: "tests/unit/settings/tenant-whatsapp-surface.test.ts"
      provides: "Static deny-list for tenant WhatsApp visibility and writers"
  key_links:
    - from: "components/workspace/project-workspace.tsx"
      to: "ClientTab"
      via: "Client tab renders without conversation preview/deep-link"
      pattern: "ClientTab"
---

<objective>
Remove every tenant WhatsApp visibility surface and disable proactive notification configuration.

Purpose: Enforce the product boundary at route/action/query level, not merely in navigation.
Output: Retired tenant inbox/history paths, cleaned project/settings surfaces, disabled proactive channel, and structural guards.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-CONTEXT.md
@app/(app)/whatsapp/page.tsx
@components/whatsapp/whatsapp-inbox.tsx
@components/workspace/project-whatsapp-card.tsx
@components/settings/notifications-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire tenant inbox, conversation actions, and project previews</name>
  <files>app/(app)/whatsapp/page.tsx, app/(app)/whatsapp/layout.tsx, app/(app)/whatsapp/loading.tsx, components/whatsapp/whatsapp-inbox.tsx, lib/actions/whatsapp-inbox.ts, lib/queries/whatsapp-inbox.ts, components/workspace/project-whatsapp-card.tsx, components/workspace/project-workspace.tsx, app/(app)/projects/[id]/page.tsx</files>
  <read_first>app/(app)/whatsapp/page.tsx, components/whatsapp/whatsapp-inbox.tsx, lib/actions/whatsapp-inbox.ts, lib/queries/whatsapp-inbox.ts, components/workspace/project-whatsapp-card.tsx, components/workspace/project-workspace.tsx, app/(app)/projects/[id]/page.tsx, lib/actions/admin-whatsapp.ts</read_first>
  <action>Delete the tenant inbox component and tenant-only reply/read/send-from-conversation actions. Remove `/whatsapp` page/layout/loading or replace the page with `notFound()` if a route tombstone is needed; it must never redirect into admin or disclose existence. Preserve shared `MessageBubble` because admin uses it. Remove `ProjectWhatsAppCard`, `getProjectConversationLink`, its server query, props, and Client-tab preview/deep-link wiring. Do not remove the project Send tab or `/api/estimates/[id]/send-whatsapp`; those are the opaque outbound path retained by D-06.</action>
  <verify>npx vitest run tests/unit/settings/tenant-whatsapp-surface.test.ts && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - No tenant component links to `/whatsapp`
    - Project pages do not query or serialize conversation previews
    - Tenant reply, mark-read, list-thread, and conversation-send actions are deleted or unreachable
    - Admin thread loading and MessageBubble remain intact
    - Estimate Send tab implementation remains present
  </acceptance_criteria>
  <done>Tenant users cannot retrieve or infer WhatsApp conversation content from app surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Remove tenant integration/notification controls and add structural denial tests</name>
  <files>app/(app)/settings/integrations/page.tsx, components/settings/notifications-form.tsx, app/(app)/settings/(tabs)/notifications/page.tsx, app/api/notifications/preferences/route.ts, lib/notifications/preferences.ts, tests/unit/settings/tenant-whatsapp-surface.test.ts</files>
  <read_first>app/(app)/settings/integrations/page.tsx, components/settings/notifications-form.tsx, app/(app)/settings/(tabs)/notifications/page.tsx, app/api/notifications/preferences/route.ts, lib/notifications/preferences.ts, lib/notifications/dispatch.ts</read_first>
  <action>Remove the tenant WhatsApp card/section from Integrations. Remove the WhatsApp column/toggles, phone resolver prop, consent state, and WhatsApp copy from Notifications while leaving In-app, Email, SMS, push, and SMS consent intact. The preferences API must ignore/reject new tenant WhatsApp preference writes but must not null or delete existing `whatsapp_opt_in_at` or historical category data. In channel resolution, force tenant-configured WhatsApp to false so old preferences cannot continue proactive sends; Plan 06 will also guard dispatch.

Add a static contract scanning `app/(app)`, tenant components, and tenant actions for banned conversation/config imports and `/whatsapp` links while allowlisting the opaque estimate send route/form and plain-text copy references.</action>
  <verify>npx vitest run tests/unit/settings/tenant-whatsapp-surface.test.ts tests/unit/notifications</verify>
  <acceptance_criteria>
    - Settings Integrations renders no WhatsApp card
    - NotificationsForm has no WhatsApp column or toggle
    - PATCH preferences does not accept a new `whatsapp_opt_in_at`
    - Existing stored consent/history is not deleted by reads or writes
    - `resolveChannels` cannot return `whatsapp: true` from tenant preferences
  </acceptance_criteria>
  <done>Tenant settings expose no WhatsApp controls and proactive dispatch is safely dormant without destroying history.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/settings/tenant-whatsapp-surface.test.ts tests/unit/notifications`
- [ ] `npx tsc --noEmit`
- [ ] Estimate WhatsApp send form/route still exist
- [ ] Admin conversation components still compile
</verification>

<success_criteria>
- No tenant inbox, history, preview, status, provisioning, or notification controls remain.
- Direct tenant URL access returns no protected content.
- Opaque estimate delivery remains available for later registry gating.
</success_criteria>

<output>
After completion, create `.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-03-SUMMARY.md`
</output>
