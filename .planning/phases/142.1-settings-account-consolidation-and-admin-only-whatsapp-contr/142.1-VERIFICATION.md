---
phase: 142.1-settings-account-consolidation-and-admin-only-whatsapp-contr
verified: 2026-06-27T18:35:00Z
status: passed
score: 16/16 must-haves verified
re_verification: false
human_verification:
  - test: "Navigate to /settings/general in browser"
    expected: "Server-side redirect to /settings/account — no General UI visible"
    why_human: "Visual redirect behavior requires browser navigation"
  - test: "Navigate to /whatsapp as tenant user"
    expected: "404 page — no content disclosure, no redirect"
    why_human: "notFound() tombstone rendering is visual"
  - test: "Open admin WhatsApp page, switch between Conversations and Accounts tabs"
    expected: "Both tabs render with proper filtering, pagination, and provisioning controls"
    why_human: "Admin UI rendering with filter state and tab switching"
  - test: "As tenant, verify Settings nav shows no 'General' entry and 'Account' renders both profile and security"
    expected: "Account page shows ProfileSection (photo/name/phone) + AccountSection (password/email)"
    why_human: "Visual settings consolidation verification"
---

# Phase 142.1: Settings Account Consolidation & Admin-Only WhatsApp Control Verification Report

**Phase Goal:** Merge General into Account, remove tenant WhatsApp surfaces, establish admin-only WhatsApp provisioning and routing authority.
**Verified:** 2026-06-27T18:35:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Company-level WhatsApp configuration and authorized sender identities are separate records | ✓ VERIFIED | `supabase/migrations/20260627000001_whatsapp_admin_registry_expand.sql` creates `whatsapp_company_configs` and `whatsapp_authorized_senders` as separate tables with 1:N FK |
| 2 | Existing valid routing numbers are copied without deleting the legacy source rows | ✓ VERIFIED | Expand migration preserves all legacy `company_whatsapp` rows; contract migration renames to `_legacy_company_whatsapp` with FORCE RLS — no drops during expand phase |
| 3 | Companies with multiple distinct current routing numbers are marked for admin review | ✓ VERIFIED | Expand migration backfill classifies multi-phone companies as `pending_review` with `review_reason` |
| 4 | Tenant profile and credential controls live together under Account | ✓ VERIFIED | `app/(app)/settings/(tabs)/account/page.tsx` renders `ProfileSection` + `AccountSection` together |
| 5 | General is absent from navigation and old URLs redirect to Account | ✓ VERIFIED | `settings-nav.tsx` has no `value: 'general'`; `general/page.tsx` contains `redirect('/settings/account')` |
| 6 | The personal phone remains account data and is not a WhatsApp writer | ✓ VERIFIED | `profile-section.tsx` has no `whatsappPhone`, `saveWhatsAppNumber`, or WhatsApp UI; `settings.ts` has no exported `saveWhatsAppNumber` |
| 7 | No tenant route or project surface exposes WhatsApp conversations or provisioning | ✓ VERIFIED | `whatsapp/page.tsx` returns `notFound()`; inbox component, actions, queries, project card all deleted; no `whatsapp` links in tenant surfaces |
| 8 | Direct legacy tenant WhatsApp access returns no protected content | ✓ VERIFIED | `/whatsapp` uses `notFound()` tombstone — no redirect, no content disclosure |
| 9 | WhatsApp notification preferences cannot be enabled while historical consent data remains stored | ✓ VERIFIED | `resolveChannels` forces `whatsapp = false` (D-15); PATCH schema ignores `whatsapp_opt_in_at`; GET preserves stored value |
| 10 | Super-admin can filter conversations by account and operational dimensions before pagination | ✓ VERIFIED | `lib/queries/admin-whatsapp.ts` exports `parseAdminWhatsAppFilters` and `listAdminWhatsAppConversations` with Zod-validated filters |
| 11 | Filters survive refresh and pagination through URL search parameters | ✓ VERIFIED | `admin-whatsapp-filters.tsx` uses URL search params; page reset on filter change; Prev/Next preserve filters |
| 12 | Conversation thread inspection remains admin-only and read-only | ✓ VERIFIED | `lib/actions/admin-whatsapp.ts` accepts `expectedCompanyId`, verifies under `requireAdmin()`, no update/insert/delete calls |
| 13 | Only a super-admin can provision or change WhatsApp account/sender state | ✓ VERIFIED | All 4 actions in `admin-whatsapp-accounts.ts` call `requireAdmin()` before any service-client access |
| 14 | Active numbers are valid E.164 and globally unique | ✓ VERIFIED | `normalizeWhatsAppPhone` validates E.164; `saveWhatsAppSender` checks active-phone uniqueness across companies |
| 15 | Every provisioning mutation produces a durable admin audit event | ✓ VERIFIED | All 4 actions call `logAdminAction` with safe metadata (last-4 phone digits only) |
| 16 | Inbound owner commands route only through one active admin-provisioned sender | ✓ VERIFIED | Webhook route calls `findActiveWhatsAppSender`; no `companies.phone` fallback; ambiguity fails closed |
| 17 | Onboarding, profile, and company phone changes cannot alter WhatsApp routing | ✓ VERIFIED | `syncOwnerPhone` deleted; no callers in app/ or lib/actions/ (authority contract test passes) |
| 18 | Opaque tenant estimate sending is enabled only by active account configuration | ✓ VERIFIED | Project page and send-whatsapp route use `getWhatsAppAccountStatus`; browser receives only enabled boolean |
| 19 | Legacy per-user routing schema and direct readers are removed after migration | ✓ VERIFIED | Contract migration renames to `_legacy_company_whatsapp` with FORCE RLS; zero `from('company_whatsapp')` in runtime code; 6 authority-contract guards pass |

**Score:** 19/19 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260627000001_whatsapp_admin_registry_expand.sql` | Deny-all RLS tables + backfill | ✓ VERIFIED | 120 lines; creates both tables, indexes, backfill, classification logic |
| `lib/whatsapp/account-registry.ts` | Server-only read boundary | ✓ VERIFIED | 160 lines; `import 'server-only'`; exports `getWhatsAppAccountStatus`, `findActiveWhatsAppSender`, `normalizeWhatsAppPhone` |
| `app/(app)/settings/(tabs)/account/page.tsx` | Unified Account page | ✓ VERIFIED | 38 lines; renders `ProfileSection` + `AccountSection` |
| `app/(app)/settings/(tabs)/general/page.tsx` | Redirect to Account | ✓ VERIFIED | 5 lines; `redirect('/settings/account')` |
| `components/settings/settings-nav.tsx` | No General entry | ✓ VERIFIED | 35 lines; no `value: 'general'` |
| `components/settings/profile-section.tsx` | No WhatsApp writer | ✓ VERIFIED | 138 lines; no `whatsappPhone`, `saveWhatsAppNumber`, or WhatsApp UI |
| `lib/actions/settings.ts` | No exported saveWhatsAppNumber | ✓ VERIFIED | Only appears in doc-comments (architectural notes), not as export |
| `tests/unit/settings/tenant-whatsapp-surface.test.ts` | Static deny-list | ✓ VERIFIED | Exists; 5 structural denial tests |
| `lib/queries/admin-whatsapp.ts` | Server-only query module | ✓ VERIFIED | 193 lines; `import 'server-only'`; Zod-parsed filters; bounded pagination |
| `app/admin/whatsapp/admin-whatsapp-filters.tsx` | URL-backed filter controls | ✓ VERIFIED | Exists; search, status, unread, date range |
| `lib/actions/admin-whatsapp-accounts.ts` | Admin provisioning actions | ✓ VERIFIED | 528 lines; 4 actions with `requireAdmin()`, audit, transition guards |
| `app/admin/whatsapp/admin-whatsapp-accounts.tsx` | Account provisioning UI | ✓ VERIFIED | Exists; Conversations/Accounts tabs, masked phones, add-sender dialog |
| `lib/admin/audit-log.ts` | WhatsApp audit types | ✓ VERIFIED | Modified to include 4 WhatsApp action types |
| `supabase/migrations/20260627000002_whatsapp_admin_registry_contract.sql` | Legacy schema contraction | ✓ VERIFIED | 88 lines; guard → backfill → revoke DML → rename → FORCE RLS |
| `tests/unit/whatsapp/admin-authority-contract.test.ts` | Repository-wide guards | ✓ VERIFIED | 174 lines; 6 static source-file scans |
| `tests/unit/whatsapp/entitlement-gate.test.ts` | Active/pending/suspended/absent gates | ✓ VERIFIED | Exists; 8 GREEN tests replacing RED stubs |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `lib/whatsapp/account-registry.ts` | `whatsapp_authorized_senders` | service-role exact E.164 lookup | ✓ WIRED | `.from('whatsapp_authorized_senders').eq('phone_e164', normalized).eq('status', 'active')` |
| `app/(app)/settings/(tabs)/general/page.tsx` | `/settings/account` | Next.js redirect | ✓ WIRED | `redirect('/settings/account')` |
| `components/workspace/project-workspace.tsx` | ClientTab | No conversation preview | ✓ WIRED | `ProjectWhatsAppCard` deleted; no deep-link wiring |
| `app/admin/whatsapp/page.tsx` | `lib/queries/admin-whatsapp.ts` | validated searchParams | ✓ WIRED | Calls `parseAdminWhatsAppFilters` + `listAdminWhatsAppConversations` |
| `lib/actions/admin-whatsapp-accounts.ts` | `lib/admin/audit-log.ts` | `logAdminAction` for each mutation | ✓ WIRED | 6 `logAdminAction` calls across 4 actions |
| `app/api/webhooks/whatsapp/route.ts` | `lib/whatsapp/account-registry.ts` | active sender resolution | ✓ WIRED | Imports and calls `findActiveWhatsAppSender` |
| `app/api/estimates/[id]/send-whatsapp/route.ts` | `lib/whatsapp/account-registry.ts` | opaque account-active gate | ✓ WIRED | Imports and calls `getWhatsAppAccountStatus` |
| `app/(app)/projects/[id]/page.tsx` | `lib/whatsapp/account-registry.ts` | send gate | ✓ WIRED | Imports and calls `getWhatsAppAccountStatus` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `account-registry.ts` → `getWhatsAppAccountStatus` | `data` from `whatsapp_company_configs` | Supabase service-role query | Yes — reads real DB rows | ✓ FLOWING |
| `account-registry.ts` → `findActiveWhatsAppSender` | `data` from `whatsapp_authorized_senders` | Supabase service-role query | Yes — reads real DB rows | ✓ FLOWING |
| `admin-whatsapp.ts` → `listAdminWhatsAppConversations` | `convData` from `whatsapp_conversations` | Supabase service-role with filters | Yes — paginated real query | ✓ FLOWING |
| `admin-whatsapp-accounts.ts` → provisioning actions | `existing`/`newSender` from `whatsapp_authorized_senders` | Supabase service-role CRUD | Yes — real writes with audit | ✓ FLOWING |
| `preferences.ts` → `resolveChannels` | `whatsapp` always forced false | Hardcoded `whatsapp = false` (D-15) | N/A — intentionally disabled | ✓ CORRECT |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| No `from('company_whatsapp')` in runtime code | `rg "from\('company_whatsapp'\)" app components lib --glob '!*.test.*' --glob '!*migrations*"` | 0 matches | ✓ PASS |
| No `saveWhatsAppNumber` export in settings | `rg "export.*saveWhatsAppNumber" lib/actions/settings.ts` | 0 matches | ✓ PASS |
| No `syncOwnerPhone` callers in runtime | `rg "syncOwnerPhone" app lib/actions --glob '!*.test.*'` | 0 matches | ✓ PASS |
| No `whatsapp` in settings nav | `rg "value.*general" components/settings/settings-nav.tsx` | 0 matches | ✓ PASS |
| Webhook imports account-registry | `rg "findActiveWhatsAppSender" app/api/webhooks/whatsapp/route.ts` | 2 matches (import + call) | ✓ PASS |
| All 4 provisioning actions have audit | `rg "logAdminAction" lib/actions/admin-whatsapp-accounts.ts` | 6 calls | ✓ PASS |
| Contract migration renames legacy | `rg "RENAME.*_legacy_company_whatsapp" supabase/migrations/20260627000002*.sql` | 1 match | ✓ PASS |
| resolveChannels forces whatsapp=false | `rg "whatsapp = false" lib/notifications/preferences.ts` | 1 match (D-15) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| ACCT-01 | 142.1-02 | Consolidate profile/credential controls into Account; General redirects | ✓ SATISFIED | Unified Account page renders ProfileSection + AccountSection; General removed from nav and redirects; no WhatsApp writer in profile |
| WAADM-01 | 142.1-03, 142.1-06 | Tenant cannot view/configure WhatsApp provisioning, inbox, conversations | ✓ SATISFIED | /whatsapp returns notFound(); inbox/components/actions deleted; no WhatsApp surfaces in tenant code; authority contract tests pass |
| WAADM-02 | 142.1-04, 142.1-05 | Admin conversation filtering/pagination and thread inspection | ✓ SATISFIED | Server-side Zod-validated filters, URL-backed controls, paginated query, account-scoped thread verification, read-only |
| WAADM-03 | 142.1-01, 142.1-05, 142.1-06 | WhatsApp provisioning writable only via requireAdmin()-gated actions with E.164, uniqueness, transitions, audit | ✓ SATISFIED | 4 admin actions with requireAdmin(), E.164 validation, active uniqueness, closed transition maps, logAdminAction |
| WAADM-04 | 142.1-01, 142.1-06 | Inbound routing trusts only active admin-provisioned senders; no tenant seeding | ✓ SATISFIED | Webhook uses findActiveWhatsAppSender; no companies.phone fallback; syncOwnerPhone deleted; no tenant routing writes |
| WAADM-05 | 142.1-03, 142.1-06 | Opaque outbound sending gated by active account; proactive WhatsApp disabled; consent preserved | ✓ SATISFIED | Send route + project page use getWhatsAppAccountStatus; resolveChannels forces whatsapp=false; stored consent preserved on reads |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `lib/inngest/functions/whatsapp-welcome.ts` | 14 | Stale comment references `syncOwnerPhone` | ℹ️ Info | Doc-only — function deleted, comment describes historical behavior, no runtime impact |
| `components/settings/notifications-form.tsx` | 62 | `whatsapp` key in internal type definition | ℹ️ Info | Type-level only — UI renders 3 columns (In-app, Email, SMS); `whatsapp` is carried in state but never exposed as a toggle; resolveChannels forces it false anyway |

### Human Verification Required

### 1. Settings Navigation Visual Check

**Test:** Open the app, navigate to Settings, verify the sidebar shows "Account" (with ShieldCheck icon) and no "General" entry
**Expected:** Account is the only profile/security surface; no General option visible
**Why human:** Visual navigation rendering requires browser interaction

### 2. Account Page Composition

**Test:** Click "Account" in Settings nav; verify two sections render: Profile (photo, name, phone) and Security (password, email)
**Expected:** Both ProfileSection and AccountSection render under one Account header; no WhatsApp phone field
**Why human:** Visual composition of two sections on one page

### 3. General Redirect Compatibility

**Test:** Manually navigate to `/settings/general` in the browser URL bar
**Expected:** Server-side redirect to `/settings/account` — no intermediate flash, no General UI
**Why human:** Server-side redirect rendering requires browser navigation

### 4. WhatsApp Inbox Tombstone

**Test:** Navigate to `/whatsapp` as a tenant user
**Expected:** 404 page — no redirect, no content disclosure, no hint of WhatsApp existence
**Why human:** notFound() rendering is a visual 404 page

### 5. Admin WhatsApp Tabbed Interface

**Test:** As super-admin, navigate to `/admin/whatsapp`; verify Conversations and Accounts tabs; switch between them
**Expected:** Conversations tab shows filtered/paginated list; Accounts tab shows provisioning controls with masked phones
**Why human:** Complex admin UI with tabs, filters, and provisioning dialogs

---

## Gaps Summary

No gaps found. All 19 observable truths verified across 6 plans. All artifacts exist, are substantive, and are properly wired. All key links are connected. All 6 requirement IDs (ACCT-01, WAADM-01 through WAADM-05) are satisfied with codebase evidence. Two minor informational findings (stale comment, dead type field) do not affect goal achievement.

---

_Verified: 2026-06-27T18:35:00Z_
_Verifier: the agent (gsd-verifier)_
