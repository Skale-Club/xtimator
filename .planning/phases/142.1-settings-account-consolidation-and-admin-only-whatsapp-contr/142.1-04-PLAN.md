---
phase: 142.1-settings-account-consolidation-and-admin-only-whatsapp-contr
plan: 04
type: execute
wave: 2
depends_on: ["142.1-01"]
files_modified:
  - lib/queries/admin-whatsapp.ts
  - app/admin/whatsapp/page.tsx
  - app/admin/whatsapp/admin-whatsapp-client.tsx
  - app/admin/whatsapp/admin-whatsapp-filters.tsx
  - lib/actions/admin-whatsapp.ts
  - tests/unit/admin/whatsapp-filters.test.ts
  - tests/e2e/admin-whatsapp.spec.ts
autonomous: true
requirements: [WAADM-02]
decisions: [D-08, D-10, D-18]
must_haves:
  truths:
    - "Super-admin can filter conversations by account and operational dimensions before pagination"
    - "Filters survive refresh and pagination through URL search parameters"
    - "Conversation thread inspection remains admin-only and read-only"
  artifacts:
    - path: "lib/queries/admin-whatsapp.ts"
      provides: "Validated server-side filtered and paginated conversation query"
      exports: ["parseAdminWhatsAppFilters", "listAdminWhatsAppConversations"]
    - path: "app/admin/whatsapp/admin-whatsapp-filters.tsx"
      provides: "URL-backed admin filter controls"
  key_links:
    - from: "app/admin/whatsapp/page.tsx"
      to: "lib/queries/admin-whatsapp.ts"
      via: "validated searchParams"
      pattern: "listAdminWhatsAppConversations"
---

<objective>
Replace the global first-500 admin list with a server-filtered, paginated account view.

Purpose: Give super-admins usable cross-tenant visibility without client-side overfetching or ambiguous account context.
Output: Admin query module, URL filters, pagination, hardened thread loader, and unit/E2E coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-CONTEXT.md
@.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-01-SUMMARY.md
@app/admin/whatsapp/page.tsx
@app/admin/whatsapp/admin-whatsapp-client.tsx
@lib/actions/admin-whatsapp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build validated server-side filter and pagination query</name>
  <files>lib/queries/admin-whatsapp.ts, tests/unit/admin/whatsapp-filters.test.ts</files>
  <read_first>app/admin/whatsapp/page.tsx, lib/actions/admin-whatsapp.ts, app/admin/events/page.tsx, app/admin/companies/page.tsx, types/database.types.ts</read_first>
  <action>Create a server-only admin query module with Zod-parsed filters: `companyId`, optional `senderId`, `q`, `status`, `unreadOnly`, `dateFrom`, `dateTo`, and positive `page` with a fixed bounded page size (25 or 50). Require `requireAdmin()` inside the exported query, apply company/sender/status/date/unread/search predicates before `.range()`, order stably by `last_message_at DESC, id DESC`, and return rows plus total/page/pageCount.

Fetch company labels and member/auth labels only for result/filter IDs, avoiding the current all-companies plus 500-conversations overfetch. Treat “account” in UI as tenant company and “sender” as an authorized member/number. Unit-test parsing defaults, invalid input normalization, combined filters, predicates-before-range, and stable pagination.</action>
  <verify>npx vitest run tests/unit/admin/whatsapp-filters.test.ts</verify>
  <acceptance_criteria>
    - Query module contains `import 'server-only'` and calls `requireAdmin()`
    - Default page size is bounded and no `.limit(500)` remains
    - Company, sender, status, unread, date, and search filters are applied server-side
    - Pagination includes a stable secondary id order
  </acceptance_criteria>
  <done>Admin conversation data is filtered at the database boundary with deterministic pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Wire URL-backed filters, pagination, and scoped thread inspection</name>
  <files>app/admin/whatsapp/page.tsx, app/admin/whatsapp/admin-whatsapp-client.tsx, app/admin/whatsapp/admin-whatsapp-filters.tsx, lib/actions/admin-whatsapp.ts, tests/e2e/admin-whatsapp.spec.ts</files>
  <read_first>app/admin/whatsapp/page.tsx, app/admin/whatsapp/admin-whatsapp-client.tsx, components/ui/select.tsx, components/ui/input.tsx, components/ui/sheet.tsx, lib/actions/admin-whatsapp.ts</read_first>
  <action>Make the admin page accept async `searchParams`, call the new query, and render account, sender, status, unread, date range, and debounced search controls that update the URL while resetting page to 1. Preserve active filters in Previous/Next links and show result count/page state. Include company/account and sender columns without exposing platform secrets.

Change `loadAdminConversationThread` to accept `{conversationId, expectedCompanyId}` and verify both under `requireAdmin()` before returning messages, protecting stale/mismatched rows. Keep it strictly read-only: no mark-read, reply, or message mutation. Add E2E coverage for company filter isolation, combined search/status, pagination URL persistence, and opening a thread.</action>
  <verify>npx vitest run tests/unit/admin/whatsapp-filters.test.ts && npx playwright test tests/e2e/admin-whatsapp.spec.ts</verify>
  <acceptance_criteria>
    - Filter values are represented in URL search parameters
    - Changing a filter resets `page=1`
    - Pagination preserves all active filters
    - Thread action verifies both conversation id and company id
    - Thread action contains no update/insert/delete calls
  </acceptance_criteria>
  <done>Super-admins can reliably inspect the right account's conversations through a scalable read-only interface.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/admin/whatsapp-filters.test.ts`
- [ ] `npx playwright test tests/e2e/admin-whatsapp.spec.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>
- All required filters execute server-side.
- Pagination is deterministic and URL-backed.
- Thread inspection is admin-only, account-scoped, and read-only.
</success_criteria>

<output>
After completion, create `.planning/phases/142.1-settings-account-consolidation-and-admin-only-whatsapp-contr/142.1-04-SUMMARY.md`
</output>
