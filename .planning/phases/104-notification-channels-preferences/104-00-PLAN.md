---
phase: 104-notification-channels-preferences
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/notifications/event-types.test.ts
  - tests/unit/notifications/category-migration.test.ts
  - tests/unit/notifications/whatsapp-channel.test.ts
  - tests/unit/notifications/owner-phone.test.ts
  - tests/unit/sms/client.test.ts
  - tests/unit/admin/whatsapp-templates.test.ts
  - tests/unit/notifications/dispatch.test.ts
  - tests/unit/notifications/preferences.test.ts
  - tests/unit/notifications/preferences-form.test.tsx
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05, NOTIF-06, NOTIF-07]
must_haves:
  truths:
    - "A failing-by-design test exists for each Wave 0 gap in VALIDATION.md"
    - "New test files import the not-yet-existing modules and are RED (module not found / assertion fail)"
    - "Extended test files (dispatch, preferences, preferences-form) add 4-channel + best-effort cases"
  artifacts:
    - path: "tests/unit/notifications/event-types.test.ts"
      provides: "NOTIF-01 — 3-category set + event→category map RED test"
    - path: "tests/unit/notifications/category-migration.test.ts"
      provides: "NOTIF-06 — pure remap fn RED test"
    - path: "tests/unit/notifications/whatsapp-channel.test.ts"
      provides: "NOTIF-03 — WhatsApp dispatch branch RED test"
    - path: "tests/unit/notifications/owner-phone.test.ts"
      provides: "NOTIF-05 — owner-phone resolver RED test"
    - path: "tests/unit/sms/client.test.ts"
      provides: "NOTIF-04 — sendSms() payload + never-throw RED test"
    - path: "tests/unit/admin/whatsapp-templates.test.ts"
      provides: "104.3 — template panel CRUD + webhook RED test"
  key_links:
    - from: "tests/unit/notifications/*.test.ts"
      to: "lib/notifications/* + lib/sms/client + lib/actions/admin-whatsapp-templates"
      via: "await import() of target modules"
      pattern: "await import\\('@/lib/(notifications|sms)"
---

<objective>
Author the Wave 0 RED/EXTEND test scaffold for Phase 104. Six NEW test files describe behaviors for modules that do not exist yet (event-types reduction, the pure migration fn, the WhatsApp dispatch branch, the owner-phone resolver, the SMS client, the admin template panel). Three EXISTING test files are EXTENDED with 4-channel and best-effort cases. Every test is failing-by-design (RED) until Waves 1-3 turn them green.

Purpose: Lock the contracts before implementation so Waves 1-3 implement against tests, not prose. This is the Nyquist-compliance gate — every downstream task has an `<automated>` command that points at a test created here.
Output: 6 new + 3 extended vitest files, all RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-notification-channels-preferences/104-RESEARCH.md
@.planning/phases/104-notification-channels-preferences/104-VALIDATION.md

<interfaces>
<!-- Contracts the test scaffold must assert against. Extracted from the live codebase. -->

CURRENT lib/notifications/event-types.ts (Wave 1 reduces these):
```typescript
export type EventCategory = 'estimate' | 'payment' | 'trial' | 'quota' | 'whatsapp' | 'ai_job' | 'admin' | 'system'
// 17 EventType members; getCategoryForEvent(eventType): EventCategory
// DEFAULT_PREFERENCES: Record<EventCategory, { in_app: boolean; email: boolean }>
```

TARGET (Wave 1):
```typescript
export type EventCategory = 'estimate' | 'billing' | 'system' | '_dropped'
// EVENT_CATEGORIES: payment.* trial.* quota.* admin.* -> 'billing'; whatsapp.inbound + ai_job.* -> '_dropped'
// DEFAULT_PREFERENCES: Record<EventCategory, { in_app, email, whatsapp, sms }>; whatsapp+sms default false everywhere
```

CURRENT lib/notifications/preferences.ts (Wave 1 widens, Wave 2 gates):
```typescript
export interface ResolvedChannels { inApp: boolean; email: boolean }  // -> add whatsapp, sms
export async function resolveChannels(eventType, userId, override?): Promise<ResolvedChannels>
```

CURRENT lib/notifications/dispatch.ts (Wave 2 adds 2 branches):
```typescript
export interface NotifyParams { ...; channels?: { inApp?: boolean; email?: boolean } }  // -> + whatsapp?, sms?
export async function notify(params: NotifyParams): Promise<NotifyResult>
```

Twilio send shape (from app/api/estimates/[id]/send-sms/route.ts — Wave 2 extracts lib/sms/client.ts):
```
POST https://api.twilio.com/2010-04-01/Accounts/{accountSid}/Messages.json
Authorization: Basic base64(`${accountSid}:${authToken}`)
body: URLSearchParams { From, To, Body }
```

WhatsApp template send (lib/whatsapp/client.ts — already exists, Wave 2 consumes):
```typescript
export async function sendWhatsAppTemplate(to: string, opts: { name: string; languageCode: string; bodyVariables?: string[]; headerVariables?: string[] }): Promise<void>
```

Owner phone read (Wave 2 resolver — service-role):
```
SELECT owner_phone, status FROM company_whatsapp WHERE company_id=? AND user_id=? AND owner_phone IS NOT NULL
```
</interfaces>

<read_first>
@tests/unit/notifications/dispatch.test.ts
@tests/unit/notifications/preferences.test.ts
@tests/unit/notifications/preferences-form.test.tsx
@lib/notifications/event-types.ts
@lib/notifications/preferences.ts
@lib/notifications/dispatch.ts
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NEW RED tests — event map, migration fn, owner-phone, sms client</name>
  <read_first>tests/unit/notifications/dispatch.test.ts (mock + await-import pattern), lib/notifications/event-types.ts, lib/notifications/preferences.ts, app/api/estimates/[id]/send-sms/route.ts</read_first>
  <files>tests/unit/notifications/event-types.test.ts, tests/unit/notifications/category-migration.test.ts, tests/unit/notifications/owner-phone.test.ts, tests/unit/sms/client.test.ts</files>
  <action>
Create four NEW failing-by-design vitest files. Mirror the existing `await import('@/lib/...')`-inside-each-`it` pattern from dispatch.test.ts so files compile before the target modules/exports exist.

1. `tests/unit/notifications/event-types.test.ts` (NOTIF-01) — import `{ EVENT_CATEGORIES, DEFAULT_PREFERENCES, getCategoryForEvent }` from `@/lib/notifications/event-types`. Assert:
   - The set of distinct categories in `EVENT_CATEGORIES` values ⊆ `['estimate','billing','system','_dropped']` (no `payment`/`trial`/`quota`/`whatsapp`/`ai_job`/`admin`).
   - `getCategoryForEvent('payment.received') === 'billing'`, `'trial.expiring_3d' === 'billing'`, `'quota.80pct' === 'billing'`, `'admin.tier_changed' === 'billing'`.
   - `getCategoryForEvent('whatsapp.inbound') === '_dropped'`, `'ai_job.failed' === '_dropped'`, `'ai_job.completed' === '_dropped'`.
   - `getCategoryForEvent('estimate.viewed') === 'estimate'`, `'system.maintenance' === 'system'`.
   - `DEFAULT_PREFERENCES.estimate` has keys `in_app,email,whatsapp,sms` and `whatsapp===false && sms===false`; same `whatsapp/sms` default-false assertion for `billing` and `system`.

2. `tests/unit/notifications/category-migration.test.ts` (NOTIF-06) — import `{ migrateCategories }` from `@/lib/notifications/category-migration` (a PURE fn, no DB; created in Wave 1). Given input `{ payment:{in_app:true,email:false}, trial:{in_app:false,email:true}, quota:{in_app:false,email:false}, admin:{in_app:false,email:false}, estimate:{in_app:true,email:true}, whatsapp:{in_app:true,email:true}, ai_job:{in_app:true,email:true}, system:{in_app:true,email:true} }` assert output:
   - `billing.in_app === true` (OR-merge: payment had it on), `billing.email === true` (OR-merge: trial had it on).
   - No `payment`/`trial`/`quota`/`admin`/`whatsapp`/`ai_job` keys remain.
   - `estimate` and `system` carried through unchanged.
   - Idempotency: `migrateCategories(migrateCategories(input))` deep-equals `migrateCategories(input)`.
   - Empty input `{}` → `{}` (no crash, no null writes).
   - whatsapp/sms channels NOT defaulted-on by the migration (assert the merged `billing` object has no `sms:true`/`whatsapp:true` key unless source had it — source has neither, so assert `billing.sms === undefined` and `billing.whatsapp === undefined`).

3. `tests/unit/notifications/owner-phone.test.ts` (NOTIF-05) — import `{ resolveOwnerPhone }` from `@/lib/notifications/owner-phone` (created Wave 2). Mock `@/lib/supabase/service` `requireServiceClient`. Build a chainable mock returning a `company_whatsapp` row. Assert:
   - Returns the E.164 `owner_phone` when a row with non-null `owner_phone` and `status:'active'` exists for `(companyId, userId)`.
   - Returns `null` when no row / `owner_phone` is null.
   - Never throws on a DB error (mock `error` set) → returns `null`.

4. `tests/unit/sms/client.test.ts` (NOTIF-04) — import `{ sendSms }` from `@/lib/sms/client` (created Wave 2). Mock `@/lib/platform-config` `getTwilioConfig` to return `{ accountSid:'AC_test', authToken:'tok_test', fromPhone:'+15550000000' }` (placeholders only — NO real creds). Mock global `fetch`. Assert:
   - POSTs to `https://api.twilio.com/2010-04-01/Accounts/AC_test/Messages.json` with an `Authorization: Basic <base64>` header and a urlencoded body containing `From=...&To=...&Body=...` (assert From/To/Body present, not exact encoding).
   - Returns `{ ok: true, sid }` on a 201/200 Twilio response (`{ sid: 'SM_test' }`).
   - Returns `{ ok: false, error }` when `getTwilioConfig()` is `null` (unconfigured) WITHOUT calling fetch and WITHOUT throwing.
   - Returns `{ ok: false, error }` (never throws) when fetch rejects or returns non-ok.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications/event-types.test.ts tests/unit/notifications/category-migration.test.ts tests/unit/notifications/owner-phone.test.ts tests/unit/sms/client.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist; `npx vitest run` reports them RED (module-not-found or failing assertions), NOT passing and NOT erroring-on-collection.
    - `grep -q "migrateCategories" tests/unit/notifications/category-migration.test.ts` and `grep -q "resolveOwnerPhone" tests/unit/notifications/owner-phone.test.ts` and `grep -q "Messages.json" tests/unit/sms/client.test.ts` all succeed.
    - No real secret literals: `grep -rEi "sk_live_|sk_test_|AC[0-9a-f]{30}|sk-ant-|EAAG" tests/unit/notifications/event-types.test.ts tests/unit/sms/client.test.ts` returns nothing.
  </acceptance_criteria>
  <done>4 new RED test files committed; quick-run shows them red.</done>
</task>

<task type="auto">
  <name>Task 2: NEW RED tests — whatsapp dispatch branch + admin template panel</name>
  <read_first>tests/unit/notifications/dispatch.test.ts, lib/whatsapp/client.ts (sendWhatsAppTemplate signature), app/api/webhooks/whatsapp/route.ts (message_template_status_update shape), 104-RESEARCH.md (Super-Admin template panel section)</read_first>
  <files>tests/unit/notifications/whatsapp-channel.test.ts, tests/unit/admin/whatsapp-templates.test.ts</files>
  <action>
Create two NEW failing-by-design vitest files using the `await import` + `vi.mock` pattern.

1. `tests/unit/notifications/whatsapp-channel.test.ts` (NOTIF-03) — import `{ notify }` from `@/lib/notifications/dispatch`. Mock `@/lib/supabase/service`, `@/lib/inngest/client`, `@/lib/notifications/preferences` (`resolveChannels` returns `{ inApp:false, email:false, whatsapp:true, sms:false }`), `@/lib/notifications/owner-phone` (`resolveOwnerPhone` returns `'+15551230000'`), and `@/lib/notifications/whatsapp-registry` (`getTemplateForEvent` returns `{ templateName:'owner_billing_alert', languageCode:'en_US', variables: () => ['Acme', '$100'] }`). Assert:
   - When whatsapp channel resolves true + phone present + a registry template exists → a WhatsApp send is dispatched (via the Inngest event the Wave-2 branch uses — assert `inngest.send` called with a `name` containing `whatsapp` OR a direct `sendWhatsAppTemplate` mock called; pick the Inngest-event contract to mirror the email branch and assert the event `name` + `data.userId`).
   - Opt-in/template gating: when `resolveOwnerPhone` returns `null` → NO whatsapp dispatch (no-op), and the in-app/email path is unaffected.
   - When `getTemplateForEvent` returns `null` (no template mapped) → NO whatsapp dispatch (no-op).
   - Best-effort: when the whatsapp dispatch path throws (make the inngest/send mock reject) → `notify()` still returns `{ ok: ... }` and does NOT throw.

2. `tests/unit/admin/whatsapp-templates.test.ts` (104.3) — import the server actions from `@/lib/actions/admin-whatsapp-templates` (created Wave 3): `{ listTemplates, createTemplate, submitTemplateToMeta, applyTemplateStatusUpdate }`. Mock `@/lib/supabase/service` `requireServiceClient` (chainable) and mock `@/lib/admin/auth` requireAdmin (or whatever the admin guard is — read app/admin to confirm; if unsure assert the action calls a service client only). Assert:
   - `createTemplate({...})` inserts a `whatsapp_notification_templates` row with `status:'draft'` and the given `template_name`/`language_code`/`event_category`.
   - `listTemplates()` selects from `whatsapp_notification_templates` and returns the rows.
   - `applyTemplateStatusUpdate({ message_template_id, event:'APPROVED' })` updates the matching row `status` to `'approved'` (and `'REJECTED'+reason` → `status:'rejected', rejection_reason`). This is the `message_template_status_update` webhook handler logic.
   - `submitTemplateToMeta(...)` (programmatic path) is allowed to be a thin no-op-returning-`{ ok:false, reason:'scope' }` stub for now — assert it does NOT throw and returns an object (de-risked MVP; Wave 3 finalizes).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications/whatsapp-channel.test.ts tests/unit/admin/whatsapp-templates.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and run RED (module/export not found or failing assertions).
    - `grep -q "whatsapp-registry\|getTemplateForEvent" tests/unit/notifications/whatsapp-channel.test.ts` succeeds.
    - `grep -q "applyTemplateStatusUpdate" tests/unit/admin/whatsapp-templates.test.ts` and `grep -q "whatsapp_notification_templates" tests/unit/admin/whatsapp-templates.test.ts` succeed.
    - No secret literals (grep as in Task 1) in either file.
  </acceptance_criteria>
  <done>2 new RED test files committed; quick-run shows them red.</done>
</task>

<task type="auto">
  <name>Task 3: EXTEND dispatch + preferences + preferences-form for 4 channels</name>
  <read_first>tests/unit/notifications/dispatch.test.ts, tests/unit/notifications/preferences.test.ts, tests/unit/notifications/preferences-form.test.tsx</read_first>
  <files>tests/unit/notifications/dispatch.test.ts, tests/unit/notifications/preferences.test.ts, tests/unit/notifications/preferences-form.test.tsx</files>
  <action>
Extend the three EXISTING test files with new RED cases (append `it(...)` blocks; do NOT delete the existing green cases — they must stay green after Wave 1/2).

1. `dispatch.test.ts` (NOTIF-07) — add: when `resolveChannels`/`channels` enable `whatsapp` and/or `sms`, the corresponding async dispatch fires; AND a throwing WhatsApp/SMS send does NOT block the in-app insert (mock the whatsapp/sms dispatch to reject, assert the `notifications` insert STILL ran and `notify` returned without throwing). Update the `resolveChannels` mock to return the 4-channel shape `{ inApp, email, whatsapp, sms }`.

2. `preferences.test.ts` (NOTIF-02) — add: `resolveChannels` returns a 4-key object `{ inApp, email, whatsapp, sms }`; `whatsapp`/`sms` resolve `false` by default (opt-in); and the gate — even when a user's `categories[cat].whatsapp===true`, the resolved `whatsapp` is `false` unless the opt-in/phone gate is satisfied (assert the gating contract Wave 2 implements; if the gate inputs aren't available to resolveChannels, assert resolveChannels exposes `whatsapp`/`sms` keys and a SEPARATE gating fn is referenced — align with the Wave-2 contract: `resolveChannels` returns raw toggle state, dispatch applies the phone/opt-in gate. Pick: assert `resolveChannels` returns the 4 keys; defer the phone-gate assertion to dispatch/whatsapp-channel tests).

3. `preferences-form.test.tsx` (NOTIF-01/02) — add: the form renders exactly 3 category rows (Estimates, Billing, System) and 4 channel columns (In-app, Email, WhatsApp, SMS); the WhatsApp and SMS switches are `disabled` when no verified phone / no opt-in is passed in props (assert `disabled` attribute on the `pref-whatsapp-billing` / `pref-sms-billing` testids). These will be RED until Wave 1 (categories+columns) and Wave 2 (phone/opt-in gating) land.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications/dispatch.test.ts tests/unit/notifications/preferences.test.ts tests/unit/notifications/preferences-form.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The three files contain the new assertions: `grep -q "sms\|whatsapp" tests/unit/notifications/preferences.test.ts` succeeds; `grep -qi "Billing" tests/unit/notifications/preferences-form.test.tsx` succeeds; `grep -q "does NOT block\|still" tests/unit/notifications/dispatch.test.ts` (the never-block case) succeeds.
    - The NEWLY ADDED cases are RED; pre-existing cases may stay green (mixed red/green is expected and acceptable for an EXTEND).
    - No watch-mode flag in any command.
  </acceptance_criteria>
  <done>3 extended files committed; new cases red, old cases intact.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/notifications tests/unit/sms tests/unit/admin/whatsapp-templates.test.ts` collects all 6 new + 3 extended files (no "No test files found").
- Every VALIDATION.md Wave 0 gap has a corresponding file on disk.
- gitleaks pre-commit passes (no Twilio/Meta literals).
</verification>

<success_criteria>
- 6 new RED test files + 3 extended files exist and are collected by vitest.
- Each downstream Wave 1-3 task has a test it must turn green.
- `wave_0_complete: true` can be set in 104-VALIDATION.md frontmatter after this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/104-notification-channels-preferences/104-00-SUMMARY.md`.
</output>
