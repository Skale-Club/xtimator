---
phase: 104-notification-channels-preferences
plan: 01
type: execute
wave: 1
depends_on: ["104-00"]
files_modified:
  - lib/notifications/event-types.ts
  - lib/notifications/preferences.ts
  - lib/notifications/category-migration.ts
  - supabase/migrations/20260621000001_notification_categories_remap.sql
  - app/api/notifications/preferences/route.ts
  - components/settings/notifications-form.tsx
  - components/notifications/category-icon.tsx
  - components/notifications/NotificationFilters.tsx
  - lib/inngest/functions/notification-email-digest.ts
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-06]
must_haves:
  truths:
    - "The preferences page shows exactly 3 categories — Estimates, Billing, System"
    - "Each category renders 4 channel columns — In-App, Email, WhatsApp, SMS (whatsapp/sms disabled this wave, enabled in Wave 2)"
    - "Existing preference rows migrate: payment/trial/quota/admin OR-merge into billing; whatsapp/ai_job dropped; new channels left absent"
    - "Every event still maps to a deliverable category or the _dropped sentinel — no call site breaks"
  artifacts:
    - path: "lib/notifications/event-types.ts"
      provides: "EventCategory reduced to estimate|billing|system|_dropped; 4-channel DEFAULT_PREFERENCES"
      contains: "_dropped"
    - path: "lib/notifications/category-migration.ts"
      provides: "Pure migrateCategories() the SQL migration mirrors"
      exports: ["migrateCategories"]
    - path: "supabase/migrations/20260621000001_notification_categories_remap.sql"
      provides: "Idempotent JSONB remap of notification_preferences.categories"
      contains: "notification_preferences"
    - path: "components/settings/notifications-form.tsx"
      provides: "3-category × 4-channel matrix"
  key_links:
    - from: "supabase/migrations/20260621000001_notification_categories_remap.sql"
      to: "lib/notifications/category-migration.ts"
      via: "SQL mirrors the pure-fn OR-merge logic"
      pattern: "billing"
    - from: "components/settings/notifications-form.tsx"
      to: "/api/notifications/preferences"
      via: "PATCH with categories (4-channel objects)"
      pattern: "api/notifications/preferences"
---

<objective>
Reduce notification categories from 8 to 3 (Estimates, Billing, System) with a `_dropped` sentinel, widen every per-category channel object to 4 channels (in_app, email, whatsapp, sms), ship the idempotent migration that remaps existing rows, and rebuild the preferences UI + feed icons/filters for the new 3×4 model. WhatsApp/SMS columns render but are DISABLED this wave (Wave 2 wires the senders + phone/opt-in gate).

Purpose: A coherent, externally-risk-free slice — owners get the clean 3×4 UI even before the paid senders land.
Output: Reduced `EventCategory`, pure migration fn + mirrored SQL, 4-channel API/resolver/UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-notification-channels-preferences/104-RESEARCH.md

<interfaces>
CURRENT lib/notifications/event-types.ts — 8-category union, 17 EventTypes, `DEFAULT_PREFERENCES: Record<EventCategory,{in_app,email}>`.
CURRENT lib/notifications/preferences.ts — `ResolvedChannels { inApp; email }`, `resolveChannels(eventType, userId, override?)`, `UserPrefs.categories: Partial<Record<EventCategory,{in_app?;email?}>>`.
CURRENT app/api/notifications/preferences/route.ts — `ChannelSchema = z.object({ in_app: bool.opt, email: bool.opt })`, `PatchSchema.categories: z.record(z.string(), ChannelSchema)`.
CURRENT components/settings/notifications-form.tsx — `CATEGORIES` (8 rows), `ChannelState = Record<EventCategory,{in_app;email}>`, grid `grid-cols-[1fr_auto_auto]`, `data-testid` `pref-in_app-${key}` / `pref-email-${key}`.
CURRENT components/notifications/category-icon.tsx — `MAP: Record<EventCategory, Icon>` (8 keys).
CURRENT components/notifications/NotificationFilters.tsx — `CATEGORY_ICONS`, `CATEGORY_LABELS`, `ORDER` (8 each).
CURRENT lib/inngest/functions/notification-email-digest.ts — `categoryFor(eventType) = EVENT_CATEGORIES[...] ?? 'system'`.
</interfaces>

<read_first>
@lib/notifications/event-types.ts
@lib/notifications/preferences.ts
@app/api/notifications/preferences/route.ts
@components/settings/notifications-form.tsx
@components/notifications/category-icon.tsx
@components/notifications/NotificationFilters.tsx
@tests/unit/notifications/event-types.test.ts
@tests/unit/notifications/category-migration.test.ts
@tests/unit/notifications/preferences.test.ts
@tests/unit/notifications/preferences-form.test.tsx
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce EventCategory to 3 + _dropped; pure migration fn + mirrored SQL migration</name>
  <read_first>lib/notifications/event-types.ts, lib/notifications/preferences.ts, tests/unit/notifications/event-types.test.ts, tests/unit/notifications/category-migration.test.ts, supabase/migrations/20260620000001_company_whatsapp_multi_user.sql (migration file conventions)</read_first>
  <files>lib/notifications/event-types.ts, lib/notifications/category-migration.ts, lib/notifications/preferences.ts, supabase/migrations/20260621000001_notification_categories_remap.sql</files>
  <behavior>
    - getCategoryForEvent maps payment/trial/quota/admin → 'billing'; whatsapp.inbound + ai_job.* → '_dropped'; estimate.* → 'estimate'; system.maintenance → 'system'
    - DEFAULT_PREFERENCES has 3 visible categories + '_dropped', each with {in_app,email,whatsapp,sms}; whatsapp+sms default false everywhere; '_dropped' all-false
    - migrateCategories({...8 keys...}) OR-merges payment/trial/quota/admin into billing, drops whatsapp/ai_job, carries estimate/system through; idempotent; {}→{}
  </behavior>
  <action>
**event-types.ts:**
- Change `EventCategory` to `'estimate' | 'billing' | 'system' | '_dropped'`. Keep the full `EventType` union UNCHANGED (all 17 members stay — call sites must not break; the `_dropped` sentinel is the no-deliver path per RESEARCH Option A).
- Rewrite `EVENT_CATEGORIES`: `estimate.*` → `'estimate'`; `payment.* / trial.* / quota.* / admin.*` → `'billing'`; `system.maintenance` → `'system'`; `whatsapp.inbound / ai_job.failed / ai_job.completed` → `'_dropped'`.
- Change `DEFAULT_PREFERENCES` to `Record<EventCategory, { in_app: boolean; email: boolean; whatsapp: boolean; sms: boolean }>`:
  - `estimate: { in_app:true, email:true, whatsapp:false, sms:false }`
  - `billing:  { in_app:true, email:true, whatsapp:false, sms:false }`
  - `system:   { in_app:true, email:true, whatsapp:false, sms:false }`
  - `_dropped: { in_app:false, email:false, whatsapp:false, sms:false }`
- Keep `getCategoryForEvent` as-is (signature unchanged).

**lib/notifications/category-migration.ts (NEW, pure, no `server-only`, no DB):**
```typescript
type Chan = { in_app?: boolean; email?: boolean; whatsapp?: boolean; sms?: boolean }
export function migrateCategories(old: Record<string, Chan>): Record<string, Chan>
```
- OR-merge `payment|trial|quota|admin` → `billing` per-boolean (`in_app`, `email`; do NOT synthesize whatsapp/sms — leave absent so the resolver re-defaults them false).
- Drop `whatsapp`, `ai_job` keys entirely.
- Carry `estimate`, `system` through unchanged.
- Omit a key whose merged object is empty (mirror `jsonb_strip_nulls`). Idempotent (running twice on already-migrated input yields the same result — guard by treating already-present `billing` as a merge source too).
- `{}` → `{}`.

**preferences.ts:** update `UserPrefs.categories` and resolver typing to the 4-channel `Chan` shape (the full `resolveChannels` 4-channel return + gating lands in this task's typing but whatsapp/sms RESOLVE false this wave — Wave 2 adds the phone/opt-in gate). Extend `ResolvedChannels` to `{ inApp; email; whatsapp; sms }`; in `resolveChannels`, compute `whatsapp`/`sms` from `userCat.whatsapp ?? defaults.whatsapp` and `userCat.sms ?? defaults.sms` BUT hard-force both to the raw toggle (no phone gate yet — Wave 2). Keep email gate + override behavior intact (override may now include whatsapp/sms).

**supabase/migrations/20260621000001_notification_categories_remap.sql (NEW):** mirror `migrateCategories` in SQL. Use the RESEARCH-provided `UPDATE notification_preferences SET categories = (jsonb_strip_nulls(jsonb_build_object('estimate', categories->'estimate', 'billing', jsonb_build_object('in_app', COALESCE((categories#>>'{payment,in_app}')::bool, (categories#>>'{trial,in_app}')::bool, (categories#>>'{quota,in_app}')::bool, (categories#>>'{admin,in_app}')::bool, false), 'email', COALESCE((categories#>>'{payment,email}')::bool, (categories#>>'{trial,email}')::bool, (categories#>>'{quota,email}')::bool, (categories#>>'{admin,email}')::bool, false)), 'system', categories->'system'))) WHERE categories ?| array['payment','trial','quota','admin','whatsapp','ai_job'];` The `WHERE` guard makes it idempotent (a second run matches nothing). Header comment: references `lib/notifications/category-migration.ts` as the unit-tested mirror. NO secrets. Do NOT default-enable whatsapp/sms (leave absent → resolver defaults false).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications/event-types.test.ts tests/unit/notifications/category-migration.test.ts tests/unit/notifications/preferences.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/notifications/event-types.test.ts tests/unit/notifications/category-migration.test.ts` → GREEN.
    - `grep -q "_dropped" lib/notifications/event-types.ts` and `grep -q "export function migrateCategories" lib/notifications/category-migration.ts` succeed.
    - `grep -q "categories ?| array" supabase/migrations/20260621000001_notification_categories_remap.sql` succeeds (idempotency guard present).
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "event-types|preferences|category-migration"` returns nothing (no new type errors in these files).
  </acceptance_criteria>
  <done>3 categories + sentinel live; migration fn + mirrored SQL exist; their tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4-channel prefs API + 3×4 preferences form + feed icons/filters</name>
  <read_first>app/api/notifications/preferences/route.ts, components/settings/notifications-form.tsx, components/notifications/category-icon.tsx, components/notifications/NotificationFilters.tsx, lib/inngest/functions/notification-email-digest.ts, tests/unit/notifications/preferences-form.test.tsx</read_first>
  <files>app/api/notifications/preferences/route.ts, components/settings/notifications-form.tsx, components/notifications/category-icon.tsx, components/notifications/NotificationFilters.tsx, lib/inngest/functions/notification-email-digest.ts</files>
  <behavior>
    - The prefs form renders exactly 3 category rows (Estimates, Billing, System) and 4 channel columns
    - WhatsApp + SMS switches render but are disabled this wave (no phone/opt-in props yet)
    - category-icon + NotificationFilters expose only estimate/billing/system; legacy feed rows in dropped categories fall back gracefully (no crash)
  </behavior>
  <action>
**preferences/route.ts:** extend `ChannelSchema` to `z.object({ in_app: z.boolean().optional(), email: z.boolean().optional(), whatsapp: z.boolean().optional(), sms: z.boolean().optional() })`. `PatchSchema` unchanged otherwise.

**notifications-form.tsx:** 
- Replace `CATEGORIES` with the 3-row set: `{ key:'estimate', label:'Estimates', description:'Views, accepts, declines, expirations.' }`, `{ key:'billing', label:'Billing', description:'Payments, trial, quota, plan changes.' }`, `{ key:'system', label:'System', description:'Maintenance and platform announcements.' }`.
- Change `ChannelState` and `buildState` to the 4-channel shape `{ in_app, email, whatsapp, sms }`.
- Add WhatsApp + SMS columns to the grid: change `grid-cols-[1fr_auto_auto]` → `grid-cols-[1fr_auto_auto_auto_auto]` in BOTH the header and each row. Add header cells `{t('WhatsApp')}` and `{t('SMS')}`. Add two more `Switch` cells per row with `data-testid={`pref-whatsapp-${c.key}`}` and `data-testid={`pref-sms-${c.key}`}`.
- Gate the new switches: add optional props `phoneOnFile?: boolean` and `whatsappOptIn?: boolean` / `smsOptIn?: boolean` to `NotificationsFormProps` (Wave 2 will pass real values; default `false` this wave). Set `disabled={!phoneOnFile}` on whatsapp switch and `disabled={!phoneOnFile || ...}` on sms switch — this wave they render disabled. Add a small muted "Add a verified phone in your profile to enable WhatsApp/SMS" affordance row when `!phoneOnFile`.
- `setChannel` signature broadens to `channel: 'in_app' | 'email' | 'whatsapp' | 'sms'`.
- `onSave` still PATCHes `{ categories: matrix, email_digest_enabled }` — now 4-channel objects.

**category-icon.tsx:** reduce `MAP` to `{ estimate: FileText, billing: DollarSign, system: Wrench }`. The existing `?? Wrench` fallback covers legacy `_dropped`/unknown feed rows (no crash).

**NotificationFilters.tsx:** reduce `CATEGORY_ICONS`, `CATEGORY_LABELS`, `ORDER` to the 3 visible categories: `estimate: FileText / 'Estimates'`, `billing: DollarSign / 'Billing'`, `system: Wrench / 'System'`. Drop the unused icon imports (Clock, Cpu, MessageSquare, ShieldCheck, Activity) if no longer referenced.

**notification-email-digest.ts:** `categoryFor` already falls back to `'system'`. Verify `_dropped`/legacy event_types group under the `'system'` fallback without crashing the digest grouping — no behavior change needed beyond confirming the fallback covers the reduced union. (If the type of `EVENT_CATEGORIES[...]` now narrows, the `?? 'system'` stays valid.)
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notifications/preferences-form.test.tsx tests/unit/notifications/email-digest.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/notifications/preferences-form.test.tsx` → the 3-category × 4-column cases GREEN; whatsapp/sms switches `disabled` (no phone props).
    - `grep -q "pref-whatsapp-" components/settings/notifications-form.tsx` and `grep -q "pref-sms-" components/settings/notifications-form.tsx` succeed.
    - `grep -cE "estimate:|billing:|system:" components/notifications/category-icon.tsx` returns 3; no `whatsapp:|ai_job:|payment:|trial:|quota:|admin:` keys remain in MAP.
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "notifications-form|NotificationFilters|category-icon|preferences/route"` returns nothing.
  </acceptance_criteria>
  <done>Prefs UI is 3×4; feed icons/filters 3-category; digest grouping unaffected; tests green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/notifications` — all NOTIF-01/02/06 cases for this wave green; no regression in pre-existing notification tests.
- The migration SQL is idempotent (`WHERE categories ?| array[...]`).
- `whatsapp`/`sms` default false everywhere — no paid channel auto-enabled.
- gitleaks clean.
</verification>

<success_criteria>
- Preferences page: exactly 3 categories × 4 channels; WhatsApp/SMS columns present but disabled (pending Wave 2).
- Existing rows migrate cleanly (payment/trial/quota/admin → billing OR-merge; whatsapp/ai_job dropped).
- Every event routes to a deliverable category or `_dropped`; no call site breaks.
</success_criteria>

<output>
After completion, create `.planning/phases/104-notification-channels-preferences/104-01-SUMMARY.md`.
</output>
