---
phase: 104-notification-channels-preferences
plan: 03
type: execute
wave: 3
depends_on: ["104-02"]
files_modified:
  - supabase/migrations/20260621000003_whatsapp_notification_templates.sql
  - lib/actions/admin-whatsapp-templates.ts
  - app/admin/whatsapp-templates/page.tsx
  - components/admin/whatsapp-templates-panel.tsx
  - components/admin/admin-nav.tsx
  - app/api/webhooks/whatsapp/route.ts
  - lib/notifications/whatsapp-registry.ts
autonomous: true
requirements: [NOTIF-03]
must_haves:
  truths:
    - "A super-admin route lists, creates, and manages WhatsApp notification templates"
    - "The whatsapp_notification_templates table is RLS service-role-only (no tenant access)"
    - "The message_template_status_update webhook flips a template's status (approved/rejected + reason)"
    - "Approved templates feed the dispatch registry so getTemplateForEvent can resolve them"
    - "Phase 98 (WhatsApp Notifications) is marked superseded by Phase 104 for owner-facing WhatsApp"
  artifacts:
    - path: "supabase/migrations/20260621000003_whatsapp_notification_templates.sql"
      provides: "whatsapp_notification_templates table + service-role-only RLS"
      contains: "whatsapp_notification_templates"
    - path: "lib/actions/admin-whatsapp-templates.ts"
      provides: "CRUD + submit-to-Meta + status-update server actions"
      exports: ["listTemplates", "createTemplate", "submitTemplateToMeta", "applyTemplateStatusUpdate"]
    - path: "app/admin/whatsapp-templates/page.tsx"
      provides: "Super-admin template management route"
    - path: "components/admin/admin-nav.tsx"
      provides: "WA Templates nav entry"
      contains: "whatsapp-templates"
  key_links:
    - from: "app/api/webhooks/whatsapp/route.ts"
      to: "lib/actions/admin-whatsapp-templates.ts"
      via: "message_template_status_update → applyTemplateStatusUpdate"
      pattern: "message_template_status_update"
    - from: "lib/notifications/whatsapp-registry.ts"
      to: "whatsapp_notification_templates"
      via: "approved DB templates drive getTemplateForEvent"
      pattern: "whatsapp_notification_templates"
---

<objective>
Build the super-admin WhatsApp notification-template panel: a service-role-only `whatsapp_notification_templates` table, an admin route + nav entry + CRUD UI, the `message_template_status_update` webhook handler that flips template status, and (de-risked) an optional programmatic submit to Meta. Approved templates feed the dispatch registry from Wave 2. Mark Phase 98 superseded.

Purpose: Proactive (out-of-24h) WhatsApp owner notifications require approved Meta templates; this is the in-app builder Phase 98 deferred — built once, here.
Output: Templates table + RLS, admin CRUD, webhook status sync, registry hookup, Phase-98 superseded note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-notification-channels-preferences/104-RESEARCH.md
@.planning/phases/98-whatsapp-template-notifications/98-CONTEXT.md

<interfaces>
Admin nav (components/admin/admin-nav.tsx) — `NAV_ITEMS` array of `{ href, label, Icon }`; existing `{ href:'/admin/whatsapp', label:'WhatsApp', Icon: MessageCircle }`.
WhatsApp webhook (app/api/webhooks/whatsapp/route.ts) — POST verifies HMAC, parses payload, early-exits on `statuses`. The `message_template_status_update` arrives under `payload.entry[].changes[].field === 'message_template_status_update'` with `value: { message_template_id, message_template_name, message_template_language, event: 'APPROVED'|'REJECTED'|'PENDING'|..., reason? }`.
Meta programmatic submit (research): `POST https://graph.facebook.com/v21.0/{WABA_ID}/message_templates` with `{ name, category, language, components }`; `wabaId` from `getWhatsAppPlatformConfig()` (already loaded, currently unused). Requires `whatsapp_business_management` token scope.
Service-role-only RLS pattern (mirror `notifications` table — migration 20260520000002): `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;` with NO anon/authenticated policies (service role bypasses RLS).
Wave-2 registry: lib/notifications/whatsapp-registry.ts `getTemplateForEvent(eventType)` currently static — this wave lets approved DB rows drive it.
</interfaces>

<read_first>
@components/admin/admin-nav.tsx
@app/api/webhooks/whatsapp/route.ts
@lib/whatsapp/client.ts
@lib/notifications/whatsapp-registry.ts
@supabase/migrations/20260520000002_notifications_system.sql
@tests/unit/admin/whatsapp-templates.test.ts
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: whatsapp_notification_templates table (service-role RLS) + CRUD + status-update server actions</name>
  <read_first>supabase/migrations/20260520000002_notifications_system.sql (RLS pattern), lib/whatsapp/client.ts, lib/whatsapp/sync-owner-phone.ts (service-client usage), tests/unit/admin/whatsapp-templates.test.ts, app/admin (admin guard pattern — requireAdmin)</read_first>
  <files>supabase/migrations/20260621000003_whatsapp_notification_templates.sql, lib/actions/admin-whatsapp-templates.ts, lib/notifications/whatsapp-registry.ts</files>
  <behavior>
    - createTemplate inserts a row with status:'draft'
    - listTemplates returns all rows (admin only, service-role read)
    - applyTemplateStatusUpdate('APPROVED') → status:'approved'; ('REJECTED', reason) → status:'rejected' + rejection_reason
    - submitTemplateToMeta returns an object (never throws) — {ok:false,reason:'scope'} when scope absent (de-risked MVP)
  </behavior>
  <action>
**supabase/migrations/20260621000003_whatsapp_notification_templates.sql (NEW):**
```sql
CREATE TABLE IF NOT EXISTS whatsapp_notification_templates (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  event_category text,                         -- 'estimate'|'billing'|'system' (nullable)
  event_type text,                             -- optional specific EventType mapping
  template_name text NOT NULL,
  language_code text NOT NULL DEFAULT 'en_US',
  meta_template_id text,                       -- null until created/registered in Meta
  status text NOT NULL DEFAULT 'draft',        -- draft|pending|approved|rejected
  rejection_reason text,
  variables_schema jsonb NOT NULL DEFAULT '[]'::jsonb,
  created_by uuid,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);
ALTER TABLE whatsapp_notification_templates ENABLE ROW LEVEL SECURITY;
-- Service-role-only (mirror notifications): NO anon/authenticated policies. Admin writes go through the service client.
```
Header comment: RLS service-role-only by design (platform-admin data, not tenant data); service role bypasses RLS. Idempotent (`IF NOT EXISTS`). NO secrets.

**lib/actions/admin-whatsapp-templates.ts (NEW, server actions):**
- `listTemplates(): Promise<TemplateRow[]>` — requireAdmin (use the existing admin guard you find in app/admin), then service-client `select('*').order('created_at', { descending })`.
- `createTemplate(input: { event_category?; event_type?; template_name; language_code; variables_schema? })` — requireAdmin, insert with `status:'draft'`, `created_by` = admin id. Return the row / `{ ok }`.
- `submitTemplateToMeta(id)` — requireAdmin. Read the row + `getWhatsAppPlatformConfig()` `wabaId` + token. If the token lacks `whatsapp_business_management` scope (or wabaId null) → return `{ ok:false, reason:'scope' }` (de-risked MVP — do NOT throw). Else `POST https://graph.facebook.com/v21.0/{wabaId}/message_templates` with `{ name, category:'UTILITY', language, components }`, store returned id + `status:'pending'`. Wrap in try/catch → `{ ok:false, error }` (never throw).
- `applyTemplateStatusUpdate(input: { message_template_id?: string; template_name?: string; language?: string; event: string; reason?: string })` — NO admin guard (called from the verified webhook with a service client). Map `event`: `'APPROVED'→'approved'`, `'REJECTED'→'rejected'` (+ `rejection_reason: reason`), `'PENDING'→'pending'`, else passthrough lowercase. Update the matching row by `meta_template_id = message_template_id` (fallback match on `template_name`+`language_code`). Best-effort; never throw.

**lib/notifications/whatsapp-registry.ts:** add an async DB-backed resolver `getApprovedTemplateForEvent(eventType): Promise<NotificationTemplate | null>` that reads an `approved` `whatsapp_notification_templates` row matching the event's category/type (service client), falling back to the static map. Keep the synchronous `getTemplateForEvent` for the existing Wave-2 dispatch path (the dispatch may adopt the async variant later — note the seam in a comment; do NOT break the Wave-2 tests). 
  </action>
  <verify>
    <automated>npx vitest run tests/unit/admin/whatsapp-templates.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/admin/whatsapp-templates.test.ts` → GREEN.
    - `grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260621000003_whatsapp_notification_templates.sql` succeeds AND `grep -ci "CREATE POLICY" supabase/migrations/20260621000003_whatsapp_notification_templates.sql` returns 0 (no tenant policies → service-role-only).
    - `grep -q "applyTemplateStatusUpdate\|createTemplate\|listTemplates\|submitTemplateToMeta" lib/actions/admin-whatsapp-templates.ts` succeeds (all four exports present).
    - No secret literals: `grep -rEi "EAAG|sk-ant-|whatsapp_business_management\"\s*:\s*\"[A-Za-z0-9]{20}" lib/actions/admin-whatsapp-templates.ts` returns nothing.
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "admin-whatsapp-templates|whatsapp-registry"` returns nothing.
  </acceptance_criteria>
  <done>Templates table + RLS + CRUD/status actions exist; registry DB-backed seam added; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Admin route + nav entry + CRUD UI + message_template_status_update webhook wiring + Phase-98 superseded note</name>
  <read_first>components/admin/admin-nav.tsx, app/admin/whatsapp (existing admin route layout pattern), app/api/webhooks/whatsapp/route.ts, lib/actions/admin-whatsapp-templates.ts, .planning/ROADMAP.md (Phase 98 entry)</read_first>
  <files>app/admin/whatsapp-templates/page.tsx, components/admin/whatsapp-templates-panel.tsx, components/admin/admin-nav.tsx, app/api/webhooks/whatsapp/route.ts</files>
  <action>
**admin-nav.tsx:** add a `NAV_ITEMS` entry after the existing WhatsApp item: `{ href: '/admin/whatsapp-templates', label: 'WA Templates', Icon: MessageCircle }` (reuse the imported `MessageCircle`).

**app/admin/whatsapp-templates/page.tsx (NEW, server component):** requireAdmin (match the existing admin route guard pattern), `await listTemplates()`, render `<WhatsAppTemplatesPanel templates={...} />`. Mirror the layout/shell of a sibling admin page (e.g. app/admin/whatsapp).

**components/admin/whatsapp-templates-panel.tsx (NEW, 'use client'):** a table of templates (name, language, event_category, status badge, rejection_reason) + a create form (template_name, language_code, event_category select estimate|billing|system, optional variables) calling `createTemplate`, + a "Submit to Meta" button per draft row calling `submitTemplateToMeta` (surface the `{ ok:false, reason:'scope' }` result as an inline notice so the admin knows to register the already-approved template manually instead). Use shadcn primitives + sonner toasts (match existing admin UI). t()-wrap copy.

**app/api/webhooks/whatsapp/route.ts:** in the POST handler, BEFORE the inbound-message `after()` path, detect `payload.entry[].changes[].field === 'message_template_status_update'`. When present, call `applyTemplateStatusUpdate({ message_template_id: value.message_template_id, template_name: value.message_template_name, language: value.message_template_language, event: value.event, reason: value.reason })` (via `after()` or inline best-effort) and return 200. Do NOT disturb the existing HMAC verification, the `statuses` early-exit, or the inbound-message routing. Best-effort: wrap in try/catch.

**ROADMAP.md (Phase 98 reconciliation):** mark the Phase 98 entry superseded — append to the v4.4 / Phase 98 line a note: "SUPERSEDED by Phase 104 (owner-facing WhatsApp notifications + in-app template builder built in 104). Do not run; revive only if a distinct customer-facing scope is ever needed." Do NOT touch Phase 98's CONTEXT artifacts beyond this note.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/admin/whatsapp-templates.test.ts 2>&1 | tail -15 && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "whatsapp-templates|admin-nav|webhooks/whatsapp" || echo "tsc-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "whatsapp-templates" components/admin/admin-nav.tsx` succeeds (nav entry added).
    - `grep -q "message_template_status_update" app/api/webhooks/whatsapp/route.ts` and `grep -q "applyTemplateStatusUpdate" app/api/webhooks/whatsapp/route.ts` succeed.
    - `app/admin/whatsapp-templates/page.tsx` and `components/admin/whatsapp-templates-panel.tsx` exist.
    - `grep -qi "SUPERSEDED by Phase 104" .planning/ROADMAP.md` succeeds.
    - `npx tsc --noEmit -p tsconfig.json` reports no NEW errors in the touched files (the grep returns nothing or `tsc-clean`).
  </acceptance_criteria>
  <done>Admin panel + nav + webhook status sync live; Phase 98 marked superseded.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/admin/whatsapp-templates.test.ts` green; full `npx vitest run` green before phase gate.
- `whatsapp_notification_templates` is service-role-only (RLS enabled, zero CREATE POLICY).
- Webhook HMAC verification + inbound routing untouched.
- Phase 98 superseded note present in ROADMAP.
- gitleaks clean (Meta token only via getWhatsAppPlatformConfig).

**Manual-only (operational, deferred to verifier — needs live creds/approval):** submit a template from the panel and confirm the `message_template_status_update` webhook flips its status; approve a template + opt-in WhatsApp + trigger an event and confirm the proactive WhatsApp message arrives (see 104-VALIDATION.md Manual-Only table).
</verification>

<success_criteria>
- Super-admin can list/create/manage WhatsApp notification templates; status syncs from Meta via webhook.
- Templates table RLS-locked to service-role; no tenant exposure.
- Approved templates can drive the dispatch registry (NOTIF-03 proactive sends).
- Phase 98 closed as superseded — no duplicate template infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/104-notification-channels-preferences/104-03-SUMMARY.md`.
</output>
