---
phase: 184-consolidated-pagination-engine
plan: 05
type: execute
wave: 4
depends_on: ["184-01", "184-02", "184-03", "184-04"]
files_modified:
  - lib/pdf/measure-header-height.ts
  - lib/pdf/render-estimate-pdf.ts
  - components/pdf/estimate-pdf.tsx
  - components/pdf/estimate-pdf-modern.tsx
  - tests/unit/pdf/_pages-for-fixture.ts
  - tests/unit/pdf/measure-header-height.test.ts
  - tests/unit/estimate/fixtures/document-fixtures.ts
  - tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
  - tests/unit/pdf/estimate-pdf-pagination.test.tsx
  - tests/unit/pdf/render-estimate-pdf-resolver.test.ts
  - tests/unit/pdf/estimate-pdf-banner-fill.test.tsx
  - tests/unit/pdf/estimate-pdf-signature.test.tsx
  - tests/unit/pdf/estimate-pdf-modern-signature.test.tsx
  - tests/unit/pdf/estimate-pdf-totals.test.tsx
  - tests/unit/pdf/estimate-pdf-modern-totals.test.tsx
  - tests/unit/pdf/estimate-pdf-photo-captions.test.tsx
  - tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx
  - tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx
  - tests/unit/estimate/pt-px-conversion-source.test.ts
  - .planning/phases/184-consolidated-pagination-engine/184-HUMAN-UAT.md
autonomous: false
requirements: [PGBRK-01, PGBRK-03, PGBRK-04]

must_haves:
  truths:
    - "In a multi-page PDF, a line-item row is never split across a page break; a section header always renders on the same page as its first item row; a section's subtotal always renders on the same page as that section's last item row."
    - "The totals block, the signature block, and each terms card each render fully on one page."
    - "Every PDF page after page 1 repeats the items-table column header, and every page shows Page N of M."
    - "For the same estimate and template, the pagination module is the one function both the PDF renderer and (in Phase 185) the web measurement provider call — verified by tests that the same block inputs always produce the same page/block assignment."
    - "The real generated PDF byte stream has exactly as many /Type /Page objects as the engine's computed page count (not just 'renderToBuffer didn't throw')."
  artifacts:
    - path: "lib/pdf/measure-header-height.ts"
      provides: "measureHeaderHeightPt(company, templateId) — data-dependent header height using max(leftColumn, rightColumn) + chrome, computed per render, never a hardcoded literal"
      contains: "export function measureHeaderHeightPt"
    - path: "tests/unit/pdf/_pages-for-fixture.ts"
      provides: "buildPagesForFixture(estimate, company, templateId, opts) — the ONE shared helper every direct-call PDF test uses to compute a real `pages` prop via the actual blocksFromModel+computePageBreaks pipeline"
      contains: "export function buildPagesForFixture"
    - path: "tests/unit/pdf/estimate-pdf-pagination.test.tsx"
      provides: "N-explicit-Page structural tests, repeated table header assertion, REAL PDF-object page-count regex assertion, renderToBuffer smoke on a multi-page fixture, determinism test"
    - path: ".planning/phases/184-consolidated-pagination-engine/184-HUMAN-UAT.md"
      provides: "Durable UAT checklist + artifact paths (status: partial) so manual-verification evidence survives even if the checkpoint gets auto-approved"
  key_links:
    - from: "lib/pdf/render-estimate-pdf.ts"
      to: "lib/estimate/pagination/engine.ts"
      via: "computePageBreaks(blocksFromModel(...), constraints, createFontkitMeasurementProvider()) called before createElement; constraints.safetyMarginPt derived from SAFETY_MARGIN_LINES * a representative line-height-in-pt"
      pattern: "computePageBreaks"
    - from: "components/pdf/estimate-pdf.tsx"
      to: "pages?: PageAssignment[] prop"
      via: "pages.map((page,i) => <Page key={i} .../>) replaces the single static <Page>; ALL 11 block kinds (incl. prepared-by) dispatch through ONE renderBlockForKind"
      pattern: "pages\\.map|renderBlockForKind"
    - from: "components/pdf/estimate-pdf.tsx"
      to: "components/pdf/shared/pdf-section-block.tsx"
      via: "PdfTableHeaderOnly ALWAYS rendered right after PdfSectionHeader, PLUS once more at page top iff page.continuesTable"
      pattern: "continuesTable"
    - from: "tests/unit/pdf/estimate-pdf-pagination.test.tsx"
      to: "renderToBuffer output"
      via: "(buf.toString('latin1').match(/\\/Type\\s*\\/Page[^s]/g) ?? []).length === pages.length"
      pattern: "Type\\\\s\\*\\\\/Page"
---

<revision_note>
**Revised per plan-checker (18 blockers, Opus).** Changes from the prior version:
- Wave restructure: now Wave 4, `depends_on: ["184-01", "184-02", "184-03", "184-04"]` (was Wave 3, `depends_on: ["184-03","184-04"]`).
- Blocker 3: Task 1 now declares `pages?: PageAssignment[]` as OPTIONAL on both templates' props (type-only in Task 1 — NOT yet consumed by the render path, which Task 2 wires).
- Blocker 4: baseline-order test update + the multi-page fixture MOVE from Task 3 into Task 2 (the task that actually wires `pages.map()` rendering) — so Task 2's own commit leaves `estimate-pdf-baseline-order.test.tsx` green, no red window.
- Blocker 1: Task 2 creates the shared helper `tests/unit/pdf/_pages-for-fixture.ts` (`buildPagesForFixture`) FIRST (baseline-order needs it). Task 3 then updates the remaining 10 test files that call the templates directly and would otherwise break once `pages` becomes load-bearing: `render-estimate-pdf-resolver.test.ts`, `estimate-pdf-banner-fill.test.tsx`, `estimate-pdf-signature.test.tsx`, `estimate-pdf-modern-signature.test.tsx`, `estimate-pdf-totals.test.tsx`, `estimate-pdf-modern-totals.test.tsx`, `estimate-pdf-photo-captions.test.tsx`, `estimate-pdf-photo-grid-atomicity.test.tsx`, `estimate-pdf-terms-atomicity.test.tsx`, and the NEW `estimate-pdf-pagination.test.tsx` itself — all via the SAME `buildPagesForFixture` helper.
- Blocker 2: Task 3 updates `render-estimate-pdf-resolver.test.ts`'s `BASE_ESTIMATE` mock to add `sections: []` and `presentation_settings: null` (so the now-wired `blocksFromModel` call inside `renderEstimatePdf` doesn't choke on a minimal mock).
- Blockers 10+11: `prepared-by` is now an ORDINARY atomic flowing block (Plan 184-02). Task 2's composition DELETES the special-cased `page.blocks.some(b => b.kind==='prepared-by')` gate entirely — ALL 11 block kinds, including `prepared-by`, render through the ONE `renderBlockForKind` dispatcher in `page.blocks` order, no exceptions.
- Blocker 12: Task 2's dispatcher ALWAYS emits `PdfTableHeaderOnly` immediately after `PdfSectionHeader` (unconditional — a new section always needs its column header once), PLUS independently renders `PdfTableHeaderOnly` once at the very top of a page's content whenever `page.continuesTable` is true (these two triggers are mutually exclusive per page by construction, since a `continuesTable` page's first block is an `item-row`, never a `section-header`).
- Blocker 13: Task 2's item-row grouping and terms-card rendering now use `block.ref` (from Plan 184-03) instead of ad hoc lookups — `ref.itemIndex`/`ref.sectionId` for row runs, `ref.termsKey` for which term `PdfTermsCard` renders.
- Blocker 14: Task 2 renders `terms-card` blocks via Plan 184-04's exported `PdfTermsCard`, driven by `block.ref.termsKey` (one call per block, not a whole-section render).
- Blocker 16: Task 1's `measureHeaderHeightPt` now uses `max(leftColumnHeight, rightColumnHeight) + paddingBottom + marginBottom + borderBottomWidth` (`PdfHeader` is a flex ROW with two columns — the rendered height is the MAX of the two, not their sum). Task 3's `renderToBuffer` smoke now ALSO asserts the REAL PDF page count via a regex over the raw buffer bytes, not just "didn't throw."
- Blocker 18: Task 4 now WRITES the 4 UAT PDFs to disk (`.planning/phases/184-consolidated-pagination-engine/uat/{classic,modern}-{1page,multipage}.pdf`) and creates `184-HUMAN-UAT.md` (status: partial) BEFORE presenting the checkpoint, so evidence survives even under auto-approval.
- Blocker 19: geometry constants come from `ESTIMATE_PAGE_GEOMETRY` (Plan 184-01), not inline literals; `render-estimate-pdf.ts` and `measure-header-height.ts` are added to `pt-px-conversion-source.test.ts`'s `CLEAN_SOURCES`.
- Blocker 23 (consuming Plan 184-02's design): Task 1 computes `constraints.safetyMarginPt = SAFETY_MARGIN_LINES * (ESTIMATE_PAGE_GEOMETRY[templateId].tableCellFontSizePt * LINE_HEIGHT[fontFamily])` — a flat per-page reserve, not a per-block addition. The engine call DROPS the old 4th `safetyMarginLines` argument.
- Blocker 27: Task 3's acceptance criteria grep for `pages.length >= 3` now targets ONLY `estimate-pdf-pagination.test.tsx` (the file that actually runs the pipeline) — `document-fixtures.ts` is dropped from that grep target (it's a data fixture, not a pipeline-invoking assertion site).
</revision_note>

<objective>
Wire the deterministic pagination engine (184-02/03) and the atomicity-fixed components (184-04) into both PDF templates for real: replace each template's single `<Page wrap>` with N explicit `<Page>` elements computed by `computePageBreaks()`, repeat the items-table column header on continuation pages, and prove it all with structural + determinism + real-PDF-byte + `renderToBuffer` tests — plus durable, on-disk UAT evidence.

Purpose: This is the phase's payoff task — PGBRK-01/03/04 all land here. Everything in Waves 1-3 exists specifically so this plan can replace emergent Yoga `wrap` with prescribed, explicit page breaks.
Output: Both PDF templates accept an optional `pages: PageAssignment[]` prop and render exactly that many `<Page>`s via ONE uniform block dispatcher; `render-estimate-pdf.ts` computes `pages` before rendering; a genuinely multi-page fixture proves the whole pipeline end-to-end; 4 UAT PDFs + a checklist doc are written to disk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-01-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-02-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-03-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-04-SUMMARY.md
@lib/pdf/render-estimate-pdf.ts
@components/pdf/shared/pdf-header.tsx
@lib/estimate/document/tokens.ts
@tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
@tests/unit/estimate/fixtures/document-fixtures.ts
@tests/unit/pdf/render-estimate-pdf-resolver.test.ts

<interfaces>
From Plan 184-02 (`lib/estimate/pagination/engine.ts` + `types.ts`):
```ts
export function computePageBreaks(blocks: PageBlock[], constraints: PageConstraints, measurementProvider: MeasurementProvider): PageAssignment[]
export interface PageConstraints { contentHeightPt: number; continuationTableHeaderHeightPt: number; safetyMarginPt: number }
export interface PageAssignment { pageIndex: number; blocks: PageBlock[]; continuesTable: boolean }
export interface PageBlockRef { sectionId?: string; itemId?: string; itemIndex?: number; termsKey?: 'estimate'|'payment'|'timeline'|'warranty'|'notes'; photoRange?: [number, number] }
```

From Plan 184-01 (`lib/estimate/pagination/measure/safety-margin.ts` + `lib/estimate/document/tokens.ts`):
```ts
export const SAFETY_MARGIN_LINES: number
export const LINE_HEIGHT: Record<string, number>
export const ESTIMATE_PAGE_GEOMETRY: Record<EstimateTemplateId, EstimatePageGeometry>
```

From Plan 184-03 (`lib/estimate/pagination/measure/estimator.ts` + `blocks-from-model.ts`):
```ts
export function createFontkitMeasurementProvider(): MeasurementProvider
export function blocksFromModel(input): PageBlock[]  // exact input shape: see 184-03-SUMMARY.md; input.sections defaults to []
```

From Plan 184-04 (`components/pdf/shared/pdf-section-block.tsx`, `pdf-photo-grid.tsx`, `pdf-terms-section.tsx`, `pdf-totals-block.tsx`):
```ts
export function PdfSectionHeader(props): ReactElement
export function PdfTableHeaderOnly(props): ReactElement
export function PdfSectionRows({ items, startIndex, styles }): ReactElement
export function PdfSectionSubtotal(props): ReactElement
export function PdfPhotoGrid({ photos, contentWidthPt, showLabel, ... }): ReactElement
export function photosPerRow(contentWidthPt: number): number
export function PdfTermsSection(props): ReactElement  // internally per-card wrap={false}
export function PdfTermsCard({ title, text, titleColor, styles }): ReactElement
// PdfTotalsBlock's outer container is now wrap={false} in both variants
```

Page geometry (from `lib/estimate/document/tokens.ts`'s `ESTIMATE_PAGE_GEOMETRY` — Plan 184-01, read from there, never inlined):

| Template | contentWidthPt | topPaddingPt | bottomPaddingPt |
|---|---|---|---|
| Classic | 532 | 40 | 60 |
| Modern | 508 | 52 | 68 |

`PdfHeader`'s live layout (`components/pdf/shared/pdf-header.tsx` + each template's `styles`) — a flex ROW with a LEFT column (name/contact/address stacked) and a RIGHT column (langBadge/logo stacked); `headerLeft`/`headerRight` each have their own `gap` (Classic: 4 / 6; Modern: 4 / 8). The rendered row height is `max(leftColumnHeight, rightColumnHeight)`, NOT the sum of both columns — this is the blocker-16 correction Task 1 implements.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data-dependent header-height measurement (MAX of two columns) + resolver wiring + optional `pages` prop declaration</name>
  <files>lib/pdf/measure-header-height.ts, lib/pdf/render-estimate-pdf.ts, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx, tests/unit/pdf/measure-header-height.test.ts, tests/unit/estimate/pt-px-conversion-source.test.ts</files>
  <read_first>components/pdf/shared/pdf-header.tsx (exact conditional rendering: name always; one contact-line joined by "  |  " only if any of phone/email/website present; one address line only if formatAddress() truthy; logo 72x72 Classic/64x64 Modern only if logo_url set; `headerRight` stacks langBadge above logo with its own `gap`); lib/pdf/render-estimate-pdf.ts (renderEstimatePdf()'s current structure — insert measurement + computePageBreaks after context/photos/preparedBy resolution, before createElement); lib/estimate/document/tokens.ts (ESTIMATE_PAGE_GEOMETRY/LINE_HEIGHT — Plan 184-01)</read_first>
  <behavior>
    - `measureHeaderHeightPt({ name: 'Acme' }, 'classic')` (name only, no phone/email/website/address/logo) is STRICTLY LESS than `measureHeaderHeightPt({ name: 'Acme', phone: '+1...', email: 'a@b.com', website: 'https://...', address: '123 Main St', city:'Austin', state:'TX', zip:'78701', logo_url: 'https://...' }, 'classic')` (two DIFFERENT computed budgets, never a shared hardcoded literal).
    - **MAX-of-columns (Plan-checker blocker 16):** given a company with a very long name (many wrapped lines, tall LEFT column) but NO `logo_url` (short RIGHT column — just the langBadge), `measureHeaderHeightPt` returns a value driven by the LEFT column's height, not the sum of both columns. Given a company with a SHORT name but a `logo_url` set (72pt/64pt tall RIGHT column, taller than the short left column), the function returns a value driven by the RIGHT column instead. Assert both directions with 2 fixtures.
    - Calling `measureHeaderHeightPt` twice with identical args returns the identical number (deterministic).
    - `'classic'` and `'modern'` template ids produce different results for the SAME company (different padding/logo-size tokens).
  </behavior>
  <action>
    Create `lib/pdf/measure-header-height.ts` exporting `measureHeaderHeightPt(company: PdfHeaderCompany, templateId: EstimateTemplateId): number` computing:
    ```
    leftColumnHeightPt = nameLineHeightPt
      + (any of phone/email/website present ? contactLineHeightPt : 0)
      + (formatAddress(company) truthy ? addressLineHeightPt : 0)
      // + headerLeft's own `gap` between present lines (Classic 4, Modern 4)
    rightColumnHeightPt = langBadgeLineHeightPt
      + (logo_url set ? logoHeightPt (72 Classic / 64 Modern) : 0)
      // + headerRight's own `gap` (Classic 6, Modern 8) between langBadge and logo
    headerRowHeightPt = max(leftColumnHeightPt, rightColumnHeightPt)
    return headerRowHeightPt + header.paddingBottom + header.marginBottom + header.borderBottomWidth
      // Classic: paddingBottom 16, marginBottom 24, borderBottomWidth 2
      // Modern:  paddingBottom 20, marginBottom 32, borderBottomWidth 0.75
    ```
    Document every constant with a comment citing `pdf-header.tsx`'s structure and each template's `styles.header`/`headerLeft`/`headerRight` StyleSheet values. Read `companyName`/`companyContact`/`langBadge` font sizes from each template's live StyleSheet (do not invent new numbers).

    In `lib/pdf/render-estimate-pdf.ts`'s `renderEstimatePdf()`, AFTER resolving `attachedPhotos`/`preparedBy` and BEFORE `createElement(...)`, add:
    ```ts
    const geometry = ESTIMATE_PAGE_GEOMETRY[templateId]
    const headerHeightPt = measureHeaderHeightPt(company, templateId)
    const fontFamily = ESTIMATE_DESIGN_TOKENS[templateId].fontFamily
    const safetyMarginPt = SAFETY_MARGIN_LINES * (geometry.tableCellFontSizePt * LINE_HEIGHT[fontFamily])
    const constraints: PageConstraints = {
      contentHeightPt: LETTER_HEIGHT_PT - geometry.topPaddingPt - geometry.bottomPaddingPt - headerHeightPt,
      continuationTableHeaderHeightPt: /* fixed per-template constant, cite pdf-section-block.tsx's tableHeader padding — same formula 184-03's height_formulas table gives for section-header's table-header row */,
      safetyMarginPt,
    }
    const blocks = blocksFromModel({ /* estimate sections/summary/terms/signature/photos/resolvedSettings/preparedBy/templateId per 184-03-SUMMARY.md's exact input shape */ })
    const pages = computePageBreaks(blocks, constraints, createFontkitMeasurementProvider())
    ```
    Thread `pages` into the existing `createElement(PDFComponent, { ..., pages })` call. `contentKey` stays unchanged (page count remains DERIVED information — do NOT add it there).

    Add `pages?: PageAssignment[]` to `EstimatePDFProps` in BOTH `estimate-pdf.tsx` and `estimate-pdf-modern.tsx` (Plan-checker blocker 3 — declared here as a TYPE ONLY; the render function does not yet consume it — Task 2 wires the actual `pages.map()` usage, keeping this task's own commit a pure additive no-op for every existing caller).

    Extend `tests/unit/estimate/pt-px-conversion-source.test.ts`'s `CLEAN_SOURCES` array to ALSO include `'lib/pdf/render-estimate-pdf.ts'` and `'lib/pdf/measure-header-height.ts'` (both must always read `ESTIMATE_PAGE_GEOMETRY`/`LETTER_HEIGHT_PT` from `tokens.ts`, never a bare `612`/`792`/`816`/`1056` literal).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/measure-header-height.test.ts tests/unit/estimate/pt-px-conversion-source.test.ts && npx tsc -p tsconfig.ci.json --noEmit</automated>
  </verify>
  <done>measureHeaderHeightPt uses max(leftColumn, rightColumn) + chrome and passes both directional behavior cases; render-estimate-pdf.ts computes `pages` via the real pipeline (with a flat per-page safetyMarginPt, not per-block) and passes it into both templates' now-optional `pages` prop; contentKey is unchanged; pt-px-conversion-source.test.ts's CLEAN_SOURCES includes both new files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire N explicit &lt;Page&gt; composition via ONE renderBlockForKind dispatcher + shared test fixture helper + baseline-order update (no red window)</name>
  <files>components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx, tests/unit/pdf/_pages-for-fixture.ts, tests/unit/estimate/fixtures/document-fixtures.ts, tests/unit/pdf/estimate-pdf-baseline-order.test.tsx</files>
  <read_first>184-RESEARCH.md's "Explicit multi-page composition sketch"; 184-04-SUMMARY.md (exact prop signatures for the split section pieces + PdfPhotoGrid/PdfTermsSection/PdfTermsCard); 184-02-SUMMARY.md (PageBlockRef); 184-RESEARCH.md's Pitfall 3</read_first>
  <behavior>
    - `pages.length === 1` (small/today's baseline fixture) → renders exactly 1 `<Page>`, with text-content order IDENTICAL to today.
    - `pages.length > 1` (a genuinely multi-page fixture, forced via a new `buildMultiPageFixtureEstimate()` in `document-fixtures.ts`) → renders exactly that many `<Page>` elements.
    - **Single dispatcher, no special-casing (Plan-checker blockers 10+11):** ALL 11 `PageBlockKind`s, INCLUDING `prepared-by`, render through ONE `renderBlockForKind(block)` function called via `page.blocks.map(renderBlockForKind)` — there is NO separate `page.blocks.some(b => b.kind === 'prepared-by')` gate anywhere in either template.
    - **Table header rule (Plan-checker blocker 12):** a `section-header` block ALWAYS triggers `PdfTableHeaderOnly` immediately after it (every section, on the page where it starts, gets its column header exactly once) — INDEPENDENTLY, `page.continuesTable` triggers ONE MORE `PdfTableHeaderOnly` at the very top of that page's content (before any dispatched block) — these two triggers never both fire on the same page (a `continuesTable` page's first block is `item-row`, never `section-header`).
    - **Terms cards via ref (Plan-checker blocker 14):** a `terms-card` block renders via `PdfTermsCard({ title: L[block.ref.termsKey], text: <the matching estimate/company field>, titleColor: block.ref.termsKey === 'estimate' ? brandText : undefined, styles })` — one call per block, driven by `block.ref.termsKey`, never a whole-section render.
    - **Item-row grouping via ref (Plan-checker blocker 13):** contiguous `item-row` blocks belonging to the same `ref.sectionId` are grouped into ONE `PdfSectionRows({ items, startIndex: firstBlock.ref.itemIndex })` call, where `items` is the slice of that section's FULL filtered item list from `firstBlock.ref.itemIndex` to `lastBlock.ref.itemIndex + 1`.
    - `PdfFooter`'s existing `render={({pageNumber,totalPages}) => ...}` callback requires ZERO code changes.
    - `page1Only` chrome (`title-banner`/`info-grid`/`summary` ONLY — NOT `prepared-by`, per Plan 184-02) renders wherever its block actually appears in `page.blocks`, via the SAME `renderBlockForKind` dispatcher as everything else — no `i === 0` literal anywhere.
    - The shared helper `buildPagesForFixture(estimateFixture, companyFixture, templateId, opts?)` (new, in `tests/unit/pdf/_pages-for-fixture.ts`) runs the REAL `blocksFromModel` + `computePageBreaks` + `createFontkitMeasurementProvider()` pipeline against a test fixture and returns `PageAssignment[]`, ready to pass as the `pages` prop — used by `estimate-pdf-baseline-order.test.tsx` (this task) and by every other direct-call test (Task 3).
    - `tests/unit/pdf/estimate-pdf-baseline-order.test.tsx`'s existing assertions (Company→ESTIMATE→Project→Labor→Materials→Subtotal→...→Warranty→[Signature]→Photos) still hold UNCHANGED for the small (single-page) baseline fixture, now computing `pages` via `buildPagesForFixture` instead of omitting the prop.
  </behavior>
  <action>
    Create `tests/unit/pdf/_pages-for-fixture.ts` exporting `buildPagesForFixture(estimate, company, templateId, opts?)` — a thin wrapper calling `blocksFromModel(...)` (per 184-03-SUMMARY.md's input shape, deriving `resolvedSettings`/`dep`/etc from the estimate fixture the same way `estimate-pdf.tsx` itself does) then `computePageBreaks(blocks, constraints, createFontkitMeasurementProvider())`, using `ESTIMATE_PAGE_GEOMETRY[templateId]` + a header-height computed via `measureHeaderHeightPt` + `SAFETY_MARGIN_LINES`-derived `safetyMarginPt` (mirroring Task 1's resolver logic, since tests bypass `render-estimate-pdf.ts` and call the templates directly).

    Add `pages: PageAssignment[]` to `EstimatePDFProps` in both templates (Task 1 made it optional; Task 2 makes actual USE of it — this is the point where the type effectively becomes load-bearing for callers that want correct output). Replace the single `<Page size="LETTER" style={styles.page}>...</Page>` with `pages.map((page, i) => <Page key={i} size="LETTER" style={styles.page}>{renderPageContent(page)}</Page>)`. `renderPageContent(page)` renders, in order: `PdfHeader(...)` (always, unchanged, still `fixed`); `page.continuesTable && PdfTableHeaderOnly({...})` (the continuation trigger); then `page.blocks.map(block => renderBlockForKind(block))`; then `PdfFooter(...)` (always, last).

    Implement `renderBlockForKind(block)` as the ONE dispatcher handling all 11 kinds:
    - `'title-banner'` → `PdfTitleBanner({...})`
    - `'info-grid'` → `PdfInfoGrid({...})`
    - `'summary'` → the inline summary `<View>`
    - `'section-header'` → `PdfSectionHeader({...})` immediately followed by `PdfTableHeaderOnly({...})` (unconditional per section-start)
    - `'item-row'` (grouped by contiguous `ref.sectionId` run) → ONE `PdfSectionRows({ items, startIndex: block.ref.itemIndex, styles })` call per run
    - `'section-subtotal'` → `PdfSectionSubtotal({...})`
    - `'totals'` → `PdfTotalsBlock({...})`
    - `'terms-card'` → `PdfTermsCard({...})` per `block.ref.termsKey` (see `<behavior>`)
    - `'signature'` → `PdfSignatureBlock({...})`
    - `'photo-row'` → `PdfPhotoGrid({ photos: attachedPhotos.slice(...block.ref.photoRange), contentWidthPt, showLabel: block.ref.photoRange[0] === 0, ... })`
    - `'prepared-by'` → the inline prepared-by `<View>` (NO special-casing — reached via the SAME dispatcher, in normal `page.blocks` order, exactly like every other kind)

    Do not remove existing `wrap={false}` atomicity guards — they stay as a redundant safety net. Do not introduce any NEW reliance on `wrap`/`minPresenceAhead` for break decisions.

    Add `buildMultiPageFixtureEstimate()` (or similar) to `document-fixtures.ts` with enough sections/items (e.g. 4 sections × 10 items, medium-length descriptions) to force `pages.length >= 3` under Classic's real geometry via `buildPagesForFixture` — tune empirically.

    Update `estimate-pdf-baseline-order.test.tsx`'s `renderTexts()`/`renderTextsSignedWithPhoto()` helpers to compute `pages` via `buildPagesForFixture(estimate, FIXTURE_COMPANY, 'classic')` (or the relevant template) before calling the template function — confirm the EXISTING assertions still hold unchanged for the small (single-page) baseline fixture. Add a comment per Pitfall 3: this file covers the SMALL/single-page case only.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-baseline-order.test.tsx && npx tsc -p tsconfig.ci.json --noEmit</automated>
  </verify>
  <done>Both templates render exactly `pages.length` `<Page>` elements via ONE `renderBlockForKind` dispatcher covering all 11 kinds (prepared-by included, no special-casing); table headers follow both triggers correctly; the shared `buildPagesForFixture` helper exists; baseline-order test is updated in THIS task and passes — no red window at this commit.</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining direct-call tests via the shared helper + real PDF-byte page-count assertion + determinism test</name>
  <files>tests/unit/pdf/render-estimate-pdf-resolver.test.ts, tests/unit/pdf/estimate-pdf-banner-fill.test.tsx, tests/unit/pdf/estimate-pdf-signature.test.tsx, tests/unit/pdf/estimate-pdf-modern-signature.test.tsx, tests/unit/pdf/estimate-pdf-totals.test.tsx, tests/unit/pdf/estimate-pdf-modern-totals.test.tsx, tests/unit/pdf/estimate-pdf-photo-captions.test.tsx, tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx, tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx, tests/unit/pdf/estimate-pdf-pagination.test.tsx</files>
  <read_first>tests/unit/pdf/render-estimate-pdf-resolver.test.ts (current `BASE_ESTIMATE` mock — mocks `createElement`/`renderToBuffer`, does NOT mock `blocksFromModel`/`computePageBreaks`, so the real pipeline now runs against this mock during the test); tests/unit/pdf/_pages-for-fixture.ts (Task 2's helper); tests/unit/estimate/_pdf-text-walker.ts</read_first>
  <action>
    Update EACH of the first 9 files above to compute a real `pages` prop via `buildPagesForFixture(...)` (Task 2's helper) before calling `EstimatePDF`/`EstimatePDFModern` directly, instead of omitting the prop — none of these files' EXISTING assertions should need to change beyond adding this one setup step (their content-order/style/caption/atomicity assertions are unaffected by pagination becoming real, since all their fixtures stay single-page).

    In `render-estimate-pdf-resolver.test.ts` specifically (Plan-checker blocker 2): add `sections: []` and `presentation_settings: null` to `BASE_ESTIMATE` (so the now-wired `blocksFromModel` call inside `renderEstimatePdf` — which this test does NOT mock — receives a minimal-but-valid shape; `blocksFromModel`'s Plan 184-03 defensive `input.sections ?? []` handles the case where a test provides no sections at all, but adding `sections: []` explicitly here keeps the mock self-documenting).

    Create `tests/unit/pdf/estimate-pdf-pagination.test.tsx` (`describe.each` over both templates), using `buildPagesForFixture`:
    - Multi-page fixture (`buildMultiPageFixtureEstimate()`, Task 2) → the returned tree contains exactly `pages.length` `<Page>` elements; assert `pages.length >= 3` explicitly in THIS file (Plan-checker blocker 27 — this is the ONLY place that assertion lives; `document-fixtures.ts` itself carries no pipeline-invoking assertion).
    - Single-page fixture → exactly 1 `<Page>` element.
    - Every genuine continuation page (`continuesTable` was true) contains the column-header labels (`L.description`/`L.qty`/`L.unit`/`L.unitPrice`/`L.total`) exactly once, scoped to that page's subtree (reuse `collectTextNodes` per-`<Page>`).
    - **Real PDF-byte page count (Plan-checker blocker 16):** a REAL (unmocked) `renderToBuffer` call on the multi-page fixture for BOTH templates; assert `(buf.toString('latin1').match(/\/Type\s*\/Page[^s]/g) ?? []).length === pages.length` (the actual generated PDF has exactly as many `/Type /Page` objects as the engine computed — NOT merely "renderToBuffer resolved without throwing"). Repeat for the single-page fixture (expect `1`).
    - Determinism: run `blocksFromModel` + `computePageBreaks` TWICE on the identical multi-page fixture input; `JSON.stringify(pagesRun1) === JSON.stringify(pagesRun2)`.
  </action>
  <acceptance_criteria>
    - `grep "pages.length >= 3" tests/unit/pdf/estimate-pdf-pagination.test.tsx` matches (and does NOT need to match in `document-fixtures.ts` — Plan-checker blocker 27)
    - `grep "Type\\\\s\\*\\\\/Page" tests/unit/pdf/estimate-pdf-pagination.test.tsx` matches (the real-PDF-byte regex assertion exists)
    - `npx vitest run tests/unit/pdf/estimate-pdf-pagination.test.tsx` includes a passing determinism assertion and a passing real-page-count assertion for both templates
    - `npx vitest run tests/unit/pdf/render-estimate-pdf-resolver.test.ts` — green with the updated `BASE_ESTIMATE`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf</automated>
  </verify>
  <done>All 10 listed test files pass; the new pagination test proves real page count (via actual PDF bytes, not just non-throwing), repeated headers, and determinism; the resolver test's BASE_ESTIMATE mock is defensively complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Write UAT PDF artifacts to disk + durable UAT checklist + manual visual verification</name>
  <files>.planning/phases/184-consolidated-pagination-engine/uat/classic-1page.pdf, .planning/phases/184-consolidated-pagination-engine/uat/classic-multipage.pdf, .planning/phases/184-consolidated-pagination-engine/uat/modern-1page.pdf, .planning/phases/184-consolidated-pagination-engine/uat/modern-multipage.pdf, .planning/phases/184-consolidated-pagination-engine/184-HUMAN-UAT.md</files>
  <action>
    Run the full suite gate first (`npx vitest run tests/unit tests/eval && npx tsc -p tsconfig.ci.json --noEmit`). Then, using the single-page and multi-page fixtures + `buildPagesForFixture` + a real (unmocked) `renderToBuffer` call, generate and WRITE 4 real PDF files to disk (Plan-checker blocker 18 — durable evidence, survives auto-approval):
    - `.planning/phases/184-consolidated-pagination-engine/uat/classic-1page.pdf`
    - `.planning/phases/184-consolidated-pagination-engine/uat/classic-multipage.pdf`
    - `.planning/phases/184-consolidated-pagination-engine/uat/modern-1page.pdf`
    - `.planning/phases/184-consolidated-pagination-engine/uat/modern-multipage.pdf`

    Create `.planning/phases/184-consolidated-pagination-engine/184-HUMAN-UAT.md` with frontmatter `status: partial` and a body containing: (a) the 4 artifact paths above; (b) a checklist mirroring the phase's Manual-Only Verification (184-VALIDATION.md): no line-item row splits mid-page, section headers stay with their first row, subtotals stay with their last row, totals/signature/terms cards never split, continuation pages repeat the column header exactly once, every page shows "Page N of M", the 1-page PDFs look visually unchanged from pre-184. Leave each checklist item unchecked (`- [ ]`) — this file records what a human should verify, not that they have.
  </action>
  <what-built>The consolidated pagination engine (lib/estimate/pagination/) fully wired into both PDF templates: explicit N-page composition via one uniform block dispatcher, repeated continuation table headers, atomic totals/signature/terms-cards/photo-rows, row/header/subtotal keep-together rules (including the 1-item-section maximal chain), and a measurement-drift-validated per-page safety margin.</what-built>
  <how-to-verify>
    1. Confirm `npx vitest run tests/unit tests/eval` and `npx tsc -p tsconfig.ci.json --noEmit` are clean.
    2. Open the 4 written PDFs under `.planning/phases/184-consolidated-pagination-engine/uat/` and check off `184-HUMAN-UAT.md`'s checklist against them: no split rows, correct keep-together, repeated continuation headers, correct "Page N of M", and the 1-page PDFs look unchanged from before this phase.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <verify>
    <manual>Open all 4 PDFs under .planning/phases/184-consolidated-pagination-engine/uat/ and check off 184-HUMAN-UAT.md's checklist.</manual>
  </verify>
  <done>4 UAT PDFs exist on disk; 184-HUMAN-UAT.md exists with status: partial and a complete checklist; user confirms multi-page and single-page PDFs render correctly for both templates with no atomicity or continuation regressions.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit tests/eval` — full suite green (matches CI's `test.yml` gate).
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
4 UAT PDFs on disk + `184-HUMAN-UAT.md` present.
Manual PDF visual check per the checkpoint task above.
</verification>

<success_criteria>
Both PDF templates render N explicit `<Page>` elements computed by the ONE shared `computePageBreaks()` function via a single uniform block dispatcher (no special-cased kinds); every locked break rule holds on a genuinely multi-page fixture, including the 1-item-section maximal chain; the real generated PDF byte stream's page count matches the engine's; the single-page case is visually unchanged; the pipeline is proven deterministic; durable UAT evidence is written to disk. This closes PGBRK-01 through PGBRK-05 for the PDF surface — Phase 185 wires the same `lib/estimate/pagination/measure/types.ts` `MeasurementProvider` contract and `computePageBreaks()` function into the web paginated preview.
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-05-SUMMARY.md`. It MUST document, for Phase 185's benefit:
- The exact `MeasurementProvider` interface (`lib/estimate/pagination/measure/types.ts`) Phase 185's DOM implementation must satisfy.
- The exact `computePageBreaks(blocks, constraints, measurementProvider): PageAssignment[]` contract (3-arg — no `safetyMarginLines`) Phase 185's web preview will call with a DOM-backed provider instead of `createFontkitMeasurementProvider()`, including how `constraints.safetyMarginPt` should be derived for the DOM case.
- `blocksFromModel()`'s input shape (Phase 185 reuses it verbatim — same blocks, different measurement provider).
- The final `SAFETY_MARGIN_LINES` value and where it's sourced from.
- The `buildPagesForFixture` test helper's signature (Phase 185's own tests may want an equivalent).
</output>
