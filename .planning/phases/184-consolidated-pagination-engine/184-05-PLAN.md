---
phase: 184-consolidated-pagination-engine
plan: 05
type: execute
wave: 4
depends_on: ["184-01", "184-02", "184-03", "184-04"]
files_modified:
  - lib/pdf/measure-header-height.ts
  - lib/pdf/render-estimate-pdf.ts
  - components/pdf/estimate-pdf.tsx
  - components/pdf/estimate-pdf-modern.tsx
  - tests/unit/pdf/measure-header-height.test.ts
  - tests/unit/pdf/render-estimate-pdf-resolver.test.ts
  - tests/unit/estimate/pt-px-conversion-source.test.ts
  - tests/unit/pdf/_pages-for-fixture.ts
  - tests/unit/estimate/fixtures/document-fixtures.ts
  - tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
  - tests/unit/pdf/estimate-pdf-banner-fill.test.tsx
  - tests/unit/pdf/estimate-pdf-signature.test.tsx
  - tests/unit/pdf/estimate-pdf-modern-signature.test.tsx
  - tests/unit/pdf/estimate-pdf-totals.test.tsx
  - tests/unit/pdf/estimate-pdf-modern-totals.test.tsx
  - tests/unit/pdf/estimate-pdf-photo-captions.test.tsx
  - tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx
  - tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx
  - tests/unit/pdf/estimate-pdf-pagination.test.tsx
  - .planning/phases/184-consolidated-pagination-engine/184-HUMAN-UAT.md
autonomous: false
requirements: [PGBRK-01, PGBRK-03, PGBRK-04]

must_haves:
  truths:
    - "In a multi-page PDF, a line-item row is never split across a page break; a section header always renders on the same page as its first item row; a section's subtotal always renders on the same page as that section's last item row."
    - "The totals block, the signature block, and each terms card each render fully on one page."
    - "Every PDF page after page 1 repeats the items-table column header, and every page shows Page N of M."
    - "For the same estimate and template, the pagination module is the one function both the PDF renderer and (in Phase 185) the web measurement provider call — verified by tests that the same block inputs always produce the same page/block assignment."
    - "The real generated PDF byte stream has exactly as many /Type /Page objects as the engine's computed page count (not just 'renderToBuffer didn't throw')."
    - "Every terms-card block renders the correct title/text for its termsKey (all 5, not just 3/5) via one explicit, pinned mapping — not a broken L[key] lookup."
  artifacts:
    - path: "lib/pdf/measure-header-height.ts"
      provides: "measureHeaderHeightPt(company, templateId) — data-dependent header height using max(leftColumn, rightColumn) + chrome, computed per render, never a hardcoded literal"
      contains: "export function measureHeaderHeightPt"
    - path: "tests/unit/pdf/_pages-for-fixture.ts"
      provides: "buildPagesForFixture(estimate, company, templateId, opts) — the ONE shared helper every direct-call PDF test uses to compute a real `pages` prop via the actual blocksFromModel+computePageBreaks pipeline"
      contains: "export function buildPagesForFixture"
    - path: "tests/unit/pdf/estimate-pdf-pagination.test.tsx"
      provides: "N-explicit-Page structural tests, repeated table header assertion, REAL PDF-object page-count regex assertion, renderToBuffer smoke on a multi-page fixture, determinism test"
    - path: ".planning/phases/184-consolidated-pagination-engine/184-HUMAN-UAT.md"
      provides: "Durable UAT checklist + artifact paths (status: partial) so manual-verification evidence survives even if the checkpoint gets auto-approved"
  key_links:
    - from: "lib/pdf/render-estimate-pdf.ts"
      to: "lib/estimate/pagination/engine.ts"
      via: "computePageBreaks(blocksFromModel(...), constraints, createFontkitMeasurementProvider()) called before createElement; constraints.safetyMarginPt derived from SAFETY_MARGIN_LINES * a representative line-height-in-pt"
      pattern: "computePageBreaks"
    - from: "components/pdf/estimate-pdf.tsx"
      to: "pages?: PageAssignment[] prop"
      via: "pages.map((page,i) => <Page key={i} .../>) replaces the single static <Page>; ALL 11 block kinds (incl. prepared-by) dispatch through ONE renderBlockForKind, each keyed by block.id"
      pattern: "pages\\.map|renderBlockForKind"
    - from: "components/pdf/estimate-pdf.tsx"
      to: "components/pdf/shared/pdf-terms-section.tsx"
      via: "terms-card dispatch uses an explicit termsKey -> {title, text, titleColor} map mirroring pdf-terms-section.tsx:68-99 (not a broken L[termsKey] lookup)"
      pattern: "Estimate Terms"
    - from: "components/pdf/estimate-pdf.tsx"
      to: "components/pdf/shared/pdf-section-block.tsx"
      via: "PdfTableHeaderOnly ALWAYS rendered right after PdfSectionHeader, PLUS once more at page top iff page.continuesTable"
      pattern: "continuesTable"
    - from: "tests/unit/pdf/estimate-pdf-pagination.test.tsx"
      to: "renderToBuffer output"
      via: "(buf.toString('latin1').match(/\\/Type\\s*\\/Page[^s]/g) ?? []).length === pages.length"
      pattern: "Type\\\\s\\*\\\\/Page"
---

<revision_note>
**Final revision per plan-checker re-verification.** Changes from the prior version:
- **Blocker 3:** the terms-card dispatch no longer uses the broken `L[block.ref.termsKey]` lookup (which only works for a property literally named `estimate`/`payment`/etc — it does NOT for `payment`→should be `L.paymentTerms` not `L.payment`, breaking 2/5 keys). FIX: an explicit map mirroring `pdf-terms-section.tsx:68-99` exactly (see `<terms_card_map>`).
- **Blocker 4:** the red window at T2's commit is now fully closed — `buildPagesForFixture` threading for ALL pre-existing direct-call test files (banner-fill, signature, modern-signature, totals, modern-totals, photo-captions, photo-grid-atomicity, terms-atomicity) is now folded INTO T2 (added to T2's `<files>`), alongside the already-planned baseline-order update. T2's `<automated>` verify becomes the BROAD `npx vitest run tests/unit/pdf` (catches all of them at T2's own commit). T3 is now reserved SOLELY for the brand-new `estimate-pdf-pagination.test.tsx`.
- **Warning 5:** the dispatcher sets `key={block.id}` on every `renderBlockForKind` return (React array-sibling correctness).
- **Warning 7:** `render-estimate-pdf-resolver.test.ts`'s `BASE_ESTIMATE` fix (`sections: []`, `presentation_settings: null`) MOVES from T3 into T1 (the task that actually wires the resolver) — added to T1's `<files>` and `<automated>` verify.
- **Warning 8:** `measureHeaderHeightPt`'s left-column formula corrected — `companyName` line (`18 × LINE_HEIGHT['Inter-Bold'] + marginBottom 4`) + optional contact line (`9 × 1.5`) + optional address line (`9 × 1.5`), with NO `headerLeft.gap` term (the prior draft incorrectly added it). Right column = `langBadge` line + (`logo_url` ? `headerRight.gap + logoHeightPt` : `0`).
- **Warning 9:** `PdfTermsCard`/`PdfPhotoGrid` calls now pass `topMarginPt` ONLY to the first card / first photo-row (`ref.photoRange[0] === 0` for photos; a one-time-computed `firstTermsCardBlockId` for terms).
- **Warning 10:** call sites pass `ESTIMATE_PAGE_GEOMETRY.classic.contentWidthPt` / `.modern.contentWidthPt` — no bare `532`/`508` literal anywhere in this plan's own code either.
- **Warning 11:** the dispatcher's item-row grouping now derives each section's full filtered item list via the SHARED `visibleSectionItems(section)` (Plan 184-01) — no third re-implementation of the empty-description filter.
- **Warning 12:** replaced the imprecise "these two triggers never both fire on the same page" claim with the more precise, checker-specified restatement.
</revision_note>

<objective>
Wire the deterministic pagination engine (184-02/03) and the atomicity-fixed components (184-04) into both PDF templates for real: replace each template's single `<Page wrap>` with N explicit `<Page>` elements computed by `computePageBreaks()`, repeat the items-table column header on continuation pages, correctly render every terms-card via an explicit pinned mapping, and prove it all with structural + determinism + real-PDF-byte + `renderToBuffer` tests — plus durable, on-disk UAT evidence.

Purpose: This is the phase's payoff task — PGBRK-01/03/04 all land here. Everything in Waves 1-3 exists specifically so this plan can replace emergent Yoga `wrap` with prescribed, explicit page breaks.
Output: Both PDF templates accept an optional `pages: PageAssignment[]` prop and render exactly that many `<Page>`s via ONE uniform, correctly-keyed block dispatcher; `render-estimate-pdf.ts` computes `pages` before rendering; a genuinely multi-page fixture proves the whole pipeline end-to-end; 4 UAT PDFs + a checklist doc are written to disk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-01-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-02-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-03-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-04-SUMMARY.md
@lib/pdf/render-estimate-pdf.ts
@components/pdf/shared/pdf-header.tsx
@lib/estimate/document/tokens.ts
@lib/estimate/document/visible-items.ts
@tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
@tests/unit/estimate/fixtures/document-fixtures.ts
@tests/unit/pdf/render-estimate-pdf-resolver.test.ts

<interfaces>
From Plan 184-02 (`lib/estimate/pagination/engine.ts` + `types.ts`):
```ts
export function computePageBreaks(blocks: PageBlock[], constraints: PageConstraints, measurementProvider: MeasurementProvider): PageAssignment[]
export interface PageConstraints { contentHeightPt: number; continuationTableHeaderHeightPt: number; safetyMarginPt: number }
export interface PageAssignment { pageIndex: number; blocks: PageBlock[]; continuesTable: boolean }
export interface PageBlockRef { sectionId?: string; itemId?: string; itemIndex?: number; termsKey?: 'estimate'|'payment'|'timeline'|'warranty'|'notes'; photoRange?: [number, number] }
```

From Plan 184-01 (`lib/estimate/pagination/measure/safety-margin.ts` + `lib/estimate/document/tokens.ts` + `visible-items.ts`):
```ts
export const SAFETY_MARGIN_LINES: number
export const LINE_HEIGHT: Record<string, number>
export const ESTIMATE_PAGE_GEOMETRY: Record<EstimateTemplateId, EstimatePageGeometry>
export function visibleSectionItems(section: DocumentSection): DocumentItem[]
```

From Plan 184-03 (`lib/estimate/pagination/measure/estimator.ts` + `blocks-from-model.ts`):
```ts
export function createFontkitMeasurementProvider(): MeasurementProvider
export function blocksFromModel(input): PageBlock[]  // exact input shape: see 184-03-SUMMARY.md; input.sections defaults to []; terms-cards emitted estimate->payment->timeline->warranty->notes
```

From Plan 184-04 (`components/pdf/shared/pdf-section-block.tsx`, `pdf-photo-grid.tsx`, `pdf-terms-section.tsx`, `pdf-totals-block.tsx`):
```ts
export function PdfSectionHeader({ sectionId, ... }): ReactElement       // key={`${sectionId}-header`}
export function PdfTableHeaderOnly({ sectionId, ... }): ReactElement    // key={`${sectionId}-thead`}
export function PdfSectionRows({ sectionId, items, startIndex, styles }): ReactElement  // key={`${sectionId}-rows`}
export function PdfSectionSubtotal({ sectionId, ... }): ReactElement    // key={`${sectionId}-subtotal`}
export function PdfPhotoGrid({ photos, contentWidthPt, showLabel, topMargin, ... }): ReactElement
export function PdfTermsSection(props): ReactElement  // internally per-card wrap={false}
export function PdfTermsCard({ title, text, titleColor, topMarginPt, styles }): ReactElement
// PdfTotalsBlock's outer container is now wrap={false} in both variants
```

Page geometry (from `lib/estimate/document/tokens.ts`'s `ESTIMATE_PAGE_GEOMETRY` — Plan 184-01, read from there, never inlined):

| Template | contentWidthPt | topPaddingPt | bottomPaddingPt |
|---|---|---|---|
| Classic | 532 | 40 | 60 |
| Modern | 508 | 52 | 68 |

`PdfHeader`'s live layout (`components/pdf/shared/pdf-header.tsx` + each template's `styles`) — a flex ROW with a LEFT column (name/contact/address stacked) and a RIGHT column (langBadge/logo stacked). The rendered row height is `max(leftColumnHeight, rightColumnHeight)`, NOT the sum of both columns.
</interfaces>

<terms_card_map>
**Plan-checker blocker 3 — the EXACT dispatch map (mirrors `pdf-terms-section.tsx:68-99` verbatim). Task 2's `renderBlockForKind` for `'terms-card'` MUST use this literal lookup by `block.ref.termsKey`, never `L[block.ref.termsKey]`:**

```ts
const TERMS_CARD_MAP = {
  estimate: { title: 'Estimate Terms', text: company.estimate_terms_text, titleColor: brandText },
  payment:  { title: L.paymentTerms,   text: estimate.payment_terms,      titleColor: undefined },
  timeline: { title: L.timeline,       text: estimate.timeline,           titleColor: undefined },
  warranty: { title: L.warranty,       text: estimate.warranty_terms,     titleColor: undefined },
  notes:    { title: L.notes,          text: estimate.notes,              titleColor: undefined },
} as const
```
</terms_card_map>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data-dependent header-height measurement (corrected column formula) + resolver wiring (incl. resolver test fix) + optional `pages` prop declaration</name>
  <files>lib/pdf/measure-header-height.ts, lib/pdf/render-estimate-pdf.ts, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx, tests/unit/pdf/measure-header-height.test.ts, tests/unit/pdf/render-estimate-pdf-resolver.test.ts, tests/unit/estimate/pt-px-conversion-source.test.ts</files>
  <read_first>components/pdf/shared/pdf-header.tsx (exact conditional rendering: name always; one contact-line joined by "  |  " only if any of phone/email/website present; one address line only if formatAddress() truthy; logo 72x72 Classic/64x64 Modern only if logo_url set; `headerRight` stacks langBadge above logo with its own `gap`); lib/pdf/render-estimate-pdf.ts (renderEstimatePdf()'s current structure — insert measurement + computePageBreaks after context/photos/preparedBy resolution, before createElement); lib/estimate/document/tokens.ts (ESTIMATE_PAGE_GEOMETRY/LINE_HEIGHT — Plan 184-01); tests/unit/pdf/render-estimate-pdf-resolver.test.ts (current `BASE_ESTIMATE` mock)</read_first>
  <behavior>
    - `measureHeaderHeightPt({ name: 'Acme' }, 'classic')` (name only, no phone/email/website/address/logo) is STRICTLY LESS than `measureHeaderHeightPt({ name: 'Acme', phone: '+1...', email: 'a@b.com', website: 'https://...', address: '123 Main St', city:'Austin', state:'TX', zip:'78701', logo_url: 'https://...' }, 'classic')`.
    - **Corrected left/right column formula (Plan-checker warning 8):** given a company with a very long name (tall LEFT column) but NO `logo_url` (short RIGHT column — just langBadge), the returned height is driven by the LEFT column. Given a SHORT name but a `logo_url` set (RIGHT column taller than the short left column), the returned height is driven by the RIGHT column. Assert both directions with 2 fixtures.
    - Calling `measureHeaderHeightPt` twice with identical args returns the identical number (deterministic).
    - `'classic'` and `'modern'` template ids produce different results for the SAME company.
    - `render-estimate-pdf-resolver.test.ts`'s existing tests still pass with the resolver now actually calling `blocksFromModel`/`computePageBreaks` internally (not mocked) against its `BASE_ESTIMATE` fixture.
  </behavior>
  <action>
    Create `lib/pdf/measure-header-height.ts` exporting `measureHeaderHeightPt(company: PdfHeaderCompany, templateId: EstimateTemplateId): number` computing, per Plan-checker warning 8's corrected formula:
    ```
    leftColumnHeightPt =
        (companyNameFontSizePt * LINE_HEIGHT['Inter-Bold' | 'Lora-Bold']) + companyNameMarginBottomPt
        // Classic: 18 * LINE_HEIGHT['Inter-Bold'] + 4;  Modern: 15 * LINE_HEIGHT['Lora-Bold'] + 5
      + (any of phone/email/website present ? (9 * proseLineHeightMultiplier) : 0)   // contact line
      + (formatAddress(company) truthy ? (9 * proseLineHeightMultiplier) : 0)         // address line
      // NO headerLeft.gap term — do not add it (corrects the prior draft's error)

    rightColumnHeightPt =
        (langBadgeFontSizePt * proseLineHeightMultiplier)   // langBadge line, always present
      + (logo_url set ? (headerRight.gap + logoHeightPt) : 0)
        // Classic: headerRight.gap=6, logoHeightPt=72;  Modern: headerRight.gap=8, logoHeightPt=64

    headerRowHeightPt = max(leftColumnHeightPt, rightColumnHeightPt)
    return headerRowHeightPt + header.paddingBottom + header.marginBottom + header.borderBottomWidth
      // Classic: paddingBottom 16, marginBottom 24, borderBottomWidth 2
      // Modern:  paddingBottom 20, marginBottom 32, borderBottomWidth 0.75
    ```
    Read `companyName`/`companyContact`/`langBadge` font sizes and `headerRight.gap`/logo dimensions from each template's live StyleSheet (do not invent new numbers); read `proseLineHeightMultiplier` from `ESTIMATE_PAGE_GEOMETRY[templateId]`. Document every constant with a comment citing `pdf-header.tsx`'s structure and each template's `styles.header`/`headerRight` StyleSheet values.

    In `lib/pdf/render-estimate-pdf.ts`'s `renderEstimatePdf()`, AFTER resolving `attachedPhotos`/`preparedBy` and BEFORE `createElement(...)`, add:
    ```ts
    const geometry = ESTIMATE_PAGE_GEOMETRY[templateId]
    const headerHeightPt = measureHeaderHeightPt(company, templateId)
    const fontFamily = ESTIMATE_DESIGN_TOKENS[templateId].fontFamily
    const safetyMarginPt = SAFETY_MARGIN_LINES * (geometry.tableCellFontSizePt * LINE_HEIGHT[fontFamily])
    const constraints: PageConstraints = {
      contentHeightPt: LETTER_HEIGHT_PT - geometry.topPaddingPt - geometry.bottomPaddingPt - headerHeightPt,
      continuationTableHeaderHeightPt: /* fixed per-template constant, cite pdf-section-block.tsx's tableHeader padding — same formula 184-03's height_formulas table gives for section-header's table-header row */,
      safetyMarginPt,
    }
    const blocks = blocksFromModel({ /* estimate sections/summary/terms/signature/photos/resolvedSettings/preparedBy/templateId per 184-03-SUMMARY.md's exact input shape */ })
    const pages = computePageBreaks(blocks, constraints, createFontkitMeasurementProvider())
    ```
    Thread `pages` into the existing `createElement(PDFComponent, { ..., pages })` call. `contentKey` stays unchanged (page count remains DERIVED information — do NOT add it there).

    Add `pages?: PageAssignment[]` to `EstimatePDFProps` in BOTH `estimate-pdf.tsx` and `estimate-pdf-modern.tsx` (declared here as a TYPE ONLY; the render function does not yet consume it — Task 2 wires the actual `pages.map()` usage, keeping this task's own commit a pure additive no-op for every existing caller).

    **Plan-checker warning 7:** in `tests/unit/pdf/render-estimate-pdf-resolver.test.ts`, add `sections: []` and `presentation_settings: null` to `BASE_ESTIMATE` (so the now-wired `blocksFromModel` call inside `renderEstimatePdf` — which this test does NOT mock — receives a minimal-but-valid shape; `blocksFromModel`'s Plan 184-03 defensive `input.sections ?? []` handles a missing `sections` too, but adding it explicitly here keeps the mock self-documenting).

    Extend `tests/unit/estimate/pt-px-conversion-source.test.ts`'s `CLEAN_SOURCES` array to ALSO include `'lib/pdf/render-estimate-pdf.ts'` and `'lib/pdf/measure-header-height.ts'` (both must always read `ESTIMATE_PAGE_GEOMETRY`/`LETTER_HEIGHT_PT` from `tokens.ts`, never a bare `612`/`792`/`816`/`1056` literal).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/measure-header-height.test.ts tests/unit/pdf/render-estimate-pdf-resolver.test.ts tests/unit/estimate/pt-px-conversion-source.test.ts && npx tsc -p tsconfig.ci.json --noEmit</automated>
  </verify>
  <done>measureHeaderHeightPt uses the corrected max(leftColumn, rightColumn) formula (no headerLeft.gap) and passes both directional behavior cases; render-estimate-pdf.ts computes `pages` via the real pipeline (flat per-page safetyMarginPt) and passes it into both templates' now-optional `pages` prop; render-estimate-pdf-resolver.test.ts passes with its defensively-completed BASE_ESTIMATE; contentKey unchanged; pt-px-conversion-source.test.ts's CLEAN_SOURCES includes both new files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire N explicit &lt;Page&gt; composition via ONE keyed renderBlockForKind dispatcher + thread `pages` through EVERY direct-call test (no red window)</name>
  <files>components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx, tests/unit/pdf/_pages-for-fixture.ts, tests/unit/estimate/fixtures/document-fixtures.ts, tests/unit/pdf/estimate-pdf-baseline-order.test.tsx, tests/unit/pdf/estimate-pdf-banner-fill.test.tsx, tests/unit/pdf/estimate-pdf-signature.test.tsx, tests/unit/pdf/estimate-pdf-modern-signature.test.tsx, tests/unit/pdf/estimate-pdf-totals.test.tsx, tests/unit/pdf/estimate-pdf-modern-totals.test.tsx, tests/unit/pdf/estimate-pdf-photo-captions.test.tsx, tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx, tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx</files>
  <read_first>184-RESEARCH.md's "Explicit multi-page composition sketch"; 184-04-SUMMARY.md (exact prop signatures for the split section pieces + PdfPhotoGrid/PdfTermsSection/PdfTermsCard); 184-02-SUMMARY.md (PageBlockRef); 184-RESEARCH.md's Pitfall 3; the `<terms_card_map>` above</read_first>
  <behavior>
    - `pages.length === 1` (small/today's baseline fixture) → renders exactly 1 `<Page>`, with text-content order IDENTICAL to today.
    - `pages.length > 1` (a genuinely multi-page fixture, forced via a new `buildMultiPageFixtureEstimate()` in `document-fixtures.ts`) → renders exactly that many `<Page>` elements.
    - **Single dispatcher, no special-casing:** ALL 11 `PageBlockKind`s, INCLUDING `prepared-by`, render through ONE `renderBlockForKind(block)` function called via `page.blocks.map(block => renderBlockForKind(block))`, each return value carrying `key={block.id}` (Plan-checker warning 5) — there is NO separate `page.blocks.some(b => b.kind === 'prepared-by')` gate anywhere in either template.
    - **Table header rule:** a `section-header` block ALWAYS triggers `PdfTableHeaderOnly` immediately after it (every section, on the page where it starts, gets its column header exactly once) — INDEPENDENTLY, `page.continuesTable` triggers ONE MORE `PdfTableHeaderOnly` at the very top of that page's content (before any dispatched block). **Precise relationship (Plan-checker warning 12 — replaces the earlier imprecise "never both fire" claim):** the page-top `continuesTable` trigger NEVER fires for a page whose first block is a `section-header` — by construction, `continuesTable` is defined as `page.blocks[0]?.kind === 'item-row'`, which is mutually exclusive with the first block being a `section-header` on that same page.
    - **Terms cards via the explicit map (Plan-checker blocker 3):** a `terms-card` block renders via `PdfTermsCard({ ...TERMS_CARD_MAP[block.ref.termsKey], topMarginPt: block.id === firstTermsCardBlockId ? (24|32) : undefined, styles })` — the `TERMS_CARD_MAP` from `<terms_card_map>` above, NEVER `L[block.ref.termsKey]`.
    - **First-block topMarginPt (Plan-checker warning 9):** `firstTermsCardBlockId` is computed ONCE (before the page-render loop) as the `id` of the first block across ALL pages with `kind === 'terms-card'`; `photo-row` blocks pass `showLabel`/the topMargin-bearing `topMargin` prop based purely on `block.ref.photoRange[0] === 0` (no cross-page lookup needed, since `photoRange` already encodes position).
    - **Item-row grouping via ref + the shared filter (Plan-checker warning 11):** contiguous `item-row` blocks belonging to the same `ref.sectionId` are grouped into ONE `PdfSectionRows({ sectionId, items, startIndex: firstBlock.ref.itemIndex, styles })` call, where `items` is a slice of `visibleSectionItems(section)` (imported from `@/lib/estimate/document/visible-items` — the SAME shared function `blocksFromModel` uses, no third re-implementation) from `firstBlock.ref.itemIndex` to `lastBlock.ref.itemIndex + 1`.
    - `PdfFooter`'s existing `render={({pageNumber,totalPages}) => ...}` callback requires ZERO code changes.
    - `page1Only` chrome (`title-banner`/`info-grid`/`summary` ONLY — NOT `prepared-by`) renders wherever its block actually appears in `page.blocks`, via the SAME `renderBlockForKind` dispatcher as everything else — no `i === 0` literal anywhere.
    - The shared helper `buildPagesForFixture(estimateFixture, companyFixture, templateId, opts?)` (new, in `tests/unit/pdf/_pages-for-fixture.ts`) runs the REAL `blocksFromModel` + `computePageBreaks` + `createFontkitMeasurementProvider()` pipeline against a test fixture and returns `PageAssignment[]`, ready to pass as the `pages` prop.
    - **No red window (Plan-checker blocker 4):** EVERY direct-call PDF test file — `estimate-pdf-baseline-order.test.tsx`, `estimate-pdf-banner-fill.test.tsx`, `estimate-pdf-signature.test.tsx`, `estimate-pdf-modern-signature.test.tsx`, `estimate-pdf-totals.test.tsx`, `estimate-pdf-modern-totals.test.tsx`, `estimate-pdf-photo-captions.test.tsx`, `estimate-pdf-photo-grid-atomicity.test.tsx`, `estimate-pdf-terms-atomicity.test.tsx` — is updated in THIS task to compute and pass a real `pages` prop via `buildPagesForFixture`. At the end of THIS task's commit, `npx vitest run tests/unit/pdf` is fully green — no file is left calling the templates without `pages`.
  </behavior>
  <action>
    Create `tests/unit/pdf/_pages-for-fixture.ts` exporting `buildPagesForFixture(estimate, company, templateId, opts?)` — a thin wrapper calling `blocksFromModel(...)` (per 184-03-SUMMARY.md's input shape) then `computePageBreaks(blocks, constraints, createFontkitMeasurementProvider())`, using `ESTIMATE_PAGE_GEOMETRY[templateId]` + a header-height computed via `measureHeaderHeightPt` + `SAFETY_MARGIN_LINES`-derived `safetyMarginPt` (mirroring Task 1's resolver logic, since tests bypass `render-estimate-pdf.ts` and call the templates directly).

    Add `pages: PageAssignment[]` to `EstimatePDFProps` in both templates (Task 1 made it optional; Task 2 makes actual USE of it). Replace the single `<Page size="LETTER" style={styles.page}>...</Page>` with `pages.map((page, i) => <Page key={i} size="LETTER" style={styles.page}>{renderPageContent(page)}</Page>)`. `renderPageContent(page)` renders, in order: `PdfHeader(...)` (always, unchanged, still `fixed`); `page.continuesTable && PdfTableHeaderOnly({...})` (the continuation trigger); then `page.blocks.map(block => renderBlockForKind(block))`; then `PdfFooter(...)` (always, last).

    Implement `renderBlockForKind(block)` as the ONE dispatcher handling all 11 kinds, each return keyed by `block.id`:
    - `'title-banner'` → `PdfTitleBanner({...})`
    - `'info-grid'` → `PdfInfoGrid({...})`
    - `'summary'` → the inline summary `<View key={block.id}>`
    - `'section-header'` → `PdfSectionHeader({ sectionId: block.ref!.sectionId!, ... })` immediately followed by `PdfTableHeaderOnly({ sectionId: block.ref!.sectionId!, ... })` (unconditional per section-start)
    - `'item-row'` (grouped by contiguous `ref.sectionId` run) → ONE `PdfSectionRows({ sectionId, items, startIndex, styles })` call per run, per `<behavior>`'s "Item-row grouping" rule
    - `'section-subtotal'` → `PdfSectionSubtotal({ sectionId: block.ref!.sectionId!, ... })`
    - `'totals'` → `PdfTotalsBlock({...})`
    - `'terms-card'` → `PdfTermsCard({ ...TERMS_CARD_MAP[block.ref!.termsKey!], topMarginPt: ..., styles })` per `<terms_card_map>`
    - `'signature'` → `PdfSignatureBlock({...})`
    - `'photo-row'` → `PdfPhotoGrid({ photos: attachedPhotos.slice(...block.ref!.photoRange!), contentWidthPt: ESTIMATE_PAGE_GEOMETRY[templateId].contentWidthPt, showLabel: block.ref!.photoRange![0] === 0, topMargin: block.ref!.photoRange![0] === 0 ? (16|20) : 0, ... })` — note `contentWidthPt` comes from `ESTIMATE_PAGE_GEOMETRY`, never a bare `532`/`508` literal
    - `'prepared-by'` → the inline prepared-by `<View key={block.id}>` (NO special-casing — reached via the SAME dispatcher, in normal `page.blocks` order, exactly like every other kind)

    Do not remove existing `wrap={false}` atomicity guards — they stay as a redundant safety net. Do not introduce any NEW reliance on `wrap`/`minPresenceAhead` for break decisions.

    Add `buildMultiPageFixtureEstimate()` (or similar) to `document-fixtures.ts` with enough sections/items (e.g. 4 sections × 10 items, medium-length descriptions) to force `pages.length >= 3` under Classic's real geometry via `buildPagesForFixture` — tune empirically (no `pages.length >= 3` assertion needed in THIS file — that lives solely in Task 3's new test).

    Update `estimate-pdf-baseline-order.test.tsx`'s `renderTexts()`/`renderTextsSignedWithPhoto()` helpers to compute `pages` via `buildPagesForFixture(estimate, FIXTURE_COMPANY, 'classic')` (or the relevant template) before calling the template function — confirm the EXISTING assertions still hold unchanged for the small (single-page) baseline fixture.

    Update the OTHER 8 direct-call test files (`estimate-pdf-banner-fill.test.tsx`, `estimate-pdf-signature.test.tsx`, `estimate-pdf-modern-signature.test.tsx`, `estimate-pdf-totals.test.tsx`, `estimate-pdf-modern-totals.test.tsx`, `estimate-pdf-photo-captions.test.tsx`, `estimate-pdf-photo-grid-atomicity.test.tsx`, `estimate-pdf-terms-atomicity.test.tsx`) — for EACH, add one setup line computing `pages` via `buildPagesForFixture` before invoking `EstimatePDF`/`EstimatePDFModern`, with NO changes needed to their existing assertions (all their fixtures stay single-page, so content/order/style/atomicity assertions are unaffected by pagination becoming real).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf</automated>
  </verify>
  <done>Both templates render exactly `pages.length` `<Page>` elements via ONE keyed `renderBlockForKind` dispatcher covering all 11 kinds (prepared-by included, no special-casing; terms-card correctly mapped via the explicit map); table headers follow both triggers correctly; the shared `buildPagesForFixture` helper exists; ALL 9 direct-call test files (baseline-order + the other 8) are updated in THIS task and `npx vitest run tests/unit/pdf` is fully green at this commit — no red window.</done>
</task>

<task type="auto">
  <name>Task 3: New pagination structural test — real PDF-byte page-count assertion + repeated-header assertion + determinism test</name>
  <files>tests/unit/pdf/estimate-pdf-pagination.test.tsx</files>
  <read_first>tests/unit/pdf/_pages-for-fixture.ts (Task 2's helper); tests/unit/estimate/_pdf-text-walker.ts; tests/unit/estimate/fixtures/document-fixtures.ts (buildMultiPageFixtureEstimate, Task 2)</read_first>
  <action>
    Create `tests/unit/pdf/estimate-pdf-pagination.test.tsx` (`describe.each` over both templates), using `buildPagesForFixture`:
    - Multi-page fixture (`buildMultiPageFixtureEstimate()`, Task 2) → the returned tree contains exactly `pages.length` `<Page>` elements; assert `pages.length >= 3` explicitly in THIS file (this is the ONLY place that assertion lives — `document-fixtures.ts` itself carries no pipeline-invoking assertion).
    - Single-page fixture → exactly 1 `<Page>` element.
    - Every genuine continuation page (`continuesTable` was true) contains the column-header labels (`L.description`/`L.qty`/`L.unit`/`L.unitPrice`/`L.total`) exactly once, scoped to that page's subtree (reuse `collectTextNodes` per-`<Page>`).
    - **Real PDF-byte page count:** a REAL (unmocked) `renderToBuffer` call on the multi-page fixture for BOTH templates; assert `(buf.toString('latin1').match(/\/Type\s*\/Page[^s]/g) ?? []).length === pages.length` (the actual generated PDF has exactly as many `/Type /Page` objects as the engine computed — NOT merely "renderToBuffer resolved without throwing"). Repeat for the single-page fixture (expect `1`).
    - Determinism: run `blocksFromModel` + `computePageBreaks` TWICE on the identical multi-page fixture input; `JSON.stringify(pagesRun1) === JSON.stringify(pagesRun2)`.
  </action>
  <acceptance_criteria>
    - `grep "pages.length >= 3" tests/unit/pdf/estimate-pdf-pagination.test.tsx` matches (and does NOT need to match in `document-fixtures.ts`)
    - `grep "Type\\\\s\\*\\\\/Page" tests/unit/pdf/estimate-pdf-pagination.test.tsx` matches (the real-PDF-byte regex assertion exists)
    - `npx vitest run tests/unit/pdf/estimate-pdf-pagination.test.tsx` includes a passing determinism assertion and a passing real-page-count assertion for both templates
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-pagination.test.tsx</automated>
  </verify>
  <done>The new pagination test proves real page count (via actual PDF bytes, not just non-throwing), repeated headers, and determinism, for both templates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Write UAT PDF artifacts to disk + durable UAT checklist + manual visual verification</name>
  <files>.planning/phases/184-consolidated-pagination-engine/uat/classic-1page.pdf, .planning/phases/184-consolidated-pagination-engine/uat/classic-multipage.pdf, .planning/phases/184-consolidated-pagination-engine/uat/modern-1page.pdf, .planning/phases/184-consolidated-pagination-engine/uat/modern-multipage.pdf, .planning/phases/184-consolidated-pagination-engine/184-HUMAN-UAT.md</files>
  <action>
    Run the full suite gate first (`npx vitest run tests/unit tests/eval && npx tsc -p tsconfig.ci.json --noEmit`). Then, using the single-page and multi-page fixtures + `buildPagesForFixture` + a real (unmocked) `renderToBuffer` call, generate and WRITE 4 real PDF files to disk (durable evidence, survives auto-approval):
    - `.planning/phases/184-consolidated-pagination-engine/uat/classic-1page.pdf`
    - `.planning/phases/184-consolidated-pagination-engine/uat/classic-multipage.pdf`
    - `.planning/phases/184-consolidated-pagination-engine/uat/modern-1page.pdf`
    - `.planning/phases/184-consolidated-pagination-engine/uat/modern-multipage.pdf`

    Create `.planning/phases/184-consolidated-pagination-engine/184-HUMAN-UAT.md` with frontmatter `status: partial` and a body containing: (a) the 4 artifact paths above; (b) a checklist mirroring the phase's Manual-Only Verification (184-VALIDATION.md): no line-item row splits mid-page, section headers stay with their first row, subtotals stay with their last row, totals/signature/terms cards never split, continuation pages repeat the column header exactly once, every page shows "Page N of M", all 5 terms cards show correct titles/text when present, the 1-page PDFs look visually unchanged from pre-184. Leave each checklist item unchecked (`- [ ]`) — this file records what a human should verify, not that they have.
  </action>
  <what-built>The consolidated pagination engine (lib/estimate/pagination/) fully wired into both PDF templates: explicit N-page composition via one uniform, keyed block dispatcher, repeated continuation table headers, correctly-mapped terms cards, atomic totals/signature/terms-cards/photo-rows, row/header/subtotal keep-together rules (including the 1-item-section maximal chain and the persistent continuation-header reservation), and a measurement-drift-validated per-page safety margin.</what-built>
  <how-to-verify>
    1. Confirm `npx vitest run tests/unit tests/eval` and `npx tsc -p tsconfig.ci.json --noEmit` are clean.
    2. Open the 4 written PDFs under `.planning/phases/184-consolidated-pagination-engine/uat/` and check off `184-HUMAN-UAT.md`'s checklist against them: no split rows, correct keep-together, repeated continuation headers, correct "Page N of M", all terms cards show correct titles, and the 1-page PDFs look unchanged from before this phase.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <verify>
    <manual>Open all 4 PDFs under .planning/phases/184-consolidated-pagination-engine/uat/ and check off 184-HUMAN-UAT.md's checklist.</manual>
  </verify>
  <done>4 UAT PDFs exist on disk; 184-HUMAN-UAT.md exists with status: partial and a complete checklist; user confirms multi-page and single-page PDFs render correctly for both templates with no atomicity, continuation, or terms-mapping regressions.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit tests/eval` — full suite green (matches CI's `test.yml` gate).
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
4 UAT PDFs on disk + `184-HUMAN-UAT.md` present.
Manual PDF visual check per the checkpoint task above.
</verification>

<success_criteria>
Both PDF templates render N explicit `<Page>` elements computed by the ONE shared `computePageBreaks()` function via a single uniform, keyed block dispatcher (no special-cased kinds, no broken terms-card mapping); every locked break rule holds on a genuinely multi-page fixture, including the 1-item-section maximal chain and the persistent continuation-header reservation; the real generated PDF byte stream's page count matches the engine's; the single-page case is visually unchanged; the pipeline is proven deterministic; durable UAT evidence is written to disk. This closes PGBRK-01 through PGBRK-05 for the PDF surface — Phase 185 wires the same `lib/estimate/pagination/measure/types.ts` `MeasurementProvider` contract and `computePageBreaks()` function into the web paginated preview.
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-05-SUMMARY.md`. It MUST document, for Phase 185's benefit:
- The exact `MeasurementProvider` interface (`lib/estimate/pagination/measure/types.ts`) Phase 185's DOM implementation must satisfy.
- The exact `computePageBreaks(blocks, constraints, measurementProvider): PageAssignment[]` contract (3-arg — no `safetyMarginLines`) Phase 185's web preview will call with a DOM-backed provider instead of `createFontkitMeasurementProvider()`, including how `constraints.safetyMarginPt` should be derived for the DOM case.
- `blocksFromModel()`'s input shape (Phase 185 reuses it verbatim — same blocks, different measurement provider).
- The final `SAFETY_MARGIN_LINES` value and where it's sourced from.
- The `buildPagesForFixture` test helper's signature (Phase 185's own tests may want an equivalent).
- The `TERMS_CARD_MAP` shape (Phase 185's web renderer needs the identical mapping).
</output>
