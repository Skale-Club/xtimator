---
phase: 184-consolidated-pagination-engine
plan: 05
type: execute
wave: 3
depends_on: ["184-03", "184-04"]
files_modified:
  - lib/pdf/measure-header-height.ts
  - lib/pdf/render-estimate-pdf.ts
  - components/pdf/estimate-pdf.tsx
  - components/pdf/estimate-pdf-modern.tsx
  - tests/unit/pdf/measure-header-height.test.ts
  - tests/unit/estimate/fixtures/document-fixtures.ts
  - tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
  - tests/unit/pdf/estimate-pdf-pagination.test.tsx
autonomous: false
requirements: [PGBRK-01, PGBRK-03, PGBRK-04]

must_haves:
  truths:
    - "In a multi-page PDF, a line-item row is never split across a page break; a section header always renders on the same page as its first item row; a section's subtotal always renders on the same page as that section's last item row."
    - "The totals block, the signature block, and each terms card each render fully on one page."
    - "Every PDF page after page 1 repeats the items-table column header, and every page shows Page N of M."
    - "For the same estimate and template, the pagination module is the one function both the PDF renderer and (in Phase 185) the web measurement provider call — verified by tests that the same block inputs always produce the same page/block assignment."
  artifacts:
    - path: "lib/pdf/measure-header-height.ts"
      provides: "measureHeaderHeightPt(company, templateId) — data-dependent header height, computed per render (Pitfall 4), never a hardcoded literal"
      contains: "export function measureHeaderHeightPt"
    - path: "tests/unit/pdf/estimate-pdf-pagination.test.tsx"
      provides: "N-explicit-Page structural tests, repeated table header assertion, renderToBuffer smoke on a multi-page fixture, determinism test"
  key_links:
    - from: "lib/pdf/render-estimate-pdf.ts"
      to: "lib/estimate/pagination/engine.ts"
      via: "computePageBreaks(blocksFromModel(...), constraints, createFontkitMeasurementProvider(), SAFETY_MARGIN_LINES) called before createElement"
      pattern: "computePageBreaks"
    - from: "components/pdf/estimate-pdf.tsx"
      to: "pages: PageAssignment[] prop"
      via: "pages.map((page,i) => <Page key={i} .../>) replaces the single static <Page>"
      pattern: "pages\\.map"
    - from: "components/pdf/estimate-pdf.tsx"
      to: "components/pdf/shared/pdf-section-block.tsx"
      via: "PdfTableHeaderOnly rendered when page.continuesTable is true"
      pattern: "continuesTable"
---

<objective>
Wire the deterministic pagination engine (184-02/03) and the atomicity-fixed components (184-04) into both PDF templates for real: replace each template's single `<Page wrap>` with N explicit `<Page>` elements computed by `computePageBreaks()`, repeat the items-table column header on continuation pages, and prove it all with structural + determinism + `renderToBuffer` tests.

Purpose: This is the phase's payoff task — PGBRK-01/03/04 all land here. Everything in Waves 1-2 exists specifically so this plan can replace emergent Yoga `wrap` with prescribed, explicit page breaks.
Output: Both PDF templates accept a `pages: PageAssignment[]` prop and render exactly that many `<Page>`s; `render-estimate-pdf.ts` computes `pages` before rendering; a genuinely multi-page fixture proves the whole pipeline end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-02-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-03-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-04-SUMMARY.md
@lib/pdf/render-estimate-pdf.ts
@components/pdf/shared/pdf-header.tsx
@lib/estimate/document/tokens.ts
@tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
@tests/unit/estimate/fixtures/document-fixtures.ts

<interfaces>
From Plan 184-02 (`lib/estimate/pagination/engine.ts` + `types.ts`):
```ts
export function computePageBreaks(blocks: PageBlock[], constraints: PageConstraints, measurementProvider: MeasurementProvider, safetyMarginLines: number): PageAssignment[]
export interface PageAssignment { pageIndex: number; blocks: PageBlock[]; continuesTable: boolean }
```

From Plan 184-01 (`lib/estimate/pagination/measure/safety-margin.ts`):
```ts
export const SAFETY_MARGIN_LINES: number
```

From Plan 184-03 (`lib/estimate/pagination/measure/estimator.ts` + `blocks-from-model.ts`):
```ts
export function createFontkitMeasurementProvider(): MeasurementProvider
export function blocksFromModel(input): PageBlock[]  // exact input shape: see 184-03-SUMMARY.md
```

From Plan 184-04 (`components/pdf/shared/pdf-section-block.tsx`, `pdf-photo-grid.tsx`, `pdf-terms-section.tsx`):
```ts
export function PdfSectionHeader(props): ReactElement
export function PdfTableHeaderOnly(props): ReactElement
export function PdfSectionRows({ items, startIndex, styles }): ReactElement
export function PdfSectionSubtotal(props): ReactElement
export function PdfPhotoGrid({ photos, contentWidthPt, showLabel, ... }): ReactElement
export function PdfTermsSection(props): ReactElement  // now internally per-card wrap={false}
```

Page geometry (from `lib/estimate/document/tokens.ts` + live StyleSheets — unchanged this plan, referenced not redefined):

| Template | contentWidthPt | topPaddingPt | bottomPaddingPt |
|---|---|---|---|
| Classic | 532 | 40 | 60 |
| Modern | 508 | 52 | 68 |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data-dependent header-height measurement + resolver wiring</name>
  <files>lib/pdf/measure-header-height.ts, lib/pdf/render-estimate-pdf.ts, tests/unit/pdf/measure-header-height.test.ts</files>
  <read_first>components/pdf/shared/pdf-header.tsx (exact conditional rendering: name always; one contact-line joined by "  |  " only if any of phone/email/website present; one address line only if formatAddress() truthy; logo 72x72 Classic/64x64 Modern only if logo_url set), lib/pdf/render-estimate-pdf.ts (renderEstimatePdf()'s current structure — insert measurement + computePageBreaks after context/photos/preparedBy resolution, before createElement)</read_first>
  <behavior>
    - `measureHeaderHeightPt({ name: 'Acme' }, 'classic')` (name only, no phone/email/website/address/logo) is STRICTLY LESS than `measureHeaderHeightPt({ name: 'Acme', phone: '+1...', email: 'a@b.com', website: 'https://...', address: '123 Main St', city:'Austin', state:'TX', zip:'78701', logo_url: 'https://...' }, 'classic')` (Pitfall 4's exact fixture pattern — two DIFFERENT computed budgets, never a shared hardcoded literal).
    - Calling `measureHeaderHeightPt` twice with identical args returns the identical number (deterministic).
    - `'classic'` and `'modern'` template ids produce different results for the SAME company (different padding/logo-size tokens).
  </behavior>
  <action>
    Create `lib/pdf/measure-header-height.ts` exporting `measureHeaderHeightPt(company: PdfHeaderCompany, templateId: EstimateTemplateId): number` — sums: a fixed company-name line height, PLUS one contact-line height IFF any of `phone`/`email`/`website` is present, PLUS one address-line height IFF `formatAddress(company)` is truthy, PLUS the logo height (72pt Classic / 64pt Modern) IFF `logo_url` is set, PLUS the header's own `paddingBottom`/`marginBottom`/`borderBottomWidth` (Classic: 16+24+2; Modern: 20+32+0.75 — read the exact current values from each template's `styles.header`/`styles.headerLeft` StyleSheet). Document each constant with a comment citing `pdf-header.tsx`'s line numbers.

    In `lib/pdf/render-estimate-pdf.ts`'s `renderEstimatePdf()`, AFTER resolving `attachedPhotos`/`preparedBy` and BEFORE `createElement(...)`, add:
    ```ts
    const headerHeightPt = measureHeaderHeightPt(company, templateId)
    const geometry = templateId === 'classic'
      ? { contentWidthPt: 532, topPaddingPt: 40, bottomPaddingPt: 60 }
      : { contentWidthPt: 508, topPaddingPt: 52, bottomPaddingPt: 68 }
    const constraints = {
      contentHeightPt: LETTER_HEIGHT_PT - geometry.topPaddingPt - geometry.bottomPaddingPt - headerHeightPt,
      continuationTableHeaderHeightPt: /* fixed per-template constant, cite pdf-section-block.tsx's tableHeader padding */,
    }
    const blocks = blocksFromModel({ /* estimate sections/summary/terms/signature/photos/resolvedSettings/preparedBy + geometry per 184-03-SUMMARY.md's exact input shape */ })
    const pages = computePageBreaks(blocks, constraints, createFontkitMeasurementProvider(), SAFETY_MARGIN_LINES)
    ```
    Thread `pages` into the existing `createElement(PDFComponent, { ..., pages })` call. Add `pages: PageAssignment[]` to `EstimatePdfContext`/the props passed downstream as needed so both `resolveEstimatePdfContext`'s cache-key logic and `renderEstimatePdf`'s render path stay consistent (page count remains DERIVED information, per Open Question 3 — do NOT add it to `contentKey`).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/measure-header-height.test.ts && npx tsc -p tsconfig.ci.json --noEmit</automated>
  </verify>
  <done>measureHeaderHeightPt is exported and passes both behavior cases; render-estimate-pdf.ts computes `pages` via the real pipeline and passes it into both templates' props; contentKey is unchanged (page count stays derived, not cached separately).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire both templates to render N explicit &lt;Page&gt; elements from the `pages` prop</name>
  <files>components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx</files>
  <read_first>184-RESEARCH.md's "Explicit multi-page composition sketch"; 184-04-SUMMARY.md (exact prop signatures for the 4 split section pieces + PdfPhotoGrid/PdfTermsSection)</read_first>
  <behavior>
    - `pages.length === 1` (small/today's baseline fixture) → renders exactly 1 `<Page>`, with text-content order IDENTICAL to today (verified in Task 3's updated baseline test).
    - `pages.length > 1` (a genuinely multi-page fixture) → renders exactly that many `<Page>` elements.
    - Every page whose `continuesTable` is true renders `PdfTableHeaderOnly` as the first content block after the fixed `PdfHeader` — and a section's OWN opening page (the page containing its `section-header` block) does NOT ALSO render a redundant `PdfTableHeaderOnly` (the section-header's own table-header, rendered once per section-start, is a separate concern from continuation repeats — no double header on the same page).
    - `PdfFooter`'s existing `render={({pageNumber,totalPages}) => ...}` callback requires ZERO code changes — react-pdf computes it from the actual `<Page>` count automatically.
    - `page1Only` chrome (`title-banner`/`info-grid`/`summary`/`prepared-by`) renders only on the page where `blocksFromModel` actually placed that block kind (checked via `page.blocks.some(b => b.kind === '...')`), NOT via a bare `i === 0` literal — so behavior stays correct even when a block is absent (e.g. no summary block when `estimate.summary` is falsy).
  </behavior>
  <action>
    Add `pages: PageAssignment[]` to `EstimatePDFProps` in both `estimate-pdf.tsx` and `estimate-pdf-modern.tsx`. Replace the single `<Page size="LETTER" style={styles.page}>...</Page>` with `pages.map((page, i) => <Page key={i} size="LETTER" style={styles.page}>...</Page>)`. Inside each `<Page>`:
    1. `PdfHeader(...)` always (unchanged, still `fixed`).
    2. Gate `PdfTitleBanner`/`PdfInfoGrid`/the inline summary `<View>`/the inline prepared-by `<View>` each on `page.blocks.some(b => b.kind === 'title-banner' | 'info-grid' | 'summary' | 'prepared-by')` respectively.
    3. `page.continuesTable && PdfTableHeaderOnly({...})`.
    4. A local `renderBlockForKind(block, isFirstPhotoRowOverall)` dispatcher mapping each of the 11 `PageBlockKind`s to its component call: `section-header` → `PdfSectionHeader` PLUS (only when this page is NOT `continuesTable` for this section, i.e. this IS the section's opening page) `PdfTableHeaderOnly`; contiguous runs of `item-row` blocks → ONE `PdfSectionRows({ items, startIndex })` call per run, where `items`/`startIndex` are derived from each block's original position within its full (already-filtered) section item list; `section-subtotal` → `PdfSectionSubtotal`; `totals` → the existing `PdfTotalsBlock` call; `terms-card` → the specific term Fragment/View this card id refers to (per 184-04's now-per-card `PdfTermsSection` pieces); `signature` → `PdfSignatureBlock`; `photo-row` → `PdfPhotoGrid` sliced to that chunk's photos, with `showLabel: true` ONLY on the very first `photo-row` block across the WHOLE document (not per-page).
    5. `PdfFooter(...)` always, last, unchanged.
    Do not remove existing `wrap={false}` atomicity guards (signature/totals/photo-rows/terms-cards) — they stay as a redundant safety net per CONTEXT.md's explicit allowance. Do not introduce any NEW reliance on `wrap`/`minPresenceAhead` for break decisions — every page boundary comes from `pages.length`/`page.blocks`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-baseline-order.test.tsx && npx tsc -p tsconfig.ci.json --noEmit</automated>
  </verify>
  <done>Both templates accept `pages` and render exactly `pages.length` `<Page>` elements; continuation pages show the repeated table header exactly once; single-page fixture output is unchanged in content/order.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-page fixture + baseline-order update + pagination structural tests + renderToBuffer smoke + determinism test</name>
  <files>tests/unit/estimate/fixtures/document-fixtures.ts, tests/unit/pdf/estimate-pdf-baseline-order.test.tsx, tests/unit/pdf/estimate-pdf-pagination.test.tsx</files>
  <read_first>184-RESEARCH.md's Pitfall 3; tests/unit/estimate/_pdf-text-walker.ts</read_first>
  <action>
    1. Add a `buildMultiPageFixtureEstimate()` (or similar) to `document-fixtures.ts` with enough sections/items (e.g. 4 sections x 10 items, medium-length descriptions) to force `pages.length >= 3` under Classic's real geometry via the actual pipeline (`blocksFromModel` + `computePageBreaks` + `createFontkitMeasurementProvider()` + `SAFETY_MARGIN_LINES`) — tune the fixture empirically and assert `pages.length >= 3` as part of the new test itself (not a hand-guessed count).
    2. Update `estimate-pdf-baseline-order.test.tsx`'s `renderTexts()`/`renderTextsSignedWithPhoto()` helpers to compute a real `pages` prop via the actual pipeline (using Classic's geometry constants for both templates, since text ORDER doesn't depend on geometry) before calling the template function — confirm the EXISTING assertions (Company→ESTIMATE→Project→Labor→Materials→Subtotal→...→Warranty→[Signature]→Photos) still hold unchanged for the small (single-page) baseline fixture. Add a comment per Pitfall 3: this file covers the SMALL/single-page case only; `estimate-pdf-pagination.test.tsx` covers genuinely multi-page assertions.
    3. Create `tests/unit/pdf/estimate-pdf-pagination.test.tsx` (`describe.each` over both templates):
       - Multi-page fixture → the returned tree contains exactly `pages.length` `<Page>` elements.
       - Single-page fixture → exactly 1 `<Page>` element.
       - Every genuine continuation page (its `continuesTable` was true) contains the column-header labels (`L.description`/`L.qty`/`L.unit`/`L.unitPrice`/`L.total`) exactly once, scoped to that page's subtree (reuse `collectTextNodes` per-`<Page>`).
       - A REAL (unmocked) `renderToBuffer` smoke test on the multi-page fixture for BOTH templates — resolves without throwing, produces a non-empty `Buffer`.
       - Determinism: run `blocksFromModel` + `computePageBreaks` TWICE on the identical multi-page fixture input; `JSON.stringify(pagesRun1) === JSON.stringify(pagesRun2)`.
  </action>
  <acceptance_criteria>
    - `grep "pages.length >= 3" tests/unit/estimate/fixtures/document-fixtures.ts tests/unit/pdf/estimate-pdf-pagination.test.tsx` matches somewhere (the multi-page assertion is explicit, not assumed)
    - `npx vitest run tests/unit/pdf/estimate-pdf-pagination.test.tsx` includes a passing determinism assertion and a passing renderToBuffer smoke for both templates
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-pagination.test.tsx tests/unit/pdf/estimate-pdf-baseline-order.test.tsx</automated>
  </verify>
  <done>Multi-page fixture forces >=3 pages via the real pipeline; baseline-order test passes unchanged for the single-page case; new pagination test proves page count, repeated headers, a real renderToBuffer render, and determinism.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual visual verification of multi-page PDFs</name>
  <files>none (visual verification only, no code changes)</files>
  <action>No autonomous action — this task pauses for human visual confirmation of the multi-page PDF output produced by Tasks 1-3.</action>
  <what-built>The consolidated pagination engine (lib/estimate/pagination/) fully wired into both PDF templates: explicit N-page composition, repeated continuation table headers, atomic totals/signature/terms-cards/photo-rows, row/header/subtotal keep-together rules, and a measurement-drift-validated safety margin.</what-built>
  <how-to-verify>
    1. Run `npx vitest run tests/unit tests/eval` and `npx tsc -p tsconfig.ci.json --noEmit` — confirm both are clean (full-suite gate).
    2. Using the multi-page fixture from Task 3, write its `renderToBuffer()` output to a temp PDF file for BOTH templates (Classic + Modern) and open them: confirm no line-item row splits mid-page, section headers stay with their first row, subtotals stay with their last row, totals/signature/terms cards never split, continuation pages repeat the column header, and every page shows "Page N of M".
    3. Do the same for the existing small (single-page) fixture — confirm it still looks identical to before this phase (1 page, no visual regression).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <verify>
    <manual>Open both generated PDFs (1-page and 4+-page, Classic + Modern) and visually confirm no split rows/atomicity violations and correct continuation headers/Page N of M.</manual>
  </verify>
  <done>User confirms multi-page and single-page PDFs render correctly for both templates with no atomicity or continuation regressions.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit tests/eval` — full suite green (matches CI's `test.yml` gate).
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
Manual PDF visual check per the checkpoint task above.
</verification>

<success_criteria>
Both PDF templates render N explicit `<Page>` elements computed by the ONE shared `computePageBreaks()` function; every locked break rule holds on a genuinely multi-page fixture; the single-page case is visually unchanged; the pipeline is proven deterministic. This closes PGBRK-01 through PGBRK-05 for the PDF surface — Phase 185 wires the same `lib/estimate/pagination/measure/types.ts` `MeasurementProvider` contract and `computePageBreaks()` function into the web paginated preview.
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-05-SUMMARY.md`. It MUST document, for Phase 185's benefit:
- The exact `MeasurementProvider` interface (`lib/estimate/pagination/measure/types.ts`) Phase 185's DOM implementation must satisfy.
- The exact `computePageBreaks(blocks, constraints, measurementProvider, safetyMarginLines): PageAssignment[]` contract Phase 185's web preview will call with a DOM-backed provider instead of `createFontkitMeasurementProvider()`.
- `blocksFromModel()`'s input shape (Phase 185 reuses it verbatim — same blocks, different measurement provider, per CONTEXT.md's "one consolidated deterministic page-break rule" mandate).
- The final `SAFETY_MARGIN_LINES` value and where it's sourced from.
</output>
