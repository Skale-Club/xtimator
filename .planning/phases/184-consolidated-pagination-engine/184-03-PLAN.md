---
phase: 184-consolidated-pagination-engine
plan: 03
type: execute
wave: 2
depends_on: ["184-01", "184-02"]
files_modified:
  - lib/estimate/pagination/measure/estimator.ts
  - lib/estimate/pagination/blocks-from-model.ts
  - tests/unit/pagination/measure/estimator.test.ts
  - tests/unit/pagination/blocks-from-model.test.ts
  - tests/unit/pagination/pagination-engine-boundary.test.ts
autonomous: true
requirements: [PGBRK-01, PGBRK-05]

must_haves:
  truths:
    - "Web and PDF measurement share the same registered font family (TTF) with a measurement provider built on fontkit + linebreak."
    - "An estimate section with only empty-description items produces zero item-row blocks and no orphaned section-header/subtotal for it."
  artifacts:
    - path: "lib/estimate/pagination/measure/estimator.ts"
      provides: "Server-only MeasurementProvider implementation (fontkit + linebreak), matching Plan 184-01's hand-validated formula exactly"
      contains: "export function createFontkitMeasurementProvider"
    - path: "lib/estimate/pagination/blocks-from-model.ts"
      provides: "DocumentSection[]/signature/photos/terms -> PageBlock[] (structure only — engine.ts computes actual heights via the measurement field at placement time)"
      contains: "export function blocksFromModel"
  key_links:
    - from: "lib/estimate/pagination/measure/estimator.ts"
      to: "lib/pdf/register-fonts.ts"
      via: "same 4 registered family names (Inter, Inter-Bold, Lora, Lora-Bold) map to the same vendored TTF paths"
      pattern: "Inter-Bold|Lora-Bold"
    - from: "lib/estimate/pagination/blocks-from-model.ts"
      to: "components/pdf/estimate-pdf.tsx:409-414"
      via: "replicated empty-description + zero-items-section filter"
      pattern: "trim\\(\\)\\s*!==\\s*''"
---

<objective>
Give the pure engine (Plan 184-02) its two real-world inputs: (1) a server-side `MeasurementProvider` backed by the exact fontkit/linebreak code `@react-pdf/pdfkit` renders with, and (2) a pure `blocksFromModel()` function that turns a real estimate's document model into the `PageBlock[]` the engine consumes — replicating today's empty-description filtering exactly.

Purpose: PGBRK-05's "measurement provider built on react-pdf's own transitive deps" and PGBRK-01's "single source of truth" both require this glue to exist before the PDF templates (Plan 184-05) can call `computePageBreaks()` for real.
Output: `lib/estimate/pagination/measure/estimator.ts`, `lib/estimate/pagination/blocks-from-model.ts`, both fully unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-RESEARCH.md
@.planning/phases/184-consolidated-pagination-engine/184-01-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-02-SUMMARY.md
@lib/pdf/register-fonts.ts
@lib/estimate/document/model.ts
@lib/estimate/presentation-settings.ts
@components/pdf/estimate-pdf.tsx

<interfaces>
From Plan 184-02's `lib/estimate/pagination/measure/types.ts` (already committed):
```ts
export interface MeasurementProvider {
  lineCount(text: string, styleKey: string, fontSizePt: number, maxWidthPt: number): number
}
```

From Plan 184-02's `lib/estimate/pagination/types.ts` (already committed — this plan's `blocksFromModel` emits arrays of these):
```ts
export type PageBlockKind =
  | 'title-banner' | 'info-grid' | 'summary' | 'section-header' | 'item-row'
  | 'section-subtotal' | 'totals' | 'terms-card' | 'signature' | 'photo-row' | 'prepared-by'

export interface PageBlock {
  kind: PageBlockKind
  id: string
  sectionId?: string
  baseHeightPt: number
  measurement?: { text: string; styleKey: string; fontSizePt: number; lineHeightMultiplier: number; maxWidthPt: number }
  keepWithNextId?: string
  keepWithPreviousId?: string
  atomic: boolean
  page1Only?: boolean
}
```

From Plan 184-01's `lib/estimate/pagination/measure/safety-margin.ts` (already committed):
```ts
export const SAFETY_MARGIN_LINES: number
```

From `lib/pdf/register-fonts.ts` (existing, unchanged) — the ONLY 4 valid `styleKey` values this phase's real estimator resolves:
```ts
Font.register({ family: 'Inter', src: '.../inter/Inter-Regular.ttf' })
Font.register({ family: 'Inter-Bold', src: '.../inter/Inter-Bold.ttf' })
Font.register({ family: 'Lora', src: '.../lora/Lora-Regular.ttf' })
Font.register({ family: 'Lora-Bold', src: '.../lora/Lora-Bold.ttf' })
```

From `components/pdf/estimate-pdf.tsx:409-414` (the EXACT filter to replicate in `blocksFromModel`):
```ts
estimate.sections
  .map((section) => ({ ...section, items: section.items.filter((i) => i.description.trim() !== '') }))
  .filter((section) => section.items.length > 0)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-only fontkit measurement provider</name>
  <files>lib/estimate/pagination/measure/estimator.ts, tests/unit/pagination/measure/estimator.test.ts</files>
  <read_first>lib/estimate/pagination/measure/types.ts (the MeasurementProvider contract), lib/pdf/register-fonts.ts (family->path pairs), lib/estimate/pagination/measure/safety-margin.ts</read_first>
  <behavior>
    - `createFontkitMeasurementProvider().lineCount('HHHH HHHH HHHH', 'Inter', 9, 30)` returns `3` (matches Plan 184-01's hand-calculated arithmetic test exactly — same font, same formula).
    - Same call with `maxWidthPt: 60` returns `2`; with `100` returns `1`.
    - `lineCount('', 'Inter', 9, 100)` returns `0`.
    - Calling `lineCount` twice with identical args returns the identical number (deterministic; no per-call font re-parsing side effect visible to the caller).
    - `lineCount(text, 'Lora', ...)` and `lineCount(text, 'Inter', ...)` for the SAME text/size/width can legitimately differ (different glyph metrics) — assert they don't throw and both return positive integers for non-empty text.
  </behavior>
  <action>
    Create `lib/estimate/pagination/measure/estimator.ts` (SERVER ONLY — this is the one file in `lib/estimate/pagination/` allowed to import `fontkit`, `linebreak`, and `node:path`). Implement:
    - `const FONT_FAMILY_TO_PATH: Record<string, string>` mapping exactly `'Inter'`, `'Inter-Bold'`, `'Lora'`, `'Lora-Bold'` to their `public/fonts/{inter,lora}/*.ttf` paths (mirror `lib/pdf/register-fonts.ts`'s own `Font.register` calls verbatim — do not invent new family names or paths).
    - A module-scope `Map<string, ReturnType<typeof fontkit.openSync>>` font cache keyed by family name, opened lazily on first use (mirrors `register-fonts.ts`'s "register once" discipline — never re-parse the same TTF per call).
    - `export function estimateLineCount(text: string, fontFamily: string, fontSizePt: number, maxWidthPt: number): number` implementing the EXACT greedy line-packer validated in Plan 184-01's `tests/unit/pagination/measure/fontkit-arithmetic.test.ts` (same chunk/scale formula — no reinterpretation).
    - `export function createFontkitMeasurementProvider(): MeasurementProvider` returning `{ lineCount: estimateLineCount }` (satisfies `measure/types.ts`'s interface with `styleKey` as the font family name).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pagination/measure/estimator.test.ts</automated>
  </verify>
  <done>estimator.ts exports `estimateLineCount` + `createFontkitMeasurementProvider`; its output for the Plan 184-01 fixture string matches that plan's hand-calculated numbers exactly, confirming no drift between the spike's throwaway math and the shipped implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: blocksFromModel() — document model to PageBlock[], with the empty-description filter</name>
  <files>lib/estimate/pagination/blocks-from-model.ts, tests/unit/pagination/blocks-from-model.test.ts, tests/unit/pagination/pagination-engine-boundary.test.ts</files>
  <read_first>lib/estimate/document/model.ts (DocumentSection/DocumentItem/DocumentSignature/DocumentPhoto), lib/estimate/presentation-settings.ts (ResolvedPresentationSettings/isSectionVisible), components/pdf/shared/pdf-totals-block.tsx (which totals rows are conditionally visible: discount_amount>0, tax_amount>0, dep.showDeposit), components/pdf/shared/pdf-terms-section.tsx (the exact 5-card visibility gates to mirror)</read_first>
  <behavior>
    - A section where every item has an empty/whitespace-only `description` → zero `item-row` blocks for it, AND no `section-header`/`section-subtotal` block emitted for that section either (the section is dropped entirely — Open Question 4's gap).
    - A section with 1 real item + 1 empty-description item → exactly 1 `item-row` block.
    - All 5 terms fields populated (`company.estimate_terms_enabled`+text, `payment_terms`, `timeline`, `warranty_terms`, `notes`, all resolver-visible) → exactly 5 `terms-card` blocks, each independently `atomic: true`, no `keepWith*` links between them.
    - 7 photos at `contentWidthPt: 532` (Classic) → 3 `photo-row` blocks chunked `[3, 3, 1]` (`Math.floor(532 / (150 + 8)) === 3` per row).
    - No `signature` input → zero `signature` blocks.
    - No `preparedBy` input → zero `prepared-by` blocks.
    - Output array order matches document order: `title-banner`, `info-grid`, `summary` (if present), then PER SECTION `section-header` → its `item-row`s → `section-subtotal` (in that exact sub-order, with the header's `keepWithNextId` pointing at the FIRST item-row's id and the subtotal's `keepWithPreviousId` pointing at the LAST item-row's id — both array-adjacent, per Plan 184-02's engine assumption), then `totals`, then up to 5 `terms-card`s, then `signature` (if present), then `photo-row`s, then `prepared-by` (if present).
    - Every block `id` is stable/deterministic — derived from `section.id`/`item.id`/a fixed terms-key/a photo-chunk-index — never `Math.random()`.
  </behavior>
  <action>
    Create `lib/estimate/pagination/blocks-from-model.ts` exporting `blocksFromModel(input): PageBlock[]`. Design `input` to carry: the estimate's sections/summary/timeline/payment_terms/warranty_terms/notes/company terms text, `signature: DocumentSignature | null`, `photos: { url: string; caption: string | null }[]`, `resolvedSettings: ResolvedPresentationSettings`, `preparedBy: string | null`, and PER-TEMPLATE geometry needed for measurement recipes and photo chunking: `contentWidthPt`, `colDescriptionWidthPt` (40% of `contentWidthPt`, matching both templates' existing `colDescription` column), font family names (`fontFamily`/`fontFamilyBold` — pass `ESTIMATE_DESIGN_TOKENS.<template>` values, never hardcode 'Inter'/'Lora' here), and font sizes/line-height multipliers for `tableCellText`/`termsText`/`sectionTitle`/`summary` (match each template's real StyleSheet values — Classic 9pt/Modern 9.5pt for item descriptions, etc; document the exact numbers chosen with a comment citing the source StyleSheet).

    Replicate `components/pdf/estimate-pdf.tsx:409-414`'s filter VERBATIM: `section.items.filter(i => i.description.trim() !== '')`, then drop sections with zero remaining items — apply this BEFORE emitting any blocks for a section.

    For `totals`, compute `baseHeightPt` as a sum over the rows actually visible for this estimate (subtotal always; discount row iff `discount_amount > 0`; tax row iff `tax_amount > 0`; grand total always; deposit+balance rows iff `dep.showDeposit`) times a per-row height constant (document the constant, cite `pdf-totals-block.tsx`'s `totalsRow`/`grandTotalRow`/`grandTotalBlock` padding values) — no `measurement` field (money/label text treated as fixed-height, per research).

    For `photo-row`, chunk `photos` into groups of `Math.floor(contentWidthPt / (150 + 8))` (150pt tile + 8pt gap, matching `pdf-photo-grid.tsx`'s literal values), each chunk its own atomic block with `baseHeightPt` covering the 150pt image row plus caption allowance if any photo in the chunk has one (document the chosen allowance).

    Extend `tests/unit/pagination/pagination-engine-boundary.test.ts`'s target-files array (created in Plan 184-02) to ALSO include `'lib/estimate/pagination/blocks-from-model.ts'` — update its exclusion comment to confirm `measure/estimator.ts` remains the only excluded file.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pagination/blocks-from-model.test.ts tests/unit/pagination/pagination-engine-boundary.test.ts</automated>
  </verify>
  <done>blocksFromModel() is exported and passes every behavior case above; the boundary-purity test now also covers blocks-from-model.ts and stays green (zero fontkit/linebreak/react-pdf imports).</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/pagination` — full pagination test directory green (boundary, rules, engine from 184-02; measure/fontkit-arithmetic, measure/safety-margin from 184-01; measure/estimator, blocks-from-model from this plan).
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
</verification>

<success_criteria>
A real estimate's document model can be turned into `PageBlock[]` via `blocksFromModel()` and measured via `createFontkitMeasurementProvider()`, ready for Plan 184-05 to feed both into `computePageBreaks()` — with the empty-description filter behavior preserved exactly and zero drift from Plan 184-01's validated measurement formula.
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-03-SUMMARY.md` documenting `blocksFromModel`'s exact input shape (field-by-field) and `createFontkitMeasurementProvider`'s signature — Plan 184-05 wires both directly from this summary.
</output>
