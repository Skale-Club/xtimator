---
phase: 184-consolidated-pagination-engine
plan: 03
type: execute
wave: 3
depends_on: ["184-01", "184-02", "184-04"]
files_modified:
  - lib/estimate/pagination/measure/estimator.ts
  - lib/estimate/pagination/blocks-from-model.ts
  - tests/unit/pagination/measure/estimator.test.ts
  - tests/unit/pagination/blocks-from-model.test.ts
  - tests/unit/pagination/pagination-engine-boundary.test.ts
  - tests/unit/estimate/pt-px-conversion-source.test.ts
autonomous: true
requirements: [PGBRK-01, PGBRK-05]

must_haves:
  truths:
    - "Web and PDF measurement share the same registered font family (TTF) with a measurement provider built on fontkit + linebreak."
    - "An estimate section with only empty-description items produces zero item-row blocks and no orphaned section-header/subtotal for it."
    - "Sections/summary/photos each respect their own presentation-settings visibility gate — a hidden section produces zero blocks for it, not just a visually-hidden one."
  artifacts:
    - path: "lib/estimate/pagination/measure/estimator.ts"
      provides: "Server-only MeasurementProvider implementation (fontkit + linebreak), matching Plan 184-01's hand-validated formula exactly"
      contains: "export function createFontkitMeasurementProvider"
    - path: "lib/estimate/pagination/blocks-from-model.ts"
      provides: "DocumentSection[]/signature/photos/terms -> PageBlock[] (structure only — engine.ts computes actual heights via the measurement field at placement time), populating PageBlock.ref for every block, using ESTIMATE_PAGE_GEOMETRY/LINE_HEIGHT tokens and photosPerRow, never re-deriving any of those numbers"
      contains: "export function blocksFromModel"
  key_links:
    - from: "lib/estimate/pagination/measure/estimator.ts"
      to: "lib/pdf/register-fonts.ts"
      via: "same 4 registered family names (Inter, Inter-Bold, Lora, Lora-Bold) map to the same vendored TTF paths"
      pattern: "Inter-Bold|Lora-Bold"
    - from: "lib/estimate/pagination/blocks-from-model.ts"
      to: "components/pdf/estimate-pdf.tsx:409-414"
      via: "replicated empty-description + zero-items-section filter"
      pattern: "trim\\(\\)\\s*!==\\s*''"
    - from: "lib/estimate/pagination/blocks-from-model.ts"
      to: "components/pdf/shared/pdf-photo-grid.tsx"
      via: "imports photosPerRow(contentWidthPt) instead of re-deriving Math.floor(width/(150+8))"
      pattern: "photosPerRow"
    - from: "lib/estimate/pagination/blocks-from-model.ts"
      to: "lib/estimate/document/tokens.ts"
      via: "reads ESTIMATE_PAGE_GEOMETRY + LINE_HEIGHT instead of hand-typed geometry/lineHeight literals"
      pattern: "ESTIMATE_PAGE_GEOMETRY|LINE_HEIGHT"
---

<revision_note>
**Revised per plan-checker (18 blockers, Opus).** Changes from the prior version:
- Wave restructure: now Wave 3, `depends_on: ["184-01", "184-02", "184-04"]` (was Wave 2, `depends_on: ["184-01","184-02"]`) — this plan now ALSO depends on Plan 184-04 because `blocksFromModel` imports `photosPerRow` from `components/pdf/shared/pdf-photo-grid.tsx` (blocker 20), which Plan 184-04 creates.
- Blocker 2: `blocksFromModel` is now explicitly DEFENSIVE — `input.sections ?? []` (never assumes `sections` is present) — required because `render-estimate-pdf-resolver.test.ts`'s existing `BASE_ESTIMATE` mock (updated in Plan 184-05 Task 3) may not always carry a `sections` array.
- Blocker 13: `blocksFromModel` now populates `PageBlock.ref` on every block it emits (per Plan 184-02's `PageBlockRef` type) — `sectionId`/`itemId`/`itemIndex` (within the FILTERED item list) for section/item blocks, `termsKey` for terms-cards, `photoRange` for photo-rows.
- Blocker 15: 3 new behavior cases — sections/summary/photos each independently respect their own `isSectionVisible` gate (previously only the empty-description filter was behavior-tested; the VISIBILITY gates were assumed but not explicitly specified).
- Blocker 19: geometry/font-size/line-height numbers now come from Plan 184-01's `ESTIMATE_PAGE_GEOMETRY`/`LINE_HEIGHT` tokens (`lib/estimate/document/tokens.ts`) — NOT hand-typed/"document the chosen numbers" placeholders. `blocks-from-model.ts` is added to `tests/unit/estimate/pt-px-conversion-source.test.ts`'s `CLEAN_SOURCES` array (guards against a future bare `612`/`792`/`816`/`1056` literal creeping in instead of importing the token).
- Blocker 20: photo-row chunking now calls the ONE exported `photosPerRow()` helper (Plan 184-04) instead of re-deriving `Math.floor(contentWidthPt / (150 + 8))` locally.
- Blocker 21 + 22: the totals `baseHeightPt` formula and EVERY block kind's `baseHeightPt` formula now cite EXACT source StyleSheet keys/values (see `<height_formulas>` below) instead of "document the constant chosen." **Correction from an earlier draft of this plan:** `summary`/`terms-card`/`info-grid`/`prepared-by` use `ESTIMATE_PAGE_GEOMETRY[template].proseLineHeightMultiplier` (the ALREADY-explicit `termsText`/`infoValue` lineHeight, 1.5 Classic/1.6 Modern) — NOT the newly-derived `LINE_HEIGHT` (which is font-metrics-derived and applies ONLY to `tableCellText`/`sectionTitle`, the 2 fields that had no lineHeight at all before this phase).
- Blocker 24: block `id` naming convention is now explicit: `${section.id}-header`, `${section.id}-rows-${item.id}`, `${section.id}-subtotal` (the conceptual repeated-table-header anchor, not itself a `PageBlock`, is `${section.id}-thead` — used only in code comments/test descriptions for traceability, never as an actual emitted block id, since the repeated header is rendered by Plan 184-05's resolver from `continuesTable`, not tracked by the engine).
- prepared-by: per Plan 184-02's revised taxonomy, `prepared-by` is now an ORDINARY atomic block (`page1Only` NOT set) — updated accordingly.
- The engine no longer takes a `safetyMarginLines` parameter (Plan 184-02 blocker 23) — `blocksFromModel`/`estimator.ts` have ZERO involvement in margin application; nothing to change here beyond removing any prior assumption that measurement recipes needed margin awareness (they never did).
</revision_note>

<objective>
Give the pure engine (Plan 184-02) its two real-world inputs: (1) a server-side `MeasurementProvider` backed by the exact fontkit/linebreak code `@react-pdf/pdfkit` renders with, and (2) a pure `blocksFromModel()` function that turns a real estimate's document model into the `PageBlock[]` the engine consumes — replicating today's empty-description filtering and every presentation-settings visibility gate exactly, populating `ref` on every block, and reading ALL geometry/line-height numbers from Plan 184-01's shared tokens.

Purpose: PGBRK-05's "measurement provider built on react-pdf's own transitive deps" and PGBRK-01's "single source of truth" both require this glue to exist before the PDF templates (Plan 184-05) can call `computePageBreaks()` for real.
Output: `lib/estimate/pagination/measure/estimator.ts`, `lib/estimate/pagination/blocks-from-model.ts`, both fully unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-RESEARCH.md
@.planning/phases/184-consolidated-pagination-engine/184-01-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-02-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-04-SUMMARY.md
@lib/pdf/register-fonts.ts
@lib/estimate/document/model.ts
@lib/estimate/document/tokens.ts
@lib/estimate/presentation-settings.ts
@components/pdf/estimate-pdf.tsx
@tests/unit/estimate/pt-px-conversion-source.test.ts

<interfaces>
From Plan 184-02's `lib/estimate/pagination/measure/types.ts` (already committed):
```ts
export interface MeasurementProvider {
  lineCount(text: string, styleKey: string, fontSizePt: number, maxWidthPt: number): number
}
```

From Plan 184-02's `lib/estimate/pagination/types.ts` (already committed — this plan's `blocksFromModel` emits arrays of these):
```ts
export type PageBlockKind =
  | 'title-banner' | 'info-grid' | 'summary' | 'section-header' | 'item-row'
  | 'section-subtotal' | 'totals' | 'terms-card' | 'signature' | 'photo-row' | 'prepared-by'

export interface PageBlockRef {
  sectionId?: string; itemId?: string; itemIndex?: number
  termsKey?: 'estimate' | 'payment' | 'timeline' | 'warranty' | 'notes'
  photoRange?: [number, number]
}

export interface PageBlock {
  kind: PageBlockKind
  id: string
  baseHeightPt: number
  measurement?: { text: string; styleKey: string; fontSizePt: number; lineHeightMultiplier: number; maxWidthPt: number }
  keepWithNextId?: string
  keepWithPreviousId?: string
  atomic: boolean
  page1Only?: boolean   // ONLY 'title-banner' | 'info-grid' | 'summary' set this — NOT 'prepared-by'
  ref?: PageBlockRef
}
```

From Plan 184-01's `lib/estimate/pagination/measure/safety-margin.ts` (already committed — NOT consumed here, margin is applied per-page by Plan 184-05's caller):
```ts
export const SAFETY_MARGIN_LINES: number
```

From Plan 184-01's `lib/estimate/document/tokens.ts` (already committed):
```ts
export const LINE_HEIGHT: Record<string, number> // Inter: 1.21, 'Inter-Bold': 1.21, Lora: 1.28, 'Lora-Bold': 1.28 — ONLY tableCellText/sectionTitle
export const ESTIMATE_PAGE_GEOMETRY: Record<EstimateTemplateId, EstimatePageGeometry>
// { contentWidthPt, topPaddingPt, bottomPaddingPt, colDescriptionWidthPt, tableCellFontSizePt,
//   sectionTitleFontSizePt, termsTextFontSizePt, summaryFontSizePt, proseLineHeightMultiplier }
// proseLineHeightMultiplier = 1.5 (classic) / 1.6 (modern) — termsText/infoValue's OWN already-explicit lineHeight
```

From Plan 184-04's `components/pdf/shared/pdf-photo-grid.tsx` (already committed):
```ts
export function photosPerRow(contentWidthPt: number): number  // Math.floor(contentWidthPt / (150 + 8))
```

From `lib/pdf/register-fonts.ts` (existing, unchanged) — the ONLY 4 valid `styleKey` values this phase's real estimator resolves:
```ts
Font.register({ family: 'Inter', src: '.../inter/Inter-Regular.ttf' })
Font.register({ family: 'Inter-Bold', src: '.../inter/Inter-Bold.ttf' })
Font.register({ family: 'Lora', src: '.../lora/Lora-Regular.ttf' })
Font.register({ family: 'Lora-Bold', src: '.../lora/Lora-Bold.ttf' })
```

From `components/pdf/estimate-pdf.tsx:409-414` (the EXACT filter to replicate in `blocksFromModel`):
```ts
estimate.sections
  .map((section) => ({ ...section, items: section.items.filter((i) => i.description.trim() !== '') }))
  .filter((section) => section.items.length > 0)
```
</interfaces>

<height_formulas>
**Plan-checker blockers 21+22 — every block kind's `baseHeightPt`/`measurement` formula, cited to its EXACT source StyleSheet key/value (both templates). All numbers below MUST be read from `ESTIMATE_PAGE_GEOMETRY`/`LINE_HEIGHT`/`ESTIMATE_DESIGN_TOKENS` (Plan 184-01) — shown here ONLY to confirm what those tokens already equal; never re-type them as bare literals in `blocks-from-model.ts`. `lineHeightMultiplier` for `item-row`/`section-header` comes from `LINE_HEIGHT[fontFamily]` (font-metrics-derived, 1.21 Inter/1.28 Lora); for `summary`/`terms-card`/`info-grid`/`prepared-by` it comes from `ESTIMATE_PAGE_GEOMETRY[template].proseLineHeightMultiplier` (already-explicit, 1.5 Classic/1.6 Modern) — do NOT swap these two.**

| Kind | Classic (`estimate-pdf.tsx`) | Modern (`estimate-pdf-modern.tsx`) |
|---|---|---|
| `title-banner` (measured, `proseLineHeightMultiplier`) | `estimateTitle` (fontSize 24, marginBottom 20) + banner `paddingVertical` 16×2 | `estimateTitle` (fontSize 13, marginBottom 6) + `estimateTitleRule` (marginBottom 28) |
| `info-grid` (measured, `proseLineHeightMultiplier`) | `infoRow` (marginBottom 20) + `infoLabel` (fontSize 8, marginBottom 4) + `infoValue` (fontSize 10, lineHeight 1.5) × up to 4 lines (project/date/estimate# + optional client block) | `infoRow` (marginBottom 28) + `infoLabel` (fontSize 8, marginBottom 5) + `infoValue` (fontSize 10, lineHeight 1.6) |
| `summary` (measured, `proseLineHeightMultiplier`) | `infoLabel` + `termsText` (fontSize 9, lineHeight 1.5, marginBottom 12), `maxWidthPt = ESTIMATE_PAGE_GEOMETRY.classic.contentWidthPt` | `infoLabel` + `termsText` (fontSize 9, lineHeight 1.6, marginBottom 14) |
| `section-header` (measured, `LINE_HEIGHT`) | `sectionHeader` (padding 8, marginTop 16) + `sectionTitle` (fontSize 11, `LINE_HEIGHT['Inter-Bold']`=1.21, Plan 184-04's pinned value) | `sectionHeader` (paddingVertical 6, marginTop 22, borderBottomWidth 1) + `sectionTitle` (fontSize 11, `LINE_HEIGHT['Lora-Bold']`=1.28) |
| `item-row` (measured, `LINE_HEIGHT` — the ONE field driving row-height variance) | `tableRow` (paddingVertical 6×2=12 `baseHeightPt`) + `tableCellText` (fontSize `ESTIMATE_PAGE_GEOMETRY.classic.tableCellFontSizePt`=9, `LINE_HEIGHT.Inter`=1.21, `maxWidthPt = ESTIMATE_PAGE_GEOMETRY.classic.colDescriptionWidthPt`) | `tableRow` (paddingVertical 8×2=16) + `tableCellText` (fontSize 9.5, `LINE_HEIGHT.Lora`=1.28, `maxWidthPt=203.2`) |
| `section-subtotal` (fixed, no measurement — numeric/label cells, single line per research) | `sectionSubtotal` (paddingVertical 6×2=12, borderTopWidth 1) + `sectionSubtotalLabel`/`Value` (fontSize 9) | `sectionSubtotal` (paddingVertical 8×2=16, borderTopWidth 0.5) + labels fontSize 9 |
| `totals` (fixed, no measurement) | `totalsContainer` (marginTop 20) + `totalsRow` (paddingVertical 4×2=8) × (1 subtotal + [discount] + [tax]) + `grandTotalRow` (paddingVertical 8×2=16, borderTopWidth 2, fontSize 14) + [deposit row(s)] × 8 each | `totalsContainer` (marginTop 28) + `totalsRow` (paddingVertical 6×2=12) × N + `grandTotalBlock` (marginTop 16 + label fontSize 9 + value fontSize 30) — see `pdf-totals-block.tsx`'s `dep.showDeposit`/`discount_amount>0`/`tax_amount>0` gates for row count |
| `terms-card` (measured, `proseLineHeightMultiplier`) | `termsTitle` (fontSize 11, marginBottom 6, paddingBottom 4, borderBottomWidth 1) + `termsText` (fontSize 9, lineHeight 1.5, marginBottom 12), `maxWidthPt = contentWidthPt` | `termsTitle` (fontSize 10.5, marginBottom 7, paddingBottom 5, borderBottomWidth 0.5) + `termsText` (fontSize 9, lineHeight 1.6, marginBottom 14) |
| `signature` (fixed) | `pdf-signature-block.tsx` literals: marginTop 16 + `termsTitle` label line + `Image` height 40 + 2 text lines (fontSize 9 each) — byte-identical shared component, same formula both templates |
| `photo-row` (fixed) | `pdf-photo-grid.tsx` literals: image 150 + gap 8 + optional caption line (fontSize 8, marginTop 2) IF any photo in the chunk has a caption — same formula both templates |
| `prepared-by` (measured, `proseLineHeightMultiplier`, ordinary atomic — NOT page1Only) | `infoLabel` (fontSize 8) + `infoValue` (fontSize 10, lineHeight 1.5), marginTop 16 | `infoLabel` + `infoValue` (fontSize 10, lineHeight 1.6), marginTop 20 |
</height_formulas>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-only fontkit measurement provider</name>
  <files>lib/estimate/pagination/measure/estimator.ts, tests/unit/pagination/measure/estimator.test.ts</files>
  <read_first>lib/estimate/pagination/measure/types.ts (the MeasurementProvider contract), lib/pdf/register-fonts.ts (family->path pairs)</read_first>
  <behavior>
    - `createFontkitMeasurementProvider().lineCount('HHHH HHHH HHHH', 'Inter', 9, 30)` returns `3` (matches Plan 184-01's hand-calculated arithmetic test exactly — same font, same formula).
    - Same call with `maxWidthPt: 60` returns `2`; with `100` returns `1`.
    - `lineCount('', 'Inter', 9, 100)` returns `0`.
    - Calling `lineCount` twice with identical args returns the identical number (deterministic; no per-call font re-parsing side effect visible to the caller).
    - `lineCount(text, 'Lora', ...)` and `lineCount(text, 'Inter', ...)` for the SAME text/size/width can legitimately differ (different glyph metrics) — assert they don't throw and both return positive integers for non-empty text.
  </behavior>
  <action>
    Create `lib/estimate/pagination/measure/estimator.ts` (SERVER ONLY — this is the one file in `lib/estimate/pagination/` allowed to import `fontkit`, `linebreak`, and `node:path`). Implement:
    - `const FONT_FAMILY_TO_PATH: Record<string, string>` mapping exactly `'Inter'`, `'Inter-Bold'`, `'Lora'`, `'Lora-Bold'` to their `public/fonts/{inter,lora}/*.ttf` paths (mirror `lib/pdf/register-fonts.ts`'s own `Font.register` calls verbatim — do not invent new family names or paths).
    - A module-scope `Map<string, ReturnType<typeof fontkit.openSync>>` font cache keyed by family name, opened lazily on first use (mirrors `register-fonts.ts`'s "register once" discipline — never re-parse the same TTF per call).
    - `export function estimateLineCount(text: string, fontFamily: string, fontSizePt: number, maxWidthPt: number): number` implementing the EXACT greedy line-packer validated in Plan 184-01's `tests/unit/pagination/measure/fontkit-arithmetic.test.ts` (same chunk/scale formula — no reinterpretation, NO margin term — margin is applied per-page by Plan 184-05's caller, never here).
    - `export function createFontkitMeasurementProvider(): MeasurementProvider` returning `{ lineCount: estimateLineCount }` (satisfies `measure/types.ts`'s interface with `styleKey` as the font family name).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pagination/measure/estimator.test.ts</automated>
  </verify>
  <done>estimator.ts exports `estimateLineCount` + `createFontkitMeasurementProvider`; its output for the Plan 184-01 fixture string matches that plan's hand-calculated numbers exactly, confirming no drift between the spike's throwaway math and the shipped implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: blocksFromModel() — document model to PageBlock[], with ref population, visibility gates, and token-sourced geometry</name>
  <files>lib/estimate/pagination/blocks-from-model.ts, tests/unit/pagination/blocks-from-model.test.ts, tests/unit/pagination/pagination-engine-boundary.test.ts, tests/unit/estimate/pt-px-conversion-source.test.ts</files>
  <read_first>lib/estimate/document/model.ts (DocumentSection/DocumentItem/DocumentSignature/DocumentPhoto), lib/estimate/document/tokens.ts (LINE_HEIGHT/ESTIMATE_PAGE_GEOMETRY — Plan 184-01), lib/estimate/presentation-settings.ts (ResolvedPresentationSettings/isSectionVisible), components/pdf/shared/pdf-photo-grid.tsx (photosPerRow — Plan 184-04), components/pdf/shared/pdf-totals-block.tsx (which totals rows are conditionally visible: discount_amount>0, tax_amount>0, dep.showDeposit), components/pdf/shared/pdf-terms-section.tsx (the exact 5-card visibility gates to mirror), the `<height_formulas>` table above</read_first>
  <behavior>
    - **Empty-description filter (Open Question 4):** a section where every item has an empty/whitespace-only `description` → zero `item-row` blocks for it, AND no `section-header`/`section-subtotal` block emitted for that section either. A section with 1 real item + 1 empty-description item → exactly 1 `item-row` block.
    - **Visibility gate — sections (Plan-checker blocker 15):** `isSectionVisible(resolvedSettings, 'sections') === false` → zero `section-header`/`item-row`/`section-subtotal` blocks emitted, even when `estimate.sections` has real items.
    - **Visibility gate — summary (Plan-checker blocker 15):** `isSectionVisible(resolvedSettings, 'summary') === false` OR `estimate.summary` falsy → zero `summary` blocks (both conditions independently tested).
    - **Visibility gate — photos (Plan-checker blocker 15):** `isSectionVisible(resolvedSettings, 'photos') === false` → zero `photo-row` blocks, even when `photos` is a non-empty array.
    - All 5 terms fields populated (`company.estimate_terms_enabled`+text, `payment_terms`, `timeline`, `warranty_terms`, `notes`, all resolver-visible) → exactly 5 `terms-card` blocks, each independently `atomic: true`, no `keepWith*` links between them, each with `ref.termsKey` set to one of `'estimate' | 'payment' | 'timeline' | 'warranty' | 'notes'`.
    - 7 photos at `contentWidthPt: 532` (Classic) → 3 `photo-row` blocks chunked `[3, 3, 1]` via the IMPORTED `photosPerRow(532) === 3` (not a locally re-derived formula) — each block's `ref.photoRange` is `[0,3]`, `[3,6]`, `[6,7]` respectively.
    - No `signature` input → zero `signature` blocks. No `preparedBy` input → zero `prepared-by` blocks. When present, the `prepared-by` block does NOT set `page1Only` (it is an ordinary atomic flowing block per Plan 184-02's revised taxonomy).
    - **`ref` population (Plan-checker blocker 13):** every `section-header`/`item-row`/`section-subtotal` block has `ref.sectionId` set to its section's id; every `item-row` additionally has `ref.itemId` (the item's id) and `ref.itemIndex` (its 0-based index WITHIN the section's FILTERED/surviving item list, not the raw pre-filter list).
    - **Defensive input (Plan-checker blocker 2):** calling `blocksFromModel({ sections: undefined, ... })` (or omitting `sections` entirely) does NOT throw — treated as `[]`.
    - **ID naming convention (Plan-checker blocker 24):** a section's header block id is EXACTLY `` `${section.id}-header` ``; each item-row block id is EXACTLY `` `${section.id}-rows-${item.id}` ``; the subtotal block id is EXACTLY `` `${section.id}-subtotal` ``.
    - Output array order matches document order: `title-banner`, `info-grid`, `summary` (if visible+present), then PER SECTION `section-header` → its `item-row`s → `section-subtotal` (with the header's `keepWithNextId` pointing at the FIRST item-row's id and the subtotal's `keepWithPreviousId` pointing at the LAST item-row's id — both array-adjacent), then `totals`, then up to 5 `terms-card`s, then `signature` (if present), then `photo-row`s (if visible), then `prepared-by` (if present).
    - Every block `id` is stable/deterministic — never `Math.random()`.
  </behavior>
  <action>
    Create `lib/estimate/pagination/blocks-from-model.ts` exporting `blocksFromModel(input): PageBlock[]`. Design `input` to carry: `sections: DocumentSection[] | undefined` (accessed defensively as `input.sections ?? []`), `summary`/`timeline`/`payment_terms`/`warranty_terms`/`notes`/company terms text, `discount_amount`/`tax_amount`/`dep: DepositDisplay` (for totals row-count), `signature: DocumentSignature | null`, `photos: { url: string; caption: string | null }[]`, `resolvedSettings: ResolvedPresentationSettings`, `preparedBy: string | null`, and `templateId: EstimateTemplateId` (used to look up `ESTIMATE_PAGE_GEOMETRY[templateId]` and the corresponding `fontFamily`/`fontFamilyBold` from `ESTIMATE_DESIGN_TOKENS[templateId]` — NEVER hardcode 'Inter'/'Lora'/532/508/9/9.5 as bare literals in this file; always read them from `ESTIMATE_PAGE_GEOMETRY`/`ESTIMATE_DESIGN_TOKENS`/`LINE_HEIGHT`).

    Gate sections on `isSectionVisible(resolvedSettings, 'sections')` (skip entirely, zero blocks, if false) BEFORE replicating `components/pdf/estimate-pdf.tsx:409-414`'s filter VERBATIM: `section.items.filter(i => i.description.trim() !== '')`, then drop sections with zero remaining items. Gate `summary` on BOTH `isSectionVisible(resolvedSettings, 'summary')` AND `estimate.summary` being truthy. Gate `photo-row` emission on `isSectionVisible(resolvedSettings, 'photos')` (in addition to `photos.length > 0`).

    For EVERY block, populate `id` per the naming convention in `<behavior>` and `ref` per `PageBlockRef`'s fields (see `<behavior>` for exactly which sub-fields each kind populates) — `terms-card` blocks get `ref.termsKey`, `photo-row` blocks get `ref.photoRange`.

    Compute EVERY block kind's `baseHeightPt`/`measurement` exactly per the `<height_formulas>` table in `<context>` above — reading `ESTIMATE_PAGE_GEOMETRY[templateId]`/`LINE_HEIGHT[fontFamily]` for every number, never a bare literal, and using `LINE_HEIGHT` ONLY for `item-row`/`section-header`'s `lineHeightMultiplier` (the two font-metrics-derived fields), `ESTIMATE_PAGE_GEOMETRY[templateId].proseLineHeightMultiplier` for every OTHER measured kind (`summary`/`terms-card`/`info-grid`/`prepared-by`/`title-banner`). For `photo-row`, chunk `photos` via the IMPORTED `photosPerRow(ESTIMATE_PAGE_GEOMETRY[templateId].contentWidthPt)` from `@/components/pdf/shared/pdf-photo-grid`.

    Extend `tests/unit/pagination/pagination-engine-boundary.test.ts`'s target-files array (created in Plan 184-02) to ALSO include `'lib/estimate/pagination/blocks-from-model.ts'` — update its exclusion comment to confirm `measure/estimator.ts` remains the only excluded file.

    Extend `tests/unit/estimate/pt-px-conversion-source.test.ts`'s `CLEAN_SOURCES` array to ALSO include `'lib/estimate/pagination/blocks-from-model.ts'` (guards against a future bare `612`/`792`/`816`/`1056` literal — this file must always go through `ESTIMATE_PAGE_GEOMETRY`, never re-derive page geometry itself).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pagination/blocks-from-model.test.ts tests/unit/pagination/pagination-engine-boundary.test.ts tests/unit/estimate/pt-px-conversion-source.test.ts</automated>
  </verify>
  <done>blocksFromModel() is exported and passes every behavior case above (empty-description filter, all 3 visibility gates, ref population, defensive input, exact ID naming); the boundary-purity test now also covers blocks-from-model.ts; pt-px-conversion-source.test.ts's CLEAN_SOURCES now includes blocks-from-model.ts, all green.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/pagination` — full pagination test directory green (boundary, rules, engine from 184-02; measure/fontkit-arithmetic, measure/safety-margin from 184-01; measure/estimator, blocks-from-model from this plan).
`npx vitest run tests/unit/estimate/pt-px-conversion-source.test.ts` — green with blocks-from-model.ts added to CLEAN_SOURCES.
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
</verification>

<success_criteria>
A real estimate's document model can be turned into fully-`ref`-populated `PageBlock[]` via `blocksFromModel()` and measured via `createFontkitMeasurementProvider()`, ready for Plan 184-05 to feed both into `computePageBreaks()` — with the empty-description filter AND every presentation-settings visibility gate preserved exactly, zero drift from Plan 184-01's validated measurement formula, and zero re-derived geometry/line-height numbers (all sourced from `lib/estimate/document/tokens.ts`).
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-03-SUMMARY.md` documenting `blocksFromModel`'s exact input shape (field-by-field), the `ref` population rules per kind, the exact ID-naming convention, and `createFontkitMeasurementProvider`'s signature — Plan 184-05 wires both directly from this summary.
</output>
