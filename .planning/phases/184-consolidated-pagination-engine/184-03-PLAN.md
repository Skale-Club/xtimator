---
phase: 184-consolidated-pagination-engine
plan: 03
type: execute
wave: 3
depends_on: ["184-01", "184-02"]
files_modified:
  - lib/estimate/pagination/measure/estimator.ts
  - lib/estimate/pagination/blocks-from-model.ts
  - tests/unit/pagination/measure/estimator.test.ts
  - tests/unit/pagination/blocks-from-model.test.ts
  - tests/unit/pagination/pagination-engine-boundary.test.ts
  - tests/unit/estimate/pt-px-conversion-source.test.ts
autonomous: true
requirements: [PGBRK-01, PGBRK-05]

must_haves:
  truths:
    - "Web and PDF measurement share the same registered font family (TTF) with a measurement provider built on fontkit + linebreak."
    - "An estimate section with only empty-description items produces zero item-row blocks and no orphaned section-header/subtotal for it."
    - "Sections/summary/photos each respect their own presentation-settings visibility gate — a hidden section produces zero blocks for it, not just a visually-hidden one."
    - "Terms cards are emitted in a fixed, pinned order (estimate, payment, timeline, warranty, notes) regardless of which subset is present."
  artifacts:
    - path: "lib/estimate/pagination/measure/estimator.ts"
      provides: "Server-only MeasurementProvider implementation (fontkit + linebreak), matching Plan 184-01's hand-validated formula exactly"
      contains: "export function createFontkitMeasurementProvider"
    - path: "lib/estimate/pagination/blocks-from-model.ts"
      provides: "DocumentSection[]/signature/photos/terms -> PageBlock[] (structure only), populating PageBlock.ref for every block, using ESTIMATE_PAGE_GEOMETRY/LINE_HEIGHT/photosPerRow/visibleSectionItems from lib/estimate/document/ — no re-derived numbers, no third filter implementation, zero import from components/pdf/*"
      contains: "export function blocksFromModel"
  key_links:
    - from: "lib/estimate/pagination/measure/estimator.ts"
      to: "lib/pdf/register-fonts.ts"
      via: "same 4 registered family names (Inter, Inter-Bold, Lora, Lora-Bold) map to the same vendored TTF paths"
      pattern: "Inter-Bold|Lora-Bold"
    - from: "lib/estimate/pagination/blocks-from-model.ts"
      to: "lib/estimate/document/visible-items.ts"
      via: "imports visibleSectionItems() instead of re-implementing the empty-description filter"
      pattern: "visibleSectionItems"
    - from: "lib/estimate/pagination/blocks-from-model.ts"
      to: "lib/estimate/document/tokens.ts"
      via: "imports photosPerRow + reads ESTIMATE_PAGE_GEOMETRY + LINE_HEIGHT — never from components/pdf/*"
      pattern: "photosPerRow|ESTIMATE_PAGE_GEOMETRY|LINE_HEIGHT"
---

<revision_note>
**Final revision per plan-checker re-verification.** Changes from the prior version:
- **Blocker 1:** `photosPerRow` now imports from `lib/estimate/document/tokens.ts` (Plan 184-01), NOT from `components/pdf/shared/pdf-photo-grid.tsx` (Plan 184-04) — importing a `components/pdf/*` file from the client-safe pagination core would poison the import-boundary guarantee. `depends_on` DROPS `184-04` (was `["184-01","184-02","184-04"]`, now `["184-01","184-02"]`) — stays Wave 3 (unaffected, since `184-02` alone already forces wave 3).
- **Warning 11:** `blocksFromModel` now imports `visibleSectionItems` from `lib/estimate/document/visible-items.ts` (Plan 184-01) instead of inlining its own copy of the empty-description filter.
- **Blocker 3 + Warning 9:** new explicit behavior case pinning terms-card emission ORDER (estimate → payment → timeline → warranty → notes); the FIRST terms-card block and the FIRST photo-row block each get an extra one-time `baseHeightPt` bonus (the margin that used to live on the shared outer container, now that each card/row is independently placed — see `<height_formulas>`).
</revision_note>

<objective>
Give the pure engine (Plan 184-02) its two real-world inputs: (1) a server-side `MeasurementProvider` backed by the exact fontkit/linebreak code `@react-pdf/pdfkit` renders with, and (2) a pure `blocksFromModel()` function that turns a real estimate's document model into the `PageBlock[]` the engine consumes — replicating today's empty-description filtering (via the shared `visibleSectionItems`) and every presentation-settings visibility gate exactly, populating `ref` on every block, pinning terms-card emission order, and reading ALL geometry/line-height/chunking numbers from Plan 184-01's shared `lib/estimate/document/` source.

Purpose: PGBRK-05's "measurement provider built on react-pdf's own transitive deps" and PGBRK-01's "single source of truth" both require this glue to exist before the PDF templates (Plan 184-05) can call `computePageBreaks()` for real.
Output: `lib/estimate/pagination/measure/estimator.ts`, `lib/estimate/pagination/blocks-from-model.ts`, both fully unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-RESEARCH.md
@.planning/phases/184-consolidated-pagination-engine/184-01-SUMMARY.md
@.planning/phases/184-consolidated-pagination-engine/184-02-SUMMARY.md
@lib/pdf/register-fonts.ts
@lib/estimate/document/model.ts
@lib/estimate/document/tokens.ts
@lib/estimate/document/visible-items.ts
@lib/estimate/presentation-settings.ts
@components/pdf/estimate-pdf.tsx
@components/pdf/shared/pdf-terms-section.tsx
@tests/unit/estimate/pt-px-conversion-source.test.ts

<interfaces>
From Plan 184-02's `lib/estimate/pagination/measure/types.ts` (already committed):
```ts
export interface MeasurementProvider {
  lineCount(text: string, styleKey: string, fontSizePt: number, maxWidthPt: number): number
}
```

From Plan 184-02's `lib/estimate/pagination/types.ts` (already committed — this plan's `blocksFromModel` emits arrays of these):
```ts
export type PageBlockKind =
  | 'title-banner' | 'info-grid' | 'summary' | 'section-header' | 'item-row'
  | 'section-subtotal' | 'totals' | 'terms-card' | 'signature' | 'photo-row' | 'prepared-by'

export interface PageBlockRef {
  sectionId?: string; itemId?: string; itemIndex?: number
  termsKey?: 'estimate' | 'payment' | 'timeline' | 'warranty' | 'notes'
  photoRange?: [number, number]
}

export interface PageBlock {
  kind: PageBlockKind
  id: string
  baseHeightPt: number
  measurement?: { text: string; styleKey: string; fontSizePt: number; lineHeightMultiplier: number; maxWidthPt: number }
  keepWithNextId?: string
  keepWithPreviousId?: string
  atomic: boolean
  page1Only?: boolean   // ONLY 'title-banner' | 'info-grid' | 'summary' set this — NOT 'prepared-by'
  ref?: PageBlockRef
}
```

From Plan 184-01's `lib/estimate/pagination/measure/safety-margin.ts` (already committed — NOT consumed here, margin is applied per-page by Plan 184-05's caller):
```ts
export const SAFETY_MARGIN_LINES: number
```

From Plan 184-01's `lib/estimate/document/tokens.ts` (already committed):
```ts
export const LINE_HEIGHT: Record<string, number> // Inter: 1.21, 'Inter-Bold': 1.21, Lora: 1.28, 'Lora-Bold': 1.28 — ONLY tableCellText/sectionTitle
export const ESTIMATE_PAGE_GEOMETRY: Record<EstimateTemplateId, EstimatePageGeometry>
// { contentWidthPt, topPaddingPt, bottomPaddingPt, colDescriptionWidthPt, tableCellFontSizePt,
//   sectionTitleFontSizePt, termsTextFontSizePt, summaryFontSizePt, proseLineHeightMultiplier }
export function photosPerRow(contentWidthPt: number): number  // Math.floor(contentWidthPt / (150 + 8)) — Plan 184-01's tokens.ts, NOT components/pdf/*
```

From Plan 184-01's `lib/estimate/document/visible-items.ts` (already committed):
```ts
export function visibleSectionItems(section: DocumentSection): DocumentItem[]  // section.items.filter(i => i.description.trim() !== '')
```

From `lib/pdf/register-fonts.ts` (existing, unchanged) — the ONLY 4 valid `styleKey` values this phase's real estimator resolves:
```ts
Font.register({ family: 'Inter', src: '.../inter/Inter-Regular.ttf' })
Font.register({ family: 'Inter-Bold', src: '.../inter/Inter-Bold.ttf' })
Font.register({ family: 'Lora', src: '.../lora/Lora-Regular.ttf' })
Font.register({ family: 'Lora-Bold', src: '.../lora/Lora-Bold.ttf' })
```

From `components/pdf/shared/pdf-terms-section.tsx:68-99` (the EXACT title/text/order mapping `blocksFromModel` must pin — mirrored 1:1 by Plan 184-05's dispatcher too):
```
estimate: { title: 'Estimate Terms', text: company.estimate_terms_text, titleColor: brandText }
payment:  { title: L.paymentTerms,   text: estimate.payment_terms }
timeline: { title: L.timeline,       text: estimate.timeline }
warranty: { title: L.warranty,       text: estimate.warranty_terms }
notes:    { title: L.notes,          text: estimate.notes }
```
Emission order (when each is present/visible): `estimate → payment → timeline → warranty → notes` — exactly `pdf-terms-section.tsx`'s current Fragment order.
</interfaces>

<height_formulas>
**Every block kind's `baseHeightPt`/`measurement` formula, cited to its EXACT source StyleSheet key/value (both templates). All numbers below MUST be read from `ESTIMATE_PAGE_GEOMETRY`/`LINE_HEIGHT` (Plan 184-01) — shown here ONLY to confirm what those tokens already equal; never re-type them as bare literals in `blocks-from-model.ts`. `lineHeightMultiplier` for `item-row`/`section-header` comes from `LINE_HEIGHT[fontFamily]` (font-metrics-derived, 1.21 Inter/1.28 Lora); for `summary`/`terms-card`/`info-grid`/`prepared-by`/`title-banner` it comes from `ESTIMATE_PAGE_GEOMETRY[template].proseLineHeightMultiplier` (already-explicit, 1.5 Classic/1.6 Modern) — do NOT swap these two.**

| Kind | Classic (`estimate-pdf.tsx`) | Modern (`estimate-pdf-modern.tsx`) |
|---|---|---|
| `title-banner` (measured, `proseLineHeightMultiplier`) | `estimateTitle` (fontSize 24, marginBottom 20) + banner `paddingVertical` 16×2 | `estimateTitle` (fontSize 13, marginBottom 6) + `estimateTitleRule` (marginBottom 28) |
| `info-grid` (measured, `proseLineHeightMultiplier`) | `infoRow` (marginBottom 20) + `infoLabel` (fontSize 8, marginBottom 4) + `infoValue` (fontSize 10, lineHeight 1.5) × up to 4 lines (project/date/estimate# + optional client block) | `infoRow` (marginBottom 28) + `infoLabel` (fontSize 8, marginBottom 5) + `infoValue` (fontSize 10, lineHeight 1.6) |
| `summary` (measured, `proseLineHeightMultiplier`) | `infoLabel` + `termsText` (fontSize 9, lineHeight 1.5, marginBottom 12), `maxWidthPt = ESTIMATE_PAGE_GEOMETRY.classic.contentWidthPt` | `infoLabel` + `termsText` (fontSize 9, lineHeight 1.6, marginBottom 14) |
| `section-header` (measured, `LINE_HEIGHT`) | `sectionHeader` (padding 8, marginTop 16) + `sectionTitle` (fontSize 11, `LINE_HEIGHT['Inter-Bold']`=1.21) | `sectionHeader` (paddingVertical 6, marginTop 22, borderBottomWidth 1) + `sectionTitle` (fontSize 11, `LINE_HEIGHT['Lora-Bold']`=1.28) |
| `item-row` (measured, `LINE_HEIGHT` — the ONE field driving row-height variance) | `tableRow` (paddingVertical 6×2=12 `baseHeightPt`) + `tableCellText` (fontSize `ESTIMATE_PAGE_GEOMETRY.classic.tableCellFontSizePt`=9, `LINE_HEIGHT.Inter`=1.21, `maxWidthPt = ESTIMATE_PAGE_GEOMETRY.classic.colDescriptionWidthPt`) | `tableRow` (paddingVertical 8×2=16) + `tableCellText` (fontSize 9.5, `LINE_HEIGHT.Lora`=1.28, `maxWidthPt=203.2`) |
| `section-subtotal` (fixed, no measurement — numeric/label cells, single line per research) | `sectionSubtotal` (paddingVertical 6×2=12, borderTopWidth 1) + `sectionSubtotalLabel`/`Value` (fontSize 9) | `sectionSubtotal` (paddingVertical 8×2=16, borderTopWidth 0.5) + labels fontSize 9 |
| `totals` (fixed, no measurement) | `totalsContainer` (marginTop 20) + `totalsRow` (paddingVertical 4×2=8) × (1 subtotal + [discount] + [tax]) + `grandTotalRow` (paddingVertical 8×2=16, borderTopWidth 2, fontSize 14) + [deposit row(s)] × 8 each | `totalsContainer` (marginTop 28) + `totalsRow` (paddingVertical 6×2=12) × N + `grandTotalBlock` (marginTop 16 + label fontSize 9 + value fontSize 30) — see `pdf-totals-block.tsx`'s `dep.showDeposit`/`discount_amount>0`/`tax_amount>0` gates for row count |
| `terms-card` (measured, `proseLineHeightMultiplier`) | `termsTitle` (fontSize 11, marginBottom 6, paddingBottom 4, borderBottomWidth 1) + `termsText` (fontSize 9, lineHeight 1.5, marginBottom 12), `maxWidthPt = contentWidthPt`. **The FIRST emitted terms-card block ONLY gets an extra `+24pt` (`termsSection.marginTop`, Classic) / `+32pt` (Modern) added to its `baseHeightPt`** — the margin that used to live on the shared outer `termsSection` container before Plan 184-04's per-card restructure (Plan-checker warning 9). ALL OTHER terms-card blocks get `+0` for this term. | Same, Modern's bonus is `+32pt` on the first card only. |
| `signature` (fixed) | `pdf-signature-block.tsx` literals: marginTop 16 + `termsTitle` label line + `Image` height 40 + 2 text lines (fontSize 9 each) — byte-identical shared component, same formula both templates |
| `photo-row` (fixed) | `pdf-photo-grid.tsx` literals: image 150 + gap 8 + optional caption line (fontSize 8, marginTop 2) IF any photo in the chunk has a caption. **The FIRST photo-row block ONLY (i.e., the block whose `ref.photoRange[0] === 0`) gets an extra `+16pt` (Classic) / `+20pt` (Modern) added to its `baseHeightPt`** — the `topMargin` that used to sit on the shared outer grid wrapper (Plan-checker warning 9). ALL OTHER photo-row blocks get `+0`. | Same, Modern's bonus is `+20pt` on the first row only. |
| `prepared-by` (measured, `proseLineHeightMultiplier`, ordinary atomic — NOT page1Only) | `infoLabel` (fontSize 8) + `infoValue` (fontSize 10, lineHeight 1.5), marginTop 16 | `infoLabel` + `infoValue` (fontSize 10, lineHeight 1.6), marginTop 20 |
</height_formulas>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-only fontkit measurement provider</name>
  <files>lib/estimate/pagination/measure/estimator.ts, tests/unit/pagination/measure/estimator.test.ts</files>
  <read_first>lib/estimate/pagination/measure/types.ts (the MeasurementProvider contract), lib/pdf/register-fonts.ts (family->path pairs)</read_first>
  <behavior>
    - `createFontkitMeasurementProvider().lineCount('HHHH HHHH HHHH', 'Inter', 9, 30)` returns `3` (matches Plan 184-01's hand-calculated arithmetic test exactly — same font, same formula).
    - Same call with `maxWidthPt: 60` returns `2`; with `100` returns `1`.
    - `lineCount('', 'Inter', 9, 100)` returns `0`.
    - Calling `lineCount` twice with identical args returns the identical number (deterministic; no per-call font re-parsing side effect visible to the caller).
    - `lineCount(text, 'Lora', ...)` and `lineCount(text, 'Inter', ...)` for the SAME text/size/width can legitimately differ (different glyph metrics) — assert they don't throw and both return positive integers for non-empty text.
  </behavior>
  <action>
    Create `lib/estimate/pagination/measure/estimator.ts` (SERVER ONLY — this is the one file in `lib/estimate/pagination/` allowed to import `fontkit`, `linebreak`, and `node:path`). Implement:
    - `const FONT_FAMILY_TO_PATH: Record<string, string>` mapping exactly `'Inter'`, `'Inter-Bold'`, `'Lora'`, `'Lora-Bold'` to their `public/fonts/{inter,lora}/*.ttf` paths (mirror `lib/pdf/register-fonts.ts`'s own `Font.register` calls verbatim — do not invent new family names or paths).
    - A module-scope `Map<string, ReturnType<typeof fontkit.openSync>>` font cache keyed by family name, opened lazily on first use (mirrors `register-fonts.ts`'s "register once" discipline — never re-parse the same TTF per call).
    - `export function estimateLineCount(text: string, fontFamily: string, fontSizePt: number, maxWidthPt: number): number` implementing the EXACT greedy line-packer validated in Plan 184-01's `tests/unit/pagination/measure/fontkit-arithmetic.test.ts` (same chunk/scale formula — no reinterpretation, NO margin term — margin is applied per-page by Plan 184-05's caller, never here).
    - `export function createFontkitMeasurementProvider(): MeasurementProvider` returning `{ lineCount: estimateLineCount }` (satisfies `measure/types.ts`'s interface with `styleKey` as the font family name).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pagination/measure/estimator.test.ts</automated>
  </verify>
  <done>estimator.ts exports `estimateLineCount` + `createFontkitMeasurementProvider`; its output for the Plan 184-01 fixture string matches that plan's hand-calculated numbers exactly, confirming no drift between the spike's throwaway math and the shipped implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: blocksFromModel() — document model to PageBlock[], with ref population, visibility gates, pinned terms order, and token-sourced geometry</name>
  <files>lib/estimate/pagination/blocks-from-model.ts, tests/unit/pagination/blocks-from-model.test.ts, tests/unit/pagination/pagination-engine-boundary.test.ts, tests/unit/estimate/pt-px-conversion-source.test.ts</files>
  <read_first>lib/estimate/document/model.ts (DocumentSection/DocumentItem/DocumentSignature/DocumentPhoto), lib/estimate/document/tokens.ts (LINE_HEIGHT/ESTIMATE_PAGE_GEOMETRY/photosPerRow — Plan 184-01), lib/estimate/document/visible-items.ts (visibleSectionItems — Plan 184-01), lib/estimate/presentation-settings.ts (ResolvedPresentationSettings/isSectionVisible), components/pdf/shared/pdf-totals-block.tsx (which totals rows are conditionally visible: discount_amount>0, tax_amount>0, dep.showDeposit), components/pdf/shared/pdf-terms-section.tsx:68-99 (the exact 5-card title/text/order mapping in `<interfaces>` to mirror), the `<height_formulas>` table above</read_first>
  <behavior>
    - **Empty-description filter (via `visibleSectionItems`):** a section where every item has an empty/whitespace-only `description` → zero `item-row` blocks for it, AND no `section-header`/`section-subtotal` block emitted for that section either. A section with 1 real item + 1 empty-description item → exactly 1 `item-row` block. (Uses `visibleSectionItems(section)` — no inline re-implementation of the filter.)
    - **Visibility gate — sections:** `isSectionVisible(resolvedSettings, 'sections') === false` → zero `section-header`/`item-row`/`section-subtotal` blocks emitted, even when `estimate.sections` has real items.
    - **Visibility gate — summary:** `isSectionVisible(resolvedSettings, 'summary') === false` OR `estimate.summary` falsy → zero `summary` blocks (both conditions independently tested).
    - **Visibility gate — photos:** `isSectionVisible(resolvedSettings, 'photos') === false` → zero `photo-row` blocks, even when `photos` is a non-empty array.
    - **Terms-card mapping + pinned order (Plan-checker blocker 3):** all 5 terms fields populated → exactly 5 `terms-card` blocks, each independently `atomic: true`, no `keepWith*` links between them, each with `ref.termsKey` set to one of `'estimate' | 'payment' | 'timeline' | 'warranty' | 'notes'`, emitted in EXACTLY that order (estimate → payment → timeline → warranty → notes) regardless of which subset is actually present (e.g., if only `payment` and `notes` are present, they still emit in `payment, notes` relative order — never `notes, payment`).
    - **First-block height bonus (Plan-checker warning 9):** given 2 terms-card blocks, only the FIRST one (`estimate`, if present, else whichever comes first) has its `baseHeightPt` inflated by the template's `termsSection.marginTop` bonus (24pt Classic / 32pt Modern); the second has no bonus. Given 2 photo-row blocks, only the block with `ref.photoRange[0] === 0` has its `baseHeightPt` inflated by the template's photo-grid `topMargin` bonus (16pt Classic / 20pt Modern); the other has no bonus.
    - 7 photos at `contentWidthPt: 532` (Classic) → 3 `photo-row` blocks chunked `[3, 3, 1]` via the IMPORTED `photosPerRow(532) === 3` from `lib/estimate/document/tokens.ts` (NOT from `components/pdf/*`) — each block's `ref.photoRange` is `[0,3]`, `[3,6]`, `[6,7]` respectively.
    - No `signature` input → zero `signature` blocks. No `preparedBy` input → zero `prepared-by` blocks. When present, the `prepared-by` block does NOT set `page1Only` (it is an ordinary atomic flowing block per Plan 184-02's revised taxonomy).
    - **`ref` population:** every `section-header`/`item-row`/`section-subtotal` block has `ref.sectionId` set to its section's id; every `item-row` additionally has `ref.itemId` (the item's id) and `ref.itemIndex` (its 0-based index WITHIN the section's FILTERED/surviving item list from `visibleSectionItems`, not the raw pre-filter list).
    - **Defensive input:** calling `blocksFromModel({ sections: undefined, ... })` (or omitting `sections` entirely) does NOT throw — treated as `[]`.
    - **ID naming convention:** a section's header block id is EXACTLY `` `${section.id}-header` ``; each item-row block id is EXACTLY `` `${section.id}-rows-${item.id}` ``; the subtotal block id is EXACTLY `` `${section.id}-subtotal` ``.
    - Output array order matches document order: `title-banner`, `info-grid`, `summary` (if visible+present), then PER SECTION `section-header` → its `item-row`s → `section-subtotal` (with the header's `keepWithNextId` pointing at the FIRST item-row's id and the subtotal's `keepWithPreviousId` pointing at the LAST item-row's id — both array-adjacent), then `totals`, then up to 5 `terms-card`s (pinned order), then `signature` (if present), then `photo-row`s (if visible), then `prepared-by` (if present).
    - Every block `id` is stable/deterministic — never `Math.random()`.
  </behavior>
  <action>
    Create `lib/estimate/pagination/blocks-from-model.ts` exporting `blocksFromModel(input): PageBlock[]`. Design `input` to carry: `sections: DocumentSection[] | undefined` (accessed defensively as `input.sections ?? []`), `summary`/`timeline`/`payment_terms`/`warranty_terms`/`notes`/company terms text (`estimate_terms_enabled`/`estimate_terms_text`), `discount_amount`/`tax_amount`/`dep: DepositDisplay` (for totals row-count), `signature: DocumentSignature | null`, `photos: { url: string; caption: string | null }[]`, `resolvedSettings: ResolvedPresentationSettings`, `preparedBy: string | null`, `L: DocumentLabels` (for `L.paymentTerms`/`L.timeline`/`L.warranty`/`L.notes` — needed only for the OTHER PDF layer's title lookup, per Plan 184-05's dispatcher; `blocksFromModel` itself just needs to know WHICH terms are present, not their display titles), and `templateId: EstimateTemplateId` (used to look up `ESTIMATE_PAGE_GEOMETRY[templateId]` and the corresponding `fontFamily`/`fontFamilyBold` from `ESTIMATE_DESIGN_TOKENS[templateId]` — NEVER hardcode 'Inter'/'Lora'/532/508/9/9.5/150/8/24/32/16/20 as bare literals in this file; always read them from `ESTIMATE_PAGE_GEOMETRY`/`ESTIMATE_DESIGN_TOKENS`/`LINE_HEIGHT`/`photosPerRow`).

    Gate sections on `isSectionVisible(resolvedSettings, 'sections')` (skip entirely, zero blocks, if false) BEFORE calling `visibleSectionItems(section)` per section (imported from `@/lib/estimate/document/visible-items`), then drop sections with zero remaining items. Gate `summary` on BOTH `isSectionVisible(resolvedSettings, 'summary')` AND `estimate.summary` being truthy. Gate `photo-row` emission on `isSectionVisible(resolvedSettings, 'photos')` (in addition to `photos.length > 0`).

    Build the terms-card list by iterating a FIXED, HARDCODED order array `['estimate', 'payment', 'timeline', 'warranty', 'notes'] as const`, including a `termsKey` in the output ONLY when its underlying gate is satisfied (`estimate`: `company.estimate_terms_enabled && company.estimate_terms_text`; `payment`: `isSectionVisible(resolvedSettings,'payment_terms') && estimate.payment_terms`; `timeline`: `isSectionVisible(resolvedSettings,'timeline') && estimate.timeline`; `warranty`: `isSectionVisible(resolvedSettings,'warranty_terms') && estimate.warranty_terms`; `notes`: `isSectionVisible(resolvedSettings,'notes') && estimate.notes`) — this fixed iteration order is what pins the emission order regardless of which subset is present.

    For EVERY block, populate `id` per the naming convention in `<behavior>` and `ref` per `PageBlockRef`'s fields — `terms-card` blocks get `ref.termsKey`, `photo-row` blocks get `ref.photoRange`.

    Compute EVERY block kind's `baseHeightPt`/`measurement` exactly per the `<height_formulas>` table in `<context>` above — reading `ESTIMATE_PAGE_GEOMETRY[templateId]`/`LINE_HEIGHT[fontFamily]` for every number. Apply the first-terms-card and first-photo-row height bonuses exactly as `<height_formulas>` describes (track "is this the first one emitted" with a simple local boolean while building each list — no cross-block lookups needed). For `photo-row`, chunk `photos` via the IMPORTED `photosPerRow(ESTIMATE_PAGE_GEOMETRY[templateId].contentWidthPt)` from `@/lib/estimate/document/tokens` (NOT from `components/pdf/shared/pdf-photo-grid`).

    Extend `tests/unit/pagination/pagination-engine-boundary.test.ts`'s target-files array (created in Plan 184-02) to ALSO include `'lib/estimate/pagination/blocks-from-model.ts'` — update its exclusion comment to confirm `measure/estimator.ts` remains the only excluded file.

    Extend `tests/unit/estimate/pt-px-conversion-source.test.ts`'s `CLEAN_SOURCES` array to ALSO include `'lib/estimate/pagination/blocks-from-model.ts'` (guards against a future bare `612`/`792`/`816`/`1056` literal — this file must always go through `ESTIMATE_PAGE_GEOMETRY`, never re-derive page geometry itself).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pagination/blocks-from-model.test.ts tests/unit/pagination/pagination-engine-boundary.test.ts tests/unit/estimate/pt-px-conversion-source.test.ts</automated>
  </verify>
  <done>blocksFromModel() is exported and passes every behavior case above (empty-description filter via visibleSectionItems, all 3 visibility gates, ref population, defensive input, exact ID naming, pinned terms-card order, first-block height bonuses); the boundary-purity test now also covers blocks-from-model.ts; pt-px-conversion-source.test.ts's CLEAN_SOURCES now includes blocks-from-model.ts, all green.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/pagination` — full pagination test directory green (boundary, rules, engine from 184-02; measure/fontkit-arithmetic, measure/safety-margin, pagination-tokens from 184-01; measure/estimator, blocks-from-model from this plan).
`npx vitest run tests/unit/estimate/pt-px-conversion-source.test.ts` — green with blocks-from-model.ts added to CLEAN_SOURCES.
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
</verification>

<success_criteria>
A real estimate's document model can be turned into fully-`ref`-populated `PageBlock[]` via `blocksFromModel()` and measured via `createFontkitMeasurementProvider()`, ready for Plan 184-05 to feed both into `computePageBreaks()` — with the empty-description filter, every presentation-settings visibility gate, and the pinned terms-card emission order preserved exactly, zero drift from Plan 184-01's validated measurement formula, and zero re-derived geometry/line-height/chunking numbers or duplicated filter logic (all sourced from `lib/estimate/document/`).
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-03-SUMMARY.md` documenting `blocksFromModel`'s exact input shape (field-by-field), the `ref` population rules per kind, the exact ID-naming convention, the pinned terms-card order + title/text mapping, and `createFontkitMeasurementProvider`'s signature — Plan 184-05 wires both directly from this summary.
</output>
