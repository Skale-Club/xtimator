---
phase: 184-consolidated-pagination-engine
plan: 04
type: execute
wave: 2
depends_on: ["184-01"]
files_modified:
  - components/pdf/shared/pdf-section-block.tsx
  - components/pdf/shared/pdf-photo-grid.tsx
  - components/pdf/shared/pdf-terms-section.tsx
  - components/pdf/shared/pdf-totals-block.tsx
  - components/pdf/estimate-pdf.tsx
  - components/pdf/estimate-pdf-modern.tsx
  - tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx
  - tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx
autonomous: true
requirements: [PGBRK-02]

must_haves:
  truths:
    - "A section's header, table-header row, item rows, and subtotal can each be placed independently (a section CAN span a page boundary) without changing today's single-page visual output."
    - "The photo grid breaks only between visual rows, never mid-row (currently the WHOLE grid is one atomic block, stricter than the locked rule)."
    - "Each terms card (Estimate Terms/Payment/Timeline/Warranty/Notes) renders fully on one page, never split (currently NONE of them are individually atomic)."
    - "The totals block's outer container is explicitly wrap={false} (currently it relies on default Yoga wrap, NOT atomic today — a real gap)."
  artifacts:
    - path: "components/pdf/shared/pdf-section-block.tsx"
      provides: "4 independently-callable pieces: PdfSectionHeader, PdfTableHeaderOnly, PdfSectionRows, PdfSectionSubtotal (replacing the single monolithic PdfSectionBlock), each setting a stable React key on its own root element"
      contains: "export function PdfSectionHeader"
    - path: "components/pdf/shared/pdf-photo-grid.tsx"
      provides: "Row-chunked photo grid (one wrap={false} View PER ROW, each optionally getting the first-row topMarginPt) — chunking via the IMPORTED photosPerRow from lib/estimate/document/tokens.ts (NOT defined here — Plan-checker blocker 1)"
      contains: "import { photosPerRow }"
    - path: "components/pdf/shared/pdf-terms-section.tsx"
      provides: "PdfTermsCard({ title, text, titleColor?, topMarginPt?, styles }) — one atomic card, exported for direct per-card rendering by Plan 184-05; PdfTermsSection composes it internally for single-page reproduction"
      contains: "export function PdfTermsCard"
    - path: "components/pdf/shared/pdf-totals-block.tsx"
      provides: "wrap={false} added to the outer totalsContainer View (both 'classic' and 'modern' branches) — closes a real pre-existing atomicity gap"
  key_links:
    - from: "components/pdf/estimate-pdf.tsx"
      to: "components/pdf/shared/pdf-section-block.tsx"
      via: "4 sequential calls per section (header, table-header, rows, subtotal) reproducing today's single-page output, each with a stable key"
      pattern: "PdfSectionHeader|PdfTableHeaderOnly|PdfSectionRows|PdfSectionSubtotal"
    - from: "components/pdf/estimate-pdf.tsx"
      to: "lib/estimate/document/tokens.ts"
      via: "PdfPhotoGrid call site passes ESTIMATE_PAGE_GEOMETRY.classic.contentWidthPt (Modern: .modern.contentWidthPt) — no bare 532/508 literal"
      pattern: "ESTIMATE_PAGE_GEOMETRY"
    - from: "tests/unit/pdf/estimate-pdf-baseline-order.test.tsx"
      to: "components/pdf/estimate-pdf.tsx"
      via: "existing text-content-order assertions stay green — this plan changes internal structure, not rendered content"
      pattern: "assertBaselineOrder"
---

<revision_note>
**Final revision per plan-checker re-verification.** Changes from the prior version:
- **Blocker 1:** `photosPerRow` is NO LONGER defined/exported here — Task 2 now IMPORTS it from `lib/estimate/document/tokens.ts` (Plan 184-01). `pdf-photo-grid.tsx` still owns the 150pt/8pt VISUAL rendering (tile width/gap in its `StyleSheet`), but the CHUNKING FORMULA itself lives in `tokens.ts` so `blocks-from-model.ts` (184-03) never has to import from `components/pdf/*`.
- **Warning 5:** `PdfSectionHeader`/`PdfTableHeaderOnly`/`PdfSectionRows`/`PdfSectionSubtotal` each now accept a `sectionId: string` prop used ONLY to set an explicit `key` on their own root returned element (`` `${sectionId}-header` ``/`` `-thead` ``/`` `-rows` ``/`` `-subtotal` ``) — required because these 4 pieces are combined as sibling ARRAY ITEMS per section (`.map((section) => [...4 calls...])`), and React needs each to carry its own distinguishing key, not just one section-level key on an outer wrapper that no longer exists.
- **Warning 9:** `PdfTermsCard` gains an optional `topMarginPt?: number` prop (extra `marginTop` on its own `wrap={false}` View, used ONLY for the first-emitted terms-card, per Plan 184-03's height-bonus rule). `PdfPhotoGrid`'s EXISTING `topMargin` prop is now explicitly clarified to apply ONLY to the FIRST row-chunk's View (not some non-existent whole-grid wrapper).
- **Warning 10:** Task 2's template call sites now pass `ESTIMATE_PAGE_GEOMETRY.classic.contentWidthPt` / `.modern.contentWidthPt` (imported from `lib/estimate/document/tokens.ts`) instead of the bare literals `532`/`508`.
- **Warning 11:** Task 1's template call site (single-page section-filtering, used for the 4-piece reproduction) now uses `visibleSectionItems` (Plan 184-01) instead of the template's own inline filter chain — avoiding a third re-implementation of the same logic alongside `blocks-from-model.ts` (184-03) and the 184-05 dispatcher.
</revision_note>

<objective>
Fix the atomicity GAPS between today's PDF components and the locked break rules (photo grid is MORE atomic than required; terms cards and the totals container are LESS atomic than required), and split the monolithic `PdfSectionBlock` into independently-placeable, individually-keyed pieces — all WITHOUT changing today's single-page visual output. This is the prerequisite restructuring Plan 184-05 needs to actually wire per-page rendering.

Purpose: PGBRK-02 requires these exact atomicity guarantees. Shipping Plan 184-05's page-count logic on top of the UNRESTRUCTURED components would be a no-op (a section literally cannot span a page today) or silently wrong (photos/terms/totals would render unpredictably at a real page boundary, or React would warn/misbehave on unkeyed array siblings).
Output: `pdf-section-block.tsx` exports 4 named, individually-keyed pieces; `pdf-photo-grid.tsx` is row-chunked using the SHARED `photosPerRow` from `lib/estimate/document/tokens.ts`; `pdf-terms-section.tsx` exposes a reusable, optionally-margined `PdfTermsCard`; `pdf-totals-block.tsx`'s outer container is `wrap={false}`; both templates' call sites updated to reproduce IDENTICAL single-page output (baseline tests stay green), reading geometry from `ESTIMATE_PAGE_GEOMETRY` and filtering items via `visibleSectionItems`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-01-SUMMARY.md
@components/pdf/shared/pdf-section-block.tsx
@components/pdf/shared/pdf-photo-grid.tsx
@components/pdf/shared/pdf-terms-section.tsx
@components/pdf/shared/pdf-totals-block.tsx
@components/pdf/estimate-pdf.tsx
@components/pdf/estimate-pdf-modern.tsx
@tests/unit/pdf/estimate-pdf-baseline-order.test.tsx
@lib/estimate/document/tokens.ts
@lib/estimate/document/visible-items.ts

<interfaces>
From Plan 184-01's `lib/estimate/document/tokens.ts` (already committed):
```ts
export const LINE_HEIGHT: Record<string, number> // Inter: 1.21, 'Inter-Bold': 1.21, Lora: 1.28, 'Lora-Bold': 1.28
export const ESTIMATE_PAGE_GEOMETRY: Record<EstimateTemplateId, EstimatePageGeometry> // .classic.contentWidthPt===532, .modern.contentWidthPt===508
export function photosPerRow(contentWidthPt: number): number // Math.floor(contentWidthPt / (150 + 8)) — THIS plan imports it, does not define it
```

From Plan 184-01's `lib/estimate/document/visible-items.ts` (already committed):
```ts
export function visibleSectionItems(section: DocumentSection): DocumentItem[]
```

The 11-block taxonomy this restructuring aligns 1:1 with (informational — this plan does NOT import from `lib/estimate/pagination/`, it just needs its output pieces to map cleanly onto Plan 184-05's later dispatch):
```
'section-header'  -> PdfSectionHeader
'item-row'        -> PdfSectionRows (one call per contiguous run of item-row blocks)
'section-subtotal'-> PdfSectionSubtotal
'terms-card'      -> PdfTermsCard (one call per block, driven by block.ref.termsKey)
```
The repeated column header (PGBRK-03) is a separate, always-reusable piece: `PdfTableHeaderOnly` — used both for a section's OWN first page and any continuation page.

Current `PdfSectionBlock` call site (BOTH templates, byte-identical shape) — Task 1 replaces this ONE call with 4 sequential, individually-keyed calls reproducing the identical tree:
```ts
// components/pdf/estimate-pdf.tsx:409-441 (Modern: estimate-pdf-modern.tsx:426-458)
estimate.sections
  .map((section) => ({ ...section, items: section.items.filter((i) => i.description.trim() !== '') }))
  .filter((section) => section.items.length > 0)
  .map((section) => PdfSectionBlock({ section, solidFill, brandColor, brandOnFill, L, fmt, styles: {...9 keys...} }))
```

Current `PdfPhotoGrid` call site (BOTH templates) — Task 2 updates this to pass the new required props, reading `contentWidthPt` from `ESTIMATE_PAGE_GEOMETRY`:
```ts
// components/pdf/estimate-pdf.tsx:497-503 (topMargin: 16); Modern: estimate-pdf-modern.tsx (topMargin: 20)
PdfPhotoGrid({ photos: attachedPhotos, L, topMargin: 16, styles: { termsTitle: styles.termsTitle } })
// becomes (Classic):
PdfPhotoGrid({ photos: attachedPhotos, L, topMargin: 16, contentWidthPt: ESTIMATE_PAGE_GEOMETRY.classic.contentWidthPt, showLabel: true, styles: { termsTitle: styles.termsTitle } })
// (Modern): contentWidthPt: ESTIMATE_PAGE_GEOMETRY.modern.contentWidthPt, topMargin: 20, showLabel: true
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split PdfSectionBlock into 4 independently-placeable, individually-keyed pieces + pin measurement-critical lineHeight + use the shared item filter</name>
  <files>components/pdf/shared/pdf-section-block.tsx, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx</files>
  <read_first>components/pdf/shared/pdf-section-block.tsx (current monolithic implementation, lines 50-123); lib/estimate/document/tokens.ts (LINE_HEIGHT — Plan 184-01); lib/estimate/document/visible-items.ts (visibleSectionItems — Plan 184-01)</read_first>
  <behavior>
    - `PdfSectionHeader({ sectionId, title, solidFill, brandColor, brandOnFill, styles })` renders exactly the header `<View key={`${sectionId}-header`}>`+`<Text>` pair (today's lines 60-77), independent of table-header/rows/subtotal.
    - `PdfTableHeaderOnly({ sectionId, L, styles })` renders exactly the column-header `<View key={`${sectionId}-thead`}>` (today's lines 79-90) — the `sectionId` prop is used purely for the React key; the visual content itself has zero dependency on any specific section (Plan 184-05's continuation-page usage will pass a different contextual id, e.g. derived from the page/section being continued).
    - `PdfSectionRows({ sectionId, items, startIndex, styles })` renders `items` (each row already keyed by `item.id`) inside an outer `<Fragment key={`${sectionId}-rows`}>` (or equivalent single wrapper carrying that key), with zebra striping computed as `(idx + startIndex) % 2` (NOT `idx % 2`) — so a slice starting mid-section (continuation page, Plan 184-05) preserves the correct alternating-row pattern relative to the section's FULL item list.
    - `PdfSectionSubtotal({ sectionId, label, value, styles })` renders exactly the subtotal `<View key={`${sectionId}-subtotal`}>` (today's lines 116-120).
    - Calling all 4 in sequence (header, table-header, rows with `startIndex: 0`, subtotal) for a section produces a text-content-order-IDENTICAL tree to today's single `PdfSectionBlock({...})` call — `tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` passes UNCHANGED.
    - `estimate-pdf.tsx`'s `tableCellText` style now has `lineHeight: 1.21` (`LINE_HEIGHT.Inter`); `estimate-pdf-modern.tsx`'s `tableCellText` has `lineHeight: 1.28` (`LINE_HEIGHT.Lora`) — imported from `tokens.ts`, not a literal.
    - Both templates' section-rendering call site now derives its filtered item list via `visibleSectionItems(section)` (imported from `@/lib/estimate/document/visible-items`) instead of an inline `.filter(i => i.description.trim() !== '')` chain — identical resulting behavior, one less duplicated implementation.
  </behavior>
  <action>
    Replace `pdf-section-block.tsx`'s single `PdfSectionBlock` export with the 4 named exports above (same file — internal module layout is Claude's Discretion), each accepting a `sectionId: string` prop used solely to construct its own root element's `key` per the naming convention (`-header`/`-thead`/`-rows`/`-subtotal`).

    Update BOTH `estimate-pdf.tsx`'s and `estimate-pdf-modern.tsx`'s section-rendering call site: replace the inline `.filter(i => i.description.trim() !== '')`/`.filter(section => section.items.length > 0)` chain with `estimate.sections.map(section => ({ ...section, items: visibleSectionItems(section) })).filter(section => section.items.length > 0)` (importing `visibleSectionItems` from `@/lib/estimate/document/visible-items`), then call all 4 pieces per section, in order, passing `sectionId: section.id` to each and `startIndex: 0` to `PdfSectionRows` (Plan 184-05 wires real per-page slicing later — this task only needs byte-identical single-page reproduction).

    In the SAME task, pin `lineHeight` on the two measurement-critical text styles that currently omit one: import `LINE_HEIGHT` from `@/lib/estimate/document/tokens.ts` in BOTH template files and set `tableCellText.lineHeight = LINE_HEIGHT.Inter` (Classic) / `LINE_HEIGHT.Lora` (Modern), and `sectionTitle.lineHeight = LINE_HEIGHT['Inter-Bold']` (Classic) / `LINE_HEIGHT['Lora-Bold']` (Modern) — these are real, font-metrics-derived values (not guesses), so this is purely additive and must NOT visibly change rendered output for today's single-page fixtures.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-baseline-order.test.tsx tests/unit/pdf/estimate-pdf-banner-fill.test.tsx</automated>
  </verify>
  <done>pdf-section-block.tsx exports PdfSectionHeader/PdfTableHeaderOnly/PdfSectionRows/PdfSectionSubtotal, each setting a stable key on its own root element (no more monolithic PdfSectionBlock); both templates compile, filter items via visibleSectionItems, and call all 4 per section; tableCellText/sectionTitle have lineHeight sourced from LINE_HEIGHT in both templates; baseline-order and banner-fill tests remain green unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Row-chunk PdfPhotoGrid via the shared photosPerRow (+ first-row topMargin) + per-card-atomic PdfTermsSection (+ exported PdfTermsCard with optional topMarginPt) + wrap={false} on totals container + token-sourced call sites</name>
  <files>components/pdf/shared/pdf-photo-grid.tsx, components/pdf/shared/pdf-terms-section.tsx, components/pdf/shared/pdf-totals-block.tsx, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx</files>
  <read_first>components/pdf/shared/pdf-photo-grid.tsx (current whole-grid wrap={false}, lines 38-57), components/pdf/shared/pdf-terms-section.tsx (current shared-View, no per-card atomicity, lines 49-102), components/pdf/shared/pdf-totals-block.tsx (both variant branches' outer `<View style={styles.totalsContainer}>`, lines 93-94 and 135-136 — NEITHER has `wrap={false}` today), lib/estimate/document/tokens.ts (photosPerRow, ESTIMATE_PAGE_GEOMETRY — Plan 184-01)</read_first>
  <behavior>
    - `PdfPhotoGrid` given 7 photos and `contentWidthPt: ESTIMATE_PAGE_GEOMETRY.classic.contentWidthPt` (532) renders 3 top-level row-`<View>`s (chunked via the IMPORTED `photosPerRow(532) === 3`: `[3, 3, 1]`), each with `wrap={false}` — NOT one `wrap={false}` around the whole grid. Only the FIRST row-chunk's View carries `marginTop: topMargin` (the existing `topMargin` prop, now applied per-row instead of to a since-removed outer wrapper).
    - The "Photos" section label renders exactly ONCE, attached only to the FIRST row-chunk (`showLabel` boolean prop, true only for chunk index 0 AND only when the caller passes `showLabel: true`) — it must NOT repeat on every row-chunk.
    - `PdfTermsCard({ title: 'Warranty', text: '1 year', topMarginPt: undefined, styles })` renders ONE `<View wrap={false} key={title}>` containing the title `<Text>` + body `<Text>`; when `topMarginPt` is provided, it's added as extra `marginTop` on this same View (used ONLY for the first-emitted card, per Plan 184-03).
    - `PdfTermsSection` given all 5 terms populated renders 5 calls to `PdfTermsCard` (one per card: Estimate Terms — which ALSO passes `titleColor: brandText` — Payment Terms, Timeline, Warranty, Notes, in that fixed order), passing `topMarginPt` (24 Classic / 32 Modern) ONLY to whichever card is emitted FIRST — replacing the current single shared `<View style={termsSection}>` with no atomicity.
    - `PdfTotalsBlock`'s outer `<View style={styles.totalsContainer}>` now has `wrap={false}` in BOTH the `'classic'` and `'modern'` branches.
    - Photo/caption/terms/totals TEXT CONTENT and ORDER are unchanged from today (existing `estimate-pdf-photo-captions.test.tsx`/`estimate-pdf-totals.test.tsx`/`estimate-pdf-modern-totals.test.tsx` stay green) — only the `wrap`/grouping/margin-placement structure changes.
  </behavior>
  <action>
    Rewrite `pdf-photo-grid.tsx`: import `photosPerRow` from `@/lib/estimate/document/tokens` (do NOT define it locally — Plan-checker blocker 1). Rewrite `PdfPhotoGrid` to accept a new required `contentWidthPt: number` prop and a `showLabel?: boolean` prop, chunk `photos` via `photosPerRow(contentWidthPt)`, render the "Photos" label once (only when `showLabel`), then map each chunk to its own `<View wrap={false}>`, applying `marginTop: topMargin` ONLY to the View for chunk index 0 (the existing `topMargin` prop's new placement — no separate outer wrapper).

    Rewrite `pdf-terms-section.tsx`: add a new export `PdfTermsCard({ title, text, titleColor, topMarginPt, styles }: { title: string; text: string; titleColor?: string; topMarginPt?: number; styles: PdfTermsSectionStyles }) => <View wrap={false} key={title} style={topMarginPt ? { marginTop: topMarginPt } : undefined}><Text style={titleColor ? [styles.termsTitle, {color: titleColor}] : styles.termsTitle}>{title}</Text><Text style={styles.termsText}>{text}</Text></View>`. Rewrite `PdfTermsSection` to compose `PdfTermsCard` internally for each of its 5 conditional cases IN FIXED ORDER (estimate → payment → timeline → warranty → notes, matching Plan 184-03's pinned emission order), passing `titleColor: brandText` ONLY for the "Estimate Terms" card, and passing `topMarginPt: 24` (Classic) / `32` (Modern) ONLY to whichever card ends up being the FIRST one actually rendered (track a local "already emitted one" boolean while composing) — the outer `termsSection` View stays as a non-atomic layout container with NO marginTop of its own anymore (moved onto the first card); each child card is now independently `wrap={false}` via `PdfTermsCard`.

    In `pdf-totals-block.tsx`, add `wrap={false}` to the outer `<View style={styles.totalsContainer}>` in BOTH the `variant === 'classic'` return and the `variant === 'modern'` return (2 edits, same file).

    Update BOTH templates' `PdfPhotoGrid` call sites to import `ESTIMATE_PAGE_GEOMETRY` from `@/lib/estimate/document/tokens` and pass `contentWidthPt: ESTIMATE_PAGE_GEOMETRY.classic.contentWidthPt` (Classic) / `ESTIMATE_PAGE_GEOMETRY.modern.contentWidthPt` (Modern) and `showLabel: true` explicitly — NO bare `532`/`508` literal at the call site (Plan-checker warning 10; see `<interfaces>` above for the exact before/after call shape).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-photo-captions.test.tsx tests/unit/pdf/estimate-pdf-baseline-order.test.tsx tests/unit/pdf/estimate-pdf-totals.test.tsx tests/unit/pdf/estimate-pdf-modern-totals.test.tsx</automated>
  </verify>
  <done>Photo grid renders N row-Views (not 1 grid-wide wrap) via the shared photosPerRow, each individually wrap={false}, label + topMargin applied only to the first chunk; terms section renders up to 5 independently wrap={false} card-Views via PdfTermsCard in pinned order, topMarginPt applied only to the first; totals container is wrap={false} in both variants; template call sites read contentWidthPt from ESTIMATE_PAGE_GEOMETRY; existing caption/order/totals tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Structural atomicity regression tests (photo grid + terms + totals) + full existing PDF suite verification</name>
  <files>tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx, tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx</files>
  <read_first>tests/unit/pdf/estimate-pdf-banner-fill.test.tsx (the style-aware DFS-walker pattern to adapt for `wrap`-prop introspection instead of `backgroundColor`)</read_first>
  <action>
    Write 2 new structural tests using the direct-function-invocation convention (`EstimatePDF({...})`/`EstimatePDFModern({...})` called directly, tree walked via a style/prop-aware DFS — adapt `estimate-pdf-banner-fill.test.tsx`'s walker pattern to check `el.props.wrap === false` instead of `backgroundColor`):
    - `estimate-pdf-photo-grid-atomicity.test.tsx`: given a 7-photo fixture, the returned tree contains exactly 3 `<View>` nodes with `wrap === false` under the photo grid (not 1) — for BOTH templates.
    - `estimate-pdf-terms-atomicity.test.tsx`: given all 5 terms populated, the tree contains exactly 5 `<View>` nodes with `wrap === false` under the terms section — for BOTH templates — PLUS one additional assertion: the totals block's outer container also has `wrap === false`.

    Then run the full existing PDF + estimate test suites to confirm zero regressions from this plan's restructuring.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx` — Classic AND Modern each assert exactly 3 wrap-false nodes
    - `npx vitest run tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx` — Classic AND Modern each assert exactly 5 wrap-false nodes under terms, AND assert the totals outer container is wrap-false
    - `npx vitest run tests/unit/pdf tests/unit/estimate` — 0 failures
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx && npx vitest run tests/unit/pdf tests/unit/estimate</automated>
  </verify>
  <done>Both new atomicity tests pass for both templates (including the totals-container wrap={false} assertion); the full existing tests/unit/pdf + tests/unit/estimate suites remain green (0 regressions from this plan's component restructuring).</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/pdf tests/unit/estimate` — full green, including the 2 new atomicity tests.
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
</verification>

<success_criteria>
Every PDF component gap identified (over-atomic photo grid, under-atomic terms cards, under-atomic totals container, monolithic un-splittable section block, unkeyed array siblings) is closed, verified by new structural tests, with zero change to today's single-page rendered output — the exact prerequisite Plan 184-05 needs before wiring real per-page composition.
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-04-SUMMARY.md` documenting the exact new prop signatures for PdfSectionHeader/PdfTableHeaderOnly/PdfSectionRows/PdfSectionSubtotal (all `sectionId`-keyed)/PdfPhotoGrid (imports photosPerRow from tokens.ts)/PdfTermsSection (+ PdfTermsCard with topMarginPt) — Plan 184-05 wires all of these directly from this summary.
</output>
