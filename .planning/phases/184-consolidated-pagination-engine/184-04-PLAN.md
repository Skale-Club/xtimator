---
phase: 184-consolidated-pagination-engine
plan: 04
type: execute
wave: 2
depends_on: ["184-02"]
files_modified:
  - components/pdf/shared/pdf-section-block.tsx
  - components/pdf/shared/pdf-photo-grid.tsx
  - components/pdf/shared/pdf-terms-section.tsx
  - components/pdf/estimate-pdf.tsx
  - components/pdf/estimate-pdf-modern.tsx
  - tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx
  - tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx
autonomous: true
requirements: [PGBRK-02]

must_haves:
  truths:
    - "A section's header, table-header row, item rows, and subtotal can each be placed independently (a section CAN span a page boundary) without changing today's single-page visual output."
    - "The photo grid breaks only between visual rows, never mid-row (currently the WHOLE grid is one atomic block, stricter than the locked rule)."
    - "Each terms card (Estimate Terms/Payment/Timeline/Warranty/Notes) renders fully on one page, never split (currently NONE of them are individually atomic)."
  artifacts:
    - path: "components/pdf/shared/pdf-section-block.tsx"
      provides: "4 independently-callable pieces: PdfSectionHeader, PdfTableHeaderOnly, PdfSectionRows, PdfSectionSubtotal (replacing the single monolithic PdfSectionBlock)"
      contains: "export function PdfSectionHeader"
    - path: "components/pdf/shared/pdf-photo-grid.tsx"
      provides: "Row-chunked photo grid — one wrap={false} View PER ROW, not one for the whole grid"
    - path: "components/pdf/shared/pdf-terms-section.tsx"
      provides: "Each of the 5 conditional terms cards wrapped in its own wrap={false} View"
  key_links:
    - from: "components/pdf/estimate-pdf.tsx"
      to: "components/pdf/shared/pdf-section-block.tsx"
      via: "4 sequential calls per section (header, table-header, rows, subtotal) reproducing today's single-page output"
      pattern: "PdfSectionHeader|PdfTableHeaderOnly|PdfSectionRows|PdfSectionSubtotal"
    - from: "tests/unit/pdf/estimate-pdf-baseline-order.test.tsx"
      to: "components/pdf/estimate-pdf.tsx"
      via: "existing text-content-order assertions stay green — this plan changes internal structure, not rendered content"
      pattern: "assertBaselineOrder"
---

<objective>
Fix the two documented atomicity GAPS between today's PDF components and the locked break rules (photo grid is MORE atomic than required; terms cards are LESS atomic than required), and split the monolithic `PdfSectionBlock` into independently-placeable pieces — all WITHOUT changing today's single-page visual output. This is the prerequisite restructuring Plan 184-05 needs to actually wire per-page rendering.

Purpose: PGBRK-02 requires these exact atomicity guarantees. Research's Pitfall 1 and Pitfall 2 both call out that shipping Plan 184-05's page-count logic on top of the UNRESTRUCTURED components would be a no-op (a section literally cannot span a page today) or silently wrong (photos/terms would render unpredictably at a real page boundary).
Output: `pdf-section-block.tsx` exports 4 named pieces instead of 1; `pdf-photo-grid.tsx` is row-chunked; `pdf-terms-section.tsx` is per-card atomic; both templates' call sites updated to reproduce IDENTICAL single-page output (baseline tests stay green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-02-SUMMARY.md
@components/pdf/shared/pdf-section-block.tsx
@components/pdf/shared/pdf-photo-grid.tsx
@components/pdf/shared/pdf-terms-section.tsx
@components/pdf/estimate-pdf.tsx
@components/pdf/estimate-pdf-modern.tsx
@tests/unit/pdf/estimate-pdf-baseline-order.test.tsx

<interfaces>
From Plan 184-02's `lib/estimate/pagination/types.ts` — the 11-block taxonomy this restructuring must align 1:1 with (informational: this plan does NOT import from `lib/estimate/pagination/`, it just needs its 4 output pieces to map cleanly onto the `section-header` | `item-row` | `section-subtotal` kinds for Plan 184-05's later dispatch):
```
'section-header' -> PdfSectionHeader
'item-row'        -> PdfSectionRows (one call per contiguous run of item-row blocks)
'section-subtotal'-> PdfSectionSubtotal
```
The repeated column header (PGBRK-03) is a separate, always-reusable piece: `PdfTableHeaderOnly` — used both for a section's OWN first page and any continuation page.

Current `PdfSectionBlock` call site (BOTH templates, byte-identical shape) — this plan's Task 1 replaces this ONE call with 4 sequential calls reproducing the identical tree:
```ts
// components/pdf/estimate-pdf.tsx:415-441 (Modern: estimate-pdf-modern.tsx:432-458)
.map((section) =>
  PdfSectionBlock({ section, solidFill, brandColor, brandOnFill, L, fmt, styles: {...9 keys...} })
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split PdfSectionBlock into 4 independently-placeable pieces + pin measurement-critical lineHeight tokens</name>
  <files>components/pdf/shared/pdf-section-block.tsx, components/pdf/estimate-pdf.tsx, components/pdf/estimate-pdf-modern.tsx</files>
  <read_first>components/pdf/shared/pdf-section-block.tsx (current monolithic implementation, lines 50-123); 184-RESEARCH.md's Pitfall 1 and Pitfall 5</read_first>
  <behavior>
    - `PdfSectionHeader({ title, solidFill, brandColor, brandOnFill, styles })` renders exactly the header `<View>`+`<Text>` pair (today's lines 60-77), independent of table-header/rows/subtotal.
    - `PdfTableHeaderOnly({ L, styles })` renders exactly the column-header `<View>` (today's lines 79-90) with zero dependency on any specific section.
    - `PdfSectionRows({ items, startIndex, styles })` renders `items` with zebra striping computed as `(idx + startIndex) % 2` (NOT `idx % 2`) — so a slice starting mid-section (continuation page, Plan 184-05) preserves the correct alternating-row pattern relative to the section's FULL item list, not the slice's local index.
    - `PdfSectionSubtotal({ label, value, styles })` renders exactly the subtotal `<View>` (today's lines 116-120).
    - Calling all 4 in sequence (header, table-header, rows with `startIndex: 0`, subtotal) for a section produces a text-content-order-IDENTICAL tree to today's single `PdfSectionBlock({...})` call — `tests/unit/pdf/estimate-pdf-baseline-order.test.tsx` passes UNCHANGED.
  </behavior>
  <action>
    Replace `pdf-section-block.tsx`'s single `PdfSectionBlock` export with the 4 named exports above (same file — internal module layout is Claude's Discretion). Update BOTH `estimate-pdf.tsx`'s and `estimate-pdf-modern.tsx`'s section-rendering `.map()` call site to call all 4 pieces per section, in order, with `startIndex: 0` (Plan 184-05 wires real per-page slicing later — this task only needs byte-identical single-page reproduction).

    In the SAME task, pin an explicit `lineHeight` on the two measurement-critical text styles that currently omit one (per Open Question 1 / Pitfall 5 — required before Plan 184-03's estimator can trust a concrete line-height number): add `lineHeight` to `tableCellText` and `sectionTitle` in BOTH templates' `StyleSheet.create({...})` blocks (`estimate-pdf.tsx`'s `styles`, `estimate-pdf-modern.tsx`'s `styles`). Choose values close to react-pdf's typical single-line default (document the chosen numbers with a comment citing this task) — this is purely additive (a style property that wasn't set before), so it must NOT visibly change rendered output for today's single-page fixtures.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-baseline-order.test.tsx tests/unit/pdf/estimate-pdf-banner-fill.test.tsx</automated>
  </verify>
  <done>pdf-section-block.tsx exports PdfSectionHeader/PdfTableHeaderOnly/PdfSectionRows/PdfSectionSubtotal (no more monolithic PdfSectionBlock); both templates compile and call all 4 per section; tableCellText/sectionTitle have explicit lineHeight in both templates; baseline-order and banner-fill tests remain green unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Row-chunk PdfPhotoGrid + per-card-atomic PdfTermsSection</name>
  <files>components/pdf/shared/pdf-photo-grid.tsx, components/pdf/shared/pdf-terms-section.tsx</files>
  <read_first>components/pdf/shared/pdf-photo-grid.tsx (current whole-grid wrap={false}, lines 38-57), components/pdf/shared/pdf-terms-section.tsx (current shared-View, no per-card atomicity, lines 49-102), 184-RESEARCH.md's Pitfall 2</read_first>
  <behavior>
    - `PdfPhotoGrid` given 7 photos and `contentWidthPt: 532` (Classic) renders 3 top-level row-`<View>`s (chunked `Math.floor(532 / (150 + 8)) === 3` photos per row: `[3, 3, 1]`), each with `wrap={false}` — NOT one `wrap={false}` around the whole grid.
    - The "Photos" section label renders exactly ONCE, attached only to the FIRST row-chunk (a new `showLabel` boolean prop, true only for chunk index 0) — it must NOT repeat on every row-chunk.
    - `PdfTermsSection` given all 5 terms populated renders 5 independent `<View wrap={false}>`s (one per card: Estimate Terms, Payment Terms, Timeline, Warranty, Notes), replacing the current single shared `<View style={termsSection}>` with no atomicity.
    - Photo/caption/terms TEXT CONTENT and ORDER are unchanged from today (existing `estimate-pdf-photo-captions.test.tsx` stays green) — only the `wrap`/grouping structure changes.
  </behavior>
  <action>
    Rewrite `pdf-photo-grid.tsx`'s `PdfPhotoGrid` to accept a new required `contentWidthPt: number` prop (Classic passes 532, Modern passes 508 — the template's own known content width, not a new hardcoded literal elsewhere) and a `showLabel?: boolean` prop. Chunk `photos` into groups of `Math.floor(contentWidthPt / (150 + 8))` (150pt tile + 8pt gap, matching the existing literal `width: 150`/`gap: 8`). Render the "Photos" label once (only when `showLabel`), then map each chunk to its own `<View wrap={false}>` (replacing the single grid-wide `wrap={false}`).

    Rewrite `pdf-terms-section.tsx`'s `PdfTermsSection` so each of its 5 conditional `Fragment`s becomes its own `<View wrap={false}>` wrapping that card's title+text pair — the outer `termsSection` View stays as a non-atomic layout container (just for `marginTop` spacing); each child card individually gets `wrap={false}`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-photo-captions.test.tsx tests/unit/pdf/estimate-pdf-baseline-order.test.tsx</automated>
  </verify>
  <done>Photo grid renders N row-Views (not 1 grid-wide wrap), each individually wrap={false}, label shown once; terms section renders up to 5 independently wrap={false} card-Views; existing caption/order tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Structural atomicity regression tests + full existing PDF suite verification</name>
  <files>tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx, tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx</files>
  <read_first>tests/unit/pdf/estimate-pdf-banner-fill.test.tsx (the style-aware DFS-walker pattern to adapt for `wrap`-prop introspection instead of `backgroundColor`)</read_first>
  <action>
    Write 2 new structural tests using the direct-function-invocation convention (`EstimatePDF({...})`/`EstimatePDFModern({...})` called directly, tree walked via a style/prop-aware DFS — adapt `estimate-pdf-banner-fill.test.tsx`'s walker pattern to check `el.props.wrap === false` instead of `backgroundColor`):
    - `estimate-pdf-photo-grid-atomicity.test.tsx`: given a 7-photo fixture, the returned tree contains exactly 3 `<View>` nodes with `wrap === false` under the photo grid (not 1) — for BOTH templates.
    - `estimate-pdf-terms-atomicity.test.tsx`: given all 5 terms populated, the tree contains exactly 5 `<View>` nodes with `wrap === false` under the terms section — for BOTH templates.

    Then run the full existing PDF + estimate test suites to confirm zero regressions from this plan's restructuring.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx` — Classic AND Modern each assert exactly 3 wrap-false nodes
    - `npx vitest run tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx` — Classic AND Modern each assert exactly 5 wrap-false nodes
    - `npx vitest run tests/unit/pdf tests/unit/estimate` — 0 failures
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pdf/estimate-pdf-photo-grid-atomicity.test.tsx tests/unit/pdf/estimate-pdf-terms-atomicity.test.tsx && npx vitest run tests/unit/pdf tests/unit/estimate</automated>
  </verify>
  <done>Both new atomicity tests pass for both templates; the full existing tests/unit/pdf + tests/unit/estimate suites remain green (0 regressions from this plan's component restructuring).</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/pdf tests/unit/estimate` — full green, including the 2 new atomicity tests.
`npx tsc -p tsconfig.ci.json --noEmit` — clean.
</verification>

<success_criteria>
Every PDF component gap Plan 184-RESEARCH identified (over-atomic photo grid, under-atomic terms cards, monolithic un-splittable section block) is closed, verified by new structural tests, with zero change to today's single-page rendered output — the exact prerequisite Plan 184-05 needs before wiring real per-page composition.
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-04-SUMMARY.md` documenting the exact new prop signatures for PdfSectionHeader/PdfTableHeaderOnly/PdfSectionRows/PdfSectionSubtotal/PdfPhotoGrid/PdfTermsSection — Plan 184-05 wires all 6 directly from this summary.
</output>
