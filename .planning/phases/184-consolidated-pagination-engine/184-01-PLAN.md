---
phase: 184-consolidated-pagination-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/estimate/pagination/measure/safety-margin.ts
  - scripts/pagination-drift-spike.ts
  - tests/unit/pagination/measure/fontkit-arithmetic.test.ts
  - tests/unit/pagination/measure/safety-margin.test.ts
  - .planning/phases/184-consolidated-pagination-engine/184-DRIFT-REPORT.md
autonomous: true
requirements: [PGBRK-05]

must_haves:
  truths:
    - "The browser-vs-fontkit measurement-drift spike has run and produced a documented go/no-go decision (ROADMAP Phase 184 Success Criteria #4)."
    - "The safety margin applied by the engine is derived from measured drift data, not guessed."
  artifacts:
    - path: "lib/estimate/pagination/measure/safety-margin.ts"
      provides: "SAFETY_MARGIN_LINES constant, derived from the spike, cited to the drift report doc"
    - path: ".planning/phases/184-consolidated-pagination-engine/184-DRIFT-REPORT.md"
      provides: "Verbatim spike console.table output + go/no-go recommendation"
    - path: "scripts/pagination-drift-spike.ts"
      provides: "Standalone, repeatable drift-measurement script (npx tsx)"
    - path: "tests/unit/pagination/measure/fontkit-arithmetic.test.ts"
      provides: "Hand-calculated validation of the fontkit layout()/advanceWidth scale formula against the real vendored Inter TTF"
  key_links:
    - from: "lib/estimate/pagination/measure/safety-margin.ts"
      to: ".planning/phases/184-consolidated-pagination-engine/184-DRIFT-REPORT.md"
      via: "code comment citing the report"
      pattern: "184-DRIFT-REPORT"
    - from: "package.json"
      to: "fontkit / linebreak"
      via: "direct dependency entries (not transitive-only)"
      pattern: "\"fontkit\":|\"linebreak\":"
---

<objective>
Close the ONE low-confidence gap research flagged for this phase: whether fontkit's glyph-measurement math (already used internally by `@react-pdf/pdfkit`) can be trusted for page-break decisions, and how much extra buffer ("safety margin") to add so the pagination engine never UNDER-predicts a real react-pdf render.

Purpose: PGBRK-05 requires a spike-validated safety margin BEFORE the estimator (Plan 184-03) is finalized. This plan is that spike, plus the arithmetic proof that the measurement formula itself is sound.
Output: `fontkit`/`linebreak` promoted to direct dependencies; a hand-verified line-count formula; a real browser-vs-fontkit drift measurement; a committed `SAFETY_MARGIN_LINES` constant + short drift report doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-consolidated-pagination-engine/184-CONTEXT.md
@.planning/phases/184-consolidated-pagination-engine/184-RESEARCH.md
@lib/pdf/register-fonts.ts

<interfaces>
This plan does NOT depend on any other Plan 184 file — it is a self-contained Wave 1 leaf.
The one file downstream plans (184-03, 184-05) will import from is:

```ts
// lib/estimate/pagination/measure/safety-margin.ts (this plan creates it)
export const SAFETY_MARGIN_LINES: number
```

Nothing else in this repo currently imports from `lib/estimate/pagination/*` — it doesn't exist yet.
</interfaces>

<known_facts>
Verified directly (via `node -e` against the vendored `public/fonts/inter/Inter-Regular.ttf` and the installed `fontkit@2.0.4`/`linebreak@1.1.0`, 2026-07-28 — use these numbers as the ground truth for Task 1's hand-calculated test, do not re-derive them differently):

- `fontkit.openSync('public/fonts/inter/Inter-Regular.ttf').unitsPerEm === 2048`
- `font.layout('H').advanceWidth === 1522` (font units)
- `font.layout('HHHH').advanceWidth === 6088` (exactly `4 * 1522` — confirms simple additive advance width for repeated identical glyphs, no kerning surprises for this glyph pair)
- Scale formula: `widthPt = advanceWidthUnits * (fontSizePt / unitsPerEm)`
- For text `'HHHH HHHH HHHH'` (3 words, single-space-separated) at `fontSizePt = 9`, using the greedy line-packer below:
  - `maxWidthPt = 30` → **3 lines**
  - `maxWidthPt = 60` → **2 lines**
  - `maxWidthPt = 100` → **1 line**
  - `''` (empty string) → **0 lines**
- `linebreak`'s `LineBreaker` gives break opportunities immediately after each space (chunk includes the trailing space), e.g. for the string above: breaks at positions 5 (`"HHHH "`), 10 (`"HHHH HHHH "`), 14 (`"HHHH HHHH HHHH"`).

Reference greedy line-packer (the exact algorithm to hand-validate in Task 1 — this is NOT yet a module, just the formula under test):
```ts
function estimateLineCount(text: string, font: Font, fontSizePt: number, maxWidthPt: number): number {
  if (text.length === 0) return 0
  const scale = fontSizePt / font.unitsPerEm
  const breaker = new LineBreaker(text)
  let lineWidthPt = 0
  let lines = 1
  let last = 0
  let bk: { position: number } | null
  while ((bk = breaker.nextBreak())) {
    const chunk = text.slice(last, bk.position)
    const { advanceWidth } = font.layout(chunk)
    const chunkWidthPt = advanceWidth * scale
    if (lineWidthPt + chunkWidthPt > maxWidthPt && lineWidthPt > 0) {
      lines += 1
      lineWidthPt = chunkWidthPt
    } else {
      lineWidthPt += chunkWidthPt
    }
    last = bk.position
  }
  return lines
}
```
</known_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote fontkit/linebreak to direct dependencies + hand-calculated arithmetic proof</name>
  <files>package.json, package-lock.json, tests/unit/pagination/measure/fontkit-arithmetic.test.ts</files>
  <read_first>lib/pdf/register-fonts.ts (confirms the vendored TTF path to test against); package.json's current dependencies/devDependencies blocks</read_first>
  <action>
    Run `npm install fontkit@^2.0.4 linebreak@^1.1.0` and `npm install -D @types/fontkit@^2.0.9` — both libraries already resolve transitively via `@react-pdf/pdfkit`, so versions should not change; this promotes them into `package.json`'s own `dependencies`/`devDependencies` (required before any first-class import from `lib/estimate/pagination/measure/*`).

    Create `tests/unit/pagination/measure/fontkit-arithmetic.test.ts` with a LOCAL (test-file-only) copy of the `estimateLineCount` function from `<known_facts>` above — do NOT import from `lib/estimate/pagination/measure/estimator.ts` (it does not exist until Plan 184-03; this test has zero dependency on Wave 2 files). Open `public/fonts/inter/Inter-Regular.ttf` via `fontkit.openSync()` once at module scope. Assert, using the EXACT numbers from `<known_facts>` (do not recompute independently — these are the hand-verified ground truth):
      - `font.unitsPerEm === 2048`
      - `font.layout('H').advanceWidth === 1522`
      - `font.layout('HHHH').advanceWidth === 6088`
      - `estimateLineCount('HHHH HHHH HHHH', font, 9, 30) === 3`
      - `estimateLineCount('HHHH HHHH HHHH', font, 9, 60) === 2`
      - `estimateLineCount('HHHH HHHH HHHH', font, 9, 100) === 1`
      - `estimateLineCount('', font, 9, 100) === 0`
  </action>
  <acceptance_criteria>
    - `grep -E '"fontkit":|"linebreak":' package.json` matches inside the `dependencies` block (not merely present in `package-lock.json`)
    - `grep '"@types/fontkit"' package.json` matches inside `devDependencies`
    - `npx vitest run tests/unit/pagination/measure/fontkit-arithmetic.test.ts` — 7/7 assertions pass
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pagination/measure/fontkit-arithmetic.test.ts</automated>
  </verify>
  <done>fontkit/linebreak are direct dependencies; @types/fontkit is a dev dependency; the hand-calculated arithmetic test passes against the real vendored Inter TTF, confirming the layout()/advanceWidth scale formula (Open Question 2) before the estimator (Plan 184-03) builds on it.</done>
</task>

<task type="auto">
  <name>Task 2: Browser-vs-fontkit drift spike + SAFETY_MARGIN_LINES constant</name>
  <files>scripts/pagination-drift-spike.ts, lib/estimate/pagination/measure/safety-margin.ts, tests/unit/pagination/measure/safety-margin.test.ts, .planning/phases/184-consolidated-pagination-engine/184-DRIFT-REPORT.md</files>
  <read_first>184-RESEARCH.md's "Spike Design (PGBRK-05)" section (the full runnable sketch — reuse it near-verbatim); playwright.config.ts (confirm the spike does NOT reuse its webServer/auth — standalone script only)</read_first>
  <action>
    Create `scripts/pagination-drift-spike.ts`, executable via `npx tsx scripts/pagination-drift-spike.ts` (no dev server, no playwright.config.ts — a standalone script using `chromium.launch()` directly from `@playwright/test`). It must:
    1. Load `public/fonts/inter/Inter-Regular.ttf` as base64 for a `@font-face` data URL.
    2. Define the 5 representative samples from 184-RESEARCH.md's `SAMPLES` array (short-description, long-description, accented, numeric-heavy, single-long-token), each at `fontSizePt: 9, widthPt: 213` (Classic's `colDescription` width).
    3. For each sample, compute `fontkitLines` using Task 1's validated `estimateLineCount` formula (inline in this script — a small self-contained copy, same as Task 1's test; Plan 184-03 will later implement the production version in `measure/estimator.ts`).
    4. For each sample, compute `domLines` via Playwright: `page.setContent()` with the `@font-face` + a `<div>` of the exact px-converted width/font-size (use `lib/estimate/document/tokens.ts`'s `PX_PER_PT` — do not hand-derive a second ratio), then `range.selectNodeContents(el); range.getClientRects().length`.
    5. `console.table(results)` where each row is `{ label, fontkitLines, domLines, drift: domLines - fontkitLines }`.
    6. Compute `safetyMarginLines = Math.max(...results.map(r => Math.abs(r.drift)))`.

    Run it: `npx tsx scripts/pagination-drift-spike.ts`. Copy the verbatim `console.table` output into `.planning/phases/184-consolidated-pagination-engine/184-DRIFT-REPORT.md`, along with a short written go/no-go recommendation (per ROADMAP Phase 184 Success Criteria #4: "produced a documented go/no-go decision") — state explicitly whether the fontkit-based approach is fit for purpose given the observed drift, and the resulting `safetyMarginLines` value.

    Create `lib/estimate/pagination/measure/safety-margin.ts` exporting `export const SAFETY_MARGIN_LINES = <the derived integer>` with a comment citing `184-DRIFT-REPORT.md` and today's date. This file has ZERO imports (a plain numeric constant) — it must NOT import fontkit/linebreak/playwright, so it stays trivially import-clean for any downstream consumer.
  </action>
  <acceptance_criteria>
    - `.planning/phases/184-consolidated-pagination-engine/184-DRIFT-REPORT.md` exists, contains a table with all 5 sample labels and an explicit go/no-go sentence
    - `grep -c "^import" lib/estimate/pagination/measure/safety-margin.ts` returns 0
    - `grep "184-DRIFT-REPORT" lib/estimate/pagination/measure/safety-margin.ts` matches
    - `SAFETY_MARGIN_LINES` is a non-negative integer
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/pagination/measure/safety-margin.test.ts</automated>
  </verify>
  <done>Spike script runs and its output is captured in a committed drift report with a stated go/no-go decision; SAFETY_MARGIN_LINES is exported from a zero-dependency constant file, ready for Plan 184-03's estimator and Plan 184-05's resolver wiring to consume without re-deriving it.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/pagination/measure` — both new test files green.
`npm ls fontkit linebreak @types/fontkit` — all three resolve at the top level (direct dependency), not merely nested.
</verification>

<success_criteria>
The measurement-drift spike (PGBRK-05) has run against the real vendored font via a real Chromium instance, its output is committed as a short report, and a concrete `SAFETY_MARGIN_LINES` constant is available for every later plan to import — closing the phase's one LOW-confidence research gap before the estimator (184-03) or engine (184-02) rely on any assumed number.
</success_criteria>

<output>
After completion, create `.planning/phases/184-consolidated-pagination-engine/184-01-SUMMARY.md` documenting: the exact `SAFETY_MARGIN_LINES` value derived, which sample category (if any) drove it, and confirmation that `fontkit`/`linebreak` are now direct dependencies.
</output>
