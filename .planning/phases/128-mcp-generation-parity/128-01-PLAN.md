---
phase: 128-mcp-generation-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agent-tools/create-estimate.ts
  - lib/mcp/tools/write.ts
  - tests/unit/agent-tools/create-estimate.test.ts
  - tests/unit/mcp/mcp-generation-parity.test.ts
autonomous: true
requirements: [MGEN-01, MPAR-01]

must_haves:
  truths:
    - "The MCP create_estimate tool dispatches estimate generation by calling the neutral createEstimate (no inline inngest.send in handleCreateEstimate)"
    - "An MCP-originated estimate still fires EVENT_ESTIMATE_GENERATE exactly once with companyId/projectId/requestId/channel:'mcp' and returns the same {job_id, status:'queued', message} envelope"
    - "The Inngest idempotency id for the MCP path still begins estimate-mcp-<projectId>- (the existing MCP suite stays green)"
    - "The neutral createEstimate namespaces the idempotency id by channel (estimate-<channel>-<projectId>-<requestId>) for every channel that passes one"
    - "A static binding test proves write.ts imports createEstimate from the neutral barrel, no longer references EVENT_ESTIMATE_GENERATE, and all three channels converge on one core"
    - "The existing v4.1 MCP test suite (create_estimate behavior, check_job_status, tool registry) stays green unchanged"
  artifacts:
    - path: "lib/agent-tools/create-estimate.ts"
      provides: "Neutral generation dispatch with channel-namespaced idempotency id"
      contains: "estimate-${"
    - path: "lib/mcp/tools/write.ts"
      provides: "MCP create_estimate delegating to neutral createEstimate"
      contains: "createEstimate("
    - path: "tests/unit/mcp/mcp-generation-parity.test.ts"
      provides: "MPAR-01 static binding + three-channel convergence test"
      contains: "createEstimate("
    - path: "tests/unit/agent-tools/create-estimate.test.ts"
      provides: "Updated neutral test asserting the channel-namespaced id"
      contains: "estimate-web-"
  key_links:
    - from: "lib/mcp/tools/write.ts (handleCreateEstimate)"
      to: "lib/agent-tools/create-estimate.ts (createEstimate)"
      via: "import { createEstimate } from '@/lib/agent-tools' + call with channel:'mcp'"
      pattern: "createEstimate\\(\\{"
    - from: "lib/agent-tools/create-estimate.ts"
      to: "@/lib/inngest/client (inngest.send)"
      via: "channel-namespaced idempotency id"
      pattern: "estimate-\\$\\{"
---

<objective>
Reconcile the existing MCP `create_estimate` tool so its generation dispatch delegates to the v4.9 channel-neutral `createEstimate` — making WhatsApp, web chat, and MCP all route generation through one core (MGEN-01) — and prove the binding is real and non-destructive with a static parity test plus the unchanged v4.1 MCP suite (MPAR-01). This is a thin, behavior-preserving refactor; it CLOSES milestone v4.10 and the Multi-Channel Core track.

Purpose: Demonstrate "three siblings, one core" for generation — the MCP tool no longer re-implements the Inngest dispatch; it calls the same neutral function the chat already binds.
Output: A delegated `handleCreateEstimate`, a channel-namespaced idempotency id on the neutral fn, an updated neutral-fn test, and a new MPAR-01 static binding test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-mcp-generation-parity/128-RESEARCH.md
@.planning/phases/128-mcp-generation-parity/128-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — no exploration required. -->

The neutral createEstimate (lib/agent-tools/create-estimate.ts) — CURRENT signature + id:
```typescript
export async function createEstimate(args: {
  companyId: string
  projectId: string
  prompts?: string[]
  language?: 'en' | 'pt' | 'es'
  channel?: 'web' | 'mcp'
}): Promise<{ jobId: string }> {
  const requestId = randomUUID()
  const payload: EstimateGeneratePayload = {
    companyId: args.companyId,
    projectId: args.projectId,
    requestId,
    ...(args.prompts ? { prompts: args.prompts } : {}),
    ...(args.language ? { language: args.language } : {}),
    ...(args.channel ? { channel: args.channel } : {}),
  }
  const { ids } = await inngest.send({
    name: EVENT_ESTIMATE_GENERATE,
    id: `estimate-${args.projectId}-${requestId}`,   // <-- WIDEN this line (Task 1)
    data: payload,
  })
  const jobId = ids[0]
  if (!jobId) throw new Error('createEstimate: inngest.send returned no event id')
  return { jobId }
}
```
It is re-exported from the neutral barrel: `import { createEstimate } from '@/lib/agent-tools'`.

The web-chat binding precedent already in place (lib/chat/tools.ts L67-76) — the pattern to mirror:
```typescript
execute: async ({ projectId, prompts }) => {
  const { jobId } = await createEstimate({
    companyId: ctx.companyId,
    projectId,
    prompts,
    language: ctx.language,
    channel: 'web',
  })
  return { jobId, status: 'queued' as const }
},
```

The MCP dispatch block to REPLACE (lib/mcp/tools/write.ts L184-202): mints `requestId`, builds an `EstimateGeneratePayload`, calls `inngest.send({ name: EVENT_ESTIMATE_GENERATE, id: \`estimate-mcp-${input.project_id}-${requestId}\`, data })`, extracts `ids[0]` as `jobId`, throws on empty. The KEEP-AS-IS sections around it: the scope gate (`ensureScope(auth, 'mcp:write')`), input parse, the project-ownership lookup via `requireServiceClient()`, and the return envelope `jsonContent({ job_id: jobId, status: 'queued', message: '...check_job_status...' })`.

The existing MCP behavior test's load-bearing assertion (tests/unit/mcp-create-estimate.test.ts L175) — MUST stay green:
```typescript
expect(call.id).toMatch(/^estimate-mcp-p1-/)
```
This is why Task 1 widens the neutral id to fold in `channel` (Option A from RESEARCH): with `channel:'mcp'` the id becomes `estimate-mcp-p1-...` and the assertion stays green.

The MPAR-01 test model (tests/unit/agent-tools/neutrality.test.ts) — static `readFileSync` + grep over source files. Mirror its `resolve(ROOT, path)` + `readFileSync(..., 'utf8')` style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the neutral createEstimate idempotency id to be channel-namespaced (Option A)</name>
  <files>lib/agent-tools/create-estimate.ts, tests/unit/agent-tools/create-estimate.test.ts</files>
  <read_first>
    - lib/agent-tools/create-estimate.ts (the full neutral fn — the id is line 47: `id: \`estimate-${args.projectId}-${requestId}\``)
    - tests/unit/agent-tools/create-estimate.test.ts (the neutral fn's behavior test — note it asserts payload fields + {jobId} but does NOT yet assert the id prefix)
    - tests/unit/agent-tools/neutrality.test.ts (the neutrality gate — confirm `channel` adds no forbidden token; FORBIDDEN list is whatsapp-specific only)
    - 128-RESEARCH.md "Pitfall 1" + "Open Questions #1" (the justified single exception to "don't touch the neutral module": channel is already a neutral param, so namespacing the id by it preserves neutrality)
  </read_first>
  <behavior>
    - Test (existing, extend): createEstimate with channel:'mcp' sends an event whose `id` matches /^estimate-mcp-proj-1-/ (channel folded into the id before projectId).
    - Test (existing, extend): createEstimate with channel:'web' sends an event whose `id` matches /^estimate-web-/ .
    - Test (existing, extend): createEstimate with NO channel sends an event whose `id` matches /^estimate-proj-1-/ (no leading channel segment — back-compat for any callerless path).
    - Existing payload + {jobId} assertions stay green (companyId/projectId/prompts/language/requestId unchanged).
  </behavior>
  <action>
    Edit lib/agent-tools/create-estimate.ts ONLY at the `inngest.send` `id` field. Replace:
    ```typescript
    id: `estimate-${args.projectId}-${requestId}`,
    ```
    with a channel-namespaced id that prefixes the channel segment ONLY when a channel is passed:
    ```typescript
    id: `estimate-${args.channel ? `${args.channel}-` : ''}${args.projectId}-${requestId}`,
    ```
    Update the module doc comment (the block at the top) to note: the idempotency id is channel-namespaced (`estimate-<channel>-<projectId>-<requestId>`) so each channel (web/mcp) gets a distinct Inngest dedupe namespace; this is the single justified widening of the neutral fn for Phase 128 (channel is already a neutral param — neutrality preserved). Do NOT add any whatsapp/channel-specific import or token.

    Then update tests/unit/agent-tools/create-estimate.test.ts to assert the new id:
    - In "Test 1" (which passes language:'en' and prompts), add `channel: 'mcp'` to the call args and add `expect((arg.id as string)).toMatch(/^estimate-mcp-proj-1-/)` (capture `arg.id` alongside `arg.name`/`arg.data`).
    - Add a new it() "namespaces the idempotency id by channel for the web path": call `createEstimate({ companyId:'co-1', projectId:'proj-1', channel:'web' })`, assert `mockSend.mock.calls[0][0].id` matches `/^estimate-web-proj-1-/`.
    - Add a new it() "omits the channel segment when no channel is passed": call `createEstimate({ companyId:'co-1', projectId:'proj-1' })`, assert the id matches `/^estimate-proj-1-/` and does NOT match `/^estimate-(web|mcp)-/`.
    Do NOT weaken or remove the existing payload/jobId/T-lrf-01 assertions.
  </action>
  <acceptance_criteria>
    - `grep -n "estimate-\${args.channel" lib/agent-tools/create-estimate.ts` returns the new template-literal id line (channel folded in).
    - `grep -c "estimate-\${args.projectId}-\${requestId}" lib/agent-tools/create-estimate.ts` returns 0 (the OLD non-namespaced id line is gone).
    - `grep -n "estimate-mcp-proj-1-\|estimate-web-proj-1-\|estimate-proj-1-" tests/unit/agent-tools/create-estimate.test.ts` shows all three id-prefix assertions present.
    - The neutrality gate stays green: no forbidden token added. `npx vitest run tests/unit/agent-tools/neutrality.test.ts` passes.
    - `npx vitest run tests/unit/agent-tools/create-estimate.test.ts` passes with the new + existing assertions.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/agent-tools/create-estimate.test.ts tests/unit/agent-tools/neutrality.test.ts</automated>
  </verify>
  <done>The neutral createEstimate emits a channel-namespaced idempotency id (estimate-&lt;channel&gt;-&lt;projectId&gt;-&lt;requestId&gt; when a channel is passed, estimate-&lt;projectId&gt;-&lt;requestId&gt; otherwise); the neutral test asserts all three forms; the neutrality gate is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delegate MCP handleCreateEstimate to the neutral createEstimate (MGEN-01) and remove the now-dead imports</name>
  <files>lib/mcp/tools/write.ts</files>
  <read_first>
    - lib/mcp/tools/write.ts (the FULL file — handleCreateEstimate L147-210, the dispatch block L184-202, the envelope L204-209; the imports L23-39; check_job_status L259-315 which uses fetch + INNGEST_SIGNING_KEY, NOT the inngest client / event constant)
    - lib/chat/tools.ts L67-76 (the binding precedent to mirror, channel:'web')
    - tests/unit/mcp-create-estimate.test.ts (THE parity guard — mocks @/lib/inngest/client at the leaf L32-34; asserts the event name, the `estimate-mcp-p1-` id L175, the payload companyId/projectId/requestId, the {job_id,status,message} envelope, language forwarding/omission, scope gate, tenant boundary)
    - 128-RESEARCH.md "Recommended exact refactor" + "Pitfall 4" (which imports are safe to remove) + "Pitfall 5" (do NOT mock @/lib/agent-tools in the existing test)
  </read_first>
  <behavior>
    - The EXISTING tests/unit/mcp-create-estimate.test.ts stays green UNCHANGED (it is the behavior guard): same event name, same `estimate-mcp-p1-` id prefix, same payload fields, same {job_id,status:'queued',message} envelope, same language forward/omit, same scope + tenant gates. The real neutral createEstimate runs against the same leaf-mocked @/lib/inngest/client, so the inngestSend assertions keep working.
  </behavior>
  <action>
    Edit lib/mcp/tools/write.ts, function handleCreateEstimate. KEEP verbatim: the `ensureScope(auth, 'mcp:write')` gate, the `parseInput(createEstimateInput, args)`, the project-ownership lookup (requireServiceClient → `.from('projects').select('id, company_id').eq('id', input.project_id).maybeSingle()` → throw notFound on missing/cross-company), and the final `return jsonContent({ job_id: jobId, status: 'queued', message: 'Estimate generation queued. Poll check_job_status to track progress.' })`.

    REPLACE the dispatch block (currently: `const requestId = randomUUID()` … through `if (!jobId) { throw new Error('create_estimate: inngest.send returned no event id') }`, L184-202) with a single delegated call mirroring lib/chat/tools.ts:
    ```typescript
    const { jobId } = await createEstimate({
      companyId: auth.company_id,
      projectId: input.project_id,
      prompts: [input.prompt],
      ...(input.language ? { language: input.language } : {}),
      channel: 'mcp',
    })
    ```
    Add the import near the other top-of-file imports:
    ```typescript
    import { createEstimate } from '@/lib/agent-tools'
    ```
    Remove the now-dead imports (verified unused after the dispatch block goes — check_job_status uses fetch + INNGEST_SIGNING_KEY, not these):
    - `import { randomUUID } from 'node:crypto'` (L25)
    - `import { inngest } from '@/lib/inngest/client'` (L29)
    - the `EVENT_ESTIMATE_GENERATE` + `EstimateGeneratePayload` import block (L30-33)
    KEEP `requireServiceClient`, `requireScope`, the error helpers (`insufficientScope`, `invalidInput`, `notFound`), the zod schemas, `__testing`, and everything in check_job_status. Do NOT change the input schema, the tool definitions/annotations, the registry builder, or the envelope strings.

    Do NOT edit tests/unit/mcp-create-estimate.test.ts — it must stay byte-unchanged and green (the channel:'mcp' delegation + Task-1's channel-namespaced id keep its `estimate-mcp-p1-` assertion passing). Do NOT add any `vi.mock('@/lib/agent-tools')` anywhere (it would stub out the real delegation and break the payload assertions — Pitfall 5).
  </action>
  <acceptance_criteria>
    - `grep -n "import { createEstimate } from '@/lib/agent-tools'" lib/mcp/tools/write.ts` returns the new import.
    - `grep -n "createEstimate({" lib/mcp/tools/write.ts` shows the delegated call with `channel: 'mcp'` (confirm `channel: 'mcp'` appears in the call).
    - `grep -c "EVENT_ESTIMATE_GENERATE" lib/mcp/tools/write.ts` returns 0 (no inline event dispatch remains).
    - `grep -c "inngest.send" lib/mcp/tools/write.ts` returns 0; `grep -c "from 'node:crypto'" lib/mcp/tools/write.ts` returns 0.
    - The `{ job_id: jobId, status: 'queued', message: …check_job_status… }` envelope is unchanged: `grep -n "job_id: jobId" lib/mcp/tools/write.ts` still present.
    - tests/unit/mcp-create-estimate.test.ts is byte-unchanged (git diff shows no change to that file) and green.
    - `npx tsc --noEmit` reports no new errors in lib/mcp/tools/write.ts (no unused-import warnings).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/mcp-create-estimate.test.ts tests/unit/mcp-check-job-status.test.ts tests/unit/mcp-tool-registry.test.ts</automated>
  </verify>
  <done>handleCreateEstimate calls the neutral createEstimate with channel:'mcp' (no inline inngest.send / EVENT_ESTIMATE_GENERATE / randomUUID in write.ts); the scope gate, project-ownership lookup, and {job_id,status,message} envelope are preserved; the existing MCP create_estimate / check_job_status / registry suites stay green unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add the MPAR-01 static binding + three-channel convergence parity test</name>
  <files>tests/unit/mcp/mcp-generation-parity.test.ts</files>
  <read_first>
    - tests/unit/agent-tools/neutrality.test.ts (the static readFileSync + grep pattern to mirror: `resolve(ROOT, path)` + `readFileSync(..., 'utf8')`, describe/it structure)
    - lib/mcp/tools/write.ts (POST-Task-2 state — imports createEstimate, no EVENT_ESTIMATE_GENERATE)
    - lib/chat/tools.ts (the web-chat binding — contains `createEstimate(`)
    - lib/whatsapp/confirm-actions.ts (the WhatsApp confirm-flow — contains `generateEstimateForProject(` at L339, the third channel's engine convergence)
    - 128-RESEARCH.md "MPAR-01 static binding-grep test" code example + 128-VALIDATION.md Wave 0 Requirements (the test file path is tests/unit/mcp/mcp-generation-parity.test.ts)
  </read_first>
  <action>
    Create tests/unit/mcp/mcp-generation-parity.test.ts (create the tests/unit/mcp/ dir if it does not exist). Mirror the static readFileSync-grep style of tests/unit/agent-tools/neutrality.test.ts (no DB, no mocks, no secrets — pure source-string assertions). Implement three it() cases under one describe('MPAR-01: MCP binds the neutral core (not a re-implementation)'):

    1. "mcp create_estimate imports + calls the neutral createEstimate":
       read lib/mcp/tools/write.ts; assert it matches `/from '@\/lib\/agent-tools(\/create-estimate)?'/` AND contains `'createEstimate('`.
    2. "mcp create_estimate no longer dispatches EVENT_ESTIMATE_GENERATE itself":
       read lib/mcp/tools/write.ts; assert it does NOT contain `'EVENT_ESTIMATE_GENERATE'` (the dispatch is delegated; the dead import was removed in Task 2).
    3. "all three channel adapters route generation through the neutral core":
       assert read('lib/chat/tools.ts') contains `'createEstimate('` (web chat); assert read('lib/mcp/tools/write.ts') contains `'createEstimate('` (MCP); assert read('lib/whatsapp/confirm-actions.ts') contains `'generateEstimateForProject('` (WhatsApp confirm-flow converges on the same engine).

    Use `const ROOT = process.cwd()` and `const read = (p) => readFileSync(resolve(ROOT, p), 'utf8')`. Do NOT add behavioral mocks — this is a static binding proof, the companion to the EXISTING behavior guard (mcp-create-estimate.test.ts) which proves the dispatch still works.
  </action>
  <acceptance_criteria>
    - File tests/unit/mcp/mcp-generation-parity.test.ts exists with 3 it() cases and uses `readFileSync` + `resolve(ROOT, ...)` (no DB/mocks).
    - `grep -n "from '@/lib/agent-tools" tests/unit/mcp/mcp-generation-parity.test.ts` and `grep -n "generateEstimateForProject" tests/unit/mcp/mcp-generation-parity.test.ts` both return matches (the binding + WhatsApp-convergence assertions).
    - `npx vitest run tests/unit/mcp/mcp-generation-parity.test.ts` passes 3/3 (only after Tasks 1-2 land — write.ts must already import createEstimate and have dropped EVENT_ESTIMATE_GENERATE).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/mcp/mcp-generation-parity.test.ts</automated>
  </verify>
  <done>A static MPAR-01 test proves write.ts binds the neutral createEstimate, no longer references EVENT_ESTIMATE_GENERATE, and all three channels (chat/createEstimate, mcp/createEstimate, whatsapp/generateEstimateForProject) converge on one core — green.</done>
</task>

</tasks>

<verification>
- MGEN-01: `npx vitest run tests/unit/mcp-create-estimate.test.ts tests/unit/mcp-check-job-status.test.ts tests/unit/mcp-tool-registry.test.ts tests/unit/agent-tools/create-estimate.test.ts` — all green (the behavior guard + the widened neutral-fn test). The MCP behavior test is byte-unchanged.
- MPAR-01: `npx vitest run tests/unit/mcp/mcp-generation-parity.test.ts tests/unit/agent-tools/neutrality.test.ts` — green (static binding + neutrality preserved).
- Phase gate (milestone close): `npx vitest run` — fully green. The only known parallel-only flake is `mcp-route-contract.test.ts` (GET-405); confirm it passes in isolation if it surfaces — it touches no Phase-128 file.
- `npx tsc --noEmit` — no new errors in lib/mcp/tools/write.ts or lib/agent-tools/create-estimate.ts (no unused imports left behind).
</verification>

<success_criteria>
- handleCreateEstimate delegates to the neutral createEstimate with channel:'mcp'; no inline inngest.send / EVENT_ESTIMATE_GENERATE / randomUUID remains in write.ts (MGEN-01).
- The MCP scope gate, project-ownership lookup, and {job_id,status,message} envelope are preserved; the existing v4.1 MCP suite (create_estimate, check_job_status, registry) stays green BYTE-UNCHANGED (MPAR-01 non-destructive).
- The neutral createEstimate emits a channel-namespaced idempotency id, keeping the MCP test's estimate-mcp-p1- assertion green and giving web the estimate-web- discriminator; the neutral test asserts all three id forms.
- The new static parity test proves the binding + three-channel convergence (MPAR-01).
- Full `npx vitest run` green — closing milestone v4.10 and the Multi-Channel Core track.
- No secrets; no migration; no new dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/128-mcp-generation-parity/128-01-SUMMARY.md`
</output>
