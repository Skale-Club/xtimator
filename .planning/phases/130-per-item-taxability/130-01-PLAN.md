---
phase: 130-per-item-taxability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/schema.ts
  - lib/ai/types.ts
  - lib/ai/prompt-builder.ts
  - lib/ai/providers/anthropic.ts
  - lib/ai/providers/gemini.ts
  - tests/unit/ai/tax-classification-schema.test.ts
autonomous: true
requirements:
  - TAX-02
must_haves:
  truths:
    - "The AI output schema accepts a per-item optional `taxable` (boolean) and `tax_category` ('labor'|'materials'|'other'); the parse still SUCCEEDS when both are omitted (retrocompat: existing AI output keeps validating)."
    - "When the AI omits the fields the parsed item carries no taxable/tax_category override (downstream defaults to taxable=true, tax_category=null) — byte-identical to today."
    - "The system prompt instructs the AI to CLASSIFY each line item as labor vs materials, with NO arithmetic and NO tax computation (classification input only)."
  artifacts:
    - path: "lib/ai/schema.ts"
      provides: "lineItemSchema widened with optional taxable + tax_category"
      contains: "tax_category"
    - path: "lib/ai/types.ts"
      provides: "LineItemOutput widened with optional taxable + tax_category"
      contains: "tax_category"
    - path: "tests/unit/ai/tax-classification-schema.test.ts"
      provides: "TAX-02 schema acceptance + omission retrocompat test"
  key_links:
    - from: "lib/ai/prompt-builder.ts"
      to: "AI model"
      via: "classification instruction (labor vs materials, no math)"
      pattern: "tax_category|labor|materials"
    - from: "lib/ai/providers/anthropic.ts"
      to: "create_estimate tool item schema"
      via: "advisory taxable/tax_category properties (NOT in required[])"
      pattern: "tax_category"
---

<objective>
Activate the TAX-02 half of per-item taxability: widen the AI estimate output contract so each
line item can carry an OPTIONAL `taxable` (boolean) and `tax_category` ('labor'|'materials'|'other')
that the AI provides as CLASSIFICATION INPUT only. The AI never computes tax — it labels labor vs
materials; the server (Plan 130-02 / GUARD-03) does all arithmetic.

Purpose: Give the deterministic engine the per-item labor/materials signal it needs for per-category
tax math, without granting the AI a calculator (ENG-01 fence stays intact).

Output: Widened `estimateOutputSchema` + `LineItemOutput` (both optional/additive), advisory tool-schema
fields on both providers, a classification-only prompt instruction, and a schema test proving the
fields are accepted AND that omission still validates byte-identically.

SCOPE FENCE: schema/types widen + AI classification input ONLY. NO server tax math here (that is
Plan 130-02 / TAX-03). NO editor fields (Phase 133). NO PDF (Phase 134). NO discount/deposit/markup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/129-pricing-schema-engine-scaffold/129-VERIFICATION.md

<interfaces>
<!-- Current AI output contract (lib/ai/schema.ts) — the single source of truth.
     EstimateOutput = z.infer<typeof estimateOutputSchema>, re-exported from types.ts.
     Widen lineItemSchema; everything downstream (LineItemOutput) follows. -->

From lib/ai/schema.ts (lineItemSchema today):
```typescript
const lineItemSchema = z.object({
  description: z.string().min(1),
  quantity: z.number().finite().nonnegative(),
  unit: z.string().optional(),
  unit_price: z.number().finite().nonnegative(),
  price_source: z.preprocess(
    (v) => (v === 'price_book' || v === 'researched' ? v : 'ai_estimate'),
    z.enum(['price_book', 'ai_estimate', 'researched'])
  ),
})
```

From lib/ai/types.ts (LineItemOutput today):
```typescript
export type LineItemOutput = {
  description: string
  quantity: number
  unit?: string
  unit_price: number
  price_source: 'price_book' | 'ai_estimate' | 'researched'
}
```

From lib/ai/providers/anthropic.ts (advisory create_estimate tool — item properties, TWO occurrences ~L70-81 and ~L172-183):
```typescript
required: ['description', 'quantity', 'unit_price', 'price_source'],
properties: {
  description: { type: 'string' },
  quantity: { type: 'number' },
  unit: { type: 'string' },
  unit_price: { type: 'number' },
  price_source: { type: 'string', enum: ['price_book', 'ai_estimate'], description: "..." },
},
```

NOTE: The Phase 129 migration already defines the persisted enum:
  estimate_items.tax_category CHECK (tax_category IS NULL OR tax_category IN ('labor','materials','other'))
The zod enum MUST match exactly: 'labor' | 'materials' | 'other'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the AI output schema + types with optional taxable/tax_category (TAX-02)</name>
  <read_first>
    - lib/ai/schema.ts (the authoritative zod lineItemSchema — lines 15-26)
    - lib/ai/types.ts (LineItemOutput — lines 12-18; EstimateOutput re-export note lines 26-30)
    - .planning/phases/129-pricing-schema-engine-scaffold/129-VERIFICATION.md (the tax_category enum is 'labor'|'materials'|'other' per the migration CHECK)
  </read_first>
  <behavior>
    - Test (NEW file tests/unit/ai/tax-classification-schema.test.ts):
      - estimateOutputSchema.safeParse SUCCEEDS on an item WITH taxable:false + tax_category:'labor' and the parsed item preserves both values.
      - estimateOutputSchema.safeParse SUCCEEDS on an item that OMITS both fields (retrocompat) and the parsed item has taxable === undefined and tax_category === undefined (no injected default — the SERVER applies the taxable=true default in Plan 130-02, not the schema).
      - safeParse REJECTS tax_category:'plumbing' (not in the labor/materials/other enum).
      - A TypeScript compile check: an object typed as LineItemOutput accepts `taxable?: boolean` and `tax_category?: 'labor'|'materials'|'other'` (assign one in the test to force the type to widen; `npx tsc --noEmit` must stay clean).
  </behavior>
  <action>
    In lib/ai/schema.ts, add to `lineItemSchema` (additive, BOTH optional — never required, so existing
    AI output keeps validating):
      taxable: z.boolean().optional(),
      tax_category: z.enum(['labor', 'materials', 'other']).optional().nullable(),
    Do NOT add a default (no `.default(true)`) — the schema must leave omitted fields as `undefined` so
    the byte-identical retrocompat path holds; the taxable=true DEFAULT is applied SERVER-SIDE in Plan
    130-02. Keep the existing price_source preprocess and all other fields byte-unchanged.

    In lib/ai/types.ts, widen `LineItemOutput` with the two OPTIONAL fields mirroring the schema:
      taxable?: boolean
      tax_category?: 'labor' | 'materials' | 'other' | null
    Leave EstimateSectionOutput and the EstimateOutput re-export untouched (EstimateOutput is z.infer of
    the schema, so it picks up the new optional fields automatically).

    Create tests/unit/ai/tax-classification-schema.test.ts implementing the <behavior> cases above,
    importing estimateOutputSchema from '@/lib/ai/schema' and the LineItemOutput type from '@/lib/ai/types'.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai/tax-classification-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tax_category" lib/ai/schema.ts` shows the optional enum field added to lineItemSchema.
    - `grep -n "taxable" lib/ai/schema.ts` shows `z.boolean().optional()` (NO `.default`).
    - `grep -n "tax_category" lib/ai/types.ts` shows the optional union added to LineItemOutput.
    - `npx vitest run tests/unit/ai/tax-classification-schema.test.ts` passes (accept-with-fields, accept-on-omission, reject-bad-enum).
    - `npx vitest run tests/unit/ai/no-ai-calculator.test.ts` stays GREEN (ENG-01 fence unbroken — no numeric/total item field added).
  </acceptance_criteria>
  <done>The AI output contract accepts optional per-item taxable/tax_category, omission still validates with no injected default, and the bad-enum case rejects.</done>
</task>

<task type="auto">
  <name>Task 2: Advisory tool-schema fields on both providers + classification-only prompt instruction (TAX-02)</name>
  <read_first>
    - lib/ai/providers/anthropic.ts (TWO create_estimate tool item-property blocks: ~L70-81 generate, ~L172-183 refine)
    - lib/ai/providers/gemini.ts (mirror create_estimate tool item-property block(s))
    - lib/ai/prompt-builder.ts (buildSystemPrompt — price-book block lines 74-81; Security block MUST stay LAST, lines 95-96)
  </read_first>
  <action>
    Both providers expose an ADVISORY create_estimate tool schema (the zod schema in Task 1 is the
    authoritative gate; the tool schema only nudges the model). In EACH item-properties block in
    lib/ai/providers/anthropic.ts (both the generate ~L71 block and the refine ~L173 block) add to
    `properties` (do NOT add to the `required` array — these stay optional so omission is valid):
      taxable: { type: 'boolean', description: 'true if this line item is taxable (default true if omitted).' },
      tax_category: { type: 'string', enum: ['labor', 'materials', 'other'], description: 'Classify the work: "labor" for labor/service lines, "materials" for physical goods, "other" otherwise. Classification only — do NOT compute any tax.' },
    Apply the SAME additive change to the create_estimate item-properties block(s) in
    lib/ai/providers/gemini.ts (match its existing property-shape conventions; keep taxable/tax_category
    OUT of any required list).

    In lib/ai/prompt-builder.ts buildSystemPrompt, append a classification instruction BEFORE the
    Security block (Security must remain the LAST section). Add it right after the price-book block
    (after line ~81, before line ~83 the Additional Instructions block) — e.g.:
      prompt += `\n\n## Tax Classification\nFor each line item, set tax_category to "labor" for labor/service work, "materials" for physical goods or supplies, or "other" otherwise. This is a CLASSIFICATION ONLY — never compute, add, or estimate any tax amount; the system computes all tax deterministically.`
    The instruction must contain NO arithmetic directive. Keep the Security block unchanged and LAST.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai/no-ai-calculator.test.ts tests/unit/ai/tax-classification-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tax_category" lib/ai/providers/anthropic.ts` is >= 2 (both generate + refine tool blocks).
    - `grep -n "tax_category" lib/ai/providers/gemini.ts` shows the advisory field added.
    - `grep -n "Tax Classification" lib/ai/prompt-builder.ts` shows the classification-only instruction; `grep -n "CLASSIFICATION ONLY\|do NOT compute\|never compute" lib/ai/prompt-builder.ts` confirms the no-arithmetic clause.
    - The Security block is still the LAST appended section (`grep -n "## Security" lib/ai/prompt-builder.ts` appears AFTER `## Tax Classification`).
    - `npx vitest run tests/unit/ai/no-ai-calculator.test.ts` GREEN (no calculator tool, no numeric-total item field — ENG-01 preserved).
  </acceptance_criteria>
  <done>Both providers advertise advisory taxable/tax_category item fields (not required), the prompt instructs labor/materials classification with explicit no-arithmetic language, Security stays last, and the ENG-01 fence test is green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/ai/tax-classification-schema.test.ts tests/unit/ai/no-ai-calculator.test.ts` all green.
- `npx tsc --noEmit` clean (types widened additively, no breakage at any existing call site).
- The retrocompat golden (tests/unit/services/pricing-retrocompat.test.ts) is UNAFFECTED by this plan — no totals math changed here.
</verification>

<success_criteria>
- TAX-02 satisfied: the AI output schema + types carry optional per-item taxable/tax_category; the AI classifies (labor/materials) and is given NO arithmetic.
- Omission of the fields still validates with no injected default → the byte-identical retrocompat path is preserved for Plan 130-02 to honor.
- ENG-01 no-AI-calculator fence remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/130-per-item-taxability/130-01-SUMMARY.md`
</output>
