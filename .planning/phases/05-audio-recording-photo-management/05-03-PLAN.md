---
phase: 05-audio-recording-photo-management
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - components/workspace/photos/photos-tab.tsx
  - components/workspace/photos/photo-drop-zone.tsx
  - components/workspace/photos/photo-grid.tsx
  - components/workspace/photos/photo-card.tsx
  - components/workspace/photos/photo-lightbox.tsx
autonomous: true
requirements:
  - PHOTO-01
  - PHOTO-02
  - PHOTO-03
  - PHOTO-04
  - PHOTO-05
  - PHOTO-06
  - PHOTO-07
  - PHOTO-08
  - PHOTO-09
  - PHOTO-10
  - PHOTO-11

must_haves:
  truths:
    - "User can upload multiple photos via file input"
    - "User can capture photos from mobile camera"
    - "User can drag and drop photos on desktop"
    - "Photos display in a responsive grid with thumbnails"
    - "User can view a photo full-size in a lightbox"
    - "User can delete individual photos"
    - "User can reorder photos via drag and drop"
    - "User can add and edit captions per photo"
    - "Photos are compressed client-side before upload"
    - "Maximum 20 photos per project is enforced"
  artifacts:
    - path: "components/workspace/photos/photos-tab.tsx"
      provides: "Main photos tab container replacing PlaceholderTab"
      exports: ["PhotosTab"]
    - path: "components/workspace/photos/photo-drop-zone.tsx"
      provides: "File input with drag-and-drop and camera capture"
      exports: ["PhotoDropZone"]
    - path: "components/workspace/photos/photo-grid.tsx"
      provides: "Sortable photo grid with @dnd-kit"
      exports: ["PhotoGrid"]
    - path: "components/workspace/photos/photo-card.tsx"
      provides: "Thumbnail card with caption and delete"
      exports: ["PhotoCard"]
    - path: "components/workspace/photos/photo-lightbox.tsx"
      provides: "Full-size photo viewer with navigation"
      exports: ["PhotoLightbox"]
  key_links:
    - from: "components/workspace/photos/photo-drop-zone.tsx"
      to: "lib/utils/image-compressor.ts"
      via: "compressImage before upload"
      pattern: "import.*compressImage.*from.*utils/image-compressor"
    - from: "components/workspace/photos/photo-drop-zone.tsx"
      to: "lib/supabase/client.ts"
      via: "Browser Storage upload for compressed photos"
      pattern: "storage.*from.*photos.*upload"
    - from: "components/workspace/photos/photo-grid.tsx"
      to: "@dnd-kit/core"
      via: "DndContext for sortable grid"
      pattern: "import.*DndContext.*from.*@dnd-kit/core"
    - from: "components/workspace/photos/photo-card.tsx"
      to: "lib/actions/photo.ts"
      via: "updatePhotoCaption and deletePhoto"
      pattern: "import.*(updatePhotoCaption|deletePhoto).*from.*actions/photo"
---

<objective>
Build the complete Photos tab with multi-file upload, camera capture, client-side compression, responsive grid with drag-and-drop reorder, lightbox viewing, caption editing, and 20-photo limit enforcement.

Purpose: Enable job-site photo capture and organization for AI estimate generation.
Output: 5 photo components that replace the Photos PlaceholderTab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-audio-recording-photo-management/05-CONTEXT.md
@.planning/phases/05-audio-recording-photo-management/05-RESEARCH.md
@.planning/phases/05-audio-recording-photo-management/05-01-SUMMARY.md

@components/workspace/project-workspace.tsx
@lib/supabase/client.ts

<interfaces>
<!-- From Plan 01 outputs -->

From lib/utils/image-compressor.ts:
```typescript
export function compressImage(file: File, maxWidth?: number, quality?: number): Promise<Blob>
```

From lib/queries/photo.ts:
```typescript
export interface Photo {
  id: string
  project_id: string
  company_id: string
  storage_path: string
  caption: string | null
  ai_description: string | null
  sort_order: number
  created_at: string
}
export function getProjectPhotos(supabase: SupabaseClient, projectId: string): Promise<Photo[]>
```

From lib/actions/photo.ts:
```typescript
export async function createPhoto(projectId: string, storagePath: string, sortOrder: number): Promise<{ data: Photo } | { error: string }>
export async function updatePhotoCaption(photoId: string, caption: string): Promise<{ data: true } | { error: string }>
export async function deletePhoto(photoId: string): Promise<{ data: true } | { error: string }>
export async function reorderPhotos(photoIds: string[]): Promise<{ data: true } | { error: string }>
```

From @dnd-kit:
```typescript
import { DndContext, closestCenter, DragEndEvent } from '@dnd-kit/core'
import { SortableContext, rectSortingStrategy, useSortable, arrayMove } from '@dnd-kit/sortable'
import { CSS } from '@dnd-kit/utilities'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Photo drop zone with compression, upload, and photo card component</name>
  <read_first>
    - lib/utils/image-compressor.ts (compressImage function)
    - lib/supabase/client.ts (browser client for Storage upload)
    - lib/actions/photo.ts (createPhoto action)
    - lib/queries/photo.ts (Photo type)
    - .planning/phases/05-audio-recording-photo-management/05-RESEARCH.md (Pattern 4: Client-Side Image Compression, Pitfall 7: canvas.toBlob)
  </read_first>
  <files>
    components/workspace/photos/photo-drop-zone.tsx,
    components/workspace/photos/photo-card.tsx,
    components/workspace/photos/photo-lightbox.tsx
  </files>
  <action>
1. Create `components/workspace/photos/photo-drop-zone.tsx` ('use client') per D-11, D-12, D-13, D-18:
   - Props: `{ projectId: string; companyId: string; currentCount: number; maxPhotos: number; onPhotosUploaded: (photos: Photo[]) => void }`
   - Default maxPhotos = 20
   - State: `isDragging`, `isUploading`, `uploadProgress` (number of completed / total)

   **File input (per D-11, PHOTO-01, PHOTO-02):**
   - Hidden `<input type="file" accept="image/*" multiple>` referenced by `inputRef`
   - Second hidden input with `capture="environment"` for mobile camera — render only a "Take Photo" button that clicks this input
   - "Upload Photos" button clicks the multi-file input

   **Drag-and-drop zone (per D-12, PHOTO-03):**
   - Div with `onDragOver` (preventDefault, set isDragging), `onDragLeave` (unset isDragging), `onDrop` (extract files from `e.dataTransfer.files`)
   - Styling: `border-2 border-dashed rounded-lg p-8 text-center` when idle, `border-primary bg-primary/5` when dragging
   - Show Upload icon from lucide-react + "Drop photos here or click to upload" text
   - Show current count: `{currentCount} / {maxPhotos} photos`

   **20-photo limit enforcement (per D-18, PHOTO-10):**
   - On file selection (from input or drop): check `currentCount + files.length > maxPhotos`
   - If exceeded: show toast error `Maximum ${maxPhotos} photos per project. You can add ${maxPhotos - currentCount} more.` and return without uploading

   **Upload pipeline (per D-13, D-19, PHOTO-09, PHOTO-11):**
   - For each file in the selected files:
     1. Call `compressImage(file, 2000, 0.85)` — wrap in try/catch, if compression fails use original file per Pitfall 7
     2. Generate UUID for photoId
     3. Upload compressed blob to `photos` bucket at `{companyId}/{projectId}/{photoId}.jpg` via browser Supabase client
     4. Call `createPhoto(projectId, storagePath, currentCount + index)` to insert DB row
   - Process uploads sequentially (not parallel) to show progress: update `uploadProgress` after each
   - On all complete: call `onPhotosUploaded(newPhotos)`, clear uploading state
   - Show progress indicator: `Uploading {completed}/{total}...` with Loader2 spinner

2. Create `components/workspace/photos/photo-card.tsx` ('use client') per D-14, D-16, PHOTO-04, PHOTO-06, PHOTO-08:
   - Props: `{ photo: Photo; onClick: () => void; onDelete: (id: string) => void }`
   - State: `imageUrl` (signed URL), `isEditingCaption`, `caption` (local), `isDeleting`
   - On mount: get signed URL via browser Supabase client `storage.from('photos').createSignedUrl(photo.storage_path, 3600)` per Pitfall 4
   - Layout: aspect-square div with `overflow-hidden rounded-lg relative group` wrapper
     - `<img>` with `object-cover w-full h-full` showing the signed URL. onClick calls `props.onClick` (opens lightbox)
     - Delete button: absolute top-right, `opacity-0 group-hover:opacity-100` transition, X icon in red circle. Calls `deletePhoto(photo.id)` then `onDelete(photo.id)`. No confirmation needed (photos are recoverable from device).
     - Caption overlay: absolute bottom, dark gradient background, shows caption text or "Add caption..." placeholder
     - Click caption area: set isEditingCaption=true, show small input field. On blur or Enter: call `updatePhotoCaption(photo.id, caption)` per D-16, set isEditingCaption=false
   - Show Skeleton while imageUrl is loading

3. Create `components/workspace/photos/photo-lightbox.tsx` ('use client') per D-15, PHOTO-05:
   - Props: `{ photos: Photo[]; selectedIndex: number; open: boolean; onOpenChange: (open: boolean) => void }`
   - Use shadcn/ui `<Dialog>` with `open` and `onOpenChange`
   - State: `currentIndex`, `imageUrl` (signed URL for current photo)
   - On index change: get new signed URL for `photos[currentIndex].storage_path`
   - DialogContent: max-w-4xl, contains full-size image with `object-contain max-h-[80vh]`
   - Navigation arrows: ChevronLeft/ChevronRight buttons on sides, hidden if at start/end
   - Show photo caption below image if present
   - Show photo number: `{currentIndex + 1} / {photos.length}`
   - Keyboard navigation: onKeyDown for ArrowLeft/ArrowRight
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(photo-drop|photo-card|photo-lightbox)" | head -10; echo "---"; ls components/workspace/photos/*.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - components/workspace/photos/photo-drop-zone.tsx exports PhotoDropZone
    - components/workspace/photos/photo-card.tsx exports PhotoCard
    - components/workspace/photos/photo-lightbox.tsx exports PhotoLightbox
    - PhotoDropZone has both file input (multiple) and capture="environment" input per D-11/D-12
    - PhotoDropZone checks 20-photo limit before upload per D-18/PHOTO-10
    - PhotoDropZone calls compressImage before each upload per D-13/PHOTO-11
    - PhotoCard gets signed URL via createSignedUrl per Pitfall 4
    - PhotoCard has inline caption editing per D-16/PHOTO-08
    - PhotoLightbox uses shadcn Dialog with prev/next navigation per D-15/PHOTO-05
  </acceptance_criteria>
  <done>Drop zone handles file/camera/drag upload with compression and limit enforcement, cards display thumbnails with captions, lightbox provides full-size viewing</done>
</task>

<task type="auto">
  <name>Task 2: Sortable photo grid with @dnd-kit and PhotosTab container</name>
  <read_first>
    - components/workspace/photos/photo-card.tsx (PhotoCard component from Task 1)
    - components/workspace/photos/photo-drop-zone.tsx (PhotoDropZone from Task 1)
    - components/workspace/photos/photo-lightbox.tsx (PhotoLightbox from Task 1)
    - lib/actions/photo.ts (reorderPhotos, deletePhoto)
    - lib/queries/photo.ts (Photo type, getProjectPhotos)
    - .planning/phases/05-audio-recording-photo-management/05-RESEARCH.md (Pattern 8: @dnd-kit Sortable Grid, Pitfall 8: hydration)
  </read_first>
  <files>
    components/workspace/photos/photo-grid.tsx,
    components/workspace/photos/photos-tab.tsx
  </files>
  <action>
1. Create `components/workspace/photos/photo-grid.tsx` ('use client') per D-17, PHOTO-07:
   - Props: `{ photos: Photo[]; onReorder: (reorderedPhotos: Photo[]) => void; onDelete: (id: string) => void; onPhotoClick: (index: number) => void }`

   **Sortable wrapper per Pattern 8:**
   ```typescript
   import { DndContext, closestCenter, type DragEndEvent, PointerSensor, TouchSensor, useSensor, useSensors } from '@dnd-kit/core'
   import { SortableContext, rectSortingStrategy, useSortable, arrayMove } from '@dnd-kit/sortable'
   import { CSS } from '@dnd-kit/utilities'
   ```
   - Configure sensors: `useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }), useSensor(TouchSensor, { activationConstraint: { delay: 200, tolerance: 5 } }))` — distance constraint prevents accidental drags, touch delay allows tap-to-click
   - `handleDragEnd(event: DragEndEvent)`: extract active.id and over.id, find old/new index, call `arrayMove(photos, oldIndex, newIndex)`, call `onReorder(reordered)`
   - Grid layout: `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3` per D-14

   **SortablePhoto wrapper component:**
   - Uses `useSortable({ id: photo.id })`
   - Apply `transform` and `transition` via CSS.Transform.toString
   - Renders `<PhotoCard>` with all handlers passed through
   - When dragging: reduce opacity to 0.5

   **Empty state:**
   - If photos.length === 0, don't render grid (parent handles empty state)

2. Create `components/workspace/photos/photos-tab.tsx` ('use client') per D-11:
   - Props: `{ projectId: string; companyId: string; initialPhotos: Photo[] }`
   - State: `photos` (from initialPhotos), `lightboxOpen`, `lightboxIndex`
   - Renders in order:
     1. `<PhotoDropZone>` with `currentCount={photos.length}`, `onPhotosUploaded` appends new photos to state
     2. `<PhotoGrid>` with photos, onReorder saves locally then calls `reorderPhotos(reorderedPhotos.map(p => p.id))` server action, onDelete removes from local state, onPhotoClick opens lightbox
     3. `<PhotoLightbox>` with photos, selectedIndex, open/onOpenChange
   - If photos is empty and not uploading: show EmptyState with Camera icon, "No photos yet", "Upload photos of the job site to help generate an accurate estimate"
   - `handleDelete`: remove photo from local state array, no need to call server action (PhotoCard already calls deletePhoto)
   - `handleReorder`: update local state immediately (optimistic), call `reorderPhotos(newPhotoIds)` in background. On error show toast and revert.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(photo-grid|photos-tab)" | head -10; echo "---"; ls components/workspace/photos/*.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - components/workspace/photos/photo-grid.tsx exports PhotoGrid
    - components/workspace/photos/photos-tab.tsx exports PhotosTab
    - PhotoGrid uses DndContext and SortableContext from @dnd-kit/core per D-17
    - PhotoGrid uses rectSortingStrategy for grid layout
    - PhotoGrid has PointerSensor and TouchSensor with activation constraints
    - PhotosTab wires PhotoDropZone, PhotoGrid, and PhotoLightbox together
    - PhotosTab calls reorderPhotos server action after drag-and-drop per PHOTO-07
    - PhotosTab shows EmptyState when no photos
    - Total photo component files: 5 (photos-tab, photo-drop-zone, photo-grid, photo-card, photo-lightbox)
  </acceptance_criteria>
  <done>Sortable photo grid with drag-and-drop reorder via @dnd-kit, PhotosTab wires all photo components together</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with all 5 photo components
- `ls components/workspace/photos/` shows 5 .tsx files
- `grep -r "DndContext" components/workspace/photos/photo-grid.tsx` confirms @dnd-kit usage
- `grep -r "compressImage" components/workspace/photos/photo-drop-zone.tsx` confirms compression
- `grep -r "capture=" components/workspace/photos/photo-drop-zone.tsx` confirms mobile camera support
- `grep -r "createSignedUrl" components/workspace/photos/photo-card.tsx` confirms signed URLs
</verification>

<success_criteria>
- All 5 photo components compile without errors
- PhotoDropZone supports file input, camera capture, and drag-and-drop with 20-photo limit
- Photos are compressed client-side before upload (max 2000px, quality 0.85)
- PhotoGrid enables drag-and-drop reorder with @dnd-kit
- PhotoLightbox provides full-size viewing with navigation
- PhotoCard supports inline caption editing with debounced save
</success_criteria>

<output>
After completion, create `.planning/phases/05-audio-recording-photo-management/05-03-SUMMARY.md`
</output>
