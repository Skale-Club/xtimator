---
phase: 115-credit-balance-ux-owner-facing
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - tests/unit/billing/credit-balance-card.test.tsx
  - components/billing/credit-balance-card.tsx
  - components/billing/credit-history-list.tsx
  - components/billing/top-up-button.tsx
  - components/app-shell/credit-chip.tsx
  - app/(app)/settings/billing/page.tsx
  - app/(app)/layout.tsx
  - components/app-shell/topbar.tsx
autonomous: false
requirements: [CREDITUI-01, CREDITUI-02]
must_haves:
  truths:
    - "On /settings/billing the owner sees a Credits card showing their balance (no token/cost math)"
    - "The owner sees a recent consumption history list (operation label + signed delta + date)"
    - "A static per-action guidance line reads approximately 'an estimate ≈ 10–15 credits'"
    - "When balance is at/below the lowest threshold, an informational warning + top-up CTA + upgrade CTA appear"
    - "A compact credit chip in the topbar shows the balance and links to /settings/billing"
  artifacts:
    - path: "components/billing/credit-balance-card.tsx"
      provides: "balance + per-action guidance + low/zero warning + CTA card"
      min_lines: 40
    - path: "components/billing/credit-history-list.tsx"
      provides: "owner-safe ledger row list"
      min_lines: 20
    - path: "components/billing/top-up-button.tsx"
      provides: "client button POSTing to create-topup-session"
    - path: "components/app-shell/credit-chip.tsx"
      provides: "compact topbar balance chip"
    - path: "app/(app)/settings/billing/page.tsx"
      provides: "Credits card + history mounted below the Usage card"
      contains: "getCreditOverview"
  key_links:
    - from: "app/(app)/settings/billing/page.tsx"
      to: "getCreditOverview (lib/queries/credits.ts)"
      via: "server-component await"
      pattern: "getCreditOverview\\("
    - from: "components/billing/top-up-button.tsx"
      to: "/api/billing/create-topup-session"
      via: "fetch POST {packIndex}"
      pattern: "create-topup-session"
    - from: "app/(app)/layout.tsx"
      to: "Topbar credit chip"
      via: "credit_balance threaded through the existing companies read"
      pattern: "credit_balance"
---

<objective>
Build the owner-facing UI for CREDITUI-01 + CREDITUI-02: a Credits balance card (balance + static per-action guidance + low/zero warning + top-up/upgrade CTA), a consumption-history list, and a top-up button, all mounted on /settings/billing below the existing "Usage This Month" card. Add an optional compact credit chip to the topbar by extending the layout's existing companies read (no extra query).

Purpose: Surface the credit balance the owner can act on, reusing the existing glass-card visual language and the Phase-113 top-up rail — informational only (enforcement is OFF this milestone). The credits card is ADDITIVE to the still-present count-based usage card (MIG-01 parallel run).
Output: Three new billing components + an optional chip, the billing page extended, and the layout/topbar threading credit_balance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-credit-balance-ux-owner-facing/115-RESEARCH.md
@app/(app)/settings/billing/page.tsx

<interfaces>
<!-- Contracts the executor needs — extracted from the repo + Plan 01's output. -->

From lib/queries/credits.ts (created in Plan 115-01 — consume directly):
```ts
export interface CreditHistoryRow {
  operation_type: string | null   // 'estimate' | 'photo_batch' | 'audio_minutes' | 'price_research' | null
  delta_credits: number           // signed: debit negative, grant/topup positive
  reason: string                  // 'grant' | 'debit' | 'topup' | 'adjust'
  created_at: string
}
export interface CreditOverview { balance: number; history: CreditHistoryRow[]; lowBalanceThresholds: number[] }
export async function getCreditOverview(companyId: string): Promise<CreditOverview>
```

From lib/billing/overage-affordance.ts (the CTA URL constants to reuse):
```ts
// shortfall>0 → { topUpUrl: '/settings/billing?topup=1', upgradeUrl: '/settings/billing' }
// For a LOW (non-zero) balance shortfall may be 0 — drive the warning off
// `balance <= Math.max(...lowBalanceThresholds)`, and reuse these two URL constants directly.
```

From app/api/billing/create-topup-session/route.ts (the top-up button target):
```ts
// POST { packIndex: number } → { url } ; redirect via window.location.href = url
```

From components/billing/upgrade-buttons.tsx (the client-fetch pattern to mirror for top-up-button).

From components/ui/card — the glass card set used by the page:
```tsx
<Card variant="glass" className="p-6">
  <CardHeader className="border-b border-[var(--glass-border)] p-0 pb-4">
    <CardTitle><T>...</T></CardTitle><CardDescription><T>...</T></CardDescription>
  </CardHeader>
  <CardContent className="space-y-3 px-0 pt-4 text-sm">...</CardContent>
</Card>
```

From components/i18n/t.tsx — `<T>copy</T>` wraps all owner-facing strings (EN/PT/ES). Client components use `useTranslation()` from `@/lib/i18n/use-translation`.

From app/(app)/settings/billing/page.tsx — server component; gets `const data = await getBillingData(claims.sub as string)` and has `data` (which exposes `tier` for the upgrade affordance). Company id: the page does NOT currently have companyId in scope — get it via `getActiveCompany()` from `@/lib/queries/active-company` (returns AppCompany with `.id`) the same way the layout does, OR extend getBillingData. PREFER `getActiveCompany()` (one helper, already cached) to obtain companyId for getCreditOverview.

From app/(app)/layout.tsx — already reads `company = await getActiveCompany()` and a parallel `billingRow` select on `companies` by `activeCompanyId`. The chip's balance should come WITHOUT a second companies query: add `credit_balance` to the EXISTING `billingRow` select (`.select('tier, tier_trial_ends_at, credit_balance')`) and thread it into `<Topbar ... creditBalance={billingRow.data?.credit_balance ?? 0} />`.

From components/app-shell/topbar.tsx — TopbarProps `{ company, userId, isAdmin?, navUser? }`; right-actions row is the `<div className="flex items-center gap-1">` block. Add an optional `creditBalance?: number` prop and render `<CreditChip balance={creditBalance} />` in that row (before NotificationBell).

From lib/queries/auth.ts AppCompany — does NOT include credit_balance (it's a separate column on companies). The chip balance therefore comes from the layout's billingRow select, NOT from getActiveCompany().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 RED): credit-balance-card component test</name>
  <read_first>
    - tests/unit/billing/billing-data.test.ts (vitest + render convention used in the billing suite; check tests/unit/settings/*.test.tsx for an existing @testing-library/react render example to copy the render+screen setup)
    - 115-RESEARCH.md §"Validation Architecture" rows for CREDITUI-01 (guidance copy) + CREDITUI-02 (low/zero CTA)
  </read_first>
  <files>tests/unit/billing/credit-balance-card.test.tsx</files>
  <behavior>
    Import `CreditBalanceCard` from `@/components/billing/credit-balance-card`. Render it with plain props (no DB — pure presentational). Mock `@/components/i18n/t` so `<T>` renders its children as text, and stub `@/lib/i18n/use-translation` `useTranslation` → `{ t: (s: string) => s }` if the card or its children import it.
    - Test 1 (CREDITUI-01 balance): given `balance={1240}`, the rendered output contains "1,240" (or "1240") — the headline balance is shown.
    - Test 2 (CREDITUI-01 guidance): the rendered output contains the static per-action guidance substring "10" and "15" and "credits" (the "an estimate ≈ 10–15 credits" range). Assert NO dollar sign and NO "markup"/"token" text appears anywhere in the render (owner never sees cost math).
    - Test 3 (CREDITUI-02 low): given `balance={40}` and `lowBalanceThresholds={[200, 50]}`, a warning region renders (e.g. `data-testid="credit-low-warning"`) AND a top-up CTA linking to `/settings/billing?topup=1` AND an upgrade CTA are present. The warning copy must NOT contain "blocked"/"denied"/"cannot".
    - Test 4 (CREDITUI-02 healthy): given `balance={5000}` and the same thresholds, the `credit-low-warning` region is ABSENT.
    - Test 5 (CREDITUI-02 zero): given `balance={0}`, the warning renders with the top-up CTA present.
  </behavior>
  <action>
    Create the RED test file (`CreditBalanceCard` does not exist yet). Use the billing suite's render convention; if no render helper exists in tests/unit/billing, import `render, screen` from `@testing-library/react` (already a devDep — confirm via an existing `*.test.tsx`). Mock `<T>` to a passthrough that renders `children` (so copy assertions work on plain text).

    Define the component's prop contract HERE (this is what Task 2 implements):
    ```ts
    interface CreditBalanceCardProps {
      balance: number
      lowBalanceThresholds: number[]
    }
    ```
    The top-up button is a separate client component; in this test, either mock `@/components/billing/top-up-button` to a stub that renders an anchor/button, or assert the top-up CTA via the `/settings/billing?topup=1` link the card renders directly. Keep the card itself presentational and deterministic.

    Run — MUST fail (component missing). Commit: `test(115-02): RED for CreditBalanceCard balance + guidance + low/zero CTA`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/credit-balance-card.test.tsx</automated>
  </verify>
  <done>Test file exists and FAILS (component missing). Assertions cover: balance shown, static guidance range present, no dollar/markup/token text, low+zero warning with top-up + upgrade CTA, healthy state hides the warning, copy free of "blocked"/"denied"/"cannot".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): CreditBalanceCard + CreditHistoryList + TopUpButton components</name>
  <read_first>
    - app/(app)/settings/billing/page.tsx (glass Card markup to mirror exactly — the "Usage This Month" card structure)
    - components/billing/trial-banner.tsx (amber AlertTriangle warning-banner style reference)
    - components/billing/upgrade-buttons.tsx (client fetch → window.location.href pattern for top-up-button)
    - lib/billing/overage-affordance.ts (the topUpUrl/upgradeUrl constants)
  </read_first>
  <files>components/billing/credit-balance-card.tsx, components/billing/credit-history-list.tsx, components/billing/top-up-button.tsx</files>
  <action>
    Create three components. All owner-facing copy wrapped in `<T>` (EN/PT/ES).

    1. `components/billing/credit-balance-card.tsx` (server-renderable, NO 'use client') — props `{ balance: number; lowBalanceThresholds: number[] }`. Render a `<Card variant="glass" className="p-6">` mirroring the Usage card:
       - CardHeader with `<CreditCard />` (lucide) icon + `<CardTitle><T>Credits</T></CardTitle>` + `<CardDescription><T>Your remaining AI credits this cycle.</T></CardDescription>`.
       - CardContent: the headline balance — `{balance.toLocaleString()}` in `font-mono font-medium` next to `<T>credits</T>`. NO dollar sign, NO token/markup math.
       - A static per-action guidance line: `<p className="text-xs text-muted-foreground"><T>Roughly speaking, an estimate uses about 10–15 credits.</T></p>` (static range string — matches the locked SEED-035 phrasing; do NOT compute from config — fake precision pre-calibration).
       - Low/zero warning: compute `const isLow = balance <= Math.max(...lowBalanceThresholds)` (guard empty array → fall back to false). When `isLow`, render an amber warning region (`data-testid="credit-low-warning"`, mirror trial-banner's `AlertTriangle` + amber classes) with INFORMATIONAL copy — `<T>You&rsquo;re running low on credits. Top up to keep generating.</T>` (NEVER "blocked"/"denied"/"cannot"; enforcement is OFF). Inside it render `<TopUpButton packIndex={0} />` and an upgrade link `<Link href="/settings/billing"><T>Upgrade plan</T></Link>` (the upgradeUrl constant). Also expose a plain top-up affordance `<Link href="/settings/billing?topup=1">` so the CTA is testable as a link.
       - Import `Link` from `next/link`, `CreditCard`/`AlertTriangle` from `lucide-react`, the glass Card set, `<T>`, and `TopUpButton`.

    2. `components/billing/credit-history-list.tsx` (server-renderable) — props `{ rows: CreditHistoryRow[] }` (import the type from `@/lib/queries/credits`). Render a `<Card variant="glass">` titled `<T>Recent activity</T>`. Map each row to a line: a human label for `operation_type`/`reason` (`estimate` → `<T>Estimate</T>`, `photo_batch` → `<T>Photos</T>`, `audio_minutes` → `<T>Audio</T>`, `price_research` → `<T>Price research</T>`, reason `grant` → `<T>Monthly grant</T>`, `topup` → `<T>Top-up</T>`, `adjust` → `<T>Adjustment</T>`), a SIGNED delta (`{row.delta_credits > 0 ? '+' : ''}{row.delta_credits.toLocaleString()}` in `font-mono`, green when positive / muted when negative), and the date (`new Date(row.created_at).toLocaleDateString('en-US', { dateStyle: 'medium' })`). Empty list → `<p><T>No credit activity yet.</T></p>`. NEVER render real_cost_usd/markup (they are not even on the row type — defense in depth from Plan 01).

    3. `components/billing/top-up-button.tsx` (`'use client'`) — props `{ packIndex: number }`. Mirror `upgrade-buttons.tsx`: a `<Button>` (`<T>Top up credits</T>`) whose onClick POSTs `/api/billing/create-topup-session` with `{ packIndex }`, then `if (res.ok && data.url) window.location.href = data.url`. Disable while pending. Do NOT import getBillingConfig (server-only) — packIndex is the only thing the client needs.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/credit-balance-card.test.tsx</automated>
  </verify>
  <done>credit-balance-card.test.tsx GREEN. Card shows balance + static 10–15 guidance + low/zero warning with top-up + upgrade CTA; history list maps owner-safe rows; top-up button POSTs to create-topup-session. No dollar/markup/token math, no "blocked"/"denied"/"cannot". tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Mount credits card on /settings/billing + optional topbar chip</name>
  <read_first>
    - app/(app)/settings/billing/page.tsx (where to insert the new cards — below the usage grid, above/within the existing space-y-8 wrapper)
    - app/(app)/layout.tsx (the existing billingRow companies select + Topbar render — extend with credit_balance)
    - components/app-shell/topbar.tsx (TopbarProps + the right-actions div)
    - lib/queries/active-company.ts (getActiveCompany → companyId for getCreditOverview on the billing page)
  </read_first>
  <files>app/(app)/settings/billing/page.tsx, app/(app)/layout.tsx, components/app-shell/topbar.tsx, components/app-shell/credit-chip.tsx</files>
  <action>
    Part A — billing page (CREDITUI-01/02 surface):
    - In `app/(app)/settings/billing/page.tsx`, after `const data = await getBillingData(...)`, obtain the active company id: `const company = await getActiveCompany()` (import from `@/lib/queries/active-company`; the page already redirects if `!data`, and `company` will be non-null for an authed onboarded user — guard `if (!company) redirect('/onboarding')`). Then `const credits = await getCreditOverview(company.id)` (import `getCreditOverview` from `@/lib/queries/credits`).
    - Import `CreditBalanceCard` + `CreditHistoryList`. Inside the existing `<div className="w-full space-y-8">`, ADD a new block BELOW the "Current plan + usage" grid and ABOVE the TierCardsGrid: a two-column (or stacked) section with `<CreditBalanceCard balance={credits.balance} lowBalanceThresholds={credits.lowBalanceThresholds} />` and `<CreditHistoryList rows={credits.history} />`. Do NOT remove or alter the existing "Usage This Month" card — the credits card is ADDITIVE (MIG-01 parallel run).

    Part B — optional topbar chip (CREDITUI-01 header surface):
    - Create `components/app-shell/credit-chip.tsx` (`'use client'` or server — make it a simple `<Link href="/settings/billing">` showing a `<Coins />` icon + `{balance.toLocaleString()}` + `<T>credits</T>` via `useTranslation` if client). Props `{ balance: number }`. Compact, glass/muted styling matching the topbar action buttons (h-9, rounded-md). NO token/cost math.
    - In `components/app-shell/topbar.tsx`: add `creditBalance?: number` to `TopbarProps`; render `{typeof creditBalance === 'number' && <CreditChip balance={creditBalance} />}` inside the right-actions `<div className="flex items-center gap-1">`, before `<NotificationBell ... />`. Import CreditChip.
    - In `app/(app)/layout.tsx`: extend the existing `billingRow` companies select from `.select('tier, tier_trial_ends_at')` to `.select('tier, tier_trial_ends_at, credit_balance')` (NO new query — reuse the in-flight one, avoiding Pitfall 4). Pass `creditBalance={billingRow.data?.credit_balance ?? 0}` into `<Topbar ... />`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>/settings/billing renders the Credits card + history below the Usage card (Usage card untouched). Topbar shows a credit chip fed by the layout's existing companies read (no second query). getCreditOverview is called with the active company id. tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of credit balance UX</name>
  <what-built>Owner-facing credit balance UX: a Credits card (balance + "an estimate ≈ 10–15 credits" guidance + low/zero warning + top-up/upgrade CTA) and a consumption-history list on /settings/billing, plus a compact credit chip in the topbar.</what-built>
  <how-to-verify>
    1. `npm run dev`, log in, open `/settings/billing`.
    2. Confirm a "Credits" glass card shows the balance, the static "≈ 10–15 credits" guidance line, and a "Recent activity" list — with NO dollar amounts, NO token counts, NO "markup" anywhere.
    3. Confirm the existing "Usage This Month" card is still present (parallel run).
    4. Confirm the topbar shows a credit chip with the balance; clicking it lands on /settings/billing.
    5. If the test company's balance is at/below 50, confirm the amber low-balance warning + "Top up credits" + "Upgrade plan" CTAs appear, and the copy says nothing like "blocked".
  </how-to-verify>
  <action>Run the app locally and visually verify the credit balance UX surfaces per the steps in how-to-verify. Per project memory, human-verify checkpoints are auto-approved — proceed without pausing.</action>
  <done>Owner sees balance + history + guidance on /settings/billing with no token/cost math; topbar chip works; low/zero state shows informational warning + top-up/upgrade CTA.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/billing/credit-balance-card.test.tsx` green.
- `npx tsc --noEmit -p tsconfig.json` clean (page + layout + topbar + 4 components).
- grep: no `$`/`real_cost_usd`/`markup`/`token` rendered in components/billing/credit-*.tsx; no "blocked"/"denied"/"cannot" in the warning copy.
- grep: layout.tsx billingRow select includes `credit_balance`; no second `from('companies')` added for the chip.
- The existing "Usage This Month" card remains in app/(app)/settings/billing/page.tsx (MIG-01 additive).
</verification>

<success_criteria>
- CREDITUI-01: owner sees balance + consumption history + static per-action guidance on /settings/billing (and a topbar chip) — never token/cost math.
- CREDITUI-02: low/zero balance shows an informational warning + working top-up CTA (create-topup-session) + upgrade CTA, driven by lowBalanceThresholds.
- Credits card is additive to the existing count-based usage card; no existing surface removed.
</success_criteria>

<output>
After completion, create `.planning/phases/115-credit-balance-ux-owner-facing/115-02-SUMMARY.md`
</output>
