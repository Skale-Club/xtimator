---
phase: 180-isolated-demo-session-read-only-foundation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [lib/demo/guard.ts, lib/actions/auth.ts, lib/actions/settings.ts, lib/demo/actions.ts, tests/unit/demo/guard.test.ts, tests/unit/demo/auth-action-boundaries.test.ts, tests/unit/demo/exit-action-boundaries.test.ts]
autonomous: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths:
    - "Demo user OR deterministic demo company denies writes without an owner, admin, provider, or support exemption."
    - "Demo credential/profile/password/account mutations are denied before Supabase Auth or service-role effects, while anonymous login/signup remain available."
    - "A demo visitor can exit without revoking another shared demo session and is sent to the validated absolute apex signup URL."
  artifacts:
    - path: lib/demo/guard.ts
      provides: shared ambient and explicit-context read-only classifier
      exports: [isDemoContext, assertWritable, assertCompanyWritable, demoGuardResponse]
    - path: lib/demo/actions.ts
      provides: local-only demo exit and validated apex signup handoff
      exports: [exitDemoToSignup]
  key_links:
    - from: lib/actions/auth.ts
      to: lib/demo/guard.ts
      via: authenticated mutation guards before Auth API calls
      pattern: assertWritable
    - from: lib/demo/actions.ts
      to: validated apex origin
      via: absolute signup redirect after local logout
      pattern: signOut.*scope.*local
---
<objective>Establish the D-07 through D-10 shared read-only authority and protect Auth mutations without breaking anonymous entry paths.</objective>
<execution_context>@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Vanildo/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-CONTEXT.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-RESEARCH.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-PATTERNS.md</context>
<tasks>
<task type="tdd">
  <name>Task 1: RED — define shared guard and Auth-effect denial tests</name>
  <read_first>@lib/demo/guard.ts @lib/demo/config.ts @lib/demo/actions.ts @lib/utils/site-url.ts @lib/actions/auth.ts @lib/actions/settings.ts @lib/actions/client.ts @tests/unit/phase83-server-action-sweep.test.ts</read_first>
  <files>tests/unit/demo/guard.test.ts, tests/unit/demo/auth-action-boundaries.test.ts, tests/unit/demo/exit-action-boundaries.test.ts</files>
  <action>Write failing focused tests first. Use `lib/actions/client.ts` as the guarded-action analog for an export sweep. Cover the four D-08 user/company truth-table states, owner/admin/provider/support-like metadata, explicit non-cookie context, and standard 403 response. Add source-order and mocked-call tests proving authenticated password reset/update, email/profile update, account deletion, and Auth admin deletion are denied before Supabase Auth/service-role calls. For D-03/D-04/D-12-compatible demo exit, mock `exitDemoToSignup()` and require exactly `supabase.auth.signOut({ scope: 'local' })`, no global/others scope, followed by a validated absolute apex `https://xtimator.com/?auth=signup`-style redirect derived only from canonical configured origin; reject demo-host, relative, header-derived, credentialed, query-bearing, and non-root origin inputs. Explicitly assert anonymous login, signup, and unauthenticated reset-request initiation retain their existing behavior because they cannot yet be classified as an authenticated demo mutation.</action>
  <verify><automated>npx vitest run tests/unit/demo/guard.test.ts tests/unit/demo/auth-action-boundaries.test.ts tests/unit/demo/exit-action-boundaries.test.ts</automated></verify>
  <done>The guard, Auth-boundary, and demo-exit tests are collected and fail before implementation.</done>
  <acceptance_criteria><item>Tests fail on either missing OR signal, a role exemption, a guard after an Auth effect, a non-local logout scope, or a non-apex absolute signup redirect.</item></acceptance_criteria>
</task>
<task type="tdd">
  <name>Task 2: GREEN — strengthen the canonical guard and Auth actions</name>
  <read_first>@tests/unit/demo/guard.test.ts @tests/unit/demo/auth-action-boundaries.test.ts @tests/unit/demo/exit-action-boundaries.test.ts @lib/demo/guard.ts @lib/demo/actions.ts @lib/demo/config.ts @lib/utils/site-url.ts @lib/queries/active-company.ts @lib/actions/client.ts</read_first>
  <files>lib/demo/guard.ts, lib/actions/auth.ts, lib/actions/settings.ts, lib/demo/actions.ts</files>
  <action>Strengthen, do not replace, D-10 `assertWritable` and `demoGuardResponse` with one `isDemoContext({ userId?, email?, companyId? })` classifier and `assertCompanyWritable`. Ambient resolution must use verified claims plus membership-validated active company, not a raw cookie. Per D-07/D-08, either demo identity signal dominates every role. In `lib/actions/auth.ts` and the Auth bypasses in settings, preserve anonymous login/signup and allowed local recovery behavior, but resolve authenticated context and deny demo password/profile/email/account mutations before `updateUser`, reset/update password, storage, email, or admin-delete calls. In `lib/demo/actions.ts`, make the explicit demo-exit allowlist call `signOut({ scope: 'local' })` and redirect only to `new URL('/?auth=signup', DEMO_APEX_ORIGIN)` after parsing the server-side `DEMO_APEX_ORIGIN` as a root HTTP(S) origin with no credentials, path, query, or fragment; reject the configured demo origin and every header-derived value. Do not permit the demo host as the destination.</action>
  <verify><automated>npx vitest run tests/unit/demo/guard.test.ts tests/unit/demo/auth-action-boundaries.test.ts tests/unit/demo/exit-action-boundaries.test.ts</automated></verify>
  <done>Shared guard tests pass, all classified authenticated Auth mutations return the repository-standard denial before any Auth effect, and demo exit revokes only the caller's host-local session before the apex signup handoff.</done>
  <acceptance_criteria><item>No canonical admin/provider exemption is introduced; anonymous flows remain behaviorally compatible and exit cannot revoke other demo visitors.</item></acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| claims/company → guard | Either trusted demo signal is sufficient. |
| action → Supabase Auth | Guard must precede mutation and service-role authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-180-04 | Elevation | guard | mitigate | OR truth table and no role bypass |
| T-180-05 | Tampering | Auth actions | mitigate | guard-before-effect tests |
| T-180-SC | Tampering | dependencies | accept | no package install |
</threat_model>
<verification>Run all three focused suites and `npx tsc --noEmit -p tsconfig.ci.json`.</verification>
<success_criteria>SAFE-01/02 deny Auth effects for either demo signal without blocking legitimate anonymous authentication, and demo exit is local-only with a validated absolute apex signup redirect.</success_criteria>
<output>Create `.planning/phases/180-isolated-demo-session-read-only-foundation/180-02-SUMMARY.md` when done.</output>
