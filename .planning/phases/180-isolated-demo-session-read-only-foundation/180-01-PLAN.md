---
phase: 180-isolated-demo-session-read-only-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [lib/demo/config.ts, lib/demo/session.ts, app/demo/entry/route.ts, proxy.ts, playwright.config.ts, tests/unit/demo/config.test.ts, tests/unit/demo/host-routing.test.ts, tests/unit/demo/session-route.test.ts]
autonomous: true
requirements: [ENTRY-01, ENTRY-02, ENTRY-03, ENTRY-04]
must_haves:
  truths:
    - "An apex request to /demo/entry redirects only to the configured demo-host /demo/entry and leaves apex auth state untouched."
    - "The exact demo host establishes or repairs only the dedicated demo identity and deterministic active company, then reaches /dashboard."
    - "The configured demo principal has no `platform_admins` authority; any such row prevents demo session activation."
    - "Invalid demo-host state is repaired once or ends in a terminal 503 without a redirect loop."
  artifacts:
    - path: lib/demo/session.ts
      provides: response-bound idempotent demo session state machine
      exports: [classifyDemoEntryRequest, establishDemoSession]
    - path: app/demo/entry/route.ts
      provides: additive apex handoff and exact demo-host entry route
      exports: [GET]
  key_links:
    - from: app/demo/entry/route.ts
      to: lib/demo/session.ts
      via: exact demo-host branch delegates cookie transitions
      pattern: establishDemoSession
    - from: lib/demo/session.ts
      to: '@supabase/ssr'
      via: final redirect response owns setAll writes
      pattern: createServerClient
    - from: lib/demo/session.ts
      to: platform_admins
      via: verified demo principal id is rejected before session activation
      pattern: platform_admins
---

<objective>
Implement the D-01 through D-07 isolated entry and dedicated-principal contract, with an additive `/demo/entry` seam only.

Purpose: public demo verification must not alter an existing apex session or the legacy `/demo` experience.
Output: validated host configuration, deterministic session state machine, proxy wiring, and focused unit tests.
</objective>

<execution_context>
@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Vanildo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/180-isolated-demo-session-read-only-foundation/180-CONTEXT.md
@.planning/phases/180-isolated-demo-session-read-only-foundation/180-RESEARCH.md
@.planning/phases/180-isolated-demo-session-read-only-foundation/180-PATTERNS.md
@.planning/phases/180-isolated-demo-session-read-only-foundation/180-VALIDATION.md
</context>

<tasks>
<task type="tdd">
  <name>Task 1: RED — specify the fixed host and session state machine</name>
  <read_first>@lib/demo/config.ts @lib/supabase/server.ts @lib/queries/active-company.ts @proxy.ts @tests/unit/middleware.test.ts</read_first>
  <files>tests/unit/demo/config.test.ts, tests/unit/demo/host-routing.test.ts, tests/unit/demo/session-route.test.ts</files>
  <action>Write failing focused tests first. Per D-01/D-04, cover only fixed `DEMO_APP_ORIGIN` parsing and exact scheme/host/port comparisons; assert apex `/demo/entry` redirects to demo-origin `/demo/entry` and performs no Supabase/Auth/cookie work. Per D-02/D-03/D-05/D-06, cover valid reuse, wrong/stale/partial repair, local-scope sign-out, expiry of observed Supabase chunks plus `active_company_id`, one repair attempt, terminal generic 503, verified membership, no `Domain`, and protocol-derived `Secure`. Per D-07, mock the verified configured demo principal's `platform_admins` lookup by immutable user id: a row must make entry fail closed with generic 503 before active-company cookie/dashboard redirect, while an absent row permits the dedicated principal; do not model authority using mutable role metadata. Assert hostile `next`, Host, forwarded-host, token, email, and company input cannot select a destination or identity.</action>
  <verify><automated>npx vitest run tests/unit/demo/config.test.ts tests/unit/demo/host-routing.test.ts tests/unit/demo/session-route.test.ts</automated></verify>
  <done>All three test files are collected and fail only because the new host/session contract is absent.</done>
  <acceptance_criteria><item>Tests name every ENTRY-01..04 state and reject cookie bleed, open redirects, credential input, and redirect loops.</item></acceptance_criteria>
</task>
<task type="tdd">
  <name>Task 2: GREEN — implement host-only idempotent entry</name>
  <read_first>@tests/unit/demo/config.test.ts @tests/unit/demo/session-route.test.ts @lib/demo/config.ts @lib/queries/active-company.ts @lib/utils/site-url.ts</read_first>
  <files>lib/demo/config.ts, lib/demo/session.ts, app/demo/entry/route.ts</files>
  <action>Implement the failing contract. Keep `lib/demo/config.ts` server-only and extend it with `getDemoAppOrigin()` that accepts only a root HTTP(S) origin, rejects credentials/query/hash/non-root paths, rejects production HTTP, and preserves D-05 canonical IDs and credentials. Create `classifyDemoEntryRequest` and `establishDemoSession`; on the apex branch, redirect only to fixed demo-host `/demo/entry` without clearing or creating apex auth state. On the exact demo host, create the redirect response before the SSR client, reuse only claims for `DEMO_USER_EMAIL` with verified `company_members` membership, then query `platform_admins` for that verified principal id and fail closed if any authority row exists (D-07). Otherwise local-sign-out and clear observed host cookies then sign in with D-05 credentials, verify identity/membership and the same no-platform-admin invariant, set deterministic host-only `active_company_id`, and redirect relatively to `/dashboard`. Omit cookie `domain`, derive `secure` from the validated origin protocol, and return generic 503 on every configuration/auth/membership/authority failure.</action>
  <verify><automated>npx vitest run tests/unit/demo/config.test.ts tests/unit/demo/session-route.test.ts</automated></verify>
  <done>All host/session unit tests pass; only a verified dedicated demo session can reach the real `/dashboard` from the demo host.</done>
  <acceptance_criteria><item>D-01 through D-06 are implemented without browser-visible credentials or apex-cookie writes.</item></acceptance_criteria>
</task>
<task type="tdd">
  <name>Task 3: GREEN — order proxy routing around the additive entry</name>
  <read_first>@proxy.ts @app/demo/entry/route.ts @tests/unit/demo/host-routing.test.ts @playwright.config.ts @AGENTS.md</read_first>
  <files>proxy.ts, playwright.config.ts, tests/unit/demo/host-routing.test.ts</files>
  <action>Wire exact `/demo/entry` routing before protected-route redirects while preserving claim-free health, webhook, cron, and Inngest routes and existing protected `Set-Cookie` propagation. Compare request metadata only to the validated origin allowlist and never build a destination from headers. Configure distinct apex and configured demo origins for the later final browser-evidence plan. Per D-12, do not modify `app/demo` index/pages, landing CTA, or any legacy `/demo` behavior; the only new demo route is `app/demo/entry/route.ts`.</action>
  <verify><automated>npx vitest run tests/unit/demo/config.test.ts tests/unit/demo/host-routing.test.ts tests/unit/demo/session-route.test.ts tests/unit/middleware.test.ts</automated></verify>
  <done>Proxy routing admits the new additive entry on both exact hosts and retains all machine and legacy-demo routing behavior.</done>
  <acceptance_criteria><item>Host routing tests pass and `git diff -- app/demo` contains only the additive `app/demo/entry/route.ts` path.</item></acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| host/request metadata → entry | Only a parsed configured origin controls routing. |
| demo credentials → Supabase | Server-held credentials and returned membership are reverified. |
| demo host → apex host | Host-only cookies prevent session collision. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-180-01 | Spoofing | entry/proxy | mitigate | exact-origin tests and fixed paths |
| T-180-02 | Information Disclosure | cookies/config | mitigate | server-only config, no Domain, generic 503 |
| T-180-03 | Denial of Service | repair flow | mitigate | one repair then terminal response |
| T-180-SC | Tampering | dependencies | accept | no package install |
</threat_model>
<verification>Run the three focused host/session suites and the middleware regression. Playwright evidence is reserved for Plan 14.</verification>
<success_criteria>ENTRY-01..04 are unit-proven; apex `/demo/entry` hands off to demo-host `/demo/entry`; legacy `/demo` remains unchanged.</success_criteria>
<output>Create `.planning/phases/180-isolated-demo-session-read-only-foundation/180-01-SUMMARY.md` when done.</output>
