---
phase: 180-isolated-demo-session-read-only-foundation
plan: 13
type: tdd
wave: 1
depends_on: []
files_modified: [tests/unit/demo/rls-migration-contract.test.ts, tests/integration/demo-readonly-rls.test.ts, supabase/migrations/20260726000001_demo_readonly_foundation.sql]
autonomous: true
requirements: [SAFE-03]
must_haves:
  truths:
    - "Demo clients can read seeded rows but cannot directly mutate current public tables or company storage."
    - "Normal clients cannot mutate deterministic demo-company rows; normal-tenant and service-role controls retain their permissions."
  artifacts:
    - path: supabase/migrations/20260726000001_demo_readonly_foundation.sql
      provides: transactional restrictive user/company/storage deny policies
      contains: AS RESTRICTIVE
    - path: tests/integration/demo-readonly-rls.test.ts
      provides: direct authenticated database/storage proof
  key_links:
    - from: storage.objects
      to: public.is_demo_company
      via: restrictive policy on trusted company path
      pattern: is_demo_company
---
<objective>Create D-11's final direct-Supabase write boundary and its static/live contracts.</objective>
<execution_context>@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Vanildo/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-CONTEXT.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-RESEARCH.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-PATTERNS.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-VALIDATION.md</context>
<tasks>
<task type="tdd"><name>Task 1: RED — write RLS migration and live direct-write contracts</name><read_first>@supabase/migrations/20260530000001_demo_readonly.sql @supabase/migrations/20260706000006_demo_readonly_rerun.sql @supabase/migrations/20260706000004_storage_rls_company_members.sql @tests/integration/platform-brand-rls.test.ts</read_first><files>tests/unit/demo/rls-migration-contract.test.ts, tests/integration/demo-readonly-rls.test.ts</files><action>Write failing static and env-gated live tests. Static parsing must demand a transaction; one non-null/non-orphan/non-ambiguous `demo_config` mapping preflight; pinned-search-path stable security-definer helpers; restrictive INSERT/UPDATE/DELETE user triplets on every current public RLS base table except `demo_config`; compatible company-ID triplets, `companies.id`, and `storage.objects`; and terminal `pg_policies` catalog assertions. The live suite uses non-persistent clients and service-role-owned fixtures/cleanup to prove demo SELECT allowed, demo CRUD/storage denied, normal principal denied on demo-company rows, normal-tenant writes allowed, and service-role reset allowed.</action><verify><automated>npx vitest run tests/unit/demo/rls-migration-contract.test.ts tests/integration/demo-readonly-rls.test.ts</automated></verify><done>Both RLS test files are collected and fail until the restrictive migration exists; absent integration env is an explicit skip only.</done><acceptance_criteria><item>Tests do not use comment-count grep gates and cleanup touches only test-owned fixtures/objects.</item></acceptance_criteria></task>
<task type="tdd"><name>Task 2: GREEN — implement transactional restrictive policy coverage</name><read_first>@tests/unit/demo/rls-migration-contract.test.ts @tests/integration/demo-readonly-rls.test.ts @supabase/migrations/20260530000001_demo_readonly.sql @supabase/migrations/20260706000004_storage_rls_company_members.sql</read_first><files>supabase/migrations/20260726000001_demo_readonly_foundation.sql</files><action>Create one rerunnable transaction. Per D-11, preflight exactly the dedicated user/company mapping before hardening `demo_config.company_id`; preserve/recreate `is_demo_user()` and add pinned-search-path `STABLE SECURITY DEFINER is_demo_company(uuid)` with least execute privilege. Add restrictive INSERT/UPDATE/DELETE user policies on every current public RLS table except `demo_config`, company-row triplets on compatible UUID `company_id` tables, `companies.id` triplets, and storage user/company-path triplets using safe first-folder UUID handling. Do not replace permissive membership policies, keep service-role reset and security-invoker estimate writes, and raise on any missing final catalog coverage.</action><verify><automated>npx vitest run tests/unit/demo/rls-migration-contract.test.ts &amp;&amp; supabase db lint</automated></verify><done>The static SQL contract and lint pass; invalid mapping or incomplete policy coverage aborts the migration atomically.</done><acceptance_criteria><item>Restrictive policies only remove direct authenticated access and include Storage plus post-rerun tables.</item></acceptance_criteria></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| browser token → PostgREST/Storage | Application guards can be bypassed directly. |
| migration → unknown data | Preflight must fail atomically rather than guess. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-180-08 | Tampering | public/storage rows | mitigate | restrictive user/company policies and live tests |
| T-180-09 | Denial of Service | migration | mitigate | transactional preflight/rollback |
| T-180-10 | Elevation | helper functions | mitigate | pinned search_path and least grants |
| T-180-SC | Tampering | dependencies | accept | no package install |
</threat_model>
<verification>Run static contract and lint; only the final authorization plan may target a linked production project.</verification><success_criteria>SAFE-03 protects all current RLS public tables and Storage while normal tenant and service-role controls remain valid.</success_criteria><output>Create `.planning/phases/180-isolated-demo-session-read-only-foundation/180-13-SUMMARY.md` when done.</output>
