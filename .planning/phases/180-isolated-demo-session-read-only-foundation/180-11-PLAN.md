---
phase: 180-isolated-demo-session-read-only-foundation
plan: 11
type: tdd
wave: 3
depends_on: [180-02, 180-09]
files_modified: [app/api/webhooks/stripe/route.ts, lib/billing/connect-webhook.ts, lib/queries/chat.ts, lib/chat/tools.ts, lib/notifications/customer-send.ts, lib/notifications/dispatch.ts, lib/integrations/xphere/dispatch.ts, tests/unit/demo/service-funnel-boundaries.test.ts]
autonomous: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths: ["Shared chat, customer-send, notification, and Xphere services reject demo company work before persistence, provider, or dispatch effects.", "A Stripe-signed event first verifies its signature, then resolves a trusted company and denies demo-company work before idempotency writes, credits, notifications, provider calls, or dispatch."]
  artifacts:
    - path: tests/unit/demo/service-funnel-boundaries.test.ts
      provides: domain-service and signed-webhook funnel proof
  key_links:
    - from: app/api/webhooks/stripe/route.ts
      to: lib/demo/guard.ts
      via: signature verification then trusted Stripe company resolution before idempotency or handler dispatch
      pattern: assertCompanyWritable
    - from: lib/notifications/dispatch.ts
      to: provider/queue work
      via: company guard before dispatch
      pattern: assertCompanyWritable
---
<objective>Implement the exact Stripe signed-webhook and shared domain-service D-09 funnels without using browser cookies as machine authority.</objective>
<execution_context>@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Vanildo/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/180-isolated-demo-session-read-only-foundation/180-CONTEXT.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-RESEARCH.md @.planning/phases/180-isolated-demo-session-read-only-foundation/180-02-SUMMARY.md</context>
<tasks>
<task type="tdd"><name>Task 1: RED — specify shared service and signed webhook funnels</name><read_first>@lib/queries/chat.ts @lib/chat/tools.ts @lib/notifications/customer-send.ts @lib/notifications/dispatch.ts @lib/integrations/xphere/dispatch.ts @app/api/webhooks/stripe/route.ts</read_first><files>tests/unit/demo/service-funnel-boundaries.test.ts</files><action>Write failing focused mocks/source-order tests for chat persistence/tools, customer sends, notification dispatch, and Xphere dispatch. For signed webhook callers, assert signature verification remains first; only after trusted event/row company resolution must the service funnel deny demo company before service-role writes, provider construction, notification, or downstream dispatch. Assert invalid signatures never reach company resolution/effects and normal signed non-demo events retain behavior.</action><verify><automated>npx vitest run tests/unit/demo/service-funnel-boundaries.test.ts</automated></verify><done>The service-funnel suite fails on any guard before signature verification or any trusted demo-company effect after verification.</done><acceptance_criteria><item>Tests cover both signed-machine and authenticated service callers without cookie-based authority.</item></acceptance_criteria></task>
<task type="tdd"><name>Task 2: GREEN — enforce trusted company denial in shared funnels</name><read_first>@tests/unit/demo/service-funnel-boundaries.test.ts @lib/demo/guard.ts @lib/queries/chat.ts @lib/chat/tools.ts @lib/notifications/customer-send.ts @lib/notifications/dispatch.ts @lib/integrations/xphere/dispatch.ts</read_first><files>lib/queries/chat.ts, lib/chat/tools.ts, lib/notifications/customer-send.ts, lib/notifications/dispatch.ts, lib/integrations/xphere/dispatch.ts</files><action>Add D-10 explicit company guards at the highest shared funnel with trusted company identity. Ensure signed webhook routes retain signature verification as their first authorization step; after that trusted resolution, the funnel denies demo company before service-role writes, provider notifications, Xphere calls, and queue/dispatch. Preserve invalid-signature responses, normal signed events, and read-only chat operations.</action><verify><automated>npx vitest run tests/unit/demo/service-funnel-boundaries.test.ts</automated></verify><done>All five domain-service funnels deny trusted demo company work before persistence/provider/dispatch effects while signed verification ordering remains intact.</done><acceptance_criteria><item>Focused test mocks prove no effect occurs after a trusted demo denial.</item></acceptance_criteria></task>
<task type="tdd"><name>Task 3: GREEN — wire the exact Stripe post-signature company funnel</name><read_first>@tests/unit/demo/service-funnel-boundaries.test.ts @app/api/webhooks/stripe/route.ts @lib/billing/connect-webhook.ts @lib/demo/guard.ts @tests/unit/billing/stripe-webhook.test.ts @tests/unit/webhooks/connect-events.test.ts</read_first><files>app/api/webhooks/stripe/route.ts, lib/billing/connect-webhook.ts, tests/unit/demo/service-funnel-boundaries.test.ts</files><action>Extend the focused suite and implementation for the exact `POST /api/webhooks/stripe` funnel. `stripe.webhooks.constructEvent` remains the first authorization operation. Only after successful signature verification, resolve company identity from trusted Stripe metadata, subscription/customer/account mapping, or referenced estimate/invoice row, then call D-10 `assertCompanyWritable` before inserting `processed_stripe_events`, calling `grantCredits`, mutating company/estimate/invoice rows, notifying, calling Stripe provider APIs, dispatching Xphere, or invoking `handleConnectEvent`. Ensure `lib/billing/connect-webhook.ts` accepts this already trusted guarded company context, or independently resolves and guards before its first write, so no direct/future caller bypasses the invariant. Invalid signatures perform no company lookup/effect; signed normal-company events retain existing behavior.</action><verify><automated>npx vitest run tests/unit/demo/service-funnel-boundaries.test.ts</automated></verify><done>Platform checkout/top-up/auto-top-up/subscription and connected-account estimate/invoice webhook paths prove signature-first, trusted-company resolution, and demo denial before every write, credit, notification, provider call, or dispatch.</done><acceptance_criteria><item>Mock ordering proves no idempotency row, grant, provider call, notification, or dispatch occurs for a verified demo-company event.</item></acceptance_criteria></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| signed webhook → domain service | Signature is verified first; company guard follows trusted resolution. |
| service funnel → provider/service role | RLS cannot protect these effects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-180-06 | Spoofing/Elevation | signed machine funnels | mitigate | signature-first and company-guard tests |
| T-180-05 | Tampering | provider dispatch | mitigate | guard-before-effect mocks |
| T-180-SC | Tampering | dependencies | accept | no package install |
</threat_model>
<verification>Run the focused service-funnel suite.</verification><success_criteria>SAFE-02 protects service-role and provider funnels while preserving signature verification and legitimate machine handling.</success_criteria><output>Create `.planning/phases/180-isolated-demo-session-read-only-foundation/180-11-SUMMARY.md` when done.</output>
