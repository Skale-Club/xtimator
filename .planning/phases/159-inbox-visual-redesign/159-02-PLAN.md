---
phase: 159-inbox-visual-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/inbox/settings/admin-whatsapp-accounts.tsx
  - components/admin/whatsapp-templates-panel.tsx
  - app/admin/inbox/settings/page.tsx
autonomous: true
requirements: [INBOX-08]

must_haves:
  truths:
    - "The /admin/inbox/settings page (Accounts + Templates tabs) visually reads as part of the same redesigned glass system as the main Inbox -- no leftover flat bg-muted-only surfaces that clash with the redesigned list/thread panes from Plan 01"
    - "The Accounts and Templates tables render inside glass-surfaced cards consistent with the proven app/admin/companies/page.tsx pattern"
  artifacts:
    - path: "app/admin/inbox/settings/admin-whatsapp-accounts.tsx"
      provides: "Company Config card, empty-state cards, and the Authorized Senders table all confirmed/polished to use Card variant=glass consistently, matching the Companies-page precedent"
    - path: "components/admin/whatsapp-templates-panel.tsx"
      provides: "Create-template form card and Templates table confirmed/polished to use Card variant=glass consistently"
    - path: "app/admin/inbox/settings/page.tsx"
      provides: "Page shell (heading, tabs) visually consistent with the glass system -- tab strip polished if flat"
  key_links:
    - from: "app/admin/inbox/settings/admin-whatsapp-accounts.tsx"
      to: "components/ui/card.tsx"
      via: "Card variant=glass wrapping tables/panels"
      pattern: "variant=\"glass\""
    - from: "components/admin/whatsapp-templates-panel.tsx"
      to: "components/ui/card.tsx"
      via: "Card variant=glass wrapping the create-template form and templates table"
      pattern: "variant=\"glass\""
---

<objective>
Bring the Inbox Settings sub-page (/admin/inbox/settings -- Accounts + Templates tabs) to full visual consistency with the redesigned main Inbox (Plan 01) and the proven Companies-page glass pattern, per INBOX-08.

Purpose: CONTEXT.md's research found this is "a much lighter lift" than the conversation-row redesign because both settings components (admin-whatsapp-accounts.tsx, whatsapp-templates-panel.tsx) were already built using `Card variant="glass"` in earlier phases (154-02, 104-03) -- this plan's job is to CONFIRM that treatment is complete and consistent everywhere on the page (not just the two big tables), and polish any remaining flat/plain spots (page shell, tab strip, hover states, empty states) so the whole settings surface reads as one coherent "Premium Xtimator" system alongside the redesigned Inbox from Plan 01.
Output: Verified + polished glass-surface treatment across app/admin/inbox/settings/page.tsx, admin-whatsapp-accounts.tsx, and whatsapp-templates-panel.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-inbox-visual-redesign/159-CONTEXT.md

<interfaces>
Current state of app/admin/inbox/settings/admin-whatsapp-accounts.tsx (already uses glass in
most places -- confirmed via direct read during planning):
- Company Config card: `<Card variant="glass" className="p-4">` (already glass)
- Empty-state cards ("No WhatsApp configuration...", "No authorized senders..."): `<Card variant="glass" className="p-4">` (already glass)
- Authorized Senders table: `<Card variant="glass" className="p-0 overflow-hidden">` wrapping a
  `<table>` with `thead className="bg-muted/30 ..."` and `tbody` rows using `hover:bg-muted/20`
  (already glass-wrapped -- the bg-muted/30 header and hover:bg-muted/20 rows are the SAME pattern
  used inside app/admin/companies/page.tsx's already-approved glass table, so these are NOT flat
  leftovers, they are the established inner-table convention within a glass Card)

Current state of components/admin/whatsapp-templates-panel.tsx (already uses glass in both places):
- Create-template form: `<Card variant="glass" className="p-6">`
- Templates table: `<Card variant="glass" className="p-0 overflow-hidden">` with the same
  `bg-muted/30` header / `hover:bg-muted/20` row inner-table convention as above

Current state of app/admin/inbox/settings/page.tsx (the page SHELL -- this is the part most
likely to still be flat, since it wraps the already-glass sub-components but its own heading/
tabs/back-link chrome has not been touched):
  <div className="space-y-8">
    <Link href="/admin/inbox" className="inline-flex items-center gap-1.5 text-sm text-muted-foreground hover:text-foreground">
      <ChevronLeft size={16} /> <T>Back to Inbox</T>
    </Link>
    <div className="space-y-2">
      <h1 className="text-[clamp(28px,3.5vw,40px)] font-semibold tracking-tight"><T>Inbox Settings</T></h1>
      <p className="text-muted-foreground"><T>Manage WhatsApp account provisioning and message templates.</T></p>
    </div>
    <Tabs defaultValue={initialTab} className="w-full gap-5">
      <div className="border-b border-border">
        <TabsList variant="line" className="w-auto h-auto bg-transparent p-0 gap-0 rounded-none justify-start">
          <TabsTrigger value="accounts" className={TAB_TRIGGER_CLASSES}><T>Accounts</T></TabsTrigger>
          <TabsTrigger value="templates" className={TAB_TRIGGER_CLASSES}><T>Templates</T></TabsTrigger>
        </TabsList>
      </div>
      <TabsContent value="accounts" className="mt-0"><AdminWhatsAppAccounts ... /></TabsContent>
      <TabsContent value="templates" className="mt-0"><WhatsAppTemplatesPanel ... /></TabsContent>
    </Tabs>
  </div>

From app/admin/companies/page.tsx lines 138, 193 (the proven reference pattern both settings
components already followed):
  <Card variant="glass" className="p-0 overflow-hidden">
    <table className="w-full text-sm">
      <thead className="bg-muted/30 text-xs uppercase tracking-wide text-muted-foreground">...</thead>
      <tbody className="divide-y divide-border">
        <tr className="hover:bg-muted/20">...</tr>
      </tbody>
    </table>
  </Card>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and polish glass consistency across the Accounts and Templates sub-components</name>
  <read_first>
    app/admin/inbox/settings/admin-whatsapp-accounts.tsx (the file you are auditing/polishing -- read the FULL file; it already uses Card variant="glass" in 3 places, confirm nothing was missed)
    components/admin/whatsapp-templates-panel.tsx (same audit -- already uses Card variant="glass" in 2 places)
    app/admin/companies/page.tsx lines 138-176, 193-256 (the reference pattern both files already mirror -- use this to judge whether anything in the two settings files deviates)
    components/ui/card.tsx (glass CVA variant definition, for reference)
  </read_first>
  <files>app/admin/inbox/settings/admin-whatsapp-accounts.tsx, components/admin/whatsapp-templates-panel.tsx</files>
  <action>
    This is primarily a VERIFICATION task with targeted polish, not a rewrite -- both files already
    use `Card variant="glass"` extensively (confirmed via direct source read during planning: 3
    occurrences in admin-whatsapp-accounts.tsx, 2 in whatsapp-templates-panel.tsx). Do the following:

    1. Run a grep across both files for Card usage and top-level div wrappers
       (pattern: "Card variant" and "className=\"rounded" and "className=\"space-y")
       to confirm every top-level section (Company Config card, empty-state cards, Authorized
       Senders table, Create-template form, Templates table) is wrapped in Card variant="glass"
       or variant="glass-strong". If you find ANY top-level container div using a plain
       border/bg-card/rounded-lg-border pattern (Card's default variant) instead of glass,
       change that specific Card's variant prop to "glass" -- do not touch any Card that
       is already glass.

    2. Confirm the inner <table> header/row classes (bg-muted/30 header, hover:bg-muted/20
       rows, divide-y divide-border body) match app/admin/companies/page.tsx's proven convention
       exactly -- these are the established INNER-table styling inside a glass Card, not a flat
       leftover, so leave them unchanged if they already match. Only change them if you find a
       literal deviation (e.g. a different opacity value like bg-muted/50 instead of bg-muted/30)
       -- normalize to the Companies-page values for consistency.

    3. In admin-whatsapp-accounts.tsx, the companyConfig.review_reason warning text currently
       renders as plain text-amber-600 (no card wrapper) -- leave this as-is; it is a small
       inline annotation inside an already-glass Card, not a separate surface needing its own
       glass treatment.

    4. Do NOT modify: maskPhone, statusBadgeVariant, any server action imports/calls (saveWhatsAppSender,
       setWhatsAppSenderStatus, removeWhatsAppSender, createTemplate, submitTemplateToMeta), any
       Dialog/AlertDialog content, or component props/state. This task is visual-container-only.
  </action>
  <acceptance_criteria>
    - grep -c "variant=\"glass\"" app/admin/inbox/settings/admin-whatsapp-accounts.tsx returns at least 3 (unchanged or increased from the pre-existing 3, never decreased)
    - grep -c "variant=\"glass\"" components/admin/whatsapp-templates-panel.tsx returns at least 2 (unchanged or increased from the pre-existing 2, never decreased)
    - grep -n "variant=\"default\"" app/admin/inbox/settings/admin-whatsapp-accounts.tsx components/admin/whatsapp-templates-panel.tsx confirms no leftover flat/default-variant top-level Card was found (or if found, was fixed to glass in this task -- re-run after edits to confirm 0 matches)
    - grep -c "saveWhatsAppSender\|setWhatsAppSenderStatus\|removeWhatsAppSender" app/admin/inbox/settings/admin-whatsapp-accounts.tsx matches the pre-task count exactly (no functional code touched)
    - grep -c "createTemplate\|submitTemplateToMeta" components/admin/whatsapp-templates-panel.tsx matches the pre-task count exactly (no functional code touched)
    - npx playwright test tests/e2e/admin-whatsapp.spec.ts --grep "admin-whatsapp-accounts" (the account-provisioning static contract tests) still passes -- confirms maskPhone, no-reply-controls, no-secrets, and action-import assertions are unbroken
  </acceptance_criteria>
  <verify>
    <automated>grep -c "variant=\"glass\"" app/admin/inbox/settings/admin-whatsapp-accounts.tsx components/admin/whatsapp-templates-panel.tsx</automated>
  </verify>
  <done>Every top-level card/table container in both settings sub-components uses Card variant="glass" (or glass-strong), with zero leftover flat/default-variant surfaces; all existing functional code (server action calls, masking, status logic) is byte-for-byte unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Polish the Settings page shell (tab strip) for glass consistency</name>
  <read_first>
    app/admin/inbox/settings/page.tsx (the file you are modifying -- read the FULL file; this is a Server Component, no use-client directive, so only static JSX/className changes are safe here)
    components/ui/tabs.tsx (TabsList variant="line" styling -- confirm what "line" variant currently renders so you do not fight its existing look)
    app/admin/inbox/admin-whatsapp-client.tsx (after Plan 01's edits -- read the redesigned list-pane header treatment so this settings page shell's tab-strip area feels like the same family, without duplicating exact classes verbatim)
  </read_first>
  <files>app/admin/inbox/settings/page.tsx</files>
  <action>
    This page currently has NO glass treatment on its own shell chrome (only the sub-components
    it renders are already glass, per Task 1). Apply light, page-shell-appropriate polish:

    1. The tab strip currently sits in a plain div with className="border-b border-border". Change
       this wrapper to also carry a subtle glass tint so the tab strip reads as part of the same
       redesigned surface family. Replace:
         <div className="border-b border-border">
       with:
         <div className="border-b border-[var(--glass-border)] bg-[var(--glass-bg-light)]">
       (Claude's Discretion: if this creates a visible edge/box artifact against the page's outer
       padding once rendered, add matching negative-margin/padding classes so the tint bleeds to
       the content edge, e.g. "-mx-6 px-6 md:mx-0 md:px-0" -- inspect the parent layout's existing
       horizontal padding first before adding any offset; only add it if a visual seam is evident.)

    2. Leave the <h1>, <p> description, and <Link> back-to-Inbox affordance as plain text --
       per the established pattern on app/admin/companies/page.tsx and app/admin/page.tsx,
       page headings and back-links are NOT glass-wrapped anywhere else in this admin panel;
       adding glass to the heading itself would be inconsistent with that established convention,
       not an improvement. Do not wrap the <h1>/description block in a Card.

    3. Do NOT change initialTab, filters, svc queries, Promise.all, or any data-fetching
       logic -- this is a Server Component with real DB reads via requireServiceClient(); this
       task touches ONLY the returned JSX's className value on the tab-strip wrapper div.
  </action>
  <acceptance_criteria>
    - grep -n "glass-bg-light\|glass-border" app/admin/inbox/settings/page.tsx confirms the tab-strip wrapper now references glass tokens
    - grep -c "requireAdmin\|requireServiceClient\|parseAdminWhatsAppFilters\|listTemplates" app/admin/inbox/settings/page.tsx matches the pre-task count exactly (no data-fetching logic touched)
    - git diff app/admin/inbox/settings/page.tsx shows changes confined to the tab-strip wrapper div's className only
    - Manual visual check (defer to UI-checker-style review, not automatable): open /admin/inbox/settings in a dev server, confirm the tab strip has a subtle glass tint consistent with the rest of the redesigned Inbox, and confirm the Accounts/Templates tables underneath still render inside visibly glass-surfaced cards
  </acceptance_criteria>
  <verify>
    <automated>grep -c "requireAdmin\|requireServiceClient\|parseAdminWhatsAppFilters\|listTemplates" app/admin/inbox/settings/page.tsx</automated>
  </verify>
  <done>The Settings page's tab-strip wrapper carries a subtle glass-token background/border consistent with the redesigned Inbox; the heading/back-link area remains plain per the established app-wide convention; zero data-fetching or server logic was touched.</done>
</task>

</tasks>

<verification>
Run the following checks before considering this plan complete:

1. Glass coverage confirmed across all three files:
   grep -c "variant=\"glass\"" app/admin/inbox/settings/admin-whatsapp-accounts.tsx components/admin/whatsapp-templates-panel.tsx
   grep -n "glass-bg-light\|glass-border" app/admin/inbox/settings/page.tsx
   Expected: glass Card count >= pre-existing baseline (3 + 2) in the sub-components, and at least
   1 glass-token reference in the page shell.

2. No functional regressions -- account/template provisioning static contracts still pass:
   npx playwright test tests/e2e/admin-whatsapp.spec.ts --grep "static contract"
   Expected: all passing, including the account-provisioning-actions-import, phone-masking,
   no-reply-controls, and no-Meta-tokens assertions.

3. TypeScript compiles cleanly:
   npx tsc --noEmit
</verification>

<success_criteria>
This plan is complete when:
- Both admin-whatsapp-accounts.tsx and whatsapp-templates-panel.tsx have zero remaining flat/
  default-variant top-level containers -- every card-like surface uses Card variant="glass"
- The Settings page shell's tab strip carries a glass-token tint consistent with the redesigned
  main Inbox from Plan 01
- Zero server-action, data-fetching, masking, or status-logic code was modified in any of the
  three files
- The v4.16-era static-contract e2e tests for the Settings sub-page components still pass
</success_criteria>

<output>
After completion, create .planning/phases/159-inbox-visual-redesign/159-02-SUMMARY.md
documenting: what (if anything) was found to be a flat leftover vs. already-compliant glass
usage, and confirmation that the account/template provisioning static-contract tests passed
unchanged.
</output>
