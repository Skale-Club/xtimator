---
phase: 139-seat-billing
plan: 03
type: execute
wave: 3
depends_on: ["139-02"]
files_modified:
  - lib/actions/invite-accept.ts
  - lib/actions/team.ts
  - tests/unit/billing/seat-billing-wiring.test.ts
autonomous: true
requirements: [SEAT-07]
must_haves:
  truths:
    - "After acceptInvite succeeds, syncSeatBilling is invoked for the joined company"
    - "After removeMember succeeds, syncSeatBilling is invoked for the company"
    - "After changeMemberRole succeeds, syncSeatBilling is invoked for the company"
    - "A syncSeatBilling failure NEVER rolls back or fails the membership change (the action still returns success)"
  artifacts:
    - path: "lib/actions/invite-accept.ts"
      provides: "syncSeatBilling(invite.company_id) after the membership insert + active-company switch"
      contains: "syncSeatBilling"
    - path: "lib/actions/team.ts"
      provides: "syncSeatBilling(companyId) after removeMember + changeMemberRole succeed"
      contains: "syncSeatBilling"
    - path: "tests/unit/billing/seat-billing-wiring.test.ts"
      provides: "asserts each action calls syncSeatBilling on success + tolerates its failure"
  key_links:
    - from: "lib/actions/invite-accept.ts acceptInvite"
      to: "syncSeatBilling"
      via: "guarded call after switchActiveCompany, before return success"
      pattern: "syncSeatBilling"
    - from: "lib/actions/team.ts removeMember + changeMemberRole"
      to: "syncSeatBilling"
      via: "guarded call after the successful delete/update, before return success"
      pattern: "syncSeatBilling"
---

<objective>
SEAT-07 (wiring) — invoke the never-throw `syncSeatBilling(companyId)` as a side-effect of
every membership change so the Stripe seat quantity stays in sync. Wire it into the three
membership mutations that change the active member count: `acceptInvite` (137), `removeMember`
and `changeMemberRole` (138). The call is guarded so a billing-side failure can never roll
back or fail the membership operation.

Purpose: close the SEAT-07 loop — membership state changes now reconcile the configurable,
gated seat charge, without ever risking the underlying team operation.
Output: three wired call sites + a wiring test proving each fires syncSeatBilling on success and tolerates its failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/actions/invite-accept.ts
@lib/actions/team.ts
@lib/billing/seat-billing.ts

<interfaces>
<!-- syncSeatBilling (from Plan 02) — server-side, never-throw, gated by enforcementEnabled: -->
```typescript
export async function syncSeatBilling(companyId: string): Promise<void>
```
<!-- acceptInvite(token): derives company from invite.company_id (NO companyId param). The success
     path ends after `await switchActiveCompany(invite.company_id)` then `return { success: true }`.
     Wire syncSeatBilling(invite.company_id as string) right before the success return. -->
<!-- removeMember(companyId, userId) + changeMemberRole(companyId, userId, role): the success path
     ends after the delete/update succeeds + revalidatePath(TEAM_PATH) then `return { success: true }`.
     Wire syncSeatBilling(companyId) right before each success return. -->
<!-- syncSeatBilling is ALREADY never-throw (try/catch + console.warn inside). The call site should
     STILL be defensive: `await syncSeatBilling(...).catch(() => {})` OR fire-and-forget `void` so a
     latency/throw could never affect the action result. Prefer an awaited-but-guarded
     `try { await syncSeatBilling(id) } catch { /* never fail the membership op */ }` so the test can
     assert it was invoked deterministically. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire syncSeatBilling into acceptInvite, removeMember, changeMemberRole</name>
  <read_first>lib/actions/invite-accept.ts (the success path: steps 7-8, the upsert + switchActiveCompany + `return { success: true }`) and lib/actions/team.ts (removeMember success after the delete + revalidatePath; changeMemberRole success after the update + revalidatePath)</read_first>
  <files>lib/actions/invite-accept.ts, lib/actions/team.ts</files>
  <action>
    1. Import syncSeatBilling: `import { syncSeatBilling } from '@/lib/billing/seat-billing'` in BOTH files.
    2. invite-accept.ts: after `await switchActiveCompany(invite.company_id as string)` and BEFORE `return { success: true as const }`, add a guarded call:
       `try { await syncSeatBilling(invite.company_id as string) } catch { /* seat sync must never fail the join */ }`
       (syncSeatBilling is itself never-throw; the extra try/catch is belt-and-suspenders so a future change can't make the join fail.)
    3. team.ts removeMember: after the successful delete + `revalidatePath(TEAM_PATH)` and BEFORE `return { success: true as const }`, add:
       `try { await syncSeatBilling(companyId) } catch { /* seat sync must never fail removal */ }`
    4. team.ts changeMemberRole: after the successful update + `revalidatePath(TEAM_PATH)` and BEFORE `return { success: true as const }`, add:
       `try { await syncSeatBilling(companyId) } catch { /* seat sync must never fail role change */ }`
    5. Do NOT add syncSeatBilling to the EARLY-RETURN error branches (not-a-manager, member-not-found, last-owner, delete/update error) — only on the SUCCESS path where the member count or billable status actually changed. (changeMemberRole on success is wired even though a member↔admin flip rarely changes billable count — a role change that flips billable status must re-sync, and the no-op idempotency in syncSeatBilling makes the redundant call cheap/safe.)
    6. Keep every existing guard, gate, and return shape byte-identical; only ADD the three guarded calls.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "invite-accept.ts|actions/team.ts" || echo "no wiring type errors"</automated>
  </verify>
  <done>
    `grep -c "syncSeatBilling" lib/actions/invite-accept.ts` >= 2 (import + call);
    `grep -c "syncSeatBilling" lib/actions/team.ts` >= 3 (import + 2 calls);
    each call sits on the SUCCESS path before the success return, inside a try/catch; no error-branch is altered.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wiring tests — each action fires syncSeatBilling on success + tolerates its failure</name>
  <behavior>
    - acceptInvite(validToken) success → syncSeatBilling called once with the invite's company_id.
    - removeMember(companyId, userId) success → syncSeatBilling called once with companyId.
    - changeMemberRole(companyId, userId, 'member') success → syncSeatBilling called once with companyId.
    - When syncSeatBilling REJECTS (mock throws), each action STILL returns { success: true } (the membership op is not rolled back / not failed).
    - A FAILED membership op (e.g. removeMember on a missing member → { error }) does NOT call syncSeatBilling.
  </behavior>
  <read_first>existing tests for these actions (grep tests/unit for acceptInvite / removeMember / changeMemberRole to reuse their service-client + switchActiveCompany + requireCompanyManager mocks) and tests/unit/billing/seat-billing.test.ts (the vi.fn() mock idiom for syncSeatBilling)</read_first>
  <files>tests/unit/billing/seat-billing-wiring.test.ts</files>
  <action>
    1. Create tests/unit/billing/seat-billing-wiring.test.ts. Mock `@/lib/billing/seat-billing` with `syncSeatBilling: vi.fn()` (default resolve; per-test reject for the tolerance case). Reuse the mock posture from the existing acceptInvite/removeMember/changeMemberRole tests for `@/lib/supabase/service`, `@/lib/supabase/server` (auth claims), `@/lib/auth/require-company-role` (requireCompanyManager), and `@/lib/actions/active-company` (switchActiveCompany).
    2. acceptInvite cases: a valid/pending/unexpired/email-matching invite → assert `syncSeatBilling` called with the invite company_id AND result is { success: true }. Then a variant where syncSeatBilling rejects → still { success: true }.
    3. removeMember cases: a non-owner target deleted → assert syncSeatBilling called with companyId + success; a missing target ({ error: 'Member not found.' }) → assert syncSeatBilling NOT called; a reject variant → still { success: true }.
    4. changeMemberRole cases: a non-owner target updated to 'member' → assert syncSeatBilling called with companyId + success; a reject variant → still { success: true }.
    5. Keep mocks minimal but faithful to the real success/error branches so the assertions are meaningful.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/seat-billing-wiring.test.ts</automated>
  </verify>
  <done>
    All wiring cases pass: each of the three actions calls syncSeatBilling with the correct company id on success, does NOT call it on the failure branch, and returns success even when syncSeatBilling rejects.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/billing/seat-billing-wiring.test.ts` → green.
- `npx vitest run tests/unit/billing` and the existing team/invite-accept suites → no regressions (the actions' existing tests stay green — only additive guarded calls).
- `npx tsc --noEmit` → no NEW errors in invite-accept.ts / team.ts.
- `grep -c "syncSeatBilling" lib/actions/invite-accept.ts lib/actions/team.ts` → invite-accept >= 2, team >= 3.
- No secrets touched.
</verification>

<success_criteria>
- syncSeatBilling is invoked on the success path of acceptInvite, removeMember, and changeMemberRole.
- A syncSeatBilling failure never fails or rolls back the membership operation (tested with a rejecting mock).
- Failure branches of the membership actions do NOT trigger a seat sync.
- Existing membership-action tests + the full billing suite stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/139-seat-billing/139-03-SUMMARY.md`.
</output>
