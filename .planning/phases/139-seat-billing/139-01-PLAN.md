---
phase: 139-seat-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/billing/billing-config.ts
  - lib/schemas/admin.ts
  - app/admin/integrations/billing-config-form.tsx
  - tests/unit/billing/billing-config.test.ts
  - tests/unit/billing/seat-config-no-hardcode.test.ts
autonomous: true
requirements: [SEAT-06]
must_haves:
  truths:
    - "getBillingConfig() resolves seatPriceCents (global) and tiers[tier].includedSeats (per-tier) with calibration-placeholder defaults"
    - "A billing_config row written BEFORE these fields existed still deep-merges to the placeholder defaults (Pitfall-6 tolerance)"
    - "A super-admin can edit seatPriceCents + each tier's includedSeats in the billing panel and save"
    - "No seat price / included-seat count is hardcoded outside billing-config.ts (static test)"
  artifacts:
    - path: "lib/billing/billing-config.ts"
      provides: "seatPriceCents on BillingConfig + includedSeats on TierBilling + DEFAULT placeholders + deep-merge"
      contains: "seatPriceCents"
    - path: "lib/schemas/admin.ts"
      provides: "billingConfigSchema accepts seatPriceCents + tiers[tier].includedSeats"
      contains: "seatPriceCents"
    - path: "app/admin/integrations/billing-config-form.tsx"
      provides: "editable seat-price + per-tier included-seats fields"
      contains: "seatPriceCents"
    - path: "tests/unit/billing/seat-config-no-hardcode.test.ts"
      provides: "static assertion no seat price/included-count hardcoded outside billing-config.ts"
  key_links:
    - from: "billing-config-form.tsx"
      to: "saveBillingConfig action -> billingConfigSchema"
      via: "payload includes seatPriceCents + tiers[tier].includedSeats"
      pattern: "seatPriceCents"
    - from: "lib/schemas/admin.ts tierBillingSchema"
      to: "BillingConfig TierBilling type"
      via: "includedSeats field on both"
      pattern: "includedSeats"
---

<objective>
SEAT-06 — make the per-seat price and per-tier included-seat counts FULLY CONFIGURABLE
in the super-admin billing panel, with ZERO hardcoded billing numbers. Extend
`BillingConfig`/`DEFAULT_BILLING_CONFIG` with `seatPriceCents` (global) and
`tiers[tier].includedSeats` (per-tier) as null-safe calibration placeholders, mirror
the shape in the admin zod schema, surface both as editable fields in the billing form,
and lock the no-hardcode invariant with a static test.

Purpose: every seat-billing number lives in ONE place (billing_config), read at runtime
via getBillingConfig(), changeable in the panel without a deploy (30s TTL flush) — exactly
the discipline markup/estimateFeePct already follow.
Output: extended config type + defaults + schema + admin form fields + a no-hardcode static test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/billing/billing-config.ts
@lib/schemas/admin.ts
@app/admin/integrations/billing-config-form.tsx
@tests/unit/billing/billing-config.test.ts

<interfaces>
<!-- Current TierBilling + BillingConfig (lib/billing/billing-config.ts). EXTEND these,
     do not rewrite. seatPriceCents is GLOBAL; includedSeats is PER-TIER inside TierBilling. -->
```typescript
export type TierBilling = { monthlyCreditGrant: number; subscriptionPriceCents: number }
export type BillingConfig = {
  markup: number
  // ...existing fields...
  estimateFeePct: number
  estimateFeeMinCents: number
  tiers: Record<BillingTier, TierBilling>
  // ...
  enforcementEnabled: boolean
}
```
<!-- The deep-merge in getBillingConfig ALREADY spreads tiers:
     tiers: { ...DEFAULT.tiers, ...(stored.tiers ?? {}) } — a stored tier object REPLACES
     the default tier object wholesale (it is NOT a per-field deep merge of each tier).
     A row written before includedSeats existed has NO tiers key at all → falls through to
     DEFAULT.tiers (which HAS includedSeats) → resolves correctly. CONFIRM this in a test;
     do NOT change the merge depth (a stored tier that includes monthlyCreditGrant/price but
     omits includedSeats is only produced by the new admin form, which always sends it). -->

<!-- admin zod (lib/schemas/admin.ts): MONEY = integer cents; counts = non-negative ints. -->
```typescript
const tierBillingSchema = z.object({
  monthlyCreditGrant: z.number().int().min(0),
  subscriptionPriceCents: z.number().int().min(0),
})
export const billingConfigSchema = z.object({ markup: ..., tiers: z.object({ free, trial, pro, business }), enforcementEnabled: z.boolean() })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BillingConfig + DEFAULT with seatPriceCents + per-tier includedSeats (placeholders)</name>
  <read_first>lib/billing/billing-config.ts (whole file — the TierBilling type, BillingConfig type, DEFAULT_BILLING_CONFIG, and the getBillingConfig deep-merge block lines 110-114)</read_first>
  <files>lib/billing/billing-config.ts</files>
  <action>
    1. Add `includedSeats: number` to the `TierBilling` type:
       `export type TierBilling = { monthlyCreditGrant: number; subscriptionPriceCents: number; includedSeats: number }`
       Add an inline comment: seats bundled in the tier before per-seat billing kicks in (e.g. the owner seat is included) — CALIBRATE BEFORE CHARGING.
    2. Add `seatPriceCents: number` to the `BillingConfig` type (global monthly price of one billable seat).
       Comment it like estimateFeePct: "// monthly price of one billable seat in integer cents (SEED-037); CALIBRATE BEFORE CHARGING placeholder".
    3. In `DEFAULT_BILLING_CONFIG`:
       - Add `seatPriceCents: 1500` (a NULL-SAFE PLACEHOLDER — comment "CALIBRATE BEFORE CHARGING, NOT a final number", same discipline as the existing markup/estimateFeePct placeholders).
       - Add `includedSeats` to EACH of the four tiers (free/trial/pro/business). Intent: the owner seat is included, so default each to `1` EXCEPT keep them as explicit placeholders — set free: 1, trial: 1, pro: 1, business: 1 with a comment that these are calibration placeholders (the owner seat is bundled). Do NOT invent generous numbers — placeholders only.
    4. Do NOT change the deep-merge block (lines ~110-114). The existing `tiers: { ...DEFAULT.tiers, ...(stored.tiers ?? {}) }` already resolves includedSeats for a pre-existing row (no tiers key → DEFAULT.tiers used). Leave it byte-identical.
    5. Keep `import 'server-only'` and `createServiceClient` usage untouched (the BILLCFG-03 static guards in billing-config.test.ts assert them).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "billing-config.ts" || echo "no billing-config.ts type errors"</automated>
  </verify>
  <done>
    `grep -c "seatPriceCents" lib/billing/billing-config.ts` >= 2 (type + default);
    `grep -c "includedSeats" lib/billing/billing-config.ts` >= 5 (TierBilling type + 4 tiers);
    DEFAULT_BILLING_CONFIG.seatPriceCents is a number; each tier has includedSeats: number;
    deep-merge block unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror seatPriceCents + tiers[tier].includedSeats in billingConfigSchema</name>
  <read_first>lib/schemas/admin.ts lines 141-170 (tierBillingSchema + billingConfigSchema)</read_first>
  <files>lib/schemas/admin.ts</files>
  <action>
    1. Add `includedSeats: z.number().int().min(0)` to `tierBillingSchema` (counts are non-negative ints — mirror monthlyCreditGrant's `.int().min(0)`).
    2. Add `seatPriceCents: z.number().int().min(0)` to `billingConfigSchema` (integer cents, mirror estimateFeeMinCents which is `.int().min(0)`). Do NOT add an upper bound that would reject a sane price.
    3. Keep the field order coherent (put seatPriceCents near estimateFeeMinCents; includedSeats after subscriptionPriceCents). The schema must still `safeParse(DEFAULT_BILLING_CONFIG)` true — that round-trip is asserted in billing-config.test.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/billing-config.test.ts -t "accepts DEFAULT_BILLING_CONFIG round-tripped"</automated>
  </verify>
  <done>
    `grep -c "seatPriceCents" lib/schemas/admin.ts` >= 1 and `grep -c "includedSeats" lib/schemas/admin.ts` >= 1;
    billingConfigSchema.safeParse(DEFAULT_BILLING_CONFIG).success stays true (existing round-trip test green).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add editable seat-price + per-tier included-seats fields to the billing-config admin form</name>
  <read_first>app/admin/integrations/billing-config-form.tsx (whole file — the useState block, updateTier helper, the payload assembly in handleSave, the "Per-tier grants & prices" fieldset, and the "Estimate fee" fieldset for the field idiom)</read_first>
  <files>app/admin/integrations/billing-config-form.tsx</files>
  <action>
    1. Add a top-level state for the global seat price: `const [seatPriceCents, setSeatPriceCents] = useState(String(current.seatPriceCents))`.
    2. Extend the per-tier state shape: the `tiers` useState reducer currently stores `{ monthlyCreditGrant, subscriptionPriceCents }` per tier — add `includedSeats: String(current.tiers[tier].includedSeats)`. Widen the `updateTier` field union to include `'includedSeats'`.
    3. In `handleSave`'s `payload`:
       - Add `seatPriceCents: Number(seatPriceCents)` at the top level.
       - In the tiers reducer, add `includedSeats: Number(tiers[tier].includedSeats)` to each tier object.
       The payload is typed `BillingConfig`, so TS will FAIL the build until both fields are present — this is the wiring guarantee.
    4. UI: add a new field for `seatPriceCents` — reuse the "Estimate fee" fieldset idiom (a `<fieldset className={fieldsetClass}>` with a `<legend>` like `{t('Seat billing')}` and a numeric input, step="1", label `{t('Per-seat price (cents)')}`, disabled={isPending}, className={inputClass}, value={seatPriceCents}, onChange setSeatPriceCents).
    5. In the existing "Per-tier grants & prices" fieldset, add a THIRD per-tier input for includedSeats (the grid is `sm:grid-cols-3`; either widen to a fourth column / wrap, or place includedSeats as an additional labeled input in the row). Label `{t('Included seats')}`, step="1", value={tiers[tier].includedSeats}, onChange={(e) => updateTier(tier, 'includedSeats', e.target.value)}. Keep it mobile-safe (stacks on small screens like the rest of the form).
    6. Do NOT touch the carried-through meteredOperations/absorbedChatRateLimitPerMin handling.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "billing-config-form.tsx" || echo "no form type errors"</automated>
  </verify>
  <done>
    `grep -c "seatPriceCents" app/admin/integrations/billing-config-form.tsx` >= 3 (state + payload + input);
    `grep -c "includedSeats" app/admin/integrations/billing-config-form.tsx` >= 3 (state + payload + input);
    payload object literal is typed BillingConfig and compiles (proves both new fields are present).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Deep-merge + no-hardcode static tests</name>
  <behavior>
    - getBillingConfig() with NO stored row → cfg.seatPriceCents === DEFAULT_BILLING_CONFIG.seatPriceCents and cfg.tiers.pro.includedSeats === DEFAULT pro includedSeats.
    - A stored row WITHOUT a tiers key (pre-existing row: `{ markup: 5 }`) still resolves cfg.tiers.pro.includedSeats to the DEFAULT (Pitfall-6 tolerance).
    - A stored row that OVERRIDES seatPriceCents (`{ seatPriceCents: 2500 }`) resolves cfg.seatPriceCents === 2500, rest default.
    - billingConfigSchema.safeParse accepts a config with seatPriceCents + per-tier includedSeats; rejects a NEGATIVE seatPriceCents and a non-integer includedSeats.
    - Static no-hardcode: scanning lib/billing + lib/actions/team.ts + lib/actions/invite-accept.ts (and any future seat-billing source) for a literal seat price / included-seat NUMBER — the ONLY file allowed to contain the seat-price/included-seat NUMERIC LITERALS is lib/billing/billing-config.ts. The test asserts no other seat-billing source hardcodes them (it greps the seat-billing modules for the forbidden numeric-literal pattern, NOT the config reader).
  </behavior>
  <read_first>tests/unit/billing/billing-config.test.ts (the existing service-client mock makeServiceClient + the BILLCFG-01 merge tests — extend the SAME file for the deep-merge cases) and tests/unit/agent-tools/neutrality.test.ts (the readFileSync/readdirSync static-walk idiom for the no-hardcode test)</read_first>
  <files>tests/unit/billing/billing-config.test.ts, tests/unit/billing/seat-config-no-hardcode.test.ts</files>
  <action>
    1. ADD to the EXISTING tests/unit/billing/billing-config.test.ts (do not create a new merge file — reuse makeServiceClient + invalidateBillingConfigCache):
       - a describe 'SEAT-06: seat config deep-merge' with the four behavior cases above (default resolution, pre-existing row tolerance via `{ markup: 5 }`, seatPriceCents override, and a stored row with a partial tiers object still resolving the non-overridden tiers' includedSeats from DEFAULT).
       - extend the existing 'BILLCFG-02: billingConfigSchema' describe with: accepts DEFAULT (already there — confirm still green), rejects `{ ...DEFAULT, seatPriceCents: -1 }`, rejects a tier with `includedSeats: 1.5`.
    2. CREATE tests/unit/billing/seat-config-no-hardcode.test.ts — a PURE static-source test (readFileSync, no DB/mocks/secrets, modeled on neutrality.test.ts):
       - read lib/billing/seat-billing.ts (if present — guard for its absence so this test passes in Wave 1 before Plan 02 exists, OR scan only files that exist), lib/actions/team.ts, lib/actions/invite-accept.ts.
       - assert NONE of them contain the placeholder seat-price literal (e.g. `/\b1500\b/` only as a hardcoded seat price is too broad — instead assert these files do NOT reference `seatPriceCents:` as an OBJECT-LITERAL ASSIGNMENT or a numeric included-seat constant; the robust invariant: every seat number must come from `getBillingConfig()`/the passed config, so assert these seat-billing source files contain NO line matching `seatPriceCents\s*=\s*\d` and NO `includedSeats\s*=\s*\d` and NO `includedSeats:\s*\d` outside an imported config). Keep the assertion SPECIFIC and grep-stable: the forbidden pattern is a NUMERIC literal directly assigned to a seat field anywhere except lib/billing/billing-config.ts.
       - assert POSITIVELY that lib/billing/billing-config.ts DOES contain `seatPriceCents` and `includedSeats` (the one legal home).
    3. The no-hardcode test must be GREEN in Wave 1 (only billing-config.ts has the numbers) and STAY GREEN after Plan 02/03 add seat-billing.ts + the wiring (which read config, never hardcode).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/billing-config.test.ts tests/unit/billing/seat-config-no-hardcode.test.ts</automated>
  </verify>
  <done>
    Both test files pass; the deep-merge cases prove pre-existing-row tolerance for includedSeats;
    the schema rejects negative seatPriceCents + non-integer includedSeats;
    the static test asserts no seat number is hardcoded outside billing-config.ts.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/billing/billing-config.test.ts tests/unit/billing/seat-config-no-hardcode.test.ts` → all green.
- `npx tsc --noEmit` → no NEW errors in billing-config.ts, admin.ts, billing-config-form.tsx.
- `grep -c "seatPriceCents" lib/billing/billing-config.ts lib/schemas/admin.ts app/admin/integrations/billing-config-form.tsx` → each >= 1.
- The deep-merge block in getBillingConfig is byte-unchanged.
- No secrets (no Stripe keys touched this plan).
</verification>

<success_criteria>
- BillingConfig + DEFAULT_BILLING_CONFIG carry seatPriceCents (global) + tiers[tier].includedSeats (per-tier) as documented calibration placeholders.
- getBillingConfig() resolves both for a pre-existing row (deep-merge tolerance, tested).
- billingConfigSchema accepts them and rejects invalid values; the admin form edits + saves them.
- A static test asserts no seat price / included-seat number is hardcoded outside billing-config.ts.
- Existing billing-config tests stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/139-seat-billing/139-01-SUMMARY.md`.
</output>
