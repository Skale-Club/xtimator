---
phase: 69-uat-validation-bug-triage-perf-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/known-issues.md
autonomous: false
checkpoint: human-action
requirements:
  - UAT-V22-01
  - UAT-V22-02
  - UAT-V30-01
  - UAT-V30-02
  - UAT-V30-03
  - UAT-V30-04
  - UAT-V30-05
  - UAT-V30-06

must_haves:
  truths:
    - "PDF attachment delivery exercised end-to-end against localhost; verdict in known-issues.md"
    - "WhatsApp status flow exercised; verdict in known-issues.md"
    - "Tier enforcement validated for free + pro + business tiers; verdict in known-issues.md"
    - "Stripe Checkout (test mode) flow completes signup→pay→tier-update; verdict in known-issues.md"
    - "Trial automation observable end-to-end; verdict in known-issues.md"
    - "Stripe Customer Portal works; verdict in known-issues.md"
    - "Admin tooling (force-tier, bonus credits, MRR view) works; verdict in known-issues.md"
    - "402 upgrade modal triggers correctly on quota-exceeded AI route; verdict in known-issues.md"
---

<objective>
Manual UAT pass for v2.2 (WhatsApp polish) + v3.0 (Monetization). User exercises each feature against `npm run dev` localhost; orchestrator captures verdicts in `.planning/known-issues.md`.

Goal: catch bugs that automated tests can't (real Stripe Checkout interaction, real WhatsApp message, real cron timing, etc.) before considering the codebase "production-ready" for v3.2 deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/known-issues.md (where verdicts go)
@.planning/STATE.md
@.planning/phases/61-production-database-foundation/61-prod-rls-snapshot.txt (DB ready)
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: UAT-V22 — WhatsApp Channel Polish (PDF + Status Flow)</name>
  <what-built>v2.2 phases shipped PDF attachment delivery (Phase 53) + WhatsApp status flow with suspend/reactivate (Phase 54). Both need real device validation.</what-built>
  <action>
    **Setup:**
    1. Start Next.js: `npm run dev` (terminal 1)
    2. Start Inngest dev: `npm run dev:inngest` (terminal 2)
    3. Start Stripe webhook listener: `stripe listen --forward-to localhost:3000/api/webhooks/stripe` (terminal 3)
    4. Login at `http://localhost:3000`

    **UAT-V22-01 — PDF Attachment Delivery:**
    1. Navigate to `/settings/integrations` → connect WhatsApp (Meta token from admin panel)
    2. Open WhatsApp connect card → set `delivery_format=pdf_attachment`
    3. Create a test project with audio capture
    4. Generate estimate
    5. Send via WhatsApp to your own real phone
    6. **Verify:** real client phone receives PDF attachment + follow-up share link

    **UAT-V22-02 — WhatsApp Status Flow:**
    1. From `/settings/integrations` WhatsApp card:
       - Confirm initial state shows correct badge (pending/verified/active/suspended)
       - Click "Suspend" → badge updates → DB row `status='suspended'`
       - Click "Reactivate" → badge updates → DB row `status='active'`
    2. Force a test where verified→active auto-promotion fires (per Phase 50 OTP flow)

    **Record verdicts in `.planning/known-issues.md`:**

    ```markdown
    ## Phase 69 — UAT Validation (2026-05-15)

    ### UAT-V22-01 — PDF Attachment Delivery: PASS / FAIL
    - Setup time: <minutes>
    - Real phone received PDF: YES / NO
    - Follow-up share link arrived: YES / NO
    - Issues found: <list>

    ### UAT-V22-02 — WhatsApp Status Flow: PASS / FAIL
    - Suspend persists: YES / NO
    - Reactivate persists: YES / NO
    - verified→active auto-promotion fires: YES / NO / SKIPPED (no OTP this run)
    - Issues found: <list>
    ```

    Commit: `docs(69-01): UAT-V22 verdicts captured`
  </action>
  <how-to-verify>
    - `.planning/known-issues.md` has UAT-V22-01 + UAT-V22-02 entries with PASS/FAIL verdict
    - Each entry has at least 1 sub-bullet (Setup time, Issues found, etc.)
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c "UAT-V22-01" .planning/known-issues.md` returns at least 1
    - `grep -c "UAT-V22-02" .planning/known-issues.md` returns at least 1
    - Each entry has either "PASS" or "FAIL" or "SKIPPED" with rationale
  </acceptance_criteria>
  <resume-signal>User reports "v22 UAT done" with brief PASS/FAIL summary. If FAIL, capture severity (blocker/major/minor) and continue to Task 2 — don't fix here, fix in Plan 69-03.</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: UAT-V30 — Monetization Stack (Tiers + Stripe + Trial + Admin)</name>
  <what-built>v3.0 monetization shipped tier enforcement, Stripe checkout (test mode), trial automation, admin tooling. Migrations recovered in Phase 61, so this is FIRST real test of these features.</what-built>
  <action>
    **UAT-V30-01 — Tier Enforcement:**
    1. Sign up new test account (becomes free tier by default per Phase 60 trial-on-signup)
    2. Or use existing account; force-tier to free via `/admin/billing`
    3. Generate estimates until quota exhausted (free=10/mo)
    4. **Verify:** 11th estimate POST returns HTTP 402 with `{ code: 'plan_limit_reached' }`
    5. Force-tier to pro → quota resets to 200/mo; 11th estimate succeeds
    6. WhatsApp inbound: with free tier (`whatsappEnabled=false`), confirm Meta download is BLOCKED before Whisper call (check Inngest dashboard — `whatsAppProcessJob` should not fire)

    **UAT-V30-02 — Stripe Checkout (test mode):**
    1. Force tier back to free
    2. Navigate to `/settings/billing` → click "Upgrade to Pro"
    3. Stripe Checkout opens (test mode card: `4242 4242 4242 4242`, CVC `123`, any future date)
    4. Complete payment
    5. **Verify webhook fires:** terminal 3 (`stripe listen`) shows `checkout.session.completed`
    6. **Verify tier updates:** redirect back to billing page shows "Pro" plan
    7. **Verify DB:** `companies.tier = 'pro'`, `stripe_subscription_id` populated

    **UAT-V30-03 — Trial Automation:**
    1. Sign up brand new account
    2. **Verify:** `companies.tier='free'`, `tier_trial_ends_at = now + 14 days`, has Pro entitlements
    3. Manually update `tier_trial_ends_at` to `now + 2 days` to trigger trial banner
    4. Reload billing page → trial banner shows "2 days remaining"
    5. Manually update `tier_trial_ends_at` to `now - 1 hour`
    6. Trigger trial expiry cron: `curl http://localhost:3000/api/cron/expire-trials -H "Authorization: Bearer $CRON_SECRET"`
    7. **Verify:** `tier_trial_ends_at` cleared, user effectively downgraded
    8. **Verify T-3 + T-0 emails:** trigger `curl http://localhost:3000/api/cron/trial-warning-emails -H "Authorization: Bearer $CRON_SECRET"`; check Resend dashboard or test inbox

    **UAT-V30-04 — Stripe Customer Portal:**
    1. Pro account on billing page → click "Manage Subscription"
    2. Stripe Customer Portal opens
    3. **Verify:** can change subscription (Pro→Business or vice versa), cancel, view invoices
    4. Cancel → webhook fires `customer.subscription.deleted` → `companies.tier='free'`

    **UAT-V30-05 — Admin Tooling:**
    1. Navigate to `/admin/billing` (must be super-admin)
    2. **Verify:** MRR shows correct sum (Pro $29 × N + Business $99 × M)
    3. Force-tier any company; reload → tier persisted
    4. Grant bonus credits (e.g. +50 estimates); reload → user has 50 extra quota

    **UAT-V30-06 — 402 Upgrade Modal:**
    1. Free tier with quota exhausted
    2. Try to generate estimate from UI
    3. **Verify:** UI shows upgrade modal/toast (not raw error); CTA links to `/settings/billing`

    **Record all 6 verdicts in known-issues.md:**

    ```markdown
    ### UAT-V30-01 — Tier Enforcement: PASS / FAIL
    - Free quota cap fires: YES / NO
    - Pro quota cap respects: YES / NO
    - WhatsApp gate blocks free tier: YES / NO
    - Issues: <list>

    ### UAT-V30-02 — Stripe Checkout (test mode): PASS / FAIL
    - Webhook fires: YES / NO
    - Tier updates in DB: YES / NO
    - Redirect shows new tier: YES / NO
    - Issues: <list>

    ### UAT-V30-03 — Trial Automation: PASS / FAIL
    - 14-day trial on signup: YES / NO
    - Banner shows <3 days: YES / NO
    - Expiry cron downgrades: YES / NO
    - T-3 + T-0 emails sent: YES / NO
    - Issues: <list>

    ### UAT-V30-04 — Stripe Customer Portal: PASS / FAIL
    - Portal opens: YES / NO
    - Cancel triggers webhook: YES / NO
    - Issues: <list>

    ### UAT-V30-05 — Admin Tooling: PASS / FAIL
    - MRR correct: YES / NO
    - Force-tier persists: YES / NO
    - Bonus credits granted: YES / NO
    - Issues: <list>

    ### UAT-V30-06 — 402 Upgrade Modal: PASS / FAIL
    - Modal shows on quota-exceed: YES / NO
    - CTA works: YES / NO
    - Issues: <list>
    ```

    Commit: `docs(69-01): UAT-V30 verdicts captured`
  </action>
  <how-to-verify>
    - All 6 UAT-V30 entries present in known-issues.md with PASS/FAIL verdicts
  </how-to-verify>
  <acceptance_criteria>
    - `grep -cE "UAT-V30-0[1-6]" .planning/known-issues.md` returns at least 6
    - Each entry has either "PASS" or "FAIL" or "SKIPPED" with rationale
  </acceptance_criteria>
  <resume-signal>User reports "v30 UAT done" with brief PASS/FAIL summary per requirement. Bugs go to Plan 69-03 triage.</resume-signal>
</task>

</tasks>

<verification>
- known-issues.md has 8 verdicts (UAT-V22-01..02, UAT-V30-01..06) — all PASS, FAIL, or SKIPPED
- Each FAIL captures issue summary for Plan 69-03 triage
</verification>

<success_criteria>
8/8 v2.2 + v3.0 UAT requirements have verdicts in known-issues.md. Bugs catalogued for Plan 69-03.
</success_criteria>

<output>
SUMMARY at .planning/phases/69-uat-validation-bug-triage-perf-audit/69-01-SUMMARY.md with:
- Verdict counts (X PASS / Y FAIL / Z SKIPPED out of 8)
- Top issues for triage in 69-03
- Commits list
</output>
