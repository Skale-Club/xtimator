---
phase: 69-uat-validation-bug-triage-perf-audit
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/known-issues.md
autonomous: false
checkpoint: human-action
requirements:
  - UAT-INNGEST-01
  - UAT-INNGEST-02
  - UAT-STORAGE-01

must_haves:
  truths:
    - "2-min audio capture flows through Inngest dashboard end-to-end with stepper UI showing progress; verdict in known-issues.md"
    - "8-min audio (would have timed out on Vercel Free without Inngest) generates an estimate end-to-end; verdict in known-issues.md"
    - "Every storage path (audio upload, photo upload, PDF generation, logo upload, WhatsApp inbound media) works post-refactor through storage.* API; verdict in known-issues.md"
---

<objective>
Manual UAT for the Inngest background pipeline (Phase 67) + storage abstraction (Phase 66). These are the foundational refactors of v3.1.1 — must work flawlessly before considering the milestone shippable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/known-issues.md
@.planning/phases/69-uat-validation-bug-triage-perf-audit/69-01-SUMMARY.md
@docs/INNGEST-LOCAL-DEV.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: UAT-INNGEST — Background AI Pipeline (2-min + 8-min audio)</name>
  <what-built>Phase 67 refactored 3 AI routes + WhatsApp handler to dispatch via Inngest. Frontend polls /api/jobs/[id] at 1.5s for stepper UI. This UAT proves it actually works end-to-end with real audio.</what-built>
  <action>
    **Setup:**
    1. `npm run dev` (terminal 1)
    2. `npm run dev:inngest` (terminal 2) — Inngest dashboard at `localhost:8288`
    3. Open Inngest dashboard in browser
    4. Login to Xtimator at `localhost:3000`

    **UAT-INNGEST-01 — 2-min audio happy path:**
    1. Create new project at `/projects/new`
    2. Navigate to capture screen
    3. Record ~2 minutes of audio (speak about a fictional landscaping job: "client wants 200 sqft of sod replacement, mulch refresh, 3 small trees removed...")
    4. Stop recording
    5. **Watch Inngest dashboard:**
       - `transcribeAudioJob` event fires → completes in ~10-30s
       - `generateEstimateJob` event fires after transcription → completes in ~15-45s
    6. **Watch capture stepper UI:**
       - Saving → Transcribing → Analyzing → Generating
       - Stepper updates every ~1.5s (polling cadence)
    7. **End state:** redirected to estimate editor with populated estimate

    **UAT-INNGEST-02 — 8-min audio (timeout-killer test):**
    1. Record ~8 minutes of audio (read a long article aloud, etc.)
    2. **Critical assertion:** estimate generates successfully — this would have **timed out at 10s on Vercel Free** without Inngest
    3. Watch Inngest dashboard show full execution timeline (potentially 60-90s total)
    4. End state: redirected to editor

    **Record verdicts:**

    ```markdown
    ### UAT-INNGEST-01 — 2-min audio: PASS / FAIL
    - transcribeAudioJob fired: YES / NO
    - generateEstimateJob fired: YES / NO
    - Stepper UI updated correctly: YES / NO
    - Estimate appeared in editor: YES / NO
    - Total time: <seconds>
    - Issues: <list>

    ### UAT-INNGEST-02 — 8-min audio: PASS / FAIL
    - Estimate generated successfully: YES / NO
    - Total time: <seconds>
    - Inngest dashboard shows full timeline: YES / NO
    - Issues: <list>
    ```

    Commit: `docs(69-02): UAT-INNGEST verdicts captured`
  </action>
  <how-to-verify>
    - known-issues.md has UAT-INNGEST-01 + UAT-INNGEST-02 entries with PASS/FAIL
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c "UAT-INNGEST-01" .planning/known-issues.md` returns at least 1
    - `grep -c "UAT-INNGEST-02" .planning/known-issues.md` returns at least 1
  </acceptance_criteria>
  <resume-signal>User reports "inngest UAT done" with PASS/FAIL summary.</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: UAT-STORAGE — Every upload path post-refactor</name>
  <what-built>Phase 66 migrated 18 production files from `supabase.storage.from(...)` direct calls to the new `storage.*` API. UAT verifies every upload path still works correctly.</what-built>
  <action>
    **Test each upload path against `npm run dev` localhost:**

    1. **Logo upload — Settings:**
       - `/settings` → upload company logo PNG
       - **Verify:** logo appears in sidebar + on estimate PDFs

    2. **Logo upload — Client:**
       - `/clients/new` → upload client logo
       - **Verify:** logo appears on client detail page

    3. **Audio upload:**
       - `/projects/new` → record audio in capture flow
       - **Verify:** audio file lands in `audio/{company_id}/...` Supabase bucket
       - **Verify:** Whisper transcription succeeds (Inngest dashboard)

    4. **Photo upload:**
       - In project workspace, upload 5 photos via Photos tab
       - **Verify:** photos land in `photos/{company_id}/...` bucket
       - **Verify:** thumbnails render in photo grid

    5. **PDF generation:**
       - Open completed estimate
       - Generate PDF via Send tab
       - **Verify:** PDF lands in `pdfs/{company_id}/...` bucket
       - **Verify:** signed URL has explicit `expiresInSeconds` (check network tab response)

    6. **WhatsApp inbound media:**
       - Send a photo via WhatsApp to your connected number
       - **Verify:** photo lands in `photos/{company_id}/...` (via Inngest worker)

    7. **Branding asset upload (admin):**
       - `/admin/branding` → upload favicon
       - **Verify:** favicon updates on next page load

    **Record single combined verdict:**

    ```markdown
    ### UAT-STORAGE-01 — All upload paths post-refactor: PASS / FAIL
    - Logo (settings): PASS / FAIL
    - Logo (client): PASS / FAIL
    - Audio: PASS / FAIL
    - Photos: PASS / FAIL
    - PDF: PASS / FAIL
    - WhatsApp inbound media: PASS / FAIL
    - Branding asset: PASS / FAIL
    - Issues: <list of any failures>
    ```

    Commit: `docs(69-02): UAT-STORAGE verdicts captured`
  </action>
  <how-to-verify>
    - known-issues.md has UAT-STORAGE-01 entry with PASS/FAIL per sub-path
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c "UAT-STORAGE-01" .planning/known-issues.md` returns at least 1
    - Entry covers at least 7 sub-paths (logos, audio, photos, pdf, wa-inbound, branding)
  </acceptance_criteria>
  <resume-signal>User reports "storage UAT done" with PASS/FAIL summary.</resume-signal>
</task>

</tasks>

<verification>
- 3 verdicts in known-issues.md: UAT-INNGEST-01, UAT-INNGEST-02, UAT-STORAGE-01
- Bugs go to Plan 69-03 triage
</verification>

<success_criteria>
3/3 Inngest + Storage UAT requirements have verdicts in known-issues.md. Bugs catalogued.
</success_criteria>

<output>
SUMMARY at .planning/phases/69-uat-validation-bug-triage-perf-audit/69-02-SUMMARY.md.
</output>
