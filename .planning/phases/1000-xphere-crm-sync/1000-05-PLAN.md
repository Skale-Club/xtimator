---
phase: 1000-xphere-crm-sync
plan: 05
type: execute
wave: 3
depends_on: ["1000-02", "1000-03"]
files_modified:
  - app/api/admin/xphere-backfill/route.ts
  - app/admin/integrations/xphere-status.tsx
  - app/admin/integrations/integration-category-content.tsx
  - tests/unit/xphere-backfill.test.ts
autonomous: true
requirements: [XPHERE-B6, XPHERE-B7]
must_haves:
  truths:
    - "An admin can POST /api/admin/xphere-backfill to enqueue xphere/sync.requested for every company"
    - "The backfill is admin-guarded, batched (chunked sends), and idempotent (safe to re-run)"
    - "The CRM admin category shows a status panel: synced count, error count, and recent xphere_sync_error rows"
  artifacts:
    - path: "app/api/admin/xphere-backfill/route.ts"
      provides: "admin-only batched backfill enqueue route"
      exports: ["POST"]
    - path: "app/admin/integrations/xphere-status.tsx"
      provides: "observability panel surfacing sync counts + errors"
  key_links:
    - from: "app/api/admin/xphere-backfill/route.ts"
      to: "lib/inngest/client.ts"
      via: "inngest.send chunked over all companies"
      pattern: "EVENT_XPHERE_SYNC"
    - from: "app/admin/integrations/integration-category-content.tsx"
      to: "app/admin/integrations/xphere-status.tsx"
      via: "render under showXphereConfig"
      pattern: "XphereStatus"
---

<objective>
Close the loop: a one-time (re-runnable) admin backfill that enqueues a sync for every existing
company, plus lightweight observability that surfaces `xphere_sync_error` and sync counts in the admin
CRM panel. No new log table — the `companies.xphere_synced_at`/`xphere_sync_error` columns plus Inngest
run history are the source of truth (per CONTEXT deferred decision).

Purpose: Existing companies predate the lifecycle hooks, so they need a backfill. And the platform owner
needs a glanceable view of which companies failed to mirror.
Output: admin backfill route + a status panel rendered in the CRM integrations category + a backfill unit test.

NOTE: depends_on includes 1000-02 because Task 2 edits app/admin/integrations/integration-category-content.tsx,
which Plan 02 also edits (the showXphereConfig render block must already exist). Plan 02 is wave 1, so it
lands before this wave-3 plan — no concurrent edit of that file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1000-xphere-crm-sync/1000-CONTEXT.md

<interfaces>
<!-- From Plan 03 (on disk): EVENT_XPHERE_SYNC = 'xphere/sync.requested' in @lib/inngest/events.ts; inngest in @lib/inngest/client.ts. inngest.send accepts an ARRAY of events (batch). -->
<!-- requireAdmin() throws notFound() if not admin — @lib/auth/admin-context.ts. For an API route, wrap in try/catch and return 401 on throw (see app/api/admin/openrouter-models/route.ts pattern). -->
<!-- requireServiceClient() — @lib/supabase/service. companies has columns: id, xphere_synced_at, xphere_sync_error (from Plan 01 migration). -->
<!-- From Plan 02 (on disk): integration-category-content.tsx already has a `category.showXphereConfig` block rendering <XphereConfigForm/>. Add <XphereStatus/> inside that same block. -->
<!-- Existing admin API route auth precedent: app/api/admin/openrouter-models/route.ts — `export const dynamic='force-dynamic'`, try { await requireAdmin() } catch { return 401 }. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin-only batched backfill route (TDD)</name>
  <read_first>
    - app/api/admin/openrouter-models/route.ts (admin-guard + force-dynamic pattern for an admin API route)
    - lib/auth/admin-context.ts (requireAdmin throws notFound)
    - lib/inngest/client.ts + lib/inngest/events.ts (inngest.send array form, EVENT_XPHERE_SYNC)
  </read_first>
  <files>app/api/admin/xphere-backfill/route.ts, tests/unit/xphere-backfill.test.ts</files>
  <behavior>
    Write tests/unit/xphere-backfill.test.ts FIRST (vitest). Extract the chunking logic into a small pure
    exported helper so it is unit-testable without HTTP:
      export function chunkEventBatches(companyIds: string[], size: number): { name: string; data: { companyId: string; event: 'company.updated' } }[][]
    Tests:
    - Test 1: chunkEventBatches(['a','b','c'], 2) returns 2 chunks: [[a,b],[c]] (lengths 2 then 1).
    - Test 2: every event object has name === 'xphere/sync.requested' and data.event === 'company.updated'.
    - Test 3: chunkEventBatches([], 100) returns [].
    - Test 4: total events across all chunks === companyIds.length.
  </behavior>
  <action>
    Create app/api/admin/xphere-backfill/route.ts:
      import { NextResponse } from 'next/server'
      import { requireAdmin } from '@/lib/auth/admin-context'
      import { requireServiceClient } from '@/lib/supabase/service'
      import { inngest } from '@/lib/inngest/client'
      import { EVENT_XPHERE_SYNC } from '@/lib/inngest/events'

      export const dynamic = 'force-dynamic'
      const CHUNK = 100

      export function chunkEventBatches(companyIds: string[], size: number) {
        const events = companyIds.map((companyId) => ({
          name: EVENT_XPHERE_SYNC,
          data: { companyId, event: 'company.updated' as const },
        }))
        const chunks: (typeof events)[] = []
        for (let i = 0; i < events.length; i += size) chunks.push(events.slice(i, i + size))
        return chunks
      }

      export async function POST() {
        try { await requireAdmin() } catch { return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }) }
        const svc = requireServiceClient()
        const { data, error } = await svc.from('companies').select('id')
        if (error) return NextResponse.json({ error: error.message }, { status: 500 })
        const ids = (data ?? []).map((r) => r.id as string)
        const chunks = chunkEventBatches(ids, CHUNK)
        for (const chunk of chunks) {
          // await each chunk so we don't overwhelm the queue; idempotent (Xphere upsert-by-external_id)
          await inngest.send(chunk)
        }
        return NextResponse.json({ enqueued: ids.length, chunks: chunks.length }, { status: 200 })
      }

    Use 'company.updated' as the backfill event (a non-noteworthy, note-less event so backfill re-runs
    don't spam the Xphere timeline — mapping.ts returns no note for 'company.updated'). Idempotent: safe
    to re-run because Xphere upserts by external_id.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/xphere-backfill.test.ts && grep -q "requireAdmin" app/api/admin/xphere-backfill/route.ts && grep -q "EVENT_XPHERE_SYNC" app/api/admin/xphere-backfill/route.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/xphere-backfill.test.ts` passes all 4 tests.
    - app/api/admin/xphere-backfill/route.ts exports POST and chunkEventBatches.
    - POST returns 401 when requireAdmin throws.
    - Backfill batches sends via inngest.send(chunk) in chunks of 100.
    - The backfill event is 'company.updated' (note-less, idempotent re-run safe).
    - `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>An admin can trigger an idempotent, batched backfill that enqueues a sync for every company.</done>
</task>

<task type="auto">
  <name>Task 2: Observability — XphereStatus panel in the CRM admin category</name>
  <read_first>
    - app/admin/integrations/integration-category-content.tsx (showXphereConfig render block added in Plan 02; add XphereStatus next to XphereConfigForm)
    - app/admin/integrations/twilio-from-phone-form.tsx (panel container styling: `rounded-lg border border-border bg-card/40 p-4 md:p-6`)
    - lib/supabase/service (requireServiceClient)
  </read_first>
  <files>app/admin/integrations/xphere-status.tsx, app/admin/integrations/integration-category-content.tsx</files>
  <action>
    1. app/admin/integrations/xphere-status.tsx — an async SERVER component (no 'use client') that queries
       companies via requireServiceClient and renders a small status panel:
         - syncedCount: count of companies where xphere_synced_at IS NOT NULL.
         - errorCount: count where xphere_sync_error IS NOT NULL.
         - errored rows: select id, name, xphere_sync_error from companies where xphere_sync_error is not null,
           limit 20, order by xphere_synced_at desc nulls last.
       Implement with `.select('id, name, xphere_sync_error, xphere_synced_at')` then count/filter in JS,
       OR two `.select('*', { count: 'exact', head: true })` head queries for the counts plus one list query
       for the errored rows (prefer head-count queries to avoid loading all rows).
       Render container `<div className="rounded-lg border border-border bg-card/40 p-4 md:p-6 space-y-3">`
       with an h3 "Sync Status", a line "Synced: {syncedCount} · Errors: {errorCount}", and a list of
       errored companies (name + truncated error). Show "All companies synced" when errorCount === 0.
       Add a short note: "Run a full backfill by POSTing /api/admin/xphere-backfill (admin only)."
       Do NOT print any API key or token.

    2. app/admin/integrations/integration-category-content.tsx — in the existing `category.showXphereConfig`
       render block (added in Plan 02), render `<XphereStatus />` below `<XphereConfigForm .../>`.
       Import XphereStatus at top. Because it's an async server component, render it directly
       (the parent IntegrationCategoryContent is already async).
  </action>
  <verify>
    <automated>test -f app/admin/integrations/xphere-status.tsx && grep -q "xphere_sync_error" app/admin/integrations/xphere-status.tsx && grep -q "XphereStatus" app/admin/integrations/integration-category-content.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/admin/integrations/xphere-status.tsx exists, is a server component (no 'use client'), and queries companies.xphere_sync_error + xphere_synced_at.
    - It renders synced count, error count, and a list of errored companies (name + error).
    - integration-category-content.tsx renders <XphereStatus /> under the showXphereConfig guard.
    - No API key/token printed.
    - `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>The CRM admin category surfaces sync counts + recent errors; backfill instructions are visible.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/xphere-backfill.test.ts` passes.
- `npx tsc --noEmit` clean.
- POST /api/admin/xphere-backfill is admin-guarded (401 otherwise), batched, and uses the note-less 'company.updated' event for idempotent re-runs.
- XphereStatus renders counts + errored companies in the CRM admin panel.
</verification>

<success_criteria>
- Existing companies can be mirrored on demand via an idempotent, batched admin backfill.
- xphere_sync_error is glanceable in the admin; no new log table (Inngest run history + companies columns suffice).
</success_criteria>

<output>
After completion, create `.planning/phases/1000-xphere-crm-sync/1000-05-SUMMARY.md`.
</output>
