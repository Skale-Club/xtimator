---
phase: 1000-xphere-crm-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/platform-config.ts
  - lib/schemas/admin.ts
  - lib/admin/integrations-providers.ts
  - app/admin/integrations/actions.ts
  - app/admin/integrations/integration-category-content.tsx
  - app/admin/integrations/xphere-config-form.tsx
autonomous: true
requirements: [XPHERE-B2]
user_setup:
  - service: xphere
    why: "CRM mirror — server-to-server sync of every company into the Xphere Xtimator org"
    env_vars:
      - name: XPHERE_API_KEY
        source: "Xphere → the Xtimator org → Settings → API Keys (scope sync:write). An xph_… token. Placeholder only in docs."
      - name: XPHERE_BASE_URL
        source: "Base origin of the Xphere deployment, e.g. https://app.xphere.example (no trailing path)"
    dashboard_config:
      - task: "Create the 'Xtimator Lifecycle' pipeline with stages Trial, Active — Pro, Active — Business, Churned (manual, Phase A — out of scope here)"
        location: "Xphere → Xtimator org → Pipelines"
must_haves:
  truths:
    - "getIntegrationKey('xphere') returns the stored API key, falling back to XPHERE_API_KEY env for local dev"
    - "getXphereConfig() returns { apiKey, baseUrl } or null when unconfigured (disabled-by-default)"
    - "A platform admin can save the Xphere API key + base URL from /admin/integrations"
  artifacts:
    - path: "lib/platform-config.ts"
      provides: "'xphere' added to IntegrationProvider + getXphereConfig() reader with env fallback"
      contains: "getXphereConfig"
    - path: "lib/admin/integrations-providers.ts"
      provides: "Xphere category card + showXphereConfig flag"
      contains: "xphere"
    - path: "app/admin/integrations/xphere-config-form.tsx"
      provides: "admin base-URL config form (mirrors TwilioFromPhoneForm)"
  key_links:
    - from: "app/admin/integrations/integration-category-content.tsx"
      to: "app/admin/integrations/xphere-config-form.tsx"
      via: "render when category.showXphereConfig"
      pattern: "XphereConfigForm"
    - from: "app/admin/integrations/xphere-config-form.tsx"
      to: "app/admin/integrations/actions.ts"
      via: "saveXphereBaseUrl server action"
      pattern: "saveXphereBaseUrl"
---

<objective>
Make the Xphere integration configurable through the existing admin panel and readable at runtime —
disabled by default. The encrypted API key flows through the established `platform_integrations` +
`getIntegrationKey` path; the non-secret base URL is stored in that row's `metadata` (same shape Twilio
uses for `from_phone`). Add a `getXphereConfig()` reader so the client/job in Plan 03 can short-circuit
when unconfigured.

Purpose: Credentials must exist and be readable before the client can POST anything. Mirroring the
Stripe/Resend/Twilio admin surface keeps the platform-owner UX uniform.
Output: 'xphere' provider wired end-to-end through schema → config reader → admin category → save actions → config form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1000-xphere-crm-sync/1000-CONTEXT.md

<interfaces>
<!-- getIntegrationKey (lib/platform-config.ts:196) — encrypted fetch + 30s cache + env fallback.
  Env fallback for a non-stripe provider tries `${PROVIDER.toUpperCase()}_API_KEY` → so 'xphere' tries XPHERE_API_KEY automatically. -->
<!-- IntegrationProvider union (lib/platform-config.ts:39) — add 'xphere'. -->
<!-- integrationKeySchema (lib/schemas/admin.ts:12) — z.enum provider list; add 'xphere'. -->
<!-- saveTwilioFromPhone (app/admin/integrations/actions.ts:293) — the EXACT template for saveXphereBaseUrl:
  requireAdmin → validate → read existing row → upsert metadata preserving ciphertext/iv/auth_tag → invalidate → revalidate → audit. -->
<!-- testIntegrationKey switch (actions.ts:133) — add an 'xphere' branch that GETs {baseUrl} and checks reachability. -->
<!-- CATEGORIES (lib/admin/integrations-providers.ts:35) — add a new category with showXphereConfig flag. -->
<!-- IntegrationCategoryContent (integration-category-content.tsx) — read metadata.base_url when showXphereConfig; render <XphereConfigForm/>. -->
<!-- TwilioFromPhoneForm (twilio-from-phone-form.tsx) — copy shape for XphereConfigForm. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 'xphere' provider + getXphereConfig() reader</name>
  <read_first>
    - lib/platform-config.ts (IntegrationProvider union ~line 39; getIntegrationKey ~196-255 incl. env-fallback candidate logic; getTwilioConfig ~291 as the reader-with-metadata template)
    - lib/schemas/admin.ts (integrationKeySchema z.enum ~line 12)
  </read_first>
  <files>lib/platform-config.ts, lib/schemas/admin.ts</files>
  <action>
    1. lib/platform-config.ts: add `| 'xphere'` to the IntegrationProvider union.
    2. lib/platform-config.ts: add a new exported reader modeled on getTwilioConfig:

       export type XphereConfig = { apiKey: string; baseUrl: string } | null
       export async function getXphereConfig(): Promise<XphereConfig> {
         const apiKey = await getIntegrationKey('xphere')   // env fallback → XPHERE_API_KEY
         if (!apiKey) return null
         // base URL from platform_integrations.xphere metadata.base_url, else env XPHERE_BASE_URL
         let baseUrl = process.env.XPHERE_BASE_URL ?? ''
         const svc = createServiceClient()
         if (svc) {
           const { data } = await svc.from('platform_integrations')
             .select('metadata').eq('provider', 'xphere').maybeSingle()
           const fromDb = (data?.metadata as { base_url?: string } | null)?.base_url
           if (fromDb && fromDb.trim()) baseUrl = fromDb.trim()
         }
         if (!baseUrl) return null
         return { apiKey, baseUrl: baseUrl.replace(/\/+$/, '') }   // strip trailing slash
       }

    3. lib/schemas/admin.ts: add 'xphere' to the integrationKeySchema provider z.enum array.
    Do NOT hardcode any real xph_ token anywhere (gitleaks). Use the env/DB paths only.
  </action>
  <verify>
    <automated>grep -q "getXphereConfig" lib/platform-config.ts && grep -q "'xphere'" lib/platform-config.ts && grep -q "xphere" lib/schemas/admin.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lib/platform-config.ts IntegrationProvider union includes 'xphere'.
    - lib/platform-config.ts exports `getXphereConfig` returning `{ apiKey, baseUrl } | null`.
    - getXphereConfig returns null when apiKey OR baseUrl is missing (disabled-by-default).
    - getXphereConfig strips trailing slash from baseUrl.
    - lib/schemas/admin.ts integrationKeySchema enum includes 'xphere'.
    - `npx tsc --noEmit` is clean.
    - No literal token matching `xph_[A-Za-z0-9]` appears in either file.
  </acceptance_criteria>
  <done>'xphere' is a valid IntegrationProvider, getXphereConfig() reads key + base URL with env fallback, types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Admin save action + test branch + base-URL config form</name>
  <read_first>
    - app/admin/integrations/actions.ts (saveTwilioFromPhone ~293 — copy verbatim structure for saveXphereBaseUrl; testIntegrationKey runTestIntegrationKey switch ~133-282 — add 'xphere' branch)
    - app/admin/integrations/twilio-from-phone-form.tsx (full file — copy shape for XphereConfigForm)
    - lib/admin/integrations-providers.ts (CATEGORIES array ~35; loadCategoryInitials ~128)
    - app/admin/integrations/integration-category-content.tsx (showFromPhone block ~26-35 and render block ~90-92 — mirror for showXphereConfig)
  </read_first>
  <files>app/admin/integrations/actions.ts, lib/admin/integrations-providers.ts, app/admin/integrations/integration-category-content.tsx, app/admin/integrations/xphere-config-form.tsx</files>
  <action>
    1. lib/admin/integrations-providers.ts — add a CATEGORY to the CATEGORIES array:
       {
         slug: 'crm',
         title: 'CRM',
         description: 'Mirror every company into the Xphere CRM (Account + Contact + Opportunity).',
         showXphereConfig: true,
         providers: [{
           id: 'xphere' as IntegrationProvider,
           title: 'Xphere',
           description: 'API key (xph_… token, scope sync:write) for the Xphere Xtimator org. Set the base URL below.',
         }],
       }
       Also add `showXphereConfig?: boolean` to the Category type definition.

    2. app/admin/integrations/actions.ts — add `saveXphereBaseUrl(baseUrl: string): Promise<ActionResult>`,
       copied from saveTwilioFromPhone but: provider 'xphere'; validate the URL is a valid http(s) origin
       (new URL(trimmed); reject if protocol not http/https → { ok:false, message:'Base URL must be a valid http(s) URL' });
       upsert metadata `{ ...existing, base_url: trimmed }` preserving ciphertext/iv/auth_tag; invalidatePlatformConfig();
       revalidatePath('/admin/integrations'); audit targetId 'xphere_base_url'.
       Also add an 'xphere' branch to runTestIntegrationKey: GET `${baseUrl}/api/xtimator/webhook` is not safe to call
       (it mutates), so instead resolve config via getXphereConfig() and, if present, return
       { ok: true, message: `Configured. Base URL ${config.baseUrl} set; key ends …${config.apiKey.slice(-4)}.` };
       if null return { ok:false, message:'Set both the API key and base URL.' }. Do NOT echo the full key.

    3. app/admin/integrations/xphere-config-form.tsx — new client component modeled on TwilioFromPhoneForm:
       props { current: string } (the saved base_url), an Input (text) for the base URL, a Save button that calls
       saveXphereBaseUrl(value) inside useTransition and toasts success/error. Label "Xphere Base URL",
       placeholder "https://app.xphere.example".

    4. app/admin/integrations/integration-category-content.tsx — mirror the showFromPhone pattern:
       when category.showXphereConfig, read platform_integrations metadata.base_url for provider 'xphere'
       into a `xphereBaseUrl` string; render `<XphereConfigForm current={xphereBaseUrl} />` in the same
       position TwilioFromPhoneForm renders. Import XphereConfigForm at top.
  </action>
  <verify>
    <automated>grep -q "saveXphereBaseUrl" app/admin/integrations/actions.ts && grep -q "showXphereConfig" lib/admin/integrations-providers.ts && grep -q "XphereConfigForm" app/admin/integrations/integration-category-content.tsx && test -f app/admin/integrations/xphere-config-form.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lib/admin/integrations-providers.ts CATEGORIES contains a category with slug 'crm', a provider id 'xphere', and showXphereConfig:true; Category type has `showXphereConfig?: boolean`.
    - app/admin/integrations/actions.ts exports saveXphereBaseUrl and has an 'xphere' case in runTestIntegrationKey.
    - app/admin/integrations/xphere-config-form.tsx exists and calls saveXphereBaseUrl.
    - integration-category-content.tsx renders XphereConfigForm under a showXphereConfig guard.
    - `npx tsc --noEmit` is clean.
    - saveXphereBaseUrl validates the URL via new URL() and rejects non-http(s).
    - No real xph_ token literal in any modified file.
  </acceptance_criteria>
  <done>Admin can save the Xphere key (existing IntegrationCard) + base URL (new form); the CRM category renders in /admin/integrations.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean across all modified files.
- getXphereConfig() returns null unless BOTH key and base URL are present (manually reason through the guards).
- /admin/integrations/crm renders the Xphere card + base-URL form (structural — IntegrationCard reused, XphereConfigForm new).
- No secret literals committed (gitleaks pre-commit will also enforce this).
</verification>

<success_criteria>
- 'xphere' is a first-class IntegrationProvider with admin save + test surface mirroring Stripe/Twilio.
- Base URL stored in platform_integrations.metadata.base_url; key stored encrypted; both readable via getXphereConfig().
- Integration is disabled-by-default: getXphereConfig() returns null when unconfigured.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-xphere-crm-sync/1000-02-SUMMARY.md`.
</output>
