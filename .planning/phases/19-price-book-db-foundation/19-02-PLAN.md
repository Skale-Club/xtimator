---
phase: 19-price-book-db-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - types/database.types.ts
autonomous: false
requirements:
  - infrastructure-prereq-PB-01
  - infrastructure-prereq-AIPRICE-03
  - infrastructure-prereq-EDITPRICE-01
  - infrastructure-prereq-EDITPRICE-02

user_setup:
  - service: supabase
    why: "Applying migration to live database requires DATABASE_URL from Supabase project settings"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection string (URI format)"
    dashboard_config: []

must_haves:
  truths:
    - "The migration 20260506000001_phase19_price_book.sql is applied to the live Supabase database without error"
    - "company_price_book table exists in the database with all 8 columns and 4 RLS policies active"
    - "estimate_items.price_source column exists, accepts NULL, accepts 'price_book' and 'ai_estimate', rejects any other non-null value"
    - "types/database.types.ts exists and reflects the new schema (contains CompanyPriceBook)"
    - "bun run build exits 0 — TypeScript compilation passes with the regenerated types"
    - "bun run test exits 0 — integration tests for price-book-rls pass (SC-1, SC-2, SC-3)"
  artifacts:
    - path: "types/database.types.ts"
      provides: "Generated TypeScript types for the full Supabase schema including company_price_book"
      contains: "CompanyPriceBook"
  key_links:
    - from: "supabase/migrations/20260506000001_phase19_price_book.sql"
      to: "Supabase PostgreSQL live DB"
      via: "bunx supabase db push --db-url $DATABASE_URL"
      pattern: "exit code 0 from db push"
    - from: "Supabase PostgreSQL live DB"
      to: "types/database.types.ts"
      via: "bunx supabase gen types typescript --db-url $DATABASE_URL"
      pattern: "CompanyPriceBook"
    - from: "types/database.types.ts"
      to: "bun run build"
      via: "TypeScript compiler resolves Database['public']['Tables']['company_price_book']"
      pattern: "bun run build exit 0"
---

<objective>
Apply the migration from Plan 01 to the live Supabase database, regenerate TypeScript types, and verify the build passes. This plan has one human checkpoint because the migration step requires DATABASE_URL configured in the executor's environment.

Purpose: Make the schema changes real and verifiable — integration tests go from SKIP to PASS, and the TypeScript build validates the new types.
Output: Applied migration, types/database.types.ts, green build and test suite.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-price-book-db-foundation/19-RESEARCH.md
@.planning/phases/19-price-book-db-foundation/19-VALIDATION.md
@.planning/phases/19-price-book-db-foundation/19-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration to live Supabase database</name>
  <read_first>
    - supabase/migrations/20260506000001_phase19_price_book.sql (confirm it exists from Plan 01 before running db push)
    - .planning/phases/19-price-book-db-foundation/19-RESEARCH.md (Pitfall 4: verify DATABASE_URL points to intended environment)
  </read_first>
  <what-built>
    Plan 01 wrote `supabase/migrations/20260506000001_phase19_price_book.sql` with the complete DDL:
    - CREATE TABLE public.company_price_book (8 columns, NUMERIC(12,2) unit_price)
    - ALTER TABLE public.company_price_book ENABLE ROW LEVEL SECURITY
    - 4 CREATE POLICY statements (SELECT/INSERT/UPDATE/DELETE) using company_id subquery
    - ALTER TABLE estimate_items ADD COLUMN price_source TEXT CHECK (price_source IS NULL OR price_source IN ('price_book', 'ai_estimate'))

    The migration has NOT been applied yet. Run the command below to apply it.
  </what-built>
  <how-to-verify>
    1. Confirm DATABASE_URL is set and points to the correct Supabase project:
       ```
       echo $DATABASE_URL | cut -c1-40  # should show postgresql://postgres...
       ```

    2. Run the migration:
       ```
       bunx supabase db push --db-url "$DATABASE_URL"
       ```
       Expected output: "Applying migration 20260506000001_phase19_price_book.sql... done"
       Expected exit code: 0

    3. Smoke-check the table exists (optional, uses psql):
       ```
       bunx supabase db execute --db-url "$DATABASE_URL" "SELECT COUNT(*) FROM company_price_book LIMIT 1;"
       ```
       Or verify via Supabase Dashboard → Table Editor → look for company_price_book table.

    If `db push` fails:
    - "already exists" → migration was already applied (safe, continue)
    - "column already exists" → price_source was already added (safe, continue)
    - Any other error → do NOT continue; fix the SQL in the migration file
  </how-to-verify>
  <resume-signal>Type "migration applied" after bunx supabase db push exits 0, or "already applied" if the migration was already present</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Regenerate TypeScript types and verify build passes</name>
  <files>types/database.types.ts</files>
  <read_first>
    - types/env.d.ts (understand what already lives in types/ directory — do not overwrite this file)
    - .planning/phases/19-price-book-db-foundation/19-RESEARCH.md (TypeScript types strategy section — codegen must run AFTER migration is applied)
  </read_first>
  <action>
Run the following commands IN ORDER. Do not skip any step. Each step must exit 0 before proceeding.

Step 1 — Regenerate types from live schema:
```bash
bunx supabase gen types typescript --db-url "$DATABASE_URL" > types/database.types.ts
```

Step 2 — Verify types contain the new table (grep for both key identifiers):
```bash
grep "company_price_book" types/database.types.ts
grep "price_source" types/database.types.ts
```
Both greps must return at least one line. If either returns nothing, the migration was not applied before codegen — stop and re-run Task 1.

Step 3 — Run the full test suite (integration tests should now PASS, not skip):
```bash
bun run test
```
Expected: all tests pass. The price-book-rls tests should show as passing (not skipped) if NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, and NEXT_PUBLIC_SUPABASE_ANON_KEY are set.

Step 4 — Run the TypeScript build:
```bash
bun run build
```
Expected: exits 0 with no TypeScript errors.

If `bun run build` fails with TypeScript errors related to `company_price_book` or `price_source`:
- The generated types may have an unexpected shape — open `types/database.types.ts` and check if `Database['public']['Tables']['company_price_book']` exists
- If types are correct but a consuming file (e.g., a lib/queries/ file) has a type mismatch, fix the consuming file — do NOT modify the generated types file
  </action>
  <verify>
    <automated>grep -c "company_price_book" types/database.types.ts && grep -c "price_source" types/database.types.ts && bun run test && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - File `types/database.types.ts` exists (was created or overwritten by codegen)
    - `grep "company_price_book" types/database.types.ts` returns at least 1 line
    - `grep "price_source" types/database.types.ts` returns at least 1 line
    - `bun run test` exits 0 — all tests pass or skip; no test fails
    - `bun run build` exits 0 — TypeScript compilation clean; no type errors related to company_price_book or price_source
    - `tests/integration/price-book-rls.test.ts` SC-1 and SC-3 tests pass (not skip) when Supabase env vars are present
  </acceptance_criteria>
  <done>TypeScript types regenerated from live schema, build passes, integration tests confirm table and column exist in the database</done>
</task>

</tasks>

<verification>
Phase 19 complete when:
1. `bun run test` — exits 0, price-book-rls SC-1/SC-2/SC-3 passing
2. `bun run build` — exits 0, no TypeScript errors
3. `grep "company_price_book" types/database.types.ts` — returns results
4. `grep -c "CREATE POLICY" supabase/migrations/20260506000001_phase19_price_book.sql` — returns 4

Supabase Dashboard verification (manual):
- Table Editor shows `company_price_book` with 8 columns
- Authentication → Policies shows 4 policies on `company_price_book`
- Table Editor → `estimate_items` shows `price_source` column with type `text`
</verification>

<success_criteria>
- Migration applied to live DB without error
- types/database.types.ts contains company_price_book and price_source
- bun run test exits 0
- bun run build exits 0
- Phase 19 is complete — Phase 20 (Price Book CRUD UI) and Phase 22 (AI Price Anchoring) can now proceed in parallel
</success_criteria>

<output>
After completion, create `.planning/phases/19-price-book-db-foundation/19-02-SUMMARY.md`
</output>
