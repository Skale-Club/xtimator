---
phase: 152-usage-progress-bar-super-admin-cost-visibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/notifications/copy.ts
  - tests/unit/notifications/copy-tenant-neutrality.test.ts
autonomous: true
gap_closure: true
requirements: [CREDITUI-04]

must_haves:
  truths:
    - "No tenant-facing notification body ever interpolates a raw credit count"
    - "The admin.bonus_credits_granted notification reads as a qualitative confirmation, not a number-bearing receipt"
  artifacts:
    - path: "lib/notifications/copy.ts"
      provides: "admin.bonus_credits_granted body reworded to drop ctx.credits interpolation"
    - path: "tests/unit/notifications/copy-tenant-neutrality.test.ts"
      provides: "Static + behavioral guard: admin.bonus_credits_granted body never contains a digit or references ctx.credits"
  key_links:
    - from: "app/admin/billing/actions.ts"
      to: "lib/notifications/copy.ts"
      via: "buildNotificationCopy('admin.bonus_credits_granted', { credits }) call whose returned body no longer surfaces the number"
      pattern: "admin.bonus_credits_granted"
---

<objective>
Close the one remaining CREDITUI-04 gap identified in 152-VERIFICATION.md: `lib/notifications/copy.ts`'s `admin.bonus_credits_granted` notification body currently renders a raw credit count (`"An admin granted you ${ctx.credits ?? 0} bonus credits."`) directly to the tenant's notification bell. This is the one tenant-facing surface named in CREDITUI-04's requirement text ("low-balance notification copy") that neither 152-01 nor 152-02 scoped in. Reword the single string to a qualitative framing consistent with the percentage-only convention already established for the Plans page and topbar chip, and add a small guard test so this cannot regress silently.

Purpose: Full closure of CREDITUI-04 — "no tenant-facing surface displays a raw credit count or dollar figure" — across ALL three named surfaces (Plans page, topbar chip, low-balance/bonus-grant notification copy), not just the first two.
Output: Reworded `admin.bonus_credits_granted` body in `lib/notifications/copy.ts`; new test file `tests/unit/notifications/copy-tenant-neutrality.test.ts` asserting the body never contains a digit and no longer interpolates `ctx.credits`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/152-usage-progress-bar-super-admin-cost-visibility/152-CONTEXT.md
@.planning/phases/152-usage-progress-bar-super-admin-cost-visibility/152-VERIFICATION.md

<interfaces>
<!-- Extracted from the actual current file during planning. Executor should use this directly — no exploration needed. -->

From `lib/notifications/copy.ts` (current full relevant excerpt — the ONLY case block this plan touches; do not touch any other `case` in this switch):
```typescript
export interface CopyContext {
  estimateNumber?: string
  projectName?: string
  amountUSD?: string
  clientName?: string
  daysRemaining?: number
  quotaPercent?: number
  whatsappFrom?: string
  tierFrom?: string
  tierTo?: string
  credits?: number
  jobType?: string
  errorMessage?: string
  maintenanceTitle?: string
  maintenanceBody?: string
}

// ... inside buildNotificationCopy's switch:
case 'admin.bonus_credits_granted':
  return {
    title: 'Bonus credits granted',
    body: `An admin granted you ${ctx.credits ?? 0} bonus credits.`,
  }
```

Caller (`app/admin/billing/actions.ts`, `grantBonusCredits(companyId, credits)`, confirmed by `tests/unit/notifications/event-sources.test.ts` — DO NOT modify this file, it already calls `notify({ eventType: 'admin.bonus_credits_granted', ctx: { credits } })` correctly; the fix is entirely in the copy string, not the call site):
```typescript
notify({ eventType: 'admin.bonus_credits_granted', companyId, ctx: { credits } })
```

Existing test that already asserts `notify()` fires with this eventType (`tests/unit/notifications/event-sources.test.ts`, lines ~374-405) — it checks `mock.calls` for the eventType, NOT the body string, so it will NOT need changes and will NOT collide with this plan's new test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reword admin.bonus_credits_granted body to drop the raw credit count, add tenant-neutrality guard test</name>
  <files>lib/notifications/copy.ts, tests/unit/notifications/copy-tenant-neutrality.test.ts</files>
  <read_first>
    - lib/notifications/copy.ts (full file — read before editing; the `case 'admin.bonus_credits_granted':` block is at line ~127-131)
    - .planning/phases/152-usage-progress-bar-super-admin-cost-visibility/152-VERIFICATION.md (Gap section, lines 6-15 — the exact gap this task closes)
  </read_first>
  <behavior>
    - Test 1: `buildNotificationCopy('admin.bonus_credits_granted', { credits: 500 })` returns a `body` string that does NOT match `/\d/` (no digit anywhere) — regardless of how large `ctx.credits` is.
    - Test 2: `buildNotificationCopy('admin.bonus_credits_granted', {})` (no `ctx.credits` at all — the defensive-default case) returns a `body` string that is still a coherent, non-empty sentence ending in a period (module's own documented convention at the top of `copy.ts`), and still contains no digit.
    - Test 3 (static source guard, mirrors the `tenant-cost-neutrality.test.ts` bare-substring technique already established in this phase): `lib/notifications/copy.ts`'s source, with comments stripped, does not contain the literal substring `ctx.credits` inside the `admin.bonus_credits_granted` case's returned template literal — i.e. the interpolation itself is gone, not just coincidentally numberless at runtime. Implemented pragmatically as: extract the case block's body value via the same `buildNotificationCopy` call as Test 1/2 (behavioral proof is sufficient and simpler than parsing the switch statement source) — do NOT introduce brittle source-slicing. It is acceptable for this test to be purely behavioral (Tests 1-2) rather than a second static-grep test, since the gap is a single string in a single case block and a static scan would be redundant here (unlike the multi-file `components/billing/**` surface Plan 152-01 needed to guard).
  </behavior>
  <action>
    In `lib/notifications/copy.ts`, change ONLY the `admin.bonus_credits_granted` case's `body` field. Replace:
    ```typescript
    case 'admin.bonus_credits_granted':
      return {
        title: 'Bonus credits granted',
        body: `An admin granted you ${ctx.credits ?? 0} bonus credits.`,
      }
    ```
    with:
    ```typescript
    case 'admin.bonus_credits_granted':
      return {
        title: 'Bonus credits granted',
        body: 'An admin added bonus credits to your account.',
      }
    ```
    This drops the `ctx.credits` interpolation entirely — the string is now a static literal, consistent with the CREDITUI-03/04 percentage-only, no-raw-number convention already applied to `credit-balance-card.tsx` and `credit-chip.tsx` in Plan 152-01. Do NOT remove `credits?: number` from the `CopyContext` interface (other future/reasonable callers of this context type may still want to pass it for logging/audit purposes upstream of `buildNotificationCopy`; removing the field is out of scope and not required to close this gap — the gap is specifically about what renders in tenant-facing copy). Do NOT touch any other `case` block in this file, and do NOT modify `app/admin/billing/actions.ts` — its call site is already correct and needs no change.

    Create `tests/unit/notifications/copy-tenant-neutrality.test.ts` covering the three behaviors above, importing `buildNotificationCopy` from `@/lib/notifications/copy`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/notifications/copy-tenant-neutrality.test.ts` passes all 3 tests.
    - `grep -n "ctx.credits" lib/notifications/copy.ts` returns zero matches (the interpolation is fully removed from the file).
    - `grep -n "An admin added bonus credits to your account" lib/notifications/copy.ts` returns exactly one match.
    - `npx vitest run tests/unit/notifications/event-sources.test.ts` still passes unchanged (proves the existing eventType-dispatch test is unaffected by this copy-only change).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/notifications/copy-tenant-neutrality.test.ts tests/unit/notifications/event-sources.test.ts</automated>
  </verify>
  <done>admin.bonus_credits_granted's body is a static, qualitative sentence with zero digits and zero ctx.credits interpolation; a dedicated guard test locks this in; the pre-existing event-dispatch test is untouched and still green.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/notifications/` after the task — expect all notification suites (including the new file and the untouched `event-sources.test.ts`) to pass. Run `npm test` once to confirm no regression, since `copy.ts` is imported by every event source that calls `notify()`.
</verification>

<success_criteria>
- `lib/notifications/copy.ts`'s `admin.bonus_credits_granted` body no longer interpolates `ctx.credits` or renders any digit.
- New guard test passes, locking in the qualitative framing.
- Full `npm test` suite remains green — zero regression to any other notification type or the existing `event-sources.test.ts` dispatch assertions.
- CREDITUI-04 is now fully satisfied across all three named surfaces: Plans page, topbar chip, and bonus-credit notification copy.
</success_criteria>

<output>
After completion, create `.planning/phases/152-usage-progress-bar-super-admin-cost-visibility/152-03-SUMMARY.md`
</output>
