---
phase: 181-real-product-cutover-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/(app)/settings/(tabs)/company/page.tsx"
  - "app/(app)/settings/(tabs)/team/page.tsx"
  - "app/(app)/settings/(tabs)/notifications/page.tsx"
  - "components/settings/notifications-form.tsx"
  - "tests/unit/settings/demo-readonly-forms.test.ts"
autonomous: true
requirements: [PARITY-02, PARITY-03]
must_haves:
  truths:
    - "A demo session's Company tab renders every field disabled (fieldset) and shows the read-only footer note instead of the Save button."
    - "A demo session's Team tab hides Invite and every per-member manage control (role Select, Remove) — a plain role-label pill renders instead, identical to what a non-manager member sees today."
    - "A demo session's Notifications tab renders every Switch and both action buttons disabled, with a read-only footer note explaining why."
  artifacts:
    - path: "components/settings/notifications-form.tsx"
      provides: "new readOnly prop cascading via a native fieldset disabled wrapper, matching CompanyInfoForm's existing pattern"
    - path: "tests/unit/settings/demo-readonly-forms.test.ts"
      provides: "static-source proof of readOnly/canManage wiring across all 3 exposed tab pages"
  key_links:
    - from: "app/(app)/settings/(tabs)/company/page.tsx"
      to: "components/settings/company-info-form.tsx"
      via: "readOnly={isDemoCompany(company.id)} passed into the form's existing readOnly prop"
      pattern: "readOnly=\\{isDemoCompany\\(company\\.id\\)\\}"
    - from: "app/(app)/settings/(tabs)/team/page.tsx"
      to: "components/settings/team-section.tsx"
      via: "canManage forced false for the demo company, reusing the existing canManage gate"
      pattern: "!isDemoCompany\\(companyId\\)"
    - from: "app/(app)/settings/(tabs)/notifications/page.tsx"
      to: "components/settings/notifications-form.tsx"
      via: "readOnly={isDemoCompany(companyId)} passed into the new readOnly prop"
      pattern: "readOnly=\\{isDemoCompany\\(companyId\\)\\}"
---

<objective>
Wire demo-aware read-only rendering into the 3 settings tabs Plan 01 makes reachable for demo sessions: reuse `CompanyInfoForm`'s existing `readOnly` prop and `TeamSection`'s existing `canManage` gate verbatim (zero new component code), and add one small new `readOnly` prop to `NotificationsForm` (the only one of the three with no existing read-only affordance).

Purpose: PARITY-03 requires every mutation-capable control to be removed or disabled for demo visitors on the newly-reachable tabs.
Output: Company/Team/Notifications render fully interactive for real tenants and fully read-only (with an explanatory footer note) for the demo company.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-real-product-cutover-verification/181-CONTEXT.md
@.planning/phases/181-real-product-cutover-verification/181-RESEARCH.md
@.planning/phases/181-real-product-cutover-verification/181-UI-SPEC.md
@lib/demo/config.ts
</context>

<interfaces>
<!-- lib/demo/config.ts (unchanged, Phase 180) — the only demo-classification helper this plan uses.
     Prefer this over isDemoSession() here: these pages already resolve companyId/company.id, so this
     is a pure synchronous check with no extra Supabase round trip. -->
```typescript
export function isDemoCompany(companyId: string | null | undefined): boolean
```

<!-- components/settings/company-info-form.tsx (existing, unmodified by this plan) — the exact pattern
     NotificationsForm's new readOnly prop must replicate: -->
```typescript
interface CompanyInfoFormProps {
  company: CompanySettings
  readOnly?: boolean
}
// <fieldset disabled={readOnly} ...> wraps every field; a conditional swap replaces the Save button
// with: <p className="text-sm text-muted-foreground">This is a read-only demo. Create a free account to edit your profile.</p>
```

<!-- components/settings/team-section.tsx (existing, unmodified by this plan) — canManage already gates
     every mutation control and already renders the fallback role-label pill: -->
```typescript
interface Props {
  companyId: string
  members: RosterMember[]
  invites: RosterInvite[]
  canManage: boolean
  seatCost: SeatCostSummary | null
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a readOnly prop to NotificationsForm and wire it from the notifications tab</name>
  <read_first>@components/settings/notifications-form.tsx @components/settings/company-info-form.tsx @app/(app)/settings/(tabs)/notifications/page.tsx @lib/demo/config.ts</read_first>
  <files>components/settings/notifications-form.tsx, app/(app)/settings/(tabs)/notifications/page.tsx, tests/unit/settings/demo-readonly-forms.test.ts</files>
  <behavior>
    - `NotificationsForm({ readOnly: true, ... })` renders every `Switch` and both `Button`s (`Save preferences`, `Enable/Disable browser notifications`) disabled via a wrapping `fieldset disabled`, and renders the footer note "This is a read-only demo. Create a free account to manage your notification settings."
    - `NotificationsForm({ readOnly: false, ... })` (or omitted, the default) is byte-for-byte unchanged from current behavior — no fieldset-driven disabling, no footer note.
  </behavior>
  <action>
    1. In `components/settings/notifications-form.tsx`: add `readOnly?: boolean` to `NotificationsFormProps` (after the existing `smsOptIn?: boolean` field) and destructure it with a default in the component signature: `export function NotificationsForm({ initial, defaults, verifiedPhone = null, smsOptIn = false, readOnly = false }: NotificationsFormProps)`.
    2. Replace the outer `<div className="space-y-6">` (the root return wrapper, line 219) with `<fieldset disabled={readOnly} className="space-y-6 border-0 p-0 m-0">` — both `<Card>` blocks (the master-switch/category-matrix card and the browser-notifications card) move inside this fieldset unchanged. Close the fieldset with `</fieldset>` where the root `</div>` currently closes.
    3. Immediately after the closing `</fieldset>`, add:
       ```tsx
       {readOnly && (
         <p className="mt-6 text-sm text-muted-foreground">
           {t('This is a read-only demo. Create a free account to manage your notification settings.')}
         </p>
       )}
       ```
       This is UI-SPEC's Read-Only Presentation Contract §3 verbatim — the exact copy string must match: `This is a read-only demo. Create a free account to manage your notification settings.`
    4. In `app/(app)/settings/(tabs)/notifications/page.tsx`: import `isDemoCompany` from `@/lib/demo/config`, and pass `readOnly={isDemoCompany(companyId)}` to `<NotificationsForm ...>` — `companyId` is already resolved via `getActiveCompanyId()` at line 21, no extra Supabase round trip needed.
    5. Write the RED test FIRST in `tests/unit/settings/demo-readonly-forms.test.ts` (same static-source-guard pattern as `tests/unit/settings/team-section-no-hardcode.test.ts` — `readFileSync` + string assertions): assert `components/settings/notifications-form.tsx` contains `readOnly?: boolean`, contains `<fieldset disabled={readOnly}`, and contains the exact string `'This is a read-only demo. Create a free account to manage your notification settings.'`; assert `app/(app)/settings/(tabs)/notifications/page.tsx` contains `isDemoCompany` and `readOnly={isDemoCompany(companyId)}`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/settings/demo-readonly-forms.test.ts</automated>
  </verify>
  <done>NotificationsForm accepts readOnly, disables every Switch/Button via the fieldset cascade, and shows the footer note when true; the notifications page wires isDemoCompany(companyId) into it.</done>
  <acceptance_criteria>
    <item>`grep "readOnly?: boolean" components/settings/notifications-form.tsx` matches.</item>
    <item>`grep "fieldset disabled={readOnly}" components/settings/notifications-form.tsx` matches.</item>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire readOnly/canManage into Company and Team tabs (reuse existing props, zero new component code)</name>
  <read_first>@app/(app)/settings/(tabs)/company/page.tsx @app/(app)/settings/(tabs)/team/page.tsx @components/settings/company-info-form.tsx @components/settings/team-section.tsx @lib/demo/config.ts @tests/unit/settings/demo-readonly-forms.test.ts</read_first>
  <files>app/(app)/settings/(tabs)/company/page.tsx, app/(app)/settings/(tabs)/team/page.tsx, tests/unit/settings/demo-readonly-forms.test.ts</files>
  <action>
    1. In `app/(app)/settings/(tabs)/company/page.tsx`: import `isDemoCompany` from `@/lib/demo/config`; change `<CompanyInfoForm company={company} />` to `<CompanyInfoForm company={company} readOnly={isDemoCompany(company.id)} />` — the `company` object returned by `getCompanySettings()` already carries `.id`, no extra lookup needed.
    2. In `app/(app)/settings/(tabs)/team/page.tsx`: import `isDemoCompany` from `@/lib/demo/config`; change `const canManage = role === 'owner' || role === 'admin'` to `const canManage = !isDemoCompany(companyId) && (role === 'owner' || role === 'admin')` — forces every manage control off for the demo company regardless of the resolved role, reusing `TeamSection`'s existing `canManage` gate and its existing role-label-pill fallback verbatim (no new markup, no "(demo)" badge — matches UI-SPEC §3 exactly).
    3. Extend `tests/unit/settings/demo-readonly-forms.test.ts` with 2 more assertions: `app/(app)/settings/(tabs)/company/page.tsx` contains `readOnly={isDemoCompany(company.id)}`; `app/(app)/settings/(tabs)/team/page.tsx` contains `!isDemoCompany(companyId) && (role === 'owner' || role === 'admin')`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/settings/demo-readonly-forms.test.ts</automated>
  </verify>
  <done>Company and Team tab pages pass demo-aware readOnly/canManage into their existing, otherwise-unmodified form components.</done>
  <acceptance_criteria>
    <item>`grep "readOnly={isDemoCompany(company.id)}" "app/(app)/settings/(tabs)/company/page.tsx"` matches.</item>
    <item>`grep "canManage = !isDemoCompany(companyId)" "app/(app)/settings/(tabs)/team/page.tsx"` matches.</item>
  </acceptance_criteria>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/settings/demo-readonly-forms.test.ts` passes, proving all 3 exposed tab pages wire `isDemoCompany` into their form components' existing (Company: `readOnly`, Team: `canManage`) or newly-added (Notifications: `readOnly`) read-only props. Live-browser proof that controls are actually absent/disabled happens in Plan 04, which depends on this plan.
</verification>

<success_criteria>
For the demo company: Company tab fields are disabled with a footer note, Team tab shows role pills with no Invite/manage controls, Notifications tab fields are disabled with a footer note. For a real tenant: all 3 tabs behave exactly as before this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/181-real-product-cutover-verification/181-02-SUMMARY.md`
</output>
