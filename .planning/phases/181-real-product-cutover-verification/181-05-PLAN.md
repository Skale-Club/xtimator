---
phase: 181-real-product-cutover-verification
plan: 05
type: execute
wave: 3
depends_on: ["181-04"]
files_modified:
  - "components/landing/hero-section.tsx"
  - "components/landing/final-cta-section.tsx"
  - "components/landing/landing-footer.tsx"
  - "app/demo/page.tsx"
  - "app/demo/layout.tsx"
  - "app/demo/dashboard/page.tsx"
  - "app/demo/dashboard/loading.tsx"
  - "app/demo/clients/page.tsx"
  - "app/demo/clients/loading.tsx"
  - "app/demo/projects/page.tsx"
  - "app/demo/projects/loading.tsx"
  - "app/demo/price-book/page.tsx"
  - "app/demo/price-book/loading.tsx"
  - "components/demo/demo-nav.tsx"
  - "DEMO-WORKSPACE.md"
  - "tests/unit/demo/demo-cutover.test.ts"
autonomous: true
requirements: [CUTOVER-01, CUTOVER-02]
must_haves:
  truths:
    - "The 3 landing 'See Demo' links point at /demo/entry, the verified Phase 180 handoff, not the retired standalone /demo index."
    - "The standalone /demo/* UI no longer exists in the repository except app/demo/entry/route.ts (Phase 180's handoff route, untouched) and components/demo/demo-banner.tsx (the in-app read-only banner, untouched)."
    - "DEMO-WORKSPACE.md accurately describes the host-isolated flow, the DEMO_APP_ORIGIN env var, DNS/Coolify domain requirements for demo.xtimator.com, local dev setup, and states production is GitHub Actions -> Docker/GHCR -> Coolify (not Vercel)."
  artifacts:
    - path: "components/landing/hero-section.tsx"
      provides: "href=\"/demo/entry\" (was /demo)"
    - path: "DEMO-WORKSPACE.md"
      provides: "rewritten host-isolation documentation replacing the stale pre-Phase-180 architecture description"
    - path: "tests/unit/demo/demo-cutover.test.ts"
      provides: "static-source proof of the CTA swap, the dead-file deletion, and doc accuracy markers"
  key_links:
    - from: "components/landing/hero-section.tsx"
      to: "app/demo/entry/route.ts"
      via: "href=\"/demo/entry\""
      pattern: "href=\"/demo/entry\""
---

<objective>
Cut the public demo entry over to the verified product-native flow (landing CTAs -> `/demo/entry`), delete the retired standalone `/demo/*` UI, and rewrite `DEMO-WORKSPACE.md` to describe the current host-isolated architecture.

Purpose: this is the explicit "swap and remove" step the user described before Phase 180 even started — gated on Plan 04's browser-verification passing first ("preserve the current demo until the new flow passes tests; only then swap /demo and remove app/demo/*").
Output: the landing page's only demo entry point is the verified handoff route; `app/demo/` contains only the Phase 180 handoff route; documentation matches reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-real-product-cutover-verification/181-CONTEXT.md
@.planning/phases/181-real-product-cutover-verification/181-RESEARCH.md
@CLAUDE.md
@DEMO-WORKSPACE.md
@app/demo/entry/route.ts
@lib/demo/session.ts
@lib/demo/config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Switch landing CTAs to /demo/entry</name>
  <read_first>@components/landing/hero-section.tsx @components/landing/final-cta-section.tsx @components/landing/landing-footer.tsx</read_first>
  <files>components/landing/hero-section.tsx, components/landing/final-cta-section.tsx, components/landing/landing-footer.tsx, tests/unit/demo/demo-cutover.test.ts</files>
  <behavior>
    - None of the 3 landing files contain the bare attribute `href="/demo"` after this task.
    - All 3 contain `href="/demo/entry"`.
  </behavior>
  <action>
    1. `components/landing/hero-section.tsx` line 258: `<Link href="/demo">See Demo</Link>` -> `<Link href="/demo/entry">See Demo</Link>`. No other change (label, button variant, className all stay identical).
    2. `components/landing/final-cta-section.tsx` line 51: `<Link href="/demo">See Demo</Link>` -> `<Link href="/demo/entry">See Demo</Link>`.
    3. `components/landing/landing-footer.tsx` line 62: `<Link href="/demo" className="transition-colors hover:text-white">See Demo</Link>` -> `<Link href="/demo/entry" className="transition-colors hover:text-white">See Demo</Link>`.
    4. Write the RED test FIRST in `tests/unit/demo/demo-cutover.test.ts` (static-source-guard pattern, `readFileSync` + regex): for each of the 3 files, assert the source does NOT match `/href="\/demo"/` (this regex requires the closing quote immediately after `/demo`, so it correctly does not match `href="/demo/entry"`) and DOES match `/href="\/demo\/entry"/`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/demo/demo-cutover.test.ts</automated>
  </verify>
  <done>All 3 landing "See Demo" links point at /demo/entry; the static regression test passes.</done>
  <acceptance_criteria>
    <item>`grep -c 'href="/demo"' components/landing/hero-section.tsx components/landing/final-cta-section.tsx components/landing/landing-footer.tsx` returns 0 total matches.</item>
    <item>`grep -c 'href="/demo/entry"' components/landing/hero-section.tsx components/landing/final-cta-section.tsx components/landing/landing-footer.tsx` returns 3 total matches (1 per file).</item>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete the standalone /demo UI (keep the Phase 180 handoff route)</name>
  <read_first>@app/demo/entry/route.ts @components/demo/demo-banner.tsx @tests/unit/demo/demo-cutover.test.ts</read_first>
  <files>app/demo/page.tsx, app/demo/layout.tsx, app/demo/dashboard/page.tsx, app/demo/dashboard/loading.tsx, app/demo/clients/page.tsx, app/demo/clients/loading.tsx, app/demo/projects/page.tsx, app/demo/projects/loading.tsx, app/demo/price-book/page.tsx, app/demo/price-book/loading.tsx, components/demo/demo-nav.tsx, tests/unit/demo/demo-cutover.test.ts</files>
  <action>
    1. Delete exactly these 11 files:
       ```
       app/demo/page.tsx
       app/demo/layout.tsx
       app/demo/dashboard/page.tsx
       app/demo/dashboard/loading.tsx
       app/demo/clients/page.tsx
       app/demo/clients/loading.tsx
       app/demo/projects/page.tsx
       app/demo/projects/loading.tsx
       app/demo/price-book/page.tsx
       app/demo/price-book/loading.tsx
       components/demo/demo-nav.tsx
       ```
       Use `git rm` (or delete + `git add -u`) so the removal is tracked. After deletion, `app/demo/dashboard/`, `app/demo/clients/`, `app/demo/projects/`, `app/demo/price-book/` should be empty directories (git does not track empty directories — this is expected).
    2. Do NOT touch `app/demo/entry/route.ts` (Phase 180's handoff route — imports only `lib/demo/session.ts`, has zero dependency on anything deleted above) or `components/demo/demo-banner.tsx` (the read-only banner rendered inside the real app shell at `app/(app)/layout.tsx:245` — a DIFFERENT file with a similarly-named sibling; verify you are deleting `demo-nav.tsx` and not `demo-banner.tsx` before running the delete).
    3. Extend `tests/unit/demo/demo-cutover.test.ts` with `existsSync` assertions (from `node:fs`): each of the 11 paths above returns `false`; `app/demo/entry/route.ts` and `components/demo/demo-banner.tsx` both return `true`.
    4. `lib/seo/route-policy.ts`'s `PRIVATE_ROUTE_PREFIXES` array already includes `/demo` as a `startsWith` prefix match — this still correctly covers `/demo/entry` after this deletion. Do not edit that file; verify only (no code change needed).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/demo/demo-cutover.test.ts && npx tsc --noEmit -p tsconfig.ci.json</automated>
  </verify>
  <done>app/demo/ contains only entry/route.ts; components/demo/ contains only demo-banner.tsx; typecheck is green (no orphaned imports from the deletion).</done>
  <acceptance_criteria>
    <item>`find app/demo -type f` outputs exactly `app/demo/entry/route.ts`.</item>
    <item>`find components/demo -type f` outputs exactly `components/demo/demo-banner.tsx`.</item>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite DEMO-WORKSPACE.md for the host-isolated architecture</name>
  <read_first>@DEMO-WORKSPACE.md @app/demo/entry/route.ts @lib/demo/session.ts @lib/demo/config.ts @playwright.config.ts @CLAUDE.md</read_first>
  <files>DEMO-WORKSPACE.md, tests/unit/demo/demo-cutover.test.ts</files>
  <action>
    Rewrite `DEMO-WORKSPACE.md` in place (do not create a second file) to replace every stale claim with the current architecture. At minimum, the rewrite must cover:
    1. **How it works (3-hop flow)**: the landing page's "See Demo" links point at `/demo/entry` (apex). On the apex host, `proxy.ts` classifies the request via `classifyDemoEntryRequest()` (`lib/demo/session.ts`) and 303-redirects to the demo host's `/demo/entry`, touching no Supabase/cookie state. On the demo host, `app/demo/entry/route.ts` calls `establishDemoSession()`, which creates or repairs a host-only session for the dedicated demo user (`DEMO_USER_EMAIL`), pins the deterministic demo company (`DEMO_COMPANY_ID`) via a host-only `active_company_id` cookie, and redirects to the real `/dashboard`. Remove the stale claim `"app/demo/route.ts programmatically signs the visitor in"` — that route does not exist.
    2. **`DEMO_APP_ORIGIN` env var**: server-only, validated by `getDemoAppOrigin()` in `lib/demo/config.ts`. Local dev value: `http://demo.localhost:9633` (matches `playwright.config.ts`'s default and the dev server port; no `/etc/hosts` entry needed — Chromium resolves any `*.localhost` name to loopback automatically). Production value: `https://demo.xtimator.com` (the only https hostname `getDemoAppOrigin()` accepts).
    3. **Supabase Auth redirect allow-list**: note that both `http://demo.localhost:9633` (dev) and `https://demo.xtimator.com` (prod) must be present in the Supabase project's Auth redirect allow-list alongside the apex origin, or `establishDemoSession()`'s magic-link `verifyOtp()` flow will fail.
    4. **DNS / Coolify domain setup for `demo.xtimator.com`** (operator action — document requirements, do not attempt to perform them, per REQUIREMENTS.md's Out-of-Scope table): a CNAME/A record for `demo.xtimator.com` must point at the same Coolify-managed origin as `xtimator.com`; the Coolify app (UUID `cf1cqh0bq8jyw91e78tcw8c6`) needs `demo.xtimator.com` added as an additional domain so both hostnames route to the same running container; `DEMO_APP_ORIGIN=https://demo.xtimator.com` must be set as a Coolify runtime env var.
    5. **Local dev setup**: set `DEMO_APP_ORIGIN=http://demo.localhost:9633` (or the project's configured dev port) in `.env.local`; visit `http://demo.localhost:<port>/demo/entry` directly, or click "See Demo" from `http://localhost:<port>` which now links to `/demo/entry` on the apex and hands off automatically.
    6. **Production topology correction**: explicitly state production is **GitHub Actions -> Docker/GHCR -> Coolify** (self-hosted), matching `CLAUDE.md` verbatim — remove every reference implying Vercel is a real deployment target for this feature.
    7. **Settings tab exposure** (new in this phase): demo sessions see a real `/settings` with exactly Company, Team, and Notifications tabs; every other tab (Account, Estimates, Plans, Message Template, Knowledge, Integrations, and their sub-pages) redirects to `/settings/company`. Company/Team/Notifications render fully read-only (disabled fields/switches, no Invite button, explanatory footer notes).
    8. **Read-only enforcement** section: keep the existing content describing the RLS + app-guard defense-in-depth (`lib/demo/guard.ts`) — this is unchanged Phase 180 behavior, only re-verify every file/route path it names still exists at that exact path.
    9. **Implementation status**: replace the stale "Phase 1-4" checklist with a summary noting Phase 180 (isolated host-only session + deny-write enforcement) and Phase 181 (real-product settings exposure + landing cutover + dead-code removal) are both complete.
    10. Update the seed-script section (deterministic demo company id, `npm run db:seed:demo` / `--reset` / `--dry-run`) — this part of the current doc is still accurate, keep it as-is.
    Every file/route path named in the rewrite must be re-verified to exist at that exact path before finalizing (do not carry forward the current doc's incorrect `app/demo/route.ts` reference).
    Extend `tests/unit/demo/demo-cutover.test.ts` with grep-based structural assertions on `DEMO-WORKSPACE.md`: does NOT contain `app/demo/route.ts`; DOES contain `demo.xtimator.com`; DOES contain `app/demo/entry/route.ts`; DOES contain `DEMO_APP_ORIGIN`; DOES contain `GitHub Actions`; does NOT contain `Coolify / Vercel`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/demo/demo-cutover.test.ts</automated>
  </verify>
  <done>DEMO-WORKSPACE.md describes the current host-isolated architecture with no stale file/route references; the structural grep assertions pass. Full narrative accuracy is a manual review item (VALIDATION.md: CUTOVER-02 has no fully-automatable doc-accuracy test).</done>
  <acceptance_criteria>
    <item>`grep -c "app/demo/route.ts" DEMO-WORKSPACE.md` returns 0.</item>
    <item>`grep -c "demo.xtimator.com" DEMO-WORKSPACE.md` returns at least 1.</item>
  </acceptance_criteria>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/demo/demo-cutover.test.ts` passes (CTA swap + dead-file deletion + doc structural markers), and `npx tsc --noEmit -p tsconfig.ci.json` is clean (no orphaned imports from the deletion). Before this plan runs, confirm Plan 04's full 3-project Playwright run (`--project=chromium --project=mobile-safari --project=mobile-chrome`) is green — this plan is the explicit "only then swap and remove" step gated on that.
</verification>

<success_criteria>
The landing page's only demo entry point is `/demo/entry`; `app/demo/` contains only the Phase 180 handoff route; `DEMO-WORKSPACE.md` accurately documents the current architecture and correctly states production is Coolify, not Vercel.
</success_criteria>

<output>
After completion, create `.planning/phases/181-real-product-cutover-verification/181-05-SUMMARY.md`
</output>
