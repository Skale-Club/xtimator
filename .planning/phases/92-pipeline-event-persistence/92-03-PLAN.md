---
phase: 92-pipeline-event-persistence
plan: 03
type: execute
wave: 3
depends_on: ["92-01", "92-02"]
files_modified:
  - lib/actions/recording.ts
  - lib/inngest/functions/transcribe-audio.ts
  - lib/inngest/functions/analyze-photos.ts
  - lib/inngest/functions/generate-estimate.ts
autonomous: true
requirements: [EVENT-02, EVENT-03, EVENT-04]

must_haves:
  truths:
    - "save_recording emits a terminal pipeline_event (succeeded after insert, failed in error branches) for both audio and text recordings"
    - "transcribe / analyze / generate_estimate each emit started + terminal (succeeded/failed via onFailure) pipeline_events with duration_ms and provider"
    - "generate_estimate succeeded path also emits a preview_redirect succeeded marker once estimate_id is resolved"
    - "The existing estimate_activity recording_added write at recording.ts:105-110 still fires unchanged (EVENT-04)"
  artifacts:
    - path: "lib/inngest/functions/transcribe-audio.ts"
      provides: "recordPipelineEvent calls at started/succeeded/onFailure with step='transcribe', provider='openrouter'"
      contains: "recordPipelineEvent"
    - path: "lib/inngest/functions/analyze-photos.ts"
      provides: "recordPipelineEvent calls with step='analyze', provider='openrouter'"
      contains: "recordPipelineEvent"
    - path: "lib/inngest/functions/generate-estimate.ts"
      provides: "recordPipelineEvent calls with step='generate_estimate' + preview_redirect terminal marker"
      contains: "preview_redirect"
    - path: "lib/actions/recording.ts"
      provides: "save_recording events in createRecording + createTextRecording; recording_added write untouched"
      contains: "save_recording"
  key_links:
    - from: "lib/inngest/functions/*.ts"
      to: "lib/observability/pipeline-events.ts"
      via: "void recordPipelineEvent(...) (fire-and-forget, off hot path)"
      pattern: "recordPipelineEvent"
    - from: "lib/actions/recording.ts createRecording"
      to: "estimate_activity recording_added"
      via: "UNCHANGED existing insert (EVENT-04)"
      pattern: "event_type: 'recording_added'"
---

<objective>
Instrument the 6 server-side step boundaries with `recordPipelineEvent()` (EVENT-02), capturing started/terminal/failed events with timing, provider, and the threaded `attemptId`/`inputType` (EVENT-03), plus the `preview_redirect` terminal marker. Confirm the EVENT-04 `recording_added` write is untouched.

Purpose: This is the actual observability payload of the phase — every step transition durably recorded. Turns `instrumentation-presence.test.ts` and `event04-regression.test.ts` GREEN.
Output: instrumentation in `recording.ts` + the 3 Inngest functions; EVENT-04 regression confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-pipeline-event-persistence/92-RESEARCH.md
@tests/unit/observability/instrumentation-presence.test.ts
@tests/unit/observability/event04-regression.test.ts
@lib/actions/recording.ts

<interfaces>
From lib/observability/pipeline-events.ts (Wave 1 — call this at every boundary):
```typescript
export function recordPipelineEvent(ev: PipelineEventInput): Promise<void>  // best-effort; void it on hot path
// step: 'save_recording'|'transcribe'|'analyze'|'generate_estimate'|'preview_redirect'
// status: 'started'|'succeeded'|'failed'
// inputType: 'recording'|'photo'|'manual_text'
```
Exact file:line sites + in-scope fields per step are in RESEARCH.md §"Instrumentation Seam" (lines 138-174). Follow the `void notify(...)` fire-and-forget pattern already in all 3 Inngest functions.
Server fallback (D-08): `const attemptId = payload.attemptId ?? crypto.randomUUID()` at each Inngest site.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument save_recording in recording.ts (preserve EVENT-04)</name>
  <files>lib/actions/recording.ts</files>
  <action>
    Add `save_recording` terminal events to BOTH `createRecording` (audio) and `createTextRecording` (text). Import `recordPipelineEvent` from `@/lib/observability/pipeline-events`. Additive only — do NOT remove or alter the `estimate_activity` `recording_added` insert (L105-110, D-10) or the `description_added` insert.

    1. To thread lineage, add optional params `attemptId?: string` and (for createRecording) keep `inputType` implicit: `createRecording` → `inputType:'recording'`, `createTextRecording` → `inputType:'manual_text'`. Default `attemptId` to `crypto.randomUUID()` (`node:crypto`) when absent (D-08). (Callers may not pass one yet — that is fine; the fallback guarantees an event.)
    2. `createRecording`: capture `const t0 = Date.now()` before the insert. In the error branch (L87-89) `void recordPipelineEvent({ attemptId, inputType:'recording', step:'save_recording', status:'failed', companyId: company.id, projectId, errorMessage:'Failed to create recording', durationMs: Date.now()-t0 })` then return as before. On success (after L110, before `revalidatePath`) `void recordPipelineEvent({ attemptId, inputType:'recording', step:'save_recording', status:'succeeded', companyId: company.id, projectId, estimateId: null, durationMs: Date.now()-t0, provider: null })`.
    3. `createTextRecording`: same shape with `inputType:'manual_text'` — `failed` in the L51-53 error branch, `succeeded` after the activity insert (L61). `provider: null`, single terminal row (save_recording is synchronous, no started row needed per D-03 collapse allowance).
    4. CRITICAL: the `recording_added` insert (L105-110) and `description_added` insert (L56-61) stay byte-for-byte — the new pipeline_events write is an ADDITIONAL insert into a DIFFERENT table.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/observability/event04-regression.test.ts --reporter=basic</automated>
  </verify>
  <done>`event04-regression.test.ts` GREEN (recording_added still fires). `save_recording` succeeded/failed events emitted in both recording functions. `tsc` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument transcribe + analyze Inngest functions</name>
  <files>lib/inngest/functions/transcribe-audio.ts, lib/inngest/functions/analyze-photos.ts</files>
  <action>
    Per RESEARCH §"Instrumentation Seam" (transcribe + analyze tables). Import `recordPipelineEvent`. Use `void recordPipelineEvent(...)` (fire-and-forget, off the hot path). At each function, `const attemptId = event.data.attemptId ?? crypto.randomUUID()` (D-08) and `const t0 = Date.now()`.

    1. `transcribe-audio.ts`:
       - started: top of handler (~L70) — `void recordPipelineEvent({ attemptId, inputType: event.data.inputType ?? 'recording', step:'transcribe', status:'started', companyId, recordingId-derived projectId/userId via loadCompanyForRecording })`.
       - succeeded: after `save-transcript` step (~L97-98) — same identity + `status:'succeeded'`, `provider:'openrouter'`, `durationMs: Date.now()-t0`.
       - failed: in the existing `onFailure` handler (L47-67, which already loads companyId/userId) — `status:'failed'`, `errorMessage: String(error)`, `errorCode` parsed from the error if HTTP-shaped else null, `inputType:'recording'`, `provider:'openrouter'`.
       Extend `loadCompanyForRecording` (L19-38) select if needed to also yield `project_id`/`user_id` for the event fields (additive select only).
    2. `analyze-photos.ts`:
       - started: top of handler (~L80-82) — `step:'analyze'`, `status:'started'`, `companyId`, `projectId`, `userId` via existing `loadOwnerUserId` (L21-33), `inputType: event.data.inputType ?? 'photo'`.
       - succeeded: after `record-usage` step (~L136) — `status:'succeeded'`, `provider:'openrouter'`, `durationMs: Date.now()-t0`.
       - failed: in `onFailure` (L58-78, already loads userId) — `status:'failed'`, `errorMessage`, `attemptId` from payload (now present after Plan 02), `step:'analyze'`.
    Provider is `'openrouter'` for BOTH (RESEARCH Pitfall 4 — transcribe/analyze route through openrouter-client, not openai).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/observability/instrumentation-presence.test.ts --reporter=basic</automated>
  </verify>
  <done>transcribe + analyze each call `recordPipelineEvent` at started/succeeded/onFailure with the right `step`/`status`/`provider` literals. Presence test passes its transcribe+analyze assertions. `tsc` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Instrument generate_estimate (+ preview_redirect marker) and run phase gate</name>
  <files>lib/inngest/functions/generate-estimate.ts</files>
  <action>
    Per RESEARCH §"Instrumentation Seam" (generate table) + Open Question 3 (preview_redirect). Import `recordPipelineEvent`; `void` each call; `const attemptId = event.data.attemptId ?? crypto.randomUUID()`; `const t0 = Date.now()`.
    1. started: ~L64-66 — `step:'generate_estimate'`, `status:'started'`, `companyId`, `projectId`, `userId` via `loadOwnerUserId` (L21), `inputType: event.data.inputType ?? 'manual_text'`.
    2. succeeded: after `record-usage` (~L82) — `status:'succeeded'`, `durationMs: Date.now()-t0`. Resolve `estimateId` from `result` if it carries the new estimate id, else leave null (do NOT add an extra query if not trivially in scope). `provider`: read from the AI config if readily available, ELSE `null` (RESEARCH Open Question 2 — provider is nullable and not load-bearing here).
    3. preview_redirect marker: IMMEDIATELY after the generate succeeded event, in the SAME succeeded path, `void recordPipelineEvent({ attemptId, inputType, step:'preview_redirect', status:'succeeded', companyId, projectId, estimateId, provider:null, durationMs:null })`. This is the server-side logical terminal "reached preview" marker since the client redirect is non-instrumentable (D-04). Document this in a code comment.
    4. failed: in `onFailure` (L42-62) — `step:'generate_estimate'`, `status:'failed'`, `errorMessage`, `errorCode` if parseable, `attemptId` (present on payload).
    5. PHASE GATE: after instrumentation, run the full suite + typecheck. All 5 Wave-0 test files must now be GREEN.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>generate_estimate emits started/succeeded/failed + a preview_redirect succeeded marker. Full `npm test` GREEN (all 5 observability tests + existing suite) and `tsc --noEmit` clean — phase gate passed.</done>
</task>

</tasks>

<verification>
- `npm test` — full suite GREEN, including all 5 `tests/unit/observability/` files.
- `npx tsc --noEmit` clean.
- EVENT-04: `recording_added` insert unchanged (regression test GREEN).
- Manual UAT (non-automatable, post-merge): trigger one capture per input type, then `SELECT * FROM pipeline_events ORDER BY created_at DESC` as super-admin and confirm rows per step with correct attempt_id/input_type/step/status/duration_ms.
</verification>

<success_criteria>
- All 6 server-side step boundaries call `recordPipelineEvent` with correct step/status/provider.
- preview_redirect terminal marker emitted from the generate succeeded path.
- EVENT-04 preserved.
- Full suite + tsc clean (phase gate).
</success_criteria>

<output>
After completion, create `.planning/phases/92-pipeline-event-persistence/92-03-SUMMARY.md`.
</output>
