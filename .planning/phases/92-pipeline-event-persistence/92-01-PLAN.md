---
phase: 92-pipeline-event-persistence
plan: 01
type: execute
wave: 1
depends_on: ["92-00"]
files_modified:
  - lib/observability/pipeline-events.ts
autonomous: true
requirements: [EVENT-01, EVENT-02, EVENT-03]

must_haves:
  truths:
    - "recordPipelineEvent() inserts a D-02-shaped row via requireServiceClient()"
    - "A write failure is swallowed (console.warn) and never throws / never rejects the caller"
    - "retry_count is computed by counting prior events for the same attempt_id + step"
  artifacts:
    - path: "lib/observability/pipeline-events.ts"
      provides: "Best-effort recordPipelineEvent() helper + PipelineEventInput type + computeRetryCount"
      min_lines: 50
      exports: ["recordPipelineEvent", "PipelineEventInput", "PipelineStep", "PipelineStatus", "PipelineInputType"]
  key_links:
    - from: "lib/observability/pipeline-events.ts"
      to: "pipeline_events table"
      via: "requireServiceClient().from('pipeline_events').insert(...)"
      pattern: "\\.from\\(['\"]pipeline_events['\"]\\)\\.insert"
    - from: "lib/observability/pipeline-events.ts"
      to: "lib/supabase/service.ts"
      via: "requireServiceClient import"
      pattern: "requireServiceClient"
---

<objective>
Create the single best-effort `recordPipelineEvent()` helper that every instrumentation site calls. It maps the camelCase `PipelineEventInput` to the snake_case D-02 row, computes `retry_count`, and writes via `requireServiceClient()` — wrapped in try/catch so it never throws or rejects the caller (D-06).

Purpose: One shape, one place to harden (D-04). Turns `tests/unit/observability/record-pipeline-event.test.ts` GREEN and unblocks all instrumentation (Wave 2).
Output: `lib/observability/pipeline-events.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-pipeline-event-persistence/92-RESEARCH.md
@.planning/phases/92-pipeline-event-persistence/92-VALIDATION.md
@tests/unit/observability/record-pipeline-event.test.ts

<interfaces>
From lib/supabase/service.ts:
```typescript
export function requireServiceClient(): SupabaseClient  // RLS-bypassing; throws if env vars absent
```
The full helper signature + body is specified verbatim in RESEARCH.md §"Helper shape (recommended)" (lines 176-225) and §"retry_count Mechanics" (lines 253-264). Implement exactly that.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement recordPipelineEvent() best-effort helper</name>
  <files>lib/observability/pipeline-events.ts</files>
  <behavior>
    - Maps camelCase PipelineEventInput → snake_case columns and inserts into `pipeline_events`.
    - When `.insert()` rejects, the call RESOLVES (no throw) and `console.warn('[recordPipelineEvent] ...')` fires (D-06).
    - retry_count = count of prior rows WHERE attempt_id = ev.attemptId AND step = ev.step AND status = ev.status (0 first execution, 1 first retry). `started` rows return 0.
    - Null-coalesces all optional fields to `null` before insert.
  </behavior>
  <action>
    Create `lib/observability/pipeline-events.ts` exactly per RESEARCH.md §"Helper shape" + §"retry_count Mechanics":
    1. Export types: `PipelineStep` (`'save_recording'|'transcribe'|'analyze'|'generate_estimate'|'preview_redirect'`), `PipelineStatus` (`'started'|'succeeded'|'failed'`), `PipelineInputType` (`'recording'|'photo'|'manual_text'`), and `PipelineEventInput` (interface with `attemptId`, `inputType`, `step`, `status` required; `companyId/projectId/estimateId/userId` optional nullable; `provider` optional `'openai'|'openrouter'|'anthropic'|null`; `errorMessage/errorCode/durationMs` optional nullable).
    2. `export async function recordPipelineEvent(ev: PipelineEventInput): Promise<void>` — wrap the ENTIRE body in try/catch. Inside: `const svc = requireServiceClient()`; `const retryCount = await computeRetryCount(svc, ev)`; then `await svc.from('pipeline_events').insert({ attempt_id, input_type, step, status, company_id, project_id, estimate_id, user_id, provider, error_message, error_code, duration_ms, retry_count })` with every optional `?? null`. In `catch (err)`: `console.warn('[recordPipelineEvent] swallowed write failure:', err)` and return — NEVER rethrow (D-06).
    3. Private `async function computeRetryCount(svc, ev)`: if `ev.status === 'started'` return 0; else run `svc.from('pipeline_events').select('id', { count: 'exact', head: true }).eq('attempt_id', ev.attemptId).eq('step', ev.step).eq('status', ev.status)` and `return count ?? 0`. Document the accepted TOCTOU race (diagnostic hint, not a billing key — RESEARCH §retry_count tradeoff).
    Import `requireServiceClient` from `@/lib/supabase/service`. Do NOT `await` is fine internally — callers will `void recordPipelineEvent(...)` on the hot path.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/observability/record-pipeline-event.test.ts --reporter=basic</automated>
  </verify>
  <done>`record-pipeline-event.test.ts` is GREEN: insert-shape, best-effort swallow (resolves + console.warn), and retry_count increment all pass. `tsc --noEmit` clean.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/observability/record-pipeline-event.test.ts` GREEN.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- `lib/observability/pipeline-events.ts` exports `recordPipelineEvent` + the three string-union types + `PipelineEventInput`.
- Best-effort guarantee proven by test (rejecting insert resolves + warns).
- retry_count computed via count query.
</success_criteria>

<output>
After completion, create `.planning/phases/92-pipeline-event-persistence/92-01-SUMMARY.md`.
</output>
