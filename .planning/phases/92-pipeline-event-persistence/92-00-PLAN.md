---
phase: 92-pipeline-event-persistence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/observability/pipeline-events-migration.test.ts
  - tests/unit/observability/record-pipeline-event.test.ts
  - tests/unit/observability/instrumentation-presence.test.ts
  - tests/unit/observability/input-type-threading.test.ts
  - tests/unit/observability/event04-regression.test.ts
  - supabase/migrations/20260529000001_phase92_pipeline_events.sql
  - scripts/check-pipeline-events-table.mjs
  - types/database.types.ts
autonomous: true
requirements: [EVENT-01, EVENT-02, EVENT-03, EVENT-04]

must_haves:
  truths:
    - "The pipeline_events table exists on the remote DB with deny-all RLS and a super-admin SELECT policy"
    - "A post-push smoke check confirms pipeline_events is queryable on remote (to_regclass not null) before later waves rely on it"
    - "All 5 Nyquist test files exist and run (RED until impl lands in later waves)"
    - "types/database.types.ts exposes a pipeline_events Row/Insert/Update block so the helper compiles"
  artifacts:
    - path: "supabase/migrations/20260529000001_phase92_pipeline_events.sql"
      provides: "pipeline_events DDL + deny-all RLS + super-admin SELECT + 4 indexes"
      contains: "CREATE TABLE IF NOT EXISTS public.pipeline_events"
    - path: "scripts/check-pipeline-events-table.mjs"
      provides: "Post-push smoke check: SELECT to_regclass('public.pipeline_events'), exits non-zero if null"
      contains: "to_regclass"
    - path: "tests/unit/observability/pipeline-events-migration.test.ts"
      provides: "Static SQL contract test for RLS/columns/indexes (EVENT-01)"
    - path: "tests/unit/observability/record-pipeline-event.test.ts"
      provides: "Helper shape + best-effort swallow + retry_count tests (EVENT-01/02/03)"
    - path: "tests/unit/observability/instrumentation-presence.test.ts"
      provides: "Static call-site presence test (EVENT-02)"
    - path: "tests/unit/observability/input-type-threading.test.ts"
      provides: "attemptId+inputType threading test (EVENT-03)"
    - path: "tests/unit/observability/event04-regression.test.ts"
      provides: "recording_added preservation test (EVENT-04)"
    - path: "types/database.types.ts"
      provides: "pipeline_events generated/hand-edited type block"
      contains: "pipeline_events:"
  key_links:
    - from: "supabase/migrations/20260529000001_phase92_pipeline_events.sql"
      to: "remote DB prmqgcrnpuvpzruyzvuv"
      via: "bunx supabase db push --db-url $DATABASE_URL"
      pattern: "ENABLE ROW LEVEL SECURITY"
    - from: "scripts/check-pipeline-events-table.mjs"
      to: "remote DB prmqgcrnpuvpzruyzvuv"
      via: "pg client SELECT to_regclass('public.pipeline_events')"
      pattern: "to_regclass"
    - from: "types/database.types.ts pipeline_events block"
      to: "lib/observability/pipeline-events.ts (Wave 1)"
      via: "Insert type used by recordPipelineEvent"
      pattern: "pipeline_events"
---

<objective>
Lay the Wave 0 foundation for Phase 92: create the five RED Nyquist test stubs/contracts named in VALIDATION.md, the `pipeline_events` migration SQL, apply it to the remote DB (with a post-push smoke check proving the table reached remote), and add the `pipeline_events` type block to `types/database.types.ts`.

Purpose: Tests are written RED first (Nyquist contract) and the table + types must exist before any helper or instrumentation can compile and turn them green. This unblocks every subsequent wave.
Output: 5 test files (RED), 1 migration applied to remote + smoke check, 1 type block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-pipeline-event-persistence/92-CONTEXT.md
@.planning/phases/92-pipeline-event-persistence/92-RESEARCH.md
@.planning/phases/92-pipeline-event-persistence/92-VALIDATION.md

# DDL is fully specified in RESEARCH.md §"Recommended DDL" — copy it verbatim.
# Smoke-check pattern to mirror (pg client + .env.local loader):
@scripts/apply-migration-86-01.mjs
# Mocking patterns to follow:
@tests/unit/inngest/transcribe-audio-job.test.ts
@tests/unit/api/transcribe-dispatch.test.ts

<interfaces>
<!-- requireServiceClient is the RLS-bypassing write client the helper will use. -->
From lib/supabase/service.ts:
```typescript
export function requireServiceClient(): SupabaseClient  // throws if env vars absent
```

<!-- Helper contract the Wave-1 file will export (tests import against this shape). -->
Target lib/observability/pipeline-events.ts (created Wave 1, mock it in tests now):
```typescript
export type PipelineStep = 'save_recording'|'transcribe'|'analyze'|'generate_estimate'|'preview_redirect'
export type PipelineStatus = 'started'|'succeeded'|'failed'
export type PipelineInputType = 'recording'|'photo'|'manual_text'
export interface PipelineEventInput {
  attemptId: string; inputType: PipelineInputType; step: PipelineStep; status: PipelineStatus
  companyId?: string|null; projectId?: string|null; estimateId?: string|null; userId?: string|null
  provider?: 'openai'|'openrouter'|'anthropic'|null
  errorMessage?: string|null; errorCode?: string|null; durationMs?: number|null
}
export function recordPipelineEvent(ev: PipelineEventInput): Promise<void>
```

<!-- Super-admin SELECT predicate (model: platform_admins migration L105-109). -->
EXISTS (SELECT 1 FROM public.platform_admins WHERE user_id = (SELECT auth.uid()))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration SQL + apply to remote + smoke-check table exists + add pipeline_events type block</name>
  <files>supabase/migrations/20260529000001_phase92_pipeline_events.sql, scripts/check-pipeline-events-table.mjs, types/database.types.ts</files>
  <action>
    1. Create `supabase/migrations/20260529000001_phase92_pipeline_events.sql` with the EXACT DDL from RESEARCH.md §"Recommended DDL" (lines 84-131). Non-negotiable contents (D-02, D-11):
       - `CREATE TABLE IF NOT EXISTS public.pipeline_events` with all 14 columns: `id uuid pk default gen_random_uuid()`, `attempt_id uuid NOT NULL`, `company_id uuid REFERENCES public.companies(id) ON DELETE CASCADE`, `project_id uuid` (no FK), `estimate_id uuid` (no FK), `user_id uuid` (no FK), `input_type text NOT NULL CHECK (input_type IN ('recording','photo','manual_text'))`, `step text NOT NULL CHECK (step IN ('save_recording','transcribe','analyze','generate_estimate','preview_redirect'))`, `status text NOT NULL CHECK (status IN ('started','succeeded','failed'))`, `error_message text`, `error_code text`, `provider text CHECK (provider IS NULL OR provider IN ('openai','openrouter','anthropic'))`, `duration_ms integer`, `retry_count integer NOT NULL default 0`, `created_at timestamptz NOT NULL default now()`.
       - `ALTER TABLE public.pipeline_events ENABLE ROW LEVEL SECURITY;`
       - ONE policy only: `CREATE POLICY "pipeline_events_select_super_admin" ON public.pipeline_events FOR SELECT TO authenticated USING (EXISTS (SELECT 1 FROM public.platform_admins WHERE user_id = (SELECT auth.uid())));`
       - NO INSERT/UPDATE/DELETE policies (deny-all; service role bypasses RLS — D-11).
       - 4 indexes: `pipeline_events_attempt_id` on `(attempt_id)`, `pipeline_events_company_created` on `(company_id, created_at DESC)`, `pipeline_events_created_at` on `(created_at DESC)`, `pipeline_events_status` on `(status)`.
       - Header comment noting the apply command and TEXT+CHECK choice (project avoids Postgres enums per STATE.md D-07/D-08). Do NOT add `updated_at` (append-only rows are never updated — RESEARCH note).
    2. Apply to the remote DB: `bunx supabase db push --db-url $env:DATABASE_URL` (DATABASE_URL is in `.env.local`; remote project `prmqgcrnpuvpzruyzvuv`, already linked). This is a HARD prerequisite (RESEARCH Pitfall 3 — the best-effort helper would silently swallow "relation does not exist" if the table is missing). `db push` errors non-zero on failure; do NOT proceed past a non-zero exit.
    3. Create `scripts/check-pipeline-events-table.mjs` as a one-off post-push smoke check, mirroring the `pg` client + `.env.local` loader pattern in `scripts/apply-migration-86-01.mjs` (L3-22: `import pg from 'pg'`; `loadEnv({ path: '.env.local' })`; force session-mode pooler `:6543`→`:5432`; `new pg.Client({ connectionString, ssl: { rejectUnauthorized: false } })`). The script runs `SELECT to_regclass('public.pipeline_events') AS reg`, and `process.exit(1)` (after `console.error`) if `reg` is null, else `console.log('pipeline_events present on remote')` and `process.exit(0)`. Always `await client.end()`. psql is NOT assumed present on this Windows box — Node `pg` is the deterministic gate. Run it: `node scripts/check-pipeline-events-table.mjs`. A non-zero exit means the table did NOT reach remote — STOP; later waves must not run against a missing table.
    4. Add the `pipeline_events` type block to `types/database.types.ts` under `Database.public.Tables`. Type-regen path: FIRST try `supabase gen types typescript --project-id prmqgcrnpuvpzruyzvuv --schema public > types/database.types.ts` (uses persisted SUPABASE_ACCESS_TOKEN PAT — Docker NOT required). If the CLI/PAT path fails, FALL BACK to hand-editing the `pipeline_events` `Row`/`Insert`/`Update` block (established no-Docker convention since Phase 19/24/38). The Insert block needs all 14 D-02 columns with `id?`, `company_id?/null`, `project_id?/null`, `estimate_id?/null`, `user_id?/null`, `error_message?/null`, `error_code?/null`, `provider?/null`, `duration_ms?/null`, `retry_count?`, `created_at?` optional; `attempt_id`, `input_type`, `step`, `status` required. Mirror the `admin_audit_log` block shape already in the file (Row/Insert/Update/Relationships).
  </action>
  <verify>
    <automated>node scripts/check-pipeline-events-table.mjs</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Migration file exists with full DDL; `supabase db push` reports success; `node scripts/check-pipeline-events-table.mjs` exits 0 (smoke check confirms pipeline_events exists on remote via `to_regclass` not null); `types/database.types.ts` contains a `pipeline_events:` key with Row/Insert/Update; `tsc --noEmit` is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 5 RED Nyquist test files</name>
  <files>tests/unit/observability/pipeline-events-migration.test.ts, tests/unit/observability/record-pipeline-event.test.ts, tests/unit/observability/instrumentation-presence.test.ts, tests/unit/observability/input-type-threading.test.ts, tests/unit/observability/event04-regression.test.ts</files>
  <action>
    Create all 5 test files under `tests/unit/observability/`. Import `{ describe, it, expect, vi }` explicitly from 'vitest' (tsc fails on globals — Phase 22 convention). Follow the static-source-read pattern from `tests/unit/inngest/transcribe-audio-job.test.ts` and the `makeSupabaseMock` table-switch pattern from `tests/unit/api/transcribe-dispatch.test.ts:52-103`.

    1. `pipeline-events-migration.test.ts` (EVENT-01) — `readFileSync` the migration SQL and assert: contains `ENABLE ROW LEVEL SECURITY`; contains a `FOR SELECT` policy with `platform_admins` + `auth.uid()`; contains all 14 column names; contains all 3 CHECK enums (`input_type`/`step`/`status` value lists); contains the 4 index names; asserts NO `FOR INSERT`/`FOR UPDATE`/`FOR DELETE ... TO authenticated` policy exists (deny-all).
    2. `record-pipeline-event.test.ts` (EVENT-01/02/03) — `vi.mock('@/lib/supabase/service')`. Assert: (a) `recordPipelineEvent` calls `.from('pipeline_events').insert(...)` with the snake_case D-02 shape mapped from camelCase input; (b) BEST-EFFORT — when `.insert()` rejects, `await recordPipelineEvent(...)` RESOLVES (does not throw) and `console.warn` was called (`vi.spyOn(console,'warn')`); (c) retry_count — when the mocked count query for `attempt_id+step` returns a prior count, the inserted row carries the incremented `retry_count`.
    3. `instrumentation-presence.test.ts` (EVENT-02) — `readFileSync` each of the 3 routes (`app/api/transcribe/route.ts`, `app/api/analyze-photos/route.ts`, `app/api/generate-estimate/route.ts`) and 3 Inngest functions (`lib/inngest/functions/{transcribe-audio,analyze-photos,generate-estimate}.ts`) and assert `recordPipelineEvent` appears with the expected `step` literal in each, plus that the relevant `status` literals (`started`/`succeeded`/`failed`) and `preview_redirect` (in generate-estimate) appear.
    4. `input-type-threading.test.ts` (EVENT-03) — assert (static source read or mocked fetch) that the photo + manual-text entrypoints (`components/projects/photos-input.tsx`, `components/projects/text-describe.tsx`, `components/workspace/ai-input-group/use-ai-input-submit.ts`) mint `crypto.randomUUID()` and send `attemptId` + `inputType` in their fetch bodies; that `components/capture/capture-recorder.tsx` sends `inputType: 'recording'` on its transcribe/generate dispatch; and that `AnalyzePhotosPayload` in `lib/inngest/events.ts` now declares `attemptId` and `inputType`.
    5. `event04-regression.test.ts` (EVENT-04) — reuse the `makeSupabaseMock` table-switch pattern; call `createRecording` from `lib/actions/recording.ts` with mocked supabase and assert `.from('estimate_activity').insert(...)` is still called with `event_type: 'recording_added'` (D-10 — the recording_added write must keep firing unchanged).

    These tests are RED now (helper/migration-as-impl/instrumentation/threading do not exist yet). Use `vi.mock('@/lib/observability/pipeline-events')` where needed so files compile before the Wave-1 source exists (Phase 12/18 Wave-0 scaffold convention).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/observability/ --reporter=basic</automated>
  </verify>
  <done>All 5 files exist, compile (tsc clean via prior task), and vitest runs them. They are expected RED (failing assertions) — confirm they FAIL for the right reason (missing impl), not import/compile errors.</done>
</task>

</tasks>

<verification>
- Migration applied to remote `prmqgcrnpuvpzruyzvuv` (table queryable).
- `node scripts/check-pipeline-events-table.mjs` exits 0 (smoke check: `to_regclass('public.pipeline_events')` not null).
- `npx tsc --noEmit` clean (type block + test files compile).
- `npx vitest run tests/unit/observability/` runs all 5 files (RED expected until later waves).
</verification>

<success_criteria>
- `supabase/migrations/20260529000001_phase92_pipeline_events.sql` exists with deny-all RLS + super-admin SELECT + 14 columns + 4 indexes and is pushed to remote.
- `scripts/check-pipeline-events-table.mjs` smoke check confirms the table exists on remote (exits 0).
- `types/database.types.ts` has a `pipeline_events` block.
- 5 RED test files exist under `tests/unit/observability/` and run.
- `tsc --noEmit` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/92-pipeline-event-persistence/92-00-SUMMARY.md`.
</output>
</content>
</invoke>
