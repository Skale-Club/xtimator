---
phase: 92-pipeline-event-persistence
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - lib/inngest/events.ts
  - components/projects/photos-input.tsx
  - components/projects/text-describe.tsx
  - components/workspace/ai-input-group/use-ai-input-submit.ts
  - components/capture/capture-recorder.tsx
  - app/api/transcribe/route.ts
  - app/api/analyze-photos/route.ts
  - app/api/generate-estimate/route.ts
autonomous: true
requirements: [EVENT-03]

must_haves:
  truths:
    - "Photo + manual-text entrypoints mint a stable attemptId (crypto.randomUUID, reused on retry) and send attemptId + inputType in the POST body"
    - "The recording entrypoint (capture-recorder) sends inputType:'recording' alongside its existing Phase 91 attemptId on the transcribe/generate dispatch"
    - "AnalyzePhotosPayload carries attemptId + inputType; Transcribe/Estimate payloads carry inputType"
    - "Each route forwards attemptId + inputType into its Inngest dispatch, defaulting inputType to manual_text when absent"
  artifacts:
    - path: "lib/inngest/events.ts"
      provides: "attemptId + inputType fields on AnalyzePhotosPayload; inputType on Transcribe + Estimate payloads"
      contains: "inputType?"
    - path: "components/projects/photos-input.tsx"
      provides: "attemptIdRef mint + {attemptId, inputType:'photo'} in fetch body"
    - path: "components/projects/text-describe.tsx"
      provides: "attemptIdRef mint + {attemptId, inputType:'manual_text'} in fetch body"
    - path: "components/workspace/ai-input-group/use-ai-input-submit.ts"
      provides: "attemptId mint + inputType in runGenerate fetch body"
    - path: "components/capture/capture-recorder.tsx"
      provides: "inputType:'recording' on the transcribe/generate dispatch payload(s) alongside existing attemptId"
      contains: "inputType"
  key_links:
    - from: "components/projects/photos-input.tsx"
      to: "/api/generate-estimate"
      via: "fetch body { attemptId, inputType }"
      pattern: "attemptId"
    - from: "components/capture/capture-recorder.tsx"
      to: "/api/transcribe"
      via: "fetch body { attemptId, inputType:'recording' }"
      pattern: "inputType"
    - from: "app/api/analyze-photos/route.ts"
      to: "AnalyzePhotosPayload dispatch"
      via: "inngest.send with attemptId + inputType"
      pattern: "attemptId"
---

<objective>
Thread `attemptId` + `inputType` lineage (EVENT-03) through the photo and manual-text entrypoints (which mint NOTHING today), the recording entrypoint (which already mints attemptId), and the three API routes, and extend the Inngest payload types so every downstream event row can carry input type and a stable per-attempt id.

Purpose: EVENT-03 requires all three input types captured and retries linked to the originating attempt. `AnalyzePhotosPayload` has no `attemptId` field at all (RESEARCH Pitfall 2). This plan establishes the lineage the Wave-3 instrumentation reads off the payloads.
Output: extended `lib/inngest/events.ts` + 3 entrypoint clients + recording entrypoint + 3 routes forwarding the fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-pipeline-event-persistence/92-RESEARCH.md
@tests/unit/observability/input-type-threading.test.ts
@tests/unit/capture/capture-attempt-lineage.test.ts

# Entrypoints (mint nothing today — copy the ensureAttempt pattern from capture-recorder L135-138):
@components/projects/photos-input.tsx
@components/projects/text-describe.tsx
@components/workspace/ai-input-group/use-ai-input-submit.ts
# Recording entrypoint (already mints attemptId — only add inputType to its dispatch payload(s)):
@components/capture/capture-recorder.tsx

<interfaces>
From lib/inngest/events.ts (current — to be extended):
```typescript
export type EstimateGeneratePayload = { companyId; projectId; requestId; language?; prompts?; attemptId? }
export type TranscribeAudioPayload  = { companyId; recordingId; storagePath; attemptId? }
export type AnalyzePhotosPayload    = { companyId; projectId; requestId }   // ← NO attemptId today
```
All new fields are OPTIONAL (`attemptId?`, `inputType?`) so older callers (MCP write.ts, capture-recorder) still compile — matches the Phase 91 `attemptId?` convention.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Inngest payload types with attemptId + inputType</name>
  <files>lib/inngest/events.ts</files>
  <action>
    In `lib/inngest/events.ts`:
    1. `AnalyzePhotosPayload` (L47-51) — ADD `attemptId?: string` and `inputType?: 'recording' | 'photo' | 'manual_text'`. Add a Phase-92 doc comment mirroring the Phase-91 attemptId comment on the other payloads.
    2. `TranscribeAudioPayload` (L36-45) — ADD `inputType?: 'recording' | 'photo' | 'manual_text'` (attemptId already present).
    3. `EstimateGeneratePayload` (L15-34) — ADD `inputType?: 'recording' | 'photo' | 'manual_text'` (attemptId already present).
    Keep all fields optional. Consider extracting a shared `PipelineInputType` import from `@/lib/observability/pipeline-events` to avoid drift — optional, but if you inline the union keep it identical to the helper's.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All three payload types declare `inputType?`; `AnalyzePhotosPayload` also declares `attemptId?`. `tsc` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mint + thread attemptId/inputType in the 3 client entrypoints + add inputType:'recording' to capture-recorder</name>
  <files>components/projects/photos-input.tsx, components/projects/text-describe.tsx, components/workspace/ai-input-group/use-ai-input-submit.ts, components/capture/capture-recorder.tsx</files>
  <action>
    For each entrypoint, mint a STABLE attemptId in a ref (mint once, reused on Retry — copy the `ensureAttempt` pattern from `capture-recorder.tsx:135-138`) and send `{ attemptId, inputType }` in the existing `/api/generate-estimate` fetch body. Do NOT change pipeline behavior — only add fields to the body.
    1. `photos-input.tsx` — add `const attemptIdRef = useRef<string | null>(null)` and an `ensureAttempt()` that mints `crypto.randomUUID()` once. In `handleGenerateFromPhotos` (L46-51), change the body to `JSON.stringify({ projectId, attemptId: ensureAttempt(), inputType: 'photo' })`.
    2. `text-describe.tsx` — same ref/ensureAttempt; in `handleTextGenerate` (L48-53) change body to `JSON.stringify({ projectId, attemptId: ensureAttempt(), inputType: 'manual_text' })`.
    3. `use-ai-input-submit.ts` — mint an attemptId (ref or useState, stable across the hook instance so a retry reuses it); in `runGenerate` (L65-69) change body to `JSON.stringify({ projectId, language: estimateLanguage, attemptId, inputType: 'manual_text' })`. (The header AI input is text/voice-driven; `manual_text` is the safe default per D-07 — the voice path keeps using capture-recorder's own attemptId.)
    4. `capture-recorder.tsx` — the recording entrypoint already mints `attemptId` (the `ensureAttempt` pattern at L135-138, threaded Phase 91). ADD one field — `inputType: 'recording'` — to its transcribe dispatch fetch body alongside the existing `attemptId` (and to the generate dispatch body too, if it dispatches one). One-line-per-payload change only; do NOT alter the attemptId minting/reuse logic. This is symmetry/explicitness — the Wave-3 server fallback `inputType ?? 'recording'` already covers EVENT-03, so this must NOT break the existing `tests/unit/capture/capture-attempt-lineage.test.ts`.
    `crypto.randomUUID()` is available in the browser (these are `'use client'` files).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/observability/input-type-threading.test.ts tests/unit/capture/capture-attempt-lineage.test.ts --reporter=basic</automated>
  </verify>
  <done>The 3 client entrypoints mint `crypto.randomUUID()` once and send `attemptId` + the correct `inputType`; `capture-recorder.tsx` sends `inputType:'recording'` on its transcribe (and generate, if present) dispatch alongside the existing attemptId. The threading test passes its entrypoint + recording assertions; `capture-attempt-lineage.test.ts` still GREEN. `tsc` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Routes read + forward attemptId/inputType into Inngest dispatch</name>
  <files>app/api/transcribe/route.ts, app/api/analyze-photos/route.ts, app/api/generate-estimate/route.ts</files>
  <action>
    Derive `inputType` at the boundary (D-07) and forward `attemptId` + `inputType` into each route's `inngest.send` dispatch payload. Additive only — do not alter existing dispatch behavior.
    1. `app/api/transcribe/route.ts` (~L102 dispatch) — set `inputType: 'recording'` on the `TranscribeAudioPayload` (attemptId already read/threaded from the body in Phase 91).
    2. `app/api/analyze-photos/route.ts` (~L89 dispatch) — read `attemptId` from the request body (currently NOT read; fall back to `crypto.randomUUID()` server-side if absent — D-08) and set `inputType: 'photo'` on the `AnalyzePhotosPayload`.
    3. `app/api/generate-estimate/route.ts` (~L113 dispatch) — read `attemptId` and `inputType` from the request body; forward both onto the `EstimateGeneratePayload`. Default `inputType` to `'manual_text'` when absent (safest for the text/MCP path — RESEARCH Open Question 1), and fall back `attemptId ?? crypto.randomUUID()` (D-08). Use Node's `crypto.randomUUID()` (`import { randomUUID } from 'node:crypto'` or `globalThis.crypto.randomUUID()`).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/observability/input-type-threading.test.ts --reporter=basic</automated>
  </verify>
  <done>Each route forwards `attemptId` + `inputType` into its dispatch; analyze-photos reads attemptId from the body; generate-estimate defaults inputType to `manual_text` and mints a fallback attemptId. `tsc` clean; threading test GREEN.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/observability/input-type-threading.test.ts` GREEN.
- `npx vitest run tests/unit/capture/capture-attempt-lineage.test.ts` still GREEN (recording entrypoint change is non-breaking).
- `npx tsc --noEmit` clean.
- No change to existing pipeline behavior — only additive payload fields.
</verification>

<success_criteria>
- `AnalyzePhotosPayload` has `attemptId?` + `inputType?`; Transcribe/Estimate payloads have `inputType?`.
- All three client entrypoints mint a stable attemptId and send `attemptId` + correct `inputType`.
- `capture-recorder.tsx` sends `inputType:'recording'` alongside its existing attemptId.
- All three routes forward the fields with the documented fallbacks.
</success_criteria>

<output>
After completion, create `.planning/phases/92-pipeline-event-persistence/92-02-SUMMARY.md`.
</output>
</content>
