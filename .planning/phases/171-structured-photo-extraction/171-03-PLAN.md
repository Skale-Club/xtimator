---
phase: 171-structured-photo-extraction
plan: 03
type: execute
wave: 2
depends_on:
  - "171-01"
files_modified:
  - lib/estimate/photo-context.ts
  - lib/services/generate-estimate.ts
  - tests/unit/estimate/photo-context.test.ts
autonomous: true
requirements:
  - PEXT-02
must_haves:
  truths:
    - "A pure serializer (lib/estimate/photo-context.ts) turns one photo row (ai_description + caption + optional ai_extraction) into the single string fed to photoDescriptions — description and caption exactly as today, plus a compact ' | Measurements: … | Materials: … | Damage: …' suffix when an extraction exists."
    - "A prose-only photo (ai_extraction NULL — every legacy photo, every fallback, kill-switch mode) produces BYTE-IDENTICAL output to today's 168-02 format ('Photo N (caption: X): desc' / 'Photo N: desc')."
    - "The serialized string flows through the EXISTING <photo_description>/sanitizeField wrapping in prompt-builder — no new prompt surface, no parallel unsanitized route (grep-provable)."
    - "Measurements serialize with unit + confidence ('220 sq ft (estimated)'), grouped by subject; empty extraction arrays contribute nothing (no dangling separators)."
  artifacts:
    - path: "lib/estimate/photo-context.ts"
      provides: "serializePhotoContext(photo: {ai_description, caption, ai_extraction}, index) → string (pure)"
  key_links:
    - from: "lib/services/generate-estimate.ts"
      to: "lib/estimate/photo-context.ts"
      via: "photoDescriptions map replaced by serializePhotoContext"
      pattern: "serializePhotoContext"
---

<objective>
The prompt half of v4.20 (PEXT-02): typed extraction data reaches the estimator. Builds on 171-01's schema (imports the PhotoExtraction type only — parallel-safe with 171-02, which owns the providers/worker; this plan owns generate-estimate.ts's prompt-assembly region + a new pure module. FILE-DISJOINT from 171-02 by design).

REQUIRED READING: .planning/REQUIREMENTS.md locked decisions. Current state to build on: generate-estimate.ts's photoDescriptions builder (prompt-assembly region, ~:211-218 post-168-02 — folds caption as 'Photo N (caption: X): desc'); getProjectPhotos does select('*') so ai_extraction arrives automatically once 171-01's column exists; prompt-builder.ts:155-162 wraps each element in <photo_description> + sanitizeField (the whole serialized string is transitively hardened — the 168-02-verified path).

SCOPE FENCE: do NOT touch lib/inngest/functions/analyze-photos.ts, lib/ai/openrouter-client.ts, or gemini.ts (171-02's territory, running in parallel). Do NOT touch prompt-builder.ts (the existing wrapping is the contract). Only the prompt-assembly region of generate-estimate.ts (the 166-02 post-AI region and the RFALL-01 seam are off-limits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/171-structured-photo-extraction/171-01-SUMMARY.md
@lib/ai/photo-extraction-schema.ts
@lib/services/generate-estimate.ts
@lib/ai/prompt-builder.ts

<interfaces>
Serialization format (compact, deterministic, no trailing separators):
```
Photo 2 (caption: north wall): Tiled bathroom floor with cracked grout...
 | Measurements: floor area 220 sq ft (estimated); baseboard length 60 linear ft (stated)
 | Materials: ceramic tile, grout
 | Damage: cracked grout near tub (moderate)
```
(one string, ' | ' separators, sections omitted when their array is empty). Measurements render as `${subject} ${dimension === 'count' ? 'count' : ''} ${value} ${unit} (${confidence})` — executor refines the exact phrasing for readability, but subject+value+unit+confidence MUST all appear. trade_signals/access_notes: include compactly when present (`| Trade signals: …`, `| Access: …`). Validate the stored JSONB through photoExtractionSchema.safeParse before serializing (defense against a future writer — invalid stored blob → serialize as prose-only, never throw).
TYPE NOTE (Opus check — tsc gate): getProjectPhotos returns the hand-written `Photo` interface (lib/queries/photo.ts:3-12) which does NOT gain ai_extraction (171-01 only touches database.types.ts). The serializer's param MUST type ai_extraction as OPTIONAL (`ai_extraction?: unknown`) so `Photo` remains assignable and tsc passes — the runtime value still arrives via select('*'), and the safeParse guard handles the unknown. Do NOT add lib/queries/photo.ts to scope. Also: 171-03 imports BOTH the PhotoExtraction type AND the runtime photoExtractionSchema (the objective's "type only" wording was imprecise — corrected here).
Serializer output is RAW (no escaping) — sanitizeField escapes exactly once downstream at prompt-builder.ts:159; the with-extraction end-to-end test lives in photo-context.test.ts / a NEW service case, NEVER by modifying generate-estimate-captions.test.ts (its assertions lock the null-extraction byte-identity).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure serializer (TDD) + wire into photoDescriptions</name>
  <files>lib/estimate/photo-context.ts, lib/services/generate-estimate.ts, tests/unit/estimate/photo-context.test.ts</files>
  <read_first>
    - lib/services/generate-estimate.ts prompt-assembly region (the 168-02 caption fold — replicate its exact prose format for the null-extraction case)
    - tests/unit/services/generate-estimate-captions.test.ts (the byte-identical contract this must keep green UNMODIFIED)
    - lib/ai/photo-extraction-schema.ts
  </read_first>
  <action>
1. TDD serializePhotoContext: (a) null extraction + caption → byte-identical to today's 'Photo N (caption: X): desc'; (b) null extraction, no caption → 'Photo N: desc' byte-identical; (c) full extraction → all sections in order, no dangling separators; (d) empty arrays omitted; (e) measurements phrasing carries subject/value/unit/confidence; (f) invalid stored JSONB → prose-only output (safeParse guard), no throw; (g) injection-y content in extraction fields survives INTO the string (sanitization happens downstream in prompt-builder — assert the serializer does NOT double-escape).
2. Replace the photoDescriptions map body in generate-estimate.ts with serializePhotoContext(p, i). The filter (non-empty ai_description) stays unchanged.
3. Keep tests/unit/services/generate-estimate-captions.test.ts GREEN UNMODIFIED (it locks the prose-only byte-identity), plus one new service-level case: a photo WITH extraction → the built user content contains the Measurements block inside <photo_description> (sanitized — assert escaped angle brackets end-to-end via buildUserContent).
  </action>
  <acceptance_criteria>
    - Serializer tests GREEN (7 cases); generate-estimate-captions.test.ts GREEN UNMODIFIED
    - End-to-end sanitization case GREEN (extraction content passes through sanitizeField exactly once)
    - `npm test` GREEN (or targeted + regression green + documented load-flake); `npx tsc --noEmit -p tsconfig.ci.json` exits 0
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/estimate/photo-context.test.ts tests/unit/services/generate-estimate-captions.test.ts</automated></verify>
  <done>Typed measurements reach the estimator; prose photos untouched.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/unit/estimate/photo-context.test.ts tests/unit/services/generate-estimate-captions.test.ts
npx tsc --noEmit -p tsconfig.ci.json
npm test
```
Regression contracts: 168-02 caption byte-identity; prompt-builder untouched; 166-02 region + RFALL-01 seam untouched; 171-02's files untouched (parallel executor).
</verification>

<success_criteria>
- PEXT-02 TRUE: extracted photos contribute a compact structured block through the hardened prompt path; prose-only photos byte-identical.
</success_criteria>

<output>
Create `.planning/phases/171-structured-photo-extraction/171-03-SUMMARY.md`: the serialization format, the safeParse guard, and the byte-identity evidence. On completion of BOTH 171-02 and 171-03, whichever finishes last marks PEXT-01..05 complete in REQUIREMENTS.md (Phase 171 complete → milestone v4.20 shipped).
</output>
