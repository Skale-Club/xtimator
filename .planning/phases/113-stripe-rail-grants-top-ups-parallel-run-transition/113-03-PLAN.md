---
phase: 113-stripe-rail-grants-top-ups-parallel-run-transition
plan: 03
type: execute
wave: 1
depends_on: ["113-01"]
files_modified:
  - app/api/billing/create-topup-session/route.ts
  - lib/billing/overage-affordance.ts
  - app/api/generate-estimate/route.ts
autonomous: true
requirements: [TOPUP-02, TOPUP-03, MIG-01]
must_haves:
  truths:
    - "A company can POST a packIndex to /api/billing/create-topup-session and receive a Stripe Checkout URL for a one-time mode:'payment' session"
    - "The session's price + credits come 100% from getBillingConfig().topUpPacks (inline price_data, no pre-created Stripe Price), and metadata carries type='credit_topup', companyId, credits (string)"
    - "The pack credits/price are looked up server-side from the packIndex — never trusted from the request body"
    - "When credit balance falls short, the over-quota response is enriched with a topUpUrl affordance, but generation is NOT blocked (enforcement off)"
  artifacts:
    - path: "app/api/billing/create-topup-session/route.ts"
      provides: "one-time top-up checkout (TOPUP-02 route side)"
      exports: ["POST"]
      contains: "mode: 'payment'"
    - path: "lib/billing/overage-affordance.ts"
      provides: "buildOverageAffordance pure helper (TOPUP-03 path)"
      exports: ["buildOverageAffordance"]
  key_links:
    - from: "app/api/billing/create-topup-session/route.ts"
      to: "lib/billing/billing-config.ts getBillingConfig().topUpPacks"
      via: "server-side pack lookup by index → inline price_data + metadata"
      pattern: "topUpPacks"
    - from: "app/api/generate-estimate/route.ts (402 response)"
      to: "lib/billing/overage-affordance.ts buildOverageAffordance"
      via: "enrich the over-quota JSON with topUpUrl (informational, no block)"
      pattern: "topUpUrl"
---

<objective>
Two additive features that flip the remaining 113-01 RED tests to GREEN, with no file overlap with plan 113-02 (runs parallel in Wave 1):
  1. TOPUP-02 (route side): a NEW one-time top-up checkout route mirroring the subscription checkout with mode:'payment' + inline price_data driven by billing_config.topUpPacks + metadata.type='credit_topup'.
  2. TOPUP-03: a thin overage AFFORDANCE — a pure buildOverageAffordance helper + enrich the existing generate-estimate over-quota response with topUpUrl. Path only — generation is NOT blocked (enforcement off). The full balance widget is Phase 115.

MIG-01: this plan adds the credit-path affordance ALONGSIDE the count path; the existing checkQuota 402 gate is preserved unchanged.

Purpose: owners can buy credits, and an over-balance state surfaces a top-up path without a hard block.
Output: new top-up route, new affordance helper, enriched 402 response.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-stripe-rail-grants-top-ups-parallel-run-transition/113-RESEARCH.md

<interfaces>
Mirror target — app/api/billing/create-checkout-session/route.ts (the subscription route):
```typescript
// auth via supabase.auth.getClaims(); demoGuardResponse() gate; company lookup by user_id;
// stripe.checkout.sessions.create({ mode:'subscription', line_items:[{price, quantity}], customer, success_url, cancel_url, metadata })
```

From lib/billing/billing-config.ts (call as-is):
```typescript
export function getBillingConfig(): Promise<BillingConfig>
// cfg.topUpPacks: { credits: number; priceCents: number }[]  // default [{1000,1500},{5000,6000}]
```

From lib/billing/credit-ledger.ts (call as-is):
```typescript
export function checkCredits(supabase, companyId, estimatedCredits?): Promise<{ allowed: boolean; balance: number; shortfall: number }>
// enforcementEnabled false this milestone → allowed ALWAYS true; still reports shortfall
```

Existing over-quota shape — app/api/generate-estimate/route.ts (~lines 78-86):
```typescript
const { allowed } = await checkQuota(supabase, companyId, 'estimate')
if (!allowed) {
  return NextResponse.json({ error: 'plan_limit_reached', upgradeUrl: '/settings/billing' }, { status: 402 })
}
```

Affordance contract the 113-01 RED test expects:
```typescript
export function buildOverageAffordance(check: { allowed: boolean; balance: number; shortfall: number }): { topUpUrl: string; upgradeUrl: string } | null
// shortfall>0 → { topUpUrl:'/settings/billing?topup=1', upgradeUrl:'/settings/billing' }; shortfall===0 → null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TOPUP-02 route — create-topup-session (mode:'payment', inline price_data from topUpPacks)</name>
  <read_first>
    - app/api/billing/create-checkout-session/route.ts (the FULL file — mirror auth, demoGuardResponse, company lookup, sessions.create shape; change only mode + line_items + metadata)
    - lib/billing/billing-config.ts lines 22, 62-65 (TopUpPack type + topUpPacks default)
    - tests/unit/billing/topup-checkout.test.ts (from 113-01 — the exact session shape it asserts)
    - 113-RESEARCH.md "Top-Up Checkout (TOPUP-02)" route snippet + Pitfall 4 (never trust client-supplied credits/price)
  </read_first>
  <files>app/api/billing/create-topup-session/route.ts</files>
  <behavior>
    - Test 1: valid packIndex=1 → returns { url }; sessions.create called with mode:'payment', line_items[0].price_data.unit_amount===6000, currency==='usd', metadata { type:'credit_topup', companyId:'co_1', credits:'5000' } (credits is the STRING form), NO `price` field on line_items.
    - Test 2: out-of-range packIndex (99) → 400, sessions.create NOT called.
    - Test 3: unauthenticated (claims null) → 401.
  </behavior>
  <action>
    Create app/api/billing/create-topup-session/route.ts as a near-copy of create-checkout-session/route.ts. Keep IDENTICAL: the createClient + auth.getClaims unauthorized 401 guard, the demoGuardResponse() block, the company lookup `from('companies').select('id, stripe_customer_id').eq('user_id', claims.sub).single()` and its "Company not found" 400, and `const stripe = await getStripeClient()`.

    Change the body parsing + session args:
      const body = await request.json().catch(() => null) as { packIndex?: number } | null
      const packIndex = Number(body?.packIndex)

      const cfg = await getBillingConfig()
      const pack = Number.isInteger(packIndex) ? cfg.topUpPacks[packIndex] : undefined
      if (!pack) {
        return NextResponse.json({ error: 'Invalid pack' }, { status: 400 })
      }

      const session = await stripe.checkout.sessions.create({
        mode: 'payment',
        line_items: [{
          quantity: 1,
          price_data: {
            currency: 'usd',
            unit_amount: pack.priceCents,        // server-side — never from the body (Pitfall 4)
            product_data: { name: `${pack.credits} Xtimator credits` },
          },
        }],
        customer: company.stripe_customer_id ?? undefined,
        success_url: `${process.env.NEXT_PUBLIC_APP_URL}/settings/billing?topup=1`,
        cancel_url: `${process.env.NEXT_PUBLIC_APP_URL}/settings/billing?topup=cancelled`,
        metadata: {
          type: 'credit_topup',
          companyId: company.id,
          credits: String(pack.credits),         // metadata values are strings
        },
      })
      return NextResponse.json({ url: session.url })

    Add the import: import { getBillingConfig } from '@/lib/billing/billing-config'. Keep all other imports identical to the subscription route. Use inline price_data ONLY — do NOT create or reference a pre-created Stripe Price (breaks the billing_config mandate, Pitfall: pre-created Prices).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/topup-checkout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "mode: 'payment'" app/api/billing/create-topup-session/route.ts` succeeds
    - `grep -q "price_data" app/api/billing/create-topup-session/route.ts` succeeds
    - `grep -q "type: 'credit_topup'" app/api/billing/create-topup-session/route.ts` succeeds
    - `grep -q "String(pack.credits)" app/api/billing/create-topup-session/route.ts` succeeds
    - `grep -q "topUpPacks" app/api/billing/create-topup-session/route.ts` succeeds
    - `grep -qv "price:" app/api/billing/create-topup-session/route.ts` (no pre-created Price id on line_items) — verify line_items uses price_data not `price`
    - All 3 tests in tests/unit/billing/topup-checkout.test.ts pass
  </acceptance_criteria>
  <done>The top-up route returns a mode:'payment' Checkout URL with inline price_data + credit_topup metadata sourced entirely from billing_config; packIndex validated server-side; all topup-checkout tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TOPUP-03 — buildOverageAffordance helper + enrich the generate-estimate over-quota response</name>
  <read_first>
    - tests/unit/billing/overage-affordance.test.ts (from 113-01 — the buildOverageAffordance contract)
    - app/api/generate-estimate/route.ts lines 78-86 (the existing checkQuota 402 shape to enrich — DO NOT change its gating semantics)
    - lib/billing/credit-ledger.ts lines 185-204 (checkCredits — allowed always true while enforcement off)
    - 113-RESEARCH.md "Overage Affordance (TOPUP-03)" + the MIG-01 safety rule (do not add a code path that can block)
  </read_first>
  <files>lib/billing/overage-affordance.ts, app/api/generate-estimate/route.ts</files>
  <behavior>
    - Test 1: buildOverageAffordance({ allowed:true, balance:0, shortfall:500 }) → { topUpUrl:'/settings/billing?topup=1', upgradeUrl:'/settings/billing' }.
    - Test 2: buildOverageAffordance({ allowed:true, balance:9000, shortfall:0 }) → null.
    - Test 3: allowed:true with shortfall>0 still yields an affordance (no block) — documents the enforcement-off path.
  </behavior>
  <action>
    Create lib/billing/overage-affordance.ts exporting a PURE function:

      export function buildOverageAffordance(
        check: { allowed: boolean; balance: number; shortfall: number }
      ): { topUpUrl: string; upgradeUrl: string } | null {
        if (check.shortfall > 0) {
          return { topUpUrl: '/settings/billing?topup=1', upgradeUrl: '/settings/billing' }
        }
        return null
      }

    No imports, no side effects — a pure mapping so it is trivially testable and reusable by the Phase-115 widget.

    Then ENRICH the existing generate-estimate over-quota response (do NOT add a new block, do NOT change the checkQuota gate). In app/api/generate-estimate/route.ts, the existing 402 returns `{ error: 'plan_limit_reached', upgradeUrl: '/settings/billing' }`. Add the credit affordance to THAT response only:
      - import { checkCredits } from '@/lib/billing/credit-ledger'
      - import { buildOverageAffordance } from '@/lib/billing/overage-affordance'
      - Inside the existing `if (!allowed) { ... }` block (the count-based 402), compute the credit affordance and merge it:
          const credit = await checkCredits(supabase, companyId)
          const affordance = buildOverageAffordance(credit)
          return NextResponse.json(
            { error: 'plan_limit_reached', upgradeUrl: '/settings/billing', ...(affordance ? { topUpUrl: affordance.topUpUrl } : {}) },
            { status: 402 }
          )
    CRITICAL (MIG-01): do NOT change the `checkQuota` call or the condition that triggers the 402. The credit path is purely additive — it only adds a `topUpUrl` field to the SAME response the count path already returns. checkCredits is enforcement-off (allowed always true), so it never causes a new block; you are only reading shortfall for the affordance. Do NOT introduce any `if (!credit.allowed) return ...` path.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing/overage-affordance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function buildOverageAffordance" lib/billing/overage-affordance.ts` succeeds
    - `grep -q "/settings/billing?topup=1" lib/billing/overage-affordance.ts` succeeds
    - `grep -q "buildOverageAffordance" app/api/generate-estimate/route.ts` succeeds
    - `grep -q "topUpUrl" app/api/generate-estimate/route.ts` succeeds
    - The generate-estimate route does NOT add a credit-based block: `grep -q "credit.allowed" app/api/generate-estimate/route.ts` returns NOTHING (no new gating path), and the original `checkQuota(supabase, companyId, 'estimate')` call is still present unchanged
    - All tests in tests/unit/billing/overage-affordance.test.ts pass
  </acceptance_criteria>
  <done>buildOverageAffordance maps a shortfall to a top-up path; the existing 402 over-quota response is enriched with topUpUrl without changing the count-based gate or adding any credit-based block.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/billing/topup-checkout.test.ts tests/unit/billing/overage-affordance.test.ts` — fully GREEN.
- `npx vitest run tests/unit/quota.test.ts tests/unit/quota-price-research.test.ts` — still GREEN (MIG-01: count gate unchanged).
- `npx tsc --noEmit` — no new type errors.
- Manual grep guard: the generate-estimate route still calls checkQuota and contains no `credit.allowed`-based return (additive-only proof).
</verification>

<success_criteria>
- A working one-time top-up checkout route driven entirely by billing_config.topUpPacks (no pre-created Prices, server-side pack validation).
- A pure overage-affordance helper + an enriched (not blocking) 402 response surfacing a top-up path.
- The count-based quota gate is unchanged (MIG-01 parallel-run safety); all 113-01 route/affordance RED tests are GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/113-stripe-rail-grants-top-ups-parallel-run-transition/113-03-SUMMARY.md`
</output>
