---
phase: 113-stripe-rail-grants-top-ups-parallel-run-transition
plan: 02
type: execute
wave: 1
depends_on: ["113-01"]
files_modified:
  - app/api/webhooks/stripe/route.ts
autonomous: true
requirements: [TOPUP-01, TOPUP-02, MIG-01]
must_haves:
  truths:
    - "On invoice.paid for a subscription, the company is granted tiers[tier].monthlyCreditGrant credits idempotently keyed on event.id"
    - "A redelivered invoice.paid never double-grants (processed_stripe_events 23505 short-circuit + grantCredits idempotencyKey second layer)"
    - "A checkout.session.completed with metadata.type='credit_topup' and payment_status='paid' grants the pack credits as a topup, parsed from the STRING metadata"
    - "The top-up arm runs BEFORE the subscription-mode early-break, so a mode:'payment' session is not skipped"
    - "The count-based checkQuota/entitlements path in this route is unchanged (credits are additive only)"
  artifacts:
    - path: "app/api/webhooks/stripe/route.ts"
      provides: "invoice.paid grant + checkout.session.completed top-up arm"
      contains: "grantCredits"
  key_links:
    - from: "app/api/webhooks/stripe/route.ts (invoice.paid branch)"
      to: "lib/billing/credit-ledger.ts grantCredits"
      via: "grant after resolving company+tier by stripe_subscription_id, idempotencyKey=event.id"
      pattern: "grantCredits\\("
    - from: "app/api/webhooks/stripe/route.ts (checkout.session.completed branch)"
      to: "lib/billing/credit-ledger.ts grantCredits"
      via: "top-up arm before the mode!=='subscription' early-break, reason:'topup'"
      pattern: "credit_topup"
---

<objective>
Wire the Phase-112 credit ledger into the existing Stripe webhook. Two additive changes to ONE file (app/api/webhooks/stripe/route.ts):
  1. TOPUP-01: in the existing invoice.paid branch, after the tier_renews_at update, resolve company+tier by stripe_subscription_id and grant tiers[tier].monthlyCreditGrant credits, idempotent on event.id.
  2. TOPUP-02 (webhook side): in checkout.session.completed, add a top-up arm BEFORE the existing `session.mode !== 'subscription'` early-break that grants pack credits when metadata.type==='credit_topup'.

This flips the RED webhook tests from 113-01 to GREEN. The count-based path (MIG-01) stays byte-for-byte untouched.

Purpose: subscriptions and one-time top-ups now credit the OUR ledger via Stripe — the rail is live.
Output: the modified webhook route + green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-stripe-rail-grants-top-ups-parallel-run-transition/113-RESEARCH.md

<interfaces>
From lib/billing/credit-ledger.ts (call as-is — DO NOT modify):
```typescript
export function grantCredits(input: {
  companyId: string
  credits: number
  reason: 'grant' | 'topup'
  refId?: string | null
  idempotencyKey?: string | null
}): Promise<void>  // best-effort, never throws, no-ops on credits<=0, dedups by idempotencyKey
```

From lib/billing/billing-config.ts (call as-is — DO NOT modify):
```typescript
export function getBillingConfig(): Promise<BillingConfig>
// cfg.tiers[tier].monthlyCreditGrant — runtime-authoritative grant amount (free=0)
```

Existing webhook structure (app/api/webhooks/stripe/route.ts) you are editing:
- handlePlatformEvent switch with cases: 'checkout.session.completed' (lines ~110-135), 'invoice.paid' (lines ~137-160).
- checkout.session.completed currently early-`break`s at line 113: `if (!companyId || session.mode !== 'subscription') break`.
- invoice.paid resolves `subId`, retrieves the subscription, updates companies.tier_renews_at filtered by stripe_subscription_id.
- `svc` is the requireServiceClient() instance in scope inside handlePlatformEvent.
- `event` is in scope (event.id is the idempotency key).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TOPUP-01 — grant tier credits in the invoice.paid branch (idempotent on event.id)</name>
  <read_first>
    - app/api/webhooks/stripe/route.ts lines 137-160 (the invoice.paid branch — the exact insertion point is AFTER the existing tier_renews_at update)
    - lib/billing/credit-ledger.ts lines 123-176 (grantCredits behavior: no-ops on <=0, dedups by idempotencyKey)
    - lib/billing/billing-config.ts lines 50-70 (DEFAULT_BILLING_CONFIG.tiers shape — free.monthlyCreditGrant===0)
    - 113-RESEARCH.md "invoice.paid Grant (TOPUP-01)" section (the load-bearing snippet)
  </read_first>
  <files>app/api/webhooks/stripe/route.ts</files>
  <action>
    At the top of the file, add two imports:
      import { grantCredits } from '@/lib/billing/credit-ledger'
      import { getBillingConfig } from '@/lib/billing/billing-config'

    Inside the `case 'invoice.paid':` branch, AFTER the existing `companies.update({ tier_renews_at }).eq('stripe_subscription_id', subId)` call and its error log (i.e., just before the `break`), insert:

      // TOPUP-01: grant the tier's monthly credit allowance on every paid invoice
      // (covers first-subscribe AND renewal — a single hook for both). Idempotent
      // on event.id so a redelivered webhook never double-grants. Grant ONLY here
      // (NOT in checkout.session.completed) — Pitfall 2 double-grant.
      const { data: grantCompany } = await svc
        .from('companies')
        .select('id, tier')
        .eq('stripe_subscription_id', subId)
        .maybeSingle()

      if (grantCompany?.id) {
        const cfg = await getBillingConfig()
        const tierKey = (grantCompany.tier ?? 'free') as keyof typeof cfg.tiers
        const grant = cfg.tiers[tierKey]?.monthlyCreditGrant ?? 0
        await grantCredits({
          companyId: grantCompany.id,
          credits: grant,        // grantCredits no-ops on <=0 (free tier = 0)
          reason: 'grant',
          refId: invoice.id,
          idempotencyKey: event.id,  // redelivered webhook → guaranteed no double-grant
        })
      }

    Notes:
    - Read the grant amount ONLY from getBillingConfig() (runtime-authoritative, BILLCFG-03) — never hard-code, never read entitlements.ts.
    - Resolve tier from companies.tier via stripe_subscription_id (NOT from metadata.plan — that field is absent on invoice.paid; Pitfall 6).
    - Do NOT change the existing tier_renews_at update, the subId resolution, or the subscriptions.retrieve cast. Additive only.
    - The webhook MUST still return 200 even if the grant is a no-op — grantCredits never throws (Anti-pattern: non-200 on grant failure).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/webhooks/stripe-credit-grant.test.ts -t "invoice.paid"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { grantCredits }" app/api/webhooks/stripe/route.ts` succeeds
    - `grep -q "import { getBillingConfig }" app/api/webhooks/stripe/route.ts` succeeds
    - `grep -q "idempotencyKey: event.id" app/api/webhooks/stripe/route.ts` succeeds
    - `grep -q "reason: 'grant'" app/api/webhooks/stripe/route.ts` succeeds
    - `grep -q "monthlyCreditGrant" app/api/webhooks/stripe/route.ts` succeeds
    - The 113-01 invoice.paid grant tests pass (the grant-on-pro-tier, free-tier-zero, and redelivery-no-double-grant cases are GREEN)
  </acceptance_criteria>
  <done>invoice.paid grants tiers[tier].monthlyCreditGrant via grantCredits keyed on event.id; free tier grants 0; redelivery does not double-grant; the three TOPUP-01 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: TOPUP-02 (webhook) — top-up arm in checkout.session.completed BEFORE the subscription early-break</name>
  <read_first>
    - app/api/webhooks/stripe/route.ts lines 110-135 (the checkout.session.completed branch; line 113 is the `session.mode !== 'subscription'` early-break the arm must precede)
    - 113-RESEARCH.md "Top-Up Checkout (TOPUP-02)" — "Webhook sub-branch" snippet + Pitfall 3 (arm placement) + Pitfall 5 (string metadata)
  </read_first>
  <files>app/api/webhooks/stripe/route.ts</files>
  <action>
    Inside `case 'checkout.session.completed':`, AT THE TOP of the branch — immediately after `const session = event.data.object as Stripe.Checkout.Session` and BEFORE the existing `const companyId = session.metadata?.companyId` / `if (!companyId || session.mode !== 'subscription') break` guard — insert the top-up arm:

      // TOPUP-02: one-time credit top-up (mode:'payment'). Handle BEFORE the
      // subscription-mode early-break below, or this arm never runs (Pitfall 3).
      // metadata values are STRINGS — parse credits (Pitfall 5). Idempotent on event.id.
      if (session.metadata?.type === 'credit_topup' && session.payment_status === 'paid') {
        const topupCompanyId = session.metadata.companyId
        const credits = Number(session.metadata.credits)
        if (topupCompanyId && credits > 0) {
          await grantCredits({
            companyId: topupCompanyId,
            credits,
            reason: 'topup',
            refId: session.id,
            idempotencyKey: event.id,
          })
        }
        break
      }

    Leave the existing subscription handling (companyId guard, mode check, tier update, dispatchXphereSync) completely intact below the arm. The arm's `break` ensures a top-up session never falls through into the subscription update path.

    Do NOT grant subscriptions here — subscriptions grant only in invoice.paid (Task 1). This arm is exclusively for `metadata.type === 'credit_topup'`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/webhooks/stripe-credit-grant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "credit_topup" app/api/webhooks/stripe/route.ts` succeeds
    - `grep -q "reason: 'topup'" app/api/webhooks/stripe/route.ts` succeeds
    - `grep -q "Number(session.metadata.credits)" app/api/webhooks/stripe/route.ts` succeeds
    - The `credit_topup` check appears in the file BEFORE the `session.mode !== 'subscription'` string (placement guard): `awk '/credit_topup/{a=NR} /mode !== .subscription./{b=NR} END{exit !(a<b)}' app/api/webhooks/stripe/route.ts` succeeds
    - ALL 113-01 webhook tests pass (both TOPUP-01 invoice.paid cases AND the TOPUP-02 top-up-arm cases, including "runs BEFORE the subscription guard" and "subscription checkout does NOT grant")
    - The MIG-01 guard test passes (route includes 'grantCredits', does not add 'maxEstimatesPerMonth')
  </acceptance_criteria>
  <done>checkout.session.completed grants top-up credits for credit_topup sessions before the subscription early-break, parses the string credits, and is idempotent on event.id; the full 113-01 webhook suite is green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/webhooks/stripe-credit-grant.test.ts` — fully GREEN (all TOPUP-01 + TOPUP-02 webhook cases + MIG-01 guard).
- `npx vitest run tests/unit/billing/stripe-webhook.test.ts` — still GREEN (existing webhook regression — subscription/deleted/payment_failed paths unchanged).
- `npx vitest run tests/unit/quota.test.ts tests/unit/quota-price-research.test.ts` — still GREEN (MIG-01: count path untouched).
- `npx tsc --noEmit` — no new type errors in the edited route.
</verification>

<success_criteria>
- Subscriptions grant tier credits on invoice.paid, idempotent on event.id; top-ups grant on checkout.session.completed via the pre-guard arm; both read grant/pack values at runtime; the count-based path is unchanged.
- All RED webhook tests from 113-01 are now GREEN; the existing webhook + quota suites stay green (zero regression).
</success_criteria>

<output>
After completion, create `.planning/phases/113-stripe-rail-grants-top-ups-parallel-run-transition/113-02-SUMMARY.md`
</output>
